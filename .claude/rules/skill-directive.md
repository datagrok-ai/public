Before creating or adding any package artifact (viewer, app, filter, cell renderer, info panel, file viewer, tests, etc.), check `.claude/skills/` for a matching skill and follow it.

When building UI components (viewers, file viewers, dialogs, layouts), use the `/ui` skill for authoritative guidelines.
