# Component registry

Named viewers, dialogs, grids, and views worth pointing skills at, with a
canonical reference package for each.

## Routing-related views

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Top-level shell view setter | `grok.shell.v` (setter on `Shell`) | Used in any app to switch the active view after building it. | js-api/src/shell.ts:71 |
| Generic view base | `DG.ViewBase` (extended by `DG.View`, `DG.TableView`, etc.) | Override `acceptsPath` / `handlePath` in subclasses to participate in URL dispatch. | js-api/src/views/view.ts:173-178 |
| TableView added from a DataFrame | `DG.TableView` via `grok.shell.addTableView(df)` | Tutorial in `routing.md` — adds two TableViews and sets `path` per view. | js-api/src/shell.ts:258 |
| Scatter plot viewer | `DG.VIEWER.SCATTER_PLOT` (string enum value) added with `view.addViewer(...)` | `addViewer(DG.VIEWER.SCATTER_PLOT)` — preferred over deprecated `view.scatterPlot()`. | js-api/src/views/view.ts:559-564, js-api/src/const.ts (VIEWER enum) |

## Routing-related package metadata

| Component | Where | Reference example |
|---|---|---|
| Package URL segment override | `package.json` → top-level `"meta": { "url": "/segment" }` | `packages/UsageAnalysis/package.json` (`"url": "/usage"`); `packages/Compute2/package.json` (`"url": "/Modelhub"`) |
| App function with URL alias | `package.ts` function header `//meta.url: /alias` + `//meta.role: app` | `packages/NodeJSDemo/src/package.ts:17-19` (`//meta.url: /todo`) |
| App with URL parameter binding (decorator form) | `@grok.decorators.param({ options: { metaUrl: true, optional: true } }) path?: string` | `packages/Flow/src/package.ts:22`, `packages/Bio/src/package.ts:1366`, `packages/MolTrack/src/package.ts:49`, `packages/UsageAnalysis/src/package.ts:105`, `packages/Docking/src/package.ts:321` |
| App with URL parameter binding (header form) | `//input: string path { meta.url: true; optional: true }` | `packages/Bio/src/package.g.ts:642`, `packages/MolTrack/src/package.g.ts:15`, `packages/Docking/src/package.g.ts:95`, `packages/CddVaultLink/src/package.g.ts:5` |

## User settings storage

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| User settings store (current API) | `UserSettingsStorage` exposed as `grok.userSettings` (singleton). Synchronous, server-synced every 10s. | Six methods: `add`, `addAll`, `put`, `get`, `getValue`, `delete`. Default `isPrivate=true`. | js-api/src/user_settings_storage.ts:12-75, js-api/grok.ts:29 |
| Legacy user data store (DEPRECATED) | `UserDataStorage` exposed as `grok.dapi.userDataStorage`. Async (returns Promises). | All methods carry `@deprecated The UserDataStorage should not be used. Use UserSettingsStorage instead`. | js-api/src/dapi.ts:168-172, js-api/src/dapi.ts:711-770 |
| Reference: chunked storage for >5000-char values | Pattern in `ClinicalCase/src/utils/layout-utils.ts` — splits payload into N parts and stores a `{parts: N}` metadata key. | Read path reads metadata, then loops `getValue` for each part and concatenates. | packages/ClinicalCase/src/utils/layout-utils.ts:20-67 |
| Reference: shared storage (`isPrivate=false`) | Pattern in `SequenceTranslator` pattern-app — stores hashes of named patterns shared across users. | All `add`/`get`/`delete` calls pass `false` as the trailing arg. | packages/SequenceTranslator/src/apps/pattern/model/data-manager.ts:84,184,222,240,268 |
| Reference: replace-whole-record (`put`) | Pattern in `Grokky/usage-limiter` — atomic replace of the entire daily-usage object. | `grok.userSettings.put(SETTINGS_NAME, { date, count, ... })`. | packages/Grokky/src/ai/usage-limiter.ts:45-58 |
| Reference: per-user form state with JSON serialization | Pattern in `CddVaultLink/search-function-editor` — saves last-search payload per `vaultId` subkey. | `add(LAST_SEARCH_KEY, String(this.vaultId), JSON.stringify(payload))` + `JSON.parse(getValue(...))`. | packages/CddVaultLink/src/search-function-editor.ts:114,153,177 |

## Access data

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Query / function dispatcher (typed) | `Data.query<T = DataFrame>(queryName, params?, adHoc?)` on `grok.data` | Returns `Promise<T>`; supports scalar or `DataFrame` via type parameter. | js-api/src/data.ts:255-262 |
| Generic function caller | `Functions.call(name, params?)` on `grok.functions` | Returns `Promise<result>`; equivalent to typing `Package:Function()` in the console. | js-api/src/functions.ts (Functions class) |
| Expression evaluator | `Functions.eval(expr)` on `grok.functions` | Used for `OpenServerFile("USER:Home/data.csv")`-style file-share reads → `Promise<DataFrame[]>`. | js-api/src/datagrok/build/web/grok_shared.dart.js:85499 |
| Server-proxied fetch | `Dapi.fetchProxy(url, init?, maxAge?)` on `grok.dapi` | Same shape as `fetch`; routes via `${this.root}/connectors/proxy` to bypass CORS and enable caching. | js-api/src/dapi.ts:189-209 |
| Demo dataset library | `DemoDatasets` exposed as `grok.data.demo` | Typed shortcuts: `.biosensor()`, `.demog()`, `.molecules()`, `.wells()`, `.geo()`, `.randomWalk()`, `.doseResponse()`. Underlying call is `getDemoTable(name, rows, cols)`. | js-api/src/data.ts:30-101 |
| Demo file loader | `DemoDatasets.loadDemoTable(path)` (also surfaced as `grok.data.getDemoTable(path)` on `Data`) | Loads a file under the demo root, returns `Promise<DataFrame>`. | js-api/src/data.ts:98-100,157-159 |
| Synthetic dataset generator | `Data.testData(name, rows?, cols?)` on `grok.data` | Synchronous, returns `DataFrame`. `name: DemoDatasetName` (seven literal values). | js-api/src/data.ts:153-155, js-api/src/const.ts:765-773 |
| URL CSV loader | `Data.loadTable(csvUrl)` on `grok.data` | Returns `Promise<DataFrame>`. Compose with `_package.webRoot` for package assets. | js-api/src/data.ts:173-175 |
| In-memory CSV parser | `Data.parseCsv(csv, options?)` on `grok.data` | Returns `DataFrame` synchronously; `options: CsvImportOptions`. | js-api/src/data.ts:166-168 |
| Table-by-GUID opener | `Data.openTable(id)` on `grok.data` | `Promise<DataFrame>`. NOT the same as `grok.data.files.openTable(path)` which reads from a file path. | js-api/src/data.ts:222 |
| File-share helper class | `Files` exposed as `grok.dapi.files` | Methods: `readAsText`, `readAsBytes`, `writeAsText`, `exists`, `rename`, `move`, `delete`, `list`. Accept path string, `FileInfo`, or connection GUID. All async. | js-api/src/dapi.ts:1287-1290,1418-1465 |
| File metadata + bytes | `FileInfo` (extends `Entity`) | Properties: `path`, `fullPath`, `extension`, `fileName`, `url`, `isFile`, `isDirectory`, `updatedOn`. Methods: `readAsString()`, `readAsBytes()`, `save()`. | js-api/src/entities/table-info.ts:63-119 |
| Package webRoot URL prefix | `Package.webRoot` getter (on `_package` instance) | Returns string ready for concatenation: `${_package.webRoot}data-samples/test.csv`. | js-api/src/entities/misc.ts:79-84 |
| Reference: package with `connections/` JSON | `packages/Chembl/connections/chembl.json` (Postgres) and `packages/Samples/connections/access-northwind.json` (Access). | Demonstrates required (`parameters`, `dataSource`) and optional (`name`, `description`, `tags`, `credentials`) fields. | packages/Chembl/connections/chembl.json |
| Reference: SQL query file with one annotated query per block | `packages/Chembl/queries/queries.sql` | Pattern: `--name`, `--friendlyName`, `--description`, `--connection`, `--input`, body, `--end`. | packages/Chembl/queries/queries.sql:1-39 |
| Reference: query post-processing triple | `<basename>.sql` + `<basename>.js` (post-process) + `<basename>.layout` (saved view layout) under `queries/` | One query per file is enforced by the basename association. | help/develop/how-to/db/access-data.md:189-213 |
| Reference: file-share connection JSON | Connection with `dataSource: Files`, `parameters.dir`, `parameters.indexFiles`, optional `credentials.parameters.{login,password}`. | When `indexFiles: true`, an indexing data job is created on deploy. | help/develop/how-to/db/access-data.md:272-298 |
| Reference: `OpenServerFile` callsite | `packages/ApiSamples/scripts/scripting/grok-scripting.js:1` (`grok.functions.eval('OpenServerFile("System:DemoFiles/beer.csv")')`); `packages/ApiTests/src/functions/functions.ts:60-66` (eval and `call` forms side-by-side). | Use `eval` for inline literals; use `call('OpenServerFile', {fullPath})` when the path is built from variables. | packages/ApiSamples/scripts/scripting/grok-scripting.js:1, packages/ApiTests/src/functions/functions.ts:60-66 |

## Database in Docker container

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| DockerContainer entity | `DG.DockerContainer` (extends `Entity`) | Returned from `grok.dapi.docker.dockerContainers.filter(name).first()`; not directly referenced by db-in-Docker connection JSON, but useful when packages also expose HTTP endpoints over the same container. | js-api/src/entities/misc.ts:219 |
| DockerContainers data source | `DockerContainersDataSource` exposed at `grok.dapi.docker.dockerContainers` | `.filter(name).first()` returns a Promise of the matching `DockerContainer`. Used by HTTP-style integrations (`fetchProxy`, `webSocketProxy`); DB connections route through the SQL connector instead. | js-api/src/dapi.ts:1045 |
| Reference: containerized Postgres connection JSON | `packages/DBTests/connections/postgres-test-docker.json` — server `${Dbtests:Dbtests<DockerContainer>}`, `db: "world"`, `dataSource: Postgres`, plain `credentials.parameters.{login,password}`. No `port`. | The connection name is `PostgresDocker`; queries reference it via `--connection: PostgresDocker`. | packages/DBTests/connections/postgres-test-docker.json |
| Reference: containerized Postgres Dockerfile | `packages/DBTests/dockerfiles/Dockerfile` — `FROM postgres`, `ENV POSTGRES_*`, `COPY world.sql /docker-entrypoint-initdb.d/`, `EXPOSE 5432`, `HEALTHCHECK pg_isready`. | The single canonical example. Article shows a stripped version without COPY/HEALTHCHECK. | packages/DBTests/dockerfiles/Dockerfile |
| Reference: container.json for an on-demand DB | `packages/DBTests/dockerfiles/container.json` — `{"on_demand": true, "shutdown_timeout": 30}`. | Lazy-start pattern — the DB only spins up on first query. Saves resources but adds first-query latency. | packages/DBTests/dockerfiles/container.json |
| Reference: alternate "no-package-prefix" placeholder form | `packages/SureChembl/connections/surechembl.json` — `${Surechembl<DockerContainer>}` (no `<package>:` prefix). Treat as a corner case; default to the documented two-segment form. | Confirms the platform tolerates the abbreviated form, but the article does not document it. | packages/SureChembl/connections/surechembl.json:6 |

## Docker container HTTP/WebSocket integrations

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Docker container entity | `DG.DockerContainer` (extends `Entity`) — exposes `.id`, `.status`, `.environmentVars`. | Returned from `dockerContainers.filter(name).first()`. Use `.id` for all proxy/run/stop/logs calls; check `.status` before `.run()`. | js-api/src/entities/misc.ts:219, packages/ApiTests/src/packages/docker.ts:8-25 |
| DockerContainers data source | `DockerContainersDataSource` exposed at `grok.dapi.docker.dockerContainers` — `extends HttpDataSource<DockerContainer>`. | Methods: `filter`, `list`, `run(id, awaitStart=false)`, `stop(id, awaitStop=false)`, `fetchProxy(id, path, params?)`, `webSocketProxy(id, path, timeout=60000)`, `webSocketProxySync(id, path, timeout=60000)`, `getContainerLogs(id, limit=10000)`. | js-api/src/dapi.ts:1045-1206 |
| Docker images data source | `DockerImagesDataSource` exposed at `grok.dapi.docker.dockerImages` — sibling of containers. Declares `revalidate(imageId): Promise<void>` and inherits `HttpDataSource` (filter/list); NO typed `getBuildLogs` method exists. | `(await dockerImages.filter(name).first()).logs` and `.status` are the runtime read accessors for image build state. The `Manage > Dockers` UI is built on these two DataSources. | js-api/src/dapi.ts:1024-1040 |
| Docker image entity | `DockerImage` (no TS class shipped — commented out at `js-api/src/entities/misc.ts:212-216`; instance arrives via the Dart bridge with `.status` and `.logs` properties). | Resolve via `dockerImages.filter('<friendlyName>').first()`. The `image.logs` string carries the FULL build log; `image.status === 'ready'` once the image is built. | js-api/src/entities/misc.ts:212-216, packages/ApiTests/src/packages/docker.ts:14-18 |
| Reference: minimal HTTP integration | `packages/Reinvent4/src/package.ts:51,61` — resolves container by `'reinvent'`, posts JSON to `/run_reinvent` via `fetchProxy`, reads JSON response. | Cleanest one-shot REST call from a package. | packages/Reinvent4/src/package.ts |
| Reference: typed service wrapper | `packages/MolTrack/src/services/moltrack-docker-service.ts` — class wraps a `Promise<DockerContainer>` and exposes typed `health()`, `getSchema()`, `submit*()` methods that all delegate to `fetchProxy`. | Pattern for "many endpoints on one container" — resolve once, reuse. | packages/MolTrack/src/services/moltrack-docker-service.ts |
| Reference: WebSocket via sync variant | `packages/Notebooks/src/package.js:446` — `webSocketProxySync(CONTAINER_ID, uri.pathname + uri.search)` for Jupyter kernel comms. | Sync form is necessary when the caller wraps an existing legacy WebSocket-using library that doesn't await. | packages/Notebooks/src/package.js |
| Reference: container.json shapes | `packages/Boltz1/dockerfiles/boltz/container.json` (`{cpu:4, memory:30721, on_demand:true, shutdown_timeout:30}`) — heavy GPU model. `packages/Grokky/dockerfiles/mcp-server/container.json` (`{cpu:0.5, memory:512, on_demand:true}`) — minimal sidecar. `packages/Admetica/dockerfiles/container.json` (`{gpu:1, cpu:1, memory:6144}`) — always-on GPU. | Three production examples spanning the resource spectrum. The article's example values (1.5 CPU, 2048 MB, etc.) are illustrative — match real workloads. | (multiple) |
| Reference: env-vars wiring an entity | `packages/MolTrack/dockerfiles/container.json` (`{"environmentVars": {"db": "#{x.moltrack}"}}`) — injects the JSON-serialized `MolTrack:moltrack` entity into the container's `db` env-var at start time. Same shape in `packages/NodeJSDemo/dockerfiles/todo-app/container.json`. | Canonical pattern for "container needs the package's connection credentials". | packages/MolTrack/dockerfiles/container.json |
| Reference: Dockerfile with single EXPOSE + ENTRYPOINT | `packages/Admetica/dockerfiles/Dockerfile:50,52-56` — `EXPOSE 8000` plus a Celery worker `ENTRYPOINT`. | Real-world reference Dockerfile linked from the article. | packages/Admetica/dockerfiles/Dockerfile |
| Reference: ApiTests smoke test for the proxy | `packages/ApiTests/src/packages/docker.ts:7-26` — three tests: `fetchProxy('/square?number=4')`, `dockerImages.filter(...).first()`-based build-logs assertion, `getContainerLogs(...)` assertion. | Use as a template when writing your own integration smoke tests. | packages/ApiTests/src/packages/docker.ts |

## Plugin Postgres database (db-in-plugin)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Plugin database directory layout | Directory tree `<Package>/databases/<Name>/NNNN_*.sql`. Auto-registered as connection `<Package>:<Name>` and schema `<Name>` in the platform Postgres. | No JS-API class — the contract is purely directory-shape + SQL conventions. | help/develop/how-to/db/db-in-plugin.md:14-16 |
| Reference: HitTriage as the canonical example | `packages/HitTriage/databases/hitdesign/` (init + dict migrations) wired to `packages/HitTriage/queries/locks.sql` and `vid.sql` via `--connection: HitTriage:hitdesign`. | Full end-to-end example: schema creation, GRANT-to-`:LOGIN` pattern, multiple queries, JS callers. | packages/HitTriage/databases/hitdesign, packages/HitTriage/queries/locks.sql |
| Reference: NodeJSDemo as minimal example | `packages/NodeJSDemo/databases/todo/000_init.sql` — single table, no GRANTs, no queries. The package interacts with the DB through a separate Node.js sidecar. | Minimal viable shape — useful when learning the directory layout without query overhead. | packages/NodeJSDemo/databases/todo/000_init.sql |
| Reference: large schema with multiple tables | `packages/Plates/databases/plts/0000_init.sql` (~190 lines, 6+ tables: properties, plate_types, templates, plates, wells, etc.) wired to `packages/Plates/queries/plates-crud.sql`. | Demonstrates schema-internal CHECK constraints, JSONB columns, FK chains, and the GRANT-to-`:LOGIN` pattern at file end. | packages/Plates/databases/plts/0000_init.sql, packages/Plates/queries/plates-crud.sql |
| Reference: incremental migrations | `packages/Biologics/databases/biologics/` — five files (`0000_init.sql`, `0001_init.sql`, `0002_peptides.sql`, `0003_regions_targets.sql`, `0004_seq_chains.sql`). | Demonstrates real forward-only migration progression; each later file ALTERs / ADD COLUMNs, never DROPs. | packages/Biologics/databases/biologics |
| Reference: `:LOGIN` GRANT pattern | Last 2-3 lines of every canonical `0000_init.sql` — `GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA <name> TO :LOGIN;` (and same for SEQUENCES). | Required for query-time access; omitting it produces "permission denied" at first query. | packages/MolTrack/databases/moltrack/0000.sql:277-279, packages/HitTriage/databases/hitdesign/0000_init.sql:18-20 |
| Reference: combining plugin DB + queries + JS | HitTriage flow — `databases/hitdesign/0000_init.sql` declares tables → `queries/locks.sql` reads/writes via `--connection: HitTriage:hitdesign` → JS calls `grok.data.query('HitTriage:acquireCampaignLock', {...})`. | The article example (`CompoundRegistrator`) is a stripped-down version of this flow. | packages/HitTriage/databases/hitdesign/0000_init.sql, packages/HitTriage/queries/locks.sql |

## Register identifiers (semantic-type detection + handlers)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Object handler base class | `DG.ObjectHandler<T>` | Subclass for each semantic type. Override `type`, `isApplicable`, plus any of `renderTooltip` / `renderCard` / `renderProperties` / `renderMarkup` / `renderIcon` / `renderView`. Register once via `DG.ObjectHandler.register(new MyHandler())`. | js-api/ui.ts:1651-1773 |
| Semantic-value wrapper | `DG.SemanticValue<T>` | Carries `value: T`, `semType: string`, and `units`. Use `x instanceof DG.SemanticValue && x.semType === '<TYPE>'` in `isApplicable`. Static `registerRegExpDetector(semType, regexp, description?)` registers a runtime detector. | js-api/src/grid.ts:1379-1380, js-api/src/grid.ts:1375-1377 |
| DB-explorer entry point class | `DBExplorer` from `@datagrok-libraries/db-explorer/src/db-explorer` | `DBExplorer.initFromConfig(config)` returns a `DBExplorer` (or `null`). Instance methods include `addEntryPoint`, `addJoinOptions`, `addCustomRenderer`, `addHeaderReplacers`, `addUniqueColumns`, `addCustomSelectedColumns`, `addEntryPointValueConverter`, `addCustomRelation`. | libraries/db-explorer/src/db-explorer.ts:10, libraries/db-explorer/src/db-explorer.ts:147-251 |
| Configuration shape | `DBExplorerConfig` from `@datagrok-libraries/db-explorer/src/types` | Required `connectionName`, `schemaName`, `entryPoints`. Optional `nqName`, `dataSourceName`, `joinOptions`, `headerNames`, `uniqueColumns`, `customSelectedColumns`, `customRenderers`, `explicitReferences`. Note: `entryPoints[].schama` is a typo and unused. | libraries/db-explorer/src/types.ts:52-75 |
| Built-in renderers (composable) | Functions from `@datagrok-libraries/db-explorer/src/renderer` | `moleculeRenderer(value)` — SMILES via `grok.chem.drawMolecule`. `textRenderer(value, withTooltip?)` — copy-on-context-menu. Plus `imageRenderer`, `rawImageRenderer`, `helmRenderer` (used by `getDefaultRendererByName`). | libraries/db-explorer/src/renderer.ts:50-59, libraries/db-explorer/src/renderer.ts:683-693 |
| Renderer-name resolver | `SupportedRenderer` type alias + `getDefaultRendererByName(name)` | `'molecule' \| 'helm' \| 'imageURL' \| 'rawImage'` → renderer function. The `customRenderers[]` entry's `renderer` field is exactly this string. | libraries/db-explorer/src/types.ts:25, libraries/db-explorer/src/renderer.ts:683-693 |
| Reference: basic-approach handler (no DB) | Article snippet `ChemblIdHandler extends DG.ObjectHandler` with manual `grok.functions.call('Chembl:chemblIdToSmiles', ...)` to query the DB inside `renderInnerCard`. | Demonstrates the inner-card pattern (`ui.divV` + `ui.h3` + `ui.wait` + `ui.bind` + `grok.chem.drawMolecule`) reused by `renderCard` and `renderTooltip`. | help/develop/how-to/db/register-identifiers.md:42-72 |
| Reference: declarative `meta.semanticTypes` package | `packages/Chemspace/package.json:67-77` (`semType: chemspace-id`, single regex parser). Also `packages/PowerPack/package.json` and `packages/monomerDB/package.json`. | Production-grade declarative approach — no autostart code needed for detection alone; only needed if a custom renderer is also being registered. | packages/Chemspace/package.json |
| Reference: end-to-end DB-explorer (Chembl) | `packages/Chembl/src/handlers.ts:7-19` (`DBExplorer.initFromConfig` + `addCustomRenderer` for SMILES). `packages/Chembl/src/explorer-config.ts:3-65` (full `DBExplorerConfig` with `entryPoints`, `joinOptions`, `headerNames`, `uniqueColumns`, `customSelectedColumns`, `customRenderers`). Wired in `packages/Chembl/src/package.ts:15-19` via `@grok.decorators.autostart()`. | The canonical full example. Article uses a stripped-down version of this exact code. | packages/Chembl/src/handlers.ts, packages/Chembl/src/explorer-config.ts, packages/Chembl/src/package.ts:15-19 |
| Reference: minimal entry-points-only config | `entryPoints` with two semantic types — one with `matchRegexp` + `regexpExample` (`CHEMBL_ID`), one without either (`molregno`, detected by other means). | Demonstrates the optional-detector pattern — a numeric ID that the platform tags via column-level metadata, not regex. | packages/Chembl/src/explorer-config.ts:8-22 |
| No-code editor entry point | UI flow `Browse > Databases > <connection>` right-click `Configure Identifiers...` | Opens an accordion with sections — Identifiers, Joins, Explicit References, Header Names, Unique Columns, Custom Selected Columns, Renderers; Import/Export JSON + Save at the bottom. Validates required fields on Save. | help/develop/how-to/db/register-identifiers.md:131-200, playwright-public/connections/02-identifiers.test.ts:69-72 |

## Custom JsViewer (dataframe-bound)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| JsViewer base class | `DG.JsViewer extends Viewer` (`js-api/src/viewer.ts:376-499`) | Subclass to build a dataframe-bound viewer. `_root` is `ui.box()`; `subs: Subscription[]` collects subscriptions cleaned up by the default `detach()`; `props: ObjectPropertyBag` holds registered properties. Override `onTableAttached()` / `onPropertyChanged(property)` / `render(...)` — DO NOT override `detach()` without `super`. | js-api/src/viewer.ts:376-499 |
| Annotated-function viewer registration | `//name: ... //tags: viewer //output: viewer result` over a factory in `package.ts` | `packages/Charts/src/viewers/sankey/sankey.ts:53-57` — single function `sankey()` returns `new SankeyViewer()`. | packages/Charts/src/viewers/sankey/sankey.ts:53-57 |
| Decorator-form viewer registration | `@grok.decorators.viewer({icon, toolbox, trellisable, viewerPath})` over the class (datagrok-tools ≥ 4.12.x) | FuncGeneratorPlugin generates the matching annotated function into `package.g.ts` at build time. The `.g.ts` file IS committed (not gitignored). | js-api/src/decorators/functions.ts:36-45 |
| Reference: SVG/D3 JsViewer | `packages/Charts/src/viewers/sankey/sankey.ts` — D3-based Sankey; class exposes `init()`, `onTableAttached()`, `render(computeData)`, `onPropertyChanged(property)` with the canonical `subs`-based subscription wiring. | Closest match to the article's worked example (D3 + dataframe filter/selection sync). | packages/Charts/src/viewers/sankey/sankey.ts |
| Reference: WebGL/Molstar JsViewer | `packages/BiostructureViewer/src/viewers/molstar-viewer/molstar-viewer.ts` — heavyweight viewer subclass with async initialization, lifecycle around an external rendering library. | Pattern for "external library owns the canvas; the JsViewer is a thin coordination layer". | packages/BiostructureViewer/src/viewers/molstar-viewer/molstar-viewer.ts |
| Reference: GIS/OpenLayers JsViewer | `packages/GIS/src/gis-viewer.ts` — OpenLayers-backed map viewer registered with `meta.role: viewer`. | Pattern for geospatial viewers with imperative tile/marker APIs. | packages/GIS/src/gis-viewer.ts |
| Built-in `rowSource` enum opt-in | `this.addRowSourceAndFormula()` invoked from the constructor (`js-api/src/viewer.ts:406-410`) | Adds the standard `rowSource` dropdown (`All / Filtered / Selected / SelectedOrCurrent / FilteredSelected / MouseOverGroup / CurrentRow / MouseOverRow`) plus a `formulaFilter` STRING property, both under the Data tab. | js-api/src/viewer.ts:406-410 |

## Custom file viewers

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| File-viewer role token | `DG.FUNC_TYPES.FILE_VIEWER = 'fileViewer'` (string constant) | The value referenced by `//meta.role: fileViewer`. JSDoc at the constant signature line is `fileViewer(FileInfo): View`. | js-api/src/const.ts:372-376 |
| File-viewer decorator | `@grok.decorators.fileViewer(config: FileViewerOptions)` from `datagrok-api/grok` | `FileViewerOptions extends FunctionOptions { fileViewer: string; fileViewerCheck?: string }` — `fileViewer` is REQUIRED. | js-api/src/decorators/functions.ts:220-223,342-348 |
| File-input wrapper | `DG.FileInfo` (extends `Entity`) | Properties: `path`, `fullPath`, `extension`, `fileName`, `name`, `url`, `isFile`, `isDirectory`. Methods: `readAsString(): Promise<string>`, `readAsBytes(): Promise<Uint8Array>`. | js-api/src/entities/table-info.ts:63-119 |
| View factory | `DG.View.create(options?: string \| {} \| null): View` | The empty-view constructor used by every file viewer. `options` accepts a CSS class (`'d4-escher-container'`) or an `_options` payload. | js-api/src/views/view.ts:243-251 |
| Reference: minimal single-extension viewer (decorator form) | `packages/EpsViewer/src/package.ts:122-145` — `@grok.decorators.fileViewer({fileViewer: 'eps'})` over a static `previewEps(file: DG.FileInfo): DG.View`. Sync return + `file.readAsBytes().then(...)` chain. | Cleanest production reference. Mounts a `<canvas>` inside `ui.div([], 'eps-viewer-host')` and chains rendering via Promise. | packages/EpsViewer/src/package.ts |
| Reference: multi-extension viewer (header form) | `packages/BiostructureViewer/src/package-ngl.ts:11-43` — four functions with `meta.fileViewer: mol,mol2,cif,…`, etc. Each is a thin wrapper around `previewNgl(file)`. | Demonstrates extension grouping by file family. Single concrete viewer; multiple registration entries. | packages/BiostructureViewer/src/package-ngl.ts |
| Reference: extension shared via `fileViewerCheck` disambiguation | `packages/Plates/src/package.g.ts:25-66` — same `txt` extension served by `previewPlate` (with `fileViewerCheck: Plates:checkFileIsPlate`) and presumably the platform's default text viewer. Same for `xlsx` via `previewPlateXlsx` + `Plates:checkExcelIsPlate`. | The canonical pattern for "viewer X is for `txt` files but only when content matches Y". `checkFileIsPlate(content: string): bool` runs first; falsey defers. | packages/Plates/src/package.g.ts |
| Reference: async viewer (file-content awaited before return) | `packages/MetabolicGraph/src/package.ts:76-101` — `async escherFileViewer(file)` awaits `file.readAsString()`, parses JSON, then sets up the view. Plus a `fileViewerCheck` (`Metabolicgraph:escherFileViewerCheck`) that bounds size and matches a JSON-Schema marker before declaring applicability. | Complete async pattern. The check fast-rejects `>10MB` files and non-Escher JSON before the viewer is ever instantiated. | packages/MetabolicGraph/src/package.ts |
| Reference: `fileViewerCheck` companion functions | `packages/Plates/src/package.ts:226` (`checkFileIsPlate(content: string): boolean`); `packages/Plates/src/package.ts:120-132` (`checkExcelIsPlate(content: Uint8Array): Promise<boolean>`); `packages/Chem/src/package.ts:2879` (`checkJsonMpoProfile(content: string): bool`); `packages/MetabolicGraph/src/package.ts:103-108` (`escherFileViewerCheck`). | Demonstrates both string-content and binary-content (`blob`/`Uint8Array`) check signatures. All return `bool`; truthy means "this viewer applies". | (multiple) |
| Reference: large extension list with `meta.fileViewerCheck` | `packages/nmrium/src/package.g.ts:33-43` — same package registers `nmrium` extension and `dx, jdx` extensions; the latter pair is gated by `Nmrium:checkNmriumJdx`. | Demonstrates one package owning both an unambiguous extension and an ambiguous one (JCAMP-DX is shared with other tools). | packages/nmrium/src/package.g.ts |

## Custom file exporters

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| File-exporter role token | `DG.FUNC_TYPES.FILE_EXPORTER = 'fileExporter'` (string constant) | The value referenced by `//meta.role: fileExporter`. Signature `fileExporter(): void`; JSDoc: "Exports a file. Gets added to the export menu at startup." | js-api/src/const.ts:378-380,485-489 |
| File-exporter decorator | `@grok.decorators.fileExporter(config?: FunctionOptions)` from `datagrok-api/grok` | Generic `FunctionOptions` — NO specialized `FileExporterOptions` (unlike `FileViewerOptions`). `description` is the only field skill authors set in practice. | js-api/src/decorators/functions.ts:350-356 |
| Download helper | `DG.Utils.download(filename, content, contentType?)` (static on `DG.Utils`) | One-line download trigger — `Blob([content], {type: contentType})` + `URL.createObjectURL` + `<a>.click()`. Default `contentType` is `'application/octet-stream'`. | js-api/src/utils.ts:230-237 |
| Active-table accessor | `grok.shell.t` (getter on `Shell`) | Returns the current `DG.DataFrame` or `null`. The standard entry point for any exporter. | js-api/src/shell.ts (Shell.t) |
| Reference: minimal direct download (decorator form) | `packages/Arrow/src/package.ts:64-76` — `@grok.decorators.fileExporter({description: 'Save as Parquet'})` static method; reads `grok.shell.t`, calls `this.toParquet(table)`, then `DG.Utils.download(table.name + '.parquet', uint8Array)`. Same shape for `Save as Feather`. | Cleanest production reference for binary payloads. Two exporters in one package, both one-liners. | packages/Arrow/src/package.ts |
| Reference: dialog-based exporter (decorator form) | `packages/Chem/src/package.ts:526-531` — `@grok.decorators.fileExporter({description: 'As SDF...'}) static async saveAsSdf()` delegates to `saveAsSdfDialog()` (factored into `packages/Chem/src/utils/sdf-utils.ts`). | Pattern for exporters that need a column-picker / format-option dialog. Wrapper stays one line; dialog logic factored into a util. | packages/Chem/src/package.ts |
| Reference: thin-wrapper exporter | `packages/Bio/src/package.ts:1404-1407` — `@grok.decorators.fileExporter({description: 'As FASTA...'}) static saveAsFasta() { saveAsFastaUI(); }`. UI lives in `packages/Bio/src/utils/save-as-fasta.ts`. | Same pattern as Chem — one-line decorator + util delegation. Useful template for sequence/text-format exporters. | packages/Bio/src/package.ts, packages/Bio/src/utils/save-as-fasta.ts |
| Reference: header-form exporter (auto-emitted) | `packages/Arrow/src/package.g.ts:60-70` — `//description: Save as Parquet` + `//meta.role: fileExporter` over a thin wrapper. | The `package.g.ts` form is auto-generated from the decorator on the source method. Skill authors should write decorator-form in `package.ts`; the codegen handles `.g.ts`. | packages/Arrow/src/package.g.ts |
| Lint rule: missing description | `tools/bin/commands/check.ts:358-359` — `if (roleDesc.role === 'fileExporter' && (!func.description || func.description === '')) addError('File exporters should have a description parameter')`. | Hard error, not a warning. Run via `grok check` during package validation. | tools/bin/commands/check.ts |
| Function-role descriptor | `functionRoles[]` entry in `js-api/src/const.ts:485-489` — `{role: FUNC_TYPES.FILE_EXPORTER, header: 'tags', description: 'Exports a file. Gets added to the "export" menu at startup.', signature: 'fileExporter(): void'}`. Same shape mirrored in `tools/bin/utils/const.ts:131-136` and `tools/bin/utils/func-generation.ts:340-346` (the codegen knows `inputs: []`, `outputs: []`). | The single source of truth for the file-exporter contract — role name, header bucket, signature, no inputs, no outputs. | js-api/src/const.ts:485-489, tools/bin/utils/func-generation.ts:340-346 |

## Custom folder content preview

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Folder-viewer role token | `DG.FUNC_TYPES.FOLDER_VIEWER = 'folderViewer'` (string constant) | The value referenced by `//meta.role: folderViewer`. Function-role descriptor: `signature: 'folderViewer(folder: File, files: list<file>): Widget'`, `description: 'Provides a custom folder content preview.'`. | js-api/src/const.ts:419,593-597, tools/bin/utils/const.ts:77 |
| Folder-viewer decorator | `@grok.decorators.folderViewer(config?: FunctionOptions)` from `datagrok-api/grok` | Generic `FunctionOptions` — NO specialized `FolderViewerOptions` (unlike `FileViewerOptions` / `FileHandlerOptions`). `config` is OPTIONAL; pass `{name}` and/or `{outputs: [...]}` only when overriding the defaults. | js-api/src/decorators/functions.ts:294-300 |
| Folder-viewer codegen entry | `funcGenerators.folderViewer` in `tools/bin/utils/func-generation.ts:420-426` — `metadata: { role: FUNC_TYPES.FOLDER_VIEWER, inputs: [{name: 'folder', type: 'file'}, {name: 'files', type: 'list<file>'}], outputs: [{name: 'result', type: 'widget'}] }`. | Default codegen produces a `(folder, files) → widget` function. Override `outputs` to `[{name: 'result', type: 'dynamic'}]` to allow returning a `DG.ViewBase`. | tools/bin/utils/func-generation.ts:420-426 |
| Widget factory | `DG.Widget.fromRoot(root: HTMLElement): Widget` (static on `Widget`) | The canonical wrap-an-HTMLElement-into-a-widget call used by every `folderViewer`. | js-api/src/widgets/base.ts:374-376 |
| Folder-info input wrapper | `DG.FileInfo` (extends `Entity`) | Same shape as a file-input — but for a folder, `isDirectory` is true. Properties used in folder viewers: `name`, `fullPath`. Use `fullPath` with `grok.dapi.files.readAsText` to read child files when `files[]` doesn't carry the bytes. | js-api/src/entities/table-info.ts:63-119 |
| Reference: SDTM-detection folder viewer (decorator form) | `packages/ClinicalCase/src/package.ts:200-228` — `@grok.decorators.folderViewer({name: 'clinicalCaseFolderLauncher'})` over `static async clinicalCaseFolderLauncher(folder, files): Promise<DG.Widget \| undefined>`. Detects `dm.csv` via `files.some(...)`, reads `${folder.fullPath}/dm.csv`, parses CSV, extracts study ID, builds a panel + launcher button. | The canonical full-fledged production reference. Article shows a stub variant of this exact function. | packages/ClinicalCase/src/package.ts |
| Reference: name-based folder viewer (decorator form) | `packages/Plates/src/package.ts:48-54` — `@grok.decorators.folderViewer({outputs: [{'name': 'result', 'type': 'dynamic'}]})` over `static async platesFolderPreview(folder, files): Promise<DG.Widget \| DG.ViewBase \| undefined>`. Inspects `folder.name?.toLowerCase().includes('plate')` and delegates to `getPlatesFolderPreview(files)`. | Demonstrates the `outputs: dynamic` override for ViewBase return types and the folder-name-based detection pattern (no per-file scan needed). | packages/Plates/src/package.ts |
| Reference: header-form folder viewer (auto-emitted) | `packages/ClinicalCase/src/package.g.ts:37-43` — `//meta.role: folderViewer` + `//input: file folder` + `//input: list<file> files` + `//output: widget result` over a thin wrapper that delegates to `PackageFunctions.clinicalCaseFolderLauncher`. | The `package.g.ts` form is auto-generated from the decorator on the source method. Skill authors should write decorator-form in `package.ts`; the codegen handles `.g.ts`. | packages/ClinicalCase/src/package.g.ts |

## Custom file handlers

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| File-handler role token | `DG.FUNC_TYPES.FILE_IMPORTER = 'file-handler'` (kebab string in const) — but emitted as `fileHandler` (camel) in every `package.g.ts`. JSDoc at the constant: "Handles custom file formats. The `meta.ext` parameter should contain a comma-separated list of extensions. Signature: `fileImporter(x: string \| TypedArray): DataFrame[]`". | The mismatch is a documented DRIFT (`DG-FACT-DRIFT-031`); use `fileHandler` (camel) in skills — both forms are accepted by the platform but `fileHandler` matches article + production `package.g.ts`. | js-api/src/const.ts:382-385,490-495, tools/bin/utils/const.ts:42-43, tools/bin/utils/func-generation.ts:93 |
| File-handler decorator | `@grok.decorators.fileHandler(config: FileHandlerOptions)` from `datagrok-api/grok` | `FileHandlerOptions extends FunctionOptions { ext: string; fileViewerCheck?: string }` — `ext` REQUIRED, `config` REQUIRED (the only file-* decorator with a non-optional `config`). | js-api/src/decorators/functions.ts:225-228,358-364 |
| File-handler codegen entry | `funcGenerators.fileHandler` in `tools/bin/utils/func-generation.ts:348-355` — `metadata: { role: FUNC_TYPES.FILE_HANDLER, inputs: [{name: 'content', type: 'string'}], outputs: [{name: 'tables', type: 'list'}] }`. | Default codegen produces a string-input handler returning `list tables`. Override `inputs` to `[{name: 'bytes', type: 'list'}]` for binary formats. | tools/bin/utils/func-generation.ts:348-355 |
| Reference: minimal single-extension handler (decorator form) | `packages/nmrium/src/package.ts:43-46` — `@grok.decorators.fileHandler({ext: 'nmrium', outputs: [{name: 'tables', type: 'list'}]}) static async nmriumFileHandler(bytes: string)`. Returns `[]` after calling `addNmriumView('nmrium', bytes)`. | Cleanest example of the side-effecting pattern — handler opens a custom view, returns no DataFrames. | packages/nmrium/src/package.ts |
| Reference: handler with `fileViewerCheck` | `packages/nmrium/src/package.ts:20-40` — two `@grok.decorators.fileHandler({fileViewerCheck: 'Nmrium:checkNmriumJdx', ext: 'jdx'/'dx'})` entries. The check disambiguates JCAMP-DX (a format shared with chemistry tools) from NMRium-flavored JDX. | The canonical pattern for "handler X claims `.jdx` only when content matches Y". | packages/nmrium/src/package.ts |
| Reference: handler taking `Uint8Array` (binary) | `packages/Chem/src/package.g.ts:793-809` — `//meta.role: fileHandler` + `//meta.ext: sdf,mol` + `//input: list bytes` + `//output: list<dataframe> result`. The TS body in `packages/Chem/src/package.ts` calls `Chem.importSdf(bytes)` from `chemistry/utils/import-sdf.ts`. | The canonical Uint8Array pattern. Same shape for `importBam(bytes)`, `importMol2(bytes)`. | packages/Chem/src/package.g.ts |
| Reference: handler taking `string` text content with multi-extension | `packages/Bio/src/package.g.ts:486-503` — `//meta.role: fileHandler` + `//meta.ext: fasta, fna, ffn, faa, frn, fa, fst` + `//input: string fileContent` + `//output: list<dataframe> result`. Single function, seven extensions. | Reference for text-format handlers covering several extensions in one registration. | packages/Bio/src/package.g.ts |
| Reference: header-form handler from the article | `function fastaFileHandler(content)` with `//input: string content` + `//output: list tables` + `//meta.role: fileHandler` + `//meta.ext: fasta`. Bare-function pattern; works but pre-`PackageFunctions` style. Skill output should prefer decorator-form in `package.ts` and let codegen produce `package.g.ts`. | The article's only example — useful for understanding the contract, not for templating modern packages. | help/develop/how-to/files/file-handlers.md:12-21 |
| Coexistence with `fileViewer` | A package can register both for the same extension — `packages/nmrium/src/package.ts:43-55` registers `fileHandler({ext: 'nmrium'})` AND `fileViewer({fileViewer: 'nmrium'})`. Handler imports + returns the table list (often `[]`); viewer adds the dedicated visualization. | Pattern for "import as table, AND show with a custom UI". The two roles are not interchangeable. | packages/nmrium/src/package.ts |

## Custom cell renderers

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Cell-renderer role token | `DG.FUNC_TYPES.CELL_RENDERER = 'cellRenderer'` (string constant). Function-role descriptor: `{role: FUNC_TYPES.CELL_RENDERER, header: 'tags', signature: 'cellRenderer(): GridCellRenderer'}` at `js-api/src/const.ts:497-501`. | The role string referenced by `//meta.role: cellRenderer` and by the class-decorator's emit path. NOTE the lookalike `Tags.CELL_RENDERER = 'cell.renderer'` at line 330 is a column-tag KEY, not the function role. | js-api/src/const.ts:330, js-api/src/const.ts:387-390, js-api/src/const.ts:497-501 |
| Cell-renderer base class | `DG.GridCellRenderer<TData = any>` (extends `CanvasRenderer`) | Required overrides — `get name(): string`, `get cellType(): string`, `render(g, x, y, w, h, gridCell, cellStyle): void`. Optional — `renderSettings(gridColumn): Element \| null` (default `null`); `defaultWidth`, `defaultHeight`, `getDefaultSize(gridColumn)`; `clip` flag (default `true`); mouse/keyboard handlers. Static `register(renderer)` is the imperative registration. | js-api/src/grid.ts:1276-1325 |
| Class-decorator surface | `@grok.decorators.cellRenderer(options?: {name?, description?, cellType, columnTags?, virtual?})` — exported as a CLASS decorator (typed `(constructor: Function) => void`). | The modern entry point. Apply directly to the renderer subclass. `cellType` REQUIRED; `virtual` is BOOLEAN. | js-api/src/decorators/functions.ts:62-80 |
| Function-decorator alternate surface | `@grok.decorators.func({meta: {role: 'cellRenderer', cellType: '<t>', virtual: 'true'}, tags: ['cellRenderer'], outputs: [{type: 'grid_cell_renderer', name: 'result'}]})` over a `static <factory>()` inside `PackageFunctions`. | Used heavily by PowerGrid (19+ entries). Required when the renderer factory needs to live next to other package functions. `meta.virtual` is STRING `'true'` here. | packages/PowerGrid/src/package.ts:85-98, js-api/src/decorators/functions.ts:117-145 |
| Codegen entry | `funcGenerators.cellRenderer` in `tools/bin/utils/func-generation.ts:323-330` — `{metadata: {role: FUNC_TYPES.CELL_RENDERER, inputs: [], outputs: [{name: 'renderer', type: 'grid_cell_renderer'}]}, genFunc: generateClassFunc}`. | Default codegen produces a no-input factory whose output type is `grid_cell_renderer`. | tools/bin/utils/func-generation.ts:323-330 |
| Build-time codegen plugin | `FuncGeneratorPlugin` from `datagrok-tools/plugins/func-gen-plugin` | Wired in every package's `webpack.config.js`: `new FuncGeneratorPlugin({outputPath: './src/package.g.ts'})`. Walks `package.ts`, reads decorators, emits `package.g.ts` (which is committed). | tools/plugins/func-gen-plugin.js, packages/PowerGrid/webpack.config.js:2,47 |
| Reference: chart-style virtual renderer (function decorator) | `packages/PowerGrid/src/package.ts:85-98` — piechart factory inside `PackageFunctions`. Returns `new PieChartCellRenderer()`. Implementation in `packages/PowerGrid/src/sparklines/piechart.ts:192-217`. | The canonical "summary column" pattern — `meta.virtual: 'true'`, `meta.gridChart: 'true'`, `meta.cellType: 'piechart'`, `tags: ['cellRenderer']`. | packages/PowerGrid/src/package.ts, packages/PowerGrid/src/sparklines/piechart.ts |
| Reference: simple semantic renderer (class decorator) | `packages/PowerGrid/src/cell-types/svg-cell-renderer.ts:6-12` — `@grok.decorators.cellRenderer({name: 'svgCellRenderer', cellType: 'SVG'})` over `class SvgCellRenderer extends DG.GridCellRenderer`. NOT virtual; renders an existing `string` cell value. | The simplest production class-decorator example — single file, single decorator, working renderer. | packages/PowerGrid/src/cell-types/svg-cell-renderer.ts |
| Reference: virtual class-decorator renderer | `packages/Curves/src/fit/fit-renderer.ts:311-315` — `@grok.decorators.cellRenderer({name: 'Fit', cellType: 'fit', virtual: true})` over `class FitChartCellRenderer extends DG.GridCellRenderer`. | Demonstrates `virtual: true` as a BOOLEAN on the class decorator (contrast with the function-decorator form's STRING `'true'`). | packages/Curves/src/fit/fit-renderer.ts |
| Reference: tag-matched renderer | `packages/PowerGrid/src/package.g.ts:185-201` — `meta.columnTags: foo=bar,units=kg` and `meta.columnTags: foo=bar,units=ton` (test fixtures). Production Macromolecule example commented at `packages/Bio/src/utils/cell-renderer.ts:63` (`columnTags: 'quality=Macromolecule, units=custom'`). | Canonical reference for the `columnTags` matcher — comma-separated `key=value` pairs. | packages/PowerGrid/src/package.g.ts, packages/Bio/src/utils/cell-renderer.ts:63 |

## Custom column tooltips

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Tooltip role token | `DG.FUNC_TYPES.TOOLTIP = 'tooltip'` (string constant) | The value referenced by `//meta.role: tooltip`. Function-role descriptor: `signature: 'tooltip(col: Column): Widget'`, `description: 'Provides a custom tooltip for columns'`, `header: 'tags'`. | js-api/src/const.ts:418, js-api/src/const.ts:586-591 |
| Tooltip decorator (no specialized form) | Generic `@grok.decorators.func({meta: {role: 'tooltip'}})` from `datagrok-api/grok` | NO specialized `@grok.decorators.tooltip()` exists in `js-api/src/decorators/functions.ts`, and no `funcGenerators.tooltip` in `tools/bin/utils/func-generation.ts`. Skill output must use the generic `func` decorator with `meta.role`. | js-api/src/decorators/functions.ts, tools/bin/utils/func-generation.ts |
| Column-input parameter decorator | `@grok.decorators.param({options: {semType: '<Type>'}})` on the `col: DG.Column` parameter | Binds the tooltip to columns of the given `semType`. The platform filters at dispatch time — the function is only invoked for matching columns. | packages/Bio/src/package.ts:151, packages/Chem/src/package.ts:268 |
| Column wrapper (input type) | `DG.Column` | The single argument to a tooltip function. Use `col` directly (or `col.dataFrame`) to build the widget — do NOT pull from `grok.shell.tv.dataFrame`. | js-api/src/dataframe.ts (Column class) |
| Reference: Macromolecule sequence tooltip (decorator form) | `packages/Bio/src/package.ts:146-159` — `@grok.decorators.func({meta: {role: 'tooltip'}, tags: ['tooltip']}) static sequenceTooltip(@grok.decorators.param({options: {semType: 'Macromolecule'}}) col: DG.Column): DG.Widget<any>` constructs `new MacromoleculeColumnWidget(col, _package.seqHelper)` and kicks off async init with error reporting. | The article's exact `sequenceTooltip` example, modernized to decorator form and using a real widget instead of `grok.shell.tv.dataFrame.plot.fromType('WebLogo', ...)`. | packages/Bio/src/package.ts |
| Reference: Molecule chem tooltip with skip-on-SMARTS (decorator form) | `packages/Chem/src/package.ts:263-...` — `@grok.decorators.func({name: 'Chemistry | Most Diverse Structures', meta: {role: 'tooltip'}}) static async chemTooltip(@grok.decorators.param({options: {semType: 'Molecule'}}) col: DG.Column): Promise<DG.Widget | undefined>`. Returns `undefined` early when any of the first 100 categories is a SMARTS pattern; otherwise builds a div with the most diverse structures. | Demonstrates the `Promise<DG.Widget | undefined>` pattern AND the early-return-undefined pattern for "no tooltip applies". | packages/Chem/src/package.ts |
| Reference: header-form tooltip (auto-emitted) | `packages/Bio/src/package.g.ts:16-22` — `//tags: tooltip` + `//input: column col { semType: Macromolecule }` + `//output: widget result` + `//meta.role: tooltip` over a thin wrapper that delegates to `PackageFunctions.sequenceTooltip`. Same shape at `packages/Chem/src/package.g.ts:38-44` (sans the `//tags: tooltip` line because Chem omitted `tags` in the decorator). | The `package.g.ts` form is auto-generated from the decorator on the source method. Skill authors should write decorator-form in `package.ts`; the codegen handles `.g.ts`. | packages/Bio/src/package.g.ts, packages/Chem/src/package.g.ts |

## Custom package settings editors

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Settings-editor role token | `DG.FUNC_TYPES.PACKAGE_SETTINGS_EDITOR = 'packageSettingsEditor'` (string constant). Function-role descriptor: `{role: FUNC_TYPES.PACKAGE_SETTINGS_EDITOR, header: 'tags', description: 'Edits package settings.', signature: 'packageSettingsEditor(): Widget'}` at `js-api/src/const.ts:502-507`. The codegen enum entry — `SETTINGS_EDITOR = 'packageSettingsEditor'` at `tools/bin/utils/func-generation.ts:95`. | The role string referenced by `//meta.role: packageSettingsEditor` and by the function decorator's `meta.role` field. `header: 'tags'` causes the role to be auto-written into the `tags` bucket on the server side, so `tags: ['packageSettingsEditor']` is implicitly set even if you only specify `meta.role`. | js-api/src/const.ts:392-394,502-507, tools/bin/utils/func-generation.ts:95 |
| Editor return type | `DG.Widget` (sync) or `Promise<DG.Widget>` (async). `DG.Widget` is the same base class used for dashboard / panel / tooltip widgets — subclass with `class X extends DG.Widget { constructor() { super(ui.div()); ... } }`. | The `super(...)` argument becomes `this.root: HTMLElement`. Override `get type(): string` to provide a stable serialization identifier. | js-api/src/widgets.ts (Widget class), packages/PowerPack/src/settings-editor.ts:11-13 |
| Decorator surface (specialized — unused in production) | `@grok.decorators.packageSettingsEditor(config?: FunctionOptions)` from `datagrok-api/grok` | The decorator exists but its body is empty (it's a marker for the codegen plugin). NO shipping production package uses this form — they all use the generic `@grok.decorators.func({meta: {role: 'packageSettingsEditor'}, tags: ['packageSettingsEditor']})` instead. Skill output should match production. | js-api/src/decorators/functions.ts:310-316 |
| Decorator surface (generic — production-canonical) | `@grok.decorators.func({name, meta: {role: 'packageSettingsEditor'}, tags: ['packageSettingsEditor']})` over a `static` method inside `PackageFunctions` | The shape used by HitTriage. Pair with `@grok.decorators.param({name: 'propList', type: 'object'}) properties: DG.Property[]` to receive the platform-supplied property list. Codegen auto-emits the matching `//meta.role: packageSettingsEditor` + `//tags: packageSettingsEditor` + `//input: object propList` + `//output: widget result` block in `package.g.ts`. | packages/HitTriage/src/package.ts:350-358, packages/HitTriage/src/package.g.ts:127-134 |
| Reference: full production editor (HitTriage) | `htPackageSettingsEditorWidget(propList: DG.Property[]): Promise<DG.Widget>` in `packages/HitTriage/src/packageSettingsEditor.ts:66-...` — filters `propList` by `category === defaultSharingGroupCategoryName`, builds `ui.input.userGroups(...)` per matching prop, subscribes `onInput` and calls `prop.set(null, JSON.stringify(...))` to stage values. Returns a `DG.Widget` wrapping `ui.div(inputs)`. | The richest production reference — demonstrates property filtering, custom input types, async setup, and the `prop.set` staging flow. | packages/HitTriage/src/packageSettingsEditor.ts |
| Reference: minimal editor (PowerPack) | `class PowerPackSettingsEditor extends DG.Widget { constructor() { super(ui.div()); this.root.appendChild(...); } get type(): string { return 'PowerPackSettingsEditor'; } }` at `packages/PowerPack/src/settings-editor.ts:11-40`. The factory function `powerPackSettingsEditor(): DG.Widget` is currently commented out — the class itself ships and is referenced from `widgets/` registration. | The simplest reference — single class, no `propList` argument, no async, just a Widget subclass with imperative DOM mutation. Article's example matches this shape. | packages/PowerPack/src/settings-editor.ts |
| Reference: header-form editor (auto-emitted) | `packages/HitTriage/src/package.g.ts:127-134` — `//name: Hit Triage package settings editor` + `//tags: packageSettingsEditor` + `//input: object propList` + `//output: widget result` + `//meta.role: packageSettingsEditor` over an `async function` wrapper that delegates to `PackageFunctions.htPackageSettingEditor`. | The `package.g.ts` form is auto-generated from the decorator on the source method. Skill authors should write the decorator form in `package.ts` and let codegen produce `.g.ts`. | packages/HitTriage/src/package.g.ts |
| Property bag passed to the editor | `DG.Property[]` — array of platform-managed `Property` instances, each carrying `.name`, `.category`, `.get(scope)`, `.set(scope, value)`. The `scope` argument is `null` in production usage (HitTriage). Properties originate from `package.json` (or codegen-driven property declarations). | The same `DG.Property` type used by function-input descriptors and column-tooltip widgets. Persistence — `prop.set(null, value)` stages, then the platform commits on the user's "save" click. | js-api/src/entities.ts (Property class), packages/HitTriage/src/packageSettingsEditor.ts:71-83 |

## Python functions (Celery-backed)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Celery app entrypoint | `app = Celery(settings.celery_name, broker=settings.broker_url)` in `app.py` | Module-level `app` is what `celery -A app worker` resolves at container start. Always named `app.py` in production references. | packages/Samples/dockerfiles/app.py:14-16 |
| Datagrok task base class | `DatagrokTask` from `datagrok-celery-task` | Required `base=DatagrokTask` argument on every `@app.task(...)` so the platform's Celery dispatch can reach the function. | packages/Samples/dockerfiles/app.py:5,32 |
| Celery settings helper | `Settings(log_level=...)` from `datagrok-celery-task` | Provides `.celery_name` and `.broker_url` consumed by `Celery(...)`. The broker URL is platform-injected at runtime. | packages/Samples/dockerfiles/app.py:5,14 |
| Function-decoration pattern | `@app.task(name='<celery_task_name>', bind=True, base=DatagrokTask)` over a Python `def` carrying Datagrok header comments | The `name=` is the celery routing identifier and is independent of the `#name:` header that registers the function in Datagrok. | packages/Samples/dockerfiles/app.py:32,57,71 |
| Reference: KNN ML triple (train/apply/isApplicable) | `packages/Samples/dockerfiles/app.py` — three `@app.task` functions sharing `#meta.mlname: PyKNN` and distinct `#meta.mlrole:` values (`train`/`apply`/`isApplicable`). Implementations factored into `train.py` + `predict.py`. | The richest production reference for parameter-rich Python functions, including `category`, `choices`, `range` per-input options. | packages/Samples/dockerfiles/app.py |
| Reference: minimal `container.json` for Celery | `packages/Samples/dockerfiles/container.json` — `{"cpu": 1, "memory": 1024}`. | Matches the article's recommended minimum for Celery-prefork containers. | packages/Samples/dockerfiles/container.json |
| Reference: micromamba+Celery Dockerfile | `packages/Samples/dockerfiles/Dockerfile` — `FROM mambaorg/micromamba:1.5.3` + `micromamba create -n myenv -f environment.yaml` + `EXPOSE 5555` + `ENTRYPOINT micromamba run -n myenv celery -A app worker --loglevel=info --hostname=$CELERY_HOSTNAME --concurrency=1 -Q $TASK_QUEUE_NAME`. | The canonical hand-written Dockerfile. Article's claim that this is "auto-generated" is DRIFT-063. | packages/Samples/dockerfiles/Dockerfile |
| Reference: pip-via-conda environment.yaml | `packages/Samples/dockerfiles/environment.yaml` — conda channels (`conda-forge`/`defaults`), Python pinning (`python=3.8`), conda deps (`numpy`, `pandas`, `scikit-learn`), then `pip:` block with `datagrok-celery-task==0.1.8` + `celery`. | Demonstrates the dual conda+pip pattern. The `datagrok-celery-task` line is REQUIRED (DRIFT-062). | packages/Samples/dockerfiles/environment.yaml |
| Counter-example: non-Celery Python container | `packages/Python/dockerfiles/python/server.py` + `Dockerfile` — uses pika/RabbitMQ + websockets directly (no Celery, no `@app.task`). NOT a python-functions reference; this is the platform's Python-scripting backend. | Use the Samples reference, not Python, for python-functions skills. | packages/Python/dockerfiles/python |

## Data enrichments

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Bundled enrichment file shape | Plain JSON (no TS interface) — `{name, connection, keySchema, keyTable, keyColumn, fields: string[], joins: Join[]}`. | One file per enrichment under `<PackageRoot>/enrichments/`. Loaded by the platform at package publish; the column context panel renders one row per discovered enrichment. | help/develop/how-to/packages/data-enrichments.md:25-50, packages/Chembl/enrichments/activities_assays.json |
| Runtime enrichment object | `interface Enrichment { name: string; keyDb: string \| null; keySchema: string; keyTable: string; keyColumn: string; fields: string[]; joins: DG.TableJoin[]; }` | The shape PowerPack writes to the file share when the user edits an enrichment via the UI. NOT the bundled-publish shape — see DRIFT-048. | packages/PowerPack/src/db-explorer.ts:755-763 |
| Join descriptor | `DG.TableJoin` — five fields `{leftTableName, rightTableName, joinType, leftTableKeys, rightTableKeys}`. | Built by `convertTableQueryToEnrichment` from the `tableQuery.joins` array of the visual db query editor. | packages/PowerPack/src/db-explorer.ts:679-681, js-api/src/dataframe.ts (TableJoin) |
| PowerPack runtime dispatcher | `PowerPack:runEnrichment` (decorated `meta: {role: 'transform'}`) | Apply-time entry point — discovered via `DG.Func.find({package: 'PowerPack', name: 'runEnrichment'})[0]` and invoked with `{conn, schema, table, column, name, df, db, localColumn?}`. | packages/PowerPack/src/package.ts:393-399, packages/PowerPack/src/db-explorer.ts:530-535,691-699 |
| Reference: Chembl enrichments folder | `packages/Chembl/enrichments/` — 17 JSON files joining `molecule_dictionary`, `activities`, `assays`, `target_dictionary`, `compound_*`, `docs`, etc. All share `connection: "Chembl:Chembl"`, `keySchema: "public"`, `joinType: "left"`. | The richest production reference — full chemistry-domain join library, single connection, all single-key joins. Useful as a template when shipping many enrichments off one connection. | packages/Chembl/enrichments |
| Reference: DBTests enrichments folder | `packages/DBTests/enrichments/` — 3 files (`company_info.json`, `customer_orders_summary.json`, `product_info.json`) joining the Northwind-style `orders`/`customers`/`products` tables on `Dbtests:PostgresTest`. | The minimal production reference — clean domain, easy to read, matches the article's `company_info` example almost verbatim (article says `Dbtests:Northwind`, repo file says `Dbtests:PostgresTest`). | packages/DBTests/enrichments |
| Reference: PowerPack enrichment column-context UI | `getEnrichmentsDiv(conn, schema, table, column, db, df, fkRefs)` and `showEnrichDialog(...)` in `packages/PowerPack/src/db-explorer.ts:479-540`. | The full column-context flow — empty state ("No enrichments yet"), per-row Apply / Edit / Delete, FK-traversed enrichments listed under "via &lt;table&gt;". Skill code does not call these directly; useful for understanding what users see. | packages/PowerPack/src/db-explorer.ts |

## Home page widgets (dashboard role)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Widget base class | `DG.Widget<TSettings>` (`class Widget` at `js-api/src/widgets/base.ts:231`). Constructor takes a single `widgetRoot: HTMLElement`; the argument becomes `this.root`. Override `get type(): string` for stable serialization (default `'Unknown'`). | All home-page widgets subclass this. Constructor body sets `this.root`'s children and registers tunable settings via `super.addProperty(name, type, defaultValue, options?)`. | js-api/src/widgets/base.ts:231-257, 322-325, 345-365 |
| `addProperty` (gear-icon settings registration) | `addProperty(propertyName: string, propertyType: Type, defaultValue: any = null, options?: IProperty): any` — returns the default value, so a class field can be initialized inline: `this.caption = super.addProperty('caption', DG.TYPE.STRING, 'Recent projects')`. | Used in EVERY canonical dashboard-widget constructor. Property metadata becomes the gear-icon settings panel. NOT a placement control — placement is a function-level decorator option. | js-api/src/widgets/base.ts:345-365, packages/PowerPack/src/widgets/recent-projects-widget.ts:13,17, packages/PowerPack/src/widgets/community-widget.ts:11,26 |
| Specialized dashboard decorator | `@grok.decorators.dashboard(config?: DashboardOptions)` — declared at `js-api/src/decorators/functions.ts:286-292`. `DashboardOptions extends FunctionOptions` adds `order?: string` (placement) and `test?: string` (smoke-test invocation). Codegen emits matching `//output: widget result` + `//meta.role: dashboard` + `//meta.order: <n>` block in `package.g.ts`. | Place over a `static` method inside the package's `PackageFunctions` class. Method signature: `(): DG.Widget`. | js-api/src/decorators/functions.ts:195-198,216-218,286-292 |
| Header form (legacy / .g.ts) | Block `//name: <Title>` + `//output: widget result` + `//meta.role: dashboard` [+ `//meta.order: <n>`] + `//meta.canView: <Roles>` over a bare exported function. The codegen emits exactly this from a `@grok.decorators.dashboard` source. | Article shows this form. Modern `package.ts` should use the decorator; the codegenerated `package.g.ts` mirrors it as the header form. | help/develop/how-to/packages/home-page-widgets.md:16-21, packages/PowerPack/src/package.g.ts:17-32, packages/UsageAnalysis/src/package.g.ts:94-110 |
| Reference: `RecentProjectsWidget` | `class RecentProjectsWidget extends DG.Widget` — `super(ui.panel([], {style: {display: 'flex'}}))`, single `caption` property, async `getProjects()` populates `this.root` via `ui.wait(...)`. Note: class is exported but NO LONGER registered as a `dashboard` widget in current `package.ts` (see DRIFT-052). | Useful as a structural template (`extends DG.Widget` + `get type` + single `caption` property + async data fetch in constructor). Article points at this file. | packages/PowerPack/src/widgets/recent-projects-widget.ts:8-46 |
| Reference: `CommunityWidget` (Spotlight's sibling, registered) | `class CommunityWidget extends DG.Widget` — `super(ui.panel([], 'welcome-community-widget'))`, fetches community latest topics via `grok.dapi.fetchProxy(..., 48 * 60 * 60)`, appends 10 link rows. Registered via `@grok.decorators.dashboard({order: '6', name: 'Community'})` at `package.ts:149-155`. | The smallest end-to-end production example: 28 lines, real network call, real registration. Best primary template for new dashboard widgets. | packages/PowerPack/src/widgets/community-widget.ts:1-28, packages/PowerPack/src/package.ts:149-155 |
| Reference: `SpotlightWidget` (most complex, registered) | `class SpotlightWidget extends DG.Widget` — large home-page tabbed widget with action-required, shared, recent items. Registered via `@grok.decorators.dashboard({meta: {showName: 'false'}, order: '-1', name: 'Spotlight'})` at `package.ts:138-147`. | Reference for a multi-section / tabbed home-page widget that needs `showName: 'false'` to hide its caption header (since it renders its own). `order: '-1'` puts it first. | packages/PowerPack/src/spotlight/spotlight-widget.ts:17-22, packages/PowerPack/src/package.ts:138-147 |
| Reference: `UsageWidget` / `ReportsWidget` (visibility-restricted) | `class UsageWidget extends DG.Widget`, `class ReportsWidget extends DG.Widget` — registered with `@grok.decorators.dashboard({'meta': {'canView': 'Developers,Administrators'}, 'name': 'Usage', 'test': 'usageWidget()'})` at `packages/UsageAnalysis/src/package.ts:397-415`. Render a usage chart and a report list respectively, both backed by `UsageAnalysis:*` queries. | Reference for the `meta.canView` pattern — restrict a home-page widget to a comma-separated list of group names. `meta.test` provides a smoke-test invocation string for CI. | packages/UsageAnalysis/src/widgets/usage-widget.ts:7, packages/UsageAnalysis/src/widgets/reports-widget.ts:5, packages/UsageAnalysis/src/package.ts:397-415 |
| Reference: bare `widget` role (NOT home-page) | `Widgets:timeWidget` registered with `//output: widget result` only — NO `//meta.role: dashboard`. Such functions are addressable widgets but do NOT auto-render on the home page. | Negative example — confirms that the `dashboard` role tag is the load-bearing piece, not the `widget` output type alone. | packages/Widgets/src/package.g.ts:20-25 |

## Work with package files

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Package handle | `DG.Package` (`class Package extends Entity` — `js-api/src/entities/misc.ts:59-209`). Constructor accepts an opt. dart pointer or a webRoot string. | Module-level `export const _package = new DG.Package();` (Chem) or a subclass (Bio's `BioPackage`, HitTriage's `HTPackage`). The exported binding is the platform's per-package singleton; its `dart` pointer is set on first construction. | js-api/src/entities/misc.ts:59-209, packages/Chem/src/package.ts:1, packages/Bio/src/package.ts |
| `webRoot` getter | `get webRoot(): string` (settable). Returns the URL prefix under which the package's source tree is HTTP-served. Trailing slash included. | `_package.webRoot + 'images/icons/hit-triage-icon.png'` (HitTriage). Used for any URL-fetchable resource — README, images, CSVs in `tables/`. | js-api/src/entities/misc.ts:79-88, packages/HitTriage/src/app/pep-triage-views/info-view.ts:22,32 |
| `files` getter | `get files(): FilesDataSource` — returns `new DG.FilesDataSource('System:AppData/' + this.name)` on every access. | `_package.files.list('templates/', true)` (HitTriage utils.ts:162) — paths are relative to the published `files/` subtree (which mirrors source `files/`). | js-api/src/entities/misc.ts:193-195, packages/HitTriage/src/app/utils.ts:162 |
| `FilesDataSource` class | `class FilesDataSource` — read/write/list/move/rename methods over an AppData connection. Per-method signatures in DG-FACT-169 / DG-FACT-170. The `FileSource` symbol is a deprecated alias (`export const FileSource = FilesDataSource`). | `_package.files.<method>` is the canonical entry point; direct construction is rare (the platform also exposes `grok.dapi.files` for arbitrary connection-rooted access). | js-api/src/dapi.ts:1292-1470,1472-1475 |
| Reserved package directories | Source-tree convention. Names: `files/`, `tables/`. Subdirectories allowed; `package.json` lives at the package root. | 25+ shipping packages have `files/` (`Bio`, `EDA`, `HitTriage`, `Chem`, `PowerPack`, …); `ClinicalCase` is the only public package using `tables/`. | help/develop/how-to/packages/work-with-package-files.md:12-23, packages/Bio/files, packages/HitTriage/files, packages/ClinicalCase/tables |
| `grok.data.loadTable` | `loadTable(csvUrl: string): Promise<DataFrame>` on `Data` (the `grok.data` namespace). Backed by `api.grok_LoadDataFrame(csvUrl)`. | The standard URL-driven DataFrame loader; canonical pairing for `_package.webRoot + 'tables/<file>.csv'`. | js-api/src/data.ts:170-175 |
| Reference: Bio test data shipped under `files/tests/` | `packages/Bio/files/tests/...` accessed via `_package.files.readAsText('tests/<name>.csv')` in `packages/Bio/src/tests/utils.ts:11` and `tests/similarity-diversity-tests.ts:21,71`. | Shows the typical `files/<subdir>/<name>` pattern for test fixtures. | packages/Bio/files, packages/Bio/src/tests/utils.ts |
| Reference: HitTriage campaigns/templates layout under `files/` | `packages/HitTriage/files/<App>/campaigns/`, `packages/HitTriage/files/<App>/templates/`. Read with `_package.files.list(...)` then `_package.files.readAsText(<json>)`. | The richest example of using `files/` as a per-app data store with subdirectories. | packages/HitTriage/files, packages/HitTriage/src/app/utils.ts:162,540, packages/HitTriage/src/app/accordeons/new-template-accordeon.ts:21,27 |
| Reference: `tables/` CSV layout | `packages/ClinicalCase/tables/sdtm-terminology.csv`, `validation-rules.csv`. | The lone production example of the `tables/` reserved folder; CSVs are URL-fetchable via `_package.webRoot + 'tables/<name>.csv'`. | packages/ClinicalCase/tables |
| Storage path resolver | `nqNameToPath(connectionNqName)` — replaces `:` with `_`. Used to derive `System:AppData/PowerPack/enrichments/<connection-no-colon>/[<keyDb>/]<keySchema>/<keyTable>/<keyColumn>/<name>.json`. | Internal to PowerPack; relevant when scripting enrichment writes via `grok.dapi.files.writeAsText`. | packages/PowerPack/src/db-explorer.ts:675-677,665-672 |

## Custom script handlers

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Script-handler role token | `DG.FUNC_TYPES.SCRIPT_HANDLER = 'scriptHandler'` (string constant). Function-role descriptor table at `js-api/src/api/ddt.api.g.ts:395-424` declares all `scriptHandler.*` meta options the platform indexes. | The role string referenced by `//meta.role: scriptHandler` and by the function decorator's `meta.role` field. The startup loop tags the function with `scriptHandler` and only registers it when `scriptHandler.language` is non-empty. | js-api/src/const.ts:420, js-api/src/api/ddt.api.g.ts:395-424, js-api/src/datagrok/build/web/grok_shared.dart.js:137559-137563 |
| Handler input type | `funccall` (literal type token in the header `//input:` line) — the platform binds a `DG.FuncCall` to the parameter. The `DG.FuncCall` instance carries `.inputs` populated from the script's `#input:` declarations and `.outputs` for the handler to write. Canonical parameter name: `scriptCall`. | Pyodide's handler reads `scriptCall.func.script` (the user's code), spawns a worker, and writes results back via `setOutputs(scriptCall, response)`. | packages/Pyodide/src/package.g.ts:15,24, packages/Pyodide/src/package.ts:361-369 |
| Decorator surface (production-canonical) | `@grok.decorators.func({meta: {role: 'scriptHandler', 'scriptHandler.language': '<name>', 'scriptHandler.extensions': '<exts>', ...}})` over a `static async <name>(scriptCall: DG.FuncCall): Promise<void>` inside the package's `PackageFunctions` class. Codegen emits the matching `//meta.role:` + `//meta.scriptHandler.*` + `//input: funccall scriptCall` block in `package.g.ts`. | Pyodide's `pyodideLanguageHandler` is the only public-repo example. There is NO specialized `@grok.decorators.scriptHandler` decorator — the generic `func` decorator is the only path. | packages/Pyodide/src/package.ts:347-369, packages/Pyodide/src/package.g.ts:15-26 |
| Vectorization function shape | `(script: DG.Script) => string` — registered package function with `//input: script script` + `//output: string result`. Targeted by the handler's `meta.scriptHandler.vectorizationFunction: <Package>:<funcName>`. | Pyodide's `makeVectorCode(script: any): string` (`packages/Pyodide/src/package.ts:11-13`) wired via `'scriptHandler.vectorizationFunction': 'Pyodide:makeVectorCode'`. | packages/Pyodide/src/package.g.ts:9-13,21, packages/Pyodide/src/package.ts |
| Decorator-typed meta keys | `Meta` interface at `js-api/src/decorators/functions.ts:117-166` declares both bracket-form (`['scriptHandler.language']`) and camelCase (`scriptHandlerLanguage`) keys for all six `scriptHandler.*` options the codegen knows. | Use bracket-form keys when authoring the decorator; codegen accepts either. The interface is the closest thing to a typed schema for valid handler annotations. | js-api/src/decorators/functions.ts:151-162 |
| Reference: full production handler (Pyodide) | `packages/Pyodide/src/package.ts:347-369` (`PackageFunctions.pyodideLanguageHandler`) + `packages/Pyodide/src/package.g.ts:15-26` (codegen wrapper). Handler delegates to `prepareRequest` + `sendRequest` → web worker running Pyodide; writes outputs via `setOutputs`. | The richest (and only) production reference — demonstrates worker delegation, async script execution, debugLogger plumbing, and the `commentStart` / `templateScript` / `vectorizationFunction` annotations together. | packages/Pyodide/src/package.ts, packages/Pyodide/src/package.g.ts |
| Reference: vectorization function (Pyodide) | `PackageFunctions.makeVectorCode(script: any): string` at `packages/Pyodide/src/package.ts` — registered as `Pyodide:makeVectorCode` and pointed at by `meta.scriptHandler.vectorizationFunction`. | The minimal production reference for a vectorization function paired with a handler. | packages/Pyodide/src/package.ts, packages/Pyodide/src/package.g.ts:9-13 |

## Context actions

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Action meta key | `DG.FuncOptions.Action = 'action'` (`js-api/src/api/ddt.api.g.ts:329-330`). The string token under `meta.<key>` that the platform indexes when discovering context actions. | Set via header `//meta.action: <text>` or decorator `meta: {action: '<text>'}`. Value is the menu-item label. | js-api/src/api/ddt.api.g.ts:329-330 |
| Visibility-scope meta keys | `DG.FuncOptions.ExcludeActionsPanel = 'exclude-actions-panel'` (`js-api/src/api/ddt.api.g.ts:434`) and `DG.FuncOptions.ExcludeCurrentValueMenu = 'exclude-current-value-menu'` (`js-api/src/api/ddt.api.g.ts:436`). | Either or both as `//meta.exclude-actions-panel: true` / `//meta.exclude-current-value-menu: true` to scope WHERE the action surfaces. Default (neither) → both surfaces. | js-api/src/api/ddt.api.g.ts:434,436 |
| Cell-input bound type | `DG.SemanticValue` — bound to `//input: semantic_value <name> { semType: <T> }`. Carries `.value`, `.cell` (`DG.Cell` with `.column`, `.rowIndex`), `.cell.column.meta.units`. | Production canonical for cell-context actions (Chem `Use as filter`, `Sort by similarity`, `Copy as ...`; Bio `Edit Helm...`). | packages/Chem/src/package.g.ts:843-849, packages/Bio/src/package.g.ts (openEditor) |
| Column-input bound type | `DG.Column` — bound to `//input: column <name> { semType: <T> }`. Action surfaces on the column-HEADER right-click. | Production canonical for column-context actions (Chem `Convert Notation...` for Molecule + ChemicalMixture columns, Bio `To Atomic Level...` for Macromolecule columns). | packages/Chem/src/package.g.ts:765-769, packages/Bio/src/package.g.ts:391-396 |
| Multi-column-input bound type | `DG.Column[]` — bound to `//input: list <name> { type: numerical }` (or unannotated). Surfaces on multi-column selection. | PowerGrid `summarizeColumns` / `addFormColumn` — accept a list of selected columns and add a sparkline / form column derived from them. | packages/PowerGrid/src/package.g.ts:170-183 |
| Filter group accessor | `tv.getFiltersGroup(options?: {createDefaultFilters?: boolean}): DG.FilterGroup` — `js-api/src/views/view.ts:405`. The canonical entry point for adding/updating filters from a context action. | Pass `{createDefaultFilters: false}` from a plugin so the platform doesn't auto-add unwanted default filters; then call `.updateOrAdd({...}, false)` to attach the new filter. | js-api/src/views/view.ts:405, js-api/CLAUDE.md:151-154, packages/Chem/src/package.ts:2034-2039 |
| Reference: full production action set (Chem) | `packages/Chem/src/package.ts:1500-2120` — 9 distinct context actions on Molecule cells (`Use as filter`, `Sort by similarity`, `Copy as...` + 5 sub-formats, `Copy as Image`) plus column-level `Convert Notation...`. Header counterparts in `packages/Chem/src/package.g.ts:765-895`. | The richest production reference — demonstrates all three input types (`semantic_value`, `column`), both visibility toggles, decorator form, and the substructure-filter pattern. | packages/Chem/src/package.ts:1500-2120, packages/Chem/src/package.g.ts:765-895 |
| Reference: minimal column-action (Bio) | `packages/Bio/src/package.g.ts:391-396` (`toAtomicLevelAction(seqCol: DG.Column)` with `//input: column seqCol { semType: Macromolecule }` + `//meta.action: to atomic level`). | Cleanest production example of a `column`-input context action. | packages/Bio/src/package.g.ts:391-396 |
| Reference: multi-column action (PowerGrid) | `packages/PowerGrid/src/package.g.ts:170-183` — `summarizeColumns` (Sparklines) + `addFormColumn` (Smart form) on `list columns { type: numerical }` and unannotated `list columns`. | The only production examples of `list`-input context actions. | packages/PowerGrid/src/package.g.ts:170-183 |

## Manipulate viewers

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Universal attach API | `TableView.addViewer(v: ViewerType \| string \| Viewer, options?: any): Viewer`. Accepts type string, `Viewer` instance, or `ViewerType` enum value. | The ONE non-deprecated entry point — `view.histogram(opts)` / `view.scatterPlot(opts)` / etc. wrappers all carry `deprecated: use addViewer(Viewer.<type>(options))`. | js-api/src/views/view.ts:418-427 |
| Native viewer factory (sync) | `DG.Viewer.fromType(viewerType, table, options=null): Viewer`, plus typed statics `Viewer.scatterPlot`, `Viewer.histogram`, `Viewer.barChart`, `Viewer.boxPlot`, `Viewer.lineChart`, `Viewer.network`, `Viewer.calendar`, `Viewer.correlationPlot`, `Viewer.densityPlot`, `Viewer.form`, `Viewer.markup`, `Viewer.matrixPlot`, `Viewer.pcPlot`, `Viewer.pieChart`, `Viewer.scatterPlot3d`, `Viewer.statistics`, `Viewer.tile`, `Viewer.treeMap`, `Viewer.trellisPlot`, `Viewer.wordCloud`, `Viewer.grid`, `Viewer.heatMap`, `Viewer.filters`. | Used everywhere a CORE viewer is needed up-front (with full typed return). | js-api/src/viewer.ts:128-130,215-305 |
| DataFrame plot helper | `DataFramePlotHelper` exposed as `dataFrame.plot`. 10 sync shortcuts (`scatter, grid, tile, form, histogram, bar, heatMap, box, line, network`) plus async `fromType(viewerType, options)` for non-core/custom types. | `let v = data.plot.scatter({x:'weight', y:'height', color:'disease'}); view.addViewer(v);` | js-api/src/dataframe/data-frame.ts:551-572 |
| Viewer property descriptor | `Property` returned by `viewer.getProperties(): Property[]`. Field accessors: `name`, `propertyType: TYPE`, `semType`, `description`, `defaultValue`, `choices: string[]`, `columnTypeFilter: ColumnType \| 'numerical' \| 'categorical' \| null` (renamed from `columnFilter`), `validators: string[]`. | Used to inspect a viewer's options surface from the console or DevTools. | js-api/src/viewer.ts:166-168, js-api/src/entities/property.ts:142,163,174,182,194,236,248-251 |
| Per-view dock manager | `View.dockManager` getter → `DockManager` (`js-api/src/docking.ts:102`). Manages window layout INSIDE the view; viewers docked here are children of the view's dock node. | `view.dockManager.dock(viewer, DG.DOCK_TYPE.RIGHT)` — viewer moves/closes with its parent view. | js-api/src/views/view.ts:440-442 |
| Shell-root dock manager | `Shell.dockManager` getter → `DockManager`. Manages the platform root layout; viewers docked here float independently of any view. | `grok.shell.dockManager.dock(viewer, DG.DOCK_TYPE.RIGHT)` — viewer remains visible after the source view closes. | js-api/src/shell.ts:361-363 |
| Dock position constants | `DG.DOCK_TYPE`: `LEFT='left'`, `RIGHT='right'`, `TOP='up'`, `DOWN='down'`, `FILL='fill'`. **Asymmetric:** `TOP` value is `'up'`. Public type `DockType` is the literal-string union. | Always reference via the constant — `view.dockManager.dock(v, DG.DOCK_TYPE.TOP)`. | js-api/src/const.ts:775-782,1003 |
| Dock dispatch method | `DockManager.dock(element: HTMLElement \| Viewer, dockType: DockType = DG.DOCK_TYPE.LEFT, refNode: DockNode \| null = null, title?: string, ratio: number = 0.5): DockNode`. | Default position is `LEFT`; omitting `refNode` docks against the manager's root. | js-api/src/docking.ts:135-137 |
| Native viewer type registry | `DG.VIEWER` enum (32 entries) and `DG.CORE_VIEWER` enum (25 entries). The 7 in VIEWER but NOT CORE_VIEWER: `GLOBE, GOOGLE_MAP, WORD_CLOUD, TIMELINES, RADAR_VIEWER, SURFACE_PLOT, SCAFFOLD_TREE`. Use `DG.Viewer.getViewerTypes({core: true})` to enumerate CORE at runtime. | String values are exact (case-sensitive): `DG.VIEWER.SCATTER_PLOT === 'Scatter plot'`, `DG.VIEWER.PIE_CHART === 'Pie chart'`, `DG.VIEWER.PC_PLOT === 'PC Plot'`. | js-api/src/const.ts:670-733, js-api/src/viewer.ts:135-139 |
| Custom-init hook | `initializationFunction: string` field on every `IBaseSettings`-derived viewer-options interface. Names a registered Datagrok function whose single `viewer` input receives the new instance; runs at construction AND on layout/project restore. | Production canonical: `Chem`'s `activityCliffsInitFunction` — `@grok.decorators.func({name: 'activityCliffsInitFunction'})` over `static async activityCliffsInitFunction(@grok.decorators.param({type: 'viewer'}) sp: DG.ScatterPlotViewer)`. Wired via `view.scatterPlot({initializationFunction: 'activityCliffsInitFunction'})`. | js-api/src/interfaces/d4.d.ts:77,231,261, packages/Chem/src/package.ts:1218,1238-1256 |
| DevTools "To JavaScript" workflow | Right-click any viewer → "To JavaScript" → snippet that recreates the viewer with selected non-default options. Provided by the `DevTools` package (https://github.com/datagrok-ai/public/tree/master/packages/DevTools). | The fastest way to derive a `setOptions` payload without enumerating `getProperties()` by hand. | help/develop/how-to/viewers/manipulate-viewers.md:181-187, packages/DevTools |

## Show formula lines

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Abstract helper | `FormulaLinesHelper` (abstract) — uniform CRUD over the JSON storage. Subclasses implement `get/set storage: string`. Methods: `add`, `addAll`, `addLine` (sets `type='line'`), `addBand` (sets `type='band'`), `updateAt`, `removeAt`, `removeWhere`, `clear`. Reader: `items: FormulaLine[]` parses storage every read. Statics: `setDefaults`, `getMeta`, `getMetaByFormula`. | Subclassed twice in core; package code never extends it directly. | js-api/src/helpers.ts:85-143 |
| DataFrame-side helper | `DataFrameFormulaLinesHelper extends FormulaLinesHelper` — `storage` getter/setter wraps `df.getTag('.formula-lines')` / `df.setTag(...)`. Exposed as `df.meta.formulaLines`. | `demog.meta.formulaLines.addLine({formula:'${height} = 2 * ${weight}'})`. | js-api/src/dataframe/formula-helpers.ts:11-21 |
| Viewer-side helper | `ViewerFormulaLinesHelper extends FormulaLinesHelper` — `storage` reads/writes `viewer.props['formulaLines']`. Trellis Plot is special-cased — routes through `viewer.props['innerViewerLook']`'s `formulaLines` via `grok_PropMixin_GetPropertyValue/SetPropertyValue`. Exposed as `viewer.meta.formulaLines`. | `plot.meta.formulaLines.addLine({formula:'${weight} = 150', color:'#ff0000'})`. | js-api/src/viewer.ts:812-857 |
| Storage tag | `DG.TAGS.FORMULA_LINES = '.formula-lines'` — the dataframe-tag key holding the JSON-stringified list. | The viewer reconnects on attach: when a dataframe with `.formula-lines` tag is bound to a supporting viewer, the lines render automatically. | js-api/src/const.ts:309 |
| Item interface | `FormulaLine` — `{id?, type?: 'line'\|'band', title?, description?, color?, visible?, opacity?, zIndex?, min?, max?, formula?, showOnPlot?, showOnTooltip?, orientation?, width?, spline?, style?, column2?, xMap?, yMap?}`. | All fields optional at the type level, but `formula` is required at runtime for both kinds; `column2` is required for bands; `type` is required when calling generic `add()`. | js-api/src/helpers.ts:9-32 |
| Item meta | `FormulaLineMeta` — `{expression?, funcName?, argName?}`. Returned by static `getMeta(item)` / `getMetaByFormula(formula, type)`; describes the parsed formula's left-hand expression and any function-call decomposition. | Useful for inspecting / normalizing a user-edited formula before persistence. | js-api/src/helpers.ts:71-78, 140-142 |
| Visibility toggles | `showDataframeFormulaLines: boolean` (default `true`) and `showViewerFormulaLines: boolean` (default `true`) on `IScatterPlotSettings`, `ILineChartSettings`, `IDensityPlotSettings`, `IBoxPlotSettings`, `IHistogramSettings`, `IBarChartSettings`. Two-toggle design lets a viewer hide either source independently. | `view.addViewer(DG.Viewer.scatterPlot(df, {showDataframeFormulaLines: false, showViewerFormulaLines: true}))`. | js-api/src/interfaces/d4.ts:597,602,909,914,1199,1204,1605,1610,1932,1937,2331,2336 |
| Reference sample | `packages/ApiSamples/scripts/data-frame/metadata/formula-lines.js` — 13 lines + 3 bands across the demog frame, exercising every documented parameter (color/zIndex/opacity/min/max/width/spline/style/visible). | Mirrors the article's fragments and is the URL the article links to (https://public.datagrok.ai/js/samples/data-frame/metadata/formula-lines). | packages/ApiSamples/scripts/data-frame/metadata/formula-lines.js:1-186 |
| Editor dialog | "Formula Lines" dialog implemented in PowerPack — calls `viewer.meta.formulaLines.add(item)` on save. Narrows popup-menu items + column picker to match each viewer's supported axes (e.g., box plot only offers Y-axis lines and constant-RHS forms). | The dialog is the canonical place that enforces the per-viewer support matrix; the JS API does NOT validate. | packages/PowerPack/src/dialogs/formula-lines.ts:906,934 |

## Custom views

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Quick view factory | `Shell.newView(name='view', children?: object[], options?: string\|{}\|null): View` — builds via `View.create(options)`, sets `name`, appends children to `root`, calls `addView`. | `let v = grok.shell.newView('Custom View', [ui.divText('Hi!')]);` (article line 17) — the one-call ad-hoc form. | js-api/src/shell.ts:234-242 |
| Subclassable view base | `DG.ViewBase` (abstract subclassing target — extends `Widget`). Constructor `(params=null, path='', createHost=true)`. Default getters: `type='js-view-base'`, `helpUrl=null`, `name='New View'`, `description=''`, `entity=null`. Default no-op methods: `getIcon()→null`, `setIcon(icon)`, `saveStateMap()→null`, `loadStateMap(stateMap)`, `handlePath(urlPath)`, `acceptsPath(urlPath)→false`, `setRibbonPanels(panels, clear=true)`, `getRibbonPanels()`, `append`, `appendAll`, `detach`, `close`. | The package-side base for any URL-routable / state-serializable / icon-bearing view that isn't a `TableView`. | js-api/src/views/view.ts:45-204 |
| Concrete reference subclass | `NotebookView extends DG.ViewBase` — `packages/Notebooks/src/package.js:27-128`. Sets per-instance `this.TYPE = 'Notebook'` / `this.PATH = '/notebook'` (instance-only convention; not on `ViewBase`). Overrides `type`, `helpUrl`, `path`, `name`, `entity` getters and `getIcon`, `saveStateMap`, `loadStateMap`, `handlePath`, `acceptsPath` methods. Registered as a function with `tags: view`. | The only public package that ships a function-registered view; production canonical for the article's example (see DRIFT-084 — article snippet drifts from real code). | packages/Notebooks/src/package.js:27-128, 528-535 |
| Inline-from-app subclasses | `Flow/src/funcflow-view.ts:24` (FuncFlowView), `Tutorials/src/demo-app/demo-app.ts:24` (DemoView), `ClinicalCase/src/model/ClinicalCaseViewBase.ts:9`, `HitTriage/src/app/base-view.ts:8`, `PreclinicalCase/src/views/configuration-view.ts:14` and `study-summary-view.ts:25`, `NodeJSDemo/src/app/todo_app.ts:15`, `UsageAnalysis/src/test-track/app.ts:61` and `service_logs/service_logs.ts:5` and `tabs/ua.ts:16`, `DevTools/src/package-testing.ts:70` and `icon-tool.ts:7`, `PhyloTreeViewer/src/apps/tree-to-grid-app.ts:12`, `CruddyDemo/src/cruddy/app.ts:21`. | App-internal views — instantiated by `meta.role: app` functions, not registered as `tags: view`. Use these as templates when the view doesn't need to be a function output. | (see facts DG-FACT-217) |
| View-tag codegen entry | `FUNC_PARAMS.VIEW = 'view'` (`tools/bin/utils/func-generation.ts:76`); `typesToAnnotation['DG.ViewBase' \| 'ViewBase' \| 'DG.View' \| 'View'] = 'view'` (`func-generation.ts:128-131`). | TS authors using `@grok.decorators.func` over a method returning `DG.ViewBase` get `output: view result` for free; the `params: map`, `path: string` inputs must still be declared explicitly. | tools/bin/utils/func-generation.ts:76, 128-131 |

## Layouts

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Layout entity | `DG.ViewLayout extends Entity` — `fromJson(json)`, `fromViewState(state)`, `toJson()`, `viewState` getter/setter, `getUserDataValue(key)` / `setUserDataValue(key, value)`, `columns: ColumnInfo[]`. Sibling `DG.ViewInfo` is the parallel type for view metadata (different lifecycle — emitted only inside `OnViewLayoutXXX` events). | `let layout = view.saveLayout(); view.loadLayout(DG.ViewLayout.fromViewState(layout.viewState));` | js-api/src/entities/view-layout.ts:15-99 |
| Layouts data source | `LayoutsDataSource extends HttpDataSource<ViewLayout>` exposed as `grok.dapi.layouts`. Adds `getApplicable(df): Promise<ViewLayout[]>`; the rest is the inherited `list/find/first/save/delete/filter/order/page/count` surface. | `grok.dapi.layouts.getApplicable(df).then(layouts => view.loadLayout(layouts[0]));` | js-api/src/dapi.ts:111-113, 645-660 |
| Layout-matching tag (column id) | `DG.TAGS.LAYOUT_ID = 'layout-id'` — when set on both source and destination column tags, `getApplicable` matches by this id instead of column name+type. | `column.tags[DG.TAGS.LAYOUT_ID] = 'patient_age';` on both frames forces the match. | js-api/src/const.ts:283 |
| Semantic-type tag (matching escape hatch) | `DG.TAGS.SEMTYPE = 'quality'` — the third leg of the `getApplicable` matching algorithm. Set via `column.semType = '<type>'`. | Tag value examples in `js-api/src/const.ts:206-227` (`SEMTYPE.MOLECULE`, `SEMTYPE.PHONE_NUMBER`, etc). | js-api/src/const.ts:318, 205-227 |
| Reference: package-bundled `.layout` files | `packages/Chem/files/demo_files/*.layout` loaded via `_package.files.readAsText(...)` → `DG.ViewLayout.fromJson(...)` → `tv.loadLayout(...)`. A `DG.delay(100)` precedes the apply because viewer construction is Dart-side async even though `loadLayout` returns sync. | The canonical pattern for shipping pre-baked dashboards inside a plugin. | packages/Chem/src/demo/demo.ts:65-68, 181-196, 419-422 |
| Reference: layout state stored on a campaign template | `HitTriage` hit-design-app — campaign `template.layoutViewState` (string) and `template.localLayoutPath` (file-share path) are tried in order on apply (`fromJson` for the file path, `fromViewState` as fallback). Save-back uses `view.saveLayout()` and persists the result. | The richest production example of round-tripping layouts through both `fromJson` and `fromViewState` paths inside one feature. | packages/HitTriage/src/app/hit-design-app.ts:652-690, 1075 |
| UI access points | Top menu `View | Layout | Save to Gallery` (Ctrl+S) — calls `dapi.layouts.save(view.saveLayout())`. `View | Layout | Download` — serializes to a `.layout` file. `Browse > <table> > Export layout` — project-scoped layouts. | Skills targeting end users should reference these UI paths alongside the JS API; package code should call the JS API directly. | help/develop/how-to/views/layouts.md:24-26, 141 |

## JS API entry points

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Function registry | `Functions` (singleton at `grok.functions`) — `register({signature, run, tags?, isAsync?, namespace?, options?}): Func`, `call(name, params?)`, `eval(expr)`, `find(name)`, `get(name)`. | `grok.functions.register({signature: 'Widget jsWidget()', tags: 'Widgets', run: () => new ui.Widget(div)})`. | js-api/src/functions.ts:131-188 |
| Events bus (global) | `Events` (singleton at `grok.events`) — every getter returns an `rxjs.Observable<T>` produced by `__obs(eventId)`. Includes `onCurrentProjectChanged`, `onTableAdded`, etc. | `grok.events.onCurrentProjectChanged.subscribe(_ => grok.shell.info(grok.shell.project.name))`. | js-api/src/events.ts:1-60 |
| Dock manager (top-level) | `grok.shell.dockManager` (`DockManager`) — `dock(element, dockType, refNode?, title?, ratio=0.5)`. Use `DG.DOCK_TYPE.{LEFT, RIGHT, TOP, DOWN, FILL}` constants. | `grok.shell.dockManager.dock(ui.divText('first'), DG.DOCK_TYPE.RIGHT, null, 'First')`. | js-api/src/shell.ts:361-363 |
| Generic DOM-wrapping widget | `Widget<TSettings>` (exported from `datagrok-api/ui`, defined at `js-api/src/widgets/base.ts:231`). Wraps a DOM root and optional settings; `new ui.Widget(domElement)` is the constructor used in registered `'Widget jsWidget()'` functions. | `let w = new ui.Widget(myDiv); w.root` returns the DOM root. | js-api/src/widgets/base.ts:231 |
| Object handler base (custom semantic types) | `DG.ObjectHandler<T>` extended for any user-defined type. Article calls this `EntityMeta` — that's a stale name (DG-FACT-DRIFT-087). The Dart-bridge subclass `EntityMetaDartProxy` is internal. | See DG-FACT-059/060 + register-identifiers.md for the full extension surface and registration flow. | js-api/ui.ts:1651-1773 |
| AI namespace | `AI` exposed as `grok.ai` (re-export from `js-api/src/ai.ts`). | Cited in the article only by structural inventory; no first-line usage example. | js-api/src/ai.ts |

## Cheminformatics

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Chem JS-API namespace | `Chem` (re-exported as `grok.chem`) — wraps `Chem:*` server functions. Key exports: `searchSubstructure`, `findSimilar`, `getSimilarities`, `diversitySearch`, `mcs`, `rGroup`, `descriptors`, `descriptorsTree`, `svgMol`, `canvasMol`, `drawMolecule`, `checkSmiles`, `isMolBlock`. | Most callers route through `grok.chem.<fn>` rather than reaching into `Chem` directly. All search/similarity functions cache per-Column fingerprints — call them on the same Column instance to reuse the cache. | js-api/src/chem.ts:699-896 |
| Chem package (server-side implementations) | `packages/Chem/` — implements the Dart-named functions (`Chem:searchSubstructure`, `Chem:findSimilar`, `Chem:getDiversities`, `Chem:FindMCS`, `Chem:FindRGroups`, `Chem:chemDescriptors`, `Chem:canvasMol`, `Chem:drawMolecule`). | Skills that mention `grok.chem.<fn>` should also note that the `Chem` package must be installed/enabled on the deployment for those calls to resolve — the JS API is just the wrapper. | packages/Chem/src/package.ts |
| Chem RDKit Python scripts | `packages/Chem/scripts/` — RDKit-Python scripts published via Datagrok Scripting. The canonical reference set for "what server-side cheminformatics already exists." | Scaffold for new RDKit-Python scripts — copy a sibling, adjust the `#name`/`#input`/`#output` header annotations, drop in. | packages/Chem/scripts/ |
| Molecule SVG renderer | `grok.chem.svgMol(smiles, width=300, height=200, options?)` returning `HTMLDivElement`. Engine: OpenChemLib JS (cheminfo fork). Sync wrapper, async fill (dynamic `import('openchemlib/full.js')`). | Use for static badges, list items, dialog previews where the rendered molecule is decorative. Auto-detects molfile via `M  END` marker. | js-api/src/chem.ts:837-851 |
| Molecule canvas renderer | `grok.chem.canvasMol(x, y, w, h, canvas, molString, scaffoldSmarts?, options?)` returning `Promise<void>`. Engine: RDKit (server-side via `Chem:canvasMol`). Async; pass an existing `<canvas>` element. | Use for grid cell renderers, drag-drop sketches, anything that participates in the platform's RDKit pipeline. Scaffold highlight via SMARTS string. Default `options = { normalizeDepiction: true, straightenDepiction: true }`. | js-api/src/chem.ts:858-868 |
| Molecule div renderer (popup-aware) | `grok.chem.drawMolecule(molString, w?, h?, popupMenu=false): HTMLDivElement` — returns a div that's filled async by `Chem:drawMolecule`. Used as the platform default for `customRenderers` `'molecule'` rows. | The renderer string `'molecule'` in `customRenderers[].renderer` resolves to this function (DG-FACT, customRenderers). | js-api/src/chem.ts:870-877 |
| External sketcher (drag-target / set/get molfile) | `Chem.Sketcher` class (`js-api/src/chem.ts:283-...`) — wrapper around the active sketcher widget that fires `canvasMol` on `onChanged`. | Used in dialogs that ask for a molecule input; implementations of new sketchers register here. | js-api/src/chem.ts:283-410 |
| Sample gallery for chem JS-API patterns | `packages/ApiSamples/scripts/domains/chem/` — runnable code samples linked from the help article (substructure-search, similarity-scoring-sorted, similarity-scoring-scores, descriptors, diversity-search, mcs, r-group, mol-rendering, mol-atoms-bonds). | Canonical reference for "what does a working call look like" — copy from here when authoring new chem skills. | packages/ApiSamples/scripts/domains/chem/ |
| OpenChemLib (cheminfo fork) | npm `openchemlib` (imported via dynamic `import('openchemlib/full.js')` in `svgMol`). Used for client-side rendering, atom/bond manipulation. | When you need a client-only computation that doesn't justify a server round-trip. | https://github.com/cheminfo/openchemlib-js |
| RDKit JS (WebAssembly) | `@rdkit/rdkit` — RDKit C++ compiled to WASM, loaded into a Web Worker by the chem package. Backs `searchSubstructure` and similarity scoring on the client side. | The MinimalLib header (`Code/MinimalLib/minilib.h` in the RDKit repo) is the source-of-truth list of supported molecule notations. | https://github.com/rdkit/rdkit/blob/master/Code/MinimalLib/minilib.h |
| RDKit Python | RDKit Python toolkit; surfaced into Datagrok via Scripting. Reference scripts in `packages/Chem/scripts/`. | When the calculation is heavy, needs canonical RDKit semantics, or already exists as Python. | https://www.rdkit.org/docs/GettingStartedInPython.html |

## Docking (AutoDock)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| AutoDock service interface (cross-package boundary) | `IAutoDockService` (`libraries/bio/src/pdb/auto-dock-service.ts:50-71`) — `ready: boolean`, `awaitStatus(status, timeout?)`, `checkOpenCl()`, `dockLigand(receptor, ligand, autodockGpf, poseCount=30, posColName='poses')`, `dockLigandColumn(receptor, ligandCol, autodockGpf, poseCount=30, posColName='poses')`, `terminate()`. Resolved via the package-finder helper `getAutoDockService()` in the same file. | `import {getAutoDockService} from '@datagrok-libraries/bio/src/pdb/auto-dock-service'; const svc = await getAutoDockService(); await svc.awaitStatus('started', 30000); const posesDf = await svc.dockLigand(receptor, ligand, gpf, 10);` (DG-FACT-246) | libraries/bio/src/pdb/auto-dock-service.ts:50-81, packages/Docking/src/utils/auto-dock-service.ts:71-233 |
| AutoDock service implementation | `AutoDockService implements IAutoDockService` — talks to the `docking-autodock` Docker container via `grok.dapi.docker.dockerContainers.fetchProxy`. Routes: `/check_opencl` (GET), `/autodock/dock_ligand` (POST, via `Docking:dockLigandCached`), `/autodock/dock_ligand_list` (POST), `/autodock/kill_process` (POST). | Internal — package code outside `Docking/src/` should go through `getAutoDockService()` from bio-lib, not import this class directly. | packages/Docking/src/utils/auto-dock-service.ts:71-233 |
| Server-cached docking call | `Docking:dockLigandCached` (function with `meta.cache: 'all'`, cron `cache.invalidateOn: '0 0 1 * *'`). Argument tuple is `(jsonForm, containerId)` — the same form against the same container is returned from cache for up to a month. | `await grok.functions.call('Docking:dockLigandCached', { jsonForm: JSON.stringify(form), containerId: dc.id })`. (DG-FACT-247) | packages/Docking/src/package.ts:64-85 |
| Vector dock-results function (preferred public entry) | `Docking:getAutodockResults` — `(table: DataFrame, ligands: Column{semType:Molecule}, target: string, poses: int)` → `DataFrame{action:'join(table)'}`. `meta.vectorFunc: 'true'` so the platform calls it once per column. Joins `binding energy` and `pose` columns (plus AutoDock property columns added on user demand) onto `table` on resolution. | The function the AutoDock dialog (`Chem | Docking | AutoDock...`) ultimately calls. Skills targeting the public surface should call this rather than `IAutoDockService.dockLigandColumn` directly. | packages/Docking/src/package.ts:87-122 |
| Top-menu entry | `Chem | Docking | AutoDock...` (function `runAutodock`, `meta.role: hitTriageFunction`). Trailing `...` because it opens a dialog. (DG-FACT-DRIFT-095) | `await grok.functions.call('Docking:runAutodock', { table, ligands, target, poses })` if you need to invoke the dialog flow programmatically. | packages/Docking/src/package.ts:124-171 |
| Target-list provider | `Docking:getConfigFiles` → `string[]` of folder names under `System:AppData/Docking/targets` that contain a `.gpf` file. The dialog parameter wires this via `choices: 'Docking:getConfigFiles'`. | Use the same function name in a custom dialog if you need a target picker; do not re-implement the `.gpf` filter. | packages/Docking/src/package.ts:52-62,133 |
| AutoDock context-panel widget (per-pose) | `Docking:autodockWidget` (`@grok.decorators.panel`, `condition: 'Docking:isApplicableAutodock(molecule)'`, `meta: {role: 'widgets', domain: 'chem'}`) — fires for cells whose value contains `binding energy`. Renders the receptor + zoomed ligand via `currentTable.plot.fromType('Biostructure', {...})` and an AutoDock property table built from PDBQT REMARK lines. Appends BiostructureViewer's `mol3dAtomPickerLinkWidget` ("Link SMILES column" checkbox) when available. | Reference for any "show pose details in context panel" widget — handles the show/hide-properties path, hover-driven comparison-table swap, and SMILES-linking checkbox in one place. | packages/Docking/src/package.ts:178-269 |
| Pose viewer (Biostructure) | `currentTable.plot.fromType('Biostructure', { dataJson: BiostructureDataJson.fromData(receptorData), ligandColumnName, zoom: true })` — the BiostructureViewer plot type, Molstar-backed. Adds CSS class `bsv-container-info-panel` on `.root` when embedded in the context panel. | The platform-blessed receptor+ligand 3D viewer; do not reach for raw NGL or Molstar from skills. | packages/Docking/src/package.ts:211-217 |
| Reference targets shipped with the package | `packages/Docking/files/targets/{BACE1,kras,nmdar1}/{<name>.pdbqt, <name>.gpf}` — three production targets used as default content. Each is a folder named after the target with exactly one `.pdbqt` and one matching `.gpf`. | Templates for adding a new target — copy the structure into `System:AppData/Docking/targets/<your-name>/`. | packages/Docking/files/targets/ |
| Reference dataset for demos | `packages/Docking/files/demo_files/demo_dataset.csv` — small SMILES list used by the article and `Demo Docking` flow (`Bioinformatics | Docking` demo path). | Use as the dogfood input for any docking skill's verify-live floor. | packages/Docking/files/demo_files/demo_dataset.csv, packages/Docking/src/package.ts:271-280 |
| Demo / app entry | `Docking:demoDocking` (registered with `meta.demoPath: 'Bioinformatics | Docking'`) and `Docking:dockingView` (`@grok.decorators.app`, `meta.browsePath: 'Bio'`). | Two distinct entry-point flavors for skills to point users at: the in-platform demo gallery vs the standalone Docking app. | packages/Docking/src/package.ts:271-326 |
| AutoDock property descriptions (column tooltip text) | `AUTODOCK_PROPERTY_DESCRIPTIONS` map: `intermolecular (1)→Final Intermolecular Energy`, `electrostatic→Electrostatic Energy`, `ligand fixed→Moving Ligand-Fixed Receptor`, `ligand moving→Moving Ligand-Moving Receptor`, `total internal (2)→Final Total Internal Energy`, `torsional free (3)→Torsional Free Energy`, `unbound systems (4)→Unbound System's Energy`. | Use as the canonical source for AutoDock property labels — these match the REMARK lines AutoDock writes into pose PDBQT, so reusing the keys keeps `getRemarksFromPdb` parsing in sync. | packages/Docking/src/utils/constants.ts:21-29 |

## Define semantic-type detectors (data-shape, JS)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Detectors-class wrapper | `class <PackageName>PackageDetectors extends DG.Package` in a fixed-name file `<PackageRoot>/detectors.js`. The `PackageDetectors` suffix is mechanical and load-bearing. Class extends `DG.Package` so `this.logger` etc. are available. | `class BiostructureViewerPackageDetectors extends DG.Package { ... }` — five detectors plus an `autostart` panel in one class. | packages/BiostructureViewer/detectors.js:1-8 |
| Detector role constant | `DG.FUNC_TYPES.SEM_TYPE_DETECTOR = 'semTypeDetector'`. Detector functions tag with `//meta.role: semTypeDetector` (or via decorator) and have signature `(col: Column) => string`. | Header-form annotation block — `//meta.role: semTypeDetector` + `//input: column col` + `//output: string semType` directly above each detector method. | js-api/src/const.ts:367-370, js-api/src/decorators/functions.ts:471-477 |
| Sampling helper for shape detection | `DG.Detector.sampleCategories(column, check, min=5, max=10, ratio=1, minStringLength=1): boolean` — order-deterministic sample of distinct categories. Returns `false` silently if `column.type !== TYPE.STRING`. | `DG.Detector.sampleCategories(col, (s) => s.match(/^COMPND/m) && s.match(/^END/m) && (s.match(/^ATOM/m) \|\| s.match(/^HETATM/m)), 1)` — sample 1 category, must match all three PDB markers. | js-api/src/data.ts:278-313, packages/BiostructureViewer/detectors.js:14-30 |
| Detector scaffold CLI | `grok add detector <SemTypeName>` — CLI subcommand in `tools/bin/commands/add.ts`. Creates `detectors.js` (with the class wrapper) if missing; appends a method named `detect<SemTypeName>` body templated from `tools/entity-template/sem-type-detector.js`. | Run from package root after a fresh `grok create`; one CLI call per new detector. | tools/bin/commands/add.ts:268-290, tools/entity-template/sem-type-detector.js |
| Reference detector package (multi-detector with sampling, units, skipTest) | `packages/BiostructureViewer/detectors.js` — `detectPdb` (uses `meta.skipTest`, multi-format with `col.meta.units = 'pdb'` vs `'pdbqt'`, returns `'Molecule3D'`); `detectPdbId` (cheap path — `col.type` + `col.name` substring + 4-char-length sample; returns `'PDB_ID'`); plus mmCIF/MAE/MOL/SDF detectors. | The canonical "what does a real detectors.js look like" reference for skills. | packages/BiostructureViewer/detectors.js |
| Reference detector (statistics-based) | Article example `detectMagnitude` — guards on `col.type === DG.TYPE.FLOAT \|\| col.type === DG.TYPE.INT`, then `0 < col.min && col.max < 10` and `col.name.toLowerCase() === 'magnitude'`. | Template for any "is this column a normalized magnitude / probability / ratio" detector — use min/max bounds plus name match before assigning the semType. | help/develop/how-to/functions/define-semantic-type-detectors.md:60-71 |
| Detector test annotations | `//meta.skipTest: <reason>`, `//meta.testData: <csv-path>`, `//meta.testDataColumnName: <name>` — read by Datagrok's auto-test harness, not by the runtime detector. | `//meta.skipTest: #2596, Fix for test data in the utils library` — ticket reference is conventional. | help/develop/how-to/functions/define-semantic-type-detectors.md:90-127, packages/BiostructureViewer/detectors.js:9-13 |

## Custom filters

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Filter abstract base | `DG.Filter` (extends `DG.Widget`); abstracts `applyFilter()` and `filterSummary`. Owns `dataFrame`, `column`, `columnName`, `indicator`, `controls`, `caption` getter, and an `onRowsFiltering` subscription set up in `attach`. | The class every custom filter subclasses. Includes commented-out static factories (`forColumn`, `histogram`, `categorical`, `multiValue`, `freeText`) that hint at platform built-in filter types. | js-api/src/widgets/filter.ts:15-119 |
| Filter group host viewer | `DG.VIEWER.FILTERS` viewer (constructed via `DG.Viewer.filters(options?)` or `view.filters(options?)` — the latter `@deprecated`). Renders one filter row per entry in `IFiltersSettings.filters`. | `tv.addViewer(DG.Viewer.filters({filters: [{type: 'Widgets:radioButtonFilter', columnName: 'race'}]}))`. | js-api/src/viewer.ts:235, js-api/src/views/view.ts:489-494 |
| Filter settings interface | `IFiltersSettings` (`js-api/src/interfaces/d4.ts:1674-1710`) — viewer-level options bag. Key field: `filters: Array<{[index: string]: any}>` — each entry typically `{type: '<Pkg>:<funcName>', columnName: '<col>'}`. Other fields: `active`, `showHistogram`, `showHeader`, `showSearchBox`, `showCurrentRow`, `showBoolCombinedFilter`, `columnNames`. | Passed wholesale to `Viewer.filters(options)` / `view.filters(options)`. | js-api/src/interfaces/d4.ts:1674-1710 |
| Reference filter — single-choice categorical | `RadioButtonFilter` (`packages/Widgets/src/filters/radio-button-filter.ts`). `isFiltering = true` (always active); picks first categorical column; renders up to `Math.min(20, categories.length)` radio buttons. | The article-cited canonical example. Demonstrates `requestFilter()` on input change and `filter.set(i, indexes[i] === categoryIdx, false)` mask write. | packages/Widgets/src/filters/radio-button-filter.ts:12-78 |
| Reference filter — multi-value (separator-split) | `MultiValueFilter` (`packages/Widgets/src/filters/multi-value-filter.ts`). Splits cells by `' | '` separator; checkbox per unique token; `isFiltering` returns `super.isFiltering && <any-checkbox-unchecked>`. | Demonstrates the `super.isFiltering &&`-chained activity getter and predicate-based mask writing for cells that contain multi-value lists of identifiers. | packages/Widgets/src/filters/multi-value-filter.ts:6-92 |
| Reference filter — substructure (cheminformatics) | `Chem:substructureFilter` invoked via the cross-package pattern (`await grok.functions.call('Chem:substructureFilter')` then `view.filters({filters: [{type: 'Chem:substructureFilter', columnName: 'smiles'}]})`). | Canonical example of a filter that requires module pre-loading. | packages/ApiSamples/scripts/ui/viewers/filters/custom-filters.js:1-10 |
| Class-decorator function (custom-filter shortcut) | `grok.decorators.filter(options?: {name?, description?, semType?})` — class decorator at `js-api/src/decorators/functions.ts:54-60`. Returns no-op decorator at runtime; `FuncGeneratorPlugin` (datagrok-tools ^4.12.x) reads it at build time and emits the package.g.ts entry. | `@grok.decorators.filter({name: 'Radio Button Filter', description: 'Single option filter', semType: 'Country'}) export class RadioButtonFilter extends DG.Filter {}` (article-only — no in-tree consumer yet). | js-api/src/decorators/functions.ts:47-60, help/develop/advanced/decorators.md:46-53 |

## Info panels (context-panel widgets)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Widget base class (panel return type) | `DG.Widget<TSettings = any>` at `js-api/src/widgets/base.ts:231`. Constructor `Widget(widgetRoot: HTMLElement)` sets `this._root`. Override `get type(): string` for serialization id. `DG.Widget.fromRoot(el)` is the static factory shortcut. | Every info-panel function on the JS side returns an instance of (or subclass of) `DG.Widget`. | js-api/src/widgets/base.ts:231-273,374-376 |
| Semantic-value input wrapper | `DG.SemanticValue<T>` at `js-api/src/grid.ts:1349`. Properties: `value: T`, `cell: Cell`, `gridCell: GridCell`, `column: Column` (via `cell.column`), `semType: string`, `units: any`, `dataType: string`, `tags: {[k]: any}`. Static factories: `fromValueType`, `fromTableCell`, `fromGridCell`, `parse`. | The wrapper that a `//input: semantic_value <name>` slot delivers to a JS panel function. | js-api/src/grid.ts:1349-1398 |
| Panel decorator (JS/TS) | `grok.decorators.panel(config?: FunctionOptions)` — method decorator at `js-api/src/decorators/functions.ts:278-284`. No-op at runtime; codegen reads it and emits `//output: widget result` + `//meta.role: <...>,panel` + the standard header block. Accepts the full `FunctionOptions` interface (name, description, condition, meta, tags, helpUrl, etc.). | `@grok.decorators.panel({name: 'Biology | Admetica', description: '…', meta: {role: 'widgets', domain: 'chem'}})` over `static async admeticaWidget(semValue: DG.SemanticValue): Promise<DG.Widget>` at `packages/Admetica/src/package.ts:30-40`. | js-api/src/decorators/functions.ts:168-187,278-284 |
| Panel registration — header form (auto-emitted) | The five-line header block: `//name: <Name>` + `//input: <type> <param>{<options>}` + `//output: widget result` + `//meta.role: <…>panel` + `//condition: <expr>`. 16 panel functions in `packages/*/src/package.g.ts` follow this exact shape. | `packages/Bio/src/package.g.ts:50-58` (Get Region panel — header + decorator coexist); `packages/Admetica/src/package.g.ts:9-17`. The header form is the codegen output, not hand-authored. | packages/*/src/package.g.ts (16 occurrences) |
| Reference: column panel (Macromolecule) | `packages/Bio/src/package.ts:188-200` — `@grok.decorators.panel({name: 'Bioinformatics | Get Region', description: '…', tags: ['panel']})` over `static getRegionPanel(@grok.decorators.param({options: {semType: 'Macromolecule'}}) seqCol: DG.Column): DG.Widget`. Receives the whole column (not a single value). | The canonical column-typed panel reference. | packages/Bio/src/package.ts:188-200, packages/Bio/src/package.g.ts:50-58 |
| Reference: semantic_value panel (Molecule) | `packages/Admetica/src/package.ts:30-40` — `@grok.decorators.panel({name: 'Biology | Admetica', meta: {role: 'widgets', domain: 'chem'}})` over `static async admeticaWidget(@grok.decorators.param({name: 'smiles', options: {semType: 'Molecule'}}) semValue: DG.SemanticValue): Promise<DG.Widget<any>>`. Calls `Chem:convertMolNotation` to normalize the molecule, then `getModelsSingle(smiles, semValue)`. | The canonical `semantic_value`-typed panel reference. Demonstrates the `meta: {role: 'widgets', …}` → `//meta.role: widgets,panel` codegen pattern. | packages/Admetica/src/package.ts:30-40, packages/Admetica/src/package.g.ts:9-17 |
| Reference: cross-package condition predicate | `packages/Boltz1/src/package.g.ts:44-58` — panel `Boltz-1` with `//condition: Boltz1:isApplicableBoltz(molecule)` referencing a sibling `//input: string molecule //output: bool result` predicate function. Same pattern in `packages/Docking/src/package.g.ts:66` (`Docking:isApplicableAutodock`). | The canonical "expensive condition expressed as a registered function" reference. The predicate is just another package function with a known input/output shape; the platform calls it during context evaluation. | packages/Boltz1/src/package.g.ts:44-58, packages/Docking/src/package.g.ts:66 |
| Reference: text-file panel with detector-defined predicate | `packages/NLP/detectors.js:5-10` defines `isTextFile(file): bool` over a hardcoded extension list (`doc/docx/odt/pdf/rtf/txt`); `packages/NLP/src/package.g.ts:4-10` uses it as `//condition: isTextFile(textfile)` on the Translation panel. | Demonstrates that condition predicates can live in `detectors.js` (alongside semantic-type detectors) — same registration model. | packages/NLP/detectors.js:5-10, packages/NLP/src/package.g.ts:4-10 |
| Reference: panel script (Python) with dataframe-join | `packages/Curves/scripts/calculateMSR.py:13` — `#output: dataframe res {action:join(table)}`. Pattern: receive `dataframe table` + `column smiles` inputs, compute per-row values, return as a single-column-or-more dataframe joined back. | The canonical "panel that augments the source table" reference. Pair with `# meta.role: panel` for context-panel visibility. | packages/Curves/scripts/calculateMSR.py |
| Server-side panel: viewer output | A `grok`-language script returning `# output: viewer plot` where `plot = table.ScatterPlot(<x>, <y>, <color?>, <size?>)`. Inside Grok-script, viewer factory methods on `dataframe` map 1:1 to JS viewer constructors. | Article-canonical sample at `add-info-panel.md:191-203` — Scatter plot panel that fires only when the demog table is open. | help/develop/how-to/ui/add-info-panel.md:191-203 |
| Server-side panel: graphics output | A `grok`-language script returning `# output: graphics pic` where `pic = <function-that-renders-image>(...)`. Image is rendered server-side (e.g., R/Python plot) and pushed to the context panel. | Article-canonical sample at `add-info-panel.md:222-232` — Spectrogram panel calling an R function via `Spectrogram("eeg", signal, 256.0, 1024, 0.1, true)`. | help/develop/how-to/ui/add-info-panel.md:222-232 |
| Server-side panel: action markup output | A script returning `# output: string actions {action: markup}` whose value is a Grok-script markup string with `#{button("Label", "<grok-script-call>")}` tokens. Buttons render as inline action UI. | Article-canonical sample at `add-info-panel.md:240-250` — Transaction review panel exposing a "Flag as suspicious" button that posts to a server. | help/develop/how-to/ui/add-info-panel.md:240-250 |

## Package tests

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Test framework module (canonical) | `@datagrok-libraries/test` (`libraries/test/package.json:2`, version `^1.1.0`+). Module entry `libraries/test/src/test.ts` exports `category`, `test`, `expect`, `expectFloat`, `expectTable`, `expectObject`, `expectArray`, `before`, `after`, `delay`, `runTests`, `TestContext`, `tests`, `initAutoTests`, `testEvent`, `testEventAsync`. Constants in scope: `STANDART_TIMEOUT = 30000` (sic), `BENCHMARK_TIMEOUT = 10_800_000`. | `import { category, test, expect } from '@datagrok-libraries/test/src/test';` at the top of every test file. | libraries/test/src/test.ts:1-304, libraries/test/package.json |
| `Test` registration class | `Test` at `libraries/test/src/test.ts:89-123` — constructor `(category, name, testFn, options?)` defaults `options.timeout` to `STANDART_TIMEOUT (30000)`. Wraps the user fn into a promise that catches/rejects and resolves with the stringified return value. Pushed into the shared `tests` registry under `currentCategory`. | Indirectly via `test('name', async () => {...})` — never instantiated by package code. | libraries/test/src/test.ts:89-123 |
| `Category` record | `Category` at `libraries/test/src/test.ts:125-138` — `{tests?: Test[], before?, after?, beforeStatus?, afterStatus?, clear?, timeout?, benchmarks?, benchmarkTimeout?, stressTests?, owner?}`. `category(name, fn, options?)` flips `currentCategory`, runs `fn()`, then stamps the options. | Read by `runTests(...)` to iterate registered suites. | libraries/test/src/test.ts:125-138,294-303 |
| Package-test entry template | `tools/package-template/src/package-test.ts:1-21` — boilerplate that every package's `src/package-test.ts` derives from. Exports `_package`, `tests`, declares `//name: test` and `//name: initAutoTests` platform-callable functions. | `grok create --test` (full package scaffold) and `grok add tests` (existing-package retrofit) both write this file verbatim. | tools/package-template/src/package-test.ts, tools/bin/commands/add.js:268-271 |
| Test-example template | `tools/entity-template/test.ts:1-16` — single `category('Examples')` with `Success`, `Fail`, `Skipped` examples. Still imports from the legacy `@datagrok-libraries/utils/src/test` path. `grok add tests` copies this to `src/tests/test-examples.ts`. | The skeleton example file in newly-scaffolded packages. Author replaces / extends it. | tools/entity-template/test.ts, tools/bin/commands/add.js:274 |
| Webpack test entry | A second entry on `webpack.config.js` — `test: {filename: 'package-test.js', library: {type: 'var', name: '${packageName}_test'}, import: './src/package-test.ts'}`. Built into a separate `package-test.js` bundle alongside `package.js`. | Written by `grok add tests` (`tools/bin/commands/add.js:257`). The platform loads `package-test.js` lazily when `initAutoTests` calls `_package.getModule('package-test.js')`. | tools/bin/commands/add.js:253-258, tools/package-template/src/package-test.ts:19 |
| Reference: standard category file | `packages/Chem/src/tests/chemprop-tests.ts` — imports `category`, `test`, `before`, `expect` from `@datagrok-libraries/test/src/test`; uses `before(async () => {...})` for shared setup; passes `{timeout, skipReason: 'GROK-19084'}` per test and `{timeout}` per category. | The canonical "category with setup + multiple tests + per-test/per-category options" reference. | packages/Chem/src/tests/chemprop-tests.ts:1-40 |
| Reference: package-test.ts index | `packages/Chem/src/package-test.ts` — 30+ side-effect imports of `./tests/<name>` files plus the standard `runTests` + `initAutoTests` block. Demonstrates the "one file per category" convention. | The canonical "large package, many categories" reference. | packages/Chem/src/package-test.ts |
| `//test:` annotation runner | Embedded in `initAutoTests` (`libraries/test/src/test.ts:325-380`). Reads each function's `f.options['test']` (an array of strings), parses inline sub-options via the `skip\|wait\|cat\|timeout` regex, registers a `Test` per entry under the `cat:`-named (or `'Auto Tests'`) category. | The platform-side path that turns `//test: square(2) == 4` annotations into runnable tests. No user-side API surface — purely metadata-driven. | libraries/test/src/test.ts:325-380 |
| Reference: `//test:` annotation in a demo function | `packages/BiostructureViewer/src/package.g.ts:274,283` — `//test: demoBioDockingConformations() //wait: 3000, timeout: 60000`. Demos use the same `//test:` channel but with the call-and-wait pattern rather than a boolean expression. | The canonical demo-style auto-test reference. | packages/BiostructureViewer/src/package.g.ts:274,283, packages/Curves/src/package.g.ts:28 |

## Test packages (runners + utilities)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Async-condition poller | `awaitCheck(checkHandler, error?, wait?, interval?): Promise<any>` — exported from `@datagrok-libraries/test/src/test`. Internally uses `setInterval`+`setTimeout` race. Defaults `wait=500`, `interval=50`. (DG-FACT-352) | DOM-wait idiom: `await awaitCheck(() => document.querySelector('canvas') !== null, 'cannot load table', 3000)` (article line 59). | libraries/test/src/test.ts:852-867 |
| Fixed delay primitive | `delay(ms: number): Promise<void>` — `setTimeout`-Promise wrapper. (DG-FACT-353) | `await delay(1000)` to let an animation settle before assertions. | libraries/test/src/test.ts:848-850 |
| Async-throw asserter | `expectExceptionAsync(action, check?): Promise<void>` — catches and validates exceptions from an async action. (DG-FACT-354) | `await expectExceptionAsync(() => grok.functions.call('nonExistingFunction'))` (article line 104). | libraries/test/src/test.ts:901-916 |
| Viewer lifecycle harness | `testViewer(viewerName, df, options?): Promise<void>` — open/close, mutate, save-layout, load-layout, fallback-df rerun. Options: `detectSemanticTypes`, `readOnly`, `arbitraryDfTest`, `packageName`, `awaitViewer`. (DG-FACT-355) | Chem viewers — `await testViewer('Chem Similarity Search', smiles, {detectSemanticTypes: true})` (article line 76); async-rendered viewers pass `awaitViewer: async (v) => await awaitCheck(...)` (article lines 86-91). | libraries/test/src/test.ts:920-975, libraries/test/src/test-viewer-utils.ts |
| Test Manager view | `TestManager extends DG.ViewBase` at `packages/DevTools/src/package-testing.ts:70`. Top-menu: `Tools | Dev | Test Manager` (registered at `packages/DevTools/src/package.ts:71`). URL prefix constant `APP_PREFIX = '/TestManager/'` (line 66). Deep link: `/apps/DevTools/TestManager/<Package>/<Category>/<Test>` + optional `?run=true`. (DG-FACT-357) | Browse all package tests in a tree, filter via the search input, run a selected node from ribbon/keyboard/context. | packages/DevTools/src/package-testing.ts:66-150, packages/DevTools/src/package.ts:70-76 |
| `grok test` CLI | `tools/bin/commands/test.ts` — builds + publishes + runs the package's `//name: test` platform function. Flags documented at `tools/README.md:122-142`. (DG-FACT-356) | Local pre-CI run: `cd <pkg> && grok test --host=dev --gui --csv`. | tools/bin/commands/test.ts, tools/README.md:122-142 |
| Platform-callable `test` function | `//name: test //input: string category {optional:true} //input: string test {optional:true} //output: dataframe result` declared in every `src/package-test.ts`. Returns `DG.DataFrame.fromObjects(await runTests({...}))`. (DG-FACT-358, DG-FACT-290) | Console invocation: `ApiTests:test(category="Layouts", test="ViewLayout.toJson()")`. | tools/package-template/src/package-test.ts:1-15, packages/ApiTests/src/package-test.ts:67-81 |
| Reference: comprehensive test library | `packages/ApiTests/` — 49 category modules covering all JS-API surfaces. `src/package-test.ts:67-90` adds two cross-cutting top-menu entries (`testPlatform`, `testPackages`) for sweeping multi-package runs via `DG.Func.find({name: 'test'})`. (DG-FACT-360) | The "what does a thorough test tree look like" reference cited by the article. | packages/ApiTests/src/package-test.ts:1-128 |
| Reference: viewer testing with async render | `packages/Chem/src/tests/*` — Chem viewers (`Chem Similarity Search`, `Chem Diversity Search`, etc.) are exercised via `testViewer` with `awaitViewer` hooks that wait on a viewer-specific selector. (DG-FACT-355) | The canonical "async-rendered viewer needs `awaitViewer`" reference. | packages/Chem/src/tests, help/develop/how-to/tests/test-packages.md:81-94 |
| CI workflow | `.github/workflows/packages.yml` — fires on every commit; for each changed package spins up a fresh Datagrok instance from latest docker, publishes the package, runs `grok test`. Artifacts: `test-console-output.log`, `test-record.mp4`, `test-report.csv`. NPM publish only on `master`. (DG-FACT-359) | Manually triggerable via Actions → Packages → Run workflow with a space-separated package list. | help/develop/how-to/tests/test-packages.md:130-153 |

## Build an app

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| App decorator | `grok.decorators.app(config: AppOptions)` — method decorator at `js-api/src/decorators/functions.ts:246-252`. `AppOptions extends FunctionOptions { browsePath?, icon?, url? }`. `config` is REQUIRED (no `?` on the parameter). No-op at runtime; codegen reads it and emits `//name`, `//meta.role: app`, `//meta.browsePath`, `//meta.icon` into `package.g.ts`. Decorated method may return `void`, `Promise<DG.View>`, or `Promise<DG.ViewBase>`. | `@grok.decorators.app({'name': 'MPO profiles', 'meta': {browsePath: 'Chem', icon: 'images/mpo.png'}}) static async mpoProfilesApp(@grok.decorators.param({options: {metaUrl: true, optional: true}}) path?: string): Promise<DG.View>` at `packages/Chem/src/package.ts:2919-2925`. | js-api/src/decorators/functions.ts:189-193,246-252 |
| AppTreeBrowser decorator | `grok.decorators.appTreeBrowser(config?: AppTreeBrowserOptions)` — method decorator at `js-api/src/decorators/functions.ts:390-396`. `AppTreeBrowserOptions extends FunctionOptions { app? }`. `config` is OPTIONAL. The `app` key binds to a sibling `@app` method's `name`. Decorated method signature: `(treeNode: DG.TreeViewGroup): Promise<void>`. | `@grok.decorators.appTreeBrowser({app: 'Benchling'}) static async benchlingLinkAppTreeBrowser(treeNode: DG.TreeViewGroup): Promise<void>` at `packages/BenchlingLink/src/package.ts:47-48`. | js-api/src/decorators/functions.ts:207-209,390-396 |
| Tree population API | `DG.TreeViewGroup` at `js-api/src/widgets/tree.ts:146`. Methods: `group(text, value?, expanded?, index?): TreeViewGroup` (`tree.ts:231`), `item(text, value?): TreeViewNode` (`tree.ts:241`). `DG.TreeViewNode.onSelected: Observable<TreeViewNode>` (`tree.ts:138`); `node.value` can carry a `DG.Func` for default-handler preview. | `treeNode.item(label).onSelected.subscribe((_) => grok.shell.info('selected'))` (article snippet, `build-an-app.md:130-133`); production — `const node = treeNode.item(label); node.value = DG.Func.byName(funcName); node.onSelected.subscribe(...)` (`packages/BenchlingLink/src/package.ts:49-58`). | js-api/src/widgets/tree.ts:82-260 |
| Reference: tree browser with DG.Func bindings | `packages/BenchlingLink/src/package.ts:47-95` — populates the tree with one `item` per resource; each `node.value = DG.Func.byName(funcName)` so the platform's default object-handler renders the function's preview on selection. | The canonical "tree browser as function index" reference. | packages/BenchlingLink/src/package.ts:47-95 |
| Reference: tree browser with nested groups | `packages/CddVaultLink/src/package.ts:120-150` — fetches a list of vaults at tree-build time, creates a `treeNode.group(vault.name)` per vault, then nested items underneath. | The canonical "tree browser with dynamic, async-built hierarchy" reference. | packages/CddVaultLink/src/package.ts:120-150 |
| Reference: app + tree browser pairing (demo) | `packages/Tutorials/src/package.ts:171-172` — `@grok.decorators.appTreeBrowser({app: 'Demo'}) static async demoAppTreeBrowser(treeNode: DG.TreeViewGroup): Promise<void>` linked to the Tutorials package's `@app({name: 'Demo'})` method. | The reference closest to the article's own `demoApp` + `demoAppTreeBrowser` example. | packages/Tutorials/src/package.ts:171-172 |
| `grok add app` template (LEGACY) | `tools/entity-template/app.js:1-7` — appends `//name: <NAME>` + `//meta.role: app` + `export function <NAME>() { grok.shell.info('Hello!'); }` to `src/package.js`. Bare-function/header form only — no decorator, no TS file, no `PackageFunctions` class. | The output the CLI produces today. Article (`build-an-app.md:18-23`) recommends `grok add app`; production packages do NOT use this shape. Treat the CLI output as throwaway and rewrite as `@grok.decorators.app(...)` in `src/package.ts`. (DG-FACT-DRIFT-BUILD-APP-001) | tools/entity-template/app.js, tools/bin/commands/add.ts:122-136 |

## Cache function results

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Client-cache control surface | `grok.functions.clientCache` (getter at `js-api/src/functions.ts:134`) → `ClientCache` instance (`functions.ts:193-212`). Methods: `clear(metaId?: string): Promise<void>`, `start(): Promise<void>`, `stop(): void`, `cleanup(): Promise<void>`, `getRecordCount(): Promise<number>`. Getter: `isRunning`. All methods are Dart-backed (`grok_ClientCache_*` bindings). | `packages/ApiSamples/scripts/functions/caching-results.js:7` — `//grok.functions.clientCache.clear();` (commented one-liner; demonstrates the canonical reset-before-test idiom). | js-api/src/functions.ts:134,193-212 |
| Function-level `Meta` cache fields | `Meta.cache?: string` (`js-api/src/decorators/functions.ts:118`), `Meta.cacheInvalidateOn?: string` (`functions.ts:119`), `Meta['cache.invalidateOn']?: string` (`functions.ts:120` — string-keyed dotted alias). Same fields are mirrored at the top level on `FunctionOptions` (`functions.ts:178-180`), so both `@func({meta: {cache: ...}})` and `@func({cache: ...})` are valid. | `packages/Chem/src/package.ts:1669-1675` — `meta: {cache: 'client', cacheInvalidateOn: '0 * * * *'}` on `synthonSearchFunc` (`meta:` wrapper form). `packages/MolTrack/src/package.ts:155-161` — `{cache: 'all', cacheInvalidateOn: '0 0 1 * *'}` at the top level on `fetchCompoundProperties` (no `meta:` wrapper). | js-api/src/decorators/functions.ts:117-120,168-187 |
| Connection-level cache properties type | `DataConnectionCacheProperties` (`js-api/src/entities/types.ts:33-40`) — `{cacheResults?: boolean; cacheSchema?: boolean; cacheInvalidateSchedule?: string}`. Mixed into `DataConnectionProperties` (`types.ts:73`). Note the schedule field is named `cacheInvalidateSchedule` at the connection level, NOT `cacheInvalidateOn` (which is the function-level name). | `packages/DBTests/connections/cached-postgresql-test.json:5-11` — canonical reference showing all three keys (`cacheResults: true`, `cacheInvalidateSchedule: '0 1 * * *'`) without `connString`. `packages/Chembl/connections/chembl.json:1-12` — `cacheResults: true` alone. | js-api/src/entities/types.ts:29-40,73 |
| CLI cache validator | `cacheValues` constant at `tools/bin/utils/utils.ts:191` — `['all', 'server', 'client', 'true']`. Header parser switch at `tools/bin/commands/check.ts:752-762` accepts only `meta.cache`, `meta.cache.invalidateOn`, and `meta.invalidateOn` (the last is rejected at `check.ts:446-447` as "Can't use invalidateOn without cache"). `f.cache` is validated against `cacheValues` at `check.ts:448-449`; `f.invalidateOn` is validated against `utils.isValidCron(...)` at `check.ts:450-451`. | The `grok check` build-time validator. Skill output that emits unknown `meta.cache.*` keys (e.g., `meta.localCache`, `meta.invalidate` — see `Chembl/queries/activityDetailsForTarget.sql:8-9`) will be SILENTLY dropped — they don't error, they just don't take effect. | tools/bin/commands/check.ts:446-451,752-762, tools/bin/utils/utils.ts:191 |
| Reference: function-level cache (script header) | `packages/BioNeMo/scripts/esmfold.py:4-5` — `#meta.cache: all` + `#meta.cache.invalidateOn: 0 * * * *` in a Python script. `packages/Chembl/queries/activityDetailsForTarget.sql:7` — `--meta.cache: true` in a SQL query. | The canonical "script header" form for cache annotation, across language families. | packages/BioNeMo/scripts/esmfold.py:1-10, packages/Chembl/queries/activityDetailsForTarget.sql:1-10 |
| Reference: dynamically-registered cached function | `packages/ApiSamples/scripts/functions/caching-results.js:10-23` — `grok.functions.register({signature, run, options: {cache: 'true', 'cache.invalidateOn': '* * * * *'}})`. Shows `cache`/`cache.invalidateOn` as `options` keys on the registration descriptor. | The canonical pattern for cache annotation on a function that isn't a decorated/declared package function. | packages/ApiSamples/scripts/functions/caching-results.js:1-35 |

## Manage credentials

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Credentials entity | `DG.Credentials` (`extends Entity`) — properties `parameters: any` (MapProxy over real values, returned hidden to non-owners) and `openParameters: Record<string, string>` (redacted display copy). Constructed via the Dart bridge; never `new`-ed by package code. | Read your own package's: `_package.getCredentials()`. Read any entity's: `await grok.dapi.credentials.forEntity(entity)`. Save mutations: `await grok.dapi.credentials.save(c)`. | js-api/src/entities/data-connection.ts:396-415, js-api/src/dapi.ts:624-642 |
| Package credentials accessor | `Package.getCredentials(): Promise<Credentials \| null>` (`js-api/src/entities/misc.ts:163-166`). Returns null when no credentials set OR caller is not in the owner group. Bridge: `grok_Package_Get_Credentials(name)`. | One-shot: `const c = await _package.getCredentials(); if (c == null) ... else c.parameters['login']`. | js-api/src/entities/misc.ts:163-166 |
| CredentialsDataSource | `grok.dapi.credentials` → `CredentialsDataSource extends HttpDataSource<Credentials>` (`js-api/src/dapi.ts:624-642`). Methods: `forEntity(e: Entity): Promise<Credentials>`, `save(c: Credentials): Promise<Credentials>`. | Mutate-and-save flow: load via `forEntity(conn)`, modify `c.parameters['password'] = '...'`, call `save(c)`. | js-api/src/dapi.ts:624-642 |
| Reference: end-to-end set + read cycle | `packages/NLP` — Python setter `packages/NLP/aws/nlp-user.py:40-54` (boto3 generates an AWS access-key pair → POST `/api/credentials/for/Nlp`) paired with TS reader `packages/NLP/src/package.ts:104-115` (`await _package.getCredentials(); if (c == null) ...; c.parameters['accessKeyId']`). | The article (`manage-credentials.md:67-74`) names this package as the canonical full-cycle example. The setter uses a service-user API key — see `nlp-user.py:50-51`. | packages/NLP/aws/nlp-user.py, packages/NLP/src/package.ts:104-115,208-217 |
| Reference: minimal read-and-display sample | `packages/ApiSamples/scripts/misc/package-credentials.js:1-8` — loads the `Api Samples` package via `grok.dapi.packages.filter('Api Samples').first()`, calls `getCredentials()`, and shows `c.openParameters` in a `grok.shell.info(...)` toast. | Use `openParameters` for displaying-back-to-user demos; use `parameters` when actually using the secrets to call an external service. | packages/ApiSamples/scripts/misc/package-credentials.js:1-8 |
| Reference: connection JSON with literal `credentials.parameters` | `packages/DBTests/connections/postgresql-test.json:10-15`, `packages/DBTests/connections/postgres-db-tests.json:10-15` — both check in literal login/password values inside `credentials.parameters`. The platform extracts that block at deploy time and routes it through the credentials store. | The two production patterns: (a) check in literal values (private repo, test fixtures), (b) omit the block and POST to `/api/credentials/for/<Pkg>.<Conn>` after deploy. There is NO `${VAR}` env-var substitution syntax in connection JSON. | packages/DBTests/connections/postgresql-test.json, packages/DBTests/connections/postgres-db-tests.json |

## UI primitives — layouts and containers (advanced/ui.md)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Simple container (height from children) | `ui.div(items, options?)` → `HTMLDivElement`. Variants: `ui.divH(items)` (flex row), `ui.divV(items)` (flex column). | `ui.div([ui.h1('Header'), ui.p('para'), DG.Viewer.scatterPlot(df)])`. | js-api/ui.ts:394,444,453 |
| Fixed-size shrink container | `ui.box(child, options?)` → `HTMLDivElement`. Used to clip / scroll oversized children. | Place a long `ui.div(...)` inside `ui.box(...)` for vertical scroll. | js-api/ui.ts:1837 |
| Panel container (full-width, 10px padding) | `ui.panel(items, options?)` → `HTMLDivElement`. (DG-FACT-372) | `ui.panel([ui.h1('Header'), ui.p('Body')])`. | js-api/ui.ts:2149 |
| Block layout (predefined ratios) | `ui.block`/`ui.block75`/`ui.block50`/`ui.block25` → `HTMLDivElement`. (DG-FACT-364) | Two adjacent blocks of 75% + 25%; four adjacent 25% blocks. | js-api/ui.ts:2102-2137 |
| Splitter container | `ui.splitH(items, options?, resize=false)` / `ui.splitV(items, options?, resize=false)` → `HTMLDivElement`. Each child becomes a `ui.box`. Nest for grid. | `ui.splitH([leftPane, ui.splitV([topRight, bottomRight])])`. | js-api/ui.ts:1863,1927 |

## UI primitives — views and chrome (advanced/ui.md)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Simple view factory (added to shell) | `grok.shell.newView(name, children?, options?): View`. (DG-FACT-368) | `let v = grok.shell.newView('Demo View'); v.append(ui.h1('Hello'))`. | js-api/src/shell.ts:234 |
| TableView factory (added to shell) | `grok.shell.addTableView(df, dockType?, width?): TableView`. (DG-FACT-368) | `let v = grok.shell.addTableView(grok.data.demo.demog())`. | js-api/src/shell.ts:258 |
| Detached view factory | `DG.View.create(options?)` / `DG.TableView.create(df, addToWorkspace=true)`. (DG-FACT-369) | Ribbon demo at `ui.md:566-577` calls `DG.View.create()`. | js-api/src/views/view.ts:246,388 |
| View ribbon panels (top chrome) | `view.setRibbonPanels(panels: HTMLElement[][], clear=true)`. (DG-FACT-369) | `view.setRibbonPanels([[ui.divText('label')], [ui.iconFA('search', fn), ui.iconFA('plus', fn)]])`. | js-api/src/views/view.ts:150 |
| View ribbon menu (top menu bar) | `view.ribbonMenu: DG.Menu` (getter/setter). Build via `DG.Menu.create().group(...).item(...)`. (DG-FACT-369) | `view.ribbonMenu = DG.Menu.create().group('Menu').item('Item', fn)`. | js-api/src/views/view.ts:135-136, js-api/src/widgets/menu.ts:47 |
| View toolbox (left rail) | `view.toolbox: HTMLElement` (getter/setter). Replace wholesale. (DG-FACT-369) | `view.toolbox = ui.accordion().root`. | js-api/src/views/view.ts:130-131 |
| Global left sidebar | `grok.shell.sidebar: TabControl`. Add a pane with `addPane(name, () => content, icon?)`. (DG-FACT-368) | `grok.shell.sidebar.addPane('First', () => ui.divText('A panel'), ui.iconFA('smile'))`. | js-api/src/shell.ts:131 |
| Right context panel | `grok.shell.o = anyObject` (setter). (DG-FACT-368) | `grok.shell.o = ui.h1('Context panel')`. | js-api/src/shell.ts:119-120 |

## UI primitives — dialogs and menus (advanced/ui.md)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Dialog builder | `ui.dialog(titleOrOptions): Dialog`. Fluent — `.add`, `.addInput`, `.onOK`, `.addButton(text, action, index?, tooltip?)`, `.addContextAction`, `.show(opts?)`, `.showModal(fullScreen)`. (DG-FACT-365) | `ui.dialog('Title').add(content).onOK(fn).show({x: 300, y: 300})` — standard. `.showModal(true)` — fullscreen modal. | js-api/ui.ts:641, js-api/src/widgets/forms.ts:29-179 |
| Top-menu builder | `DG.Menu.create(): Menu`. Fluent — `.group(text, order?)`, `.item(text, fn, order?, options?)`, `.items(texts, fn)`, `.separator()`. (DG-FACT-367) | `view.ribbonMenu = DG.Menu.create().group('Menu label').item('Item label', fn)`. | js-api/src/widgets/menu.ts:47,85,96,156 |
| Popup (context) menu | `DG.Menu.popup(): Menu` — same fluent surface, ends with `.show()`. (DG-FACT-367) | `DG.Menu.popup().item('Show info', fn).separator().items(['First', 'Second'], fn).show()`. | js-api/src/widgets/menu.ts:52 |
| Combo popup (button-anchored menu) | `ui.comboPopupItems(caption, {label: fn, ...})`. | `ui.div([ui.comboPopupItems('Combo popup label', {'Item 1': fn, 'Item 2': fn})])`. | js-api/ui.ts:512 |

## UI primitives — inputs and forms (advanced/ui.md)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Input factory namespace | `ui.input.<kind>(name, options?)` — every kind returns an `InputBase<T>` or subclass. (DG-FACT-362) | `ui.input.int('Age', {value: 30})`; `ui.input.string('Name', {value: '...'})`; `ui.input.search('')`. | js-api/ui.ts:1013-1149 |
| Choice / multi-choice / radio | `ui.input.choice<T>(name, {items, value, onValueChanged?})`, `ui.input.multiChoice<T>(...)`, `ui.input.radio(name, {items})`. | `ui.input.choice('Label', {value: 'Value 1', items: ['Value 1', 'Value 2']})`. | js-api/ui.ts:1060,1064,1132 |
| Boolean / toggle | `ui.input.bool(name, {value})` (checkbox), `ui.input.toggle(name, {value})` (switch). | `ui.input.toggle('Active', {value: true})`. | js-api/ui.ts:1088,1092 |
| Text area | `ui.input.textArea(name, {value, ...})`. | `ui.input.textArea('Label', {value: 'multi\nline'})`. | js-api/ui.ts:1124 |
| Color picker | `ui.input.color(name, {value: '#hex'})`. | `ui.input.color('Hair color', {value: '#ff0000'})`. | js-api/ui.ts:1128 |
| Range slider (two handles) | `ui.rangeSlider(minRange, maxRange, min, max, vertical=false, style='barbell')`. | `ui.rangeSlider(0, 10, 2, 5)`. | js-api/ui.ts:661 |
| Form layout | `ui.inputs([input1, input2, ui.buttonsInput([applyBtn, cancelBtn])])`. (DG-FACT-362) | `ui.inputs([ui.input.string('Name'), ui.input.int('Age'), ui.buttonsInput([ui.bigButton('Apply', fn), ui.button('Cancel', fn)])])`. | js-api/ui.ts:818,2251 |
| Drag-and-drop helpers | `ui.makeDraggable(src, {getDragObject, getDragCaption})`, `ui.makeDroppable(tgt, {acceptDrop, doDrop})`. (DG-FACT-371) | Drop target uses `inputBase.input` (the raw `<input>` element). | js-api/ui.ts:723,776 |

## UI primitives — small components (advanced/ui.md)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Typography helpers | `ui.h1(s)`, `ui.h2(s)`, `ui.h3(s)`, `ui.p(text)`, `ui.span(items)`, `ui.label(text)`, `ui.link(text, url\|fn, tooltip?)`, `ui.inlineText([...])`, `ui.divText(text)`. | `ui.h1('Hello World')`; `ui.link('Datagrok', 'https://datagrok.ai', 'tooltip')`. | js-api/ui.ts:164,176,188,2138,372,2158,614,386,226 |
| Buttons | `ui.button(label, handler, tooltip?)`, `ui.bigButton(label, handler, tooltip?)`, `ui.buttonsInput([buttons])`. (DG-FACT-361) | `ui.button(ui.iconFA('info'), fn)` for icon-only. | js-api/ui.ts:487,491,2251 |
| FontAwesome icon | `ui.iconFA(name, handler?, tooltip?)` — name without `fa-` prefix. Variants: `ui.iconFAB` (blue), `ui.iconSvg`, `ui.iconImage`. (DG-FACT-363) | `ui.iconFA('search', () => grok.shell.info('clicked'))`. | js-api/ui.ts:250-313 |
| Image control | `ui.image(src, width, height, {target?, tooltipMsg?})`. | `ui.image('https://example.com/img.gif', 400, 200, {target: 'https://...'})`. | js-api/ui.ts:599 |
| Static HTML table from items | `DG.HtmlTable.create(items, (item, idx) => [cell1, cell2])`. (DG-FACT-370) | `DG.HtmlTable.create(myList, (item) => [item.key, item.value])`. | js-api/src/widgets/data-widgets.ts:73 |
| Static HTML table from map | `ui.tableFromMap({key1: value1, key2: value2})`. (DG-FACT-370) | `ui.tableFromMap({project: grok.shell.project.toMarkup(), time: new Date()})`. | js-api/ui.ts:519 |
| List | `ui.list(items, options?)` where `options?: {processNode?, maxRows?}`. | `ui.list(['element 1', grok.shell.user, 'element 3'])`. | js-api/ui.ts:555 |
| Card | `ui.card(content)`. | `ui.card(ui.div([ui.h1('header'), ui.divText('body')]))`. | js-api/ui.ts:458 |
| Accordion (collapsible) | `ui.accordion()` → `Accordion`; `acc.addPane(name, () => content)`; mount `acc.root`. | `let acc = ui.accordion(); acc.addPane('Pane', () => ui.div([...]))`. | js-api/ui.ts:200 |
| Tabs | `ui.tabControl(pages, vertical=false, key?)`. Values can be `HTMLElement` (eager) or `() => HTMLElement` (lazy). | `ui.tabControl({'First Tab': panel1, 'Second Tab': () => panel2})`. | js-api/ui.ts:210 |
| Tag editor | `DG.TagEditor.create()`; `.addTag(value)`. (DG-FACT-370) | `let e = DG.TagEditor.create(); e.addTag('demo'); e.addTag('test')`. | js-api/src/widgets/tree.ts:17 |
| Markdown rendering | `ui.markdown(text): HTMLElement`. | `ui.markdown('## Header\n* item 1\n* item 2')`. | js-api/ui.ts:240 |
| Iframe | `ui.iframe({src, width, height})`. Width/height accept strings. | `ui.iframe({src: 'https://...', width: '400', height: '200'})`. | js-api/ui.ts (search `iframe`) |
| Info bar (in-view) | `ui.info(children, header?, reopenable=true)`. (DG-FACT-373) | `ui.info('Info text', 'Optional header', false)`. | js-api/ui.ts:409 |
| Async/loading placeholder | `ui.wait(async () => HTMLElement)`. (DG-FACT-373) | `ui.wait(async () => { let r = ui.div(); /* await ... */; return r })`. | js-api/ui.ts:541 |
| Update overlay indicator | `ui.setUpdateIndicator(el, updating=true, message?, onCancel?)`. (DG-FACT-373) | `ui.setUpdateIndicator(view.root, true)`. | js-api/ui.ts:476 |
| Tooltip binding | `ui.tooltip.bind(el, tooltipMsg)`. | `ui.tooltip.bind(ui.label('Label'), 'Tooltip message')`. | help/develop/advanced/ui.md:1067-1073 |
| Toasts (transient) | `grok.shell.info(msg)`, `.warning(msg)`, `.error(msg)`. (DG-FACT-368) | `grok.shell.info('Success'); grok.shell.error('Oops')`. | js-api/src/shell.ts:161,169,176 |
| Taskbar progress | `DG.TaskBarProgressIndicator.create(title)`; `.update(percent, msg)`, `.close()`. (DG-FACT-370) | `let pi = DG.TaskBarProgressIndicator.create('Loading…'); setTimeout(() => pi.close(), 3000)`. | js-api/src/widgets/progress.ts:13 |
| Tree view | `ui.tree(): TreeViewGroup`; `.group(text, value, expanded?)`, `.item(text, value)`, `.enableCheckBox(true)`. | `let t = ui.tree(); let g = t.group('group 1', 1); g.enableCheckBox(true); g.item('item 1.1')`. | js-api/src/widgets/tree.ts:146 |

## UI primitives — color (advanced/ui.md)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Static color utilities | `DG.Color.toRgb(n)`, `DG.Color.getCategoricalColor(i)`, `DG.Color.getContrastColor(c)`, `DG.Color.categoricalPalette` (getter, `number[]`), `DG.Color.categoricalPalettes` (getter, named palettes). Colors are 32-bit ARGB integers. (DG-FACT-366) | `DG.Color.toRgb(DG.Color.getCategoricalColor(0))` → first palette CSS color; `DG.utils.identity(30).map(DG.Color.getCategoricalColor)` for 30 colors looping the palette. | js-api/src/color.ts:9,51,56,67,85,90 |

## Demo scripts (how-to/misc/write-demo-scripts.md)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Stepwise demo driver | `DemoScript` class from `@datagrok-libraries/tutorials`; constructor `(name, description, isAutomatic?, {autoStartFirstStep?, path?})`. Fluent `step(name, async () => {...}, {description?, delay?})` + `start()`. (DG-FACT-374, DG-FACT-375, DG-FACT-376) | `new DemoScript('Overview', 'Overview of Cheminformatics functionality', undefined, {autoStartFirstStep: true, path: 'Cheminformatics/Overview'})` chained with `.step('Load molecules', async () => {...}, {description: '...', delay: 3000})` etc. | libraries/tutorials/src/demo-script.ts:20-417 |
| Step type | `type Step = {name: string; func: () => Promise<void>; options?: {description?: string; delay?: number}}` — exported alongside `DemoScript`. | Skill authors can type their step builders against this when generating sequences programmatically. | libraries/tutorials/src/demo-script.ts:8-17 |
| Reference: canonical automatic demo | `packages/Chem/src/demo/demo.ts:55-72` — `new DemoScript('Overview', ...).step(...).step(...)...` with `delay: 3000`, `autoStartFirstStep: true`, `path: 'Cheminformatics/Overview'`. | Mirror this for any new automatic demo. Registered upstream by `packages/Chem/src/package.g.ts:1057-1064`. | packages/Chem/src/demo/demo.ts:55-72, packages/Chem/src/package.g.ts:1057-1064 |
| Reference: minimal manual demo | `packages/Bio/src/demo/bio03-atomic-level.ts:32` — `new DemoScript('Atomic Level', '...', false, {autoStartFirstStep: true}).step(...).step(...)`. No `path` set, manual stepping. | The smallest legible reference for a manual-with-auto-first-step demo. | packages/Bio/src/demo/bio03-atomic-level.ts:32-67 |
| Function-options constant | `DG.FUNC_OPTIONS.DEMO_PATH` (the canonical name of the `meta.demoPath` field). | Lookups (e.g., `func.options[DG.FUNC_OPTIONS.DEMO_PATH]` to filter demo functions in the Demo app widget). | packages/Tutorials/src/demo-app/widget.ts:34 |
| Demo function registration form | `package.g.ts` header — `//name: ...`, `//description: ...`, `//meta.isDemoScript: true`, `//meta.demoPath: <Group> | <Name>` above the exported function. (DG-FACT-380) | `packages/Chem/src/package.g.ts:1057-1064` for `demoChemOverview`. Optional sibling: `//meta.isDemoDashboard: true` for static-dashboard demos. | packages/Chem/src/package.g.ts:1057-1064, 1080-1093 |

## Tutorials (how-to/misc/write-tutorials.md)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Tutorial base class | `abstract class Tutorial extends DG.Widget` from `@datagrok-libraries/tutorials/src/tutorial`. Four abstract getters (`name`, `description`, `icon`, `steps: number`) plus `protected abstract _run(): Promise<void>`. Inherits the hint/progress UI, prerequisites check, demoTable load, and userSettings persistence. (DG-FACT-381, DG-FACT-382) | Subclass per tutorial; one file per tutorial under `src/tracks/<track>/tutorials/<slug>.ts`. | libraries/tutorials/src/tutorial.ts:12-21,97 |
| Track aggregator | `class Track` from `@datagrok-libraries/tutorials/src/track`. Constructor `(name, tutorials: Tutorial[], helpUrl)`; instances set `t.track = this` on each child. | Only meaningful when constructed from a track-registration function — the runtime discards in-body Track instances and rebuilds an empty one keyed by `meta.name`. (DG-FACT-386) | libraries/tutorials/src/track.ts:5-16 |
| Prerequisites contract | `TutorialPrerequisites = { packages?: string[]; jupyter?: boolean; grokCompute?: boolean; grokConnect?: boolean; h2o?: boolean }`. Service flags map through `Tutorial.SERVICES`. (DG-FACT-387) | Set `prerequisites: TutorialPrerequisites = { packages: ['Foo'] }` on the subclass — checked by the runner before `_run`. | libraries/tutorials/src/tutorial.ts:23,63-68,135-159,672-679 |
| Reference: full track wiring | `packages/Tutorials/src/tracks/eda/index.ts` — collects `Tutorial` subclasses into `new Track('Exploratory Data Analysis', tutorials.map(T => new T()), 'https://datagrok.ai/help/...')`. | The canonical pattern for in-tree (Tutorials package) registration; out-of-tree packages use `//tags: tutorial`/`//tags: track` instead (DG-FACT-385, DG-FACT-386). | packages/Tutorials/src/tracks/eda/index.ts:1-21 |
| Reference: tutorial subclass | `packages/Tutorials/src/tracks/eda/tutorials/grid.ts:11-26` (`GridTutorial extends Tutorial`) — minimal shape: four getters, optional `helpUrl`, then `_run` body of sequential `await this.action(...)` calls. | Mirror this when writing a new tutorial. `steps` must equal the count of `action(...)` calls (drives the progress bar). | packages/Tutorials/src/tracks/eda/tutorials/grid.ts:11-26 |
| Tutorials package discovery hook | `DG.Func.find({tags: ['tutorial']})` and `DG.Func.find({tags: ['track']})` are run on Tutorials-package init (`packages/Tutorials/src/package.ts:79-100`). | Out-of-tree tutorials only appear in the gallery after the Tutorials package reloads — no per-package wiring needed. | packages/Tutorials/src/package.ts:79-124 |

## Data enrichments (how-to/packages/data-enrichments.md)

| Component | Class / type | Reference usage | Source |
|---|---|---|---|
| Enrichment record | `interface Enrichment { name; keyDb: string \| null; keySchema; keyTable; keyColumn; fields: string[]; joins: DG.TableJoin[] }` — defined inside PowerPack (not exported from `js-api`). Plugin-bundled JSON additionally carries a top-level `connection: string` field consumed at install time. (DG-FACT-443, DG-FACT-DRIFT-DE-001) | One `.json` per record in `<package>/enrichments/`. PowerPack reads it via `JSON.parse(text)` in `readEnrichConfig`. | packages/PowerPack/src/db-explorer.ts:755-763 |
| `DG.TableJoin` | Dart-defined mixin with `leftTableName`, `rightTableName`, `rightTableAlias`, `joinType`, `leftTableKeys: string[]`, `rightTableKeys: string[]`. JOIN type comes from `DG.JOIN_TYPE` (`'inner' \| 'outer' \| 'left' \| 'right'`). (DG-FACT-444, DG-FACT-447) | Used inside an `Enrichment`'s `joins` array AND as `query.joins` on a `DG.TableQuery`. | js-api/src/datagrok/build/web/grok_shared.dart.js:58687 (TableJoin field list); js-api/src/const.ts:59-64 (JOIN_TYPE) |
| `PowerPack:runEnrichment` | Datagrok function looked up via `DG.Func.find({package: 'PowerPack', name: 'runEnrichment'})[0]`; called with `(conn, schema, table, column, name, df, db?, localColumn?)`. Internally builds a `DG.TableQuery` via `convertEnrichmentToQuery(e, conn)` and executes it. (DG-FACT-448) | Used by the column-context-panel enrichment button — and by any package that wants to programmatically re-run a saved enrichment. | packages/PowerPack/src/db-explorer.ts:531,683-697 |
| Reference: simple single-join enrichment | `packages/DBTests/enrichments/company_info.json` — Northwind `orders.customerid → customers.customerid`, fields pull `companyname` and `contacttitle`. Smallest end-to-end working example. | Copy as a template when authoring a new enrichment for a 1:M lookup. | packages/DBTests/enrichments/company_info.json:1-25 |
| Reference: production enrichment library | `packages/Chembl/enrichments/` (17 JSON files) — covers compound identity, activities, mechanisms, structures, hierarchy, and indications. Each keys on either `molregno` or another ChEMBL ID. | Mirror these for any plugin that wraps a curated schema with multiple "join extras" entry points. | packages/Chembl/enrichments/compound_activities_summary.json; packages/Chembl/enrichments/drug_mechanism_targets.json |
| AppData enrichment store (consumed by UI) | Files under `System:AppData/PowerPack/enrichments/{nqName-with-_-for-:}/{keyDb?}/{keySchema}/{keyTable}/{keyColumn}/{name}.json` are listed/read/written by `grok.dapi.files.list/readAsText/writeAsText/delete`. (DG-FACT-446) | Inspect this tree with `grok.dapi.files` when debugging "my bundled enrichment doesn't appear" — if it isn't here after install, the bundling step failed. | packages/PowerPack/src/db-explorer.ts:637-678 |
