{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Curves",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8888",
            "preLaunchTask": "debug-curves",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
