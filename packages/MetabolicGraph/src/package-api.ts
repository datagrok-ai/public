/**
This file is auto-generated by the grok api command.
If you notice any changes, please push them to the repository.
Do not edit this file manually.
*/
import * as grok from 'datagrok-api/grok';
import * as DG from 'datagrok-api/dg';


export namespace scripts {
  /**
   * Computes extreme points (FBA for each reaction, maximize and minimize) to define the feasibility space
   */
  export async function computeExtremePoints(cobraModel: string ): Promise<string> {
    return await grok.functions.call('MetabolicGraph:ComputeExtremePoints', { cobraModel });
  }

  /**
   * Samples the metabolic map and returns raw flux samples
   */
  export async function optGpSampling(cobraModel: string , nSamples: number | null, thinning: number | null): Promise<DG.DataFrame> {
    return await grok.functions.call('MetabolicGraph:OptGpSampling', { cobraModel, nSamples, thinning });
  }
}

export namespace funcs {
  /**
   * Metabolic graph application
   */
  export async function metabolicGraphApp(path?: string , filter?: string ): Promise<DG.View> {
    return await grok.functions.call('MetabolicGraph:MetabolicGraphApp', { path, filter });
  }

  export async function escherFileViewer(file: DG.FileInfo ): Promise<DG.View> {
    return await grok.functions.call('MetabolicGraph:EscherFileViewer', { file });
  }

  export async function escherFileViewerCheck(content: string ): Promise<boolean> {
    return await grok.functions.call('MetabolicGraph:EscherFileViewerCheck', { content });
  }

  /**
   * Current state of the metabolic graph view - model and map summary, FBA objectives, loaded flux data, selected metabolites, active time-course and analysis name. Call this first to understand what is loaded
   */
  export async function getMetabolicState(view: DG.View ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:GetMetabolicState', { view });
  }

  /**
   * Search the model reactions, metabolites and genes by id or name substring. Returns brief entries with bounds, objective and flux for reactions
   * @param {string} query - Substring matched against ids and names. Pass an empty string to list everything
   * @param {string} kind
   *   choices: ["reaction", "metabolite", "gene"]
   * @param {number} limit - Max matches returned, default 15
   */
  export async function findMetabolicEntities(view: DG.View , query: string , kind?: string , limit?: number ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:FindMetabolicEntities', { view, query, kind, limit });
  }

  /**
   * Full detail of one reaction (stoichiometry, bounds, gene rule, flux), metabolite (formula, compartment, its reactions) or gene by id
   */
  export async function getMetabolicEntityDetails(view: DG.View , entityId: string ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:GetMetabolicEntityDetails', { view, entityId });
  }

  /**
   * Set flux bounds of model reactions. Pass a map of reaction id to a two-number list of lower and upper bound. Set both to 0 to knock a reaction out. Affects subsequent FBA and sampling
   */
  export async function setReactionBounds(view: DG.View , bounds: any ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:SetReactionBounds', { view, bounds });
  }

  /**
   * Run flux balance analysis and color the map by the resulting fluxes. Optionally pass objectives as a map of reaction id to maximize or minimize, replacing the current objectives. Returns the objective value and fluxes
   */
  export async function runFba(view: DG.View , objectives?: any ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:RunFba', { view, objectives });
  }

  /**
   * Sample the feasible flux space of the model. Colors the map by average flux and puts a flux histogram into every reaction tooltip. Returns the largest average fluxes
   * @param {number} samples - Number of samples, default 10000
   * @param {number} bins - Histogram bins in reaction tooltips, default 20
   * @param {number} thinning - Sampler thinning interval, default 10
   * @param {boolean} addDataFrame - Also open the raw samples as a table view
   */
  export async function sampleReactionFluxes(view: DG.View , samples?: number , bins?: number , thinning?: number , addDataFrame?: boolean ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:SampleReactionFluxes', { view, samples, bins, thinning, addDataFrame });
  }

  /**
   * Time-course simulation - interpolates reaction bounds from start to end over the steps, samples fluxes at every step, then shows an animation slider on the map and a table of average flux per step. Pass startBounds and endBounds as maps of reaction id to a two-number list of lower and upper bound, both listing the same reactions
   * @param {number} steps - Interpolation steps including both endpoints, default 10
   * @param {number} samples - Samples per step, default 10000
   */
  export async function runTimeCourse(view: DG.View , startBounds: any , endBounds: any , steps?: number , samples?: number ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:RunTimeCourse', { view, startBounds, endBounds, steps, samples });
  }

  /**
   * Color the map with custom per-reaction values. Pass values as a map of reaction id to number. Omit values to clear the current reaction data
   * @param {string} source - Label shown as the data source
   */
  export async function setReactionFluxes(view: DG.View , values?: any , source?: string ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:SetReactionFluxes', { view, values, source });
  }

  /**
   * Zoom the map to a reaction or metabolite and highlight it so the user sees it
   */
  export async function showMetabolicEntity(view: DG.View , entityId: string ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:ShowMetabolicEntity', { view, entityId });
  }

  /**
   * Find and highlight the k-th shortest path between two metabolites on the map. Direction matters
   * @param {number} k - 1 for the shortest path, 2 for the second shortest and so on
   */
  export async function highlightShortestPath(view: DG.View , fromMetabolite: string , toMetabolite: string , k?: number ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:HighlightShortestPath', { view, fromMetabolite, toMetabolite, k });
  }

  /**
   * List the saved metabolic analyses that loadMetabolicAnalysis can open
   */
  export async function listMetabolicAnalyses(view: DG.View ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:ListMetabolicAnalyses', { view });
  }

  /**
   * Save the current analysis (map, model, selection and flux data) under a name. Refuses to overwrite an existing analysis unless overwrite is true
   */
  export async function saveMetabolicAnalysis(view: DG.View , name: string , description?: string , overwrite?: boolean ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:SaveMetabolicAnalysis', { view, name, description, overwrite });
  }

  /**
   * Load a saved analysis by name, replacing the current map, model and flux data. ALWAYS confirm with the user first when unsaved work could be lost
   */
  export async function loadMetabolicAnalysis(view: DG.View , name: string ): Promise<any> {
    return await grok.functions.call('MetabolicGraph:LoadMetabolicAnalysis', { view, name });
  }
}
