/**
This file is auto-generated by the grok api command from databases/<schema>/schema.json manifests.
If you notice any changes, please push them to the repository.
Do not edit this file manually.
*/
import * as grok from 'datagrok-api/grok';
import * as DG from 'datagrok-api/dg';
import type {Dayjs} from 'dayjs';

export type MonomerPolymerType = 'PEPTIDE' | 'RNA' | 'CHEM' | 'BLOB';

export type MonomerMonomerType = 'Backbone' | 'Branch' | 'Terminal' | 'Undefined';

export type SetPlaceholderPolymerType = 'PEPTIDE' | 'RNA' | 'CHEM' | 'BLOB';

export type SetPlaceholderMonomerType = 'Backbone' | 'Branch' | 'Terminal' | 'Undefined';

/** Row of `monomers.library`. */
export interface LibraryRow {
  id: string;
  version: number;
  created_on: Dayjs;
  updated_on: Dayjs;
  author_id: string;
  name: string;
  description?: string;
  source?: string;
}

/** Insert payload for `monomers.library`. */
export interface LibraryInsert {
  name: string;
  description?: string;
  source?: string;
}

export type LibraryColumn = 'id' | 'version' | 'created_on' | 'updated_on' | 'author_id' | 'name' | 'description' |
  'source';

/** Expand keys of `monomers.library` → fields each adds to the row (consumed by query()/builder). */
export type LibraryExpand = {
  'details:monomer': {monomer?: MonomerRow[]};
};

/** Row of `monomers.monomer`. */
export interface MonomerRow {
  id: string;
  version: number;
  created_on: Dayjs;
  updated_on: Dayjs;
  author_id: string;
  library_id: string;
  symbol: string;
  name?: string;
  polymer_type: MonomerPolymerType;
  monomer_type?: MonomerMonomerType;
  molfile?: string;
  smiles?: string;
  natural_analog?: string;
  author?: string;
  create_date?: string;
  helm_id?: number;
  rgroups?: string;
  meta?: string;
}

/** Insert payload for `monomers.monomer`. */
export interface MonomerInsert {
  library_id: string;
  symbol: string;
  name?: string;
  polymer_type: MonomerPolymerType;
  monomer_type?: MonomerMonomerType;
  molfile?: string;
  smiles?: string;
  natural_analog?: string;
  author?: string;
  create_date?: string;
  helm_id?: number;
  rgroups?: string;
  meta?: string;
}

export type MonomerColumn = 'id' | 'version' | 'created_on' | 'updated_on' | 'author_id' | 'library_id' | 'symbol' |
  'name' | 'polymer_type' | 'monomer_type' | 'molfile' | 'smiles' | 'natural_analog' | 'author' | 'create_date' |
  'helm_id' | 'rgroups' | 'meta';

/** Expand keys of `monomers.monomer` → fields each adds to the row (consumed by query()/builder). */
export type MonomerExpand = {
  'library_id': {'library_id.name'?: string; 'library_id.description'?: string; 'library_id.source'?: string};
};

/** Row of `monomers.monomer_set`. */
export interface MonomerSetRow {
  id: string;
  version: number;
  created_on: Dayjs;
  updated_on: Dayjs;
  author_id: string;
  name: string;
  description?: string;
}

/** Insert payload for `monomers.monomer_set`. */
export interface MonomerSetInsert {
  name: string;
  description?: string;
}

export type MonomerSetColumn = 'id' | 'version' | 'created_on' | 'updated_on' | 'author_id' | 'name' | 'description';

/** Expand keys of `monomers.monomer_set` → fields each adds to the row (consumed by query()/builder). */
export type MonomerSetExpand = {
  'details:set_placeholder': {set_placeholder?: SetPlaceholderRow[]};
};

/** Row of `monomers.set_placeholder`. */
export interface SetPlaceholderRow {
  id: string;
  version: number;
  created_on: Dayjs;
  updated_on: Dayjs;
  author_id: string;
  set_id: string;
  symbol: string;
  polymer_type: SetPlaceholderPolymerType;
  monomer_type?: SetPlaceholderMonomerType;
  monomer_links?: string;
}

/** Insert payload for `monomers.set_placeholder`. */
export interface SetPlaceholderInsert {
  set_id: string;
  symbol: string;
  polymer_type: SetPlaceholderPolymerType;
  monomer_type?: SetPlaceholderMonomerType;
  monomer_links?: string;
}

export type SetPlaceholderColumn = 'id' | 'version' | 'created_on' | 'updated_on' | 'author_id' | 'set_id' |
  'symbol' | 'polymer_type' | 'monomer_type' | 'monomer_links';

/** Expand keys of `monomers.set_placeholder` → fields each adds to the row (consumed by query()/builder). */
export type SetPlaceholderExpand = {
  'set_id': {'set_id.name'?: string; 'set_id.description'?: string};
};

export type MonomersTransactionOp =
  {op: 'insert'; table: 'library'; ref?: string; values: DG.DomainTxValues<LibraryInsert>} |
  {op: 'update'; table: 'library'; id: string; values: DG.DomainTxValues<Partial<LibraryRow>>; expectedVersion?: number} |
  {op: 'delete'; table: 'library'; id: string} |
  {op: 'insert'; table: 'monomer'; ref?: string; values: DG.DomainTxValues<MonomerInsert>} |
  {op: 'update'; table: 'monomer'; id: string; values: DG.DomainTxValues<Partial<MonomerRow>>; expectedVersion?: number} |
  {op: 'delete'; table: 'monomer'; id: string} |
  {op: 'insert'; table: 'monomer_set'; ref?: string; values: DG.DomainTxValues<MonomerSetInsert>} |
  {op: 'update'; table: 'monomer_set'; id: string; values: DG.DomainTxValues<Partial<MonomerSetRow>>; expectedVersion?: number} |
  {op: 'delete'; table: 'monomer_set'; id: string} |
  {op: 'insert'; table: 'set_placeholder'; ref?: string; values: DG.DomainTxValues<SetPlaceholderInsert>} |
  {op: 'update'; table: 'set_placeholder'; id: string; values: DG.DomainTxValues<Partial<SetPlaceholderRow>>; expectedVersion?: number} |
  {op: 'delete'; table: 'set_placeholder'; id: string};

/** Typed clients for the `monomers` domain schema tables (lazy — no import-time side effects). */
export const monomersDb = {
  get libraries() {
    return grok.dapi.domains.table<LibraryRow, LibraryInsert, LibraryColumn, LibraryExpand>('monomers.library');
  },
  get monomers() {
    return grok.dapi.domains.table<MonomerRow, MonomerInsert, MonomerColumn, MonomerExpand>('monomers.monomer');
  },
  get monomerSets() {
    return grok.dapi.domains.table<MonomerSetRow, MonomerSetInsert, MonomerSetColumn, MonomerSetExpand>('monomers.monomer_set');
  },
  get setPlaceholders() {
    return grok.dapi.domains.table<SetPlaceholderRow, SetPlaceholderInsert, SetPlaceholderColumn, SetPlaceholderExpand>('monomers.set_placeholder');
  },
  transaction<T extends MonomersTransactionOp[]>(ops: [...T]):
      Promise<{[K in keyof T]: DG.DomainOpResultFor<T[K]>}> {
    return grok.dapi.domains.transaction('monomers', ops) as
      Promise<{[K in keyof T]: DG.DomainOpResultFor<T[K]>}>;
  },
};
