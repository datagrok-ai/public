import * as grok from 'datagrok-api/grok';
import * as ui from 'datagrok-api/ui';
import * as DG from 'datagrok-api/dg';

export enum TREE_TAGS {
  NEWICK = '.newick',
  NEWICK_JSON = '.newick_json',
  OBJECTS = '.newick_objects',
}
