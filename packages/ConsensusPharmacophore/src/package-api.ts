/**
This file is auto-generated by the grok api command.
If you notice any changes, please push them to the repository.
Do not edit this file manually.
*/
import * as grok from 'datagrok-api/grok';
import * as DG from 'datagrok-api/dg';


export namespace scripts {
  /**
  Stage 2a - global Calpha Kabsch alignment of input PDBs. Template is the highest-resolution entry; transform is row-major 4x4 in JSON.
  */
  export async function pharmacophoreStage2AlignPockets(pdb_qc: DG.DataFrame , chain_selection: string , alt_loc_filter: boolean , min_occupancy: number ): Promise<DG.DataFrame> {
    return await grok.functions.call('ConsensusPharmacophore:PharmacophoreStage2AlignPockets', { pdb_qc, chain_selection, alt_loc_filter, min_occupancy });
  }

  /**
  Stage 2b - pocket-only Calpha Kabsch refinement. Replaces pass-1 transforms with pass-2 over the consensus-pocket Calpha subset. Filters by selected cluster (selected_pdb_ids).
  */
  export async function pharmacophoreStage2bAlignPocketsPocket(aligned_structures: DG.DataFrame , pocket_atoms: DG.DataFrame , selected_pdb_ids: string ): Promise<DG.DataFrame> {
    return await grok.functions.call('ConsensusPharmacophore:PharmacophoreStage2bAlignPocketsPocket', { aligned_structures, pocket_atoms, selected_pdb_ids });
  }

  /**
  Stage 3 - isolate binding-pocket atoms in the consensus frame. Cutoff (default) keeps protein atoms within `pocket_radius` of any ligand atom; DBSCAN (advanced) clusters the union point cloud and keeps the largest non-noise cluster.
  */
  export async function pharmacophoreStage3IsolatePocket(aligned_structures: DG.DataFrame , pocket_method: string , pocket_radius: number , dbscan_eps: number , dbscan_min_samples: number ): Promise<DG.DataFrame> {
    return await grok.functions.call('ConsensusPharmacophore:PharmacophoreStage3IsolatePocket', { aligned_structures, pocket_method, pocket_radius, dbscan_eps, dbscan_min_samples });
  }

  /**
  Stage 4 - per-ligand pharmacophore extraction via ProLIF. Each detected protein-ligand interaction emits one feature row; ligand-side atom centroid lifted into the consensus frame.
  */
  export async function pharmacophoreStage4ExtractFeatures(aligned_structures: DG.DataFrame , pocket_atoms: DG.DataFrame , selected_pdb_ids: string ): Promise<DG.DataFrame> {
    return await grok.functions.call('ConsensusPharmacophore:PharmacophoreStage4ExtractFeatures', { aligned_structures, pocket_atoms, selected_pdb_ids });
  }

  /**
  Stage 5a - per-family k-means consensus over raw ligand_features. Keeps the top N clusters per family that are supported by at least min_cluster_size_fraction of the input ligands.
  */
  export async function pharmacophoreStage5aConsensusKmeans(ligand_features: DG.DataFrame , kq: number , top_cluster_number: number , min_cluster_size_fraction: number , kmeans_random_state: number ): Promise<DG.DataFrame> {
    return await grok.functions.call('ConsensusPharmacophore:PharmacophoreStage5aConsensusKmeans', { ligand_features, kq, top_cluster_number, min_cluster_size_fraction, kmeans_random_state });
  }
}

export namespace funcs {
  export async function consensusPharmacophoreApp(): Promise<DG.View> {
    return await grok.functions.call('ConsensusPharmacophore:ConsensusPharmacophoreApp', {});
  }

  /**
  Demo: Consensus Pharmacophore on 5 EGFR kinase structures.
  */
  export async function consensusPharmacophoreDemo(): Promise<void> {
    return await grok.functions.call('ConsensusPharmacophore:ConsensusPharmacophoreDemo', {});
  }
}
