---
phase: 16-sample-level-spc-tracking
plan: 05
type: execute
wave: 2
depends_on:
  - 16-02
  - 16-03
  - 16-04
files_modified:
  - src/analysis/experiment-setup.ts
  - src/package.ts
autonomous: true
requirements:
  - SPC-04
  - SPC-06

must_haves:
  truths:
    - "D-01: Run identity capture is INLINE in showAnnotationDialog (Annotate Experiment), OPTIONAL. Empty values are TOLERATED at annotation time so v1.0–v1.3 paths are not broken for users who skip SPC. Persisted as proteomics.spc_run_meta JSON tag {instrument_id, acquisition_datetime}. Compute SPC Status REFUSES to run with empty spc_run_meta and surfaces the UI-SPEC nudge copy. Spectronaut Candidates is hard-refused for Compute SPC Status (no per-sample intensities to compute against). Plan 16-04 implements the parser-side seed extraction half of D-01; this plan implements the dialog half."
    - "Annotate Experiment dialog gains two new optional inputs in fixed order AFTER `Group 2`: `Instrument` (ui.input.string) and `Acquisition datetime` (ui.input.date, date-only precision per RESEARCH.md gotcha #1). Both inputs are tooltipped per UI-SPEC."
    - "Dialog reads `proteomics.spc_run_meta_seed` for prefill (parser-side seed) AND `proteomics.spc_run_meta` for re-open (operator-set value). Re-opens of an already-annotated run show the persisted instrument + date, not a blank form."
    - "On OK, the two new values (acquisition_datetime serialized as ISO-8601 via dayjs().toISOString()) are persisted via setRunMeta(df, meta) from Plan 02's spc.ts. Empty values are tolerated."
    - "package.ts gains two new menu items: `Proteomics | Analyze | Compute SPC Status` (immediate, no `...`) and `Proteomics | Visualize | SPC Dashboard...` (`...` because it opens a picker). Both register via the existing `@grok.decorators.func` pattern."
    - "`Compute SPC Status` gates: (a) tv.dataFrame exists, (b) `proteomics.source !== 'spectronaut-candidates'`, (c) `getRunMeta(df) !== null` AND both fields non-empty, (d) `getGroups(df) !== null`. Each gate surfaces the exact warning copy from UI-SPEC §'Refuse states'."
    - "On successful Compute SPC Status, the dataflow runs: computeSpcMetrics → loadBaseline → evaluateNelsonRules → setSpcStatus → appendRun → success toast with the exact copy from UI-SPEC §'Success confirmations'."
    - "Re-running Compute SPC Status on a previously-computed run shows the 'Updated SPC for <run_label> (previous status: <prior_status>)' toast (T-16-03 mitigation surfaced to operator)."
  artifacts:
    - path: "src/analysis/experiment-setup.ts"
      provides: "showAnnotationDialog extended with two new optional inputs + AnnotationSeed extension + setRunMeta call on OK"
    - path: "src/package.ts"
      provides: "Two new menu handlers + Compute SPC Status orchestration + SPC Dashboard menu shim"
  key_links:
    - from: "src/package.ts (computeSpcStatus handler)"
      to: "src/analysis/spc.ts (computeSpcMetrics, evaluateNelsonRules, setSpcStatus)"
      via: "imports + call sequence"
      pattern: "from './analysis/spc'"
    - from: "src/package.ts (computeSpcStatus handler)"
      to: "src/analysis/spc-storage.ts (appendRun, loadBaseline)"
      via: "imports + call sequence"
      pattern: "from './analysis/spc-storage'"
    - from: "src/analysis/experiment-setup.ts (showAnnotationDialog)"
      to: "src/analysis/spc.ts (getRunMeta, setRunMeta)"
      via: "seed read + persist"
      pattern: "from './spc'"
---

<objective>
Wire the math + storage layers (Plans 02–03) and the parser-side seed (Plan 04) into the user-facing surfaces: the Annotate Experiment dialog (run-identity capture) and the package's top-menu (Compute SPC Status orchestrator + SPC Dashboard shim for Plan 06 to expand). After this plan, an operator can import a Spectronaut PG export, open Annotate Experiment (auto-prefilled), pick groups, click Compute SPC Status, and see the run land in `runs.csv` with a status toast — end-to-end SPC compute path lit up.

Purpose: This plan is the integration glue. It owns NO new math, NO new I/O — only orchestration. Plan 06 still has to build the dashboard viewer, but a stub menu handler (toast: "SPC Dashboard coming in 16-06") is registered here so the menu surface is complete and dependency-resolvable.

Output: `src/analysis/experiment-setup.ts` extended (~30-50 added LOC); `src/package.ts` extended (~80-120 added LOC for the two new handlers + gate helpers). Tests `SPC:annotation_dialog_persists_run_meta` (asserts dialog OK writes proteomics.spc_run_meta), `SPC:candidates_refuse` (gate test) turn GREEN. (The pure-TS helper test `SPC:run_meta_helpers` is owned by Plan 16-02.) Also the success path is now testable end-to-end and the existing SPC math tests still PASS in the integrated flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-sample-level-spc-tracking/16-CONTEXT.md
@.planning/phases/16-sample-level-spc-tracking/16-RESEARCH.md
@.planning/phases/16-sample-level-spc-tracking/16-UI-SPEC.md
@packages/Proteomics/CLAUDE.md
@packages/Proteomics/src/analysis/experiment-setup.ts
@packages/Proteomics/src/package.ts
@packages/Proteomics/src/analysis/spc.ts
@packages/Proteomics/src/analysis/spc-storage.ts
</context>

<interfaces>
<!-- Functions this plan calls (already exported from Plans 02-04). -->

From `src/analysis/spc.ts` (Plan 02):
- `RunMeta`, `SpcMetrics`, `SpcRuleResult`, `BaselineSnapshot`, `SpcRulesEnabled`
- `computeSpcMetrics(df, groups, runMeta): SpcMetrics`
- `evaluateNelsonRules(newMetrics, priorSeries, baseline, enabled): SpcRuleResult`
- `setSpcStatus(df, metrics, ruleResult): void`
- `setRunMeta(df, meta): void`
- `getRunMeta(df): RunMeta | null`

From `src/analysis/spc-storage.ts` (Plan 03):
- `SpcRunRow`, `LockedBaseline`
- `appendRun(row): Promise<SpcRunRow>`
- `loadRuns(instrumentId?): Promise<SpcRunRow[]>`
- `loadBaseline(instrumentId): Promise<LockedBaseline | null>`

From `src/analysis/experiment-setup.ts` (existing, lines 1-30):
- `setGroups`, `getGroups`, `GroupAssignment`, `AnnotationSeed`, `seedAnnotationDialogInputs`

From `datagrok-api/ui` (verified in RESEARCH.md):
- `ui.input.string(name, {value, tooltipText})`
- `ui.input.date(name, {value: dayjs.Dayjs, tooltipText})` — date-only; NO ui.input.dateTime per RESEARCH.md gotcha #1
- `ui.dialog(title).add(input).onOK(fn).show()`

From `datagrok-api/grok`:
- `grok.shell.tv?.dataFrame`, `grok.shell.warning(msg)`, `grok.shell.info(msg)`
- `DG.TaskBarProgressIndicator.create(msg)` / `.close()`

UI-SPEC copy (LOAD-BEARING — every reviewer-facing string must match VERBATIM):

| Trigger | Copy |
|---------|------|
| No analyzed file open | `Open an analyzed file first, then run Compute SPC Status.` |
| Empty spc_run_meta | `Open Annotate Experiment to set instrument + acquisition datetime.` |
| Spectronaut Candidates source | `SPC requires per-sample intensities. Re-import this analysis from the Spectronaut PG report (not the Candidates report) to compute SPC.` |
| No proteomics.groups | `Annotate experimental groups first (Proteomics → Annotate Experiment) — SPC needs Group 1 to compute control-replicate correlation.` |
| Fewer than 2 control columns (INFO, proceeds) | `Group 1 has fewer than 2 samples — control-replicate correlation can't be computed. The other three metrics will still be recorded.` |
| Success (pass) | `SPC computed: <run_label> — status: pass.` |
| Success (flagged) | `SPC computed: <run_label> — flagged on <N> rule(s): <list>.` |
| Success (out_of_control) | `SPC computed: <run_label> — OUT OF CONTROL on <N> rule(s): <list>.` |
| Overwrites prior row | `Updated SPC for <run_label> (previous status: <prior_status>).` |

Tooltips (per UI-SPEC):
- Instrument: `Free-text identifier for the instrument that ran this analysis (e.g. QExactive-01). Used to group runs in the SPC dashboard.`
- Acquisition datetime: `When the samples were acquired on the instrument. Used to order runs on the SPC trend chart — not the file-import time.`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend showAnnotationDialog with Instrument + Acquisition datetime inputs</name>
  <files>src/analysis/experiment-setup.ts</files>
  <read_first>
    - src/analysis/experiment-setup.ts (whole file, ~100 LOC)
    - src/analysis/spc.ts (Plan 02 output) — setRunMeta / getRunMeta / RunMeta interface
    - .planning/phases/16-sample-level-spc-tracking/16-UI-SPEC.md § "Tooltips" + § "Annotate Experiment dialog — D-01 extension"
    - .planning/phases/16-sample-level-spc-tracking/16-RESEARCH.md § "Summary" item 1 (date-only precision)
    - src/tests/spc.ts → test "SPC:run_meta_helpers" assertion shape
  </read_first>
  <behavior>
    Extend `AnnotationSeed` interface to add two optional fields: `instrumentId?: string` and `acquisitionDatetime?: string` (ISO-8601 string; date-only precision is acceptable per RESEARCH.md gotcha #1).

    Extend `seedAnnotationDialogInputs(df, available)` to ALSO read:
    1. The persisted `proteomics.spc_run_meta` tag via `getRunMeta(df)` — if present, populate `instrumentId` and `acquisitionDatetime` from there (operator-set values take priority).
    2. The parser-side `proteomics.spc_run_meta_seed` tag — JSON.parse with try/catch, if present AND `getRunMeta(df) === null`, use seed values (parser-side seed is the fallback prefill).
    3. If neither tag is present, leave both fields undefined.

    Extend `showAnnotationDialog(df)` to append two new inputs to the dialog AFTER `group2Cols`:
    1. `const instrumentInput = ui.input.string('Instrument', {value: seed.instrumentId ?? '', tooltipText: '<tooltip from UI-SPEC verbatim>'});`
    2. `const datetimeInput = ui.input.date('Acquisition datetime', {value: seed.acquisitionDatetime ? dayjs(seed.acquisitionDatetime) : null, tooltipText: '<tooltip from UI-SPEC verbatim>'});` — using `ui.input.date` (date-only), NOT `ui.input.dateTime`.
    3. Add both inputs to the existing `.add(...)` chain in dialog ordering: Group1Name, Group1Cols, Group2Name, Group2Cols, Instrument, Acquisition datetime (preserve existing order — UI-SPEC §"Annotate Experiment dialog — D-01 extension").

    On OK (inside the existing `.onOK(...)` callback, AFTER the existing setGroups call):
    - Read instrumentInput.value (string, may be empty) and datetimeInput.value (dayjs.Dayjs | null).
    - If EITHER is non-empty, build `meta: RunMeta = {instrument_id: instrumentInput.value ?? '', acquisition_datetime: datetimeInput.value ? datetimeInput.value.toISOString() : ''}` and call `setRunMeta(df, meta)`.
    - If BOTH are empty AND the df already has a persisted `proteomics.spc_run_meta` tag, do NOT clear it (preserve the prior annotation; user clearing both fields is an unusual case and CONTEXT.md D-01 tolerates empty values without forcing a clear).
    - The existing `grok.shell.info(\`Groups assigned: \${g1.length} + \${g2.length} samples\`)` toast remains; an additional info toast is NOT needed here (the existing one acknowledges OK).

    Empty values tolerated at annotation time per CONTEXT.md D-01 — only Compute SPC Status enforces non-empty (Task 2).
  </behavior>
  <action>
    Use Edit (not Write) on `src/analysis/experiment-setup.ts`. Add `import {RunMeta, setRunMeta, getRunMeta} from './spc';` at the top (if `'./spc'` import path differs, adjust to match Plan 02's file). Check whether `dayjs` is already imported in this file; if not, add `import * as dayjs from 'dayjs';` per the package's existing dayjs import convention.

    Extend `AnnotationSeed` with the two optional fields. Update `seedAnnotationDialogInputs` to perform the two new reads in priority order (persisted first, seed second). Update `showAnnotationDialog` to:
    1. Compute seed via the extended `seedAnnotationDialogInputs`.
    2. Create the two new inputs with verbatim tooltip text from UI-SPEC.
    3. Chain `.add(instrumentInput).add(datetimeInput)` after `.add(group2Cols)` and BEFORE `.onOK`.
    4. Inside `.onOK`, after the existing setGroups call but BEFORE the existing info toast, persist `setRunMeta(df, meta)` if either field is non-empty.

    Copy/paste discipline: the two tooltip strings MUST match UI-SPEC verbatim (whitespace, punctuation, exact wording). The dimension-1 copywriting checker greps for these — see Plan 01's RED test `SPC:run_meta_helpers` for the assertion shape.

    No fenced code in this action — signatures come from interfaces block.
  </action>
  <verify>
    <automated>cd packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --host localhost --category "SPC" --test "SPC:annotation_dialog_persists_run_meta"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ui.input.string('Instrument'" src/analysis/experiment-setup.ts` returns 1.
    - `grep -c "ui.input.date('Acquisition datetime'" src/analysis/experiment-setup.ts` returns 1.
    - `grep -c "ui.input.dateTime" src/analysis/experiment-setup.ts` returns 0 (RESEARCH.md gotcha #1 — dateTime does not exist).
    - `grep -c "setRunMeta(df, " src/analysis/experiment-setup.ts` returns ≥ 1.
    - `grep -c "Free-text identifier for the instrument" src/analysis/experiment-setup.ts` returns 1 (tooltip text verbatim).
    - `grep -c "Used to order runs on the SPC trend chart" src/analysis/experiment-setup.ts` returns 1.
    - `npm run build` exit 0; the existing `Annotate Experiment` dialog still opens (manual: open Datagrok → import any df → Proteomics → Annotate Experiment → confirm 6 inputs visible).
    - `SPC:annotation_dialog_persists_run_meta` GREEN: after dialog OK with instrument+datetime, `df.getTag('proteomics.spc_run_meta')` parses to the expected RunMeta and `getRunMeta(df)` returns that object. (Note: the pure-TS `SPC:run_meta_helpers` test is owned by Plan 16-02 — this test asserts the END-TO-END dialog flow, not the helper.)
    - Existing Annotate Experiment tests under `src/tests/analysis.ts` (if present — verify) still GREEN (no regression).
  </acceptance_criteria>
  <done>Annotate Experiment dialog gains two new inputs; seed + persist round-trip; SPC:annotation_dialog_persists_run_meta GREEN (the pure-TS SPC:run_meta_helpers stays owned by Plan 16-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the two new menu items + implement Compute SPC Status orchestrator + SPC Dashboard shim in src/package.ts</name>
  <files>src/package.ts</files>
  <read_first>
    - src/package.ts (existing menu handlers — read `differentialExpression`, `annotateExperiment`, `showVolcanoPlot` around lines 340-385 for handler conventions: decorator placement, `requireDifferentialExpression` precondition helper, progress indicator usage)
    - src/analysis/spc.ts (Plan 02 output) — full export list
    - src/analysis/spc-storage.ts (Plan 03 output) — full export list
    - .planning/phases/16-sample-level-spc-tracking/16-UI-SPEC.md § "Refuse states" + § "Success confirmations" + § "Interaction Flow — Locked Sequences" → "1. Compute SPC Status"
    - .planning/phases/16-sample-level-spc-tracking/16-CONTEXT.md § D-01 (gate sequence)
    - src/tests/spc.ts → tests "SPC:candidates_refuse", "SPC:status_tags" (integrated path)
  </read_first>
  <behavior>
    Add two new static async methods on the `PackageFunctions` class:

    `computeSpcStatus()` — registered as `Proteomics | Analyze | Compute SPC Status` (immediate, NO `...`):
    1. `const tv = grok.shell.tv; const df = tv?.dataFrame;`
    2. Gate A — df present: `if (!tv || !df) { grok.shell.warning('Open an analyzed file first, then run Compute SPC Status.'); return; }` (UI-SPEC verbatim copy).
    3. Gate B — source !== 'spectronaut-candidates': `if (df.getTag('proteomics.source') === 'spectronaut-candidates') { grok.shell.warning('SPC requires per-sample intensities. Re-import this analysis from the Spectronaut PG report (not the Candidates report) to compute SPC.'); return; }`.
    4. Gate C — run-meta non-empty: `const runMeta = getRunMeta(df); if (!runMeta || !runMeta.instrument_id || !runMeta.acquisition_datetime) { grok.shell.warning('Open Annotate Experiment to set instrument + acquisition datetime.'); return; }`.
    5. Gate D — groups annotated: `const groups = getGroups(df); if (!groups) { grok.shell.warning('Annotate experimental groups first (Proteomics → Annotate Experiment) — SPC needs Group 1 to compute control-replicate correlation.'); return; }`.
    6. Group 1 size check (INFO, proceeds): `if (groups.group1.columns.length < 2) { grok.shell.info('Group 1 has fewer than 2 samples — control-replicate correlation can\\'t be computed. The other three metrics will still be recorded.'); }` — info-level, computation continues (UI-SPEC: info, proceed).
    7. Open `const pi = DG.TaskBarProgressIndicator.create('Computing SPC status...');` (per UI-SPEC §Interaction Flow). Wrap remaining steps in try/finally with `pi.close()` in finally.
    8. `const metrics = computeSpcMetrics(df, groups, runMeta);`
    9. `const baseline = await loadBaseline(runMeta.instrument_id);` — may be null on first-ever run for this instrument.
    10. Build prior-series + enabled-rules from baseline OR sensible defaults if baseline is null:
        - If `baseline === null`: skip Nelson rule evaluation (no baseline → no statistical comparison possible). Set `ruleResult = {status: 'pass', rulesTripped: []}` and surface an info toast: `'No baseline locked for <instrument_id> yet — recorded as pass. Define a baseline once at least 4 runs have been computed.'` (Add this exact copy — covers the case where the operator computes runs BEFORE locking a baseline, which is the expected pre-baseline workflow.)
        - Else: load prior series via `await loadRuns(runMeta.instrument_id)` → extract the four metric arrays → call `evaluateNelsonRules(metrics, priorSeries, baseline.metrics, baseline.rules_enabled) → ruleResult`.
    11. Capture the prior status BEFORE overwrite: `const priorRow = (await loadRuns(runMeta.instrument_id)).find(r => r.acquisition_datetime === runMeta.acquisition_datetime); const priorStatus = priorRow?.status ?? null;`.
    12. `setSpcStatus(df, metrics, ruleResult);` (writes tags + belt-and-braces column on df).
    13. Build the runs-csv row: `const row = { instrument_id: runMeta.instrument_id, acquisition_datetime: runMeta.acquisition_datetime, run_label: df.name, median_intensity: metrics.median_intensity, missing_pct: metrics.missing_pct, control_corr: metrics.control_corr, protein_count: metrics.protein_count, status: ruleResult.status, rules_tripped: JSON.stringify(ruleResult.rulesTripped), source_project_id: null, source_df_name: df.name, computed_at: metrics.computed_at };` and `await appendRun(row);`.
    14. Pick success toast:
        - If priorStatus !== null: `grok.shell.info(\`Updated SPC for \${df.name} (previous status: \${priorStatus}).\`)` (UI-SPEC: "Compute SPC Status overwrites prior row" line).
        - Else if ruleResult.status === 'pass': `grok.shell.info(\`SPC computed: \${df.name} — status: pass.\`)`.
        - Else if ruleResult.status === 'flagged': `grok.shell.info(\`SPC computed: \${df.name} — flagged on \${ruleResult.rulesTripped.length} rule(s): \${ruleResult.rulesTripped.join(', ')}.\`)`.
        - Else (out_of_control): `grok.shell.info(\`SPC computed: \${df.name} — OUT OF CONTROL on \${ruleResult.rulesTripped.length} rule(s): \${ruleResult.rulesTripped.join(', ')}.\`)`.

    `showSpcDashboard()` — registered as `Proteomics | Visualize | SPC Dashboard...`:
    - Plan 06 owns the implementation. This task registers the menu + emits a stub that surfaces `grok.shell.info('SPC Dashboard wiring lands in plan 16-06 — for now, runs.csv is at System:AppData/Proteomics/spc/runs.csv.')` so the menu is reachable and the dependency chain is satisfied. Plan 06 will REPLACE this body.
  </behavior>
  <action>
    Use Edit (not Write) on `src/package.ts`. Add imports at the top: `import {computeSpcMetrics, evaluateNelsonRules, setSpcStatus, getRunMeta, RunMeta, SpcRulesEnabled} from './analysis/spc';` and `import {appendRun, loadRuns, loadBaseline} from './analysis/spc-storage';`. Verify `getGroups` is already imported (existing menu handlers use it); if not, add `import {getGroups} from './analysis/experiment-setup';`.

    Add two new `@grok.decorators.func` decorated static async methods on `PackageFunctions`. Place them in the same region as the other Analyze/Visualize handlers — between `differentialExpression` and `showVolcanoPlot` (Compute SPC Status under Analyze), and after `showPcaPlot` or near `showQcDashboard` (SPC Dashboard under Visualize). Match the existing decorator + handler shape verbatim — same docstring style, same `tv?.dataFrame` early-return pattern, same `requireSampleLevelData` helper usage if applicable (but verify: SPC does NOT require sample-level data per se — Group 1 control_corr is the only sample-level dependency, gated separately above).

    Toast copy MUST match UI-SPEC verbatim. The dimension-1 copywriting checker greps every reviewer-facing string against the banned-jargon list (`DataFrame`, `tag`, `semType`, `viewer factory`, `helper`, `ACL`, `dispatcher`, `mutate`). The toast strings above are biologist-audited and contain none of those words.

    Stub for SPC Dashboard: emit `grok.shell.info(...)` toast referencing 16-06. Plan 06 REPLACES this body.

    No fenced code in this action — signatures come from interfaces block.
  </action>
  <verify>
    <automated>cd packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --host localhost --category "SPC" --test "SPC:candidates_refuse|SPC:status_tags|SPC:classification|SPC:idempotent_upsert"</automated>
    <human-check>Manually run end-to-end: import a Spectronaut PG export → confirm seed prefills Annotate Experiment dialog → OK → Compute SPC Status → confirm `System:AppData/Proteomics/spc/runs.csv` now has 1 row. Re-run Compute SPC Status on the same df → confirm "Updated SPC for ... (previous status: ...)" toast AND runs.csv still has 1 row (idempotent upsert).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "Proteomics | Analyze | Compute SPC Status" src/package.ts` returns 1.
    - `grep -c "Proteomics | Visualize | SPC Dashboard...'" src/package.ts` returns 1.
    - `grep -c "Open an analyzed file first" src/package.ts` returns 1 (UI-SPEC refuse copy verbatim).
    - `grep -c "SPC requires per-sample intensities" src/package.ts` returns 1.
    - `grep -c "Open Annotate Experiment to set instrument" src/package.ts` returns 1.
    - `grep -c "Annotate experimental groups first" src/package.ts` returns 1.
    - `grep -c "Updated SPC for" src/package.ts` returns 1.
    - `grep -c "OUT OF CONTROL" src/package.ts` returns 1.
    - No banned jargon in new toast strings: `grep -E "DataFrame|semType|viewer factory" src/package.ts` returns the SAME line count as before this edit (i.e. no NEW occurrences in the SPC-related additions — verify by diffing against the pre-edit baseline).
    - `npm run build` exit 0.
    - 4 tests listed in `<verify>` GREEN. Cumulative SPC GREEN: 23 (after Plan 04) + at most 4 (status_tags + classification were already GREEN after Plan 02; this adds candidates_refuse + idempotent_upsert was already GREEN from Plan 03 — net 1 new GREEN, but the integrated path is now exercised) = 25 of 29 non-Pareto tests (this plan adds SPC:annotation_dialog_persists_run_meta + SPC:candidates_refuse as NEW GREENs; status_tags + classification + idempotent_upsert were already GREEN after Plans 02/03 and remain GREEN through the integrated path).
    - Manual integration test (human-check): end-to-end import → annotate → compute → second-compute-overwrites works.
  </acceptance_criteria>
  <done>Two new menu items registered; Compute SPC Status orchestrator wires Plans 02 + 03 + 04 end-to-end; SPC Dashboard stub registered (Plan 06 replaces body); manual integration test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| df tag `proteomics.source` → gate decision | Trusted within package boundaries; written only by parsers in `src/parsers/*` |
| `runMeta.instrument_id` user input → `slugifyInstrumentId` → AppData path | Inherits T-16-01 mitigation from Plan 03 |
| Re-compute on same key → overwrite | Surfaced to operator via "Updated SPC..." toast (T-16-03 mitigation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | `instrument_id` propagation to Plan 03's `slugifyInstrumentId` | mitigate | Inherited from Plan 03. Plan 05 passes the value through unchanged. |
| T-16-03 | Tampering | Idempotent upsert visibility | mitigate | "Updated SPC for <run_label> (previous status: <prior_status>)" info toast explicit. Operator sees that a prior row was overwritten. |
</threat_model>

<verification>
- `SPC:candidates_refuse`, `SPC:idempotent_upsert` (integrated), `SPC:status_tags` (integrated), `SPC:classification` (integrated) all GREEN.
- Human end-to-end run yields exactly 1 row in `System:AppData/Proteomics/spc/runs.csv` after two computes on same key.
- `npm run build` exit 0; no banned-jargon regressions.
</verification>

<success_criteria>
The full Compute SPC Status pipeline is reachable from the menu. Annotate Experiment auto-prefills from parser seed. Re-running on the same key is idempotent AND surfaces "Updated SPC ..." to the operator. Plan 06 can now focus solely on the dashboard viewer.
</success_criteria>

<output>
Create `.planning/phases/16-sample-level-spc-tracking/16-05-SUMMARY.md` when done.
</output>
