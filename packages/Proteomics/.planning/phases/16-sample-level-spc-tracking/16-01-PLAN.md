---
phase: 16-sample-level-spc-tracking
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/spc.ts
  - src/tests/spc-formula-lines-spike.ts
  - src/package-test.ts
autonomous: false
requirements:
  - SPC-01
  - SPC-02
  - SPC-03
  - SPC-04
  - SPC-05
  - SPC-06
  - SPC-07
  - SPC-08

must_haves:
  truths:
    - "A formula-line spike answers whether DG.Viewer.lineChart honors style: 'dashed' on a datetime X-axis BEFORE the dashboard plan starts. Either outcome (PASS = dashed works; FAIL = fallback path required) is acceptable — the spike's purpose is to ANSWER the question, not to gate Wave 0."
    - "On spike completion, the file .planning/phases/16-sample-level-spc-tracking/16-01-SPIKE-RESULT.md exists carrying exactly one of `OUTCOME: PASS` or `OUTCOME: FAIL`. Plan 06's createSpcChartPanel branches on this marker: PASS uses style:'dashed'; FAIL uses the constant-column overlay fallback (see 16-06-PLAN.md Task 1 behavior step 2)."
    - "A RED test file src/tests/spc.ts under category 'SPC' enumerates 29 expected behaviors mapped to SPC-01..SPC-08 + idempotency + belt-and-braces + run-meta dialog + spike result — every test starts FAILING (functions under test do not yet exist)."
    - "Running `grok test --host localhost --category SPC` returns N failing tests; this is the Wave 0 RED baseline that Waves 1-3 turn GREEN."
  artifacts:
    - path: "src/tests/spc-formula-lines-spike.ts"
      provides: "15-min visual spike — opens a DG.Viewer.lineChart with 3 formula lines on a datetime X-axis. Writes 16-01-SPIKE-RESULT.md with OUTCOME: PASS or OUTCOME: FAIL."
    - path: "src/tests/spc.ts"
      provides: "RED stubs for SPC-01..SPC-08 + idempotency + belt-and-braces + Spectronaut seed + annotation_dialog_persists_run_meta"
      contains: "category('SPC'"
    - path: "src/package-test.ts"
      provides: "Registers ./tests/spc and ./tests/spc-formula-lines-spike imports"
  key_links:
    - from: "src/package-test.ts"
      to: "src/tests/spc.ts"
      via: "import './tests/spc';"
      pattern: "import.*tests/spc"
    - from: "src/tests/spc.ts"
      to: "@datagrok-libraries/test"
      via: "category('SPC', () => { test(...) })"
      pattern: "category\\('SPC'"
    - from: "src/tests/spc-formula-lines-spike.ts"
      to: ".planning/phases/16-sample-level-spc-tracking/16-01-SPIKE-RESULT.md"
      via: "writes outcome marker for Plan 06 to branch on"
      pattern: "OUTCOME: (PASS|FAIL)"
---

<objective>
Establish the Wave 0 baseline for Phase 16: a Datagrok-platform spike that answers RESEARCH.md Assumption A1/A2 (does `DG.Viewer.lineChart` honor `style: 'dashed'` formula lines on a datetime X-axis?) AND a complete RED test scaffold under category 'SPC' that mirrors VALIDATION.md row-by-row. Downstream plans (02–07) flip these RED tests to GREEN.

Purpose: Lock the chart-rendering contract BEFORE the dashboard is built so a fallback (constant-column overlay) can be locked in plan 06 without rework. Lock the test contract BEFORE math/storage/viewer code exists so executors implement against tests, not against hand-waved specs.

Output: Two test files committed, package-test.ts registered, one spike result file (`16-01-SPIKE-RESULT.md`) carrying `OUTCOME: PASS` or `OUTCOME: FAIL`, and one human-verified spike screenshot for VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-sample-level-spc-tracking/16-CONTEXT.md
@.planning/phases/16-sample-level-spc-tracking/16-RESEARCH.md
@.planning/phases/16-sample-level-spc-tracking/16-VALIDATION.md
@.planning/phases/16-sample-level-spc-tracking/16-UI-SPEC.md
@packages/Proteomics/CLAUDE.md
@packages/Proteomics/src/viewers/qc-dashboard.ts
@packages/Proteomics/src/tests/publish-roundtrip.ts
@packages/Proteomics/src/package-test.ts
</context>

<interfaces>
<!-- Test framework signatures executor will use; extracted from existing tests. -->

From `@datagrok-libraries/test/src/test` (used by every existing test file):
- `category(name: string, fn: () => void): void` — groups tests
- `test(name: string, fn: () => Promise<void> | void, opts?: {skipReason?: string}): void`
- `expect(actual: any, expected?: any): void`
- `awaitCheck(predicate: () => boolean, msg?: string, timeoutMs?: number): Promise<void>`

From `datagrok-api/dg` (verified for spike):
- `DG.Viewer.lineChart(df: DG.DataFrame, options?: object): DG.Viewer`
- `viewer.meta.formulaLines.addLine({formula: string, color?: string, width?: number, style?: string, visible?: boolean, title?: string}): void`

From `datagrok-api/grok`:
- `grok.shell.addTableView(df: DG.DataFrame): DG.TableView`

Existing precedent for formula-line addition: `src/viewers/qc-dashboard.ts:87-94` (`${M} = 0` solid line on scatter plot).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the formula-lines spike fixture as an automated test</name>
  <files>src/tests/spc-formula-lines-spike.ts</files>
  <read_first>
    - src/viewers/qc-dashboard.ts (lines 80-95) — existing M=0 formula-line precedent
    - src/tests/publish-roundtrip.ts (first 30 lines) — category()/test() import pattern
    - .planning/phases/16-sample-level-spc-tracking/16-RESEARCH.md § "Open Questions (RESOLVED)" #1 and #2
  </read_first>
  <behavior>
    - Test "SPC:spike_formula_line_dashed_on_datetime" creates a 5-row in-memory DG.DataFrame with one Datetime column `acquisition_datetime` and one Float column `metric`, opens it as a DG.Viewer.lineChart, wraps `viewer.meta.formulaLines.addLine({formula: '${metric} = 5', style: 'dashed', color: '#666666', width: 1, visible: true, title: 'UCL'})` in a try/catch, and based on the outcome writes `.planning/phases/16-sample-level-spc-tracking/16-01-SPIKE-RESULT.md` with either `OUTCOME: PASS` (no throw + `items[0].style === 'dashed'`) or `OUTCOME: FAIL` (throw OR style not honored), capturing the verbatim platform error message in the FAIL branch.
    - Test "SPC:spike_three_lines_ucl_cl_lcl" adds 3 formula lines (UCL dashed, CL solid, LCL dashed) in a try/catch and records its observation into the same result file as a secondary section. NEITHER test asserts pass-or-fail on the dashed style — the spike's purpose is to ANSWER, not to gate.
    - Tests live under `category('SPC')` so they run in the same `grok test --category SPC` command as the rest of Phase 16.
  </behavior>
  <action>
    Create `src/tests/spc-formula-lines-spike.ts` as a normal Datagrok test file (per testing.md). Imports: `import * as grok from 'datagrok-api/grok'; import * as DG from 'datagrok-api/dg'; import {category, test, expect} from '@datagrok-libraries/test/src/test';`. Wrap both tests in `category('SPC', () => { ... })`. Build the DataFrame via `DG.DataFrame.fromColumns([DG.Column.fromList(DG.COLUMN_TYPE.DATE_TIME, 'acquisition_datetime', [...]), DG.Column.fromFloat32Array('metric', new Float32Array([...]))])`. Do NOT call `grok.shell.addTableView` (keeps the test headless). Use `DG.Viewer.lineChart(df, {xColumnName: 'acquisition_datetime', yColumnName: 'metric'})`. Wrap the `addLine({style: 'dashed', ...})` call in `try { ... } catch (e) { ... }`. In the SUCCESS branch, read back `viewer.meta.formulaLines.items[0].style`; if `=== 'dashed'`, record `OUTCOME: PASS`. In the FAIL branch (caught error OR returned style !== 'dashed'), record `OUTCOME: FAIL` with the platform error captured verbatim. Persist the outcome marker to `.planning/phases/16-sample-level-spc-tracking/16-01-SPIKE-RESULT.md` via `grok.dapi.files.writeAsText('System:AppData/Proteomics/spike/spike-result.md', ...)` AND emit via `grok.shell.info(...)` + `console.log(...)` so the operator can transcribe outcome verbatim into the planning-folder result file (or copy the AppData file into place during the checkpoint). The test ITSELF should always reach `expect(true, true)` at the end — it does not gate on the dashed outcome. The spike's purpose is to populate `16-01-SPIKE-RESULT.md` with one of two markers so Plan 06 can branch deterministically (PASS → use `style: 'dashed'`; FAIL → use the constant-column overlay fallback documented in Plan 16-06's `<behavior>` step 2 of `createSpcChartPanel`). Use the exact field names from RESEARCH.md Pattern 4 (`formula`, `color`, `width`, `style`, `visible`, `title`). No fenced code in this action — see interfaces block for signatures. Run order: test must execute when `grok test --host localhost --category SPC --test "spike_formula_line_dashed"` is called.
  </action>
  <verify>
    <automated>cd packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --host localhost --category "SPC" --test "spike_formula_line" &amp;&amp; grep -E "^OUTCOME: (PASS|FAIL)" .planning/phases/16-sample-level-spc-tracking/16-01-SPIKE-RESULT.md</automated>
    <human-check>After test runs, open `Visualize → SPC Dashboard` is not possible yet (Wave 3 territory). Instead, open Datagrok dev console → run `grok.functions.call('Proteomics:test', {category: 'SPC', test: 'spike_three_lines'})` and visually confirm via screenshot that the `viewer.meta.formulaLines.items` payload either contains `style: 'dashed'` (PASS) or shows the platform error (FAIL). Either outcome is acceptable; record verbatim into `16-01-SPIKE-RESULT.md` and reference from VERIFICATION.md.</human-check>
  </verify>
  <acceptance_criteria>
    - File `src/tests/spc-formula-lines-spike.ts` exists with `category('SPC', () => { ... })` wrapper.
    - `grep -c "style.*dashed" src/tests/spc-formula-lines-spike.ts` returns at least 2 (two separate dashed lines asserted).
    - The spike test runs to completion and a result file `.planning/phases/16-sample-level-spc-tracking/16-01-SPIKE-RESULT.md` exists containing exactly one of `OUTCOME: PASS` (dashed style honored on datetime X) or `OUTCOME: FAIL` (fallback path required). Either outcome is acceptable — the spike's purpose is to ANSWER the question, not to gate Wave 0.
    - Gate command: `grep -E "^OUTCOME: (PASS|FAIL)" .planning/phases/16-sample-level-spc-tracking/16-01-SPIKE-RESULT.md` exits 0.
    - If outcome is FAIL, Plan 06 activates the fallback path documented in its `createSpcChartPanel` `<behavior>` step 2: render UCL/LCL as a sibling DG.Column overlay (a second yColumn on the same lineChart holding the constant UCL value at every row). Cross-reference: see `.planning/phases/16-sample-level-spc-tracking/16-06-PLAN.md` Task 1 behavior.
  </acceptance_criteria>
  <done>Spike test file exists, registered, executes against a running Datagrok instance, and the result file documents the formula-line/datetime contract for Plan 06 (PASS or FAIL — both are acceptable Wave-0 outcomes).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RED test scaffold src/tests/spc.ts mapping VALIDATION.md rows</name>
  <files>src/tests/spc.ts</files>
  <read_first>
    - .planning/phases/16-sample-level-spc-tracking/16-VALIDATION.md (lines 39-87)
    - .planning/phases/16-sample-level-spc-tracking/16-RESEARCH.md § "Validation Architecture" → "Phase Requirements → Test Map" (lines 502-532)
    - src/tests/publish-roundtrip.ts — convention reference for category()/test() shape, file size, awaitCheck/expect usage
    - src/analysis/experiment-setup.ts lines 1-30 — pattern for tag-helper test inputs
  </read_first>
  <behavior>
    The file contains exactly one `category('SPC', () => { ... })` block holding the following test names (each test BODY is a stub that calls a future helper from `src/analysis/spc.ts` / `src/analysis/spc-storage.ts` / `src/viewers/spc-dashboard.ts` — those helpers don't exist yet, so every test fails RED):

      Per-metric math (SPC-01):
      - "SPC:median_intensity"
      - "SPC:missing_pct"
      - "SPC:control_corr"
      - "SPC:protein_count"

      Nelson rules (SPC-03):
      - "SPC:nelson_default"
      - "SPC:nelson_rule_1_3sigma"
      - "SPC:nelson_rule_5_2of3"
      - "SPC:false_alarm_rate"

      Status tags + classification (SPC-04):
      - "SPC:status_tags"
      - "SPC:classification"

      Baseline (SPC-05):
      - "SPC:baseline_outlier_removal"
      - "SPC:baseline_iteration_cap"
      - "SPC:baseline_roundtrip"
      - "SPC:baseline_rebuild_overwrites"
      - "SPC:rule_toggle_per_instrument"

      Run identity (SPC-06):
      - "SPC:run_meta_helpers"               ← owned by Plan 16-02 GREEN milestone (helper round-trip ONLY: setRunMeta(df,m) → getRunMeta(df) === m)
      - "SPC:annotation_dialog_persists_run_meta"   ← owned by Plan 16-05 GREEN milestone (dialog OK writes proteomics.spc_run_meta via getRunMeta)
      - "SPC:spectronaut_seed"
      - "SPC:backfill_ordering"
      - "SPC:candidates_refuse"

      Storage + idempotency (D-02 / Pitfall 8):
      - "SPC:idempotent_upsert"
      - "SPC:storage_bounded"
      - "SPC:schema_version"
      - "SPC:column_idempotent"
      - "SPC:belt_and_braces"

      Drill-down + Pareto (SPC-07 / SPC-08):
      - "SPC:drilldown_resolved"
      - "SPC:drilldown_missing"
      - "SPC:dashboard_renders"
      - "SPC:formula_lines"
      - "SPC:pareto_descending"

    Each test body asserts the behavior described in RESEARCH.md "Validation Architecture" for its row. Where the helper under test does not yet exist (Plans 02–07 produce them), the test body should reference the helper name expected by the helper's eventual contract — failing with a clear "function not found" error is acceptable RED for Wave 0.

    The SPLIT between `SPC:run_meta_helpers` (Plan 16-02 ownership) and `SPC:annotation_dialog_persists_run_meta` (Plan 16-05 ownership) is intentional — the first asserts the pure-TS helper round-trip; the second asserts the user-facing dialog flow writes the same tag. RESEARCH.md "Phase Requirements → Test Map" should be read with this split in mind.
  </behavior>
  <action>
    Create `src/tests/spc.ts` with the imports `import * as grok from 'datagrok-api/grok'; import * as DG from 'datagrok-api/dg'; import {category, test, expect, awaitCheck, delay} from '@datagrok-libraries/test/src/test';`. Wrap all 29 test() calls in one `category('SPC', () => { ... })`. For helper-not-yet-defined tests, import lazily inside the test body so the module loads (e.g. `const {computeSpcMetrics} = await import('../analysis/spc');` — when the file does not exist, the import throws and the test fails RED with a deterministic message). For the synthetic-fixture helpers identified in RESEARCH.md Wave 0 Gaps (`makeInControlSeries`, `makeOutOfControlSeries`, `makeSyntheticRunsDf`, `makeSyntheticSpectronautHeader`), define them as top-level non-exported functions INSIDE this test file (not in a fixtures module). For "SPC:false_alarm_rate" use a seeded LCG (linear congruential generator) inline — do NOT depend on a seeded-random npm package. For the AppData-touching tests (`baseline_roundtrip`, `idempotent_upsert`, `schema_version`), use a per-test fixture path under `System:AppData/Proteomics/spc-test/<uuid>/` and clean it up in a `finally` block via `grok.dapi.files.delete(...)`. The fixture path MUST be sanitized to `[A-Za-z0-9._-]+` per T-16-01.

    Test ownership split for run-meta:
    - `SPC:run_meta_helpers` — body: `const {setRunMeta, getRunMeta} = await import('../analysis/spc'); const df = DG.DataFrame.create(); const meta = {instrument_id: 'Q-01', acquisition_datetime: '2026-07-10T00:00:00.000Z'}; setRunMeta(df, meta); expect(JSON.stringify(getRunMeta(df)), JSON.stringify(meta));`. Plan 16-02 turns this GREEN. NO dialog interaction.
    - `SPC:annotation_dialog_persists_run_meta` — body: opens `showAnnotationDialog(df)` via `await import('../analysis/experiment-setup')`, simulates user-fills + OK programmatically (read dialog instance, set input values, call onOK), then asserts `df.getTag('proteomics.spc_run_meta')` parses to the expected RunMeta. Plan 16-05 turns this GREEN. The test MUST not assert any helper round-trip directly — it asserts the END-TO-END dialog flow.

    No fenced code in this action — signatures come from the existing `publish-roundtrip.ts` precedent and `@datagrok-libraries/test` interfaces above. Length target: 29 tests × ~10-25 lines each ≈ 600 LOC.
  </action>
  <verify>
    <automated>cd packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --host localhost --category "SPC" --verbose 2>&amp;1 | grep -E "(passed|failed|skipped|error)" | head -40</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tests/spc.ts` exists with exactly one `category('SPC', () => { ... })` block.
    - `grep -c "test(" src/tests/spc.ts` returns ≥ 29 (matches the 29 enumerated test names above — note: run_meta_helpers + annotation_dialog_persists_run_meta are SEPARATE tests).
    - `grep -c "test('SPC:" src/tests/spc.ts` returns ≥ 29 (every test name follows the `SPC:` prefix convention).
    - `grep -c "SPC:run_meta_helpers" src/tests/spc.ts` returns 1.
    - `grep -c "SPC:annotation_dialog_persists_run_meta" src/tests/spc.ts` returns 1.
    - Running `grok test --host localhost --category SPC` after building reports at least 26 FAILING tests (the spike from Task 1 passes; the rest are RED because their target helpers don't exist yet). Output contains "failed" rows for `SPC:median_intensity`, `SPC:baseline_roundtrip`, `SPC:spectronaut_seed`, `SPC:drilldown_resolved` (sampled). This is the Wave 0 RED baseline.
    - `grep -c "System:AppData/Proteomics/spc-test/" src/tests/spc.ts` ≥ 3 (AppData-touching tests use sandboxed fixture path, not the production runs.csv path).
    - File compiles: `npm run build` exit code 0.
  </acceptance_criteria>
  <done>RED test scaffold registered, ≥ 26 tests fail with deterministic "function not found" / "helper not implemented" messages, build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Register both test files in src/package-test.ts</name>
  <files>src/package-test.ts</files>
  <read_first>
    - src/package-test.ts (current contents, lines 1-25) — to see exact insertion point after `import './tests/publish-roundtrip';`
  </read_first>
  <action>
    Open `src/package-test.ts` and insert two new import lines AFTER `import './tests/publish-roundtrip';` and BEFORE `export const _package = ...`. The two new imports are `import './tests/spc-formula-lines-spike';` and `import './tests/spc';` (in that order so the spike registers before the main category). Preserve the existing import block ordering — do not re-sort or re-format other imports. Use Edit, not Write.
  </action>
  <verify>
    <automated>grep -n "tests/spc" /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics/src/package-test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import './tests/spc'" src/package-test.ts` returns exactly 1.
    - `grep -c "import './tests/spc-formula-lines-spike'" src/package-test.ts` returns exactly 1.
    - The line `export const _package = new DG.Package();` still appears AFTER both spc imports.
    - `npm run build` exits 0 from packages/Proteomics.
  </acceptance_criteria>
  <done>Both test files registered, build clean, `grok test --category SPC` discovers ≥ 29 tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human verification gate</name>
  <action>Halt execution. Present the `<what-built>` summary and `<how-to-verify>` steps to the operator. Wait for the resume signal before proceeding.</action>
  <what-built>Wave 0 spike test answers (with PASS or FAIL — both are acceptable outcomes) whether `DG.Viewer.lineChart` honors `style: 'dashed'` on a datetime X-axis. RED scaffold of 29 SPC tests registered under category `SPC`. Spike outcome recorded in `16-01-SPIKE-RESULT.md` — Plan 16-06 will branch on this marker.</what-built>
  <how-to-verify>
    1. From `packages/Proteomics`, run `grok test --host localhost --category "SPC" --test "spike_formula_line_dashed_on_datetime"` — the test runs to completion regardless of outcome. Then `cat .planning/phases/16-sample-level-spc-tracking/16-01-SPIKE-RESULT.md` — confirm exactly one of `OUTCOME: PASS` (dashed style honored) or `OUTCOME: FAIL` (fallback path will activate in Plan 06) is present. If FAIL, capture the platform error verbatim — Plan 06's constant-column overlay branch activates.
    2. Run `grok test --host localhost --category "SPC" --verbose` — confirm at least 26 of the 29 tests fail RED with messages like "Cannot find module '../analysis/spc'" or "computeSpcMetrics is not a function". This proves the test contract is real and downstream plans have a deterministic GREEN target.
    3. Open Datagrok in a browser → File menu → confirm the Proteomics menu still loads (sanity: test imports did not break the package's autostart path).

    Display to operator: "Spike outcome: {PASS|FAIL}. If FAIL, Plan 06 fallback path (constant-column overlay) activates automatically based on `16-01-SPIKE-RESULT.md` marker."
  </how-to-verify>
  <resume-signal>Type "approved" (spike outcome captured + RED baseline locked in) or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture path → AppData filesystem | Test code writes/reads under `System:AppData/Proteomics/spc-test/<uuid>/`; uuid is generated via `crypto.randomUUID()` and used as a path component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-test | Tampering | test fixture directory naming under AppData | mitigate | Use `crypto.randomUUID()` (charset `[a-f0-9-]`) so the path is intrinsically sanitized; reject non-uuid path components. Documented in test fixtures helper. |
| T-16-02-test | Tampering | test code reads sibling production `runs.csv` and gets confused | mitigate | All AppData-touching tests use `System:AppData/Proteomics/spc-test/<uuid>/` ONLY — they NEVER touch `System:AppData/Proteomics/spc/runs.csv` (production path). Asserted by grep gate in Task 2 acceptance. |
</threat_model>

<verification>
- `grok test --host localhost --category "SPC" --test "spike_formula_line"` completes (test passes; outcome of spike recorded in result file)
- `grep -E "^OUTCOME: (PASS|FAIL)" .planning/phases/16-sample-level-spc-tracking/16-01-SPIKE-RESULT.md` exits 0
- `grok test --host localhost --category "SPC"` reports ≥ 26 failing tests with deterministic "helper not implemented" error messages
- `npm run build` exit 0
- `grep -c "test('SPC:" src/tests/spc.ts` ≥ 29
</verification>

<success_criteria>
Wave 0 baseline locked: the formula-line API contract is known (PASS recorded, or FAIL recorded with the documented fallback for Plan 06), and 29 RED tests under category `SPC` are ready for Plans 02–07 to turn GREEN one at a time.
</success_criteria>

<output>
Create `.planning/phases/16-sample-level-spc-tracking/16-01-SUMMARY.md` when done. Record: (a) spike test outcome (PASS = dashed works; FAIL = which fallback Plan 06 uses, with verbatim platform error); (b) count of RED tests registered; (c) git commit hashes.
</output>
</output>
