---
phase: 16-sample-level-spc-tracking
plan: 06
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-02
  - 16-03
  - 16-05
files_modified:
  - src/viewers/spc-dashboard.ts
  - src/package.ts
autonomous: false
requirements:
  - SPC-02
  - SPC-03
  - SPC-05
  - SPC-07

must_haves:
  truths:
    - "`openSpcDashboard()` opens a NEW TableView for the runs.csv-derived sibling DataFrame (per ARCHITECTURE.md §'Cross-DataFrame Selection') — the dashboard NEVER docks viewers onto the protein DataFrame's view."
    - "First open with no baseline locked shows the empty-state banner with VERBATIM UI-SPEC copy + [Define baseline...] CTA."
    - "Define-baseline modal renders a checkbox list of all runs for the selected instrument (ordered by acquisition_datetime ASCENDING, default first-7 checked), an 'Iterate 3σ outlier removal' toggle (default ON, cap 2), AND a 4-metric × 8-rule Nelson rule toggle grid (rules 1+5 ON; rest OFF)."
    - "On modal OK, iterativeOutlierRemoval runs per metric, computeBaselineStats runs over retained runs, saveBaseline persists the LockedBaseline JSON — single file per instrument, overwrites on rebuild."
    - "Dashboard renders 4 I-charts (median_intensity, missing_pct, control_corr, protein_count) and 4 MR-charts as DG.Viewer.lineChart with X = acquisition_datetime; each I-chart carries 3 formula lines (UCL/CL/LCL) using the exact style from Plan 01's spike outcome (dashed if spike PASSED; constant-column overlay if spike FAILED)."
    - "Drill-down: clicking a row in the table that has `status === 'flagged' OR 'out_of_control'` AND `source_project_id !== null` calls `grok.dapi.projects.find(id).then(p => p?.open())`. Null id → warning toast with VERBATIM UI-SPEC copy. id resolves but `find` returns null → different warning toast (also verbatim)."
    - "Sidebar carries the instrument picker, baseline status block, per-metric rule-toggle grid (4×8), and [Rebuild baseline...] button. Rule toggles persist to baseline JSON IMMEDIATELY on change (no separate save step) and re-evaluate Nelson rules over all runs in the slice (point colors update in place)."
    - "Last-picked instrument is cached via `grok.dapi.userDataStorage.put('spc.last_picked_instrument', ...)` (Phase 13 D-02 precedent — userDataStorage is soft-deprecated but matches v1.3 codebase; v1.5 migration to userSettings is captured as a pending todo)."
  artifacts:
    - path: "src/viewers/spc-dashboard.ts"
      provides: "openSpcDashboard + createSpcChartPanel + showDefineBaselineDialog + drillDownHandler + sidebar"
      min_lines: 350
      exports:
        - "openSpcDashboard"
        - "createSpcChartPanel"
        - "showDefineBaselineDialog"
    - path: "src/package.ts"
      provides: "showSpcDashboard handler body REPLACED to call openSpcDashboard"
  key_links:
    - from: "src/viewers/spc-dashboard.ts"
      to: "src/analysis/spc-storage.ts (loadRuns, loadBaseline, saveBaseline)"
      via: "imports"
      pattern: "from '../analysis/spc-storage'"
    - from: "src/viewers/spc-dashboard.ts"
      to: "src/analysis/spc.ts (evaluateNelsonRules, WHEELER_D4)"
      via: "imports (rule re-evaluation on toggle + MR-chart UCL formula)"
      pattern: "from '../analysis/spc'"
    - from: "src/viewers/spc-dashboard.ts I-chart click handler"
      to: "grok.dapi.projects.find(id).open()"
      via: "drill-down via Phase 15 precedent"
      pattern: "grok\\.dapi\\.projects\\.find"
---

<objective>
Build the SPC Dashboard viewer + sidebar + Define-baseline modal + drill-down handler. This plan owns the entire user-facing visualization surface for SPC and turns the remaining RED tests GREEN: `SPC:dashboard_renders`, `SPC:formula_lines`, `SPC:drilldown_resolved`, `SPC:drilldown_missing`. Per CONTEXT.md `<specifics>`, the Pareto chart (SPC-08, P2) is intentionally DEFERRED to Plan 07 so it can ship as a v1.4.1 hotfix without unwinding this plan.

Purpose: This is the most visually complex artifact in Phase 16 and the only one operators interact with daily. It must (a) honor PCA-style sibling-TableView rule (never dock onto the protein df's view), (b) carry zero banned-jargon strings, (c) live-update rule toggles without dialog confirms, (d) cache last-picked-instrument across sessions.

Output: `src/viewers/spc-dashboard.ts` (~400-550 LOC TS) + a one-line `src/package.ts` edit replacing Plan 05's `showSpcDashboard` stub with a call to `openSpcDashboard()`. Cumulative SPC test GREEN: 25 (after Plan 05, now including SPC:annotation_dialog_persists_run_meta as a NEW Plan 16-05 GREEN milestone alongside SPC:run_meta_helpers from Plan 16-02) + 4 (this plan) = 29 of 29 NON-Pareto tests. SPC:pareto_descending (Pareto, owned by Plan 16-07) brings the phase total to 30. The Pareto-specific `SPC:pareto_descending` test stays RED until Plan 07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-sample-level-spc-tracking/16-CONTEXT.md
@.planning/phases/16-sample-level-spc-tracking/16-RESEARCH.md
@.planning/phases/16-sample-level-spc-tracking/16-UI-SPEC.md
@packages/Proteomics/CLAUDE.md
@packages/Proteomics/src/viewers/qc-dashboard.ts
@packages/Proteomics/src/viewers/enrichment-viewers.ts
@packages/Proteomics/src/publishing/post-open-recovery.ts
@packages/Proteomics/src/analysis/spc.ts
@packages/Proteomics/src/analysis/spc-storage.ts
@packages/Proteomics/src/package.ts
@packages/Proteomics/src/analysis/subcellular-location.ts
</context>

<interfaces>
<!-- Public exports + DG API surfaces the executor composes. -->

```
// Public exports for src/viewers/spc-dashboard.ts
export async function openSpcDashboard(): Promise<void>;
export function createSpcChartPanel(
  runsDf: DG.DataFrame,
  baseline: LockedBaseline | null,
  metric: 'median_intensity' | 'missing_pct' | 'control_corr' | 'protein_count',
): {iChart: DG.Viewer; mrChart: DG.Viewer};
export function showDefineBaselineDialog(
  instrumentId: string,
  runs: SpcRunRow[],
  onLocked: (baseline: LockedBaseline) => void | Promise<void>,
  existing?: LockedBaseline,         // when provided, modal is in "Rebuild baseline" mode (pre-populated)
): void;
```

From `src/analysis/spc-storage.ts` (Plan 03):
- `loadRuns(instrumentId?)`, `loadBaseline(instrumentId)`, `saveBaseline(instrumentId, baseline)`
- `iterativeOutlierRemoval(series, maxIter)`, `computeBaselineStats(retainedRows)`
- `RUNS_CSV_COLUMNS`, `SpcRunRow`, `LockedBaseline`

From `src/analysis/spc.ts` (Plan 02):
- `evaluateNelsonRules`, `WHEELER_D4`, `SpcRulesEnabled`, `BaselineSnapshot`

From `datagrok-api/grok` + `dg` + `ui`:
- `grok.shell.addTableView(df): DG.TableView` — sibling TableView for runs df.
- `tv.dockManager.dock(element, DG.DOCK_TYPE.{LEFT,RIGHT,DOWN,UP}, parentNode | null, title, ratio): DG.DockNode`
- `DG.Viewer.lineChart(df, options): DG.Viewer` + `viewer.meta.formulaLines.addLine({formula, color, width, style?, visible, title})`
- `DG.Viewer.barChart(df, options)` — used by Plan 07, NOT this plan.
- `ui.dialog(title).add(...).onOK(...).onCancel(...).show()`
- `ui.input.bool(name, {value, tooltipText})`, `ui.input.choice(name, {value, items, tooltipText})`
- `ui.divV([...elts]) / ui.divH([...elts]) / ui.button(label, onClick) / ui.iconFA(name) / ui.h2(...)`
- `grok.dapi.userDataStorage.put(name, data: {[key: string]: string}) / get(name)`
- `grok.dapi.projects.find(id): Promise<DG.Project | null>`
- `viewer.onCurrentRowChanged.subscribe(...)` — for I-chart click drill-down (single-producer subscription per CONTEXT.md — NOT a CampaignSelectionBus).

Spike outcome (from Plan 01 SUMMARY): if `style: 'dashed'` worked on lineChart with datetime X, use it; otherwise add a sibling constant-value column to runsDf (`UCL_median_intensity`, `LCL_median_intensity`) and plot it as a second yColumn.

UI-SPEC copy (LOAD-BEARING — every reviewer-facing string verbatim):

Empty-state banner:
- Heading: `No baseline locked for <instrument_id>.`
- Body: `Pick the runs that represent normal operation, then click Lock baseline. The dashboard turns on once a baseline exists.`
- CTA: `Define baseline...`

No-runs banner:
- Heading: `No SPC runs recorded yet.`
- Body: `Open an analyzed file, then run Analyze → Compute SPC Status. The dashboard turns on once at least 4 runs have been computed.`

Drill-down toasts:
- Source missing: `Source for '<run_label>' is not currently available. Open the run's analyzed file and re-run Compute SPC Status.`
- Source null: `'<run_label>' was computed without a saved project link. Open the run's analyzed file and re-run Compute SPC Status to capture the link.`

Define-baseline modal:
- Title: `Lock baseline for <instrument_id>` (Define) OR `Rebuild baseline for <instrument_id>` (Rebuild)
- Primary button: `Lock baseline` (Define) OR `Rebuild` (Rebuild)
- Body bullet (Rebuild only, above OK): `This replaces the locked baseline from <prior_locked_date>. The previous baseline is not kept.`

Success toasts:
- `Baseline locked for <instrument_id>: <N_included> runs included, <N_excluded> excluded.`
- `Baseline rebuilt for <instrument_id>: <N_included> runs included, <N_excluded> excluded. Previous baseline replaced.`

False-alarm-rate tooltip rewrite (per rule subset):
- Rule 1 only: `Expected false-alarm rate at this rule set: ~0.27%. (Rule 1 only — a point beyond 3σ.)`
- Rules 1+5 default: `Expected false-alarm rate at this rule set: ~0.4%. (Rules 1 + 5 — a single point beyond 3σ, or 2 of 3 beyond 2σ.)`
- Rules 1+5+2: `Enabling rules 1+5+2 raises the expected false-alarm rate to ~1.1%.`
- All 8: `Expected false-alarm rate at this rule set: ~2.65%. Enabling all 8 rules will flag roughly 1 in 38 normal-operation runs.`

userDataStorage key: `'spc.last_picked_instrument'` (Phase 13 D-02 convention).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement openSpcDashboard + createSpcChartPanel + sidebar + drill-down handler</name>
  <files>src/viewers/spc-dashboard.ts</files>
  <read_first>
    - src/viewers/qc-dashboard.ts (full file, lines 25-153) — multi-viewer TableView + dockManager + formula-line precedent
    - src/viewers/enrichment-viewers.ts (lines 170-240) — sibling DataFrame TableView pattern
    - src/publishing/post-open-recovery.ts — grok.dapi.projects.find(id).open() precedent for drill-down
    - src/analysis/subcellular-location.ts (lines 240-260) — userDataStorage.get/put precedent
    - .planning/phases/16-sample-level-spc-tracking/16-UI-SPEC.md (full file) — every visible string and dock arrangement
    - .planning/phases/16-sample-level-spc-tracking/16-RESEARCH.md § "Pattern 3" (dockManager) + § "Pattern 4" (formula lines)
    - .planning/phases/16-sample-level-spc-tracking/16-01-SUMMARY.md (Plan 01 spike outcome) — dashed/style decision
    - src/tests/spc.ts → tests SPC:dashboard_renders, SPC:formula_lines, SPC:drilldown_resolved, SPC:drilldown_missing
  </read_first>
  <behavior>
    `openSpcDashboard()`:
    1. `const allRuns = await loadRuns();` (no filter → all instruments).
    2. If `allRuns.length === 0` → surface a no-runs banner (open a small ui.dialog with the no-runs copy, or grok.shell.warning, per the simpler path: warning toast with verbatim copy then return).
    3. Build unique instrument list: `const instruments = Array.from(new Set(allRuns.map(r => r.instrument_id)));`.
    4. Read last-picked instrument: `const last = (await grok.dapi.userDataStorage.get('spc.last_picked_instrument'))?.['instrument_id'] ?? instruments[0];`.
    5. Open instrument picker dialog (`ui.dialog('SPC Dashboard').add(ui.input.choice('Instrument', {value: last, items: instruments, tooltipText: 'Switches the trend charts and baseline to a different instrument. Only instruments with computed SPC runs are listed.'}))).onOK(async (value) => ...).show();`. The choice items are formatted per UI-SPEC: `"<instrument_id> (baseline locked)"` if `baseline-<slug>.json` exists, else `"<instrument_id> (no baseline)"` — pre-compute via `await Promise.all(instruments.map(i => loadBaseline(i)))`.
    6. On OK:
        a. `await grok.dapi.userDataStorage.put('spc.last_picked_instrument', {instrument_id: pickedInstrument});`
        b. `const slice = await loadRuns(pickedInstrument);` (already sorted by acquisition_datetime ASCENDING).
        c. Build a DG.DataFrame from `slice` via `DG.DataFrame.fromObjects(slice)` (numeric coercion done inside spc-storage). Set `df.name = \`SPC — \${pickedInstrument}\``.
        d. For the MR-chart, compute per-metric `abs(diff(metric))` columns and append to the df via `df.columns.addNewFloat('MR_median_intensity').init(i => i === 0 ? DG.FLOAT_NULL : Math.abs(metric[i] - metric[i-1]))` etc. (4 new columns: `MR_median_intensity`, `MR_missing_pct`, `MR_control_corr`, `MR_protein_count`).
        e. `const tv = grok.shell.addTableView(df);` — SIBLING TableView. Do NOT dock viewers onto the calling protein-df view.
        f. `const baseline = await loadBaseline(pickedInstrument);`
        g. Build the sidebar element (see step "Sidebar build" below).
        h. Dock sidebar: `const sidebarNode = tv.dockManager.dock(sidebarElement, DG.DOCK_TYPE.LEFT, null, 'SPC Controls', 0.25);`
        i. If `baseline === null`: dock the empty-state banner element into the right pane (a single ui.divV with the UI-SPEC empty-state heading/body/CTA). The CTA's onClick opens `showDefineBaselineDialog(pickedInstrument, slice, async (newBaseline) => { await saveBaseline(pickedInstrument, newBaseline); /* refresh in place */ })`.
        j. Else: render 4 I-charts + 4 MR-charts via `createSpcChartPanel(df, baseline, metric)` for each metric, docked per UI-SPEC §'Docking rules' (I-chart row TOP, MR-chart row BELOW that I-chart, repeated for 4 metrics; arrangement in 2×2 macro-grid per UI-SPEC ASCII).
        k. Wire drill-down: subscribe to `tv.dataFrame.onCurrentRowChanged` (single-producer subscription per CONTEXT.md anti-pattern note — NO CampaignSelectionBus). Handler reads current row, branches on status + source_project_id:
            - `if (status !== 'flagged' && status !== 'out_of_control') return;` (no drill-down for pass rows)
            - `if (source_project_id === null) { grok.shell.warning(\`'\${run_label}' was computed without a saved project link. Open the run's analyzed file and re-run Compute SPC Status to capture the link.\`); return; }`
            - `const proj = await grok.dapi.projects.find(source_project_id); if (!proj) { grok.shell.warning(\`Source for '\${run_label}' is not currently available. Open the run's analyzed file and re-run Compute SPC Status.\`); return; } await proj.open();`

    `createSpcChartPanel(runsDf, baseline, metric)`:
    1. Build I-chart: `const iChart = DG.Viewer.lineChart(runsDf, {xColumnName: 'acquisition_datetime', yColumnName: metric, title: <human-readable label per UI-SPEC>});`
    2. If `baseline !== null` AND `!isNaN(baseline.metrics[metric].mean)` AND `!isNaN(baseline.metrics[metric].sd)`:
        - Add 3 formula lines per Plan 01 spike outcome. If spike PASSED, use `style: 'dashed'` for UCL/LCL and solid for CL. If spike FAILED, use the fallback: add 3 constant-value columns to `runsDf` (`~UCL_<metric>`, `~CL_<metric>`, `~LCL_<metric>`) and plot them as additional yColumns on the same lineChart (read RESEARCH.md Assumption A1 fallback).
        - UCL formula: `\${\${metric}} = \${baseline.metrics[metric].mean + 3 * baseline.metrics[metric].sd}`
        - CL formula:  `\${\${metric}} = \${baseline.metrics[metric].mean}`
        - LCL formula: `\${\${metric}} = \${baseline.metrics[metric].mean - 3 * baseline.metrics[metric].sd}`
        - Colors from UI-SPEC §'Color': CL `#888888`, UCL/LCL `#666666`. Width 1.
    3. Build MR-chart: `DG.Viewer.lineChart(runsDf, {xColumnName: 'acquisition_datetime', yColumnName: \`MR_\${metric}\`, title: <label>});`
    4. If baseline non-null: add 2 formula lines to MR-chart:
        - UCL = `WHEELER_D4 × mean(MR_<metric>)` — compute mean from the slice's MR column non-null values.
        - CL = `mean(MR_<metric>)`
        - LCL = 0 is the X-axis itself; do NOT draw an explicit LCL on MR-chart per UI-SPEC §'Formula lines on each MR-chart'.
    5. Return `{iChart, mrChart}`.

    Sidebar build (inside openSpcDashboard step "g"):
    - Top: instrument picker mirroring the dialog's picker (live-switch — but per UI-SPEC the picker is in the dialog; in the dashboard sidebar, a smaller readout of the current instrument is fine — `ui.divV([ui.h2(pickedInstrument)])`).
    - Middle: baseline status block (per UI-SPEC §'Sidebar — baseline status block'). If baseline is non-null: `Baseline locked: <baseline.locked_at> / <N_included> runs included · <N_excluded> runs excluded (3σ outlier) / [Rebuild baseline...]`. If baseline is null: empty-state banner with [Define baseline...] CTA.
    - Bottom: per-metric Nelson rule toggle grid (4 metrics × 8 rules). Use `ui.input.bool` for each cell. On any toggle change:
        a. Update the in-memory `baseline.rules_enabled[metric][rule]` immediately.
        b. `await saveBaseline(pickedInstrument, baseline);` (persist immediately — no separate save button per CONTEXT.md D-04).
        c. Re-evaluate Nelson rules for every row in the slice: for each row, build the prior-series, call `evaluateNelsonRules(...)`, update row's `status` cell + `rules_tripped` cell IN PLACE on the df. The chart point colors update automatically because of df binding.
        d. Update the false-alarm-rate disclosure tooltip text per UI-SPEC §'False-alarm-rate disclosure'. Compute the disclosure dynamically: sum the published false-alarm rates per Nelson rule (rule 1 = 0.27%, rule 2 = 0.4%, rule 3 = ~0.5%, etc. — per Nelson 1984; the canonical values are documented in RESEARCH.md). For Phase 16, hard-code the lookup table for the 5 canonical subsets enumerated in UI-SPEC (rule 1 only, 1+5, 1+5+2, all 8, and the generic "more rules → higher rate" fallback for any other subset).
    - Button: [Rebuild baseline...] (only when baseline non-null) — opens `showDefineBaselineDialog(pickedInstrument, slice, ..., baseline /* existing → Rebuild mode */)`.

    Tooltips: every input gets the UI-SPEC tooltip (verbatim copy). Same banned-jargon rule applies.
  </behavior>
  <action>
    Create `src/viewers/spc-dashboard.ts` (new file). Imports at top: `import * as grok from 'datagrok-api/grok'; import * as ui from 'datagrok-api/ui'; import * as DG from 'datagrok-api/dg';` + named imports from `'../analysis/spc'` and `'../analysis/spc-storage'`. No barrel re-exports; named exports only. 2-space indent, single quotes, semicolons, CRLF.

    File structure:
    1. Const lookup tables: `const METRIC_LABELS = {median_intensity: 'Median log2 intensity', missing_pct: 'Missing %', control_corr: 'Control replicate correlation', protein_count: 'Protein count'};` and `const FALSE_ALARM_DISCLOSURE: Record<string, string> = {...};` (the 5 canonical subsets from UI-SPEC).
    2. `createSpcChartPanel` (pure factory — no shell side effects).
    3. `showDefineBaselineDialog` (Task 2 owns this; for Task 1 emit a stub that throws "Implemented in 16-06 task 2" — actually NO, both modals are owned by THIS plan; just split the work across tasks for clarity).
    4. `openSpcDashboard` (orchestrator).
    5. Private helpers: `buildSidebar`, `wireDrillDown`, `formatChoiceItems`, `recomputeStatusesInPlace`.

    Per memory `feedback_dg_column_bulk_init`, use `col.init(i => ...)` over per-row `col.set(i, ...)` for the MR-chart difference columns (255× speedup on 8k-row volcano benchmark — applies even at SPC's ~104 rows/year).

    Per memory `feedback_dg_column_init_null_sentinel`, when first MR row should be null (no prior to diff from), use `col.set(0, null)` AFTER `col.init` (not `init(i => i === 0 ? null : ...)` which leaves the FLOAT_NULL sentinel unsynced).

    Per CONTEXT.md anti-pattern note + RESEARCH.md "Anti-Patterns to Avoid": drill-down uses `tv.dataFrame.onCurrentRowChanged.subscribe(...)` (single-producer, single-consumer); explicit unsubscribe on `tv.onClosed.subscribe(() => sub.unsubscribe())` or via `DG.Subscriber` — do NOT build a CampaignSelectionBus.

    Per memory `feedback_keep_workaround_capture_future`: userDataStorage is soft-deprecated but matches v1.3 precedent. Add an inline comment `// userDataStorage is soft-deprecated; matches v1.3 codebase (subcellular-location.ts:246). Migrate to grok.userSettings as cross-cutting cleanup in v1.5.` next to the put/get calls.

    No fenced code in this action — signatures come from interfaces block. Length target: ~250-350 LOC for Task 1 (createSpcChartPanel + openSpcDashboard + helpers; modal is Task 2).
  </action>
  <verify>
    <automated>cd packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --host localhost --category "SPC" --test "SPC:dashboard_renders|SPC:formula_lines|SPC:drilldown_resolved|SPC:drilldown_missing"</automated>
  </verify>
  <acceptance_criteria>
    - `src/viewers/spc-dashboard.ts` exists. `npm run build` exit 0.
    - `grep -c "export async function openSpcDashboard" src/viewers/spc-dashboard.ts` returns 1.
    - `grep -c "export function createSpcChartPanel" src/viewers/spc-dashboard.ts` returns 1.
    - `grep -c "grok.shell.addTableView" src/viewers/spc-dashboard.ts` returns 1 (sibling TableView, exactly one call per dashboard open).
    - `grep -c "grok.dapi.userDataStorage" src/viewers/spc-dashboard.ts` ≥ 2 (get + put).
    - `grep -c "grok.dapi.projects.find" src/viewers/spc-dashboard.ts` returns 1 (drill-down).
    - `grep -c "CampaignSelectionBus" src/viewers/spc-dashboard.ts` returns 0 (Phase 18 territory only — anti-pattern guard).
    - `grep -c "formulaLines.addLine" src/viewers/spc-dashboard.ts` ≥ 3 (UCL + CL + LCL on at least one I-chart).
    - `grep -c "WHEELER_D4" src/viewers/spc-dashboard.ts` returns ≥ 1 (MR-chart UCL).
    - Banned-jargon grep: `grep -E "DataFrame|semType|viewer factory|dispatcher" src/viewers/spc-dashboard.ts | grep -v "^//" | grep -v "JSDoc"` shows ZERO occurrences in code strings (comments allowed).
    - The 4 tests in `<verify>` GREEN. Cumulative SPC GREEN: 25 (after Plan 05, now including SPC:annotation_dialog_persists_run_meta) + 4 (this task) = 29 of 29 NON-Pareto tests. `SPC:pareto_descending` remains RED until Plan 16-07 — phase total = 30 tests.
    - Drill-down test `SPC:drilldown_missing` exercises BOTH the null-id and the find()-returns-null branches (two distinct warning strings).
  </acceptance_criteria>
  <done>Dashboard renders 4 I-charts + 4 MR-charts; drill-down resolves OR surfaces correct toast; rule toggles persist + re-evaluate live; 4 RED tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement showDefineBaselineDialog (Define + Rebuild modes)</name>
  <files>src/viewers/spc-dashboard.ts</files>
  <read_first>
    - src/viewers/spc-dashboard.ts (Task 1 output) — to extend
    - .planning/phases/16-sample-level-spc-tracking/16-UI-SPEC.md § "Define-baseline modal layout" (ASCII art) + § "Destructive confirmations" + § "Per-rule checkbox tooltip"
    - .planning/phases/16-sample-level-spc-tracking/16-CONTEXT.md § D-04 (modal flow + rebuild OVERWRITES)
    - src/analysis/spc-storage.ts → iterativeOutlierRemoval, computeBaselineStats, saveBaseline
    - src/tests/spc.ts → tests SPC:baseline_outlier_removal, SPC:baseline_iteration_cap, SPC:baseline_roundtrip, SPC:rule_toggle_per_instrument
  </read_first>
  <behavior>
    `showDefineBaselineDialog(instrumentId, runs, onLocked, existing?)`:
    1. Mode = `existing ? 'rebuild' : 'define'`.
    2. Title: `Lock baseline for <instrumentId>` (define) OR `Rebuild baseline for <instrumentId>` (rebuild).
    3. Body content:
        a. Heading line: `Pick the runs that represent normal operation:` (define) OR show the destructive-confirmation bullet first (rebuild): `This replaces the locked baseline from <existing.locked_at>. The previous baseline is not kept.`
        b. Scrollable checkbox list of `runs` (ordered by acquisition_datetime ASC). Each row: `[checkbox]  <acquisition_datetime-formatted-as-date>  <run_label>`. Defaults:
            - Define: first 7 checked (or all if fewer than 7); rest unchecked.
            - Rebuild: existing.included_run_ids checked; rest unchecked.
        c. `[checkbox] Iterate 3σ outlier removal (recommended)` — default ON (define) OR `existing.iteration_trace.length > 0` (rebuild).
        d. Per-metric Nelson rule toggle grid (4 metrics × 8 rules, ASCII per UI-SPEC). Tooltips per rule from UI-SPEC §'Per-rule checkbox tooltip'.
        e. Live false-alarm-rate disclosure line: `Expected false-alarm rate: ~<X>%.` — re-computed as the user toggles rules (use the same FALSE_ALARM_DISCLOSURE lookup from Task 1).
    4. Buttons: `[Cancel] [Lock baseline]` (define) OR `[Cancel] [Rebuild]` (rebuild).
    5. On OK:
        a. Collect checked run_ids → checked_runs.
        b. If iterate toggle ON: for each of 4 metrics, run `iterativeOutlierRemoval(checked_runs.map(r => ({run_id: r.run_id, value: r[metric]})), 2)`. UNION the excluded_run_ids across the 4 metrics → final excluded set. (Per CONTEXT.md D-04 the policy is to exclude a run if ANY metric flags it as a 3σ outlier; this is conservative but produces the cleanest baseline.) Build the iteration_trace from the per-metric traces (annotate each entry with the metric that drove the exclusion).
        c. Retained = checked - excluded. If retained.length < 3: surface `grok.shell.warning('At least 3 runs must remain after outlier removal — too few to compute a baseline.')` and abort.
        d. `const metricsSnapshot = computeBaselineStats(retained);`
        e. Build the rules_enabled object from the toggle grid.
        f. Build the `LockedBaseline` object: `{instrument_id: instrumentId, locked_at: new Date().toISOString(), included_run_ids: retained.map(r => r.run_id), excluded_run_ids: [...exclusionUnion], iteration_trace: [...traceUnion], metrics: metricsSnapshot, rules_enabled: rulesEnabledFromGrid}`.
        g. Call `onLocked(baseline)` (caller in Task 1 handles persistence + chart refresh).
        h. Toast: define → `grok.shell.info(\`Baseline locked for \${instrumentId}: \${included.length} runs included, \${excluded.length} excluded.\`)`; rebuild → `Baseline rebuilt for \${instrumentId}: ... Previous baseline replaced.`
    6. On Cancel: no state change; dialog closes.

    Modal width: platform default (do NOT override per UI-SPEC §'Width').
  </behavior>
  <action>
    Use Edit (not Write) on `src/viewers/spc-dashboard.ts` from Task 1. Add `showDefineBaselineDialog` as a named export.

    Build the dialog body with `ui.divV(...)` containing:
    - The scrollable run-checkbox list as a `ui.divV` with `style.maxHeight = '300px'; style.overflowY = 'auto';` (only inline style allowed per UI-SPEC; no custom CSS files).
    - Each checkbox row as a `ui.divH([checkbox, ui.divText(dateFormatted), ui.divText(run_label)])`.
    - Iterate-toggle as `ui.input.bool(...)` with UI-SPEC tooltip.
    - Rule toggle grid as a 5-row table (header + 4 metrics): `ui.divV([ui.divH([ui.divText(''), ui.divText('R1'), ...ui.divText('R8')]), ...4 metric rows])`. Each rule checkbox carries the per-rule tooltip from UI-SPEC.
    - False-alarm-rate disclosure text in a `ui.divText(...)` element whose `textContent` is re-assigned on every rule toggle change.

    Wire the live false-alarm-rate update: subscribe to each rule checkbox's `.onChanged` event (DG `Observable` pattern); on change recompute the disclosure string from FALSE_ALARM_DISCLOSURE lookup and rewrite the textContent.

    On dialog OK, run the per-metric iterativeOutlierRemoval + UNION-based exclusion as described in `<behavior>` step 5b. Build LockedBaseline. Call `onLocked(baseline)` — the caller in Task 1's openSpcDashboard handles `await saveBaseline(instrumentId, baseline)` + chart refresh (refresh path: re-call `createSpcChartPanel` for each metric, replace existing docked nodes via `tv.dockManager.dock(...)` or `viewer.dataFrame = df` to re-bind).

    No fenced code in this action.
  </action>
  <verify>
    <automated>cd packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --host localhost --category "SPC" --test "SPC:baseline_outlier_removal|SPC:baseline_iteration_cap|SPC:baseline_roundtrip|SPC:baseline_rebuild_overwrites|SPC:rule_toggle_per_instrument"</automated>
    <human-check>Manual flow:
      1. Open Datagrok → import a Spectronaut PG export → Annotate Experiment (confirm seed prefill) → Compute SPC Status → repeat for 8 runs spanning 8 dates.
      2. Visualize → SPC Dashboard... → pick the instrument from the picker.
      3. Confirm empty-state banner appears with [Define baseline...] CTA.
      4. Click [Define baseline...] → confirm modal shows 8 run checkboxes (first 7 checked), iterate-toggle ON, rule grid with R1+R5 ON for every metric, false-alarm-rate disclosure says ~0.4%.
      5. Toggle R2 ON for median_intensity → confirm disclosure updates live.
      6. Click [Lock baseline] → confirm baseline-<instrument>.json now exists in AppData → confirm dashboard re-renders with 4 I-charts + 4 MR-charts + formula lines visible.
      7. Click a flagged point (if any) → confirm drill-down toast OR project opens.
      8. Sidebar → toggle R3 ON for missing_pct → confirm I-chart point colors update in place AND baseline-<instrument>.json's rules_enabled updates immediately.
      9. Click [Rebuild baseline...] → confirm pre-populated modal opens with current included_run_ids checked.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function showDefineBaselineDialog" src/viewers/spc-dashboard.ts` returns 1.
    - `grep -c "Lock baseline for" src/viewers/spc-dashboard.ts` ≥ 1 (UI-SPEC title verbatim).
    - `grep -c "Rebuild baseline for" src/viewers/spc-dashboard.ts` ≥ 1.
    - `grep -c "This replaces the locked baseline from" src/viewers/spc-dashboard.ts` returns 1 (rebuild bullet verbatim).
    - `grep -c "Iterate 3σ outlier removal" src/viewers/spc-dashboard.ts` returns 1.
    - `grep -c "Baseline locked for" src/viewers/spc-dashboard.ts` ≥ 1.
    - `grep -c "Baseline rebuilt for" src/viewers/spc-dashboard.ts` ≥ 1.
    - `grep -c "Nelson rule 1: a single point beyond 3σ" src/viewers/spc-dashboard.ts` returns 1 (per-rule tooltip verbatim).
    - `grep -c "Nelson rule 5: 2 out of 3 consecutive points" src/viewers/spc-dashboard.ts` returns 1.
    - Banned-jargon grep clean (same rule as Task 1).
    - 5 tests in `<verify>` GREEN (note: SPC:baseline_outlier_removal, SPC:baseline_iteration_cap, SPC:baseline_roundtrip, SPC:baseline_rebuild_overwrites, SPC:rule_toggle_per_instrument were already passing after Plan 03 since they test the math/I-O layer; this task verifies the integrated dialog path doesn't regress them).
    - Manual end-to-end: 8-run dataset → Define baseline → confirm dashboard renders with formula lines → rule toggle live-updates persist → Rebuild pre-populates.
  </acceptance_criteria>
  <done>Define + Rebuild modes both functional; UI-SPEC copy verbatim; live false-alarm-rate disclosure works; rule toggles persist to baseline JSON immediately; manual end-to-end demo passes.</done>
</task>

<task type="auto">
  <name>Task 3: Replace src/package.ts SPC Dashboard stub with a call to openSpcDashboard</name>
  <files>src/package.ts</files>
  <read_first>
    - src/package.ts (the `showSpcDashboard` handler stub registered by Plan 05)
    - src/viewers/spc-dashboard.ts (Task 1+2 output) — confirm `openSpcDashboard` is exported
  </read_first>
  <action>
    Use Edit on `src/package.ts`. Add the import `import {openSpcDashboard} from './viewers/spc-dashboard';` at the top alongside other viewer imports. Replace the body of the `showSpcDashboard` handler (the one with the `'SPC Dashboard wiring lands in plan 16-06 ...'` info toast from Plan 05) with `await openSpcDashboard();`. Preserve the `@grok.decorators.func({'top-menu': 'Proteomics | Visualize | SPC Dashboard...'})` decorator.

    No fenced code in this action.
  </action>
  <verify>
    <automated>cd packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok check --soft</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "openSpcDashboard" src/package.ts` ≥ 2 (1 import + 1 call).
    - `grep -c "SPC Dashboard wiring lands in plan 16-06" src/package.ts` returns 0 (stub copy removed).
    - `npm run build` exit 0; `grok check --soft` clean.
    - Manual: click Proteomics → Visualize → SPC Dashboard... → the picker dialog from Task 1 opens (no more stub toast).
  </acceptance_criteria>
  <done>Menu wired to the real dashboard; stub removed; build + grok check clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human verification gate</name>
  <action>Halt execution. Present the `<what-built>` summary and `<how-to-verify>` steps to the operator. Wait for the resume signal before proceeding.</action>
  <what-built>The full Phase 16 P1 surface: Compute SPC Status (Plan 05) + SPC Dashboard + Define-baseline modal + Rebuild + drill-down + live rule toggles. SPC-08 Pareto still deferred to Plan 07.</what-built>
  <how-to-verify>
    Pre-demo dress rehearsal per UI-SPEC §"Audience pin" (biologist-readability audit):
    1. Open Datagrok → import a Spectronaut PG export → confirm parser-side seed prefills Annotate Experiment → OK → Compute SPC Status. Repeat for at least 8 runs across 8 distinct acquisition_datetimes.
    2. Proteomics → Visualize → SPC Dashboard... → confirm:
       - Instrument picker lists the right instruments with "(no baseline)" suffix initially.
       - Empty-state banner copy is biologist-readable (no `DataFrame` / `tag` / `semType` jargon).
       - [Define baseline...] CTA opens the modal with first-7-checked default, iterate-toggle ON, rules 1+5 ON.
       - False-alarm-rate disclosure says ~0.4%.
    3. Click [Lock baseline] → confirm:
       - 4 I-charts + 4 MR-charts render in 2×2 macro-grid.
       - X-axis is `acquisition_datetime`, NOT run_label or insertion order.
       - I-chart formula lines (UCL/CL/LCL) visible per Plan 01 spike outcome (dashed if spike PASSED).
       - I-chart point colors honor status (green/amber/red per UI-SPEC §Color).
       - Sidebar shows baseline-locked badge + per-metric rule toggle grid + [Rebuild baseline...] button.
    4. Toggle R3 ON for median_intensity in the sidebar → confirm:
       - Point colors update IN PLACE (no view re-open).
       - `baseline-<slug>.json` in AppData reflects the rules_enabled change immediately.
       - False-alarm-rate disclosure updates.
    5. Click a flagged point → confirm drill-down toast OR project opens (depending on whether source_project_id is null in your test data).
    6. Click [Rebuild baseline...] → confirm modal opens pre-populated with current included_run_ids + current rules_enabled.
    7. Banned-jargon audit: read every visible string in the dashboard, modal, banner, drill-down toast. Flag ANY occurrence of `DataFrame`, `tag`, `semType`, `viewer factory`, `ACL`, `helper`, `dispatcher`, `mutate`. Per UI-SPEC §"Banned wording".
  </how-to-verify>
  <resume-signal>Type "approved" if the full surface renders correctly AND no banned jargon is visible AND the rule-toggle live update works. Otherwise list which step failed + screenshot.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drill-down `source_project_id` → `grok.dapi.projects.find(id)` | The caller's ACL is enforced by the platform — if the user lacks read access, `find` returns null and the fallback toast surfaces |
| Rule toggle change → `saveBaseline` overwrite | Single-user write per CONTEXT.md D-02 |
| userDataStorage write of last_picked_instrument | Per-user storage; no cross-user disclosure risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | instrument_id propagation to slugifyInstrumentId (rule toggle path → saveBaseline) | mitigate | Inherited from Plan 03. |
| T-16-02 | Tampering | LockedBaseline JSON parsed on dashboard open | mitigate | Plan 03's loadBaseline wraps JSON.parse in try/catch. The viewer surfaces a `grok.shell.warning('Baseline file for <instrument_id> appears corrupt — please rebuild the baseline.')` if loadBaseline returns null AND the file exists. Differentiate from "no baseline yet" by checking `grok.dapi.files.exists` separately. |
| T-16-04 | Tampering | Concurrent rule-toggle writes from two browser tabs | accept | Single-writer assumption per CONTEXT.md D-02. The rule-toggle handler does a full read-modify-write per toggle; rapid toggling at most loses one transitional state, never corrupts the file. Documented; v1.5 deferred for file lock. |
| T-16-V4-ACL | Information Disclosure | Drill-down opens project the calling user lacks read access to | mitigate | `grok.dapi.projects.find(id)` returns null when ACL denies access; the fallback toast surfaces gracefully. Per RESEARCH.md §Security Domain V4. |
</threat_model>

<verification>
- All SPC-tagged tests except `SPC:pareto_descending` GREEN.
- Manual dress-rehearsal: 8-run dataset → end-to-end → biologist-readable surface confirmed.
- No banned jargon surfaces.
- `npm run build` + `grok check --soft` clean.
</verification>

<success_criteria>
A proteomics expert (and a biologist sitting next to them) can: import a run → annotate (auto-prefilled) → compute SPC → see it land in the dashboard → define a baseline → see flagged runs colored → drill into the source project → toggle Nelson rules and see colors update in place. The full P1 surface is shipped.
</success_criteria>

<output>
Create `.planning/phases/16-sample-level-spc-tracking/16-06-SUMMARY.md` when done.
</output>
