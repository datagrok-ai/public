# Backlog

## Future Ideas

- [ ] **Gene symbol lookup and display in context panel** — When a user clicks a protein/gene row, show gene symbol info (name, description, GO terms) in the Datagrok context panel. Related to Phase 4 UniProt annotation work.
- [ ] **Storey q-value as an FDR option in differential expression** — Add π0-aware Storey q-value alongside Benjamini–Hochberg as a selectable FDR method in the DE step. Rationale: better-powered and methodologically consistent with what most proteomics tools (including Spectronaut) use. NOTE: this is a general methodology improvement, **not** a fix for Spectronaut volcano visual reconciliation — Storey is less conservative than BH and would make the plot taller/bushier, not closer to a Spectronaut Candidates plot. Scope and decide on its own merits.
- [ ] **Un-quarantine 3 tests failing on newer Datagrok runtime (initial-release CI)** — Surfaced by the `Proteomics: Build, test: Datagrok latest version` CI job on PR #3878 (datagrok-ai/public). All three pass on `release/1.27.3` (localhost) but fail on dev/latest, so they are runtime-behavior differences, not package regressions. Currently `skipReason`-quarantined to land the initial release green; each needs a focused debug session (reproduce on dev with `grok test --host dev`):
  1. **`Proteomics: 14-02` volcano counter — `volcanoCounterFilterRecompute` + `volcanoCounterEmptyFilter`.** The live counter overlay (`attachCounterOverlay` in `src/viewers/volcano.ts`) does not refresh on `df` filter change on the newer runtime. Verified via probes: `df.onFilterChanged` fires (=2), `sp.root` is stable (`sameRoot=true`), filter `trueCount` is correct (0), yet the overlay text stays at the initial Total. The debounced `recompute` subscription isn't updating the node. Possible **real** product issue (counter wouldn't update when a user filters on the new platform) — confirm in a live attached view, then fix the overlay/subscription, then drop the `skipReason`.
  2. **`SPC` — `SPC:backfill_ordering`.** Three sequential `upsertRun` calls to `grok.dapi.userDataStorage` read back as 2 via `loadRuns` on the newer runtime (write/read consistency / race). Harden the storage layer (`src/analysis/spc-storage.ts`) or the test's await, then drop the `skipReason`.
- [ ] **Write the Datagrok Proteomics help page** — The package README's "See also" links to `https://datagrok.ai/help/domains/bio/proteomics`, which does not exist yet (currently flagged as a TODO in the README). Author the user-facing help/landing page for the Proteomics package — overview, supported imports, the analysis pipeline, and viewers — under the `help/` Docusaurus tree, then restore the README link. Source material: the package README and the research/spec in `~/datagrok/products/proteomics/`.

## CK-omics gap analysis — cluster C (review later)

Source: review of the client's CK-omics tool (~/Downloads/ck/CKomics_tool2.py). Clusters A/B became Phases 13/14; these were deferred as product decisions / low-value, to be reviewed before any v1.4 scoping.

- [ ] **Consolidated standalone HTML report export** — CK-omics ships a single self-contained HTML (volcano + pathway + correlation + methods + summary stats) the client opens offline without Datagrok (`create_consolidated_html`, ~2,000 lines). This is the literal client hand-off format, but it runs against Datagrok's live-workspace model — a "bundle the open viewers into one static HTML" export is a real product decision, not a quick win. Decide whether client offline-deliverable parity justifies it before scoping.
- [ ] **Custom numerator/denominator display labels** — CK-omics lets the analyst override contrast condition names with friendly labels used in figure titles/filenames (`get_display_name`, CKomics_tool2.py:687). Minor UX nicety on the Annotate/DE dialog; bundle with a future dialog-polish pass.
- [ ] **Organism selection (human/mouse/rat/fly) for g:Profiler + UniProt** — CK-omics asks organism up front (`organism_var_tab1`, :371) and passes it to g:Profiler and UniProt. Verify whether Proteomics enrichment is hardcoded to `hsapiens` — if so, non-human datasets (the rat data in ~/Downloads/ck/) silently mis-map. Promote to a phase if the hardcoding is confirmed; otherwise close.

## Tech debt surfaced by Phase 13 code review (pre-existing — not introduced by P13)

Source: 13-REVIEW.md (advisory, non-blocking). These predate Phase 13 or are CK-faithful; tracked for a future hardening pass, not a P13 regression.

- [ ] **DE column rebuild idempotency + FLOAT_NULL-into-float-column pattern** — `copyDEResultsToFrame` / `runDifferentialExpression` rebuild log2FC/p-value columns without an ensureFreshFloat-style remove-if-exists guard, and several `col.init` paths return the double `DG.FLOAT_NULL` sentinel into FLOAT columns (see memory feedback_dg_column_init_null_sentinel). All null-bearing tests currently pass green, so this is latent, not active — schedule a focused debug session to confirm/normalize the null-write idiom across differential-expression.ts and volcano.ts ensureNegLog10Column. (Pre-existing; Phase 13's new normalizeCandidatesSign already uses number[] doubles and is unaffected.)
- [ ] **splitGenesByDirection gene-in-both-directions** — a gene with both a significant-up and a significant-down protein-group row enters both g:Profiler queries. This mirrors CK-omics run_gprofiler_analysis (which also splits significant_data rows by sign), so it is faithful to the locked contract; revisit only if the client wants gene-level (not row-level) directional dedup.
