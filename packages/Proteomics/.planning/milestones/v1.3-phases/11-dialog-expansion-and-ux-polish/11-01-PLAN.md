---
phase: 11-dialog-expansion-and-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/Proteomics/src/analysis/normalization.ts
autonomous: true
requirements: [NORM-03, NORM-04]

must_haves:
  truths:
    - "User opens normalization dialog and sees a method selector with Median Centering, Quantile, and VSN options"
    - "User sees a box plot showing per-sample intensity distributions that updates reactively when method changes"
    - "User sees a yellow/orange inline warning banner when Spectronaut pre-normalized data is detected"
    - "User clicks OK and the selected normalization method is applied to the DataFrame"
  artifacts:
    - path: "packages/Proteomics/src/analysis/normalization.ts"
      provides: "Expanded showNormalizationDialog with method selector, box plot preview, and pre-normalized warning"
      contains: "ui.input.choice"
  key_links:
    - from: "normalization.ts:showNormalizationDialog"
      to: "DG.Viewer.boxPlot"
      via: "reactive preview on method/column change"
      pattern: "DG\\.Viewer\\.boxPlot"
    - from: "normalization.ts:showNormalizationDialog"
      to: "proteomics.preNormalized tag"
      via: "getTag check for inline warning"
      pattern: "proteomics\\.preNormalized"
---

<objective>
Expand the normalization dialog from a single-method (median centering) column picker into a multi-method dialog with reactive box plot distribution preview and Spectronaut pre-normalized data warning.

Purpose: Users need to choose between Median Centering, Quantile, and VSN normalization methods and see how each affects their data distributions before committing.
Output: Expanded `showNormalizationDialog()` in `normalization.ts`
</objective>

<execution_context>
@/Users/edjaeger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edjaeger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dialog-expansion-and-ux-polish/11-CONTEXT.md
@.planning/phases/11-dialog-expansion-and-ux-polish/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/Proteomics/src/analysis/normalization.ts:
```typescript
export function medianNormalize(df: DG.DataFrame, colNames: string[]): void;
export function quantileNormalize(df: DG.DataFrame, colNames: string[]): void;
export async function vsnNormalize(df: DG.DataFrame, colNames: string[]): Promise<void>;
export function showNormalizationDialog(df: DG.DataFrame): void;
```

From packages/Proteomics/src/viewers/qc-computations.ts (for unpivotIntensities):
```typescript
export function unpivotIntensities(df: DG.DataFrame, intensityCols: string[]): DG.DataFrame;
```

From packages/Proteomics/src/utils/proteomics-types.ts:
```typescript
export const SEMTYPE = { INTENSITY: 'Proteomics-Intensity', ... };
```

Existing dialog pattern (normalization.ts:155-180):
- Gets log2 intensity columns by SEMTYPE.INTENSITY + startsWith('log2(')
- Uses ui.input.columns for column selection
- Simple ui.dialog().add().onOK().show() pattern

Proven box plot pattern (qc-dashboard.ts:127-130):
```typescript
const longDf = unpivotIntensities(df, intensityCols);
const boxPlot = DG.Viewer.boxPlot(longDf, {
  valueColumnName: 'Intensity', categoryColumnName: 'Sample',
} as any);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand normalization dialog with method selector, box plot preview, and pre-normalized warning</name>
  <files>packages/Proteomics/src/analysis/normalization.ts</files>
  <action>
Rewrite `showNormalizationDialog()` in `normalization.ts` to include:

1. **NORM-04 -- Pre-normalized warning banner:**
   - Create an inline div with yellow/orange styling: `background:#FFF3CD; border:1px solid #FFEEBA; color:#856404; padding:8px; margin:4px 0; border-radius:4px`
   - Text: "This data may be pre-normalized (Spectronaut). Additional normalization may distort results."
   - Show only if `df.getTag('proteomics.preNormalized') === 'true'`, otherwise `display:none`
   - Warning is non-blocking -- user can still click OK

2. **NORM-03 -- Method selector:**
   - Add `ui.input.choice('Method', { value: 'Median Centering', items: ['Median Centering', 'Quantile', 'VSN'], nullable: false })`
   - Keep existing column picker (`ui.input.columns`) below method selector

3. **NORM-03 -- Reactive box plot preview:**
   - Import `unpivotIntensities` from `../viewers/qc-computations`
   - Create a container div with explicit dimensions: `width:100%; height:250px; border:1px solid #ddd; margin-top:8px`
   - On each method or column change: clear container, clone the DataFrame, run the selected normalization method on the clone (for Median Centering call `medianNormalize`, for Quantile call `quantileNormalize`, for VSN show un-normalized distributions since it requires async R call), convert to long format via `unpivotIntensities()`, create `DG.Viewer.boxPlot(longDf, { valueColumnName: 'Intensity', categoryColumnName: 'Sample' } as any)`, set viewer root style to `width:100%; height:220px`, append to container
   - Subscribe to both `methodInput.onChanged` and `colsInput.onChanged`
   - Call `updatePreview()` once after dialog construction for initial render

4. **Dialog layout order:**
   - warningDiv (if applicable)
   - methodInput
   - colsInput
   - plotContainer

5. **onOK handler:**
   - Read selected method and columns
   - If 'Median Centering': call `medianNormalize(df, selected)`
   - If 'Quantile': call `quantileNormalize(df, selected)`
   - If 'VSN': call `await vsnNormalize(df, selected)` -- make onOK async
   - Show info message: `Normalized ${selected.length} columns (${method})`

6. **Keep existing early-return guard** for `proteomics.normalized` tag at the top of the function.

Anti-patterns to avoid:
- Do NOT modify the original DataFrame during preview -- always clone first
- Do NOT destroy/recreate inputs on method switch -- all inputs exist upfront
- When updating box plot, clear container innerHTML before adding new viewer root
- For VSN preview, show current (un-normalized) distributions since VSN requires async server call
  </action>
  <verify>
    <automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics && npx tsc --noEmit src/analysis/normalization.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    - showNormalizationDialog contains method selector with 3 options (Median Centering, Quantile, VSN)
    - Dialog includes reactive box plot preview that updates on method/column change
    - Yellow/orange warning banner shows when proteomics.preNormalized tag is 'true'
    - onOK dispatches to correct normalization function based on method selection
    - Existing medianNormalize, quantileNormalize, vsnNormalize functions are unchanged
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `cd packages/Proteomics && npx tsc --noEmit`
- Dialog function signature unchanged: `showNormalizationDialog(df: DG.DataFrame): void`
- All three normalization functions still exported and unchanged
- New imports (unpivotIntensities) resolve correctly
</verification>

<success_criteria>
- Normalization dialog has method selector dropdown with Median Centering, Quantile, VSN
- Box plot preview renders per-sample distributions and updates reactively
- Yellow warning banner appears for Spectronaut pre-normalized data
- OK button runs selected normalization method on original DataFrame
</success_criteria>

<output>
After completion, create `.planning/phases/11-dialog-expansion-and-ux-polish/11-01-SUMMARY.md`
</output>
