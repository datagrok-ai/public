---
phase: 12-spectronaut-input-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/spectronaut-aggregate.sql
  - tools/spectronaut-aggregate.sh
  - tools/generate-spectronaut-precursor-fixture.mjs
  - tools/derive-precursor-golden-sidecar.mjs
  - src/tests/spectronaut-parser.ts
  - files/demo/spectronaut-hye-precursor.tsv
  - files/demo/spectronaut-hye-precursor-golden.tsv
  - files/demo/spectronaut-hye-precursor-golden.json
  - files/demo/README.md
autonomous: true
requirements: [R3, R5, R6]
tags: [spectronaut, streaming, proteomics, parser, fixture]

must_haves:
  truths:
    - "The duckdb aggregation script is committed under the package git tree (tools/) with the reference-file-only R.Condition flip documented as such AND the two unused any_value carry-along columns dropped + documented"
    - "A small synthetic precursor-level TSV fixture exists under files/demo/ carrying the D-01 signature columns and exercising every R2 filter branch"
    - "A duckdb-derived golden .tsv sits beside the fixture and is regeneratable via a single documented command"
    - "A JSON sidecar derived FROM the duckdb golden .tsv is committed beside it so the golden test can pin to real duckdb output even when the test runner cannot read committed files"
    - "D-03: synthetic generator — the extended makeLongFormatTsv (and the standalone fixture generator) emits multi-precursor / decoy / sub-threshold / non-numeric / empty-qvalue rows and the existing 19 Spectronaut tests still pass"
  artifacts:
    - path: "tools/spectronaut-aggregate.sql"
      provides: "Committed duckdb aggregation (the D-04 oracle + D-05 manual fallback), flip documented reference-only, PG.Genes/PG.ProteinAccessions SELECT terms dropped + documented"
      contains: "max(TRY_CAST"
    - path: "tools/spectronaut-aggregate.sh"
      provides: "Wrapper that resolves the sibling .sql via dirname and runs duckdb"
      contains: "spectronaut-aggregate.sql"
    - path: "tools/generate-spectronaut-precursor-fixture.mjs"
      provides: "Node generator emitting the synthetic precursor fixture, mirroring generate-spectronaut-candidates-fixture.mjs"
      contains: "files/demo/spectronaut-hye-precursor.tsv"
    - path: "tools/derive-precursor-golden-sidecar.mjs"
      provides: "Node script that parses the committed duckdb golden .tsv and emits the JSON sidecar (no aggregation logic of its own — pure transcription of duckdb output)"
      contains: "spectronaut-hye-precursor-golden.json"
    - path: "files/demo/spectronaut-hye-precursor.tsv"
      provides: "Synthetic precursor-level fixture with EG.ModifiedPeptide/FG.Charge/PEP.StrippedSequence and CondA/CondB"
      min_lines: 30
    - path: "files/demo/spectronaut-hye-precursor-golden.tsv"
      provides: "duckdb-aggregated golden output of the fixture"
      min_lines: 5
    - path: "files/demo/spectronaut-hye-precursor-golden.json"
      provides: "JSON map {`<protein><condition>_<replicate>` -> {quantity, qvalue}} transcribed verbatim from the duckdb golden .tsv — the in-test oracle when committed-file reads are unavailable"
      contains: "quantity"
    - path: "src/tests/spectronaut-parser.ts"
      provides: "Extended makeLongFormatTsv emitting precursor + decoy + edge-qvalue rows"
      contains: "EG.ModifiedPeptide"
    - path: "files/demo/README.md"
      provides: "Documentation rows for the new fixture + golden + sidecar, regen one-liner, flip caveat"
      contains: "spectronaut-hye-precursor"
  key_links:
    - from: "tools/spectronaut-aggregate.sh"
      to: "tools/spectronaut-aggregate.sql"
      via: "dirname-resolved sibling path"
      pattern: "dirname.*spectronaut-aggregate\\.sql"
    - from: "tools/generate-spectronaut-precursor-fixture.mjs"
      to: "files/demo/spectronaut-hye-precursor.tsv"
      via: "fs.writeFileSync into files/demo/"
      pattern: "files/demo/spectronaut-hye-precursor"
    - from: "files/demo/spectronaut-hye-precursor.tsv"
      to: "files/demo/spectronaut-hye-precursor-golden.tsv"
      via: "tools/spectronaut-aggregate.sh run over the fixture"
      pattern: "spectronaut-aggregate\\.sh"
    - from: "files/demo/spectronaut-hye-precursor-golden.tsv"
      to: "files/demo/spectronaut-hye-precursor-golden.json"
      via: "tools/derive-precursor-golden-sidecar.mjs transcribes the golden .tsv into JSON (no re-aggregation)"
      pattern: "derive-precursor-golden-sidecar"
---

<objective>
Land the Wave 0 test infrastructure and dev/CI assets every later wave depends on: relocate the duckdb aggregation script into the package `tools/` directory (D-05), build a Node generator that emits a small synthetic precursor-level Spectronaut fixture (D-03), generate its duckdb golden file (D-04), derive a JSON sidecar transcribed verbatim from that golden so the equivalence test can pin to real duckdb output even when the `grok test` runner cannot read committed files, extend `makeLongFormatTsv` to emit precursor/decoy/edge rows, and document the new fixtures in `files/demo/README.md`.

This plan establishes the equivalence oracle and CI fixture before any streaming code exists. It writes NO production parser/handler code — only tooling, fixtures, the golden + its JSON transcription, the test helper, and docs. Implements R5 (bundled documented duckdb fallback) and R6 (regression fixture); produces the assets R2/R3 verification consumes.

Purpose: The streaming aggregator (Plan 02) and the equivalence tests (Plan 03) cannot be verified without (a) the committed duckdb oracle, (b) a fixture that routes through the new path, (c) a golden file pinned to the real duckdb logic, and (d) a JSON transcription of that golden the in-browser test can read with certainty. Building these first is the Nyquist Wave 0 requirement from 12-VALIDATION.md.
Output: `tools/spectronaut-aggregate.{sql,sh}`, `tools/generate-spectronaut-precursor-fixture.mjs`, `tools/derive-precursor-golden-sidecar.mjs`, `files/demo/spectronaut-hye-precursor.tsv`, `files/demo/spectronaut-hye-precursor-golden.tsv`, `files/demo/spectronaut-hye-precursor-golden.json`, extended `makeLongFormatTsv`, updated `files/demo/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-spectronaut-input-coverage/12-SPEC.md
@.planning/phases/12-spectronaut-input-coverage/12-CONTEXT.md
@.planning/phases/12-spectronaut-input-coverage/12-RESEARCH.md
@.planning/phases/12-spectronaut-input-coverage/12-PATTERNS.md
@.planning/phases/12-spectronaut-input-coverage/12-VALIDATION.md

<interfaces>
<!-- Extracted from codebase — executor needs no exploration. -->

Existing makeLongFormatTsv signature (src/tests/spectronaut-parser.ts:9-42), to be EXTENDED in place:
  function makeLongFormatTsv(opts: {
    proteins: {id: string; organism?: string}[];
    conditions: string[];
    replicates: number[];
    qValues?: Map<string, number | string>;
    ibaqValues?: Map<string, number>;
    fileNameTemplate?: string;
    quantityColumn?: string;            // 'PG.IBAQ' default; 'PG.Quantity' alias
  }): string
  Current header (line 19-22): R.FileName, R.Condition, R.Replicate, PG.ProteinGroups,
    PG.Organisms, <quantityCol>, EG.Qvalue, PEP.StrippedSequence
  Current body: 2 peptide rows per protein×sample (PEPTIDER / ANOTHERPEP), 8-field rows.

tools/ precedent (tools/generate-spectronaut-candidates-fixture.mjs:1-30):
  - Node ESM, shebang #!/usr/bin/env node
  - top doc block ends with a `Run:  node tools/...` line
  - import fs from 'node:fs'; path; fileURLToPath
  - PKG_ROOT = path.resolve(__dirname, '..'); writes into path.join(PKG_ROOT, 'files/demo/...')
  - console.log summary at end

duckdb script source (relocation target, ONE documented edit): /tmp/spectronaut-aggregate.sql,
  /tmp/spectronaut-aggregate.sh. The .sh resolves its SQL via:
    SQL="$(dirname "$0")/spectronaut-aggregate.sql"  (line 16)
  so moving BOTH files into tools/ keeps the wrapper working unchanged.
  NOTE: /tmp/spectronaut-aggregate.sql lines 16-17 SELECT `any_value("PG.Genes")` and
  `any_value("PG.ProteinAccessions")`. The package's Spectronaut demo data
  (files/demo/spectronaut-hye-mix.tsv) does NOT carry those two columns, and
  src/parsers/spectronaut-parser.ts `pivotSpectronaut`/`aggToPivotResult` consume
  ONLY R.Condition / R.Replicate / PG.ProteinGroups / quantity (PG.IBAQ|PG.Quantity)
  / EG.Qvalue / R.FileName / PG.Organisms — never PG.Genes/PG.ProteinAccessions. If
  those two SELECT terms remained, duckdb v1.3.0 raises a Binder Error and exits
  non-zero on the unbound column reference when run over the no-Genes/no-Accessions
  fixture (ignore_errors does NOT cover Binder Errors), so the golden .tsv would
  never be produced and the whole Wave 0 chain (golden → sidecar → 12-03 golden
  test) would have no oracle. They MUST be dropped (see Task 1 action).

duckdb aggregation contract the golden encodes (SQL, after the documented edit):
  - read_csv delim='\t' header=true sample_size=-1 ignore_errors=true nullstr=['','NaN','NA']
  - WHERE (TRY_CAST EG.Qvalue AS DOUBLE IS NULL OR <= 0.01)
          AND PG.ProteinGroups IS NOT NULL
          AND NOT LIKE 'CON\_\_%' AND NOT LIKE 'REV\_\_%' (ESCAPE '\')
  - GROUP BY PG.ProteinGroups, R.Condition, R.Replicate
  - max(TRY_CAST PG.Quantity AS DOUBLE), min(TRY_CAST EG.Qvalue AS DOUBLE)
  - any_value(R.FileName)  [PG.Genes/PG.ProteinAccessions any_value terms DROPPED]
  - R.Condition DMD↔WT CASE flip — REFERENCE-FILE-ONLY, structural no-op on
    CondA/CondB data; KEEP in committed SQL, document loudly.

README precedent block: files/demo/README.md "## spectronaut-hye-candidates.tsv"
  section (lines 76-93) + License lines (95-101) — mirror the table + "Derived" +
  "Regenerate with `node tools/...`" + License row pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the duckdb aggregation script into tools/ with one documented edit (drop unused carry-along columns) and documented reference-only flip</name>
  <files>tools/spectronaut-aggregate.sql, tools/spectronaut-aggregate.sh</files>
  <read_first>
    - /tmp/spectronaut-aggregate.sql (full — source; note lines 16-17 SELECT any_value("PG.Genes")/any_value("PG.ProteinAccessions") that the package's data CANNOT supply and that the parser never consumes)
    - /tmp/spectronaut-aggregate.sh (full — verbatim source; note dirname-resolved SQL path at line 16)
    - src/parsers/spectronaut-parser.ts:28-122 (pivotSpectronaut/aggToPivotResult — confirms only R.Condition/R.Replicate/PG.ProteinGroups/quantity/EG.Qvalue/R.FileName/PG.Organisms are read; PG.Genes/PG.ProteinAccessions are NOT part of the parity contract)
    - files/demo/spectronaut-hye-mix.tsv (header line only — confirm it carries NO PG.Genes/PG.ProteinAccessions, so the fixture in Task 2 cannot either and the /tmp SQL's any_value terms would Binder-Error on it)
    - tools/generate-spectronaut-candidates-fixture.mjs:1-30 (tools/ doc-block + PKG_ROOT precedent)
    - .planning/phases/12-spectronaut-input-coverage/12-RESEARCH.md (Pitfall 1, Open Questions Q2 — the flip is reference-only; same SQL serves as fallback AND oracle because CondA/CondB makes it a no-op; the Q2 RESOLVED note records the any_value drop)
    - .planning/phases/12-spectronaut-input-coverage/12-SPEC.md (R5 acceptance)
  </read_first>
  <action>
    Copy `/tmp/spectronaut-aggregate.sql` to `tools/spectronaut-aggregate.sql` and `/tmp/spectronaut-aggregate.sh` to `tools/spectronaut-aggregate.sh`, byte-preserving the *filter/group/aggregate* logic (read_csv args, WHERE filter, GROUP BY, the `max(TRY_CAST PG.Quantity)` / `min(TRY_CAST EG.Qvalue)` / `any_value(R.FileName)` aggregates) so the committed SQL's parity-relevant behavior is identical to the proven `/tmp` logic per D-05. Keep the executable bit on the `.sh`. The `.sh` already resolves its SQL sibling via `$(dirname "$0")/spectronaut-aggregate.sql` — do not change that line; co-locating both files in `tools/` keeps it working.

    Make exactly ONE documented edit to the SQL: remove ONLY the two SELECT terms `any_value("PG.Genes") AS "PG.Genes"` and `any_value("PG.ProteinAccessions") AS "PG.ProteinAccessions"` (currently `/tmp` SQL lines 16-17). Reason — the package's Spectronaut demo data (`files/demo/spectronaut-hye-mix.tsv`) and the synthetic precursor fixture built in Task 2 do NOT carry `PG.Genes`/`PG.ProteinAccessions` columns, and `src/parsers/spectronaut-parser.ts` (`pivotSpectronaut`/`aggToPivotResult`) never consumes them, so they are NOT part of the D-04 parity contract; left in, duckdb v1.3.0 raises a Binder Error and exits non-zero on the unbound column reference over the fixture (`ignore_errors` does NOT cover Binder Errors), so the golden `.tsv` would never be produced and the entire Wave 0 oracle chain (golden → sidecar → 12-03 golden test) would have no source. Replace the two removed lines with a single SQL comment recording the divergence from `/tmp`, e.g. `-- DIVERGENCE FROM /tmp/spectronaut-aggregate.sql: dropped any_value("PG.Genes")/any_value("PG.ProteinAccessions") — the package's Spectronaut data does not carry these columns and pivotSpectronaut/aggToPivotResult never consume them, so they are not part of the parity contract; keeping them would Binder-Error duckdb on the fixture.` Do NOT touch the `any_value("R.FileName")` term, the WHERE filter, the GROUP BY, the `max`/`min` aggregates, or the read_csv args — only the two named carry-along columns are dropped.

    Strengthen the SQL comment block above the `CASE "R.Condition" WHEN 'DMD' ... WHEN 'WT' ...` flip: prepend a loud `WARNING: REFERENCE-FILE-ONLY` notice stating this DMD↔WT relabel is a one-off correction for the mislabeled `2026-05-13 BP DMD WT.tsv` reference file ONLY, that it is a structural no-op on any non-DMD/WT data (including the CondA/CondB synthetic fixture, which is why this same script doubles as the D-04 golden oracle), and that the streaming TS aggregator must NOT port it (per RESEARCH Pitfall 1, the single highest-risk parity trap). Do not alter the flip's behavior — only the comment.

    Add a top-of-file usage note to the `.sh` doc block mirroring the `tools/generate-spectronaut-candidates-fixture.mjs` doc-block convention: a one-line `# Run: tools/spectronaut-aggregate.sh <input.tsv> [output.tsv]` plus a sentence that this is the documented manual fallback for a Spectronaut report too large for the in-browser streaming importer.
  </action>
  <verify>
    <automated>test -f tools/spectronaut-aggregate.sql && test -f tools/spectronaut-aggregate.sh && grep -q 'spectronaut-aggregate.sql' tools/spectronaut-aggregate.sh && grep -q 'max(TRY_CAST' tools/spectronaut-aggregate.sql && grep -q 'any_value("R.FileName")' tools/spectronaut-aggregate.sql && ! grep -q 'PG.Genes' tools/spectronaut-aggregate.sql && ! grep -q 'PG.ProteinAccessions' tools/spectronaut-aggregate.sql && grep -iq 'DIVERGENCE FROM /tmp' tools/spectronaut-aggregate.sql && grep -iq 'REFERENCE-FILE-ONLY' tools/spectronaut-aggregate.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tools/spectronaut-aggregate.sql` and `tools/spectronaut-aggregate.sh` exist in the package git tree.
    - `tools/spectronaut-aggregate.sql` contains `max(TRY_CAST` and `min(TRY_CAST` aggregates, `any_value("R.FileName")`, and the `WHERE` q-value/CON__/REV__ filter unchanged from `/tmp`.
    - The committed SQL does NOT reference `PG.Genes` or `PG.ProteinAccessions` anywhere (the two `any_value` carry-along SELECT terms were removed) and a SQL comment documents that drop as a deliberate `DIVERGENCE FROM /tmp` with the reason (data does not carry them; parser never consumes them; would Binder-Error the fixture).
    - The DMD↔WT `CASE` flip is present but its comment block contains an explicit `REFERENCE-FILE-ONLY` warning naming the reference file and stating the streaming aggregator must not port it.
    - `tools/spectronaut-aggregate.sh` still resolves the SQL via `$(dirname "$0")/spectronaut-aggregate.sql` (grep finds `spectronaut-aggregate.sql` in the `.sh`).
    - `git status` shows both files as new tracked files under `tools/`.
  </acceptance_criteria>
  <done>Both duckdb files committed under tools/; filter/group/aggregate logic byte-equivalent to /tmp; the two unused PG.Genes/PG.ProteinAccessions any_value carry-along columns dropped with a documented DIVERGENCE comment (so duckdb runs cleanly over the no-Genes fixture); flip retained but documented as reference-file-only; .sh→.sql sibling resolution intact.</done>
</task>

<task type="auto">
  <name>Task 2: Extend makeLongFormatTsv to emit precursor/decoy/edge rows; add the precursor fixture generator</name>
  <files>src/tests/spectronaut-parser.ts, tools/generate-spectronaut-precursor-fixture.mjs</files>
  <read_first>
    - src/tests/spectronaut-parser.ts (full — makeLongFormatTsv L9-42 and all 19 existing tests that consume it)
    - src/parsers/spectronaut-parser.ts:46-87 (pivotSpectronaut filter/dedup semantics the fixture must exercise)
    - tools/generate-spectronaut-candidates-fixture.mjs:1-30 (Node ESM doc-block + PKG_ROOT + files/demo write convention)
    - files/demo/spectronaut-hye-mix.tsv (header line only — confirm it carries NO PG.Genes/PG.ProteinAccessions and uses PG.IBAQ; the fixture must not assume those columns)
    - .planning/phases/12-spectronaut-input-coverage/12-RESEARCH.md (Pitfalls 1, 4, 5; Assumption A1, A6 — fixture invariants)
    - .planning/phases/12-spectronaut-input-coverage/12-PATTERNS.md (makeLongFormatTsv extension block; fixture column shape)
  </read_first>
  <action>
    Extend `makeLongFormatTsv` IN PLACE (do not rename; the 19 existing tests must keep passing): add `EG.ModifiedPeptide`, `FG.Charge`, and `FG.Id` to the header so emitted TSVs carry the D-01 precursor signature (`PEP.StrippedSequence` is already emitted). Emit at least 2 distinct precursor/fragment rows per (protein × condition × replicate) varying `EG.ModifiedPeptide`/`FG.Charge`/`FG.Id` (the current 2 peptide rows can become the precursor rows). Keep the quantity column value CONSTANT per (protein × condition × replicate) so duckdb `max()` equals `pivotSpectronaut`'s first-encountered value (RESEARCH Assumption A1). Keep `PG.Organisms`/`R.FileName` constant within each group so duckdb `any_value()` equals first-non-null (RESEARCH Pitfall 5). Default conditions stay `CondA`/`CondB` (NOT `DMD`/`WT`) so the committed SQL flip is a structural no-op (RESEARCH Pitfall 1). Add NO `PG.Genes`/`PG.ProteinAccessions` columns — the real hye-mix demo lacks them; fixtures/SQL/tests must not assume them. This no-`PG.Genes`/no-`PG.ProteinAccessions` fixture mandate is the reason Task 1 drops the two `any_value("PG.Genes")`/`any_value("PG.ProteinAccessions")` SELECT terms from the committed SQL: the contradiction between the `/tmp` SQL (which selected them) and this fixture (which cannot supply them) is reconciled by Task 1's documented edit so duckdb runs cleanly over this fixture in Task 3.

    Add opts to drive the deliberate filter-branch rows so a single generator call can produce a fixture exercising every R2 branch: a way to inject `CON__`/`REV__`-prefixed protein rows, rows with sub-threshold numeric `EG.Qvalue` (>0.01, must be dropped), rows with non-numeric `EG.Qvalue` (e.g. `Profiled` — must pass), and rows with empty-string `EG.Qvalue` (duckdb `nullstr` → passes). Preserve every existing `makeLongFormatTsv` opts field and default behavior so the existing suite is unaffected; existing tests must observe the same protein/row counts as before (the added columns are extra columns, not extra protein groups).

    Create `tools/generate-spectronaut-precursor-fixture.mjs` mirroring `tools/generate-spectronaut-candidates-fixture.mjs` conventions: ESM `#!/usr/bin/env node` shebang, a doc block ending with `Run:  node tools/generate-spectronaut-precursor-fixture.mjs`, `PKG_ROOT = path.resolve(__dirname, '..')`, write the TSV to `path.join(PKG_ROOT, 'files/demo/spectronaut-hye-precursor.tsv')`, end with a `console.log` row/protein/sample summary. The generator must produce a small (≈ a few dozen proteins, 2 conditions × ≥2 replicates) precursor-level TSV that: carries `EG.ModifiedPeptide`/`FG.Charge`/`PEP.StrippedSequence` (so D-01 header-sniff routes it to the streaming path), uses `CondA`/`CondB`, keeps the quantity column constant per group, carries NO `PG.Genes`/`PG.ProteinAccessions` column, and includes at least one `CON__` row, one `REV__` row, one protein with a >0.01 numeric q-value precursor mixed with a passing precursor, one non-numeric `EG.Qvalue` row, and one empty `EG.Qvalue` row. Reuse/share the row-emission logic with the extended `makeLongFormatTsv` shape conceptually (same column order) so the fixture and the in-test synthetic data are structurally consistent; the `.mjs` is standalone Node (no datagrok-api import) and may inline its own row builder.
  </action>
  <verify>
    <automated>node tools/generate-spectronaut-precursor-fixture.mjs && head -1 files/demo/spectronaut-hye-precursor.tsv | grep -q 'EG.ModifiedPeptide' && head -1 files/demo/spectronaut-hye-precursor.tsv | grep -q 'PEP.StrippedSequence' && ! head -1 files/demo/spectronaut-hye-precursor.tsv | grep -q 'PG.Genes' && grep -q 'CON__' files/demo/spectronaut-hye-precursor.tsv && grep -q 'REV__' files/demo/spectronaut-hye-precursor.tsv && grep -cvP '^#' files/demo/spectronaut-hye-precursor.tsv | awk '{exit ($1>30)?0:1}' && grok test --category "Spectronaut" --host localhost 2>&1 | tee /tmp/12-01-t2.log; node -e 'const s=require("fs").readFileSync("/tmp/12-01-t2.log","utf8");const m=s.match(/[Ff]ailed[:\s]+(\d+)/);if(!m){console.error("no failed-count in grok test output");process.exit(1)}process.exit(m[1]==="0"?0:1)' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `makeLongFormatTsv` header includes `EG.ModifiedPeptide`, `FG.Charge`, `FG.Id`, and still `PEP.StrippedSequence`.
    - `makeLongFormatTsv` emits ≥2 distinct precursor rows per (protein×condition×replicate) with constant quantity within each group.
    - Existing 19 Spectronaut tests still pass: the `<automated>` block runs `grok test --category "Spectronaut" --host localhost` and gates on the runner's machine-readable failed-count == 0 (no protein-count or sample-column regressions).
    - `node tools/generate-spectronaut-precursor-fixture.mjs` runs clean and writes `files/demo/spectronaut-hye-precursor.tsv`.
    - The fixture header carries `EG.ModifiedPeptide` + `PEP.StrippedSequence` (D-01 signature), uses `CondA`/`CondB`, and contains at least one `CON__`, one `REV__`, one sub-threshold-qvalue, one non-numeric-qvalue, and one empty-qvalue row.
    - The fixture contains NO `PG.Genes` or `PG.ProteinAccessions` column (this is why Task 1 drops the matching `any_value` SELECT terms — the contradiction is reconciled in the plan text).
  </acceptance_criteria>
  <done>makeLongFormatTsv extended in place (existing 19 tests green via the failed-count==0 gate), precursor fixture generator created and producing files/demo/spectronaut-hye-precursor.tsv exercising every R2 filter branch with CondA/CondB and group-constant quantity, no PG.Genes/PG.ProteinAccessions columns (matching Task 1's SQL edit).</done>
</task>

<task type="auto">
  <name>Task 3: Generate the duckdb golden file, derive the JSON sidecar from it, and document both new fixtures in README</name>
  <files>files/demo/spectronaut-hye-precursor-golden.tsv, files/demo/spectronaut-hye-precursor-golden.json, tools/derive-precursor-golden-sidecar.mjs, files/demo/README.md</files>
  <read_first>
    - tools/spectronaut-aggregate.sh (from Task 1 — the wrapper; usage `spectronaut-aggregate.sh <in> [out]`)
    - tools/spectronaut-aggregate.sql (from Task 1 — confirms group-by + aggregates the golden encodes; PG.Genes/PG.ProteinAccessions already dropped so duckdb binds cleanly over the no-Genes fixture)
    - files/demo/spectronaut-hye-precursor.tsv (from Task 2 — the input to aggregate; carries NO PG.Genes/PG.ProteinAccessions)
    - tools/generate-spectronaut-candidates-fixture.mjs:1-30 (Node ESM doc-block + PKG_ROOT precedent for the sidecar deriver script)
    - files/demo/README.md (full — the `## spectronaut-hye-candidates.tsv` section L76-93 + License L95-101 are the exact template to mirror)
    - .planning/phases/12-spectronaut-input-coverage/12-RESEARCH.md (Pitfall 1, Open Questions Q2 — flip caveat + the any_value-drop reconciliation; "Runtime State Inventory" — the pair must regenerate together; first-encountered-vs-max divergence note ~L326/L428-429)
    - .planning/phases/12-spectronaut-input-coverage/12-VALIDATION.md (Wave 0 README requirement; R5 manual doc check)
  </read_first>
  <action>
    Run `tools/spectronaut-aggregate.sh files/demo/spectronaut-hye-precursor.tsv files/demo/spectronaut-hye-precursor-golden.tsv` (duckdb v1.3.0 is on the dev machine per RESEARCH Environment Availability). Because Task 1 dropped the two `any_value("PG.Genes")`/`any_value("PG.ProteinAccessions")` SELECT terms, duckdb binds cleanly over the no-Genes fixture and the wrapper exits 0; if it exits non-zero, STOP — the golden was not produced and the rest of this task has no input. Commit the produced `files/demo/spectronaut-hye-precursor-golden.tsv` as the D-04 equivalence oracle — it is the real duckdb output, not a hand-derived approximation. Verify the golden has a header plus one row per surviving (protein × CondA/CondB × replicate) group and that no `CON__`/`REV__` protein appears in it (the WHERE filter dropped them).

    Create `tools/derive-precursor-golden-sidecar.mjs` (mirror the `tools/generate-spectronaut-candidates-fixture.mjs` ESM conventions: `#!/usr/bin/env node` shebang, doc block ending with `Run:  node tools/derive-precursor-golden-sidecar.mjs`, `PKG_ROOT = path.resolve(__dirname, '..')`, `console.log` summary). This script does NO aggregation — it is a pure transcription of the already-committed duckdb golden: read `files/demo/spectronaut-hye-precursor-golden.tsv`, parse its header to locate the `PG.ProteinGroups`, `R.Condition`, `R.Replicate`, quantity (`PG.Quantity` or `PG.IBAQ`, same preference order as the parser), and `EG.Qvalue` columns, and for each golden row emit a key `` `${protein}${condition}_${replicate}` `` → `{quantity: <number>, qvalue: <number>}` (numbers exactly as duckdb wrote them, parsed via `Number(...)`). Write the resulting object to `files/demo/spectronaut-hye-precursor-golden.json` as pretty-printed JSON. The sidecar is DERIVED FROM and committed alongside the duckdb golden — it carries no aggregation logic of its own, so R2 stays pinned to real duckdb output. Run the script and commit the JSON.

    Add to `files/demo/README.md`, mirroring the `## spectronaut-hye-candidates.tsv` table+prose template (L76-93): a `## spectronaut-hye-precursor.tsv` section describing it as a synthetic precursor-level Spectronaut report (precursor/fragment rows per protein×sample, carries the D-01 signature columns, CondA/CondB, deliberate decoy + edge-qvalue rows, NO PG.Genes/PG.ProteinAccessions) with a `Regenerate with `node tools/generate-spectronaut-precursor-fixture.mjs`` line; a `## spectronaut-hye-precursor-golden.tsv` section describing it as the duckdb-derived aggregation oracle with the regen one-liner `tools/spectronaut-aggregate.sh files/demo/spectronaut-hye-precursor.tsv files/demo/spectronaut-hye-precursor-golden.tsv`; and a `## spectronaut-hye-precursor-golden.json` section describing it as the JSON transcription of the golden `.tsv` (consumed by `grok test` when committed-file reads are unavailable in the runner) with the regen one-liner `node tools/derive-precursor-golden-sidecar.mjs` and an explicit note that it is derived FROM the `.tsv`, never re-aggregated. Include a **flip caveat** paragraph: the committed `tools/spectronaut-aggregate.sql` carries a reference-file-only DMD↔WT `R.Condition` correction that is a structural no-op on this `CondA`/`CondB` fixture, which is why the same script serves as both the documented manual fallback and the test oracle; note also that the committed SQL drops the unused `any_value("PG.Genes")`/`any_value("PG.ProteinAccessions")` terms from the `/tmp` original because the package's Spectronaut data does not carry them (a documented divergence that lets duckdb run over the fixture). State the fixture → golden `.tsv` → golden `.json` chain must be regenerated in order (generate fixture, run the aggregate wrapper, run the sidecar deriver) or they silently drift. Append matching `## License` lines: all synthetic/derived → "no restrictions".
  </action>
  <verify>
    <automated>tools/spectronaut-aggregate.sh files/demo/spectronaut-hye-precursor.tsv files/demo/spectronaut-hye-precursor-golden.tsv && test -s files/demo/spectronaut-hye-precursor-golden.tsv && ! grep -qE '(^|\t)(CON__|REV__)' files/demo/spectronaut-hye-precursor-golden.tsv && node tools/derive-precursor-golden-sidecar.mjs && test -s files/demo/spectronaut-hye-precursor-golden.json && node -e 'const g=require("./files/demo/spectronaut-hye-precursor-golden.json");const k=Object.keys(g);if(k.length<1)process.exit(1);const v=g[k[0]];process.exit(typeof v.quantity==="number"&&typeof v.qvalue==="number"?0:1)' && grep -q 'spectronaut-hye-precursor.tsv' files/demo/README.md && grep -q 'spectronaut-hye-precursor-golden.tsv' files/demo/README.md && grep -q 'spectronaut-hye-precursor-golden.json' files/demo/README.md && grep -q 'generate-spectronaut-precursor-fixture.mjs' files/demo/README.md && grep -q 'derive-precursor-golden-sidecar.mjs' files/demo/README.md && grep -iq 'flip\|DMD' files/demo/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tools/spectronaut-aggregate.sh files/demo/spectronaut-hye-precursor.tsv files/demo/spectronaut-hye-precursor-golden.tsv` exits 0 (duckdb binds cleanly because Task 1 dropped the PG.Genes/PG.ProteinAccessions SELECT terms the fixture cannot supply) and the golden `.tsv` is non-empty.
    - `files/demo/spectronaut-hye-precursor-golden.tsv` exists, is the verbatim output of `tools/spectronaut-aggregate.sh` over the fixture, and contains NO `CON__`/`REV__` protein rows.
    - The golden has one row per surviving (protein × condition × replicate) group plus a header.
    - `tools/derive-precursor-golden-sidecar.mjs` exists, performs NO aggregation (pure transcription of the golden `.tsv`), and produces `files/demo/spectronaut-hye-precursor-golden.json` as `{`<protein><cond>_<rep>`: {quantity:number, qvalue:number}}`.
    - The JSON sidecar's values come only from the committed golden `.tsv` (re-running the deriver after Task 1/2 unchanged produces a byte-identical JSON).
    - `files/demo/README.md` contains `## spectronaut-hye-precursor.tsv`, `## spectronaut-hye-precursor-golden.tsv`, and `## spectronaut-hye-precursor-golden.json` sections, each with a regeneration one-liner.
    - README contains the flip caveat (mentions the reference-file-only DMD↔WT correction is a no-op on CondA/CondB) AND notes the documented drop of the unused PG.Genes/PG.ProteinAccessions any_value terms, and states the fixture→golden.tsv→golden.json chain must be regenerated in order.
    - README `## License` block has entries for all three new files marked no-restrictions.
  </acceptance_criteria>
  <done>aggregate wrapper exits 0 over the no-Genes fixture (Task 1 edit verified end-to-end), duckdb golden committed beside the fixture (decoys filtered out), JSON sidecar transcribed verbatim from the golden and committed beside it, README documents all three artifacts with regen one-liners, the flip caveat, and the any_value-drop divergence; license rows added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI machine → committed assets | The fixture + golden + JSON sidecar are generated by dev tooling and committed; consumed only by `grok test` and as docs. No untrusted input crosses here (synthetic data only). |
| `tools/*.sh` → duckdb CLI | The wrapper substitutes file paths into a SQL template and invokes the local duckdb binary. |
| golden `.tsv` → golden `.json` | The sidecar deriver transcribes (does not re-compute) the duckdb output; the JSON cannot diverge from the golden unless the deriver is run against a stale golden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `tools/spectronaut-aggregate.sql` flip ported into TS aggregator; or the unused PG.Genes/PG.ProteinAccessions carry-along terms left in, Binder-Erroring duckdb over the no-Genes fixture and starving the oracle chain | mitigate | The flip's comment is rewritten to a loud `REFERENCE-FILE-ONLY` warning (Task 1); the two `any_value("PG.Genes")`/`any_value("PG.ProteinAccessions")` SELECT terms are removed with a `DIVERGENCE FROM /tmp` comment so duckdb binds cleanly over the fixture (Task 1) and Task 3 gates on the wrapper exiting 0 before asserting golden contents; the fixture uses `CondA`/`CondB` so the flip is a structural no-op and the golden is unaffected (Task 2/3); Plan 03's equivalence test would fail if the flip were ported. |
| T-12-02 | Tampering | fixture↔golden↔sidecar silent drift | mitigate | README documents the three-step regen chain must run in order (Task 3); the sidecar deriver does no aggregation so it cannot mask a fixture change; Plan 03's golden test detects any drift on the next test run. |
| T-12-03 | Information disclosure | client-data licensing in committed fixture | accept | Fixture is fully synthetic (D-03); no client data, no licensing concern — documented as "no restrictions" in README. |
| T-12-04 | Denial of service | committed fixture size bloats the repo | accept | Fixture is bounded to a few dozen proteins × 2 conditions × ≥2 replicates (tens of KB, comparable to the existing 20 KB candidates fixture); the JSON sidecar is smaller still; not a DoS vector. |

CSV/TSV formula-injection (spreadsheet) is out of scope: these TSVs are consumed only by `grok test` (browser DataFrame) and duckdb, never opened in a spreadsheet — noted explicitly per the security threat-model directive.
</threat_model>

<verification>
- `grok test --category "Spectronaut" --host localhost` — all 19 existing tests still green after the makeLongFormatTsv extension, gated on the runner's machine-readable failed-count == 0 (regression gate, in Task 2's `<automated>`).
- `node tools/generate-spectronaut-precursor-fixture.mjs` runs clean; fixture carries the D-01 signature, CondA/CondB, all R2 edge rows, NO PG.Genes/PG.ProteinAccessions.
- `tools/spectronaut-aggregate.sh files/demo/spectronaut-hye-precursor.tsv files/demo/spectronaut-hye-precursor-golden.tsv` exits 0 and produces a non-empty golden; no CON__/REV__ in output.
- `node tools/derive-precursor-golden-sidecar.mjs` transcribes the golden into `files/demo/spectronaut-hye-precursor-golden.json` with numeric quantity/qvalue values.
- `files/demo/README.md` documents all three new files, regen one-liners, the flip caveat, and the any_value-drop divergence.
- `git status` shows the new files (tools .sql/.sh/.mjs ×2, two demo TSVs, one demo JSON) + modified test file + README.
</verification>

<success_criteria>
- R5: duckdb script committed under `tools/` with usage docs; flip documented reference-only; the two unused carry-along columns dropped with a documented divergence.
- R6: synthetic precursor fixture + golden + JSON sidecar committed; regen one-liners documented.
- R3 prerequisite: fixture routes through the D-01 path (carries signature columns) and is shape-compatible with the existing parser.
- Existing 19 Spectronaut tests remain green (no regression from the makeLongFormatTsv extension), enforced by Task 2's failed-count==0 automated gate.
- All Wave 0 asset gaps from 12-VALIDATION.md closed; the Plan-01 internal contradiction (SQL selects columns the fixture lacks) is reconciled in the plan text and verified end-to-end by Task 3's exit-0 gate.
</success_criteria>

<output>
After completion, create `.planning/phases/12-spectronaut-input-coverage/12-01-SUMMARY.md`
</output>
