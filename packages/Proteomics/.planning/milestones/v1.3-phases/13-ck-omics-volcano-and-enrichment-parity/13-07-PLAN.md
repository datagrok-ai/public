---
phase: 13-ck-omics-volcano-and-enrichment-parity
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viewers/enrichment-viewers.ts
  - src/tests/enrichment-visualization.ts
autonomous: true
gap_closure: true
requirements: [R2]
tags: [enrichment, volcano, dockmanager, cross-link, layout]

must_haves:
  truths:
    - "After running Proteomics | Analyze | Enrichment, a volcano of the protein DataFrame is visible on the SAME view as the Up/Down dot+bar charts"
    - "Clicking a term row in the merged enrichment grid (or in a directional dot/bar chart) highlights the matching proteins on a volcano the user can see without switching views"
    - "The original volcano on the protein TableView (if one was already docked there) continues to render the same selection — neither volcano is mutated, both bind to proteinDf and share its selection BitSet"
    - "An automated layout test asserts that, after openEnrichmentVisualization runs against a real (post-addTableView) enrichment view, a ScatterPlotViewer bound to proteinDf is among the dock-manager's viewers on that view"
  artifacts:
    - path: "src/viewers/enrichment-viewers.ts"
      provides: "co-located volcano in openEnrichmentVisualization"
      contains: "DG.Viewer.scatterPlot(proteinDf"
    - path: "src/tests/enrichment-visualization.ts"
      provides: "layout regression test for cross-link visibility"
      contains: "openEnrichmentVisualization"
  key_links:
    - from: "src/viewers/enrichment-viewers.ts openEnrichmentVisualization"
      to: "tv.dockManager.dock(volcano, ...)"
      via: "a ScatterPlotViewer constructed against proteinDf and docked onto the enrichment TableView"
      pattern: "DG.Viewer.scatterPlot\\(proteinDf"
    - from: "wireEnrichmentToVolcano subscriptions"
      to: "proteinDf.selection (shared BitSet)"
      via: "unchanged — co-docked volcano renders the same selection state as the protein-view volcano"
      pattern: "proteinDf\\.selection\\.fireChanged"
---

<objective>
Close Gap 1 from 13-UAT.md: the enrichment cross-link is firing correctly on every term-row click (proteinDf.selection.clearAll → set matching rows → fireChanged) but the highlight renders on the backgrounded protein TableView, which is invisible to the user. Co-dock a volcano bound to the protein DataFrame onto the enrichment TableView so the highlight lands next to the dot/bar charts the user just clicked. The data-layer wiring is untouched; only the visible layout changes.

Purpose: make the existing Phase-9 cross-link perceptible without altering its contract. Layout option (B) from the UAT diagnosis — co-dock proteinDf-bound volcano onto the enrichment view — is the minimal-risk choice because (a) the directional charts already deserve the larger view real estate, (b) the protein TableView is untouched (no regression risk for users coming from Show All Visualizations or the Visualize | Volcano menu), (c) wireEnrichmentToVolcano keeps its single-responsibility selection-mutation contract.

Output: openEnrichmentVisualization docks one extra viewer (a ScatterPlot of proteinDf) on the enrichment TableView; a new "Enrichment Visualization" test asserts the volcano is present on that view post-dock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-UAT.md
@.planning/debug/enrichment-cross-link-not-firing.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-02-SUMMARY.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-06-SUMMARY.md
@packages/Proteomics/CLAUDE.md
@src/viewers/enrichment-viewers.ts
@src/viewers/volcano.ts
@src/tests/enrichment-visualization.ts

<interfaces>
<!-- Key types and contracts the executor needs. No codebase exploration required. -->

From src/viewers/enrichment-viewers.ts:
- openEnrichmentVisualization(enrichDf: DG.DataFrame, proteinDf: DG.DataFrame): void
- wireEnrichmentToVolcano(enrichDf: DG.DataFrame, proteinDf: DG.DataFrame): rxjs.Subscription
- createTopNEnrichmentDf(enrichDf: DG.DataFrame, topN?: number): DG.DataFrame
- createEnrichmentDotPlot(topDf: DG.DataFrame): DG.ScatterPlotViewer
- createEnrichmentBarChart(topDf: DG.DataFrame): DG.Viewer
- filterByDirection (file-local helper; unchanged)
- activeSubscriptions: rxjs.Subscription[] (module-level cleanup pool — unchanged)

From src/viewers/volcano.ts:
- createVolcanoPlot(df: DG.DataFrame, options?: {fcThreshold?, pThreshold?, topNLabels?, title?}): DG.ScatterPlotViewer
- The function is sync; it sets up Y/direction columns and threshold lines on the bound df, then returns the scatter.
- IMPORTANT: createVolcanoPlot calls pickMetricColumn internally, which THROWS when no `adj.p-value` or `p-value` column exists on df. This is the failure mode the try/catch guard in Action step 5 handles.

From src/utils/proteomics-types.ts (unchanged):
- SEMTYPE.GENE_SYMBOL ("Proteomics-Gene-Symbol")

Datagrok APIs used:
- tv.dockManager.dock(viewer, DG.DOCK_TYPE.LEFT | RIGHT | DOWN | UP, anchorNode | null, title, ratio)
- DG.DOCK_TYPE.LEFT / RIGHT / DOWN / UP
- grok.shell.tableViews: TableView[]
- A ScatterPlotViewer bound to proteinDf renders proteinDf.selection — co-docked viewers of the same DataFrame share selection state automatically.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Co-dock a proteinDf-bound volcano on the enrichment view + layout test</name>
  <files>src/viewers/enrichment-viewers.ts, src/tests/enrichment-visualization.ts</files>
  <behavior>
    - Test A (existing data-layer contract, MUST still pass): "wireEnrichmentToVolcano selects matching genes on protein DataFrame" — unchanged. Re-asserts proteinDf.selection.get(0)===true after row-click. No regression to the selection contract.
    - Test B (NEW layout regression): "openEnrichmentVisualization co-docks a volcano of the protein DataFrame on the enrichment view (directional-split path)". Build a minimal enrichDf (with Direction Up/Down rows AND Intersection column) and a proteinDf produced by the EXTENDED makeMockProteinDf (which now includes `adj.p-value` — see Action step 1). Call openEnrichmentVisualization(enrichDf, proteinDf). Then:
      1. Locate the TableView whose dataFrame === enrichDf (grok.shell.tableViews scan).
      2. Assert that view exists.
      3. Read tv.viewers (or equivalent: iterate dockManager and collect every viewer whose dataFrame === proteinDf). Assert count >= 1 — i.e. at least one viewer bound to proteinDf is docked on the enrichment view.
      4. Assert the first such viewer is a DG.VIEWER.SCATTER_PLOT (or duck-type: `.type === 'Scatter plot'`).
      5. Trigger enrichDf.currentRowIdx = 0; assert proteinDf.selection.trueCount >= 1 (the data-layer wire still fires through the new dock).
      6. Cleanup: close the table view (or clear it from the shell) to avoid leaking state into subsequent tests; ALSO call any returned subscriptions' unsubscribe — the module's activeSubscriptions is cleared on next openEnrichmentVisualization, so leaving them is acceptable but cleaning up the TableView is required so the test does not pollute downstream categories.
    - Test C (NEW, single-direction fallback): construct an enrichDf with NO Direction column (single-direction layout path) and a proteinDf from the same extended makeMockProteinDf. Assert the co-docked volcano is also present on the enrichment view. Same assertion shape as Test B.

    A note on test scope: Datagrok ScatterPlotViewer DOM rendering is not strictly required for the assertions — we only check that (a) the viewer exists in the dock manager bound to proteinDf, and (b) the selection-mutation contract continues to fire. This is the smallest assertion that would have caught Gap 1: it requires real openEnrichmentVisualization execution (not synthetic frames in isolation) and exercises the layout decision being added here.

    A note on regression coverage: the file currently has 8 tests (5 createTopNEnrichmentDf + 3 wireEnrichmentToVolcano). Tests B and C are ADDITIVE — total becomes 10. The fixture extension in Action step 1 enables Tests B and C to run; the existing 8 tests do not consume `adj.p-value` (they exercise the topN selector and the wireEnrichmentToVolcano contract independently of any volcano construction) so they remain unaffected.
  </behavior>
  <action>
    Modify src/viewers/enrichment-viewers.ts openEnrichmentVisualization (the function at L162) so that, in BOTH the directional-split branch and the single-direction fallback branch, after the existing dock calls are issued, ALSO dock a proteinDf-bound volcano on the enrichment TableView. Step ordering is REQUIRED — perform step 1 (fixture extension) BEFORE writing any new test code (steps 6+) so the new tests can run.

    1. **FIXTURE EXTENSION (do this FIRST so Tests B/C have the data shape they require).** Open src/tests/enrichment-visualization.ts and extend the file-local `makeMockProteinDf` helper to add an `adj.p-value` column populated as a `Float32Array` of plausible adjusted p-values matching the existing row count (e.g. for the 5-row helper, use values like [0.001, 0.01, 0.5, 0.0001, 0.04]). Use the DG.Column.fromFloat32Array idiom (or `DG.Column.fromList(DG.TYPE.FLOAT, 'adj.p-value', [...])`) — bulk init, NOT per-row col.set (memory feedback_dg_column_bulk_init). The 5-row column shape preserves backward compatibility for the existing 3 wireEnrichmentToVolcano tests (which don't read `adj.p-value`). Verify by mental walk-through that the 8 existing tests still type-check and run unchanged before proceeding.
    2. Import createVolcanoPlot at the top of src/viewers/enrichment-viewers.ts: `import {createVolcanoPlot} from './volcano';`. The function is sync and idempotent on the negLog10P/direction columns (volcano.ts Pitfall 5 pattern — never remove+re-add).
    3. STRUCTURAL INSERTION POINT (directional-split branch): after the existing `tv.dockManager.dock(createEnrichmentBarChart(downTop), DG.DOCK_TYPE.DOWN, downDotNode, 'Down — Bar Chart', 0.5);` call and BEFORE the `activeSubscriptions.push(wireEnrichmentToVolcano(enrichDf, proteinDf), wireEnrichmentToVolcano(upTop, proteinDf), wireEnrichmentToVolcano(downTop, proteinDf));` block, insert the co-dock guarded as described in step 5. Use this structural anchor (do not rely on line numbers — line numbers shift after the createVolcanoPlot import at the top of the file is added).
    4. STRUCTURAL INSERTION POINT (single-direction fallback): after `const sub2 = wireEnrichmentToVolcano(topDf, proteinDf);` and BEFORE `activeSubscriptions.push(sub1, sub2);`, insert the co-dock guarded as described in step 5. Same structural anchor approach — do not use line numbers.
    5. CO-DOCK PATTERN (used in both branches, wrapped in try/catch per blocker-fix below): construct `const volcano = createVolcanoPlot(proteinDf, {title: 'Volcano (linked)'}); tv.dockManager.dock(volcano, DG.DOCK_TYPE.LEFT, null, 'Volcano (linked)', 0.4);`. Place it LEFT of the existing dot/bar grid so the user reads volcano-then-charts naturally; ratio 0.4 leaves 60% for the four dot/bar viewers. Do NOT push the volcano viewer into activeSubscriptions — DG.Viewer does not expose a teardown compatible with rxjs.Subscription. The existing "Clean up previous subscriptions" block implicitly leaves stale docked viewers on a re-open; this matches the pre-change behavior for the dot/bar viewers and is out of scope for this gap.
    6. CONVENTION PRESERVATION: createVolcanoPlot is a `create*` factory — it returns a viewer with no shell side-effects. Calling it twice on the same proteinDf is safe because volcano.ts ensures NEG_LOG_COL and DIRECTION_COL are reused in place and applyThresholdLines filters formula lines by prefix before adding new ones. So a re-run of openEnrichmentVisualization (e.g. user clicks Enrichment Charts twice) does not stack viewers' formula lines.
    7. GUARD (REQUIRED — blocker fix): if proteinDf does NOT have an `adj.p-value` column (i.e. the cross-link was opened with a DataFrame that hasn't had DE run — possible from the `enrichmentCharts` fallback path in src/package.ts which passes `df` as both args when no DE-complete candidate exists), createVolcanoPlot throws ('adj.p-value column not found'). Wrap the createVolcanoPlot+dock pair in a `try { const volcano = createVolcanoPlot(proteinDf, {title: 'Volcano (linked)'}); tv.dockManager.dock(volcano, DG.DOCK_TYPE.LEFT, null, 'Volcano (linked)', 0.4); } catch (e: any) { console.warn('Co-docked volcano skipped: ' + (e?.message ?? e)); }`. Do NOT propagate — the enrichment view should still render, just without the co-docked volcano. The fixture extension in step 1 ensures this guard does NOT swallow the throw for Tests B/C — the proteinDf they pass HAS `adj.p-value`, so createVolcanoPlot succeeds and the dock happens.
    8. Add tests B and C in src/tests/enrichment-visualization.ts inside the existing `category('Enrichment Visualization', () => { ... })` block, right after the existing "wireEnrichmentToVolcano clears selection before new selection" test. Reuse the now-extended makeMockEnrichmentDf / makeMockProteinDf. Add a Direction column on the enrichment fixture for Test B (the directional-split path) and omit it for Test C (the single-direction fallback path).

    Reference existing code:
    - createVolcanoPlot signature and column requirements: src/viewers/volcano.ts (createVolcanoPlot function + pickMetricColumn helper).
    - openEnrichmentVisualization current shape: src/viewers/enrichment-viewers.ts L162+ (dock-manager idiom, find-by-DataFrame-identity for the TableView, two layout branches).
    - Existing wireEnrichmentToVolcano test (do not modify it; keep it as the unit-level cross-link contract): src/tests/enrichment-visualization.ts (the third wireEnrichmentToVolcano test in the category).
    - Heatmap precedent for co-docking on the current view via `tv.addViewer(...)`: src/package.ts (Show All Visualizations).

    Gap reason: cross-link IS firing correctly; layout/visibility issue — highlight rendered on backgrounded protein TableView. Fix is layout, not wiring.
  </action>
  <verify>
    <automated>cd packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --category "Enrichment Visualization"</automated>
  </verify>
  <done>
    Build succeeds (no TypeScript errors from the new import or guard). All Enrichment Visualization tests pass: existing 8 tests + 2 new (B, C) = 10 total. The fixture extension is verifiable by inspecting makeMockProteinDf for an `adj.p-value` column before any new dock-assertion runs. Existing wireEnrichmentToVolcano data-layer test still passes. After running Proteomics | Analyze | Enrichment on a Candidates or Report DE result, the enrichment TableView shows the volcano docked LEFT and the dot/bar charts docked RIGHT; clicking a term row highlights points on the co-docked volcano. The original volcano on the protein TableView (if present) continues to render the same selection.
  </done>
</task>

</tasks>

<verification>
1. Build: `cd packages/Proteomics && npm run build` exits 0.
2. Targeted tests: `grok test --category "Enrichment Visualization"` — all green (existing 8 tests + 2 new = 10 tests).
3. Regression check: `grok test --category "Enrichment"` — existing wireEnrichmentToVolcano test in src/tests/enrichment.ts still passes (synthetic-frame data-layer contract is untouched by this change).
4. (Human-UAT, not gated automatically here — covered by re-running 13-UAT Test 2 once these plans land.)
</verification>

<success_criteria>
- src/viewers/enrichment-viewers.ts imports createVolcanoPlot and dock-creates a volcano of proteinDf in BOTH layout branches of openEnrichmentVisualization.
- Both new tests (B, C) assert a proteinDf-bound viewer is present on the enrichment TableView's dock manager after openEnrichmentVisualization.
- makeMockProteinDf is extended FIRST (before new test code) with an `adj.p-value` Float32Array column so the new co-dock can construct a volcano successfully.
- The try/catch guard around the co-dock prevents the rare no-DE proteinDf case (package.ts fallback) from breaking enrichment rendering.
- No change to wireEnrichmentToVolcano signature, body, or its three subscription sites.
- No change to the protein TableView's viewer set (the existing Show-All-Visualizations volcano on the protein view, if any, is untouched).
</success_criteria>

<output>
Create `.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-07-SUMMARY.md` when done. Include: which UX option was chosen (B), the rationale for not modifying the protein view, the createVolcanoPlot idempotency justification, and a one-line note that 13-UAT.md Test 2 should be re-run to confirm the gap closes.
</output>
