---
phase: 13-ck-omics-volcano-and-enrichment-parity
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [R2, R5]
files_modified: [src/analysis/enrichment.ts, src/viewers/enrichment-viewers.ts, src/tests/enrichment.ts]
must_haves:
  truths:
    - "D-10: two g:Profiler queries (up-regulated / down-regulated significant genes) are merged into one enrichment DataFrame with a Direction column; the Phase-9 dot/bar + volcano cross-link is unchanged; background = all detected proteins for both directional queries"
    - "D-11: the two directions are presented as split Up/Down dot + bar charts side-by-side; the source filter applies to both"
    - "D-12: 'WP' (WikiPathways) is added to the g:Profiler source list, default-on"
    - "Enrichment splits significant genes by log2FC sign into separate up and down g:Profiler queries"
    - "Both directional queries use the same all-detected-proteins custom background"
    - "The merged enrichment DataFrame has a Direction column with values Up and Down"
    - "WikiPathways (WP) is a default-on enrichment source in the dialog"
    - "The Phase-9 onCurrentRowChanged volcano cross-link still works on the merged DataFrame"
    - "Up and Down dot+bar charts are docked side-by-side"
  artifacts:
    - {path: src/analysis/enrichment.ts, provides: "up/down split + WP source", contains: "Direction"}
    - {path: src/viewers/enrichment-viewers.ts, provides: "Direction-aware split charts", contains: "Direction"}
    - {path: src/tests/enrichment.ts, provides: "R2 split + R5 WP tests", contains: "Direction"}
  key_links:
    - {from: src/analysis/enrichment.ts, to: "gGOSt twice (up,down)", via: "split significantGenes by fc sign", pattern: "gGOSt"}
    - {from: src/viewers/enrichment-viewers.ts, to: "proteinDf.selection", via: "onCurrentRowChanged unchanged", pattern: "onCurrentRowChanged"}
---

<objective>
R2 (up/down enrichment split, D-10/D-11) and R5 (WikiPathways source, D-12). Split significant genes by log2FC sign into two g:Profiler queries sharing the all-detected background, merge into one DataFrame with a Direction column, present Up/Down dot+bar side-by-side, and add WP as a default-on source. Caller-only change to enrichment; the Phase-9 cross-link is preserved verbatim.
Purpose: Match CK-omics directional deliverables for the BP DMD/WT engagement.
Output: split enrichment pipeline, Direction-aware viewers, WP source, extended tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-CONTEXT.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-RESEARCH.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-PATTERNS.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-VALIDATION.md
@.planning/milestones/v1.2-phases/09-enrichment-visualization-volcano-integration/09-CONTEXT.md
<interfaces>
enrichment.ts: gGOSt(queryGenes, backgroundGenes, organism, sources, threshold) — JSON body via JSON.stringify, sources passed through unchanged. buildEnrichmentDf(results, queryGenes, pThreshold) builds columns Source/Term ID/Term Name/FDR/Gene Count/Gene Ratio/Intersection/Significant via addNewX().init() (bulk). runEnrichmentPipeline(df, fcThr, pThr, organismCode, sources) builds significantGenes (Set, both signs) + backgroundGenes (all mapped rows) then one gGOSt. showEnrichmentDialog has bool inputs goBpInput/goMfInput/goCcInput/keggInput/reactomeInput; OK-handler pushes 'GO:BP'/'GO:MF'/'GO:CC'/'KEGG'/'REAC' into selectedSources.
enrichment-viewers.ts: createTopNEnrichmentDf(enrichDf, topN) clones via mask; createEnrichmentDotPlot(topDf)/createEnrichmentBarChart(topDf) factories; wireEnrichmentToVolcano(enrichDf, proteinDf) returns the onCurrentRowChanged subscription; openEnrichmentVisualization(enrichDf, proteinDf) docks dotPlot RIGHT then barChart DOWN via tv.dockManager.dock(...0.5); module-level activeSubscriptions array unsubscribed on re-open.
CK contract: CKomics_tool2.py:4450-4684 run_gprofiler_analysis (up_data=sig[fc>0], down_data=sig[fc<0], strict >0/<0; background = all detected, both calls; DO NOT adopt highlight-filtering — that is Phase 14). :478-486 / get_selected_databases — WP literal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split enrichment by direction + add Direction column + WP source</name>
  <files>src/analysis/enrichment.ts, src/tests/enrichment.ts</files>
  <read_first>
- src/analysis/enrichment.ts lines 90-130 (gGOSt body), 134-189 (buildEnrichmentDf bulk pattern), 213-322 (runEnrichmentPipeline significant/background build), 327-419 (showEnrichmentDialog source inputs + OK handler)
- src/tests/enrichment.ts 1-40 (makeMockResults, GostResult shape, buildEnrichmentDf/countSignificantProteins imports)
- 13-RESEARCH.md "CK-omics Algorithm Port: g:Profiler Up/Down Split" + "R5: WikiPathways Source"
- 13-PATTERNS.md "src/analysis/enrichment.ts" section
- ~/Downloads/ck/CKomics_tool2.py 4450-4684 (split) and 478-486 (WP literal)
  </read_first>
  <behavior>
- Given sig genes with mixed fc signs, runEnrichmentPipeline issues exactly two gGOSt calls: upGenes=fc>0, downGenes=fc<0, both with the identical all-detected bgArray
- Merged DataFrame has a categorical Direction string column with only 'Up'/'Down'; Intersection/Source/Term columns retained from buildEnrichmentDf shape
- If a direction has zero genes, that gGOSt call is skipped (no empty-query request) and only the other direction appears
- wpInput defaults to true; when checked, 'WP' (literal) is pushed into selectedSources; gGOSt body otherwise unchanged (no highlight flag)
  </behavior>
  <action>In runEnrichmentPipeline: keep backgroundGenes = all detected (unchanged); split significantGenes into upGenes (fcRaw>0) / downGenes (fcRaw<0) using the existing getRawData bulk read; call gGOSt twice with the same bgArray; build per-direction result arrays, concat, and emit a Direction string column ('Up'/'Down') via the addNewString().init() bulk idiom used in buildEnrichmentDf (memory feedback_dg_column_bulk_init — never per-row set). Reuse buildEnrichmentDf for each direction's rows then concat + tag Direction, OR extend buildEnrichmentDf to accept a direction label. Do NOT change the gGOSt request body, significance_threshold_method, domain_scope, or background behaviour. In showEnrichmentDialog add const wpInput = ui.input.bool('WikiPathways', {value: true}) and .add(wpInput); in the OK handler add if (wpInput.value) selectedSources.push('WP'). Preserve proteomics.enrichment tag.</action>
  <verify><automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --category Enrichment</automated></verify>
  <acceptance_criteria>
- enrichment.ts issues two gGOSt calls (up/down) with identical bgArray; verified by an Enrichment test asserting two distinct query sets and one shared background
- merged DataFrame Direction column ∈ {'Up','Down'} only; buildEnrichmentDf columns preserved
- 'WP' literal pushed when wpInput.value; wpInput default true; gGOSt body byte-unchanged except sources array
- grok test --category Enrichment green (rebuilt, not --skip-build)
  </acceptance_criteria>
  <done>Enrichment produces a directional, WP-capable result DataFrame with the Phase-9 column shape intact.</done>
</task>

<task type="auto">
  <name>Task 2: Direction-aware side-by-side Up/Down dot+bar charts</name>
  <files>src/viewers/enrichment-viewers.ts, src/tests/enrichment.ts</files>
  <read_first>
- src/viewers/enrichment-viewers.ts 1-52 (createTopNEnrichmentDf clone-by-mask), 58-85 (dot/bar factories), 92-134 (wireEnrichmentToVolcano onCurrentRowChanged → proteinDf.selection), 140-170 (openEnrichmentVisualization dock idiom + activeSubscriptions cleanup)
- 13-CONTEXT.md D-11; 13-RESEARCH.md "Enrichment Viewer Direction Extension"; 13-PATTERNS.md "src/viewers/enrichment-viewers.ts"
- .claude/skills/ui/SKILL.md (worktree root or package) — REQUIRED before touching dockManager / viewer layout
  </read_first>
  <action>In openEnrichmentVisualization, when the enrichDf has a Direction column, build two top-N subsets via the existing createTopNEnrichmentDf clone-by-mask idiom filtered to Direction=='Up' and Direction=='Down', create createEnrichmentDotPlot + createEnrichmentBarChart per direction, and dock them side-by-side using the established tv.dockManager.dock(viewer, DG.DOCK_TYPE.*, anchorNode, title, ratio) idiom (titles e.g. 'Up — Dot Plot' / 'Down — Dot Plot'). Keep wireEnrichmentToVolcano and the proteinDf.selection cross-link UNCHANGED (the merged DataFrame keeps Intersection; Direction is just an extra categorical). Reuse the module-level activeSubscriptions cleanup on re-open (do not leak subscriptions). Single-direction results: fall back to the existing single dot+bar layout. Do not mutate the source DataFrame in the cross-link.</action>
  <verify><automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --category Enrichment</automated></verify>
  <acceptance_criteria>
- a Direction-bearing enrichDf produces 4 docked viewers (Up dot+bar, Down dot+bar) via dockManager.dock; single-direction → original 2
- wireEnrichmentToVolcano signature/body unchanged; onCurrentRowChanged still sets proteinDf.selection (assert in an Enrichment test that the subscription fires and selection mutates, source DF unmutated)
- activeSubscriptions unsubscribed on re-open (no growth across two openEnrichmentVisualization calls)
- grok test --category Enrichment green
  </acceptance_criteria>
  <done>Up/Down charts render side-by-side; the Phase-9 volcano cross-link is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → g:Profiler (/api/gost/profile/) | outbound POST via fetchProxy; gene symbols from the parsed DataFrame |
| g:Profiler response → client | untrusted JSON parsed into the enrichment DataFrame |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | g:Profiler request injection via gene names | mitigate | Body built via JSON.stringify in existing gGOSt (no string concat); two split calls reuse the same body shape |
| T-13-02-02 | Denial of Service | oversized/empty directional gene set | mitigate | Skip the gGOSt call when a direction has zero genes; existing pipeline already guards empty query with a thrown error surfaced to the user |
| T-13-02-03 | Tampering | malformed g:Profiler JSON | accept | buildEnrichmentDf already tolerates missing fields (intersections optional); no new parse surface added (caller-only change) |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; RESEARCH "Package Legitimacy Audit" = N/A (zero external packages) |
</threat_model>

<verification>
grok test --category Enrichment green after each task (rebuilt, not --skip-build); two gGOSt calls with shared background; Direction ∈ {Up,Down}; WP default-on literal; cross-link intact.
</verification>

<success_criteria>
Directional, WP-capable enrichment with side-by-side Up/Down charts and an unbroken Phase-9 volcano cross-link.
</success_criteria>

<output>
Create `.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-02-SUMMARY.md` when done
</output>
