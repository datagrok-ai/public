---
phase: 13-ck-omics-volcano-and-enrichment-parity
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
autonomous: true
requirements: [R3, R4]
files_modified: [src/parsers/spectronaut-candidates-parser.ts, src/package.ts, src/tests/spectronaut-candidates-parser.ts, src/tests/spectronaut-candidates-e2e.ts]
must_haves:
  truths:
    - "A canonical single-orientation Candidates file is unchanged by the sign-flip (no mirror)"
    - "Rows whose declared comparison is the reverse of canonical get log2FC negated, AVG Group Quantity Numerator/Denominator swapped, and the comparison relabeled"
    - "The AVG Group Quantity swap is skipped when those columns are absent"
    - "The parser performs NO grok.shell work (stays pure)"
    - "A Candidates file with >1 distinct Comparison docks a native Filter viewer scoped to Comparison; a single-comparison file does not"
  artifacts:
    - {path: src/parsers/spectronaut-candidates-parser.ts, provides: "D-08 per-row sign normalization", contains: "Comparison"}
    - {path: src/package.ts, provides: "D-07 Comparison Filter viewer docking", contains: "dockManager"}
    - {path: src/tests/spectronaut-candidates-parser.ts, provides: "canonical-unchanged + reversed-flip + guard tests", contains: "Comparison"}
  key_links:
    - {from: src/parsers/spectronaut-candidates-parser.ts, to: "log2FC/AVG Group Quantity columns", via: "getRawData bulk flip then init", pattern: "getRawData"}
    - {from: src/package.ts, to: "tv.dockManager.dock Filters", via: "if >1 distinct Comparison", pattern: "dockManager"}
---

<objective>
R3/D-08: normalize each Candidates row's sign so positive log2FC = declared-group1-enriched, flipping log2FC + swapping AVG Group Quantity Numerator/Denominator + relabeling ONLY for rows whose declared comparison is the reverse of canonical (CK-omics create_subset_data flipped-string case). R4/D-07: when a file has >1 distinct Comparison, dock a native Filter viewer scoped to Comparison so the analyst narrows interactively. This unparks the report-DE direction defect's Candidates half (memory project_proteomics_spectronaut_de_direction_default).
Purpose: Volcano sign matches the client CK-omics figure by default; multi-contrast files are usable.
Output: pure sign-normalizing parser, Filter-viewer docking, regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-CONTEXT.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-RESEARCH.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-PATTERNS.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-VALIDATION.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-WAVE0-FINDINGS.md
<interfaces>
spectronaut-candidates-parser.ts: parseSpectronautCandidatesText(text) PURE → DataFrame. Has findCol(df,names), renameToCanonical(df,src,canonical), name-variant arrays LOG2FC_COLUMNS/ADJ_P_COLUMNS/P_VALUE_COLUMNS at top. Bulk block ~138-146: sigCol.init reads fcRaw/adjPRaw via getRawData; FLOAT_NULL guarded. Keeps the Comparison column verbatim. Sets proteomics.source='spectronaut-candidates', de_complete='true', de_method='spectronaut'.
package.ts importSpectronautCandidates() (~156-172): after parseSpectronautCandidatesText → df.name=... → grok.shell.addTableView(df) → grok.shell.info(...). Dock idiom (from enrichment-viewers.ts:164): tv.dockManager.dock(viewer, DG.DOCK_TYPE.RIGHT, null, 'Title', 0.3); Filters viewer scoped via its columnNames look property.
CK contract: CKomics_tool2.py:1625-1662 create_subset_data — flipped-string case: AVG Log2 Ratio *= -1; relabel Comparison; swap AVG Group Quantity Numerator/Denominator if present; set Condition Numerator/Denominator if present. Comparison value form: "DMD / WT" (group1 / group2). 13-WAVE0-FINDINGS.md A2 = the definitive per-row flip rule + AVG Group Quantity presence; OBEY it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-row Candidates sign normalization (pure parser)</name>
  <files>src/parsers/spectronaut-candidates-parser.ts, src/tests/spectronaut-candidates-parser.ts</files>
  <read_first>
- src/parsers/spectronaut-candidates-parser.ts (entire file — findCol/renameToCanonical/name-variant arrays, the ~138-146 getRawData/FLOAT_NULL block to extend)
- src/tests/spectronaut-candidates-parser.ts 1-30 (makeTsv, BASE_HEADERS incl. 'Comparison (group1/group2)', baseRow)
- 13-WAVE0-FINDINGS.md (A2 — the LOCKED per-row flip rule + AVG Group Quantity column presence; this overrides any assumption)
- 13-RESEARCH.md "CK-omics Algorithm Port: create_subset_data Flip"; "Pitfall 4" (no unconditional inversion), "Pitfall 6" (numeric sentinel)
- 13-PATTERNS.md "src/parsers/spectronaut-candidates-parser.ts"; ~/Downloads/ck/CKomics_tool2.py 1625-1662
- memory feedback_dg_column_bulk_init, feedback_dg_column_init_null_sentinel
  </read_first>
  <behavior>
- Canonical single-orientation file (declared comparison already canonical): log2FC, AVG Group Quantity columns, and Comparison strings are byte-identical to pre-change output (the existing "parses standard rows" test still passes)
- File containing the reversed comparison string for some rows: exactly those rows get log2FC negated, AVG Group Quantity Numerator/Denominator values swapped, and Comparison relabeled to canonical; canonical rows untouched
- AVG Group Quantity columns absent: log2FC still flips for reversed rows; no swap attempted, no throw (A6 guard)
- FLOAT_NULL log2FC cells stay null after the flip (no 2.6789e-34 sentinel leak)
- No grok.shell reference anywhere in the parser
  </behavior>
  <action>Follow the A2 rule recorded in 13-WAVE0-FINDINGS.md exactly. Add AVG_GROUP_QTY_NUMERATOR/DENOMINATOR name-variant arrays alongside LOG2FC_COLUMNS and detect via findCol. Define a canonical orientation from each row's declared "group1 / group2" Comparison (group1/group2) string (substring before/after " / "). Before the existing sigCol bulk block: getRawData() the log2FC array, build a per-row decision (flip ONLY when that row's declared comparison is the reverse of canonical per the A2 finding — NEVER unconditional), write a new Float32Array with negated values for flipped rows preserving FLOAT_NULL via isNone()/explicit DG.FLOAT_NULL exactly as the sigCol block, then col.init from it (bulk, memory feedback_dg_column_bulk_init). If both AVG Group Quantity columns exist, build swapped Float32Arrays for the flipped rows and init those columns the same way; if absent, skip the swap (A6). Relabel the Comparison value per flipped row to canonical (string column init, not per-row set). Keep the parser PURE (Pitfall 3 — no grok.shell). Extend the test file: keep the existing canonical test, add a reversed-comparison-string fixture asserting flipped log2FC sign + swapped quantity values + relabeled Comparison, and a quantity-columns-absent fixture asserting flip-without-swap and no throw.</action>
  <verify><automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --category SpectronautCandidates</automated></verify>
  <acceptance_criteria>
- existing "parses standard rows" + canonical tests still pass unchanged (no mirror on canonical input)
- new reversed-string test: flipped rows have negated log2FC, swapped AVG Group Quantity values, relabeled Comparison; canonical rows identical
- new quantity-absent test: flip occurs, no swap, no exception
- grep shows zero grok.shell tokens in spectronaut-candidates-parser.ts
- grok test --category SpectronautCandidates green (rebuilt, not --skip-build)
  </acceptance_criteria>
  <done>Candidates parser normalizes sign per declared contract, purely, with the A2 rule honored.</done>
</task>

<task type="auto">
  <name>Task 2: Dock a native Comparison Filter viewer for multi-contrast files</name>
  <files>src/package.ts, src/tests/spectronaut-candidates-e2e.ts</files>
  <read_first>
- src/package.ts importSpectronautCandidates() ~156-172 (addTableView + info; the shell-orchestration site)
- src/viewers/enrichment-viewers.ts 158-170 (the tv.dockManager.dock idiom to reuse)
- src/tests/spectronaut-candidates-e2e.ts 1-40 (FIXTURE_PATH, _package.files.readAsText, the e2e category shape)
- 13-CONTEXT.md D-07 (no modal picker; native Filter viewer; single-comparison → skip); 13-RESEARCH.md "R4: Comparison Filter Viewer Docking" (incl. the documented "counts reflect all rows" caveat)
- .claude/skills/ui/SKILL.md (worktree root or package) — REQUIRED before adding the Filter viewer / dockManager work
  </read_first>
  <action>In importSpectronautCandidates, after grok.shell.addTableView(df): resolve the comparison column (the verbatim "Comparison (group1/group2)" / "Comparison" column the parser retains), count distinct non-null values. If >1, get the table view and dock a native Datagrok Filters viewer using the established tv.dockManager.dock(filterViewer, DG.DOCK_TYPE.RIGHT, null, 'Comparison', 0.3) idiom, scoping it to only the Comparison column via the Filters viewer columnNames look property. If exactly one distinct value, skip the filter. Parser stays pure — all of this is package.ts shell orchestration only (Pitfall 3). Extend spectronaut-candidates-e2e.ts with a multi-comparison fixture asserting the Filter viewer is docked and a single-comparison fixture asserting it is not (use the e2e category's running-instance pattern; rebuild before running — feedback_grok_test_skipbuild_stale).</action>
  <verify><automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --category "SpectronautCandidates E2E"</automated></verify>
  <acceptance_criteria>
- >1 distinct Comparison → a Filters viewer is docked scoped to the Comparison column (e2e assertion)
- single distinct Comparison → no Filter viewer docked (e2e assertion)
- spectronaut-candidates-parser.ts unchanged by this task (purity preserved)
- grok test --category "SpectronautCandidates E2E" green (rebuilt)
  </acceptance_criteria>
  <done>Multi-contrast Candidates files get an interactive Comparison filter; single-contrast files are unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| uploaded file → parser | untrusted Candidates TSV/CSV text parsed into a DataFrame |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Tampering | malformed Comparison string (no " / ") | mitigate | Treat unparseable comparison as canonical (no flip) rather than throwing; never invert on ambiguity (Pitfall 4) |
| T-13-03-02 | Tampering | absent AVG Group Quantity columns | mitigate | findCol guard — swap only when both present (A6); flip log2FC regardless, no throw |
| T-13-03-03 | Denial of Service | very large Candidates file | accept | Existing parser already handles full-file parse; bulk getRawData/init is the measured-fast path (feedback_dg_column_bulk_init) |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; RESEARCH "Package Legitimacy Audit" = N/A (zero external packages) |
</threat_model>

<verification>
SpectronautCandidates + "SpectronautCandidates E2E" categories green after each task (rebuilt, not --skip-build); canonical input unchanged; reversed rows flipped+swapped+relabeled; quantity-absent guarded; parser shell-free; Filter viewer docked only when >1 Comparison.
</verification>

<success_criteria>
No-mirror on canonical files, correct flip on reversed contrasts per the A2-locked rule, pure parser, interactive multi-contrast filtering.
</success_criteria>

<output>
Create `.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-03-SUMMARY.md` when done
</output>
