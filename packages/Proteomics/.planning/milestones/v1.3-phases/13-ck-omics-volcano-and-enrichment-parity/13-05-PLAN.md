---
phase: 13-ck-omics-volcano-and-enrichment-parity
plan: 05
type: execute
wave: 2
depends_on: ["13-01"]
autonomous: true
requirements: [R3]
files_modified: [src/analysis/differential-expression.ts, src/tests/analysis.ts]
must_haves:
  truths:
    - "For a Spectronaut report import, the DE dialog defaults the Comparison to the declared/intended contrast, not the alphabetical condition order"
    - "The Comparison dropdown remains as a manual override (same items), only its default changes"
    - "DE hit-list magnitudes are unchanged (direction-only fix)"
  artifacts:
    - {path: src/analysis/differential-expression.ts, provides: "D-09 declared-contrast default", contains: "comparisonInput"}
    - {path: src/tests/analysis.ts, provides: "D-09 default-orientation regression test", contains: "Comparison"}
  key_links:
    - {from: src/analysis/differential-expression.ts, to: "declared contrast orientation", via: "comparisonInput default value", pattern: "comparisonInput"}
---

<objective>
R3/D-09: unpark the Spectronaut report-import DE direction defect. The DE dialog currently defaults its Comparison choice to pairs[0] (alphabetical condition order), the reverse of Spectronaut's declared Numerator/Denominator (memory project_proteomics_spectronaut_de_direction_default). Make the default the declared/intended contrast and retire the manual workaround as the required step (the dropdown stays as an override). This phase is the explicit prompt that overrides the memory's "do not implement unprompted" note.
Purpose: An unaware analyst gets the correct volcano sign by default; the manual dropdown workaround is demoted to optional.
Output: direction-only default fix in `src/analysis/differential-expression.ts` (the declared-contrast default of `comparisonInput`) + regression test.

**D-09 fix site is PINNED to the DE dialog default in `src/analysis/differential-expression.ts`.** PATTERNS.md unambiguously names `src/analysis/differential-expression.ts` as the D-09 target; RESEARCH.md identifies the `pairs[0]`/alphabetical default as the defect. 13-WAVE0-FINDINGS.md A3 records the exact `differential-expression.ts` function:line and the declared-orientation value to apply — it confirms (does not reroute) this site. `src/parsers/spectronaut-parser.ts` is **NOT** modified by this plan and is intentionally absent from `files_modified`: parse-time group order is left as-is; only the dialog's default selection changes. If a Wave-0 finding were to ever contradict this pin (it does not — A3 is resolved to the dialog default in RESEARCH.md), the executor must STOP and surface the conflict rather than touch an undeclared file — worktree file isolation is non-negotiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-CONTEXT.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-RESEARCH.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-PATTERNS.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-VALIDATION.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-WAVE0-FINDINGS.md
<interfaces>
differential-expression.ts showDEDialog(df,onComplete): pairs = [`${g2.name} vs ${g1.name}`, `${g1.name} vs ${g2.name}`]; comparisonInput = ui.input.choice('Comparison',{value: pairs[0], items: pairs, nullable:false}); updateHint derives numerator/denominator by pairs index; OK-handler (~355-361) reversed = comparisonInput.value===pairs[1] → numeratorCols/denominatorCols/names by index. Group order comes from getGroups(df) (proteomics.groups JSON via setGroups). THE FIX: change only the comparisonInput default `value` from `pairs[0]` to the declared/intended-contrast pair (the entry whose numerator/denominator matches the declared Spectronaut Numerator/Denominator). Keep both `items` selectable and the OK-handler index mapping byte-for-byte.
spectronaut-parser.ts auto-populates groups from R.Condition conditions[0]/conditions[1] insertion order. OUT OF SCOPE for this plan — do not edit; read-only context for understanding why pairs[0] is wrong by default.
13-WAVE0-FINDINGS.md A3 = confirms the differential-expression.ts function:line and the declared-orientation default value to set. It validates the pinned site; it never reroutes the fix to the parser. Memory: this is direction-only — does NOT move hit-list metrics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Default the DE Comparison to the declared/intended contrast</name>
  <files>src/analysis/differential-expression.ts, src/tests/analysis.ts</files>
  <read_first>
- 13-WAVE0-FINDINGS.md (A3 — confirms the exact `differential-expression.ts` function:line and the declared-orientation default value; it validates the pinned dialog-default site, it does NOT reroute the fix elsewhere — follow it)
- src/analysis/differential-expression.ts 268-361 (showDEDialog: pairs, comparisonInput value: pairs[0] ← the only line that changes, updateHint, OK-handler index mapping ~355-361 — keep the index mapping intact, change only the default value)
- src/parsers/spectronaut-parser.ts ~123-200 (autoPopulateGroups conditions[0..1] → setGroups — READ-ONLY context only; this plan does NOT modify the parser. It is not in files_modified.)
- src/analysis/experiment-setup.ts 1-60 (getGroups/setGroups/GroupAssignment — go through these, never parse the proteomics.groups tag directly)
- 13-RESEARCH.md "Report-DE Direction Fix (D-09)"; 13-PATTERNS.md "src/analysis/differential-expression.ts"
- src/tests/analysis.ts 1-25 (makeTestDf, setGroups/getGroups/runDifferentialExpression imports — the regression-test surface)
- ~/.claude/projects/-Users-edjaeger-datagrok-src-public/memory/project_proteomics_spectronaut_de_direction_default.md
  </read_first>
  <behavior>
- After the fix, for a Spectronaut-report-shaped DataFrame whose declared contrast is Numerator/Denominator, the DE dialog's default Comparison resolves to the declared (intended) orientation — NOT the alphabetical pairs[0]
- The Comparison dropdown still offers both items (manual override preserved); only the default selection changes
- Running DE with the new default yields log2FC signs consistent with the declared contrast; |log2FC| magnitudes and p-values are unchanged vs. the same data run with the previously-manual correct selection (direction-only)
- The fix is contained entirely within src/analysis/differential-expression.ts; no parser file is touched
  </behavior>
  <action>In `src/analysis/differential-expression.ts` showDEDialog, change ONLY the `comparisonInput` default `value` from `pairs[0]` to the pair whose index maps (via the existing OK-handler index logic) to the declared/intended Spectronaut Numerator/Denominator orientation, exactly as confirmed in 13-WAVE0-FINDINGS.md A3. Do NOT modify `src/parsers/spectronaut-parser.ts` or any file other than the two in `files_modified` — if 13-WAVE0-FINDINGS.md A3 unexpectedly pointed at the parser instead of the dialog default (it does not — A3 is resolved to the dialog default), STOP and surface the conflict rather than edit an undeclared file. Keep the OK-handler index-based numerator/denominator mapping intact and keep both pairs items selectable (override preserved). Do NOT alter the DE statistics, thresholds, or method paths — direction-only. Add a test to src/tests/analysis.ts: build a two-condition DataFrame mirroring the Spectronaut report group order, assert the resolved default orientation equals the declared contrast (sign of the resulting log2FC for a constructed strong marker), and assert |log2FC| equals the value from an explicit reversed-selection run (magnitude unchanged).</action>
  <verify><automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --category Analysis</automated></verify>
  <acceptance_criteria>
- the change is the `comparisonInput` default value in `src/analysis/differential-expression.ts` at the function:line confirmed by 13-WAVE0-FINDINGS.md A3; direction-only edit applied
- no file outside files_modified (differential-expression.ts, src/tests/analysis.ts) is modified — spectronaut-parser.ts untouched
- new Analysis test asserts default orientation = declared contrast (sign) AND |log2FC| unchanged vs explicit reversed selection
- Comparison dropdown still has both items; OK-handler index mapping unchanged
- grok test --category Analysis green (rebuilt, not --skip-build)
  </acceptance_criteria>
  <done>The DE dialog defaults to the declared contrast; manual workaround demoted to optional; magnitudes unchanged; fix contained to differential-expression.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| in-memory DataFrame → DE dialog | group order read via getGroups (proteomics.groups tag); no external input crosses here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Tampering | malformed/absent proteomics.groups | accept | getGroups already returns null on parse failure; showDEDialog warns and aborts (pre-existing guard, unchanged) |
| T-13-05-02 | Repudiation | silent wrong-direction default (the original defect) | mitigate | This plan removes the defect; regression test asserts the default orientation and is part of the suite gate |
| T-13-05-03 | Tampering | edit leaking outside declared files into spectronaut-parser.ts | mitigate | Fix site pinned to differential-expression.ts; plan instructs STOP-and-surface if a finding ever points elsewhere; files_modified is the isolation contract |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; RESEARCH "Package Legitimacy Audit" = N/A (zero external packages) |
</threat_model>

<verification>
grok test --category Analysis green (rebuilt, not --skip-build); default orientation = declared contrast; |log2FC| unchanged vs explicit reversed run; both dropdown items retained; change is the comparisonInput default value in differential-expression.ts; spectronaut-parser.ts unmodified.
</verification>

<success_criteria>
Spectronaut report DE defaults to the declared/intended contrast with no magnitude change, the manual workaround demoted to an optional override, and the change contained entirely within the two declared files.
</success_criteria>

<output>
Create `.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-05-SUMMARY.md` when done
</output>
