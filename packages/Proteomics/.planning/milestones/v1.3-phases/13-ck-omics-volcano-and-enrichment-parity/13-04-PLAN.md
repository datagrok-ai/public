---
phase: 13-ck-omics-volcano-and-enrichment-parity
plan: 04
type: execute
wave: 2
depends_on: ["13-01"]
autonomous: true
requirements: [R1]
files_modified: [src/analysis/subcellular-location.ts, src/panels/uniprot-panel.ts, src/tests/subcellular-location.ts]
must_haves:
  truths:
    - "D-01: subcellular locations are fetched via the UniProt stream endpoint through grok.dapi.fetchProxy (chunked accession:(...) OR-query, fields accession,cc_subcellular_location,go_c), replacing the per-row raw fetch"
    - "D-02: a persistent cross-session accession-keyed cache lives in this shared subcellular-location module and uniprot-panel.ts is refactored to reuse it (closes the folded cache-uniprot todo)"
    - "D-03: Unknown+gene-name accessions trigger the reviewed-entry-by-gene-name batched fallback (one extra batched gene_exact+reviewed:true query, reviewed location back-assigned)"
    - "D-04: the 11-category + Unknown classifier (keyword map, UniProt order-of-importance / GO-CC fallback ordering rule, exact hex palette) is ported verbatim from Subcellular_Location_Classification_README.txt — locked client contract, no re-derivation"
    - "parseSubcellularLocation returns the CK-omics category for subcell-first, GO-fallback, earliest-position tie-break, and Unknown cases"
    - "The 11-color palette ARGB values exactly match the locked CK-omics hex palette"
    - "UniProt stream TSV is parsed positionally (header line skipped, parts[0..4]) with reviewed-priority merge"
    - "Unknown+gene-name accessions trigger the D-03 reviewed-by-gene batched fallback"
    - "Location results are cached cross-session per the mechanism chosen in 13-WAVE0-FINDINGS.md"
    - "uniprot-panel.ts delegates its UniProt fetch to the shared module (folded cache todo)"
  artifacts:
    - {path: src/analysis/subcellular-location.ts, provides: "fetch + verbatim classifier + cache", contains: "parseSubcellularLocation"}
    - {path: src/panels/uniprot-panel.ts, provides: "refactored to reuse shared module", contains: "subcellular-location"}
    - {path: src/tests/subcellular-location.ts, provides: "classifier/palette/TSV tests", contains: "parseSubcellularLocation"}
  key_links:
    - {from: src/analysis/subcellular-location.ts, to: "rest.uniprot.org/uniprotkb/stream", via: "grok.dapi.fetchProxy", pattern: "fetchProxy"}
    - {from: src/panels/uniprot-panel.ts, to: src/analysis/subcellular-location.ts, via: "import shared fetch+cache", pattern: "subcellular-location"}
---

<objective>
R1 foundation (D-01/D-02/D-03/D-04): new shared src/analysis/subcellular-location.ts batching accessions to the UniProt stream endpoint via fetchProxy, the CK-omics 11-category classifier + hex palette ported verbatim, the D-03 reviewed-by-gene fallback, cross-session cache; uniprot-panel.ts refactored to reuse it (closes the folded cache-uniprot todo).
Purpose: Provides the accession→category map + color contract the volcano color path (13-06) consumes; any divergence from the locked README is a parity regression.
Output: shared module, refactored panel, pure classifier/palette/TSV tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-CONTEXT.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-RESEARCH.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-PATTERNS.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-VALIDATION.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-WAVE0-FINDINGS.md
<interfaces>
uniprot-panel.ts: parseAccession(raw) EXPORTED, stays here (imported by enrichment.ts:4 — do NOT move). fetchUniProtData ~77-91: grok.dapi.fetchProxy, !resp.ok warn+null, try/catch warn+null — discipline to mirror. Render path 120-206 unchanged.
SEMTYPE.SUBCELLULAR_LOCATION added in 13-01 (assigned in 13-06, not here). Verified: js-api/src/dapi.ts:735-760 grok.dapi.userDataStorage get/put string KV. Cache mechanism + __v key = the choice in 13-WAVE0-FINDINGS.md (A4) — OBEY it.
UniProt stream (D-01): rest.uniprot.org/uniprotkb/stream?query=(accession:A OR accession:B ...)&fields=accession,cc_subcellular_location,go_c,reviewed,gene_primary&format=tsv. Header is display names (Entry, NOT accession) — skip lines[0], split '\t', index parts[0..4]; reviewed literal 'reviewed'/'unreviewed'; ~100 accessions/chunk.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verbatim classifier + 11-color palette + positional TSV merge (pure)</name>
  <files>src/analysis/subcellular-location.ts, src/tests/subcellular-location.ts</files>
  <read_first>
- 13-RESEARCH.md "CK-omics Algorithm Port: Subcellular Classification" (verbatim keyword map insertion order, exact hex palette table, parse_subcellular_location algorithm, "Critical semantics to preserve") + "Verified UniProt stream response shape"
- ~/Downloads/ck/DMD_vs_WT/volcano_plots/Subcellular_Location_Classification_README.txt (LOCKED contract — port verbatim)
- ~/Downloads/ck/CKomics_tool2.py 1384-1436 parse_subcellular_location, 1608 get_location_colors
- 13-PATTERNS.md "src/analysis/subcellular-location.ts" Pattern D; "Pitfall 1" (positional TSV), "Pitfall 2" (no pre-strip)
- src/tests/spectronaut-candidates-parser.ts 1-30 (test-file shape); src/tests/subcellular-location.ts (13-01 scaffold to fill in)
  </read_first>
  <behavior>
- parseSubcellularLocation(subcell,go): subcell scanned fully before go; earliest char-position keyword match across ALL categories wins; ties broken by map insertion order; '\b'-bounded case-insensitive scan over the LOWERCASED RAW string (no pre-strip of SUBCELLULAR LOCATION:/{ECO:...}/Note=); empty/no-match → 'Unknown'
- LOCATION_COLORS ARGB = 0xFF000000 | parseInt(hex.slice(1),16) for all 12 entries (11 + Unknown) using the exact locked hex
- fixture TSV (header + data lines) parses positionally parts[0..4], header skipped, reviewed-priority merge (reviewed&&!Unknown overwrites; Unknown overwritten by non-Unknown)
  </behavior>
  <action>Create src/analysis/subcellular-location.ts. Port the 11-category keyword map in exact insertion order (Nucleus, Cytoplasm, Mitochondria, ER, Golgi, Plasma Membrane, Lysosome, Peroxisome, Ribosome, Extracellular, Vesicles) and the exact hex palette from the locked README/RESEARCH; export LOCATION_COLORS as ARGB ints. Implement parseSubcellularLocation(subcell,go) byte-faithfully (defensive re.escape; new RegExp('\\b'+esc+'\\b','i'); min match.index across all categories; subcell fully before go; 'Unknown' default). Implement pure mergeStreamTsv(text) → {locByAcc:Map, geneByAcc:Map}: skip line 0, split '\t', index parts[0..4], call parseSubcellularLocation(parts[1],parts[2]), priority-merge (reviewed&&loc!=Unknown overwrites; current Unknown overwritten by non-Unknown). No I/O in these. Fill src/tests/subcellular-location.ts: subcell-first vs GO-fallback, earliest-position tie-break, Unknown default, full ARGB-equals-hex assertion for all 12 entries, positional-merge fixture test (header skipped, reviewed beats unreviewed).</action>
  <verify><automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --category SubcellularLocation</automated></verify>
  <acceptance_criteria>
- classifier returns CK-omics categories for subcell-first/GO-fallback/tie-break/Unknown (asserted)
- all 12 LOCATION_COLORS ARGB values exactly equal the locked hex conversion (asserted per-entry)
- positional-merge test passes with header skipped and reviewed-priority; no I/O in pure functions
- grok test --category SubcellularLocation green (rebuilt, not --skip-build)
  </acceptance_criteria>
  <done>Verbatim classifier + palette + pure positional merge land, fully unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Batched stream fetch + D-03 reviewed-by-gene fallback + cross-session cache; refactor panel</name>
  <files>src/analysis/subcellular-location.ts, src/panels/uniprot-panel.ts</files>
  <read_first>
- src/panels/uniprot-panel.ts (entire file — parseAccession EXPORTED stays; fetchUniProtData ~77-91 fetchProxy/!resp.ok/try-catch discipline; render path 120-206 unchanged)
- 13-RESEARCH.md "Fetch + priority-merge + reviewed-by-gene fallback", "Persistent cache via platform KV", "Pitfall 7" (cache size/string), Assumption A1 (taxonomy optional)
- 13-WAVE0-FINDINGS.md (A4 — the chosen cache mechanism + __v invalidation key; OBEY)
- 13-PATTERNS.md "src/analysis/subcellular-location.ts" Patterns A/B/C + "src/panels/uniprot-panel.ts"; CLAUDE.md client-side-fallback + fetchProxy rules
- js-api/src/dapi.ts:735-760 (userDataStorage); src/analysis/enrichment.ts 55-69 (fetchWithTimeout precedent if a timeout race is wanted)
  </read_first>
  <action>Add getSubcellularLocations(accessions: string[], organism?: string) to the module: cache-lookup first (use the 13-WAVE0-FINDINGS.md mechanism — userDataStorage with a __v schema-version key, or the documented IndexedDB/prefix-shard fallback if A4 said so); for misses chunk ~100 accessions, build the OR-query via accession:X joined by ' OR ', add the taxonomy_id filter only when an organism is known (A1 — accessions are globally unique so it is optional), grok.dapi.fetchProxy the stream URL with the exact fields list, on !resp.ok warn+continue (mirror fetchUniProtData), parse via mergeStreamTsv. D-03 second pass: for every acc whose final loc=='Unknown' AND has a gene name, group by gene, batch ~20 genes/query with (gene_exact:G ...) AND reviewed:true (+ taxonomy when known), parse, and assign the reviewed loc back to all unreviewed accs of that gene. Write-through cache the merged map. Never throw out of the fetch loop (catch per-batch, continue — V5 defensive parse). Refactor uniprot-panel.ts fetchUniProtData to delegate to the shared module (keep parseAccession exported here; render path unchanged); the folded cache-uniprot todo is closed by this delegation.</action>
  <verify><automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --category SubcellularLocation &amp;&amp; grep -q "subcellular-location" src/panels/uniprot-panel.ts &amp;&amp; echo OK</automated></verify>
  <acceptance_criteria>
- getSubcellularLocations chunks (~100), uses grok.dapi.fetchProxy only (no raw fetch — grep shows zero `fetch(` outside dapi), warn+continue on !resp.ok
- D-03 fallback issues a second batched gene_exact+reviewed:true query for Unknown+gene accessions and back-assigns
- cache uses the 13-WAVE0-FINDINGS.md mechanism with a __v key (write-through)
- uniprot-panel.ts imports/delegates to the shared module; parseAccession still exported; render path unchanged; build green; SubcellularLocation tests still green
  </acceptance_criteria>
  <done>Batched cached location fetch with D-03 fallback; panel refactored; folded cache-uniprot todo closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → UniProt stream (rest.uniprot.org) | outbound GET via fetchProxy; accessions from the parsed DataFrame |
| UniProt TSV response → client | untrusted free-text TSV parsed positionally into a category map |
| client ↔ userDataStorage | per-user platform KV (not attacker-writable) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Tampering/DoS | UniProt header/field shape drift | mitigate | Positional parse with length-guarded parts[]; missing field → '' → 'Unknown'; catch per-batch, never throw out of the loop (V5) |
| T-13-04-02 | (platform policy) | CORS bypass via raw fetch | mitigate | Mandatory grok.dapi.fetchProxy; this phase removes the existing raw-fetch defect (CONCERNS.md) |
| T-13-04-03 | Tampering | location-cache poisoning | mitigate | userDataStorage is per-user platform storage; values validated against the fixed 12-name category enum on read; __v schema-version key |
| T-13-04-04 | DoS | oversized UniProt response / huge accession set | mitigate | ~100/chunk OR-query under URL limits; per-batch try/catch continues on failure |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; RESEARCH "Package Legitimacy Audit" = N/A (zero external packages) |
</threat_model>

<verification>
SubcellularLocation category green after each task (rebuilt, not --skip-build); fetchProxy-only (no raw fetch); positional parse + reviewed-priority + D-03 fallback; cache per 13-WAVE0-FINDINGS.md; panel delegates and still renders.
</verification>

<success_criteria>
A shared, cached, fetchProxy-only location service with a verbatim CK-omics classifier/palette and a refactored panel that closes the cache-uniprot todo.
</success_criteria>

<output>
Create `.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-04-SUMMARY.md` when done
</output>
