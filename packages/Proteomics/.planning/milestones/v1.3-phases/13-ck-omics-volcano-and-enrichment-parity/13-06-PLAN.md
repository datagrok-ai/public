---
phase: 13-ck-omics-volcano-and-enrichment-parity
plan: 06
type: execute
wave: 3
depends_on: ["13-01", "13-03", "13-04"]
autonomous: true
requirements: [R1, R6]
files_modified: [src/viewers/volcano.ts, src/package.ts, src/tests/volcano.ts]
must_haves:
  truths:
    - "D-05: one volcano (not two) — a Subcellular Location categorical column with the locked 11-colour map, and the colour switches between significance (up/down/NS) and Subcellular Location via the viewer colour property plus a menu/dialog switch"
    - "D-06: a live Q-value ↔ P-value viewer-property toggle recomputes the Y axis, the up/down/NS classification, and the threshold lines together and keeps them in sync on every toggle; default = adj.p-value"
    - "The volcano can color by significance (up/down/NS) or by Subcellular Location via the locked 11-color map"
    - "The Subcellular Location column carries SEMTYPE.SUBCELLULAR_LOCATION and the exact locked palette"
    - "Toggling significance metric adj.p-value ↔ p-value recomputes the Y axis, up/down/NS class, and threshold lines together — dots and coloring never disagree"
    - "Default metric is adj.p-value and default color is significance"
    - "The p-value toggle is guarded/disabled when the p-value column is absent"
  artifacts:
    - {path: src/viewers/volcano.ts, provides: "parameterized metric + location color + synchronized recompute", contains: "recomputeVolcano"}
    - {path: src/package.ts, provides: "volcano color/metric toggle wiring", contains: "Volcano"}
    - {path: src/tests/volcano.ts, provides: "in-place recompute + palette + guard tests", contains: "Volcano"}
  key_links:
    - {from: src/viewers/volcano.ts, to: src/analysis/subcellular-location.ts, via: "LOCATION_COLORS + getSubcellularLocations", pattern: "subcellular-location"}
    - {from: src/package.ts, to: "recomputeVolcano", via: "Volcano Options dialog / context-menu", pattern: "recomputeVolcano"}
---

<objective>
R1/D-05 (volcano Subcellular Location color dimension with the locked 11-color map) and R6/D-06 (live adj.p-value ↔ p-value metric toggle). One volcano: a single synchronized recomputeVolcano keeps Y axis + up/down/NS classification + threshold lines + color in sync on every toggle so dots and coloring never disagree. Defaults match the client CK-omics figure (adj.p-value, significance).
Purpose: The Datagrok volcano matches the client's CK-omics static figure out of the box and supports the location/Q-P parity views.
Output: parameterized volcano helpers, color/metric toggle wiring, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-CONTEXT.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-RESEARCH.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-PATTERNS.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-VALIDATION.md
<interfaces>
volcano.ts: ensureNegLog10Column(df) — fixed name 'negLog10padj', EARLY-RETURNS if exists (Pitfall 5 — must change to always re-init in place under a stable generic name), reads df.col('adj.p-value'). ensureDirectionColumn(df,fc,p) — updates in place (col = df.col(name) ?? addNewString; col.init; meta.colors.setCategorical({'up':0xFFCC0000,'down':0xFF0000CC,'not significant':0xFFAAAAAA})) — preserve the in-place discipline. createVolcanoPlot(df,opts) — df.plot.scatter({x:'log2FC',y,color}); df.meta.formulaLines stale-filter-by-prefix then addLine for hLine/±fcThreshold; sp.props.showViewerFormulaLines.
subcellular-location.ts (from 13-04): exported LOCATION_COLORS (ARGB ints, 12 entries) + getSubcellularLocations(accessions,organism?). SEMTYPE.SUBCELLULAR_LOCATION (from 13-01). parseAccession from panels/uniprot-panel.ts; findColumn(df, SEMTYPE.*, hints).
package.ts showVolcanoPlot ~236-247: requireDifferentialExpression → createVolcanoPlot(df,{title}) → tv.addViewer(sp). Menu paths opening a dialog end with '...'.
RESEARCH recommends approach (A): factory stays a ScatterPlotViewer; a 'Proteomics | Visualize | Volcano Options…' dialog (ui.input.choice metric + color-by) or sp.onContextMenu item calls one recomputeVolcano(df,sp,metric,colorDim,fcThr,pThr).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterize metric, add location color, single synchronized recompute</name>
  <files>src/viewers/volcano.ts, src/tests/volcano.ts</files>
  <read_first>
- src/viewers/volcano.ts (entire file — ensureNegLog10Column early-return at 13-16, ensureDirectionColumn in-place 48-62, formulaLines stale-cleanup 94-118)
- 13-RESEARCH.md "Volcano Q/P Live Toggle (D-06) and Color Dimension (D-05)" (the recomputeVolcano signature + fixed-name-in-place rule), "Categorical color map on the location column", "Pitfall 5"
- 13-PATTERNS.md "src/viewers/volcano.ts"; src/analysis/subcellular-location.ts (LOCATION_COLORS, getSubcellularLocations — from 13-04)
- src/tests/volcano.ts (13-01 scaffold to fill in); src/tests/spectronaut-candidates-parser.ts 1-30 (test-file shape)
  </read_first>
  <behavior>
- ensureNegLog10Column(df, metric) takes metric ∈ {'adj.p-value','p-value'}, uses a FIXED generic column name, ALWAYS re-inits values in place (no early-return) so the Y binding name is stable but values change on toggle
- ensureDirectionColumn(df, fcThr, pThr, metric) reads the chosen p-column, updates the existing column in place (no remove+re-add), keeps the existing up/down/NS ARGB map
- ensureLocationColumn(df) builds a 'Subcellular Location' string column from getSubcellularLocations via the bulk init pattern, sets meta.colors.setCategorical(LOCATION_COLORS) and col.semType = SEMTYPE.SUBCELLULAR_LOCATION
- recomputeVolcano(df,sp,metric,colorDim,fcThr,pThr) recomputes Y + direction + threshold lines and sets sp.props.color to the direction column (colorDim 'significance') or the location column ('location'); after a Q→P toggle the Y values, direction classes and threshold line all move together (no stale disagreement)
- requesting metric 'p-value' when df.col('p-value')==null is guarded (no throw; stays on adj.p-value)
  </behavior>
  <action>Refactor volcano.ts: parameterize ensureNegLog10Column and ensureDirectionColumn by metric (stable generic column name, always re-init in place — drop the early-return per Pitfall 5; mirror the existing ensureDirectionColumn in-place idiom; never remove+re-add). Add ensureLocationColumn(df) using getSubcellularLocations (feed parseAccession primary accessions) + the bulk Column.init string pattern + meta.colors.setCategorical(LOCATION_COLORS) + col.semType=SEMTYPE.SUBCELLULAR_LOCATION (memory feedback_dg_column_bulk_init). Add recomputeVolcano(df,sp,metric,colorDim,fcThr,pThr) that re-runs the parameterized helpers, re-runs the existing formulaLines stale-filter-then-addLine block (lines replace, not stack), and sets sp.props.color. Guard 'p-value' when the column is absent. createVolcanoPlot stays a ScatterPlotViewer factory with defaults metric 'adj.p-value', color 'significance'. Fill src/tests/volcano.ts: toggling metric re-inits the neg-log column values under the same name (binding stable), reclassifies up/down/NS, and moves the threshold line consistently; LOCATION_COLORS applied + semType set on the location column; 'p-value'-absent path does not throw and stays on adj.p-value.</action>
  <verify><automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --category Volcano</automated></verify>
  <acceptance_criteria>
- ensureNegLog10Column no longer early-returns; same column name, values change on metric toggle (asserted)
- ensureDirectionColumn parameterized, still in-place (no remove+re-add); ARGB map unchanged
- location column carries SEMTYPE.SUBCELLULAR_LOCATION + exact LOCATION_COLORS (asserted per the 13-04 palette)
- recomputeVolcano keeps Y + class + threshold line consistent across a Q↔P toggle (asserted); p-value-absent guarded
- grok test --category Volcano green (rebuilt, not --skip-build)
  </acceptance_criteria>
  <done>One volcano with a synchronized metric/color recompute; dots and coloring never disagree.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the color/metric toggle into the volcano menu</name>
  <files>src/package.ts, src/tests/volcano.ts</files>
  <read_first>
- src/package.ts showVolcanoPlot ~236-247 (requireDifferentialExpression → createVolcanoPlot → tv.addViewer; the auto-open path ~221-235 too); 13-PATTERNS.md "src/package.ts"
- 13-RESEARCH.md "Volcano Q/P Live Toggle (D-06)" "Toggle wiring (Discretion)" (Volcano Options… dialog or sp.onContextMenu calling recomputeVolcano; defaults adj.p-value / significance)
- 13-CONTEXT.md D-05/D-06 + "Claude's Discretion" (wording/placement of the switch is discretionary; behavior is locked)
- .claude/skills/ui/SKILL.md (worktree root or package) — REQUIRED before adding the dialog/menu/context-menu/viewer-property work
  </read_first>
  <action>Add the toggle entry point per RESEARCH approach (A): a 'Proteomics | Visualize | Volcano Options…' menu item (path ends with '...' per naming convention) with two ui.input.choice inputs (significance metric: adj.p-value default, p-value; color-by: significance default, location) and/or an sp.onContextMenu item, that calls recomputeVolcano on the current volcano + DataFrame. Disable/omit the p-value choice when df.col('p-value')==null. Do not change createVolcanoPlot's defaults. Keep showVolcanoPlot's existing requireDifferentialExpression gate and title logic. Add a Volcano test asserting the toggle wiring calls recomputeVolcano and the resulting sp.props.color/Y reflect the selected metric+colorDim (rebuild before running — feedback_grok_test_skipbuild_stale).</action>
  <verify><automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &amp;&amp; grok test --category Volcano</automated></verify>
  <acceptance_criteria>
- a 'Volcano Options…' menu item (or context-menu item) drives recomputeVolcano with metric+colorDim; default adj.p-value/significance preserved
- p-value choice guarded when the column is absent
- showVolcanoPlot DE-gate + title unchanged; grok test --category Volcano green (rebuilt)
  </acceptance_criteria>
  <done>The analyst can switch metric and color dimension live; defaults match the client figure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| in-memory DataFrame → viewer | no external input crosses here; UniProt fetch is owned by 13-04's module |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-01 | Tampering | location categories from the shared module | accept | Values are the fixed 12-name enum from the 13-04 verbatim classifier; setCategorical keyed to the locked palette |
| T-13-06-02 | DoS | p-value column absent on toggle | mitigate | Guard df.col('p-value')==null — disable the choice, no throw, stay on adj.p-value |
| T-13-06-03 | Tampering | stale threshold lines accumulating on repeated toggles | mitigate | Reuse the existing formulaLines stale-filter-by-prefix cleanup so lines replace, not stack |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; RESEARCH "Package Legitimacy Audit" = N/A (zero external packages) |
</threat_model>

<verification>
grok test --category Volcano green after each task (rebuilt, not --skip-build); metric toggle re-inits values under a stable name; location palette+semType exact; threshold/Y/class consistent across toggles; p-value-absent guarded. Manual: visual parity vs ~/Downloads/ck/DMD_vs_WT/volcano_plots/ static HTML (13-VALIDATION.md Manual-Only).
</verification>

<success_criteria>
One volcano matching the client CK-omics figure by default, with live Subcellular-Location coloring and a synchronized adj.p ↔ p metric toggle.
</success_criteria>

<output>
Create `.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-06-SUMMARY.md` when done
</output>
