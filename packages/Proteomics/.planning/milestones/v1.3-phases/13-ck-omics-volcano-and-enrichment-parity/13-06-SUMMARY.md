---
phase: 13-ck-omics-volcano-and-enrichment-parity
plan: 06
subsystem: ui
tags: [volcano, scatterplot, subcellular-location, metric-toggle, tdd]

requires:
  - phase: 13-ck-omics-volcano-and-enrichment-parity
    provides: SEMTYPE.SUBCELLULAR_LOCATION (13-01), getSubcellularLocations + LOCATION_COLORS (13-04), sign-correct Candidates (13-03)
provides:
  - parameterized ensureNegLog10Column / ensureDirectionColumn (metric-driven, in-place)
  - ensureLocationColumn (locked-palette Subcellular Location column)
  - recomputeVolcano (single synchronized Y + class + threshold + color recompute)
  - 'Proteomics | Visualize | Volcano Options...' metric/color toggle
affects: []

tech-stack:
  added: []
  patterns:
    - "Stable generic bound column name + always-re-init-in-place (no early-return, no remove+re-add)"
    - "One synchronized recompute keeps Y/classification/threshold/color from disagreeing"

key-files:
  created: []
  modified:
    - src/viewers/volcano.ts
    - src/package.ts
    - src/package.g.ts
    - src/package-api.ts
    - src/tests/volcano.ts

key-decisions:
  - "Generic stable column name 'negLog10P' so the Y binding survives a metric toggle; values re-init in place"
  - "pickMetricColumn falls back to adj.p-value when p-value absent (D-06 guard, no throw)"
  - "createVolcanoPlot stays a sync ScatterPlotViewer factory; recomputeVolcano (async) owns the location fetch"
  - "Defaults adj.p-value + significance match the client CK-omics static figure"

patterns-established:
  - "Metric/color toggle is a viewer-property recompute, not a viewer rebuild"

requirements-completed: [R1, R6]

duration: 161min
completed: 2026-05-18
---

# Phase 13 Plan 06: Volcano Subcellular Color + Q/P Metric Toggle Summary

**One volcano: metric-parameterized in-place Y/direction helpers, a locked-palette Subcellular Location color column from the shared 13-04 service, and recomputeVolcano that moves Y + up/down/NS + threshold lines + color together — wired to a 'Volcano Options...' dialog with adj.p-value/significance defaults.**

## Performance

- **Duration:** ~161 min (inline sequential; verbatim-palette care + 2 builds + Volcano tests, incl. one Float32-precision test fix)
- **Started:** 2026-05-17T22:22Z
- **Completed:** 2026-05-18T01:03Z
- **Tasks:** 2 (Task 1 TDD RED → GREEN)
- **Files modified:** 5 (2 auto-generated by grok api)

## Accomplishments
- `ensureNegLog10Column(df, metric)` / `ensureDirectionColumn(df, fc, p, metric)` — fixed generic names (`negLog10P`, `direction`), always re-init in place (early-return dropped per Pitfall 5; never remove+re-add). `pickMetricColumn` resolves the metric and guards p-value-absent → adj.p-value (no throw).
- `ensureLocationColumn(df)` — parseAccession → `getSubcellularLocations` (13-04) → bulk-init `Subcellular Location` string column, `setCategorical(LOCATION_COLORS)`, `semType = SUBCELLULAR_LOCATION`.
- `recomputeVolcano(df, sp, metric, colorDim, fc, p)` — re-runs Y + direction + threshold lines (replace, not stack) and repoints `colorColumnName`; a Q↔P toggle moves all of them together.
- `Proteomics | Visualize | Volcano Options...` dialog (metric + color-by choices; p-value omitted when the column is absent) drives recomputeVolcano on the open volcano scatter; createVolcanoPlot defaults unchanged.
- 7 Volcano tests green on localhost (1 Float32-precision test assertion corrected to tolerance compare), exit 0.

## Task Commits

1. **Task 1 RED: parameterized-volcano tests** - `4d029d6777` (test)
2. **Task 1 GREEN: parameterized helpers + recomputeVolcano** - `dba16af251` (feat)
3. **Task 2: Volcano Options menu wiring** - `53821b8703` (feat)

**Plan metadata:** this commit (docs: complete plan)

## Files Created/Modified
- `src/viewers/volcano.ts` - metric-parameterized helpers, ensureLocationColumn, recomputeVolcano, applyThresholdLines extraction
- `src/package.ts` - volcanoOptions menu handler
- `src/package.g.ts`, `src/package-api.ts` - regenerated by `grok api` (not hand-edited)
- `src/tests/volcano.ts` - 7 tests (replaced the 13-01 placeholder scaffold)

## Decisions Made
See key-decisions frontmatter.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Correctness] Float32 precision in a test assertion**
- **Found during:** Task 1 (recomputeVolcano consistency test)
- **Issue:** Asserted `col.get(1) === -Math.log10(0.5)` exactly; the column is Float32 so the stored value differs from the JS double in the 8th decimal.
- **Fix:** Tolerance compare (`< 1e-4`), consistent with the rest of the suite.
- **Files modified:** src/tests/volcano.ts
- **Verification:** Volcano category 7/7 green after rebuild
- **Committed in:** 53821b8703

---

**Total deviations:** 1 auto-fixed (1 correctness — test precision)
**Impact on plan:** Test-only; production volcano.ts behavior unchanged. No scope change.

## Issues Encountered
- First Volcano run failed one assertion on Float32 vs double precision; fixed the test (above) and rebuilt before re-running (feedback_grok_test_skipbuild_stale).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 13 implementation complete (6/6 plans). Ready for phase verification.
- **HUMAN-UAT (visual parity):** the volcano's visual match to `~/Downloads/ck/DMD_vs_WT/volcano_plots/` static HTML, real UniProt-backed Subcellular Location coloring, and the live Q↔P toggle against client data are manual-only checks (13-VALIDATION.md Manual-Only) — unit tests cover the column/recompute logic and palette/semType, not the rendered figure.

---
*Phase: 13-ck-omics-volcano-and-enrichment-parity*
*Completed: 2026-05-18*
