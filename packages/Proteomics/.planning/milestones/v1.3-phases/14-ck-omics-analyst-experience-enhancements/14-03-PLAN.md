---
phase: 14-ck-omics-analyst-experience-enhancements
plan: 03
type: execute
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - src/package.ts
  - src/tests/spectronaut-candidates-parser.ts
autonomous: true
requirements: [G4, R2]
user_setup: []

must_haves:
  truths:
    - "D-02: MUST-have for BP DMD/WT client deliverable — Plan 14-03 ships G4 + the D-05 unified-search seam that completes the BP-trio rollup (G1 + R1 + R2 already live from 14-01/14-02)"
    - "When a multi-contrast Spectronaut Candidates file is imported, the docked Filters viewer contains exactly the columns Comparison + Display Name + Source ID (or Protein ID as fallback) — never the `Flags` boolean column (G4)"
    - "Typing into the Display Name or Source ID search box highlights matching rows via df.selection — NOT df.filter — so non-matched proteins remain visible as the NS cloud (D-05)"
    - "Single-contrast Candidates files dock no Filters viewer (existing Phase 13 behavior preserved)"
    - "The docked Filters viewer uses the typed `filters` array (not `columnNames` alone) so the boolean Flags auto-combine cannot reintroduce excluded columns"
    - "When the search match changes df.selection, the volcano's top-N label seed (Plan 02 setTopNLabels) unions instead of clobbers"
  artifacts:
    - path: "src/package.ts"
      provides: "dockComparisonFilterIfMultiContrast extended with Display Name + Source ID filter specs, showBoolCombinedFilter: false, search-match → selection wiring (defensive capture-restore path)"
      contains: "showBoolCombinedFilter"
    - path: "src/tests/spectronaut-candidates-parser.ts"
      provides: "Filter-viewer scoping test asserting Flags absence and Display Name / Source ID presence"
      contains: "filtersScopingNoFlags"
  key_links:
    - from: "src/package.ts:dockComparisonFilterIfMultiContrast"
      to: "DG.Viewer.filters typed filters array"
      via: "{type: 'categorical' | 'free-text', column: name}[]"
      pattern: "showBoolCombinedFilter: false"
    - from: "src/package.ts:dockComparisonFilterIfMultiContrast"
      to: "df.selection.set(idx, true)"
      via: "defensive capture-restore: save df.filter state, write df.selection from match indices, restore df.filter via copyFrom(savedBitSet)"
      pattern: "df\\.selection\\.set"
    - from: "src/package.ts:dockComparisonFilterIfMultiContrast"
      to: "src/viewers/volcano.ts:setTopNLabels (mode='union')"
      via: "after search match updates selection, re-apply top-N in union mode"
      pattern: "setTopNLabels.*union"
---

<objective>
G4 — Filters viewer column scoping: replace the `columnNames` allowlist (which Phase 13 observed silently extending itself with the boolean `Flags` column) with the typed `filters` array (Pattern 1, root-cause fix). D-05 — Unified protein search: extend the same Filters viewer to include `Display Name` and `Source ID` free-text filters so the analyst can search-by-gene without a custom textarea, with matches highlighted via `df.selection` (NOT `df.filter`) so non-matched proteins stay visible in the NS cloud.

Purpose: Third piece of the MUST-have BP DMD/WT client deliverable trio (labelled here as a client-deliverable MUST-have for grouping purposes; wave assignment is governed mechanically by `wave = max(deps) + 1`, not by deliverable-group narrative). Closes Phase 13 UAT Gap 4 (minor — Flags column auto-inclusion) and delivers the D-05 unified-search-via-Filters-viewer locked decision. Depends on Plan 01 because the new filter columns `Display Name` and `Source ID` are created by Plan 01's gene-label resolver. Depends on Plan 02 because the search-match → selection wiring imports `setTopNLabels` from `src/viewers/volcano.ts` (a Plan 02 export) and calls it in `mode='union'` to prevent top-N labels from clobbering search-match selection (Pitfall 7).

Output: In-place extension of `dockComparisonFilterIfMultiContrast` in `src/package.ts:117-131` (current 15 lines → ~50 lines). New test in `src/tests/spectronaut-candidates-parser.ts` asserting Filter viewer scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-ck-omics-analyst-experience-enhancements/14-CONTEXT.md
@.planning/phases/14-ck-omics-analyst-experience-enhancements/14-RESEARCH.md
@.planning/phases/14-ck-omics-analyst-experience-enhancements/14-PATTERNS.md
@.planning/phases/14-ck-omics-analyst-experience-enhancements/14-UI-SPEC.md
@.planning/phases/14-ck-omics-analyst-experience-enhancements/14-01-SUMMARY.md
@.planning/phases/14-ck-omics-analyst-experience-enhancements/14-02-SUMMARY.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- From Plan 01 (already shipped): Display Name + Source ID columns on every parsed DataFrame. -->
<!-- From Plan 02 Task 2 (already shipped): setTopNLabels(df, sp, n, mode) export from volcano.ts. -->

<!-- From src/package.ts (CURRENT — extend dockComparisonFilterIfMultiContrast at lines 117-131): -->

export function dockComparisonFilterIfMultiContrast(
  tv: DG.TableView, df: DG.DataFrame,
): boolean {
  const cmpCol = df.col('Comparison (group1/group2)') ?? df.col('Comparison');
  if (!cmpCol) return false;
  const distinct = new Set<string>();
  for (let i = 0; i < df.rowCount; i++) {
    const v = cmpCol.get(i);
    if (typeof v === 'string' && v.length > 0) distinct.add(v);
  }
  if (distinct.size <= 1) return false;
  const filters = DG.Viewer.filters(df, {columnNames: [cmpCol.name]});  // PHASE 13: columnNames silently extended to include Flags
  tv.dockManager.dock(filters, DG.DOCK_TYPE.RIGHT, null, 'Comparison', 0.3);
  return true;
}

Called once from src/package.ts:191 (importSpectronautCandidates handler).

<!-- From js-api/src/interfaces/d4.ts (per 14-RESEARCH.md verification): -->

interface IFiltersSettings {
  columnNames?: string[];                        // allowlist (Phase 13 observed silently extending)
  filters?: Array<{[k: string]: any}>;           // typed per-column filter spec — ROOT-CAUSE FIX
  showBoolCombinedFilter?: boolean;              // defaults to true — auto-includes Flags; set to false
  showHeader?: boolean;
  showSearchBox?: boolean;
}

Filter type strings (verified from datagrok.ai/help/visualize/viewers/filters per 14-RESEARCH.md Sources):
- 'categorical' — for the Comparison column
- 'free-text' — for the Display Name and Source ID search-box behavior
- 'histogram', 'text', 'expression', 'hierarchical', 'combined-boolean' — other types (not used here)

<!-- LOCKED API contract (Pattern 1 root-cause fix): -->

const filters = DG.Viewer.filters(df, {
  filters: [
    {type: 'categorical', column: cmpCol.name},
    {type: 'free-text',   column: displayNameCol.name},
    {type: 'free-text',   column: sourceIdCol.name},
  ],
  showBoolCombinedFilter: false,                 // explicit Flags exclusion
  showHeader: true,
  showSearchBox: true,
});

<!-- Verification gate (RESEARCH §"Pattern 1"): -->

After dock, read viewer.getOptions().look.filters and assert Flags is absent.
If platform still auto-includes Flags, fall back to a post-create viewer.setOptions({filters, columnNames}) with both fields explicitly set.

<!-- Search match → selection wiring (D-05, UI-SPEC §"Interaction Contracts > Volcano: protein search"): -->

The Datagrok Filters viewer's free-text filter on a string column drives df.filter (hides non-matching rows by default). D-05 requires highlight-not-hide — so the wiring uses the DEFENSIVE CAPTURE-RESTORE path (committed at plan-write time, per 14-RESEARCH.md Open Q 1 fallback recommendation):
1. Capture df.filter into a saved BitSet before subscribing.
2. On df.onFilterChanged: derive the matched indices from the current df.filter.
3. Restore df.filter via df.filter.copyFrom(savedBitSet).
4. Write df.selection.set(idx, true) on the matched indices.
5. Call setTopNLabels(df, sp, n, 'union') so top-N labels seed AND search match coexist.

This path is correct regardless of platform behavior. A potential simplification via direct event handling is captured as a `<follow_up>` note below — NOT a required task.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate to typed filters array + add Display Name / Source ID free-text filters + verify Flags exclusion</name>
  <files>src/package.ts</files>
  <read_first>
    - src/package.ts:1-50 (imports — confirm findColumn / SEMTYPE imports already present from Plan 01 or add them)
    - src/package.ts:117-131 (current dockComparisonFilterIfMultiContrast — full 15 lines)
    - src/package.ts:180-200 (importSpectronautCandidates handler — the call site at line 191)
    - 14-RESEARCH.md §"Pattern 1: Filters viewer with typed per-column filters" (full body — exact API call shape)
    - 14-RESEARCH.md §"Pitfall 1: Filters viewer columnNames is not a strict allowlist"
    - 14-UI-SPEC.md §"Interaction Contracts > Volcano: protein search via unified Filters viewer (D-05, G4)"
    - 14-PATTERNS.md §"4. src/package.ts > D-05 + G4"
  </read_first>
  <action>
    Extend `dockComparisonFilterIfMultiContrast` in `src/package.ts:117-131`.

    Add to the imports at the top of src/package.ts (if not already present):
    - `import {findColumn} from './utils/column-detection';`
    - `import {SEMTYPE} from './utils/proteomics-types';`

    Inside the function, after the existing distinct-count guard (`if (distinct.size <= 1) return false;`):

    1. Look up Display Name and Source ID columns via SEMTYPE-first findColumn (NEVER raw df.col per CLAUDE.md):
       ```
       const displayNameCol = findColumn(df, SEMTYPE.DISPLAY_NAME, ['display name']);
       const sourceIdCol = findColumn(df, SEMTYPE.SOURCE_ID, ['source id']);
       const proteinIdCol = findColumn(df, SEMTYPE.PROTEIN_ID, ['primary protein id', 'protein id', 'uniprot', 'accession']);
       ```

    2. Build the typed `filters` spec array:
       ```
       const filterSpecs: Array<{type: string; column: string}> = [
         {type: 'categorical', column: cmpCol.name},
       ];
       if (displayNameCol) filterSpecs.push({type: 'free-text', column: displayNameCol.name});
       if (sourceIdCol) filterSpecs.push({type: 'free-text', column: sourceIdCol.name});
       // Fallback: if Display Name is missing (old DataFrame predating Plan 01), include Protein ID instead
       if (!displayNameCol && proteinIdCol)
         filterSpecs.push({type: 'free-text', column: proteinIdCol.name});
       ```

    3. Replace the current `DG.Viewer.filters(df, {columnNames: [cmpCol.name]})` call with:
       ```
       const filters = DG.Viewer.filters(df, {
         filters: filterSpecs,
         showBoolCombinedFilter: false,  // explicit: do not auto-include boolean Flags column (G4 root-cause fix)
         showHeader: true,
         showSearchBox: true,
       });
       ```

    4. Verification gate (per RESEARCH §"Pattern 1"): immediately after creation, read `(filters as any).getOptions?.()?.look?.filters` and check whether the platform expanded the filter spec. If a `Flags` column appears anywhere in the look options, fall back to a post-create override:
       ```
       const fOpts = (filters as any).getOptions?.();
       const hasFlags = fOpts?.look?.filters?.some((f: any) => f.column === 'Flags') ||
                        fOpts?.look?.columnNames?.includes('Flags');
       if (hasFlags) {
         (filters as any).setOptions?.({
           filters: filterSpecs,
           columnNames: filterSpecs.map((f) => f.column),
           showBoolCombinedFilter: false,
         });
       }
       ```
       Document the fallback path in a code comment referencing 14-RESEARCH.md Pitfall 1.

    5. Update the dock label from `'Comparison'` to `'Filters'` (the viewer now holds 3+ filter columns, not just Comparison):
       ```
       tv.dockManager.dock(filters, DG.DOCK_TYPE.RIGHT, null, 'Filters', 0.3);
       ```

    6. Return `true` as before.

    Do NOT touch the early-return guards (cmpCol null / distinct.size <= 1) — they preserve Phase 13's behavior of skipping the dock for single-contrast files.
  </action>
  <verify>
    <automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &gt; /tmp/14-03-task1-build.log 2&gt;&amp;1 &amp;&amp; grep -c "showBoolCombinedFilter: false" src/package.ts &amp;&amp; grep -c "filters: filterSpecs" src/package.ts</automated>
  </verify>
  <done>
    dockComparisonFilterIfMultiContrast uses the typed filters array with explicit showBoolCombinedFilter:false. Display Name and Source ID free-text filters are added when their SEMTYPE columns exist. Verification gate handles Flags-leak fallback. Build is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire search-match → df.selection via defensive capture-restore + setTopNLabels union mode</name>
  <files>src/package.ts</files>
  <read_first>
    - src/package.ts:117-end-of-function (the function after Task 1's extensions)
    - src/viewers/volcano.ts (Plan 02 Task 2's setTopNLabels export with mode='union' parameter)
    - 14-UI-SPEC.md §"Interaction Contracts > Volcano: protein search via unified Filters viewer" — D-05 search-match contract
    - 14-RESEARCH.md §"Pitfall 7: D-03 top-N labels collide with D-05 search-match selection"
    - 14-RESEARCH.md §"Open Questions (RESOLVED) > 1. Does the Filters viewer's free-text search-box mutate df.filter" — RESOLVED: defensive capture-restore path is the committed implementation
    - 14-RESEARCH.md §"Assumption A5" — df.selection.set does NOT trigger df.filter.onChanged
    - src/viewers/enrichment-viewers.ts (activeSubscriptions cleanup pattern — Pattern S-4)
  </read_first>
  <behavior>
    - After the unified Filters viewer is docked, if the analyst types into the Display Name free-text box, the matched rows are selected (df.selection.set(idx, true)) while df.filter ends up restored to its pre-search state (no rows hidden). The volcano renders matched points in selection color in place; the NS cloud stays visible. (D-05 highlight-not-hide.)
    - Implementation uses the defensive capture-restore path (committed at plan-write time per 14-RESEARCH.md Open Q 1 recommendation):
      a) Before subscribing, save the current `df.filter` into `savedBitSet` (a fresh BitSet).
      b) On `df.onFilterChanged` (debounced 100ms): capture matched indices = rows where `df.filter.get(i) === true`.
      c) Restore `df.filter.copyFrom(savedBitSet)` so the pre-search filter state is back in place.
      d) Write `df.selection.set(idx, true, false)` for every matched index, then `df.selection.fireChanged()`.
      e) Call `setTopNLabels(df, sp, 15, 'union')` so the top-N label seed coexists with the search match (Pitfall 7).
    - Subscriptions are added to a module-level `activeFilterSubscriptions` array and disposed on re-entry (when dockComparisonFilterIfMultiContrast is called again, e.g. on a second multi-contrast import).
    - If df.selection.set ever triggers df.filter.onChanged (would cause infinite loop — per Assumption A5 it does not), guard with a `restoring: boolean` flag that suppresses re-entry into the subscription body during the restore step.
  </behavior>
  <action>
    Add a module-level subscription store at the top of src/package.ts (mirroring src/viewers/enrichment-viewers.ts:9):
    ```
    let activeFilterSubscriptions: rxjs.Subscription[] = [];
    ```

    Inside `dockComparisonFilterIfMultiContrast`, after the filters viewer is docked:

    1. Dispose previous subscriptions: `for (const s of activeFilterSubscriptions) s.unsubscribe(); activeFilterSubscriptions = [];`.

    2. Look up the volcano viewer if one exists on the table view (it may not — the Filters viewer can be docked before the volcano is created):
       ```
       const findVolcano = (): DG.ScatterPlotViewer | null => {
         for (const v of tv.viewers) {
           if (v.type === DG.VIEWER.SCATTER_PLOT &&
               (v as DG.ScatterPlotViewer).props.yColumnName === 'negLog10P')
             return v as DG.ScatterPlotViewer;
         }
         return null;
       };
       ```
       The volcano may appear later; re-look-up on every subscription fire.

    3. Capture the pre-search filter state via BitSet clone (DG BitSet exposes a copy/clone helper; executor uses `DG.BitSet.create(df.rowCount).copyFrom(df.filter)` or the analogous current-API call):
       ```
       const savedBitSet = DG.BitSet.create(df.rowCount);
       savedBitSet.copyFrom(df.filter);
       let restoring = false;
       ```

    4. Subscribe to df.onFilterChanged with debounceTime(100):
       ```
       import {debounceTime} from 'rxjs/operators';
       activeFilterSubscriptions.push(df.onFilterChanged.pipe(debounceTime(100)).subscribe(() => {
         if (restoring) return;  // guard against the restore call re-triggering this handler

         // Capture matched indices from the current (search-mutated) df.filter
         const matched: number[] = [];
         for (let i = 0; i < df.rowCount; i++) {
           if (df.filter.get(i)) matched.push(i);
         }

         // Restore df.filter to its pre-search state
         restoring = true;
         try {
           df.filter.copyFrom(savedBitSet);
         } finally {
           restoring = false;
         }

         // Write df.selection from the match (per Assumption A5, does NOT re-trigger onFilterChanged)
         df.selection.setAll(false, false);
         for (const idx of matched) df.selection.set(idx, true, false);
         df.selection.fireChanged();

         // Reapply top-N in union mode so its seed coexists with the search match
         const sp = findVolcano();
         if (sp) setTopNLabels(df, sp, 15, 'union');
       }));
       ```
       Add `import {setTopNLabels} from './viewers/volcano';` at the top of src/package.ts.

    5. Inline code comments must reference:
       - 14-RESEARCH.md §"Pitfall 7" (top-N labels collide with search-match selection — union mode handle)
       - 14-RESEARCH.md §"Open Questions (RESOLVED) > 1" (defensive capture-restore is the committed path)
       - 14-RESEARCH.md §"Assumption A5" (df.selection.set does NOT trigger df.filter.onChanged — relied on to avoid the loop)

    Per UI-SPEC §"Interaction Contracts > Volcano: protein search > Reset affordance": the platform Filters viewer's clear-search button restores df.filter via its own mechanism (the capture-restore handler will run once more on the clear-driven onFilterChanged fire, restore savedBitSet, and end up with df.selection cleared because no rows match). No additional reset wiring needed.
  </action>
  <verify>
    <automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &gt; /tmp/14-03-task2-build.log 2&gt;&amp;1 &amp;&amp; grep -c "df\.filter\.copyFrom\|df\.selection\.set\|df\.selection\.fireChanged" src/package.ts</automated>
  </verify>
  <done>
    Defensive capture-restore wiring is in place: savedBitSet captured before subscription, df.filter.copyFrom(savedBitSet) restores on each match, df.selection.set written from match indices. setTopNLabels(df, sp, 15, 'union') is called after selection update. Subscription cleanup pattern prevents leaks. Build is clean. Code comments document Pitfall 7 + Open Q 1 (RESOLVED) + Assumption A5.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Filters-viewer scoping test (Flags absence + Display Name presence)</name>
  <files>src/tests/spectronaut-candidates-parser.ts</files>
  <read_first>
    - src/tests/spectronaut-candidates-parser.ts (existing test file — observe the existing category/test/expect framing and any fixture builders)
    - src/parsers/spectronaut-candidates-parser.ts (full file — to understand what shape the parser produces; Flags column is added by the parser per 14-INBOX.md G4)
    - 14-VALIDATION.md §"Per-Task Verification Map" seam: filtersScopingNoFlags
    - 14-UI-SPEC.md §"Interaction Contracts > Volcano: protein search via unified Filters viewer" — exact expected columnNames list
  </read_first>
  <behavior>
    - Test 'filtersScopingNoFlags': construct a fixture DataFrame mimicking a multi-contrast Spectronaut Candidates output — must include: a 'Comparison (group1/group2)' string column with 2+ distinct values, a 'Gene name' string column, a 'Display Name' string column (SEMTYPE.DISPLAY_NAME), a 'Source ID' string column (SEMTYPE.SOURCE_ID), AND a 'Flags' boolean column (the offender). Call dockComparisonFilterIfMultiContrast(tv, df) where tv is a mock TableView (use grok.shell.tv after addTableView, or construct directly per existing test patterns). After dock, read `(filters as any).getOptions?.()?.look?.filters` and assert: (a) entry for column='Comparison (group1/group2)' is present with type='categorical', (b) entry for column='Display Name' is present with type='free-text', (c) entry for column='Source ID' is present with type='free-text', (d) NO entry for column='Flags' anywhere in the look options.
    - Test 'filtersScopingSingleContrast': fixture with only one distinct Comparison value, assert dockComparisonFilterIfMultiContrast returns false and no Filters viewer is docked.
    - Test 'filtersScopingFallbackToProteinIdWhenNoDisplayName': fixture without Display Name column (simulating old DataFrame), assert filter spec includes Protein ID free-text instead.
  </behavior>
  <action>
    Add tests to `src/tests/spectronaut-candidates-parser.ts` (or create a new dedicated test file `src/tests/filter-scoping.ts` if the existing file's category doesn't fit — planner picks based on the existing category names; if Spectronaut-specific, use the existing file).

    For the fixture: use `DG.DataFrame.fromColumns([...])` with explicit `DG.Column.fromStrings('Comparison (group1/group2)', [...])`, `DG.Column.fromStrings('Display Name', [...])`, etc. Set `df.col('Display Name').semType = SEMTYPE.DISPLAY_NAME` and `df.col('Source ID').semType = SEMTYPE.SOURCE_ID` after creating columns.

    For the TableView mock: the cleanest path is `const tv = grok.shell.addTableView(df);` then `dockComparisonFilterIfMultiContrast(tv, df);` then `tv.close()` in a `finally` block to clean up. If `tv.viewers` is iterable, find the docked Filters viewer by `Array.from(tv.viewers).find((v) => v.type === DG.VIEWER.FILTERS)` (verify the exact viewer type constant during implementation).

    Read `viewer.getOptions().look.filters` to verify the spec. Per Pitfall 1, ALSO check `viewer.getOptions().look.columnNames` — if Flags appears in either, the test fails.

    Register the new test file in src/package-test.ts if a new file was created (Task 3 may extend an existing one — only register if new).

    Update 14-VALIDATION.md per-task verification map: replace the placeholder ❌ for the filtersScopingNoFlags seam with the task ID once this plan executes. (This is a documentation update; the executor handles it as part of the SUMMARY write at plan completion.)
  </action>
  <verify>
    <automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &gt; /tmp/14-03-task3-build.log 2&gt;&amp;1 &amp;&amp; grok test --catch-unhandled=false --category "Proteomics: 14-03"</automated>
  </verify>
  <done>
    filtersScopingNoFlags + filtersScopingSingleContrast + filtersScopingFallbackToProteinIdWhenNoDisplayName tests pass. Flags column is verified absent from the docked Filters viewer's look options. Build is clean.
  </done>
</task>

</tasks>

<follow_up>
## Optional smoke-test simplification (NOT a required task — recorded here for future-phase cleanup)

If a future phase has spare context budget, a 30-minute smoke test against a live Datagrok Filters viewer could determine whether the platform exposes a direct "matched-rows" event that does NOT mutate df.filter. If such an event exists, the defensive capture-restore loop in Task 2 could be simplified to a direct subscription:

1. Dock a free-text filter on a 100-row DataFrame.
2. Type a substring into the search box.
3. Inspect whether df.filter.trueCount changes vs whether a separate event fires.

If a direct event path is found, the Task 2 wiring could shrink to: subscribe to that event, write df.selection.set(idx, true) on the match, call setTopNLabels(df, sp, 15, 'union'). The defensive capture-restore (savedBitSet, copyFrom, restoring flag) would not be needed.

This is NOT a Phase 14 task. The defensive path shipped in Task 2 is correct as-is; this is purely a potential simplification for later. Captured per 14-RESEARCH.md §"Open Questions (RESOLVED) > 1" recommendation.
</follow_up>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User search input → df.selection | Free-text search strings typed into the Filters viewer drive selection-set operations. Untrusted text from analyst input. |
| Filters viewer settings → DOM render | The typed filters array controls which columns the platform renders search boxes for. Misconfiguration could expose unintended columns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-T1 | Tampering | Filters viewer auto-expands to include excluded columns (Phase 13 Flags observation) | mitigate | Use typed `filters` array AND `showBoolCombinedFilter: false`; runtime verification gate falls back to post-create setOptions if Flags still appears. |
| T-14-03-I1 | Information Disclosure | Search text logged or persisted | accept | Datagrok platform-owned behavior; no Phase-14 code logs search input; no external transmission. |
| T-14-03-D1 | Denial of Service | Free-text search recompute on every keystroke | mitigate | debounceTime(100) on the onFilterChanged subscription; setTopNLabels union mode is O(n log n) over filtered set; `restoring` flag prevents infinite re-entry from the copyFrom call. |
| T-14-03-R1 | Repudiation | Silent change in highlight-not-hide semantics confuses analyst expecting filter behavior | mitigate | UI-SPEC explicitly documents the highlight-not-hide contract; counter overlay still reflects filter state so the analyst sees the search effect immediately. |
| T-14-03-SC | Tampering | npm/pip/cargo installs in this plan | accept | Zero new packages. |
</threat_model>

<verification>
After all 3 tasks complete:

1. `cd packages/Proteomics && npm run build` exits 0.
2. `grok test --catch-unhandled=false --category "Proteomics: 14-03"` — all tests green.
3. Manual: import a multi-contrast Spectronaut Candidates file with the `Flags` column present. Open the docked Filters viewer — confirm only Comparison + Display Name + Source ID search boxes appear; no Flags filter.
4. Manual: type a partial gene name in the Display Name search box. Confirm: (a) matched points highlight on the volcano via selection color, (b) NS cloud remains visible (df.filter unchanged), (c) top-15 labels still visible alongside the search match (union mode), (d) clearing the search restores the default top-N labels only.
5. Manual: import a single-contrast Candidates file — confirm no Filters viewer is docked (existing Phase 13 behavior preserved).
</verification>

<success_criteria>
- [ ] dockComparisonFilterIfMultiContrast uses typed `filters` array with explicit showBoolCombinedFilter:false
- [ ] Display Name and Source ID free-text filters are added when their SEMTYPE columns exist
- [ ] Verification gate catches and overrides Flags-leak if platform still auto-includes
- [ ] Defensive capture-restore wiring: savedBitSet captured, df.filter.copyFrom(savedBitSet) restores on each match, df.selection.set written from match indices (highlight-not-hide preserved)
- [ ] After selection update, setTopNLabels(df, sp, 15, 'union') is called so top-N seed and search match coexist
- [ ] Subscription cleanup prevents leak on re-import
- [ ] Single-contrast files still skip the dock (no regression of Phase 13 behavior)
- [ ] filtersScopingNoFlags test passes; build clean
</success_criteria>

<output>
Create `.planning/phases/14-ck-omics-analyst-experience-enhancements/14-03-SUMMARY.md` when done.
</output>
</content>
