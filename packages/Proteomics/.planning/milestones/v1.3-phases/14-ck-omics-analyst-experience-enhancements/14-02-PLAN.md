---
phase: 14-ck-omics-analyst-experience-enhancements
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/viewers/volcano.ts
  - src/package.ts
  - src/tests/volcano.ts
autonomous: true
requirements: [R2, G1, G2, G3]
user_setup: []

must_haves:
  truths:
    - "D-02: MUST-have for BP DMD/WT client deliverable — Plan 14-02 ships R2 + G1 (the second piece of the G1 + R1 + R2 trio); depends on 14-01 (R1) for Display Name binding"
    - "Volcano titlebar reads `Volcano Plot: {group1} vs {group2}` synthesized from getGroups(df) — never 'Volcano' or a generic string when groups exist"
    - "Volcano legend categories read `Enriched in {group1}` / `Enriched in {group2}` / `Not significant` (per D-04) — never 'up' / 'down' / 'not significant'"
    - "Direction column color map is exactly {Enriched in g1: 0xFFFF00FF magenta, Enriched in g2: 0xFF00FFFF cyan, Not significant: 0xFFAAAAAA gray} — LOCKED hex (D-04)"
    - "Top-15 proteins by ascending significance metric (tiebreaker |log2FC| desc) carry visible labels on initial render and recompute on metric/color/filter change (D-03)"
    - "Volcano labels bind to `Display Name` column (Plan 01 contract) — never `Gene name`; markers `*` / `†` round-trip into the label layer"
    - "Live counter overlay floats bottom-right of the volcano, shows Total + per-category counts, recomputes within one animation frame on df.filter / df.selection / viewer-property changes (D-06)"
    - "Y-axis label is `-Log10(Q-value)` in adj.p-value mode and `-Log10(p-value)` in p-value mode; X-axis label is `Log2 Fold Change`; both rewrite live on metric toggle (UI-SPEC §Copywriting)"
    - "Reopening Volcano Options dialog after a metric or color toggle shows the currently applied values (G2)"
    - "Color → Location switch displays a TaskBarProgressIndicator that appears on click and dismisses in finally (G3)"
    - "All counter overlay subscriptions are disposed on viewer re-entry (no stacked overlays, no frozen counter values — Pitfall 6)"
  artifacts:
    - path: "src/viewers/volcano.ts"
      provides: "Extended createVolcanoPlot + ensureDirectionColumn + new attachCounterOverlay + setTopNLabels + axis-label overlay + state-readers for dialog preload"
      contains: "DIRECTION_COLORS_BASE"
      min_lines: 350
    - path: "src/package.ts"
      provides: "Extended volcanoOptions handler with G2 preload + axis label sync + progress wrapping carry-forward"
      contains: "readVolcanoState"
    - path: "src/tests/volcano.ts"
      provides: "Updated assertions for direction strings, color hex, top-N selection, counter math, dialog state"
      contains: "Enriched in"
  key_links:
    - from: "src/viewers/volcano.ts:ensureDirectionColumn"
      to: "src/analysis/experiment-setup.ts:getGroups"
      via: "read group names live from proteomics.groups JSON"
      pattern: "getGroups\\(df\\)"
    - from: "src/viewers/volcano.ts:createVolcanoPlot"
      to: "src/utils/gene-label-resolver.ts (via SEMTYPE.DISPLAY_NAME)"
      via: "findColumn(df, SEMTYPE.DISPLAY_NAME, ['display name']) for labelColumnNames"
      pattern: "SEMTYPE\\.DISPLAY_NAME"
    - from: "src/viewers/volcano.ts:attachCounterOverlay"
      to: "df.onFilterChanged + df.onSelectionChanged + sp.onPropertyChanged"
      via: "rxjs subscriptions with debounceTime(50)"
      pattern: "onFilterChanged|onSelectionChanged|onPropertyChanged"
    - from: "src/package.ts:volcanoOptions"
      to: "sp.getOptions().look + df.tag('proteomics.volcano_metric')"
      via: "G2 source-of-truth precedence chain"
      pattern: "proteomics\\.volcano_metric"
---

<objective>
Volcano polish (R2, G1, G2, G3, D-03, D-04, D-06) — second piece of the MUST-have BP DMD/WT client deliverable trio (labelled here as a client-deliverable MUST-have for grouping purposes; wave assignment is governed mechanically by `wave = max(deps) + 1`, not by deliverable-group narrative). Extends `src/viewers/volcano.ts` in place per Phase 13 D-05 lock ("one volcano with property-toggles, never fork"). Implements: (a) D-04 magenta/cyan/gray color migration with group-name-derived legend strings, (b) D-03 default top-15 point labels driven by selection, (c) D-06 live floating counter overlay subscribed to df.filter + df.selection + sp.onPropertyChanged, (d) G1 synthesized title + axis-label rewriting, (e) G2 dialog state preload from sp.getOptions + df.tag, (f) G3 verification of progress indicator lifecycle on Color → Location.

Purpose: Closes Phase 13 UAT Gap 1 (major — generic title, raw axes, no labels, generic red/blue) plus Gaps 2 and 3 (minor). Delivers the BP DMD/WT client volcano visual parity per CONTEXT.md §"Specifics" — magenta=numerator/cyan=denominator/gray=NS color lock is non-negotiable. Depends on Plan 01 because the volcano now binds labels to `Display Name` (created by the gene-label resolver).

Output: In-place extensions to `src/viewers/volcano.ts` (current 250 lines → ~400 lines) and `src/package.ts` `volcanoOptions` handler. Updated `src/tests/volcano.ts` with new assertions. No new files in this plan.
</objective>

<scope_note>
This plan knowingly carries 4 tasks (Task 2 + Task 3 each touch 6-7 tests) rather than splitting into two plans. The reason: every task in this plan modifies `src/viewers/volcano.ts`, and the file-coupling between volcano polish steps (title synth ↔ axis labels ↔ counter overlay subscriptions ↔ direction-column migration ↔ dialog state reader) means a split would re-pay the `<read_first>` cost twice on the same file. The per-task verify commands keep feedback latency under 60s; context per task remains within the ~30% per-task budget. Accepted as-is per checker W-2.
</scope_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-ck-omics-analyst-experience-enhancements/14-CONTEXT.md
@.planning/phases/14-ck-omics-analyst-experience-enhancements/14-RESEARCH.md
@.planning/phases/14-ck-omics-analyst-experience-enhancements/14-PATTERNS.md
@.planning/phases/14-ck-omics-analyst-experience-enhancements/14-UI-SPEC.md
@.planning/phases/14-ck-omics-analyst-experience-enhancements/14-01-SUMMARY.md
@.planning/phases/13-ck-omics-volcano-and-enrichment-parity/13-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- From Plan 01 (already shipped before this plan runs): -->

src/utils/proteomics-types.ts exports:
- SEMTYPE.DISPLAY_NAME = 'Proteomics-DisplayName'
- SEMTYPE.SOURCE_ID = 'Proteomics-SourceId'
- SEMTYPE.GENE_SYMBOL = 'Proteomics-GeneSymbol' (unchanged carry-forward)

src/utils/gene-label-resolver.ts provides Display Name (always populated, even on no-op) and Source ID columns on every DataFrame produced by any of the 5 parsers.

<!-- From src/viewers/volcano.ts (CURRENT — extend in place): -->

Lines 17-25: type MetricKind, constants NEG_LOG_COL, DIRECTION_COL, LOCATION_COL.
Lines 31-40: pickMetricColumn(df, metric).
Lines 45-56: ensureNegLog10Column(df, metric) — Y axis column, set in place via col.init.
Lines 62-86: ensureDirectionColumn(df, fcThreshold, pThreshold, metric) — CURRENT category strings 'up'/'down'/'not significant' and CURRENT color map {'up': 0xFFCC0000 red, 'down': 0xFF0000CC blue, 'not significant': 0xFFAAAAAA gray}. THIS PLAN MIGRATES this function.
Lines 100-168: ensureLocationColumn — Phase 13 11-color subcellular palette (DO NOT regress this).
Lines 193-222: createVolcanoPlot factory — sp.plot.scatter(...), formulaLines, labelColumnNames binding to 'gene name' or 'gene symbol'.
Lines 231-250: recomputeVolcano(df, sp, fcThreshold, pThreshold, metric, colorDim) — called by volcanoOptions OK handler on metric / colorDim toggle.

<!-- From src/package.ts (CURRENT — extend volcanoOptions): -->

Line 117-131: dockComparisonFilterIfMultiContrast — DO NOT TOUCH in this plan (Plan 03 owns this).
Line 261-272: showVolcanoPlot handler — pure factory call createVolcanoPlot(df, {title}). May need updated title synth.
Line 274-355 (approx): volcanoOptions handler — opens the Options dialog, currently DOES NOT preload (G2 gap). Already wraps recomputeVolcano in TaskBarProgressIndicator + try/finally (G3 fix scope — verify only).
Line 337 (approx): existing DG.TaskBarProgressIndicator.create('...') call — pattern to mirror.

<!-- From src/analysis/experiment-setup.ts: -->

getGroups(df: DG.DataFrame): GroupAssignment | null — reads proteomics.groups JSON tag.
GroupAssignment interface: { group1: { name: string; columns: string[] }; group2: { name: string; columns: string[] } }.

<!-- From src/viewers/enrichment-viewers.ts (analog for subscription cleanup, Pattern S-4): -->

Line 9: `let activeSubscriptions: rxjs.Subscription[] = [];`
Lines 167-170: on re-entry: `for (const s of activeSubscriptions) s.unsubscribe(); activeSubscriptions = [];`

<!-- LOCKED color contract (D-04, CONTEXT.md §"Specifics"): -->

DIRECTION_COLORS_BASE = {
  enrichedG1: 0xFFFF00FF,  // magenta (ARGB)
  enrichedG2: 0xFF00FFFF,  // cyan (ARGB)
  notSig:     0xFFAAAAAA,  // gray (ARGB)
}

Category strings (UI-SPEC §"Copywriting"):
- `Enriched in {group1.name}` (e.g. "Enriched in DMD")
- `Enriched in {group2.name}` (e.g. "Enriched in WT")
- `Not significant` (note the capital N — different from CURRENT 'not significant')

Title format (UI-SPEC §"Copywriting"): `Volcano Plot: {group1.name} vs {group2.name}` (e.g. "Volcano Plot: DMD vs WT"). Fallback when groups unset: `Volcano Plot`.

Y-axis label: `-Log10(Q-value)` for metric='adj.p-value'; `-Log10(p-value)` for metric='p-value'.
X-axis label: `Log2 Fold Change` (invariant).

<!-- Counter overlay copy (UI-SPEC §"Copywriting" + §"Interaction Contracts > Volcano: live counter overlay"): -->

Heading: `Visible Proteins`
Rows (significance mode):
- `Total: {n}`
- `Enriched in {group1.name}: {n}`
- `Enriched in {group2.name}: {n}`
- `Not significant: {n}`

Rows (location mode): `Total: {n}` then per-category row using the LOCKED 11 location names + 'Unknown'.

Empty filter (zero rows): still render the overlay with `Total: 0` and per-category zero rows. Do NOT hide.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-04 direction-column migration — magenta/cyan/gray + group-name-derived legend strings + update existing tests</name>
  <files>src/viewers/volcano.ts, src/tests/volcano.ts</files>
  <read_first>
    - src/viewers/volcano.ts:62-86 (current ensureDirectionColumn — read in full)
    - src/viewers/volcano.ts:170-222 (createVolcanoPlot — to see what coupling the direction column has to formulaLines / label binding / color binding)
    - src/analysis/experiment-setup.ts (full file — getGroups signature and GroupAssignment shape)
    - src/tests/volcano.ts (every existing test — grep `'up'`, `'down'`, `'not significant'` to find assertions that must update)
    - 14-RESEARCH.md §"Code Examples > Verifying the volcano color-string migration" (full body of the migration excerpt)
    - 14-RESEARCH.md §"Pitfall 5" — color column rename breaks enrichment cross-link assertions
    - 14-UI-SPEC.md §"Copywriting Contract" (exact strings) and §"Color" (LOCKED hex)
    - 14-PATTERNS.md §"3. src/viewers/volcano.ts > D-04 color + legend migration"
  </read_first>
  <behavior>
    - Test 'volcanoDirectionStrings': fixture DataFrame with proteomics.groups JSON tag (group1.name='DMD', group2.name='WT'), log2FC and adj.p-value columns. After calling ensureDirectionColumn(df, 0.58, 0.05, 'adj.p-value'), df.col('direction').categories includes exactly 'Enriched in DMD', 'Enriched in WT', 'Not significant'. NEVER contains 'up' / 'down' / 'not significant' (lowercase).
    - Test 'volcanoDirectionStringsFallback': fixture without proteomics.groups tag — categories fall back to 'Enriched in group1' / 'Enriched in group2' / 'Not significant'. Assert no exception.
    - Test 'volcanoDirectionColors': after ensureDirectionColumn runs, `df.col('direction').meta.colors` exposes the categorical map containing the literal ARGB ints 0xFFFF00FF, 0xFF00FFFF, 0xFFAAAAAA mapped to the matching category strings. Read back via the platform's color-map API and assert exact int values.
    - Existing tests in src/tests/volcano.ts that assert on the old 'up' / 'down' / 'not significant' strings must update to the new strings in this same task. Pitfall 5: do not let red tests linger across task boundaries.
  </behavior>
  <action>
    Replace the body of `ensureDirectionColumn` in `src/viewers/volcano.ts:62-86` with the migration shown in 14-RESEARCH.md §"Code Examples > Verifying the volcano color-string migration".

    Specific changes:
    - Add `import {getGroups, GroupAssignment} from '../analysis/experiment-setup';` to the imports at the top of volcano.ts (alongside the existing imports of findColumn, SEMTYPE, parseAccession, subcellular-location module).
    - Add module-level constants:
      ```
      export const DIRECTION_COLORS_BASE = {
        enrichedG1: 0xFFFF00FF,  // magenta
        enrichedG2: 0xFF00FFFF,  // cyan
        notSig:     0xFFAAAAAA,  // gray
      } as const;
      ```
    - Inside `ensureDirectionColumn`, read `const groups = getGroups(df);`. Synthesize `g1Label = groups ? \`Enriched in ${groups.group1.name}\` : 'Enriched in group1';`, `g2Label = groups ? \`Enriched in ${groups.group2.name}\` : 'Enriched in group2';`, `nsLabel = 'Not significant';`.
    - The classification logic remains: `if (p <= pThreshold && fc > fcThreshold) return g1Label;` etc. Mirror the existing logic exactly — only the return strings change.
    - The color map call becomes: `col.meta.colors.setCategorical({[g1Label]: DIRECTION_COLORS_BASE.enrichedG1, [g2Label]: DIRECTION_COLORS_BASE.enrichedG2, [nsLabel]: DIRECTION_COLORS_BASE.notSig});`.
    - Do NOT remove the existing `col.init(...)` call — the column is still the same column, only string values change. NEVER remove+re-add the direction column (the volcano scatter binds to this column by name — removing it breaks the binding per the existing volcano.ts comment at lines 19-22).
    - Use bulk init via `col.init((i) => arr[i])` per project memory feedback_dg_column_bulk_init. Build the string[] in advance.

    Update `src/tests/volcano.ts` assertions:
    - Grep for every literal `'up'`, `'down'`, `'not significant'` (lowercase n) in the file and replace with the group-name-aware strings (or the fallback strings if the test fixture doesn't set proteomics.groups).
    - Add `volcanoDirectionStrings` test (fixture WITH groups tag).
    - Add `volcanoDirectionStringsFallback` test (fixture WITHOUT groups tag).
    - Add `volcanoDirectionColors` test asserting exact ARGB ints.
    - Wrap new tests in the existing category block (or create a new `category('Proteomics: 14-02', () => {...})` block — pick whichever matches the existing test file's structure). If creating a new category, register the entry alongside the existing volcano tests in src/package-test.ts (already imports './tests/volcano').

    Per 14-RESEARCH.md §"Pitfall 5": the existing `enrichment-viewers.ts:wireEnrichmentToVolcano` does NOT use the literal 'up' / 'down' strings (it uses the Intersection column) — verify this by greping `'up'\\|'down'` in src/viewers/enrichment-viewers.ts; if any literal use is found, update it in this same task.
  </action>
  <verify>
    <automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &gt; /tmp/14-02-task1-build.log 2&gt;&amp;1 &amp;&amp; grok test --catch-unhandled=false --category "Proteomics: 14-02" &amp;&amp; grep -nE "(expect|assert|expectArray|expectFloat|expectTable)\b.*['\"](up|down|not significant)['\"]" src/tests/volcano.ts src/viewers/enrichment-viewers.ts | wc -l | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    ensureDirectionColumn returns Enriched-in-{group}/Not-significant strings. Color map contains exact ARGB ints 0xFFFF00FF / 0xFF00FFFF / 0xFFAAAAAA. No literal 'up' / 'down' / 'not significant' string appears in assertion contexts in src/tests/volcano.ts or src/viewers/enrichment-viewers.ts. All volcano tests pass. Build is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: G1 title + axis labels + D-03 top-N labels + label binding to Display Name</name>
  <files>src/viewers/volcano.ts, src/package.ts, src/tests/volcano.ts</files>
  <read_first>
    - src/viewers/volcano.ts:193-222 (createVolcanoPlot factory — current labelColumnNames binding and title handling)
    - src/viewers/volcano.ts:231-250 (recomputeVolcano — to understand recompute paths)
    - src/package.ts:261-272 (showVolcanoPlot handler — currently synthesizes a title; reconcile with the new format)
    - src/package.ts:252-258 (DE-completion auto-volcano launcher — also synthesizes a title)
    - 14-RESEARCH.md §"Pattern 3: Default top-N point labels driven by selection" (full body)
    - 14-RESEARCH.md §"Pitfall 7: D-03 top-N labels collide with D-05 search-match selection" — Plan 03 will rely on the union-mode handle this task exposes
    - 14-UI-SPEC.md §"Copywriting" (title and axis label strings) and §"Interaction Contracts > Volcano: default top-N labels"
    - 14-PATTERNS.md §"3. src/viewers/volcano.ts > D-03 default top-N labels"
  </read_first>
  <behavior>
    - Test 'volcanoTitle': fixture with proteomics.groups (g1='DMD', g2='WT'), call createVolcanoPlot(df), assert sp.getOptions().look.title === 'Volcano Plot: DMD vs WT' (or whichever options-key the platform writes title under — the executor reads sp.getOptions() output to determine the exact key).
    - Test 'volcanoTitleFallback': fixture without groups, assert title === 'Volcano Plot'.
    - Test 'volcanoAxisLabels': after createVolcanoPlot, the volcano's Y axis label DOM (sp.root.querySelector for the axis label) reads '-Log10(Q-value)' in adj.p-value mode and '-Log10(p-value)' in p-value mode after toggling. X axis reads 'Log2 Fold Change'. If platform DOM probing is fragile, fall back to asserting that `sp.props.yColumnLabel` and `sp.props.xColumnLabel` (or whichever props the platform exposes; planner reads source during implementation) carry the expected strings.
    - Test 'volcanoTopN': fixture of 100 rows with varying adj.p-value, call setTopNLabels(df, sp, 15), assert df.selection.trueCount === 15 AND the 15 selected rows are the lowest-adj.p-value rows (ties broken by |log2FC| desc).
    - Test 'volcanoLabelBindsToDisplayName': fixture where df has both Gene name and Display Name columns (Display Name semType=SEMTYPE.DISPLAY_NAME), call createVolcanoPlot, assert sp.props.labelColumnNames includes 'Display Name' not 'Gene name'.
    - Test 'volcanoLabelFallbackToGeneName': fixture with ONLY Gene name (no Display Name — simulating an old DataFrame predating Plan 01), assert label binds to 'Gene name'.
    - Test 'setTopNLabelsUnionMode': fixture where setTopNLabels(df, sp, 5, 'replace') seeds selection of 5, then a synthetic external "search match" sets selection on 3 additional rows, then setTopNLabels(df, sp, 5, 'union') is called — assert df.selection.trueCount === 8 (5 top-N ∪ 3 search). This handle is what Plan 03 will use.
  </behavior>
  <action>
    Extend `createVolcanoPlot` in `src/viewers/volcano.ts:193-222`:

    Title synthesis (UI-SPEC §"Copywriting"):
    - After `const sp = df.plot.scatter({...})` (or wherever the scatter is created), call:
      ```
      const groups = getGroups(df);
      const titleStr = groups
        ? `Volcano Plot: ${groups.group1.name} vs ${groups.group2.name}`
        : 'Volcano Plot';
      sp.setOptions({title: titleStr});
      ```
    - This overrides any title the caller passed in via options.title — by design (the synthesized title is the contract). Update the showVolcanoPlot handler in src/package.ts:261-272 and the DE-completion launcher at 252-258 to no longer pre-compute a title (or to keep computing one but accept that createVolcanoPlot will replace it — planner picks; the cleaner path is to remove the title from the options arg).

    Axis label rewriting:
    - Datagrok platform note: `sp.props` includes `xAxisCustomTitle` / `yAxisCustomTitle` style properties; the executor reads the scatter viewer source or @datagrok-libraries to find the exact property name. If a direct prop exists, use it. Per 14-RESEARCH.md §Architecture Patterns ("DOM overlay for axis labels (Y label: ... X label: ...) — platform has no xAxisCustomTitle prop"), if no platform prop exists, render two absolutely-positioned `ui.divText` overlays anchored to the bottom-center (X) and left-center (Y, rotated -90°) of sp.root, with the same lifecycle as the counter overlay (subscribed to sp.onPropertyChanged to rewrite Y label when metric toggles). PREFER the platform prop if it exists; fall back to DOM overlay otherwise.
    - X label: literal 'Log2 Fold Change'.
    - Y label: derive from current metric via `df.getTag('proteomics.volcano_metric')` (Task 4 sets this tag) — 'adj.p-value' → '-Log10(Q-value)'; 'p-value' → '-Log10(p-value)'. Default to '-Log10(Q-value)' when tag absent.

    D-03 default top-N labels:
    - Add module-level export `export function setTopNLabels(df: DG.DataFrame, sp: DG.ScatterPlotViewer, n: number = 15, mode: 'replace' | 'union' = 'replace'): void` following 14-RESEARCH.md §"Pattern 3" exactly.
    - Selection drive: in replace mode, `df.selection.setAll(false, false)` then `for (const i of topN) df.selection.set(i, true, false)` then `df.selection.fireChanged()`. In union mode, do NOT clear existing selection; only add the top-N indices to whatever is currently selected.
    - Top-N selection: filter to filtered-in rows (df.filter.get(i)), exclude FLOAT_NULL on both negLog10P (Y) and log2FC (X), sort by negLog10P desc (higher -log10(p) means more significant ⇒ first), tiebreak by |log2FC| desc.
    - Per Pitfall 7: the default mode is 'replace' on initial render and on metric/color/filter change; Plan 03 calls 'union' after a search match. Subscribe to recompute on sp.onPropertyChanged + df.onFilterChanged (after Task 3 adds those subscriptions) — pick the mode='replace' at those triggers.

    Label binding to Display Name:
    - In createVolcanoPlot where labelColumnNames is set (currently around line 209-213), replace `findColumn(df, SEMTYPE.GENE_SYMBOL, ['gene name', 'gene symbol'])` with `findColumn(df, SEMTYPE.DISPLAY_NAME, ['display name']) ?? findColumn(df, SEMTYPE.GENE_SYMBOL, ['gene name', 'gene symbol'])` — Display Name takes priority; falls back to Gene name only for old DataFrames.
    - Set both `sp.props.labelColumnNames = [labelCol.name]` and call `setTopNLabels(df, sp, options?.topNLabels ?? 15)`. Add `topNLabels?: number` to the `options` parameter type.
    - Per UI-SPEC §"Interaction Contracts > Volcano: default top-N labels": preserve hover by setting `sp.props.showLabelsFor = 'Selected'` (which the scatter renders on top of MouseOverRow — verify behavior with a small smoke test during implementation; if the platform requires explicit Selected+MouseOverRow union, fall back to a custom showLabelsFor value).

    Add tests to src/tests/volcano.ts as listed in `<behavior>`. Use mock fixtures (50-100 rows with synthetic log2FC + adj.p-value distributions).
  </action>
  <verify>
    <automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &gt; /tmp/14-02-task2-build.log 2&gt;&amp;1 &amp;&amp; grok test --catch-unhandled=false --category "Proteomics: 14-02"</automated>
  </verify>
  <done>
    sp.setOptions title written by createVolcanoPlot. setTopNLabels exported, both 'replace' and 'union' modes implemented. Volcano labels bind to Display Name with Gene name fallback. Axis labels are correct in both metric modes. All listed tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-06 live counter overlay + subscription cleanup + recompute triggers</name>
  <files>src/viewers/volcano.ts, src/tests/volcano.ts</files>
  <read_first>
    - src/viewers/volcano.ts:193-250 (createVolcanoPlot + recomputeVolcano)
    - src/viewers/enrichment-viewers.ts (full file — module-level activeSubscriptions pattern at line 9, dispose pattern around lines 167-170; this is the exact in-tree precedent to mirror)
    - 14-RESEARCH.md §"Pattern 2: Live counter overlay anchored to a viewer" (full SVG/DOM body)
    - 14-RESEARCH.md §"Pitfall 6: Volcano counter overlay attached to a re-created viewer"
    - 14-UI-SPEC.md §"Copywriting" (Counter overlay heading + per-row strings) and §"Interaction Contracts > Volcano: live counter overlay"
    - 14-RESEARCH.md §"Assumption A10" — `df.col(name).categories` accessor verification
  </read_first>
  <behavior>
    - Test 'volcanoCounterRenders': fixture with 100 rows, mixed direction categories. After createVolcanoPlot, sp.root contains a child div with text "Visible Proteins" and 4 lines: Total, Enriched in g1, Enriched in g2, Not significant. Counts match a manual computation over df.filter bits.
    - Test 'volcanoCounterFilterRecompute': after creating the volcano with 100 rows visible, call `df.filter.setAll(false)` then `df.filter.set(0, true)` then `df.filter.set(1, true)` then `df.filter.fireChanged()`. Assert that within one animation frame (use a 60ms setTimeout to bridge the debounceTime(50) in the subscription), the counter total reads `Total: 2` and the per-category counts reflect rows 0 and 1.
    - Test 'volcanoCounterSelectionDoesNotChangeCounts': counts are derived from df.filter ONLY, not df.selection. Toggling df.selection triggers a recompute (per UI-SPEC contract — for parity with CK-omics behavior) but the displayed Total stays the same — only category counts may change IF the per-category breakdown is selection-aware. Per CONTEXT D-06: "Recomputes on df.filter changes, df.selection changes, and viewer-property changes. Counts respect filtered-in rows only." So Total = df.filter.trueCount unconditionally; category counts also iterate filter bits only. Selection-change triggers a recompute but the values don't change (defensive — preserves UI-SPEC contract).
    - Test 'volcanoCounterPropertyToggleRecompute': simulate sp.onPropertyChanged.next(...) for the colorColumnName property — assert counter recomputes (test by changing the color column source between direction and a second category column with different categories).
    - Test 'volcanoCounterSubscriptionsDisposedOnReentry': call createVolcanoPlot twice in succession on the same df (replacing the first). After the second call, df.filter.fireChanged() — assert only ONE counter overlay is on screen (the second), not two. Implement via a count of `sp.root.querySelectorAll('[data-volcano-counter]')`.
    - Test 'volcanoCounterEmptyFilter': set df.filter.setAll(false), assert overlay still renders with `Total: 0` and all category rows showing `: 0`.
    - Test 'volcanoCounterLocationMode': fixture with Subcellular Location column populated, change sp.props.colorColumnName to LOCATION_COL, assert counter rebuilds per-location-category breakdown using the 11 location names + Unknown.
  </behavior>
  <action>
    Add `import * as rxjs from 'rxjs';` and `import {debounceTime} from 'rxjs/operators';` (both already in the package per package.json — webpack externals).

    Add module-level subscription store mirroring `src/viewers/enrichment-viewers.ts:9`:
    ```
    let activeVolcanoSubscriptions: rxjs.Subscription[] = [];
    ```

    Implement `function attachCounterOverlay(sp: DG.ScatterPlotViewer, df: DG.DataFrame): void` per 14-RESEARCH.md §"Pattern 2" verbatim. Differences from the analog:
    - Heading text: `'Visible Proteins'` (UI-SPEC exact string).
    - Per-row format: `'Total: {n.toLocaleString()}'`, `'{category}: {n.toLocaleString()}'` — use the category column's category iterator. Category column is `direction` (Plan-02-Task-1 migrated strings) or `Subcellular Location` (Phase-13 carry-forward) depending on sp.props.colorColumnName. If colorColumnName matches neither, render only the Total row.
    - DOM tag: set `overlay.dataset.volcanoCounter = 'true'` so the dispose test can count it.
    - Style: `position: absolute; bottom: 8px; right: 8px; padding: 8px 16px; background: rgba(255,255,255,0.85); borderRadius: 4px; fontSize: 0.9em; pointerEvents: none; zIndex: 5;` per 14-UI-SPEC §"Spacing Scale" and §"Interaction Contracts > Volcano: live counter overlay".
    - Anchor parent: ensure `sp.root.style.position` is set to 'relative' if not already.
    - Recompute trigger subscriptions (all three, debounceTime(50)): df.onFilterChanged, df.onSelectionChanged, sp.onPropertyChanged. Push every subscription into `activeVolcanoSubscriptions`.

    Add a cleanup helper `function disposeVolcanoSubscriptions(): void` that unsubscribes every entry in activeVolcanoSubscriptions and clears the array. Call it at the START of createVolcanoPlot before any new subscriptions are attached — this is the dispose-on-re-entry pattern (Pitfall 6). Also remove any existing counter overlay DOM under sp.root.

    Inside createVolcanoPlot (after sp is created, after setTopNLabels is called, after axis labels are set): call `attachCounterOverlay(sp, df)`.

    Add tests to src/tests/volcano.ts as listed in `<behavior>`.

    Counter category lookup: detect which mode (significance vs location) by reading `sp.props.colorColumnName`. If it equals DIRECTION_COL ('direction'), build per-direction-category counts; if it equals LOCATION_COL ('Subcellular Location'), build per-location-category counts. Read categories via `df.col(colName).categories` (verify exact API name during implementation per Assumption A10; if `.categories` is wrong, use `.toList()` to enumerate distinct values).
  </action>
  <verify>
    <automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &gt; /tmp/14-02-task3-build.log 2&gt;&amp;1 &amp;&amp; grok test --catch-unhandled=false --category "Proteomics: 14-02"</automated>
  </verify>
  <done>
    attachCounterOverlay renders the floating div with the exact UI-SPEC strings. All 7 counter tests pass. Subscription cleanup prevents stacked overlays on re-entry. Build is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: G2 dialog state preload + G3 progress indicator verification + persist metric tag</name>
  <files>src/viewers/volcano.ts, src/package.ts, src/tests/volcano.ts</files>
  <read_first>
    - src/package.ts:274-355 (full volcanoOptions handler — current dialog structure, OK handler, existing TaskBarProgressIndicator usage near line 337)
    - src/viewers/volcano.ts:231-250 (recomputeVolcano signature — what metric / colorDim parameters it takes)
    - 14-RESEARCH.md §"Pattern 5: Dialog state preload from viewer" (full body)
    - 14-RESEARCH.md §"Pitfall 2: sp.getOptions() returns a snapshot, not a live binding"
    - 14-UI-SPEC.md §"Interaction Contracts > Volcano Options dialog: state preload"
    - 14-PATTERNS.md §"3. src/viewers/volcano.ts > G2 dialog-state preload"
  </read_first>
  <behavior>
    - Test 'volcanoOptionsPreloadMetric': create volcano with default metric='adj.p-value', call recomputeVolcano(..., 'p-value', ...) to switch metric, then re-open Volcano Options dialog (simulate by calling readVolcanoState(df, sp)). Assert returned state.metric === 'p-value'.
    - Test 'volcanoOptionsPreloadColorDim': initial colorDim='significance' (sp.props.colorColumnName === 'direction'), toggle to 'location' (sp.props.colorColumnName === 'Subcellular Location'), assert readVolcanoState returns colorDim='location'.
    - Test 'volcanoMetricTagPersistsViaSetOptions': after recomputeVolcano(df, sp, ..., 'p-value', ...), assert df.getTag('proteomics.volcano_metric') === 'p-value'.
    - Manual verification (HUMAN-UAT only, no automated test): G3 — clicking Color = Location in Volcano Options shows a TaskBarProgressIndicator that persists until the location classification completes and dismisses in a finally block even on error.
  </behavior>
  <action>
    In `src/viewers/volcano.ts`, add:
    ```
    export type ColorDim = 'significance' | 'location';
    export interface VolcanoState { metric: MetricKind; colorDim: ColorDim; }
    export function readVolcanoState(df: DG.DataFrame, sp: DG.ScatterPlotViewer): VolcanoState {
      const opts = sp.getOptions();
      const look = (opts as any).look ?? {};
      const colorDim: ColorDim = look.colorColumnName === LOCATION_COL ? 'location' : 'significance';
      const metric = (df.getTag('proteomics.volcano_metric') as MetricKind | null) ?? 'adj.p-value';
      return {metric, colorDim};
    }
    ```

    Inside `recomputeVolcano(df, sp, fcThreshold, pThreshold, metric, colorDim)` (lines 231-250 — read the current body before extending), after the metric param is applied to ensureNegLog10Column and ensureDirectionColumn, persist the metric to a df tag:
    ```
    df.setTag('proteomics.volcano_metric', metric);
    ```
    This makes the dialog idempotent across re-opens AND across page reloads of the same DataFrame (per Pattern 5 counterpart guidance).

    Update `src/package.ts:274-355` `volcanoOptions` handler to call `readVolcanoState(df, sp)` at dialog-open time and seed every input from the result. Specifically (read the current dialog body to map exactly):
    - `metricInput` value defaults from `state.metric` instead of the hardcoded 'adj.p-value'.
    - `colorDimInput` (the choice/toggle between Significance and Location) defaults from `state.colorDim`.
    - Per Pitfall 2: the read is at dialog-open time; OK uses the form input values directly (NOT a re-read of sp.getOptions). This is already the convention but verify it in the existing handler.

    G3 verification — read the current `volcanoOptions` body to confirm `DG.TaskBarProgressIndicator.create('...')` already wraps the `recomputeVolcano` call with try/finally (it does per 14-PATTERNS.md §"3 > G3 progress indicator"). If the wording on the progress label is generic (e.g. 'Updating volcano...'), change it to something Color → Location-specific when colorDim === 'location' (e.g. 'Classifying subcellular locations…'). This is a small UX wording fix — the lifecycle is already correct. No new code needed beyond the wording adjustment.

    Add tests to src/tests/volcano.ts as listed in `<behavior>`. The 'volcanoMetricTagPersistsViaSetOptions' test is the most directly verifiable.

    Note on G2 manual verification: the dialog re-open is hard to automate because the dialog is a modal UI element. The 'volcanoOptionsPreload*' tests assert the readVolcanoState helper instead — this is the verifiable seam. The HUMAN-UAT in 14-HUMAN-UAT.md handles the full re-open assertion.
  </action>
  <verify>
    <automated>cd /Users/edjaeger/datagrok/src/public/.claude/worktrees/proteomics/packages/Proteomics &amp;&amp; npm run build &gt; /tmp/14-02-task4-build.log 2&gt;&amp;1 &amp;&amp; grok test --catch-unhandled=false --category "Proteomics: 14-02"</automated>
  </verify>
  <done>
    readVolcanoState exported with VolcanoState type. recomputeVolcano persists 'proteomics.volcano_metric' df tag. volcanoOptions dialog reads state at open time and seeds inputs. G3 progress wording updated. All listed tests pass. Build is clean. Plan-02 success criteria fully met.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataFrame group-name strings → DOM | group1.name / group2.name from proteomics.groups JSON tag are rendered into the volcano title, legend, and counter overlay. Strings originate from the user's experimental annotation. |
| sp.props.colorColumnName → counter recompute | colorColumnName value drives which categorical column is iterated. Untrusted only if a malicious extension forces an arbitrary column name; this is platform-trust boundary, not a Phase 14 concern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-T1 | Tampering | Group name strings rendered in title / legend / counter | mitigate | All renders go through `ui.divText` or `sp.setOptions({title})` — both safe-by-default (no innerHTML). The setCategorical color map keys ARE the group strings; this is fine for color binding (not a render path). |
| T-14-02-I1 | Information Disclosure | sp.getOptions() may expose serialized viewer state | accept | Platform-owned API; analyst already sees viewer state in the Property Panel; no new exposure. |
| T-14-02-D1 | Denial of Service | Counter recompute on every filter mutation | mitigate | debounceTime(50) on all 3 subscriptions caps the rerender rate; iteration cost is O(rowCount) per recompute, acceptable for up to ~50k rows. Subscription cleanup on viewer re-entry prevents leak. |
| T-14-02-R1 | Repudiation | Silent color/legend migration breaks any external dashboard expecting 'up'/'down' strings | mitigate | This is a contract migration; documented in CONTEXT D-04 and UI-SPEC; no external dashboard depends on these strings (verified via codebase grep in Task 1 verify command). |
| T-14-02-SC | Tampering | npm/pip/cargo installs in this plan | accept | Zero new packages — uses platform-bundled rxjs, datagrok-api/dg, datagrok-api/ui. |
</threat_model>

<verification>
After all 4 tasks complete:

1. `cd packages/Proteomics && npm run build` exits 0.
2. `grok test --catch-unhandled=false --category "Proteomics: 14-02"` — all tests green.
3. Existing volcano category tests in src/tests/volcano.ts still pass (no regression on Phase 13 contracts: 11-color subcellular palette, location column hash invalidation, metric toggle).
4. Manual visual check against `~/Downloads/ck/DMD_vs_WT/volcano_plots/`: side-by-side compare on a Spectronaut Candidates BP DMD vs WT fixture — title matches, axis labels match, magenta/cyan/gray match, top-15 labels visible by default, counter overlay floats bottom-right with live counts.
5. Manual interaction: toggle filter category in a Filters viewer → counter updates within ~50ms. Re-open Volcano Options dialog after toggling metric → dialog shows the now-applied metric, not the default.
6. Manual G3: click Color = Location → TaskBarProgressIndicator appears immediately and dismisses on completion.
</verification>

<success_criteria>
- [ ] ensureDirectionColumn returns group-name-derived strings; color map carries exact ARGB ints
- [ ] No 'up' / 'down' / 'not significant' literal remains in src/tests/volcano.ts or src/viewers/enrichment-viewers.ts
- [ ] createVolcanoPlot synthesizes title via sp.setOptions and rewrites axis labels
- [ ] setTopNLabels exported with replace/union modes; default top-15 labels visible on initial render
- [ ] Label binding prefers SEMTYPE.DISPLAY_NAME (Plan 01 contract) with Gene name fallback
- [ ] attachCounterOverlay renders floating div with UI-SPEC strings, recomputes on filter/selection/property change
- [ ] activeVolcanoSubscriptions disposal pattern prevents stacked overlays
- [ ] readVolcanoState exported; G2 dialog preloads from sp.getOptions + df.tag
- [ ] recomputeVolcano persists `proteomics.volcano_metric` df tag
- [ ] G3 progress indicator lifecycle verified (already in place; wording updated)
- [ ] All Plan-02 tests pass; build clean; no regression on Phase 13 contracts
</success_criteria>

<output>
Create `.planning/phases/14-ck-omics-analyst-experience-enhancements/14-02-SUMMARY.md` when done.
</output>
</content>
