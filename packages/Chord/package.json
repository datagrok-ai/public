{
	"name": "chord",
	"fullName": "Chord",
	"version": "0.0.1",
	"description": "",
	"dependencies": {
		"datagrok-api": "latest",
		"circos": "^2.1.0",
		"d3": "^6.2.0"
	},
	"devDependencies": {
		"datagrok-tools": "^4.0.12",
		"css-loader": "^5.0.0",
		"style-loader": "^2.0.0",
		"webpack": "^4.44.2",
		"webpack-cli": "^4.2.0"
	},
	"scripts": {
		"debug-chord": "grok publish --rebuild",
		"release-chord": "grok publish --rebuild --release",
		"build-chord": "webpack",
		"build": "webpack",
		"debug-chord-public": "grok publish public --rebuild",
		"release-chord-public": "grok publish public --rebuild --release",
		"debug-chord-local": "grok publish local --rebuild",
		"release-chord-local": "grok publish local --rebuild --release",
		"debug-chord-localhost": "grok publish localhost --rebuild",
		"release-chord-localhost": "grok publish localhost --rebuild --release"
	}
}
