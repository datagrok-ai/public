# Thin wrapper so the platform manages this as an on-demand package container.
# The image itself is built by CI from deploy/jkg_r (it needs the core source tree
# for the datlas worker bundle, which a package build context does not have).
# Bump the tag here to roll this language forward.
FROM datagrok/jkg_r:bleeding-edge
