
```datagrok-exec
grokky.selectRows(t, (i) => t.getCol('age').get(i) > 40);
return {selected: t.selection.trueCount, total: t.rowCount};
```

Selected rows where **age > 40**. The result above shows how many rows matched out of the total.