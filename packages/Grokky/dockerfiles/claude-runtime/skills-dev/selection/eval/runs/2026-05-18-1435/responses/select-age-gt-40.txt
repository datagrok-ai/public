
```datagrok-exec
const age = t.getCol('age');
t.selection.init(i => age.get(i) > 40);
return {selected: t.selection.trueCount, total: t.rowCount};
```

Rows where **age > 40** have been selected. The result above shows how many rows matched out of the total.