
```datagrok-exec
grokky.invertSelection(t);
return {selected: t.selection.trueCount, total: t.rowCount};
```

Selection inverted — previously unselected rows are now selected, and previously selected rows are now deselected.