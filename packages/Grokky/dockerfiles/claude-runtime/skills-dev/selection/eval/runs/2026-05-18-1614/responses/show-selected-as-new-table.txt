
```datagrok-exec
const subset = t.clone(t.selection);
grok.shell.addTableView(subset);
```

Done! The selected rows have been opened as a new table view. If no rows were selected, the new table will be empty.