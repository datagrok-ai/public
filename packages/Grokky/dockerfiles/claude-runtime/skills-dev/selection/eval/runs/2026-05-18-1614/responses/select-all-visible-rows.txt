
```datagrok-exec
t.selection.copyFrom(t.filter);
```

Done! All currently-visible rows (those passing the active filter) are now selected. `t.selection.copyFrom(t.filter)` copies the filter BitSet directly into the selection, so only rows that pass the filter get marked as selected.