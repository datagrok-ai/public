
```datagrok-exec
// Find the molecule/SMILES column and set its width to 300px
const grid = view.grid;
let found = false;

// First try to find by semantic type
for (const col of t.columns.toList()) {
  if (col.semType === 'Molecule') {
    const gc = grid.col(col.name);
    if (gc) {
      gc.width = 300;
      found = true;
    }
  }
}

// Fallback: look for common molecule column names
if (!found) {
  const candidates = ['smiles', 'SMILES', 'molecule', 'Molecule', 'mol', 'Mol', 'structure', 'Structure'];
  for (const name of candidates) {
    const gc = grid.col(name);
    if (gc) {
      gc.width = 300;
      found = true;
      break;
    }
  }
}

if (!found) return ui.divText('No molecule column found. Please specify the column name.');
```

Done! The molecule column has been set to 300 pixels wide. If the wrong column was resized, let me know the exact column name and I'll fix it.