```datagrok-exec
t.col('activity').meta.colors.setLinear(['#FF0000', '#00FF00']);
```

The **activity** column is now color-coded with a red → green gradient, where low values appear red and high values appear green.