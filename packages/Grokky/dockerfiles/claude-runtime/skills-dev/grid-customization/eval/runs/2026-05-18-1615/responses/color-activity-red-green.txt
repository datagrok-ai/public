I'll apply a red-to-green linear color coding on the `activity` column now.

```datagrok-exec
t.col('activity').meta.colors.setLinear(['#FF0000', '#00FF00']);
```

Done! The **activity** column is now color-coded from **red** (low values) to **green** (high values). The gradient scales automatically between the column's min and max values, and it will appear consistently across all viewers (grid, scatter plots, etc.).