```datagrok-exec
grokky.colorCode(t.col('activity'), {kind: 'linear', range: ['#FF0000', '#FFFF00', '#00FF00']});
```

The **activity** column is now color-coded with a red → yellow → green gradient, where:
- 🔴 **Red** = low values
- 🟡 **Yellow** = mid-range values
- 🟢 **Green** = high values

This applies data-side, so all viewers (scatter plots, etc.) will reflect the same color scheme.