
```datagrok-exec
if (!(view instanceof DG.TableView)) return;

// Close all viewers except the grid
for (const viewer of view.viewers.toList()) {
  if (viewer.type !== DG.VIEWER.GRID) {
    viewer.close();
  }
}

// Reset grid customization
const cols = view.grid.columns;
for (let i = 1; i < cols.length; i++) {
  const gc = cols.byIndex(i);
  if (gc) gc.visible = true;
}
view.grid.setColumnsWidthType(DG.ColumnWidthType.Optimal);
view.grid.sort([], []);
for (const c of t.columns.toList())
  c.meta.colors.setDisabled();
```

Done! The layout has been reset to just the grid — all extra viewers have been closed, columns are restored and visible, sort is cleared, and color coding is disabled.