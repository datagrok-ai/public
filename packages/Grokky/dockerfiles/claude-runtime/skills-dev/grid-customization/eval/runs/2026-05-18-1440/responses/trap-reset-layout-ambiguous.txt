
```datagrok-exec
view.resetLayout();
```

The layout has been reset to just the grid — all viewers (scatter plots, histograms, filter panels, etc.) have been removed, leaving only the default table grid.