```datagrok-exec
t.col('activity').meta.colors.setLinear(['#FF0000', '#FFFF00', '#00FF00']);
```

The **activity** column is now color-coded with a red → yellow → green gradient, where:
- 🔴 **Red** = low values
- 🟡 **Yellow** = mid-range values
- 🟢 **Green** = high values