```datagrok-exec
grokky.colorCode(t.col('activity'), {kind: 'linear', range: ['#FF0000', '#FFFF00', '#00FF00']});
```

The **activity** column is now color-coded with a red → yellow → green gradient across its value range. Lower values appear red, mid-range yellow, and higher values green. This applies across all viewers (scatter plots, etc.) that use this column.