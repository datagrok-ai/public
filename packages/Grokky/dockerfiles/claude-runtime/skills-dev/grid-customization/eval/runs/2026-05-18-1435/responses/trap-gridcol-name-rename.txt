```datagrok-exec
t.col('activity').meta.friendlyName = 'Potency';
```

This sets the **display name** of the `activity` column to **"Potency"** across all viewers (grid, scatter plot, etc.), while keeping the underlying column name `activity` unchanged — so existing formulas and references won't break.