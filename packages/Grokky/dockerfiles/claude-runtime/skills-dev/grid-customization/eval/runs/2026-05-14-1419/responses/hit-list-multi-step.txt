```datagrok-exec
grokky.colorCode(t.col('activity'), {kind: 'linear', range: ['#FF0000', '#FFFF00', '#00FF00']});
grokky.configureGrid(view, {
  hide: ['index'],
  pin:  ['SMILES'],
});
grokky.applySort(view, ['activity'], ['desc']);
```

Done! Here's what was applied to your table:

- 📌 **SMILES** pinned to the left
- 🙈 **index** column hidden
- 🎨 **activity** color-coded red → yellow → green (low to high)
- 🔽 Rows sorted by **activity** descending