I'll look up the SMILES for CHEMBL301 using the ChEMBL ID to SMILES converter:

```datagrok-exec
const smiles = await grok.functions.call('Chembl:chemblIdToSmilesTs', { id: 'CHEMBL301' });
return ui.tableFromMap({ 'ChEMBL ID': 'CHEMBL301', 'SMILES': smiles });
```