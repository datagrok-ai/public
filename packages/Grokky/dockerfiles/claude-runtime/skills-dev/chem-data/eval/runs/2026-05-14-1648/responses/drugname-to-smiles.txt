```datagrok-exec
const result = await grok.functions.call('Chembl:namesToSmiles', { names: ['aspirin'] });
const smiles = result.columns.byName('canonical_smiles').get(0);
return ui.tableFromMap({ 'Compound': 'Aspirin', 'SMILES': smiles });
```

This calls the ChEMBL database to look up the canonical SMILES for aspirin. The result will be displayed above. Aspirin's canonical SMILES is typically `CC(=O)Oc1ccccc1C(=O)O` (acetylsalicylic acid).