I'll look up CHEMBL25 using the ChEMBL ID to SMILES converter:

```datagrok-exec
const smiles = await grok.functions.call('Chembl:chemblIdToSmilesTs', { id: 'CHEMBL25' });
return ui.tableFromMap({ 'ChEMBL ID': 'CHEMBL25', 'SMILES': smiles });
```

This will resolve **CHEMBL25** to its canonical SMILES string using the local ChEMBL database. The result will be displayed as a table with the ChEMBL ID and the corresponding structure (SMILES).