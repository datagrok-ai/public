
```datagrok-exec
const result = await grok.functions.call('MolTrack:getCompoundByCorporateId', { corporateValue: 'DG-12345' });
return ui.tableFromMap(typeof result === 'object' && result !== null ? result : { result: String(result) });
```

I've queried MolTrack for the compound with corporate ID **DG-12345**. The result is displayed above. If the compound exists in MolTrack, you'll see its properties (structure, registration details, dynamic fields, etc.). If nothing is shown or an error occurred, it likely means no compound with that corporate ID is registered.