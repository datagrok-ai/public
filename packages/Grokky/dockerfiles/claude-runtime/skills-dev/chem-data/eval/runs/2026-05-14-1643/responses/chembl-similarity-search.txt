
```datagrok-exec
const df = await grok.functions.call('Chembl:chemblSimilaritySearch', { molecule: 'CCOc1ccc(cc1)C(=O)O' });
grok.shell.addTableView(df);
return DG.Viewer.grid(df).root;
```

Here are the ChEMBL compounds similar to **CCOc1ccc(cc1)C(=O)O** (4-ethoxybenzoic acid). The results include:

- **`smiles`** — canonical SMILES of each similar compound
- **`chembl_id`** — ChEMBL identifier
- **`similarity`** — similarity score (Tanimoto coefficient)

The table has been opened as a new table view for further exploration. You can sort by `similarity` descending to see the closest matches at the top.