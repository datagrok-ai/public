```datagrok-exec
const molCol = t.columns.bySemType(DG.SEMTYPE.MOLECULE) ?? t.col('molecules');
const result = await grok.functions.call('Chem:findSimilar', {
  molStringsColumn: molCol,
  molString: 'CCOc1ccc(cc1)C(=O)O',
  limit: 10,
  cutoff: 0.0
});
return DG.Viewer.grid(result).root;
```

This runs a Morgan fingerprint / Tanimoto similarity search against the **molecules** column using `CCOc1ccc(cc1)C(=O)O` (4-ethoxybenzoic acid) as the query, returning the **top 10 most similar** molecules along with their similarity scores.