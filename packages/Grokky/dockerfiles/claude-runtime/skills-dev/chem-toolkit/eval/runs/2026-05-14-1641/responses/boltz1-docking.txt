I need to find the ligand to dock. Let me check what's currently in the table.

```datagrok-exec
// Check current table for molecule column
const molCol = t.columns.bySemType(DG.SEMTYPE.MOLECULE);
if (!molCol) throw new Error('No molecule column found in the current table.');

// Get available Boltz configs
const configs = await grok.functions.call('Boltz1:getBoltzConfigFolders', {});
return ui.tableFromMap({
  'Molecule column': molCol.name,
  'Row count': t.rowCount,
  'Available Boltz configs': configs.join(', ') || '(none found)'
});
```

<exec-result>
{"type":"map","value":{"Molecule column":"smiles","Row count":"1","Available Boltz configs":"DRD2"}}
</exec-result>

I found the molecule column (`smiles`, 1 row) and a Boltz-1 config: **DRD2**. Running the docking now!

```datagrok-exec
const molCol = t.columns.bySemType(DG.SEMTYPE.MOLECULE);
await grok.functions.call('Boltz1:docking', {
  table: t,
  ligands: molCol,
  config: 'DRD2'
});
```

Boltz-1 docking has been submitted for your ligand against the **DRD2** target. Here's what to expect:

- **Results** will be appended as new columns to your current table once the job completes (binding energy, confidence scores, and 3D pose data).
- The **DRD2** (Dopamine Receptor D2) config was used, as it's the only configured Boltz-1 target available on this instance.
- You can visualize the 3D binding pose via the molecule context panel once results arrive.

If you'd like to dock against a different target or run additional analyses (e.g., AutoDock Vina via `Docking:runAutodock`), just let me know!