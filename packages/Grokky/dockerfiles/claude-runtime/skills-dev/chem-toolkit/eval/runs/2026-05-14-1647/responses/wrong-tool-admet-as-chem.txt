
```datagrok-exec
const mol = t.columns.bySemType(DG.SEMTYPE.MOLECULE);
if (!mol) throw new Error('No molecule column found in the current table.');
await grok.functions.call('Admetica:getAdmeProperties', { table: t, molecules: mol });
```

ADME properties have been predicted and appended as new columns to your table. The results cover **Absorption, Distribution, Metabolism, Excretion, and Toxicity** endpoints from the Admetica models.

You can scroll right in the grid to see all the new columns, or use the **Column Manager** to show/hide specific ADMET properties of interest.