
```datagrok-exec
// Get all numeric column names
const numericCols = t.columns.numerical.toList().map(c => c.name);

view.addViewer(DG.VIEWER.CORR_PLOT, {
  columnNames: numericCols,
});
```

Added a **Correlation Plot** using all **numeric columns** in your table. Each cell shows the correlation coefficient between a pair of columns — hover over cells to explore pairwise relationships.