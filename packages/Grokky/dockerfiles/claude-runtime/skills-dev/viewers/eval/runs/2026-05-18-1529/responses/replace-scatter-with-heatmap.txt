```datagrok-exec
// Close all scatter plots
Array.from(view.viewers).slice(1)
  .filter((v) => v.type === DG.VIEWER.SCATTER_PLOT)
  .forEach((v) => { try { v.close(); } catch {} });

// Add a correlation plot (heat map of correlations)
view.addViewer(DG.VIEWER.CORR_PLOT);
```Done! I've closed the scatter plot and replaced it with a **Correlation Plot** — Datagrok's heat map of pairwise correlations across your numeric columns. Each cell shows the correlation coefficient between two columns, color-coded by strength and direction.