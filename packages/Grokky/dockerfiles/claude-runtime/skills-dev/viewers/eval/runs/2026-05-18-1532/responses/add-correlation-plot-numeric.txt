
```datagrok-exec
// Find all numeric columns
const numericCols = t.columns.numerical.toList().map(c => c.name);

if (numericCols.length < 2) {
  return ui.divText('Not enough numeric columns to create a correlation plot (need at least 2).');
}

view.addViewer(DG.VIEWER.CORR_PLOT, {
  columnNames: numericCols,
});
```

I've added a **Correlation Plot** using all numeric columns in your dataset (`${undefined}`). The plot shows pairwise correlations — hover over cells to see the exact correlation coefficients, and click to filter by the corresponding scatter plot.