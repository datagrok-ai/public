```datagrok-exec
grokky.closeViewer('Scatter plot', view);
grokky.addViewer(view, 'Correlation plot');
```

Done! I've replaced the scatter plot with a **Correlation plot** — Datagrok's heatmap-style viewer that shows pairwise correlations between all numeric columns. You can click any cell to drill into the scatter plot for that pair.