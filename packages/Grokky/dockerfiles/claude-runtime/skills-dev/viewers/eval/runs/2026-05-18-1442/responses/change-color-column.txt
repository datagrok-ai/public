```datagrok-exec
const sp = Array.from(view.viewers).slice(1)
  .find((v) => v.type === DG.VIEWER.SCATTER_PLOT);
if (sp) {
  sp.setOptions({ colorColumnName: 'activity' });
} else {
  return ui.divText('No scatter plot found on the current view.');
}
```

This sets the **color column** to `activity` on the existing scatter plot. If you don't see the change, make sure there's a scatter plot open on the current view.