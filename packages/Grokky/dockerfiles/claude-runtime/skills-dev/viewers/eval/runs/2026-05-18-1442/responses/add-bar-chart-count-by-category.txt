```datagrok-exec
view.addViewer(DG.VIEWER.BAR_CHART, {
  splitColumnName: 'category',
  valueColumnName: 'count',
  valueAggrType: 'sum',
});
```

I've added a **Bar Chart** with:
- **Split by**: `category` column
- **Value**: `count` column (aggregated by sum)