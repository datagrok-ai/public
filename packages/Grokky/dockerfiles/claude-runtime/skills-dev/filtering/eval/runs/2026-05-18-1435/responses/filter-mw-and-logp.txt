
```datagrok-exec
const mw = t.getCol('MW');
const logP = t.getCol('LogP');
t.filter.init((i) => mw.get(i) < 500 && logP.get(i) < 5);
return ui.tableFromMap({ 'Visible rows': t.filter.trueCount, 'Hidden rows': t.filter.falseCount });
```

Applied a combined filter: **MW < 500** AND **LogP < 5**. The result above shows how many rows pass both conditions.