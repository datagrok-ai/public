
```datagrok-exec
const filtered = t.clone(t.filter);
grok.shell.addTableView(filtered);
```

Done! A new table view has been opened containing only the **${filtered.rowCount} rows** that were passing the current filter. The original table is unchanged.