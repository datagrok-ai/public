
```datagrok-exec
t.rows.removeWhereIdx((i) => t.getCol('category').get(i) === 'X');
```

All rows where **category = 'X'** have been permanently removed from the table. Note this action is **destructive** — the rows cannot be recovered unless you reload the original data.