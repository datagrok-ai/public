
```datagrok-exec
const filtered = t.clone(t.filter);
grok.shell.addTableView(filtered);
```

Done! A new table view has been created containing only the **{filtered rows}** rows currently passing the filter. The original table is unchanged.