
```datagrok-exec
t.filter.init((i) => t.getCol('activity').get(i) <= 7);
```

Done! Rows where **activity > 7** are now hidden (filtered out). The data is intact — you can clear the filter anytime to restore all rows.