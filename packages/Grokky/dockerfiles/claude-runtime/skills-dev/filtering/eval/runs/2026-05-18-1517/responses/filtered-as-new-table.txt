
```datagrok-exec
const subset = t.clone(t.filter);
grok.shell.addTableView(subset);
```

This clones the currently visible (filtered) rows into a new DataFrame and opens it in a new table view. The original table and its filter remain unchanged.