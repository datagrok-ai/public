
```datagrok-exec
await grokky.filterSubstructure(view, 'smiles', '[#6][!#1]');
```

The substructure filter has been applied using the SMARTS pattern `[#6][!#1]` (any carbon bonded to any non-hydrogen atom). The table now shows only matching compounds. The filter was auto-detected as SMARTS and applied via the UI filter panel.