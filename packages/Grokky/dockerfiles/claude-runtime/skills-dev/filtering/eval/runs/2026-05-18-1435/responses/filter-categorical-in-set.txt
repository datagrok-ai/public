
```datagrok-exec
view.getFiltersGroup({createDefaultFilters: false}).updateOrAdd({
  type: DG.FILTER_TYPE.CATEGORICAL,
  column: 'category',
  selected: ['A', 'B', 'C'],
});
```

Applied a categorical filter on the **category** column — only rows where the value is **A**, **B**, or **C** are now visible.