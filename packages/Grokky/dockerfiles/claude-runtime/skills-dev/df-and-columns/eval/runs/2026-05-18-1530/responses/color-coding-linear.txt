```datagrok-exec
t.getCol('age').meta.colors.setLinear(
  ['#ff0000', '#ffff00', '#00ff00'],
  { min: 19, max: 70 }
);
```

Done! The **age** column is now color-coded with a red → yellow → green linear gradient, pinned between **19** (red) and **70** (green).