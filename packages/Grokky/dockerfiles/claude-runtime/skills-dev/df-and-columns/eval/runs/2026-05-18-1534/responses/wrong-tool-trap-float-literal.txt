
```datagrok-exec
t.columns.addNewFloat('score');
```

Done! A new float-type column named **`score`** has been added to the current table. All values are initialized to `0` by default — you can edit them directly in the grid or populate them programmatically.