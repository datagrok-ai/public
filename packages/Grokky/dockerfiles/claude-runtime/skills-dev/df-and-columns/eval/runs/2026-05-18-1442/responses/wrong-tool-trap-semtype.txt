
```datagrok-exec
t.columns.bySemType(DG.SEMTYPE.MOLECULE)?.semType; // check if already set
const smilesCol = t.col('smiles') ?? t.columns.firstWhere(c => c.name.toLowerCase().includes('smiles'));
if (!smilesCol) throw new Error('No column named "smiles" found');
smilesCol.semType = DG.SEMTYPE.MOLECULE;
return ui.divText(`Set semType of "${smilesCol.name}" to Molecule`);
```

The semantic type of the **smiles** column has been set to `Molecule`. Datagrok will now recognize it as a chemical structure column, enabling cheminformatics features like structure rendering, similarity search, and substructure filtering.