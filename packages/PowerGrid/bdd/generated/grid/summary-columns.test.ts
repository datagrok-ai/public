/* eslint-disable max-len */
/* eslint-disable comma-spacing */
/* eslint-disable quotes */
/* ---
generated: features/grid/summary-columns.feature
generator: @datagrok-libraries/bdd — do not edit; run `grok-bdd compile` to regenerate
sub_features_covered: [viewers.grid]
--- */
import {test} from '@playwright/test';
import '@datagrok-libraries/bdd/bindings/common/kinds';
import '@datagrok-libraries/bdd/bindings/common/parameter-types';
import '@datagrok-libraries/bdd/bindings/platform/datasets';
import '@datagrok-libraries/bdd/bindings/platform/elements';
import {loggedIn} from '@datagrok-libraries/bdd/bindings/common/session';
import {clickOn, hoverOver, pressKey, shouldBe, shouldContainText, typeInto} from '@datagrok-libraries/bdd/bindings/common/steps';
import {columnCount, hasColumn, hasNoColumn} from '@datagrok-libraries/bdd/bindings/platform/columns';
import {autostartsCompleted, closeAllViews, openDataset, openProject, saveAsProject} from '@datagrok-libraries/bdd/bindings/platform/steps';
import {addViewer, areaPainted, clickArea, clickAreaHolding, closeContextMenu, hasArea, hasNoArea, loadLayout, noErrors, pickFromAreaContextMenu, readingReads, repainted, rightClickArea, saveLayoutToServer, showsRows} from '@datagrok-libraries/bdd/bindings/tiers/viewers/steps';
import {ds, el, feature} from '@datagrok-libraries/bdd/runtime';

test.describe("Grid summary columns", () => {
  const session = feature(test, "features/grid/summary-columns.feature", import.meta.url);
  test("Add > Summary Columns > Sparklines appends a sparkline column [item=Sparklines, type=sparkline]", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(50, "When user picks \"Add > Summary Columns > Sparklines\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Sparklines", "cell 2 of USUBJID", el("grid")));
    await session.step(51, "Then the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Sparklines\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Sparklines"));
    await session.step(52, "And the \"cell type of Sparklines\" reading of grid should be \"sparkline\"", () => readingReads(page, "cell type of Sparklines", el("grid"), "sparkline"));
    await session.step(53, "And the \"cell 2 of Sparklines\" area of grid should be painted", () => areaPainted(page, "cell 2 of Sparklines", el("grid")));
    await session.step(54, "And the table should have 11 columns", () => columnCount(page, 11));
    await session.step(55, "And no errors should have been logged", () => noErrors(page));
  });
  test("Add > Summary Columns > Bar Chart appends a barchart column [item=Bar Chart, type=barchart]", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(50, "When user picks \"Add > Summary Columns > Bar Chart\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Bar Chart", "cell 2 of USUBJID", el("grid")));
    await session.step(51, "Then the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Bar Chart\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Bar Chart"));
    await session.step(52, "And the \"cell type of Bar Chart\" reading of grid should be \"barchart\"", () => readingReads(page, "cell type of Bar Chart", el("grid"), "barchart"));
    await session.step(53, "And the \"cell 2 of Bar Chart\" area of grid should be painted", () => areaPainted(page, "cell 2 of Bar Chart", el("grid")));
    await session.step(54, "And the table should have 11 columns", () => columnCount(page, 11));
    await session.step(55, "And no errors should have been logged", () => noErrors(page));
  });
  test("Add > Summary Columns > Pie Chart appends a piechart column [item=Pie Chart, type=piechart]", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(50, "When user picks \"Add > Summary Columns > Pie Chart\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Pie Chart", "cell 2 of USUBJID", el("grid")));
    await session.step(51, "Then the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Pie Chart\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Pie Chart"));
    await session.step(52, "And the \"cell type of Pie Chart\" reading of grid should be \"piechart\"", () => readingReads(page, "cell type of Pie Chart", el("grid"), "piechart"));
    await session.step(53, "And the \"cell 2 of Pie Chart\" area of grid should be painted", () => areaPainted(page, "cell 2 of Pie Chart", el("grid")));
    await session.step(54, "And the table should have 11 columns", () => columnCount(page, 11));
    await session.step(55, "And no errors should have been logged", () => noErrors(page));
  });
  test("Add > Summary Columns > VlaaiVis appends a vlaaivis column [item=VlaaiVis, type=vlaaivis]", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(50, "When user picks \"Add > Summary Columns > VlaaiVis\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > VlaaiVis", "cell 2 of USUBJID", el("grid")));
    await session.step(51, "Then the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, VlaaiVis\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, VlaaiVis"));
    await session.step(52, "And the \"cell type of VlaaiVis\" reading of grid should be \"vlaaivis\"", () => readingReads(page, "cell type of VlaaiVis", el("grid"), "vlaaivis"));
    await session.step(53, "And the \"cell 2 of VlaaiVis\" area of grid should be painted", () => areaPainted(page, "cell 2 of VlaaiVis", el("grid")));
    await session.step(54, "And the table should have 11 columns", () => columnCount(page, 11));
    await session.step(55, "And no errors should have been logged", () => noErrors(page));
  });
  test("Add > Summary Columns > Radar appends a radar column [item=Radar, type=radar]", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(50, "When user picks \"Add > Summary Columns > Radar\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Radar", "cell 2 of USUBJID", el("grid")));
    await session.step(51, "Then the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Radar\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Radar"));
    await session.step(52, "And the \"cell type of Radar\" reading of grid should be \"radar\"", () => readingReads(page, "cell type of Radar", el("grid"), "radar"));
    await session.step(53, "And the \"cell 2 of Radar\" area of grid should be painted", () => areaPainted(page, "cell 2 of Radar", el("grid")));
    await session.step(54, "And the table should have 11 columns", () => columnCount(page, 11));
    await session.step(55, "And no errors should have been logged", () => noErrors(page));
  });
  test("Add > Summary Columns > Smart Form appends a smartform column [item=Smart Form, type=smartform]", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(50, "When user picks \"Add > Summary Columns > Smart Form\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Smart Form", "cell 2 of USUBJID", el("grid")));
    await session.step(51, "Then the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Smart Form\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Smart Form"));
    await session.step(52, "And the \"cell type of Smart Form\" reading of grid should be \"smartform\"", () => readingReads(page, "cell type of Smart Form", el("grid"), "smartform"));
    await session.step(53, "And the \"cell 2 of Smart Form\" area of grid should be painted", () => areaPainted(page, "cell 2 of Smart Form", el("grid")));
    await session.step(54, "And the table should have 11 columns", () => columnCount(page, 11));
    await session.step(55, "And no errors should have been logged", () => noErrors(page));
  });
  test("Add > Summary Columns > Tags appends a tags column [item=Tags, type=tags]", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(50, "When user picks \"Add > Summary Columns > Tags\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Tags", "cell 2 of USUBJID", el("grid")));
    await session.step(51, "Then the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Tags\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Tags"));
    await session.step(52, "And the \"cell type of Tags\" reading of grid should be \"tags\"", () => readingReads(page, "cell type of Tags", el("grid"), "tags"));
    await session.step(53, "And the \"cell 2 of Tags\" area of grid should be painted", () => areaPainted(page, "cell 2 of Tags", el("grid")));
    await session.step(54, "And the table should have 11 columns", () => columnCount(page, 11));
    await session.step(55, "And no errors should have been logged", () => noErrors(page));
  });
  test("Add > Summary Columns > Confidence Interval appends a confidenceinterval column [item=Confidence Interval, type=confidenceinterval]", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(50, "When user picks \"Add > Summary Columns > Confidence Interval\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Confidence Interval", "cell 2 of USUBJID", el("grid")));
    await session.step(51, "Then the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Confidence Interval\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Confidence Interval"));
    await session.step(52, "And the \"cell type of Confidence Interval\" reading of grid should be \"confidenceinterval\"", () => readingReads(page, "cell type of Confidence Interval", el("grid"), "confidenceinterval"));
    await session.step(53, "And the \"cell 2 of Confidence Interval\" area of grid should be painted", () => areaPainted(page, "cell 2 of Confidence Interval", el("grid")));
    await session.step(54, "And the table should have 11 columns", () => columnCount(page, 11));
    await session.step(55, "And no errors should have been logged", () => noErrors(page));
  });
  test("The menu lists the renderer items", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(69, "When user right-clicks on the \"cell 2 of USUBJID\" area of grid", () => rightClickArea(page, "cell 2 of USUBJID", el("grid")));
    await session.step(70, "And user hovers over \"Add\" menu item in context menu", () => hoverOver(page, el("\"Add\" menu item in context menu")));
    await session.step(71, "And user hovers over \"Summary Columns\" menu item in context menu", () => hoverOver(page, el("\"Summary Columns\" menu item in context menu")));
    await session.step(72, "Then \"Sparklines\" menu item in context menu should be visible", () => shouldBe(page, el("\"Sparklines\" menu item in context menu"), "visible"));
    await session.step(73, "And \"Bar Chart\" menu item in context menu should be visible", () => shouldBe(page, el("\"Bar Chart\" menu item in context menu"), "visible"));
    await session.step(74, "And \"VlaaiVis\" menu item in context menu should be visible", () => shouldBe(page, el("\"VlaaiVis\" menu item in context menu"), "visible"));
    await session.step(75, "And \"Confidence Interval\" menu item in context menu should be visible", () => shouldBe(page, el("\"Confidence Interval\" menu item in context menu"), "visible"));
    await session.step(76, "When user closes the context menu", () => closeContextMenu(page));
    await session.step(77, "Then no errors should have been logged", () => noErrors(page));
  });
  test("The remove icon on the top panel removes a selected summary column (GROK-18256)", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(80, "When user picks \"Add > Summary Columns > Sparklines\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Sparklines", "cell 2 of USUBJID", el("grid")));
    await session.step(81, "Then grid should have a \"header Sparklines\" area", () => hasArea(page, el("grid"), "header Sparklines"));
    await session.step(82, "And remove selected columns icon should be disabled", () => shouldBe(page, el("remove selected columns icon"), "disabled"));
    await session.step(83, "When user clicks on the \"header Sparklines\" area of grid holding Control", () => clickAreaHolding(page, "header Sparklines", el("grid"), "Control"));
    await session.step(84, "Then remove selected columns icon should be enabled", () => shouldBe(page, el("remove selected columns icon"), "enabled"));
    await session.step(85, "When user clicks on remove selected columns icon", () => clickOn(page, el("remove selected columns icon")));
    await session.step(86, "Then grid should not have a \"header Sparklines\" area", () => hasNoArea(page, el("grid"), "header Sparklines"));
    await session.step(87, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(88, "And the table should have 11 columns", () => columnCount(page, 11));
    await session.step(89, "And no errors should have been logged", () => noErrors(page));
  });
  test("The grid keeps drawing a Tags column whose source column was removed (GROK-19942)", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(92, "When user picks \"Add > Summary Columns > Tags\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Tags", "cell 2 of USUBJID", el("grid")));
    await session.step(93, "Then the \"cell type of Tags\" reading of grid should be \"tags\"", () => readingReads(page, "cell type of Tags", el("grid"), "tags"));
    await session.step(94, "And the \"cell 3 of Tags\" area of grid should be painted", () => areaPainted(page, "cell 3 of Tags", el("grid")));
    await session.step(95, "When user picks \"Remove\" from the context menu of the \"header CONTROL\" area of grid", () => pickFromAreaContextMenu(page, "Remove", "header CONTROL", el("grid")));
    await session.step(96, "Then the table should not have a column \"CONTROL\"", () => hasNoColumn(page, "CONTROL"));
    await session.step(97, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, STARTED, SEVERITY, Tags\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, STARTED, SEVERITY, Tags"));
    await session.step(98, "When user clicks on the \"cell 3 of AGE\" area of grid", () => clickArea(page, "cell 3 of AGE", el("grid")));
    await session.step(99, "And user presses Control+End", () => pressKey(page, "Control+End"));
    await session.step(100, "Then grid should have repainted", () => repainted(page, el("grid")));
    await session.step(101, "And grid should have a \"cell 1000 of Tags\" area", () => hasArea(page, el("grid"), "cell 1000 of Tags"));
    await session.step(102, "And the \"cell type of Tags\" reading of grid should be \"tags\"", () => readingReads(page, "cell type of Tags", el("grid"), "tags"));
    await session.step(103, "When user presses Control+Home", () => pressKey(page, "Control+Home"));
    await session.step(104, "Then grid should have a \"cell 1 of Tags\" area", () => hasArea(page, el("grid"), "cell 1 of Tags"));
    await session.step(105, "And no errors should have been logged", () => noErrors(page));
    await session.step(106, "When user picks \"Remove\" from the context menu of the \"header Tags\" area of grid", () => pickFromAreaContextMenu(page, "Remove", "header Tags", el("grid")));
    await session.step(107, "Then grid should not have a \"header Tags\" area", () => hasNoArea(page, el("grid"), "header Tags"));
  });
  test("A renamed source column stays in its summary column", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(110, "When user picks \"Add > Summary Columns > Sparklines\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Sparklines", "cell 2 of USUBJID", el("grid")));
    await session.step(111, "And user clicks on the \"cell 1 of Sparklines\" area of grid", () => clickArea(page, "cell 1 of Sparklines", el("grid")));
    await session.step(112, "Then context panel should contain text \"HEIGHT: 174.705\"", () => shouldContainText(page, el("context panel"), "HEIGHT: 174.705"));
    await session.step(113, "When user picks \"Column Properties...\" from the context menu of the \"header HEIGHT\" area of grid", () => pickFromAreaContextMenu(page, "Column Properties...", "header HEIGHT", el("grid")));
    await session.step(114, "And user types \"STATURE\" into \"New name:\" input in HEIGHT dialog", () => typeInto(page, "STATURE", el("\"New name:\" input in HEIGHT dialog")));
    await session.step(115, "And user clicks on OK button in HEIGHT dialog", () => clickOn(page, el("OK button in HEIGHT dialog")));
    await session.step(116, "Then the table should have a column \"STATURE\"", () => hasColumn(page, "STATURE"));
    await session.step(117, "And the table should not have a column \"HEIGHT\"", () => hasNoColumn(page, "HEIGHT"));
    await session.step(118, "When user clicks on the \"cell 2 of Sparklines\" area of grid", () => clickArea(page, "cell 2 of Sparklines", el("grid")));
    await session.step(119, "Then context panel should contain text \"STATURE: 150.288\"", () => shouldContainText(page, el("context panel"), "STATURE: 150.288"));
    await session.step(120, "And context panel should contain text \"AGE: 30\"", () => shouldContainText(page, el("context panel"), "AGE: 30"));
    await session.step(121, "And context panel should contain text \"WEIGHT: 64\"", () => shouldContainText(page, el("context panel"), "WEIGHT: 64"));
    await session.step(122, "And no errors should have been logged", () => noErrors(page));
  });
  test("The eight summary columns and their renderers come back from a layout and a project", {tag: ["@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(43, "Given user is logged in", () => loggedIn(page));
    await session.step(44, "And the package autostarts have completed", () => autostartsCompleted(page));
    await session.step(45, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(46, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(47, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
    await session.step(125, "When user picks \"Add > Summary Columns > Sparklines\" from the context menu of the \"cell 2 of USUBJID\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Sparklines", "cell 2 of USUBJID", el("grid")));
    await session.step(126, "And user picks \"Add > Summary Columns > Bar Chart\" from the context menu of the \"cell 2 of Sparklines\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Bar Chart", "cell 2 of Sparklines", el("grid")));
    await session.step(127, "And user picks \"Add > Summary Columns > Pie Chart\" from the context menu of the \"cell 2 of Bar Chart\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Pie Chart", "cell 2 of Bar Chart", el("grid")));
    await session.step(128, "And user picks \"Add > Summary Columns > VlaaiVis\" from the context menu of the \"cell 2 of Pie Chart\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > VlaaiVis", "cell 2 of Pie Chart", el("grid")));
    await session.step(129, "And user picks \"Add > Summary Columns > Radar\" from the context menu of the \"cell 2 of VlaaiVis\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Radar", "cell 2 of VlaaiVis", el("grid")));
    await session.step(130, "And user picks \"Add > Summary Columns > Smart Form\" from the context menu of the \"cell 2 of Radar\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Smart Form", "cell 2 of Radar", el("grid")));
    await session.step(131, "And user picks \"Add > Summary Columns > Tags\" from the context menu of the \"cell 2 of Smart Form\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Tags", "cell 2 of Smart Form", el("grid")));
    await session.step(132, "And user picks \"Add > Summary Columns > Confidence Interval\" from the context menu of the \"cell 2 of Tags\" area of grid", () => pickFromAreaContextMenu(page, "Add > Summary Columns > Confidence Interval", "cell 2 of Tags", el("grid")));
    await session.step(133, "Then the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Sparklines, Bar Chart, Pie Chart, VlaaiVis, Radar, Smart Form, Tags, Confidence Interval\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Sparklines, Bar Chart, Pie Chart, VlaaiVis, Radar, Smart Form, Tags, Confidence Interval"));
    await session.step(134, "When user saves the layout of the current table view to the server", () => saveLayoutToServer(page));
    await session.step(135, "And user closes all views", () => closeAllViews(page));
    await session.step(136, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(137, "And user adds a scatter plot viewer", () => addViewer(page, "scatter plot"));
    await session.step(138, "Then grid should not have a \"header Sparklines\" area", () => hasNoArea(page, el("grid"), "header Sparklines"));
    await session.step(139, "When user loads the saved layout", () => loadLayout(page));
    await session.step(140, "Then scatter plot viewer should be absent", () => shouldBe(page, el("scatter plot viewer"), "absent"));
    await session.step(141, "And the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Sparklines, Bar Chart, Pie Chart, VlaaiVis, Radar, Smart Form, Tags, Confidence Interval\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Sparklines, Bar Chart, Pie Chart, VlaaiVis, Radar, Smart Form, Tags, Confidence Interval"));
    await session.step(142, "And the \"cell type of Sparklines\" reading of grid should be \"sparkline\"", () => readingReads(page, "cell type of Sparklines", el("grid"), "sparkline"));
    await session.step(143, "And the \"cell type of Bar Chart\" reading of grid should be \"barchart\"", () => readingReads(page, "cell type of Bar Chart", el("grid"), "barchart"));
    await session.step(144, "And the \"cell type of Pie Chart\" reading of grid should be \"piechart\"", () => readingReads(page, "cell type of Pie Chart", el("grid"), "piechart"));
    await session.step(145, "When user clicks on the \"cell 2 of Pie Chart\" area of grid", () => clickArea(page, "cell 2 of Pie Chart", el("grid")));
    await session.step(146, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
    await session.step(147, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
    await session.step(148, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
    await session.step(149, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
    await session.step(150, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
    await session.step(151, "Then the \"current column\" reading of grid should be \"Confidence Interval\"", () => readingReads(page, "current column", el("grid"), "Confidence Interval"));
    await session.step(152, "And the \"cell type of VlaaiVis\" reading of grid should be \"vlaaivis\"", () => readingReads(page, "cell type of VlaaiVis", el("grid"), "vlaaivis"));
    await session.step(153, "And the \"cell type of Radar\" reading of grid should be \"radar\"", () => readingReads(page, "cell type of Radar", el("grid"), "radar"));
    await session.step(154, "And the \"cell type of Smart Form\" reading of grid should be \"smartform\"", () => readingReads(page, "cell type of Smart Form", el("grid"), "smartform"));
    await session.step(155, "And the \"cell type of Tags\" reading of grid should be \"tags\"", () => readingReads(page, "cell type of Tags", el("grid"), "tags"));
    await session.step(156, "And the \"cell type of Confidence Interval\" reading of grid should be \"confidenceinterval\"", () => readingReads(page, "cell type of Confidence Interval", el("grid"), "confidenceinterval"));
    await session.step(157, "When user saves the current view as project \"zz-grid-summary-columns\"", () => saveAsProject(page, "zz-grid-summary-columns"));
    await session.step(158, "And user closes all views", () => closeAllViews(page));
    await session.step(159, "And user opens the \"zz-grid-summary-columns\" project", () => openProject(page, "zz-grid-summary-columns"));
    await session.step(160, "Then the \"column order\" reading of grid should be \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Sparklines, Bar Chart, Pie Chart, VlaaiVis, Radar, Smart Form, Tags, Confidence Interval\"", () => readingReads(page, "column order", el("grid"), "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY, Sparklines, Bar Chart, Pie Chart, VlaaiVis, Radar, Smart Form, Tags, Confidence Interval"));
    await session.step(161, "And the \"cell type of Sparklines\" reading of grid should be \"sparkline\"", () => readingReads(page, "cell type of Sparklines", el("grid"), "sparkline"));
    await session.step(162, "And the \"cell type of Bar Chart\" reading of grid should be \"barchart\"", () => readingReads(page, "cell type of Bar Chart", el("grid"), "barchart"));
    await session.step(163, "And the \"cell type of Pie Chart\" reading of grid should be \"piechart\"", () => readingReads(page, "cell type of Pie Chart", el("grid"), "piechart"));
    await session.step(164, "When user clicks on the \"cell 2 of Pie Chart\" area of grid", () => clickArea(page, "cell 2 of Pie Chart", el("grid")));
    await session.step(165, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
    await session.step(166, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
    await session.step(167, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
    await session.step(168, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
    await session.step(169, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
    await session.step(170, "Then the \"current column\" reading of grid should be \"Confidence Interval\"", () => readingReads(page, "current column", el("grid"), "Confidence Interval"));
    await session.step(171, "And the \"cell type of VlaaiVis\" reading of grid should be \"vlaaivis\"", () => readingReads(page, "cell type of VlaaiVis", el("grid"), "vlaaivis"));
    await session.step(172, "And the \"cell type of Radar\" reading of grid should be \"radar\"", () => readingReads(page, "cell type of Radar", el("grid"), "radar"));
    await session.step(173, "And the \"cell type of Smart Form\" reading of grid should be \"smartform\"", () => readingReads(page, "cell type of Smart Form", el("grid"), "smartform"));
    await session.step(174, "And the \"cell type of Tags\" reading of grid should be \"tags\"", () => readingReads(page, "cell type of Tags", el("grid"), "tags"));
    await session.step(175, "And the \"cell type of Confidence Interval\" reading of grid should be \"confidenceinterval\"", () => readingReads(page, "cell type of Confidence Interval", el("grid"), "confidenceinterval"));
    await session.step(176, "And no errors should have been logged", () => noErrors(page));
  });
});
