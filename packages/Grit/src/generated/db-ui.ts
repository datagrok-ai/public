/**
This file is auto-generated by the grok api command (--ui) from databases/<schema>/schema.json manifests.
It is regenerated on every `grok api` run as long as it exists; delete it to opt out.
If you notice any changes, please push them to the repository.
Do not edit this file manually.
*/
import * as DG from 'datagrok-api/dg';
import {domainHandler, domains, DomainAppView, DomainAppViewOptions, DomainDb, DomainDialogOptions,
  DomainEntityAppView, DomainEntityAppViewOptions, DomainForm, DomainFormOptions, DomainGrid, DomainGridOptions,
  DomainTable, EntityListOptions, EntityListWidget} from '@datagrok-libraries/domain-ui';
import {gritDb, ProjectColumn, ProjectExpand, ProjectInsert, ProjectRow, StatusColumn, StatusExpand, StatusInsert,
  StatusRow, PriorityColumn, PriorityExpand, PriorityInsert, PriorityRow, IssueTypeColumn, IssueTypeExpand,
  IssueTypeInsert, IssueTypeRow, IssueColumn, IssueExpand, IssueInsert, IssueRow, CommentColumn, CommentExpand,
  CommentInsert, CommentRow, LabelColumn, LabelExpand, LabelInsert, LabelRow, IssueLabelColumn, IssueLabelExpand,
  IssueLabelInsert, IssueLabelRow} from './db';

/** Query spec of `grit.project` — columns and expand keys are compile-checked. */
export type ProjectQuerySpec = DG.DomainQuerySpec<ProjectColumn, keyof ProjectExpand & string>;

/** {@link DG.DomainQuery} parameters of `grit.project` (schema and table are implied). */
export interface ProjectQueryParams extends
    Omit<DG.DomainQueryParams, 'schema' | 'table' | 'columns'> {
  columns?: ProjectColumn[];
}

/** Options of {@link ProjectUi.form} / {@link ProjectUi.formDialog}. */
export interface ProjectFormOptions extends Omit<DomainFormOptions, 'values'> {
  values?: Partial<ProjectInsert>;
}

/** Options of {@link ProjectUi.grid}. */
export interface ProjectGridOptions extends Omit<DomainGridOptions, 'query' | 'defaults'> {
  query?: ProjectQuerySpec;
  defaults?: Partial<ProjectInsert>;
}

/** Options of {@link ProjectUi.list}. */
export interface ProjectListOptions extends Omit<EntityListOptions, 'query'> {
  query?: ProjectQuerySpec;
}

/** Options of {@link ProjectUi.listView} / {@link ProjectUi.app}. */
export interface ProjectAppViewOptions extends Omit<DomainAppViewOptions, 'query'> {
  query?: ProjectQuerySpec;
}

/**
 * Typed UI over `grit.project`: the reflective components of
 * `@datagrok-libraries/domain-ui`, with this table's columns and row type checked at
 * compile time. Reach it through {@link projectUi}.
 */
export class ProjectUi {
  /** The table address, `'<schema>.<table>'`. */
  readonly address: string = 'grit.project';

  /** The typed client — the same one `gritDb.projects` returns. */
  get client(): DG.DomainTableClient<ProjectRow, ProjectInsert, ProjectColumn, ProjectExpand> {
    return gritDb.projects;
  }

  /** The prefetched handle on `grit.project` — THE async boundary, typed. Every
   * widget factory on it is synchronous, so acquire it ONCE when a page builds
   * more than one widget; the shortcuts below acquire one of their own. */
  table(): Promise<DomainTable<ProjectRow, ProjectInsert, ProjectColumn, ProjectExpand>> {
    return domains.table<ProjectRow, ProjectInsert, ProjectColumn, ProjectExpand>(this.client);
  }

  /** The handler registered for the table (the reflective default when none is). */
  handler(): DG.DomainObjectHandler {
    return domainHandler(this.address);
  }

  /** The property form of ONE row — a new one by default, an existing one with
   * `{row}` or `{id}`; the values are this table's. */
  async form(options?: ProjectFormOptions): Promise<DomainForm> {
    return (await this.table()).form(options);
  }

  /** {@link form} in a dialog; resolves to whether a row was saved. */
  async formDialog(options?: ProjectFormOptions & DomainDialogOptions): Promise<boolean> {
    return (await this.table()).formDialog(options);
  }

  /** The browse/CRUD page: list, search, New, and a deep-linkable query. */
  async listView(options?: ProjectAppViewOptions): Promise<DomainAppView> {
    return (await this.table()).listView(options);
  }

  /** THE app — {@link listView} under the name that says what it is. */
  app(options?: ProjectAppViewOptions): Promise<DomainAppView> {
    return this.listView(options);
  }

  /** The row page: form, detail tabs, history. Takes the row or its id. */
  async entityView(row: string | DG.DomainRow,
    options?: DomainEntityAppViewOptions): Promise<DomainEntityAppView> {
    return (await this.table()).entityView(row, options);
  }

  /** An editable grid: batch editing, one-transaction save. */
  async grid(options?: ProjectGridOptions): Promise<DomainGrid> {
    return (await this.table()).grid(options);
  }

  /** A list of rows (cards / brief / grid). */
  async list(options?: ProjectListOptions): Promise<EntityListWidget> {
    return (await this.table()).list(options);
  }

  /** Opens the platform's create dialog, or the edit dialog for [row]. */
  edit(row?: DG.DomainRow): Promise<boolean> {
    return this.handler().editRow(row);
  }

  /** Opens the platform's row picker. */
  pick(): Promise<DG.DomainRow | null> {
    return this.handler().pickRow();
  }

  /** Wraps one `query()` row as a {@link DG.DomainRow} — locally, no round trip. */
  row(values: Partial<ProjectRow> | null): DG.DomainRow {
    return this.handler().rowFrom(values);
  }

  /** A serializable query over the table: deep links, saved filters, Open in Table View. */
  query(params?: ProjectQueryParams): DG.DomainQuery {
    return new DG.DomainQuery({...params, schema: 'grit', table: 'project'});
  }
}

/** Typed UI over `grit.project` (see {@link ProjectUi}). */
export const projectUi = new ProjectUi();

/** Query spec of `grit.status` — columns and expand keys are compile-checked. */
export type StatusQuerySpec = DG.DomainQuerySpec<StatusColumn, keyof StatusExpand & string>;

/** {@link DG.DomainQuery} parameters of `grit.status` (schema and table are implied). */
export interface StatusQueryParams extends
    Omit<DG.DomainQueryParams, 'schema' | 'table' | 'columns'> {
  columns?: StatusColumn[];
}

/** Options of {@link StatusUi.form} / {@link StatusUi.formDialog}. */
export interface StatusFormOptions extends Omit<DomainFormOptions, 'values'> {
  values?: Partial<StatusInsert>;
}

/** Options of {@link StatusUi.grid}. */
export interface StatusGridOptions extends Omit<DomainGridOptions, 'query' | 'defaults'> {
  query?: StatusQuerySpec;
  defaults?: Partial<StatusInsert>;
}

/** Options of {@link StatusUi.list}. */
export interface StatusListOptions extends Omit<EntityListOptions, 'query'> {
  query?: StatusQuerySpec;
}

/** Options of {@link StatusUi.listView} / {@link StatusUi.app}. */
export interface StatusAppViewOptions extends Omit<DomainAppViewOptions, 'query'> {
  query?: StatusQuerySpec;
}

/**
 * Typed UI over `grit.status`: the reflective components of
 * `@datagrok-libraries/domain-ui`, with this table's columns and row type checked at
 * compile time. Reach it through {@link statusUi}.
 */
export class StatusUi {
  /** The table address, `'<schema>.<table>'`. */
  readonly address: string = 'grit.status';

  /** The typed client — the same one `gritDb.statuses` returns. */
  get client(): DG.DomainTableClient<StatusRow, StatusInsert, StatusColumn, StatusExpand> {
    return gritDb.statuses;
  }

  /** The prefetched handle on `grit.status` — THE async boundary, typed. Every
   * widget factory on it is synchronous, so acquire it ONCE when a page builds
   * more than one widget; the shortcuts below acquire one of their own. */
  table(): Promise<DomainTable<StatusRow, StatusInsert, StatusColumn, StatusExpand>> {
    return domains.table<StatusRow, StatusInsert, StatusColumn, StatusExpand>(this.client);
  }

  /** The handler registered for the table (the reflective default when none is). */
  handler(): DG.DomainObjectHandler {
    return domainHandler(this.address);
  }

  /** The property form of ONE row — a new one by default, an existing one with
   * `{row}` or `{id}`; the values are this table's. */
  async form(options?: StatusFormOptions): Promise<DomainForm> {
    return (await this.table()).form(options);
  }

  /** {@link form} in a dialog; resolves to whether a row was saved. */
  async formDialog(options?: StatusFormOptions & DomainDialogOptions): Promise<boolean> {
    return (await this.table()).formDialog(options);
  }

  /** The browse/CRUD page: list, search, New, and a deep-linkable query. */
  async listView(options?: StatusAppViewOptions): Promise<DomainAppView> {
    return (await this.table()).listView(options);
  }

  /** THE app — {@link listView} under the name that says what it is. */
  app(options?: StatusAppViewOptions): Promise<DomainAppView> {
    return this.listView(options);
  }

  /** The row page: form, detail tabs, history. Takes the row or its id. */
  async entityView(row: string | DG.DomainRow,
    options?: DomainEntityAppViewOptions): Promise<DomainEntityAppView> {
    return (await this.table()).entityView(row, options);
  }

  /** An editable grid: batch editing, one-transaction save. */
  async grid(options?: StatusGridOptions): Promise<DomainGrid> {
    return (await this.table()).grid(options);
  }

  /** A list of rows (cards / brief / grid). */
  async list(options?: StatusListOptions): Promise<EntityListWidget> {
    return (await this.table()).list(options);
  }

  /** Opens the platform's create dialog, or the edit dialog for [row]. */
  edit(row?: DG.DomainRow): Promise<boolean> {
    return this.handler().editRow(row);
  }

  /** Opens the platform's row picker. */
  pick(): Promise<DG.DomainRow | null> {
    return this.handler().pickRow();
  }

  /** Wraps one `query()` row as a {@link DG.DomainRow} — locally, no round trip. */
  row(values: Partial<StatusRow> | null): DG.DomainRow {
    return this.handler().rowFrom(values);
  }

  /** A serializable query over the table: deep links, saved filters, Open in Table View. */
  query(params?: StatusQueryParams): DG.DomainQuery {
    return new DG.DomainQuery({...params, schema: 'grit', table: 'status'});
  }
}

/** Typed UI over `grit.status` (see {@link StatusUi}). */
export const statusUi = new StatusUi();

/** Query spec of `grit.priority` — columns and expand keys are compile-checked. */
export type PriorityQuerySpec = DG.DomainQuerySpec<PriorityColumn, keyof PriorityExpand & string>;

/** {@link DG.DomainQuery} parameters of `grit.priority` (schema and table are implied). */
export interface PriorityQueryParams extends
    Omit<DG.DomainQueryParams, 'schema' | 'table' | 'columns'> {
  columns?: PriorityColumn[];
}

/** Options of {@link PriorityUi.form} / {@link PriorityUi.formDialog}. */
export interface PriorityFormOptions extends Omit<DomainFormOptions, 'values'> {
  values?: Partial<PriorityInsert>;
}

/** Options of {@link PriorityUi.grid}. */
export interface PriorityGridOptions extends Omit<DomainGridOptions, 'query' | 'defaults'> {
  query?: PriorityQuerySpec;
  defaults?: Partial<PriorityInsert>;
}

/** Options of {@link PriorityUi.list}. */
export interface PriorityListOptions extends Omit<EntityListOptions, 'query'> {
  query?: PriorityQuerySpec;
}

/** Options of {@link PriorityUi.listView} / {@link PriorityUi.app}. */
export interface PriorityAppViewOptions extends Omit<DomainAppViewOptions, 'query'> {
  query?: PriorityQuerySpec;
}

/**
 * Typed UI over `grit.priority`: the reflective components of
 * `@datagrok-libraries/domain-ui`, with this table's columns and row type checked at
 * compile time. Reach it through {@link priorityUi}.
 */
export class PriorityUi {
  /** The table address, `'<schema>.<table>'`. */
  readonly address: string = 'grit.priority';

  /** The typed client — the same one `gritDb.priorities` returns. */
  get client(): DG.DomainTableClient<PriorityRow, PriorityInsert, PriorityColumn, PriorityExpand> {
    return gritDb.priorities;
  }

  /** The prefetched handle on `grit.priority` — THE async boundary, typed. Every
   * widget factory on it is synchronous, so acquire it ONCE when a page builds
   * more than one widget; the shortcuts below acquire one of their own. */
  table(): Promise<DomainTable<PriorityRow, PriorityInsert, PriorityColumn, PriorityExpand>> {
    return domains.table<PriorityRow, PriorityInsert, PriorityColumn, PriorityExpand>(this.client);
  }

  /** The handler registered for the table (the reflective default when none is). */
  handler(): DG.DomainObjectHandler {
    return domainHandler(this.address);
  }

  /** The property form of ONE row — a new one by default, an existing one with
   * `{row}` or `{id}`; the values are this table's. */
  async form(options?: PriorityFormOptions): Promise<DomainForm> {
    return (await this.table()).form(options);
  }

  /** {@link form} in a dialog; resolves to whether a row was saved. */
  async formDialog(options?: PriorityFormOptions & DomainDialogOptions): Promise<boolean> {
    return (await this.table()).formDialog(options);
  }

  /** The browse/CRUD page: list, search, New, and a deep-linkable query. */
  async listView(options?: PriorityAppViewOptions): Promise<DomainAppView> {
    return (await this.table()).listView(options);
  }

  /** THE app — {@link listView} under the name that says what it is. */
  app(options?: PriorityAppViewOptions): Promise<DomainAppView> {
    return this.listView(options);
  }

  /** The row page: form, detail tabs, history. Takes the row or its id. */
  async entityView(row: string | DG.DomainRow,
    options?: DomainEntityAppViewOptions): Promise<DomainEntityAppView> {
    return (await this.table()).entityView(row, options);
  }

  /** An editable grid: batch editing, one-transaction save. */
  async grid(options?: PriorityGridOptions): Promise<DomainGrid> {
    return (await this.table()).grid(options);
  }

  /** A list of rows (cards / brief / grid). */
  async list(options?: PriorityListOptions): Promise<EntityListWidget> {
    return (await this.table()).list(options);
  }

  /** Opens the platform's create dialog, or the edit dialog for [row]. */
  edit(row?: DG.DomainRow): Promise<boolean> {
    return this.handler().editRow(row);
  }

  /** Opens the platform's row picker. */
  pick(): Promise<DG.DomainRow | null> {
    return this.handler().pickRow();
  }

  /** Wraps one `query()` row as a {@link DG.DomainRow} — locally, no round trip. */
  row(values: Partial<PriorityRow> | null): DG.DomainRow {
    return this.handler().rowFrom(values);
  }

  /** A serializable query over the table: deep links, saved filters, Open in Table View. */
  query(params?: PriorityQueryParams): DG.DomainQuery {
    return new DG.DomainQuery({...params, schema: 'grit', table: 'priority'});
  }
}

/** Typed UI over `grit.priority` (see {@link PriorityUi}). */
export const priorityUi = new PriorityUi();

/** Query spec of `grit.issue_type` — columns and expand keys are compile-checked. */
export type IssueTypeQuerySpec = DG.DomainQuerySpec<IssueTypeColumn, keyof IssueTypeExpand & string>;

/** {@link DG.DomainQuery} parameters of `grit.issue_type` (schema and table are implied). */
export interface IssueTypeQueryParams extends
    Omit<DG.DomainQueryParams, 'schema' | 'table' | 'columns'> {
  columns?: IssueTypeColumn[];
}

/** Options of {@link IssueTypeUi.form} / {@link IssueTypeUi.formDialog}. */
export interface IssueTypeFormOptions extends Omit<DomainFormOptions, 'values'> {
  values?: Partial<IssueTypeInsert>;
}

/** Options of {@link IssueTypeUi.grid}. */
export interface IssueTypeGridOptions extends Omit<DomainGridOptions, 'query' | 'defaults'> {
  query?: IssueTypeQuerySpec;
  defaults?: Partial<IssueTypeInsert>;
}

/** Options of {@link IssueTypeUi.list}. */
export interface IssueTypeListOptions extends Omit<EntityListOptions, 'query'> {
  query?: IssueTypeQuerySpec;
}

/** Options of {@link IssueTypeUi.listView} / {@link IssueTypeUi.app}. */
export interface IssueTypeAppViewOptions extends Omit<DomainAppViewOptions, 'query'> {
  query?: IssueTypeQuerySpec;
}

/**
 * Typed UI over `grit.issue_type`: the reflective components of
 * `@datagrok-libraries/domain-ui`, with this table's columns and row type checked at
 * compile time. Reach it through {@link issueTypeUi}.
 */
export class IssueTypeUi {
  /** The table address, `'<schema>.<table>'`. */
  readonly address: string = 'grit.issue_type';

  /** The typed client — the same one `gritDb.issueTypes` returns. */
  get client(): DG.DomainTableClient<IssueTypeRow, IssueTypeInsert, IssueTypeColumn, IssueTypeExpand> {
    return gritDb.issueTypes;
  }

  /** The prefetched handle on `grit.issue_type` — THE async boundary, typed. Every
   * widget factory on it is synchronous, so acquire it ONCE when a page builds
   * more than one widget; the shortcuts below acquire one of their own. */
  table(): Promise<DomainTable<IssueTypeRow, IssueTypeInsert, IssueTypeColumn, IssueTypeExpand>> {
    return domains.table<IssueTypeRow, IssueTypeInsert, IssueTypeColumn, IssueTypeExpand>(this.client);
  }

  /** The handler registered for the table (the reflective default when none is). */
  handler(): DG.DomainObjectHandler {
    return domainHandler(this.address);
  }

  /** The property form of ONE row — a new one by default, an existing one with
   * `{row}` or `{id}`; the values are this table's. */
  async form(options?: IssueTypeFormOptions): Promise<DomainForm> {
    return (await this.table()).form(options);
  }

  /** {@link form} in a dialog; resolves to whether a row was saved. */
  async formDialog(options?: IssueTypeFormOptions & DomainDialogOptions): Promise<boolean> {
    return (await this.table()).formDialog(options);
  }

  /** The browse/CRUD page: list, search, New, and a deep-linkable query. */
  async listView(options?: IssueTypeAppViewOptions): Promise<DomainAppView> {
    return (await this.table()).listView(options);
  }

  /** THE app — {@link listView} under the name that says what it is. */
  app(options?: IssueTypeAppViewOptions): Promise<DomainAppView> {
    return this.listView(options);
  }

  /** The row page: form, detail tabs, history. Takes the row or its id. */
  async entityView(row: string | DG.DomainRow,
    options?: DomainEntityAppViewOptions): Promise<DomainEntityAppView> {
    return (await this.table()).entityView(row, options);
  }

  /** An editable grid: batch editing, one-transaction save. */
  async grid(options?: IssueTypeGridOptions): Promise<DomainGrid> {
    return (await this.table()).grid(options);
  }

  /** A list of rows (cards / brief / grid). */
  async list(options?: IssueTypeListOptions): Promise<EntityListWidget> {
    return (await this.table()).list(options);
  }

  /** Opens the platform's create dialog, or the edit dialog for [row]. */
  edit(row?: DG.DomainRow): Promise<boolean> {
    return this.handler().editRow(row);
  }

  /** Opens the platform's row picker. */
  pick(): Promise<DG.DomainRow | null> {
    return this.handler().pickRow();
  }

  /** Wraps one `query()` row as a {@link DG.DomainRow} — locally, no round trip. */
  row(values: Partial<IssueTypeRow> | null): DG.DomainRow {
    return this.handler().rowFrom(values);
  }

  /** A serializable query over the table: deep links, saved filters, Open in Table View. */
  query(params?: IssueTypeQueryParams): DG.DomainQuery {
    return new DG.DomainQuery({...params, schema: 'grit', table: 'issue_type'});
  }
}

/** Typed UI over `grit.issue_type` (see {@link IssueTypeUi}). */
export const issueTypeUi = new IssueTypeUi();

/** Query spec of `grit.issue` — columns and expand keys are compile-checked. */
export type IssueQuerySpec = DG.DomainQuerySpec<IssueColumn, keyof IssueExpand & string>;

/** {@link DG.DomainQuery} parameters of `grit.issue` (schema and table are implied). */
export interface IssueQueryParams extends
    Omit<DG.DomainQueryParams, 'schema' | 'table' | 'columns'> {
  columns?: IssueColumn[];
}

/** Options of {@link IssueUi.form} / {@link IssueUi.formDialog}. */
export interface IssueFormOptions extends Omit<DomainFormOptions, 'values'> {
  values?: Partial<IssueInsert>;
}

/** Options of {@link IssueUi.grid}. */
export interface IssueGridOptions extends Omit<DomainGridOptions, 'query' | 'defaults'> {
  query?: IssueQuerySpec;
  defaults?: Partial<IssueInsert>;
}

/** Options of {@link IssueUi.list}. */
export interface IssueListOptions extends Omit<EntityListOptions, 'query'> {
  query?: IssueQuerySpec;
}

/** Options of {@link IssueUi.listView} / {@link IssueUi.app}. */
export interface IssueAppViewOptions extends Omit<DomainAppViewOptions, 'query'> {
  query?: IssueQuerySpec;
}

/**
 * Typed UI over `grit.issue`: the reflective components of
 * `@datagrok-libraries/domain-ui`, with this table's columns and row type checked at
 * compile time. Reach it through {@link issueUi}.
 */
export class IssueUi {
  /** The table address, `'<schema>.<table>'`. */
  readonly address: string = 'grit.issue';

  /** The typed client — the same one `gritDb.issues` returns. */
  get client(): DG.DomainTableClient<IssueRow, IssueInsert, IssueColumn, IssueExpand> {
    return gritDb.issues;
  }

  /** The prefetched handle on `grit.issue` — THE async boundary, typed. Every
   * widget factory on it is synchronous, so acquire it ONCE when a page builds
   * more than one widget; the shortcuts below acquire one of their own. */
  table(): Promise<DomainTable<IssueRow, IssueInsert, IssueColumn, IssueExpand>> {
    return domains.table<IssueRow, IssueInsert, IssueColumn, IssueExpand>(this.client);
  }

  /** The handler registered for the table (the reflective default when none is). */
  handler(): DG.DomainObjectHandler {
    return domainHandler(this.address);
  }

  /** The property form of ONE row — a new one by default, an existing one with
   * `{row}` or `{id}`; the values are this table's. */
  async form(options?: IssueFormOptions): Promise<DomainForm> {
    return (await this.table()).form(options);
  }

  /** {@link form} in a dialog; resolves to whether a row was saved. */
  async formDialog(options?: IssueFormOptions & DomainDialogOptions): Promise<boolean> {
    return (await this.table()).formDialog(options);
  }

  /** The browse/CRUD page: list, search, New, and a deep-linkable query. */
  async listView(options?: IssueAppViewOptions): Promise<DomainAppView> {
    return (await this.table()).listView(options);
  }

  /** THE app — {@link listView} under the name that says what it is. */
  app(options?: IssueAppViewOptions): Promise<DomainAppView> {
    return this.listView(options);
  }

  /** The row page: form, detail tabs, history. Takes the row or its id. */
  async entityView(row: string | DG.DomainRow,
    options?: DomainEntityAppViewOptions): Promise<DomainEntityAppView> {
    return (await this.table()).entityView(row, options);
  }

  /** An editable grid: batch editing, one-transaction save. */
  async grid(options?: IssueGridOptions): Promise<DomainGrid> {
    return (await this.table()).grid(options);
  }

  /** A list of rows (cards / brief / grid). */
  async list(options?: IssueListOptions): Promise<EntityListWidget> {
    return (await this.table()).list(options);
  }

  /** Opens the platform's create dialog, or the edit dialog for [row]. */
  edit(row?: DG.DomainRow): Promise<boolean> {
    return this.handler().editRow(row);
  }

  /** Opens the platform's row picker. */
  pick(): Promise<DG.DomainRow | null> {
    return this.handler().pickRow();
  }

  /** Wraps one `query()` row as a {@link DG.DomainRow} — locally, no round trip. */
  row(values: Partial<IssueRow> | null): DG.DomainRow {
    return this.handler().rowFrom(values);
  }

  /** A serializable query over the table: deep links, saved filters, Open in Table View. */
  query(params?: IssueQueryParams): DG.DomainQuery {
    return new DG.DomainQuery({...params, schema: 'grit', table: 'issue'});
  }
}

/** Typed UI over `grit.issue` (see {@link IssueUi}). */
export const issueUi = new IssueUi();

/** Query spec of `grit.comment` — columns and expand keys are compile-checked. */
export type CommentQuerySpec = DG.DomainQuerySpec<CommentColumn, keyof CommentExpand & string>;

/** {@link DG.DomainQuery} parameters of `grit.comment` (schema and table are implied). */
export interface CommentQueryParams extends
    Omit<DG.DomainQueryParams, 'schema' | 'table' | 'columns'> {
  columns?: CommentColumn[];
}

/** Options of {@link CommentUi.form} / {@link CommentUi.formDialog}. */
export interface CommentFormOptions extends Omit<DomainFormOptions, 'values'> {
  values?: Partial<CommentInsert>;
}

/** Options of {@link CommentUi.grid}. */
export interface CommentGridOptions extends Omit<DomainGridOptions, 'query' | 'defaults'> {
  query?: CommentQuerySpec;
  defaults?: Partial<CommentInsert>;
}

/** Options of {@link CommentUi.list}. */
export interface CommentListOptions extends Omit<EntityListOptions, 'query'> {
  query?: CommentQuerySpec;
}

/** Options of {@link CommentUi.listView} / {@link CommentUi.app}. */
export interface CommentAppViewOptions extends Omit<DomainAppViewOptions, 'query'> {
  query?: CommentQuerySpec;
}

/**
 * Typed UI over `grit.comment`: the reflective components of
 * `@datagrok-libraries/domain-ui`, with this table's columns and row type checked at
 * compile time. Reach it through {@link commentUi}.
 */
export class CommentUi {
  /** The table address, `'<schema>.<table>'`. */
  readonly address: string = 'grit.comment';

  /** The typed client — the same one `gritDb.comments` returns. */
  get client(): DG.DomainTableClient<CommentRow, CommentInsert, CommentColumn, CommentExpand> {
    return gritDb.comments;
  }

  /** The prefetched handle on `grit.comment` — THE async boundary, typed. Every
   * widget factory on it is synchronous, so acquire it ONCE when a page builds
   * more than one widget; the shortcuts below acquire one of their own. */
  table(): Promise<DomainTable<CommentRow, CommentInsert, CommentColumn, CommentExpand>> {
    return domains.table<CommentRow, CommentInsert, CommentColumn, CommentExpand>(this.client);
  }

  /** The handler registered for the table (the reflective default when none is). */
  handler(): DG.DomainObjectHandler {
    return domainHandler(this.address);
  }

  /** The property form of ONE row — a new one by default, an existing one with
   * `{row}` or `{id}`; the values are this table's. */
  async form(options?: CommentFormOptions): Promise<DomainForm> {
    return (await this.table()).form(options);
  }

  /** {@link form} in a dialog; resolves to whether a row was saved. */
  async formDialog(options?: CommentFormOptions & DomainDialogOptions): Promise<boolean> {
    return (await this.table()).formDialog(options);
  }

  /** The browse/CRUD page: list, search, New, and a deep-linkable query. */
  async listView(options?: CommentAppViewOptions): Promise<DomainAppView> {
    return (await this.table()).listView(options);
  }

  /** THE app — {@link listView} under the name that says what it is. */
  app(options?: CommentAppViewOptions): Promise<DomainAppView> {
    return this.listView(options);
  }

  /** The row page: form, detail tabs, history. Takes the row or its id. */
  async entityView(row: string | DG.DomainRow,
    options?: DomainEntityAppViewOptions): Promise<DomainEntityAppView> {
    return (await this.table()).entityView(row, options);
  }

  /** An editable grid: batch editing, one-transaction save. */
  async grid(options?: CommentGridOptions): Promise<DomainGrid> {
    return (await this.table()).grid(options);
  }

  /** A list of rows (cards / brief / grid). */
  async list(options?: CommentListOptions): Promise<EntityListWidget> {
    return (await this.table()).list(options);
  }

  /** Opens the platform's create dialog, or the edit dialog for [row]. */
  edit(row?: DG.DomainRow): Promise<boolean> {
    return this.handler().editRow(row);
  }

  /** Opens the platform's row picker. */
  pick(): Promise<DG.DomainRow | null> {
    return this.handler().pickRow();
  }

  /** Wraps one `query()` row as a {@link DG.DomainRow} — locally, no round trip. */
  row(values: Partial<CommentRow> | null): DG.DomainRow {
    return this.handler().rowFrom(values);
  }

  /** A serializable query over the table: deep links, saved filters, Open in Table View. */
  query(params?: CommentQueryParams): DG.DomainQuery {
    return new DG.DomainQuery({...params, schema: 'grit', table: 'comment'});
  }
}

/** Typed UI over `grit.comment` (see {@link CommentUi}). */
export const commentUi = new CommentUi();

/** Query spec of `grit.label` — columns and expand keys are compile-checked. */
export type LabelQuerySpec = DG.DomainQuerySpec<LabelColumn, keyof LabelExpand & string>;

/** {@link DG.DomainQuery} parameters of `grit.label` (schema and table are implied). */
export interface LabelQueryParams extends
    Omit<DG.DomainQueryParams, 'schema' | 'table' | 'columns'> {
  columns?: LabelColumn[];
}

/** Options of {@link LabelUi.form} / {@link LabelUi.formDialog}. */
export interface LabelFormOptions extends Omit<DomainFormOptions, 'values'> {
  values?: Partial<LabelInsert>;
}

/** Options of {@link LabelUi.grid}. */
export interface LabelGridOptions extends Omit<DomainGridOptions, 'query' | 'defaults'> {
  query?: LabelQuerySpec;
  defaults?: Partial<LabelInsert>;
}

/** Options of {@link LabelUi.list}. */
export interface LabelListOptions extends Omit<EntityListOptions, 'query'> {
  query?: LabelQuerySpec;
}

/** Options of {@link LabelUi.listView} / {@link LabelUi.app}. */
export interface LabelAppViewOptions extends Omit<DomainAppViewOptions, 'query'> {
  query?: LabelQuerySpec;
}

/**
 * Typed UI over `grit.label`: the reflective components of
 * `@datagrok-libraries/domain-ui`, with this table's columns and row type checked at
 * compile time. Reach it through {@link labelUi}.
 */
export class LabelUi {
  /** The table address, `'<schema>.<table>'`. */
  readonly address: string = 'grit.label';

  /** The typed client — the same one `gritDb.labels` returns. */
  get client(): DG.DomainTableClient<LabelRow, LabelInsert, LabelColumn, LabelExpand> {
    return gritDb.labels;
  }

  /** The prefetched handle on `grit.label` — THE async boundary, typed. Every
   * widget factory on it is synchronous, so acquire it ONCE when a page builds
   * more than one widget; the shortcuts below acquire one of their own. */
  table(): Promise<DomainTable<LabelRow, LabelInsert, LabelColumn, LabelExpand>> {
    return domains.table<LabelRow, LabelInsert, LabelColumn, LabelExpand>(this.client);
  }

  /** The handler registered for the table (the reflective default when none is). */
  handler(): DG.DomainObjectHandler {
    return domainHandler(this.address);
  }

  /** The property form of ONE row — a new one by default, an existing one with
   * `{row}` or `{id}`; the values are this table's. */
  async form(options?: LabelFormOptions): Promise<DomainForm> {
    return (await this.table()).form(options);
  }

  /** {@link form} in a dialog; resolves to whether a row was saved. */
  async formDialog(options?: LabelFormOptions & DomainDialogOptions): Promise<boolean> {
    return (await this.table()).formDialog(options);
  }

  /** The browse/CRUD page: list, search, New, and a deep-linkable query. */
  async listView(options?: LabelAppViewOptions): Promise<DomainAppView> {
    return (await this.table()).listView(options);
  }

  /** THE app — {@link listView} under the name that says what it is. */
  app(options?: LabelAppViewOptions): Promise<DomainAppView> {
    return this.listView(options);
  }

  /** The row page: form, detail tabs, history. Takes the row or its id. */
  async entityView(row: string | DG.DomainRow,
    options?: DomainEntityAppViewOptions): Promise<DomainEntityAppView> {
    return (await this.table()).entityView(row, options);
  }

  /** An editable grid: batch editing, one-transaction save. */
  async grid(options?: LabelGridOptions): Promise<DomainGrid> {
    return (await this.table()).grid(options);
  }

  /** A list of rows (cards / brief / grid). */
  async list(options?: LabelListOptions): Promise<EntityListWidget> {
    return (await this.table()).list(options);
  }

  /** Opens the platform's create dialog, or the edit dialog for [row]. */
  edit(row?: DG.DomainRow): Promise<boolean> {
    return this.handler().editRow(row);
  }

  /** Opens the platform's row picker. */
  pick(): Promise<DG.DomainRow | null> {
    return this.handler().pickRow();
  }

  /** Wraps one `query()` row as a {@link DG.DomainRow} — locally, no round trip. */
  row(values: Partial<LabelRow> | null): DG.DomainRow {
    return this.handler().rowFrom(values);
  }

  /** A serializable query over the table: deep links, saved filters, Open in Table View. */
  query(params?: LabelQueryParams): DG.DomainQuery {
    return new DG.DomainQuery({...params, schema: 'grit', table: 'label'});
  }
}

/** Typed UI over `grit.label` (see {@link LabelUi}). */
export const labelUi = new LabelUi();

/** Query spec of `grit.issue_label` — columns and expand keys are compile-checked. */
export type IssueLabelQuerySpec = DG.DomainQuerySpec<IssueLabelColumn, keyof IssueLabelExpand & string>;

/** {@link DG.DomainQuery} parameters of `grit.issue_label` (schema and table are implied). */
export interface IssueLabelQueryParams extends
    Omit<DG.DomainQueryParams, 'schema' | 'table' | 'columns'> {
  columns?: IssueLabelColumn[];
}

/** Options of {@link IssueLabelUi.form} / {@link IssueLabelUi.formDialog}. */
export interface IssueLabelFormOptions extends Omit<DomainFormOptions, 'values'> {
  values?: Partial<IssueLabelInsert>;
}

/** Options of {@link IssueLabelUi.grid}. */
export interface IssueLabelGridOptions extends Omit<DomainGridOptions, 'query' | 'defaults'> {
  query?: IssueLabelQuerySpec;
  defaults?: Partial<IssueLabelInsert>;
}

/** Options of {@link IssueLabelUi.list}. */
export interface IssueLabelListOptions extends Omit<EntityListOptions, 'query'> {
  query?: IssueLabelQuerySpec;
}

/** Options of {@link IssueLabelUi.listView} / {@link IssueLabelUi.app}. */
export interface IssueLabelAppViewOptions extends Omit<DomainAppViewOptions, 'query'> {
  query?: IssueLabelQuerySpec;
}

/**
 * Typed UI over `grit.issue_label`: the reflective components of
 * `@datagrok-libraries/domain-ui`, with this table's columns and row type checked at
 * compile time. Reach it through {@link issueLabelUi}.
 */
export class IssueLabelUi {
  /** The table address, `'<schema>.<table>'`. */
  readonly address: string = 'grit.issue_label';

  /** The typed client — the same one `gritDb.issueLabels` returns. */
  get client(): DG.DomainTableClient<IssueLabelRow, IssueLabelInsert, IssueLabelColumn, IssueLabelExpand> {
    return gritDb.issueLabels;
  }

  /** The prefetched handle on `grit.issue_label` — THE async boundary, typed. Every
   * widget factory on it is synchronous, so acquire it ONCE when a page builds
   * more than one widget; the shortcuts below acquire one of their own. */
  table(): Promise<DomainTable<IssueLabelRow, IssueLabelInsert, IssueLabelColumn, IssueLabelExpand>> {
    return domains.table<IssueLabelRow, IssueLabelInsert, IssueLabelColumn, IssueLabelExpand>(this.client);
  }

  /** The handler registered for the table (the reflective default when none is). */
  handler(): DG.DomainObjectHandler {
    return domainHandler(this.address);
  }

  /** The property form of ONE row — a new one by default, an existing one with
   * `{row}` or `{id}`; the values are this table's. */
  async form(options?: IssueLabelFormOptions): Promise<DomainForm> {
    return (await this.table()).form(options);
  }

  /** {@link form} in a dialog; resolves to whether a row was saved. */
  async formDialog(options?: IssueLabelFormOptions & DomainDialogOptions): Promise<boolean> {
    return (await this.table()).formDialog(options);
  }

  /** The browse/CRUD page: list, search, New, and a deep-linkable query. */
  async listView(options?: IssueLabelAppViewOptions): Promise<DomainAppView> {
    return (await this.table()).listView(options);
  }

  /** THE app — {@link listView} under the name that says what it is. */
  app(options?: IssueLabelAppViewOptions): Promise<DomainAppView> {
    return this.listView(options);
  }

  /** The row page: form, detail tabs, history. Takes the row or its id. */
  async entityView(row: string | DG.DomainRow,
    options?: DomainEntityAppViewOptions): Promise<DomainEntityAppView> {
    return (await this.table()).entityView(row, options);
  }

  /** An editable grid: batch editing, one-transaction save. */
  async grid(options?: IssueLabelGridOptions): Promise<DomainGrid> {
    return (await this.table()).grid(options);
  }

  /** A list of rows (cards / brief / grid). */
  async list(options?: IssueLabelListOptions): Promise<EntityListWidget> {
    return (await this.table()).list(options);
  }

  /** Opens the platform's create dialog, or the edit dialog for [row]. */
  edit(row?: DG.DomainRow): Promise<boolean> {
    return this.handler().editRow(row);
  }

  /** Opens the platform's row picker. */
  pick(): Promise<DG.DomainRow | null> {
    return this.handler().pickRow();
  }

  /** Wraps one `query()` row as a {@link DG.DomainRow} — locally, no round trip. */
  row(values: Partial<IssueLabelRow> | null): DG.DomainRow {
    return this.handler().rowFrom(values);
  }

  /** A serializable query over the table: deep links, saved filters, Open in Table View. */
  query(params?: IssueLabelQueryParams): DG.DomainQuery {
    return new DG.DomainQuery({...params, schema: 'grit', table: 'issue_label'});
  }
}

/** Typed UI over `grit.issue_label` (see {@link IssueLabelUi}). */
export const issueLabelUi = new IssueLabelUi();

/**
 * The typed schema handle over `grit`: one prefetched {@link DomainTable}
 * per table, resolved together by {@link gritUiDb} —
 * the schema-level async boundary (see `domains.db`).
 */
export interface GritUiDb extends DomainDb {
  readonly projects: DomainTable<ProjectRow, ProjectInsert, ProjectColumn, ProjectExpand>;
  readonly statuses: DomainTable<StatusRow, StatusInsert, StatusColumn, StatusExpand>;
  readonly priorities: DomainTable<PriorityRow, PriorityInsert, PriorityColumn, PriorityExpand>;
  readonly issueTypes: DomainTable<IssueTypeRow, IssueTypeInsert, IssueTypeColumn, IssueTypeExpand>;
  readonly issues: DomainTable<IssueRow, IssueInsert, IssueColumn, IssueExpand>;
  readonly comments: DomainTable<CommentRow, CommentInsert, CommentColumn, CommentExpand>;
  readonly labels: DomainTable<LabelRow, LabelInsert, LabelColumn, LabelExpand>;
  readonly issueLabels: DomainTable<IssueLabelRow, IssueLabelInsert, IssueLabelColumn, IssueLabelExpand>;
}

/** Acquires the {@link GritUiDb} handle — every table of
 * `grit`, prefetched together (see `domains.db`). */
export function gritUiDb(): Promise<GritUiDb> {
  return domains.db('grit') as Promise<GritUiDb>;
}
