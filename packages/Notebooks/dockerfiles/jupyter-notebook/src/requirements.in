# Jupyter notebook requirements
jupyter-contrib-nbextensions==0.7.0
    # Collection of extensions that add functionality to the Jupyter notebook.

# Jupyter requirements
jupyter==1.0.0
    #  notebook, qtconsole, and the IPython kernel in one package
notebook==6.5.7
    # Jupyter Notebook
    # 6.5.7 lifts the pyzmq<25 cap of 6.5.6
nbconvert>=7.17
    # Jupyter Notebook Conversion to other formats
nbformat==5.9.1
    # Jupyter Notebook format

# DataGrok API
datagrok-api==0.1.0
    # DataGrok API client library (pinned)

# NLP features
# textract
    # Extract text from any document
    # 1.6.3 (latest on 15/07/2021 ) version uses the outdated version of six package
    # textract package needs to be installed before requirements to avoid dependencies hell
clean-text[GPL]==0.6.0
    # Preprocess your scraped data to create a normalized text representation
flair>=0.15.1
    # NLP
konoha>=5.7
    # Requirement for flair
    # 4.6.3 pinned overrides<4 which conflicts with jupyter-server>=2.20 (overrides>=5)
gensim==4.3.1
    # NLP
spacy==3.6.1
    # NLP
    # 3.6.1 adds pydantic v2 compatibility (pydantic 2 is forced by mlflow 3)
nltk>=3.9.4
    # NL Toolkit
pycountry==22.3.5
    # ISO databases
# pycld3 and gcld3 are installed in the Dockerfile: they ship no cp311 wheels and
# their sdists need protoc and --no-build-isolation to build


# Common python libraries for Scripts
keras>=3.13.2
    # Deep learning API
efficientnet==1.1.1
    # Keras reimplementation of EfficientNet
hdbscan>=0.8.40
    # Data clustering
image==1.5.33
    # Operations with images and videos
opencv-python>=4.8.1.78
    # Computer vision
git+https://github.com/MPBA/pyphysio.git
    # Analysis of physiological signals
matplotlib==3.7.2
    # Plotting library for creating visualizations
mlflow>=3.11
    # ML lifecycle
networkx==3.1
    # Studying graphs and networks
numpy==1.26.4
    # Support for large, multi-dimensional arrays and matrices
    # tensorflow>=2.20 needs >=1.26; pandas 2.0.3 and scipy 1.10.1 need <2
pandas==2.0.3
    # Data structures and operations for manipulating numerical tables and time series
patsy==0.5.3
    # Describing statistical models and building design matrices
plotly==5.15.0
    # The front end for ML and data science models
scipy==1.10.1
    # Scientific and technical computing
statsmodels==0.14.0
    # Statistical computations
seaborn==0.12.2
    # Data visualization library
sympy>=1.13.3
    # Symbolic computation
    # torch>=2.6 requires sympy>=1.13.3
tensorflow>=2.20
    # ML
torch==2.13.0+cpu
    # ML
torchvision==0.28.0+cpu
    # Datasets, model architectures, and common image transformations for computer vision
umap-learn>=0.5.7
    # dimension reduction technique
descriptastorus==2.6.0
    # Features generator for chemprop
rdkit==2023.3.2
    # Frequently used Cheminformatics Software
    # Required by descriptastorus, chemprop
scikit-learn>=1.5
    # ML Software
    # Required by descriptastorus
# kyotocabinet-python is installed in the Dockerfile: its setup.py relies on
# `from distutils.core import *` leaking `os` (py<=3.8 stdlib distutils only)
# and needs a patch + the installed core kyotocabinet C library

mistune>=2.0.3

# Security floors for transitive dependencies (VEX, GROK-18695)
bleach>=6.4
cryptography>=48.0.1
filelock>=3.20.3
fonttools>=4.60.2
gdown>=5.2.2
jupyter-server>=2.20
markdown>=3.8.1
pdfminer.six>=20251230
pillow>=12.2
protobuf>=5.29.6
pyarrow>=14.0.1
pygments>=2.20
pyjwt>=2.13
requests>=2.33
sentencepiece>=0.2.1
tornado>=6.5.7
transformers>=4.48
urllib3>=2.7

# Grok Helper
gunicorn>=22
pyyaml
Flask>=3.1.3
werkzeug>=3.1.6
