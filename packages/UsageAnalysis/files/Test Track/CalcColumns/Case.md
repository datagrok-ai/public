Open cars.csv and use the following formula for Add new column:

 case ${turbo} when 1 then "fast" else "slow" end

(TODO: Add expected result, how should the resulted column look like)

---
{
  "order": 5
}
