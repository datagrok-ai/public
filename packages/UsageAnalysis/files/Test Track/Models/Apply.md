#### Apply predictive model
1. Open demog.csv (from Demo files or by clicking 'Star' icon);
2. Go to **ML > Models > Apply Model...**. "Apply predictive model" dialog opens;
1. "Select the 'TestDemog' model;
3. Once the application process is complete, verify that the prediction column has been added to the table.

---
{
  "order": 2,
  "datasets": ["System:DemoFiles/demog.csv"]
}