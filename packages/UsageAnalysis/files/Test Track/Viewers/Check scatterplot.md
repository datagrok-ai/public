(use datasets SPGI_v2, BrokenScatterPlots, OOMscatter.plot.small, scatter.plot.broken2, ForDG.csv):
1. log scale + Layout
2. invert axes + slider + Layout
3. log scale + invert axes + slider + Layout
---
{
  "order": 13
}