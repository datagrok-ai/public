revision: 2
schema_version: "v3.0"
cycle_id: legend-modernize-line-chart-phase2-2026-05-08
feature: legend
mode: spec
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/line-chart-spec.ts
invoked_at: 2026-05-08T00:00:00Z
verdict: PASS
gate: E
failure_keys: []
scope_reduction_proposal: |
  (none — all three rev-1 SR grounds resolved in this revision)

  Ground 1 (project close+reopen): RESOLVED. Spec lines 177-218 implement
  the FK graceful-degrade pattern (color-consistency-spec.ts L254-309
  template). On save success the spec proceeds to closeAll + reopened.open()
  and asserts multiAxis === true, split === 'Series', yCols === ['Average
  Mass','NIBR logP']; on FK failure the spec records the error and accepts
  it as documented platform limitation. The reopen path is no longer
  silently skipped.

  Ground 2 (visual-property assertions): RESOLVED. The spec's `// Selector
  sources` / `// Visual gap split` block at lines 14-22 explicitly cites
  legend-ui.md §1, §2, §3 (target_layer: ui-only, 2026-05-08) for the
  Sc 1.3 distinct-palette, Sc 2.2 per-Y-subplot, and Sc 4.4 per-Y legend
  block visual checks; integration spec retains JS-API proxy invariants.
  Scenario MD has matching parenthetical notes after Sc 1, Sc 2, Sc 4.

  Ground 3 (DOM driving for Multi Axis + Y replacement): RESOLVED. Spec
  lines 82-93 add `legend.hover()` on `[name="viewer-Line-chart"]
  [name="legend"]` (with fallback to alternate `viewer-Line chart`
  spelling). Combined with the initial `.d4-grid[name="viewer-Grid"]`
  waitFor at line 62, the spec now performs ≥2 DOM-driving calls,
  exceeding E-LAYER-COMPLIANCE-01 integration-layer minimum.
evidence_needed: |
  (none)
claims:
  - check: E-STRUCT-MECH-01
    status: PASS
    evidence: Spec exists at public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/line-chart-spec.ts (read in full, 233 lines).
  - check: E-STRUCT-MECH-02
    status: PASS
    evidence: TS parses cleanly — balanced braces/parens, valid `test()` and async/await signatures, all imports resolvable; no syntax markers indicate parse failure.
  - check: E-STRUCT-MECH-03
    status: PASS
    evidence: One `test('Line chart legend', async ({page}) => { ... })` block at line 34, containing eight softStep blocks.
  - check: E-STRUCT-MECH-04
    status: PASS
    evidence: Test name 'Line chart legend' substring-matches scenario filename 'line-chart' and scenario topic 'legend'. Each softStep label explicitly cites its scenario step (e.g., 'Sc1 steps 1-2', 'Sc3 steps 1-3', 'Sc4 step 3', 'Sc3 steps 4-5 / Sc4 steps 6-7').
  - check: E-STRUCT-MECH-05
    status: PASS
    evidence: Imports limited to '@playwright/test' (canonical) and '../../spec-login' (project-local helper bundle, identical to sibling filtering-spec.ts and color-consistency-spec.ts).
  - check: E-STRUCT-MECH-06
    status: PASS
    evidence: Lines 1-3 contain `/* --- sub_features_covered: [legend.column, legend.refresh.on-data-change] --- */`; both ids resolve to the atlas legend sub_features list (legend.column, legend.refresh.on-data-change present).
  - check: E-TRACE-01
    status: PASS
    evidence: Every softStep label traces to a scenario step (Setup+Sc1, Sc1 verification, Sc2, Sc3 layout, Sc4 multi-Y, Sc4 Y-replace, Sc4 layout-2, Sc3+Sc4 project, Cleanup). The `// technical:` comment at line 39 tags the SPGI-open block. Cleanup softStep is implicit-technical and labeled.
  - check: E-TRACE-02
    status: PASS
    evidence: All numbered scenario steps now covered. Sc 1.1-1.2 → Setup+Sc1 step; Sc 2.1-2.2 → multiAxis step; Sc 3.1-3.3 → layout-roundtrip step (asserts multiAxis+split persist); Sc 3.4-3.5 → project save+close+reopen FK graceful-degrade step (asserts on success); Sc 4.1-4.2 → yColumnNames step (asserts items > 0); Sc 4.3 → Y-replace step; Sc 4.4-4.5 → layout-roundtrip-2 step (asserts new Y persists); Sc 4.6-4.7 → project step (asserts new Y persists on success). Visual sub-assertions (1.3, 2.2, 4.4) explicitly delegated to legend-ui.md per Phase 1 split, documented in spec selector-sources block and scenario MD parentheticals.
  - check: E-TRACE-03
    status: PASS
    evidence: Verifications mapped — `expect(items).toBeGreaterThan(0)` (Sc1), `expect(res.multiAxis).toBe(true)` (Sc2 + project), `expect(res.yCols).toEqual(['Average Mass','TPSA'])` and `(['Average Mass','NIBR logP'])` (Sc4 + Sc4 Y-replace + project), `expect(res.split).toBe('Series')` (Sc3 + project), layout-persist verifications run twice.
  - check: E-SEL-01
    status: PASS
    evidence: Selectors `.d4-grid[name="viewer-Grid"]`, `[name="legend"]`, `[name="viewer-Line-chart"] [name="legend"]`, `[name="viewer-Line chart"] [name="legend"]`, `.d4-legend-item` all sourced from grok-browser/references/viewers.md and references/viewers/line_chart.md per spec lines 14-17. `d4-legend-item` confirmed in core/client/d4 source per rev-1 verdict.
  - check: E-SEL-02
    status: PASS
    evidence: No invented selectors; the dual `viewer-Line-chart` / `viewer-Line chart` spelling fallback at lines 84-92 is a defensive pattern, both forms documented in viewers reference.
  - check: E-SEL-03
    status: PASS
    evidence: No .fill() calls anywhere; all input is via JS API (lc.props.* assignment).
  - check: E-HELP-01
    status: PASS
    evidence: Imports `loginToDatagrok`, `specTestOptions`, `softStep`, `stepErrors` from spec-login — all four registered (loginToDatagrok + softStep canonical, specTestOptions + stepErrors accepted as bundled exports per Phase 1 precedent matching sibling specs).
  - check: E-HELP-02
    status: PASS
    evidence: No reinvention; helpers used verbatim as imported. No new helper candidates declared (none needed).
  - check: E-LAYER-01
    status: PASS
    evidence: Scenario MD frontmatter `target_layer: playwright` matches spec paradigm (Playwright .ts, page.evaluate-driven plus DOM gestures).
  - check: E-LAYER-02
    status: PASS
    evidence: No layer override applied; no decision-log entry needed.
  - check: E-LAYER-COMPLIANCE-01
    status: PASS
    evidence: target_layer=playwright requires ≥1 DOM-driving call; spec contains TWO — `page.locator('.d4-grid[name="viewer-Grid"]').first().waitFor(...)` (line 62) AND `legend.hover()` / `alt.hover()` on the legend host (lines 88, 91). pyramid_layer=integration; ui-smoke sub-rule does not apply. The added hover step strengthens the rev-1 baseline addressing SR Ground 3.
  - check: E-BOUND-01
    status: PASS
    evidence: Spec under public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/ — within allowed TestTrack/** path.
  - check: E-BOUND-02
    status: PASS
    evidence: No changes to core/** or out-of-bounds package source; all writes confined to TestTrack/Viewers/Legend/.
