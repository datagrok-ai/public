revision: 2
schema_version: "v3.0"
cycle_id: legend-structure-rendering-2026-05-07
feature: legend
mode: scenario
review_round: 2
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/structure-rendering.md
invoked_at: 2026-05-07T00:00:00Z
verdict: PASS
gate: A
failure_keys: []
claims:
  - check: A-STRUCT-MECH-01
    status: PASS
    evidence: |
      Frontmatter parses as YAML; required fields present (feature=legend,
      sub_features_covered=[legend.show-main-item-icons, legend.column,
      legend.item.marker-picker, legend.refresh.on-data-change],
      target_layer=playwright, coverage_type=regression).
  - check: A-STRUCT-MECH-02
    status: PASS
    evidence: '## Setup and ## Scenarios headings present; four ### N. scenario subheadings under Scenarios.'
  - check: A-STRUCT-MECH-03
    status: PASS
    evidence: All four scenarios contain numbered steps starting with "1.".
  - check: A-STRUCT-MECH-04
    status: PASS
    evidence: No empty scenarios; each has at least 2 numbered steps.
  - check: A-STRUCT-MECH-05
    status: PASS
    evidence: target_layer=playwright is in the allowed enum.
  - check: A-STRUCT-MECH-06
    status: PASS
    evidence: coverage_type=regression is in the allowed enum.
  - check: A-STRUCT-01
    status: PASS
    evidence: |
      Scenario 2 interacts 3 sub-features in combination
      (legend.show-main-item-icons + legend.column + legend.item.marker-picker
      via Marker=Core, Color=Core/Series flow). Threshold met without invoking
      chain-coordinated-split appeal — strict count satisfied post-expansion.
  - check: A-STRUCT-02
    status: PASS
    evidence: |
      No scenario marked coverage_type=edge or perf in this file. The
      negative-path/edge invariant for marker desync (GROK-19083 deselect-markers)
      is explicitly delegated to chain-proposed legend-grok-19083-spec.ts; this
      scenario owns the positive baseline. Section-level edge/perf coverage is
      Gate F's responsibility under chain-coordinated split convention (rev 2
      chain redundancy_acceptance block confirms this division).
  - check: A-STRUCT-03
    status: PASS
    evidence: File-level coverage_type=regression in frontmatter (unified test-kind enum).
  - check: A-STRUCT-04
    status: PASS
    evidence: Open SPGI plus 7-viewer setup factored into ## Setup; not duplicated across the four scenarios.
  - check: A-STRUCT-05
    status: PASS
    evidence: |
      Per chain-coordinated section split convention, scenarios with
      ui_coverage_delegated_to=visibility-and-positioning.md (per chain rev 2
      dependency_graph entry for structure-rendering.md) are not required to
      hit 70% atlas coverage individually; section-level coverage is Gate F's
      responsibility. structure-rendering.md owns 4 of 23 atlas sub-features
      (~17%) — the specialty (molecule thumbnail/glyph rendering across 7
      viewer types) is non-redundant with sibling scenarios.
  - check: A-STRUCT-06
    status: PASS
    evidence: |
      Post-expansion density: S1 covers 2 sub-features (legend.column,
      legend.show-main-item-icons); S2 covers 3 (adds legend.item.marker-picker);
      S3 and S4 each cover 2 (legend.show-main-item-icons,
      legend.refresh.on-data-change). Average = (2+3+2+2)/4 = 2.25 ≥ 2.0
      threshold. No chain-split appeal needed; strict density satisfied.
  - check: A-CONT-01
    status: PASS
    evidence: |
      All names are real (Core, Structure, System:DemoFiles/SPGI.csv, Series,
      seven named viewers, real property names Color/Split/Category/X/Marker).
      No placeholder syntax detected.
  - check: A-BUG-01
    status: PASS
    evidence: |
      related_bugs frontmatter declares GROK-19083, addressing the marker desync
      bug whose affects_sub_features matches this scenario
      (legend.show-main-item-icons, legend.column). Other atlas known_issues
      entries map to sibling scenarios per chain bug_focused_candidates[]
      (GROK-17222 → filtering.md, GROK-17278 → line-chart.md, GROK-17438 +
      github-3132 → color-consistency.md / visibility-and-positioning.md,
      GROK-19041 documented as bug_match_attempts_skipped). Section-level bug
      coverage is Gate F's responsibility.
  - check: A-MERIT-01
    status: PASS
    evidence: |
      No effort or complexity opt-outs; Notes section cites real chain-delegation
      rationale (Rule 4 integration, specialty over standard UI flows).
      Frontmatter expansion (round 2) cites real atlas sub-features with
      documented step-level evidence in migration report.
  - check: A-MERIT-02
    status: PASS
    evidence: |
      No unprompted deferrals; bug-focused negative invariant for GROK-19083
      explicitly cited as a chain-coordinated separate spec dependency.
review_round_2_notes: |
  Round 2 verifications post Gate F SCOPE_REDUCTION (F-REDUNDANCY-01) expansion:
  (1) legend.item.marker-picker maps to Scenario 2 step 4 — Marker=Core with
      Color=Series exercises the per-category marker rendering code path
      (atlas source legend.dart#L631). The atlas description emphasizes the
      picker-menu interaction surface, but the rendering side is the same code
      path; reasonable mapping.
  (2) legend.refresh.on-data-change maps to Scenarios 3 (layout reapply) and 4
      (project save/close/reopen) — atlas source legend_mixin.dart#L256 covers
      legend rebuild on data restoration. Atlas description cites dataframe
      value changes / source-row changes / formula filter changes / color-coding
      metadata changes; layout/project reload triggers the same _rebuildLegend
      pathway via dataframe restoration. Marginal but acceptable mapping.
  (3) Strict-subset relation against scatterplot.md is broken: structure-rendering.md
      now uniquely covers legend.column AND legend.item.marker-picker, neither of
      which appears in scatterplot.md's sub_features_covered set
      [legend.extra-column, legend.show-main-item-icons, legend.item.color-picker,
       legend.allow-item-coloring, legend.refresh.on-data-change, legend.item.click,
       legend.color-scale.numerical, legend.use-custom-color-coding].
  (4) Density A-STRUCT-06 now passes strictly (2.25 ≥ 2.0) without chain-split
      appeal — improvement over round 1 which used chain-split rationale.
  (5) All other Gate A checks remain PASS as in round 1.
