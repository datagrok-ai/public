revision: 1
schema_version: "v3.0"
cycle_id: legend-filtering-2026-05-07
feature: legend
mode: scenario
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/filtering.md
invoked_at: 2026-05-07T00:00:00Z
verdict: SCOPE_REDUCTION
gate: A
failure_keys: []
scope_reduction_proposal: |
  Add per-scenario `coverage_type: edge` heading marker on Scenario 9
  ("Bar chart edge case — Stack with `includeNulls = false`") so that
  A-STRUCT-02 (at least one scenario labeled edge or perf) is satisfied
  alongside the file-level `coverage_type: regression`. Scenario 9 is
  genuinely an edge case per atlas: the Bar chart Stack + `includeNulls
  = false` ghost-entry path is a documented edge boundary tied to
  bug-library entry GROK-17222 family and the legend.show-nulls /
  legend.extra-column sub-features. The label gap is purely a
  classification-marker issue, not a content gap. Real dependency:
  unified test-kind enum (smoke|regression|edge|perf) per
  C7-followup 2026-04-30 requires per-scenario marker for an explicit
  edge call-out when file-level frontmatter is regression.

  Implementation: append `(coverage_type: edge)` marker line under the
  `### 9. Bar chart edge case` heading or change the heading to
  `### 9. Bar chart edge case — Stack with includeNulls = false [coverage_type: edge]`,
  matching the per-scenario marker pattern allowed by A-STRUCT-03.

claims:
  - check: A-STRUCT-MECH-01
    status: PASS
    evidence: |
      Frontmatter parses as YAML; required fields present (feature: legend,
      sub_features_covered: 4 items, target_layer: playwright,
      coverage_type: regression).
  - check: A-STRUCT-MECH-02
    status: PASS
    evidence: |
      Body contains `## Setup`, `## Scenarios`, `## Notes` and 9 numbered
      `### N.` scenario headings under `## Scenarios`.
  - check: A-STRUCT-MECH-03
    status: PASS
    evidence: |
      Every `### N.` scenario heading is followed by a numbered list
      starting at `1.` (Scenarios 1-9 each have 2-8 steps).
  - check: A-STRUCT-MECH-04
    status: PASS
    evidence: |
      No empty scenarios — every `### N.` block has at least 2 numbered
      steps (Scenario 4 has 2 steps; Scenario 9 has 4 steps).
  - check: A-STRUCT-MECH-05
    status: PASS
    evidence: |
      target_layer=playwright is one of the allowed enum values
      (uitests-package | playwright | api-contract | manual-only).
  - check: A-STRUCT-MECH-06
    status: PASS
    evidence: |
      coverage_type=regression is in the allowed enum (smoke | regression
      | edge | perf). Per-scenario edge marker missing on Scenario 9 is
      tracked under A-STRUCT-02, not the mechanical enum check.
  - check: A-STRUCT-01
    status: PASS
    evidence: |
      Scenario 1 combines legend.refresh.on-data-change + legend.column
      + legend.extra-column across numerical/categorical/structure
      filters. Scenario 9 combines legend.show-nulls + legend.extra-column
      + legend.refresh.on-data-change. Both exceed the 3-sub-feature
      interaction threshold.
  - check: A-STRUCT-02
    status: FAIL
    evidence: |
      File frontmatter is coverage_type: regression. No per-scenario
      heading carries an edge or perf marker, even though Scenario 9 is
      narratively labeled "Bar chart edge case" and matches atlas
      edge_cases (Bar chart Stack + includeNulls=false ghost-entry tied
      to GROK-17222 family). Scope reduction proposes adding a per-
      scenario coverage_type: edge marker for Scenario 9.
  - check: A-STRUCT-03
    status: PASS
    evidence: |
      coverage_type: regression is present at file frontmatter level
      (allowed by A-STRUCT-03 either at file or per-scenario level).
      Severity-axis priority: p1 is also present but does not interfere.
  - check: A-STRUCT-04
    status: PASS
    evidence: |
      Common 4-step setup (Open SPGI, add 7 viewers, set legend column,
      open Filter Panel) is factored into `## Setup`, not duplicated
      across 9 scenarios.
  - check: A-STRUCT-05
    status: NA
    evidence: |
      Exempted per orchestrator instruction — chain-coordinated split
      with delegated UI flows to visibility-and-positioning.md (PASS).
      Aggregate atlas coverage is Gate F's responsibility
      (F-UI-COVERAGE-01 / F-REDUNDANCY-01).
  - check: A-STRUCT-06
    status: PASS
    evidence: |
      9 scenarios; all but Scenario 4 (in-viewer filter property only)
      touch ≥2 sub-features. Average sub-features per scenario is well
      above 2 across the file (refresh.on-data-change + column appear
      in the majority of scenarios; Scenario 9 adds show-nulls +
      extra-column). Density rule satisfied.
  - check: A-CONT-01
    status: PASS
    evidence: |
      All references are concrete: SPGI.csv, columns Average Mass, Stereo
      Category, Core, Structure, Primary Scaffold Name, CAST Idea ID;
      categorical values R_ONE, S_UNKN; viewer types and their legend-
      source properties named explicitly. No angle/bracket placeholders
      and no generic stand-ins.
  - check: A-BUG-01
    status: PASS
    evidence: |
      Scenario file references GROK-17222 in `related_bugs` frontmatter
      and discusses the bug invariant in the Notes block ("legend SHOULD
      update on every filter source"). Other atlas known_issues
      (GROK-17438, github-3132, GROK-17278, GROK-19083, GROK-19041) are
      owned by sibling scenarios in the same chain (visibility-and-
      positioning, color-consistency, structure-rendering, scatterplot,
      line-chart) per chain YAML bug_focused_candidates and skip
      records — section-level bug coverage is verified at Gate F
      (F-BUG-COVERAGE-01).
  - check: A-MERIT-01
    status: PASS
    evidence: |
      No scenario opted out for "effort" or "complexity". The Notes
      block cites a real architectural reason for delegation (standard
      legend UI flows owned by sibling visibility-and-positioning.md
      scenario), not effort-avoidance.
  - check: A-MERIT-02
    status: PASS
    evidence: |
      No "TODO: add later" / "to be done in next phase" markers. The
      delegated UI flow ownership is concrete (sibling scenario, not
      future work), and the bug-focused spec reference (legend-grok-
      17222-spec.ts) names a real cross-cutting candidate from chain
      YAML, not a deferral.
