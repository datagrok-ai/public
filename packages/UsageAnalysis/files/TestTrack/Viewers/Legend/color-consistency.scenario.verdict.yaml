revision: 1
schema_version: "v3.0"
cycle_id: legend-migrate-2026-05-07
feature: legend
mode: scenario
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/color-consistency.md
invoked_at: 2026-05-07T00:00:00Z
verdict: SCOPE_REDUCTION
gate: A
failure_keys: []
scope_reduction_proposal: |
  Add per-scenario coverage_type markers to lift A-STRUCT-02 cleanly:
    - Sections 3 (layout persistence) and 4 (project persistence) verify
      the GROK-17278 invariant which is documented in
      feature-atlas/legend.yaml `edge_cases[]` (entry with
      source_bug: GROK-17278). Persistence-of-customization across
      sessions is a published edge case, not regression-class behavior.
    - Mark Section 3 and Section 4 with per-heading `coverage_type: edge`
      (e.g., "### 3. Layout persistence ... {coverage_type: edge}"), or
      split file-level frontmatter into per-scenario blocks: Sec 1, 2 =
      regression; Sec 3, 4 = edge.
    - Real dependency cited: atlas `edge_cases` source_bug GROK-17278
      (state-management edge — runtime-only color customizations not
      serialized to layout/project) is the precise behavior tested by
      Sec 3-4. Atlas-level dependency is documented; not an
      effort/complexity opt-out.
  After this reduction, A-STRUCT-02 passes (≥1 scenario with
  coverage_type ∈ {edge, perf}). All other Gate A checks already pass.
evidence_needed: ""
claims:
  - check_id: A-STRUCT-MECH-01
    status: PASS
    evidence: Frontmatter parses as YAML; required fields feature, sub_features_covered, target_layer, coverage_type all present.
  - check_id: A-STRUCT-MECH-02
    status: PASS
    evidence: "## Setup, ## Scenarios, ## Notes plus four ### sub-scenario headings present."
  - check_id: A-STRUCT-MECH-03
    status: PASS
    evidence: Each of the 4 sub-scenarios has numbered steps (1., 2., 3., …).
  - check_id: A-STRUCT-MECH-04
    status: PASS
    evidence: No empty scenarios; each has 3+ steps.
  - check_id: A-STRUCT-MECH-05
    status: PASS
    evidence: target_layer is "playwright" — valid enum value.
  - check_id: A-STRUCT-MECH-06
    status: PASS
    evidence: coverage_type is "regression" — valid enum value.
  - check_id: A-STRUCT-01
    status: PASS
    evidence: |
      Each sub-scenario combines the trio legend.use-custom-color-coding +
      legend.item.color-picker + legend.allow-item-coloring through
      grid color-coding propagation, swatch picker, and recolor flow.
      Scenario 1 in particular interacts all three by enabling
      categorical color coding (use-custom-color-coding), changing
      colors which require recoloring permission (allow-item-coloring),
      and verifying propagation surfaces through the picker domain
      (item.color-picker is the bidirectional surface tested by
      Scenario 2).
  - check_id: A-STRUCT-02
    status: FAIL
    evidence: |
      File-level coverage_type is "regression"; no scenario carries
      edge or perf marker. Sections 3 and 4 substantively test the
      GROK-17278 atlas edge_case (persistence of legend color
      customizations across layout/project save-reload), but lack
      explicit edge labeling. This drives the SCOPE_REDUCTION
      verdict — a labeling-only fix per scope_reduction_proposal.
  - check_id: A-STRUCT-03
    status: PASS
    evidence: File-level frontmatter coverage_type=regression labels every sub-scenario.
  - check_id: A-STRUCT-04
    status: PASS
    evidence: Single ## Setup section (3 steps) shared across all 4 sub-scenarios; no duplication.
  - check_id: A-STRUCT-05
    status: NA
    evidence: |
      Per orchestrator instruction, chain-coordinated section split
      convention applies (Gate F responsibility). Atlas has 23
      sub-features; this file owns 3 (color-coding propagation
      specialty) per chain YAML legend.yaml. Aggregate atlas coverage
      across the 6-file section is verified at Gate F.
  - check_id: A-STRUCT-06
    status: PASS
    evidence: Average sub-features per scenario = 3 (3 sub_features_covered shared across 4 sub-scenarios) ≥ 2 threshold.
  - check_id: A-CONT-01
    status: PASS
    evidence: |
      All references real: dataset System:DemoFiles/SPGI.csv, column
      Stereo Category, categories R_ONE / S_UNKN, viewer names
      (Histogram, Line chart, Bar chart, Pie chart, Trellis plot,
      Box plot), and viewer-specific legend-source properties (Split,
      Category, X). No angle-bracket / square-bracket / generic
      placeholders.
  - check_id: A-BUG-01
    status: PASS
    evidence: |
      Atlas known_issues with test_coverage: needed = 6 bugs total.
      This file references 3 in related_bugs: GROK-17438, github-3132,
      GROK-17278 — exactly the set that chain YAML
      bug_focused_candidates assigns to color-consistency.md spans.
      Remaining bugs (GROK-17222, GROK-19083, GROK-19041) are owned
      by sibling scenarios per chain mapping (filtering.md,
      structure-rendering.md, scatterplot.md). Per-file Gate A is
      satisfied; section-level coverage is Gate F's domain.
  - check_id: A-MERIT-01
    status: PASS
    evidence: |
      Two opt-outs, both with real dependencies: (a) Scatter plot
      omitted from setup — covered by sibling scatterplot.md per
      chain Section 5; (b) standard legend UI flows delegated to
      visibility-and-positioning.md per chain ui_coverage_plan.
      Atlas/chain-level dependency cited; no effort/complexity
      handwaving.
  - check_id: A-MERIT-02
    status: PASS
    evidence: |
      No "TODO: add later" or "to be done in next phase" strings.
      Deferrals reference sibling chain artifacts (scatterplot.md,
      visibility-and-positioning.md) by name with chain_id legend
      ordering preserved.
