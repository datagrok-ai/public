revision: 2
schema_version: "v3.0"
cycle_id: legend-modernize-filtering-2026-05-08
feature: legend
mode: spec
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/filtering-spec.ts
invoked_at: 2026-05-08T00:00:00Z
verdict: PASS
gate: E
failure_keys: []
claims:
  - check: E-STRUCT-MECH-01
    status: PASS
    evidence: |
      Spec exists at
      public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/filtering-spec.ts.
  - check: E-STRUCT-MECH-02
    status: PASS
    evidence: |
      File parses as valid TypeScript under standalone tsc (--target es2020
      --module esnext --skipLibCheck --esModuleInterop). Imports, single
      test() block, balanced braces, valid type assertions.
  - check: E-STRUCT-MECH-03
    status: PASS
    evidence: |
      Single `test('Legend filtering', async ({page}) => {...})` block.
  - check: E-STRUCT-MECH-04
    status: PASS
    evidence: |
      Test name "Legend filtering" matches scenario filename `filtering.md`;
      each softStep label references its Scenario N, steps X-Y mapping
      via inline comments traceable to scenario headings.
  - check: E-STRUCT-MECH-05
    status: PASS
    evidence: |
      Imports only from `@playwright/test` and sibling local
      `../../spec-login` (canonical TestTrack sibling-spec pattern,
      same as visibility-and-positioning-spec.ts and
      color-consistency-spec.ts).
  - check: E-STRUCT-MECH-06
    status: PASS
    evidence: |
      Frontmatter `/* --- sub_features_covered: [legend.refresh.on-data-change,
      legend.column, legend.show-nulls, legend.extra-column] --- */`
      is the first content; all four ids resolve to atlas
      legend.yaml `sub_features[].id`.
  - check: E-TRACE-01
    status: PASS
    evidence: |
      Every softStep traces to a scenario step, OR is marked
      `// technical:`. Technical markers placed on: setup
      (open SPGI / add 7 viewers / configure split columns / prime
      Filter Panel), reset+rebuild glue inside Save+re-apply layout
      step, reset+clear glue inside Scatter zoom-filter / Bar / Pie /
      Trellis click steps, layout-reset glue in Scenario 7, Cleanup
      softStep.
  - check: E-TRACE-02
    status: PASS
    evidence: |
      Every numbered scenario step covered: Setup (page.evaluate setup
      block + filter-panel hover), Scenario 1 steps 1-2 (numerical),
      4-5 (categorical), 6-7 (structure); Scenario 2 (save+reapply
      layout); Scenario 3+4 (reset + in-viewer Scatter filter);
      Scenario 5 (compose); Scenario 6.1-6.2 (Scatter zoom-filter via
      sp.props.filter range expression — scenario sanctions JS API
      alternative); Scenario 6.3-6.4 (Bar chart canvas locator.click);
      Scenario 6.5-6.6 (Pie chart canvas locator.click); Scenario
      6.7-6.8 (Trellis cell DIV synthetic dispatch — recon-validated
      because inner-canvas events do not propagate to filter handler);
      Scenario 7 (layout persistence — save, reset filter, reload,
      verify restored); Scenario 8 (Row Source cycles); Scenario 9
      [coverage_type: edge] (includeNulls=false stack edge case).
  - check: E-TRACE-03
    status: PASS
    evidence: |
      Verifications use `expect(...).toBe(...)` /
      `.toBeGreaterThan(...)` / `.toBeLessThan(...)` against JS API
      reads (`df.filter.trueCount`, `df.col(...)` per-row count
      survivors, `sp.props.filter`, legend item DOM count).
  - check: E-SEL-01
    status: PASS
    evidence: |
      Selectors `[name="legend"]`, `[name="viewer-Filters"]`,
      `[name="viewer-Bar chart"]`, `[name="viewer-Pie chart"]`,
      `[name="viewer-Grid"]`, `.d4-legend-item`, `.d4-filter`,
      `.d4-grid` documented in
      `.claude/skills/grok-browser/references/{viewers,filters}.md`.
      `.d4-trellis-plot-cell` validated 2026-05-08 in
      `.claude/plan/legend-mcp-recon-2026-05-08-canvas-filter.md`
      (cited in spec header `// Selector sources` block).
  - check: E-SEL-02
    status: PASS
    evidence: |
      No invented `[name="..."]` selectors. Every name attribute and
      CSS class either appears in a grok-browser reference or is
      validated in the cited 2026-05-08 MCP recon notes file.
  - check: E-SEL-03
    status: PASS
    evidence: |
      No `.fill()` calls on Dart input fields. Filter values set via
      JS API `fg.updateOrAdd(...)` and viewer prop assignments. The
      only `.click()` calls land on canvas elements (Bar/Pie viewer
      canvases) via `locator.click({position})` — native pointer
      event chain, not text-input typing.
  - check: E-HELP-01
    status: PASS
    evidence: |
      Helpers used: `loginToDatagrok`, `softStep`, `specTestOptions`,
      `stepErrors` — all exported from
      `public/packages/UsageAnalysis/files/TestTrack/spec-login.ts`
      and registered in helpers-registry under the Playwright layer.
  - check: E-HELP-02
    status: PASS
    evidence: |
      No open-coded login (uses `loginToDatagrok`); no reinvented
      soft-step harness (uses `softStep` / `stepErrors`). Layout
      round-trip uses canonical `grok.dapi.layouts.save/find` JS API.
  - check: E-LAYER-01
    status: PASS
    evidence: |
      Scenario frontmatter `target_layer: playwright`; spec uses
      Playwright runner with `test(..., async ({page}) => ...)`,
      page.locator, page.evaluate. Paradigm matches.
  - check: E-LAYER-02
    status: NA
    evidence: |
      No target_layer override applied; rule vacuous.
  - check: E-LAYER-COMPLIANCE-01
    status: PASS
    evidence: |
      Multiple Playwright DOM-driving calls in spec body satisfy the
      `target_layer: playwright` mechanical requirement and provide
      real interactions for the owned UI flows:
        page.locator('.d4-grid[name="viewer-Grid"]').first().waitFor(...)
        page.locator('[name="viewer-Filters"] .d4-filter').first().waitFor(...)
        page.locator('[name="viewer-Filters"] .d4-filter').first().hover()
        page.locator('[name="viewer-Bar chart"] canvas').first()
          .click({position: {x: localX, y: localY}, timeout: 5000})
        page.locator('[name="viewer-Pie chart"] canvas').first()
          .click({position: {x: localX, y: localY}, timeout: 5000})
      Trellis click uses synthetic dispatch on `.d4-trellis-plot-cell`
      DIV because the inner-viewer canvas does not propagate events
      to the Trellis filter handler (recon-validated 2026-05-08;
      documented in spec header). Pyramid_layer is `integration`
      (not `ui-smoke`), so JS API substitution remains permitted for
      non-UI-owned operations. Resolves prior failure
      `legend-filtering-frontmatter-only-x14`.
  - check: E-BOUND-01
    status: PASS
    evidence: |
      Modified file at
      `public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/filtering-spec.ts`
      under allowed `public/packages/UsageAnalysis/files/TestTrack/**` path.
  - check: E-BOUND-02
    status: PASS
    evidence: |
      No `core/**` changes; no package source outside TestTrack/.
scope_reduction_proposal: ""
evidence_needed: ""
