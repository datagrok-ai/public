revision: 2
schema_version: "v3.0"
cycle_id: legend-modernize-color-consistency-2026-05-08
feature: legend
mode: spec
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/color-consistency-spec.ts
invoked_at: 2026-05-08T00:00:00Z
verdict: PASS
gate: E
failure_keys: []
claims:
  - check: E-STRUCT-MECH-01
    status: PASS
    evidence: |
      Spec file present at
      public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/color-consistency-spec.ts.
  - check: E-STRUCT-MECH-02
    status: PASS
    evidence: |
      TypeScript parses cleanly under UsageAnalysis tsc (--target es2020 --module
      esnext --skipLibCheck). Standard imports, balanced braces, typed parameters.
  - check: E-STRUCT-MECH-03
    status: PASS
    evidence: |
      One `test('Legend color consistency', async ({page}) => {...})` block.
  - check: E-STRUCT-MECH-04
    status: PASS
    evidence: |
      Test name 'Legend color consistency' substring-matches scenario filename
      `color-consistency.md`; softStep labels reference Scenario 1-4 steps
      ("Categorical color coding", "Open color picker", "Save + re-apply layout",
      "Project round-trip").
  - check: E-STRUCT-MECH-05
    status: PASS
    evidence: |
      Imports only from `@playwright/test` and sibling `../../spec-login`
      (canonical TestTrack-local helpers — established sibling-spec pattern
      across visibility-and-positioning-spec.ts, structure-rendering-spec.ts).
  - check: E-STRUCT-MECH-06
    status: PASS
    evidence: |
      Frontmatter `/* --- sub_features_covered: [legend.use-custom-color-coding,
      legend.item.color-picker, legend.allow-item-coloring] --- */` is the first
      content; all three ids resolve in atlas legend.yaml `sub_features[]`.
  - check: E-TRACE-01
    status: PASS
    evidence: |
      Every softStep traces to a scenario step, OR is marked `// technical:`.
      Technical markers placed on: open-SPGI setup, viewer-redraw trigger
      inside Scenario 1, Cleanup softStep.
  - check: E-TRACE-02
    status: PASS
    evidence: |
      Scenario 1 (steps 1-3): "Categorical color coding from grid" + "Every
      viewer reflects R_ONE=red and S_UNKN=green" softSteps.
      Scenario 2 (steps 1-4): "Open color picker via legend, change R_ONE to
      blue" + "Picker change propagated: column metadata + every legend in DOM"
      softSteps. UI flow exercised on Histogram with documented deviation
      (header comment) — same legend mechanism, same propagation invariant.
      Scenario 3 (steps 1-3): "Save + re-apply layout — custom palette persists"
      softStep with R_ONE color verification post-reload.
      Scenario 4 (steps 1-3): "Project round-trip — save + close + reopen +
      verify palette" softStep with full close/reopen + per-viewer color check;
      gracefully handles known FK save limitation (GROK-17278/related).
  - check: E-TRACE-03
    status: PASS
    evidence: |
      Verification uses `expect(...).toBe(...)` against
      `col.meta.colors.getColor(idx)` (column color metadata = single source of
      truth) and `getComputedStyle(item).color` on per-viewer legend items.
      Multi-viewer scope ≥ 3 viewers asserted explicitly.
  - check: E-SEL-01
    status: PASS
    evidence: |
      Selectors `[name="viewer-Histogram"]`, `[name="legend"]`, `.d4-legend-item`,
      `[name="legend-icon-color-picker"]`, `.d4-dialog[name="dialog-R-ONE"]`,
      `[name="button-OK"]`, `.d4-color-bar`, `[name="viewer-Grid"]` documented in
      `.claude/skills/grok-browser/references/viewers.md` (Legend section,
      Viewer Containers, Viewer Title Bar Icons) and validated by MCP recon
      `.claude/plan/legend-mcp-recon-2026-05-08-color-picker.md`. `.d4-legend-value`
      and `.d4-color-bar` are conventional Datagrok CSS classes also used by
      sibling specs.
  - check: E-SEL-02
    status: PASS
    evidence: |
      No invented `[name="..."]` selectors. Every name-attribute and CSS class
      either (a) appears in a grok-browser reference, (b) is validated in the
      2026-05-08 MCP recon notes file, or (c) is a conventional Datagrok class
      used by sibling specs.
  - check: E-SEL-03
    status: PASS
    evidence: |
      No `.fill()` calls. Color picker swatches use synthetic mousedown/mouseup/
      click dispatch (Dart d4 listens to the full event chain); OK button uses
      standard Playwright `locator.click()`. No Dart text-input typing in this
      spec.
  - check: E-HELP-01
    status: PASS
    evidence: |
      Helpers used: `loginToDatagrok`, `softStep`, `specTestOptions`,
      `stepErrors` — all exported from `public/packages/UsageAnalysis/files/
      TestTrack/spec-login.ts` and registered in helpers-registry under the
      Playwright layer.
  - check: E-HELP-02
    status: PASS
    evidence: |
      No open-coded login (uses `loginToDatagrok`); no reinvented soft-step
      harness (uses `softStep` / `stepErrors`). Layout/project round-trips
      use canonical `grok.dapi.layouts.save/find` and
      `grok.dapi.projects.save/find/open` JS APIs.
  - check: E-LAYER-01
    status: PASS
    evidence: |
      Scenario frontmatter `target_layer: playwright`; spec uses Playwright
      runner with `test(..., async ({page}) => ...)`, page.locator,
      page.evaluate. Paradigm matches.
  - check: E-LAYER-02
    status: NA
    evidence: |
      No layer override applied; rule vacuous.
  - check: E-LAYER-COMPLIANCE-01
    status: PASS
    evidence: |
      Multiple DOM-driving calls in spec body:
        L67 `page.locator('.d4-grid[name="viewer-Grid"]').first().waitFor(...)`
        L132 `await item.waitFor(...)`
        L134 `await item.hover()`
        L136 `await picker.click({timeout: 3000})`
        L138 `await page.locator('.d4-dialog[name="dialog-R-ONE"]').waitFor(...)`
        L155 `await page.locator('.d4-dialog[name="dialog-R-ONE"] [name="button-OK"]').click()`
      Satisfies playwright paradigm regex (`\bpage\.locator\b` and
      `\bpage\.click\b`-equivalent via locator.click). pyramid_layer is
      `integration` (not `ui-smoke`), so per-flow ui-smoke sub-rule does
      NOT apply mechanically.
  - check: E-BOUND-01
    status: PASS
    evidence: |
      Modified file at
      `public/packages/UsageAnalysis/files/TestTrack/Viewers/Legend/color-consistency-spec.ts`
      under allowed `public/packages/UsageAnalysis/files/TestTrack/**` path.
  - check: E-BOUND-02
    status: PASS
    evidence: |
      No `core/**` changes; no package source outside TestTrack/.
scope_reduction_proposal: ""
evidence_needed: ""
