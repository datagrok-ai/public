---
gate: B
target: public/packages/UsageAnalysis/files/TestTrack/Chem/Advanced/scaffold-tree-functions-spec.ts
run_date: 2026-05-12
runner: claude-code-automator
cycle_id: chem-migrate-spec-phase-2026-05-12-phase-A-validation-sweep
spec_id: scaffold-tree-functions

verdict: PASS-WITH-SCOPE-LIMIT
runs: 3
pass_rate: 3/3
results:
  run_1: PASS (1.0m)
  run_2: PASS (1.0m)
  run_3: PASS (1.0m)

claims:
  - check: B-RUN-PASS
    status: PASS
    evidence: 3 consecutive PASS runs ~1 min each, stable on dev.datagrok.ai.
  - check: B-FULL-SCENARIO-COVERAGE
    status: SCOPE_REDUCTION
    evidence: |
      Test executes successfully but covers only 4/9 scenario steps.
      Per Critic E (sister verdict YAML): missing steps 3 (empty-state),
      5 (hierarchy verify), 6 (first-node click), 7 (filter row count),
      8 (toolbox inspect), 9 (property panel). Wrong dataset (smiles.csv
      vs smiles-50.csv) — works but not as scenario specifies.

cross_references:
  - scaffold-tree-functions-spec.verdict.yaml (Critic E FAIL — E-STRUCT-MECH-06, E-TRACE-02, E-TRACE-03, E-DATASET-MISMATCH)

remediation_path: |
  Spec body works as-coded (3/3 PASS) but does NOT validate the
  ui-smoke pyramid_layer's full scenario contract. To bring to full
  coverage:
    - Switch to smiles-50.csv
    - Add empty-state placeholder check
    - Add first-node click + filter row count assertion
    - Add toolbox + property panel inspection
    - Add frontmatter block
