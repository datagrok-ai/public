revision: 1
schema_version: "v3.0"
cycle_id: chem-migrate-spec-phase-2026-05-12-phase-A-validation-sweep
feature: chem
mode: spec
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Chem/Advanced/scaffold-tree-functions-spec.ts
invoked_at: 2026-05-12T19:20:00Z
review_method: inline-walkthrough (Critic E subagent cost-reduction; per-spec critique inline)
verdict: FAIL
gate: E
failure_keys:
  - E-STRUCT-MECH-06
  - E-TRACE-02
  - E-TRACE-03
claims:
  - check: E-STRUCT-MECH-06
    status: FAIL
    evidence: Missing /* --- sub_features_covered: [...] --- */ frontmatter block.
  - check: E-TRACE-02
    status: FAIL
    evidence: |
      Scenario has 9 numbered steps. Spec covers steps 1 (partial — WRONG dataset
      smiles.csv vs scenario-mandated smiles-50.csv), 2 (top-menu), 4 (magic wand).
      Uncovered: step 3 (empty-state "Scaffold Tree is empty" verification),
      step 5 (nodes appear hierarchy verification), step 6 (click first node),
      step 7 (filter applied + row count), step 8 (toolbox inspect),
      step 9 (property panel inspect).
  - check: E-TRACE-03
    status: FAIL
    evidence: |
      Verifications missing for filter row count (step 7), toolbox render
      (step 8), property panel render (step 9). Only nodes > 0 asserted.
  - check: E-DATASET-MISMATCH
    status: FAIL
    evidence: |
      Scenario mandates System:DemoFiles/chem/smiles-50.csv (50-row).
      Spec reads System:DemoFiles/chem/smiles.csv (1000-row). 25s Chem
      cascade may not be needed for 50-row dataset.
remediation_path: |
  - Switch dataset to smiles-50.csv
  - Add frontmatter block
  - Add empty-state verification (placeholder text)
  - Add first-node click + filter row-count verification
  - Add toolbox + property panel inspection (via Properties... right-click
    menu per chem.md Sim Search property panel pattern, applicable here)
