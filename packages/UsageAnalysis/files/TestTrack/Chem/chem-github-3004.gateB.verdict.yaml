revision: 2
schema_version: "v3.0"
cycle_id: chem-new-2026-05-12
predecessor_cycle_id: chem-new-2026-05-11-batch-2
feature: chem
mode: validator
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Chem/chem-github-3004-spec.ts
invoked_at: 2026-05-12T09:48:00Z
verdict: PASS
verdict_transition: SCOPE_REDUCTION → FAIL (R1, test-bug) → PASS (R2)
sr_class: null
scope_reduction_proposal: null

hypothesis_history:
  - round: 0
    category: proactive-env-pending-SR
    theory: |
      Same Chem package cold-start structural blocker class as
      chem-grok-14028 / chem-grok-17964 / chem-grok-16870 — applies
      a priori because Scaffold Tree functionality is a Chem-package-
      contributed surface (top-menu entry + viewer + addScaffoldTree
      function all from Chem package).
    investigation_evidence: |
      Empirical evidence from prior 3 specs in this blocker class:
        - chem-grok-14028 (batch-1): warmChemPackage insufficient,
          Chem detector + sketcher pipeline not registered in 180s.
        - chem-grok-17964 R2 + R4 (batch-2): 180s and 360s test
          budgets both exhausted on first step; warmChemPackage()
          Chem:getRdKitModule does not complete in fresh Playwright
          sessions within any practical per-test budget.
        - chem-grok-16870 R2 (batch-2): 180s budget exhausted same
          way; Molecule semType detector + cellRenderer-Molecule both
          fail to register.
      Scaffold Tree is a stricter dependency — adds the
      chem.analyze.scaffold-tree.add function registration requirement
      on top of the Molecule detector / RDKit renderer requirements.
      No reason to expect different outcome here. Authoring decision:
      bake test.fixme() into the spec from initial commit; document the
      env-pending SR proactively.
    fix_applied: |
      test.fixme() with rationale citing the structural blocker +
      cross-reference to chem-grok-17964 R4 360s probe outcome +
      forward path globalSetup landing.
    outcome: env-pending defensive SR (no Validator run needed)
    next_round_id: 1
    skip_validator_run_rationale: |
      Per orchestrator SR dispatch table acceptable-SR-class env-pending
      defensive skip routing — when a NEW spec falls in a documented
      env-pending blocker class with structural evidence from ≥3 prior
      specs (chem-grok-14028 / chem-grok-17964 / chem-grok-16870), and
      the spec dependency surface (Chem package cold-start) is identical
      or stricter, proactively applying test.fixme() with documented
      rationale is acceptable. Running Validator would yield identical
      timeout exhaustion + waste CI minutes; the outcome is empirically
      determined.
  - round: 1
    category: test-bug
    theory: |
      Post-breakthrough Round 0 hypothesis was over-weighted (per the
      chem-grok-17964 breakthrough re-attribution — Playwright+WASM
      event-loop interaction, NOT package-load resource issue). With
      test.fixme removed and the canonical pattern in body, the env-
      pending blocker did NOT recur. Spec progressed through Setup +
      30s settle + Molecule semType verify + Top-menu Chem dispatch +
      Scaffold Tree viewer assertion-on-tableB. Failed on final
      assertion step at line 138.
    investigation_evidence: |
      Stderr: "TypeError: tvs.find is not a function" at
      chem-github-3004-spec.ts:138. Investigation: line 119 wrapped
      `tv.viewers` in `Array.from(...)` correctly; line 138 used
      `grok.shell.tableViews` directly with `.find()`. The shell
      returns an Iterable, not a plain Array — `.find` undefined.
      Cheap-check confirmation: line 119 wrapped pattern works in
      previous specs; line 138 unwrapped pattern fails consistently.
    fix_applied: |
      Wrapped `grok.shell.tableViews` in `Array.from(...)` at line 138
      (one-character surgical fix). No other body changes.
    outcome: PASS
    next_round_id: null
  - round: 2
    category: PASS (post-test-bug-fix confirmation)
    theory: PASS confirmation re-run after the surgical test-bug fix.
    investigation_evidence: |
      Re-ran chem-github-3004-spec.ts on dev.datagrok.ai with
      NODE_OPTIONS=--max-old-space-size=8192, workers=1. Duration
      38.1s (total 40.7s including Playwright overhead). All
      softSteps green; final regression-lock assertion confirms
      Scaffold Tree viewer binds to tableB (active), zero
      attachment to tableA.
    fix_applied: none — confirmation of R1 fix.
    outcome: PASS
    next_round_id: null

remediation_path: null

cross_references:
  - 'cycle_completions[chem-new-2026-05-11-batch-1] :: lessons_learned #3 (Chem package cold-start, original hypothesis — superseded by R1)'
  - 'cycle_completions[chem-new-2026-05-11-batch-2-breakthrough] :: breakthrough retro (root-cause re-attribution from package-load to Playwright+WASM event-loop)'
  - public/packages/UsageAnalysis/files/TestTrack/Chem/chem-grok-17964.gateB.verdict.yaml (canonical PASS pattern source)
  - public/packages/UsageAnalysis/files/TestTrack/Chem/chem-grok-16870.gateB.verdict.yaml (peer PASS this cycle)

claims:
  - check_id: B-RUN-01
    status: PASS
    evidence: |
      Spec executed under DATAGROK_URL=https://dev.datagrok.ai, workers=1,
      NODE_OPTIONS=--max-old-space-size=8192. Round 1 surfaced test-bug
      at line 138 (`tvs.find` on Iterable); fixed via `Array.from(...)`.
      Round 2 PASS confirmation: 38.1s duration. All softSteps green.
      Regression-lock assertion confirms Scaffold Tree viewer binds to
      active table (tableB), zero spurious attachment to tableA.
