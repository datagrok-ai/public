verdict: SCOPE_REDUCTION
gate: A
review_round: 1
failure_keys: []
scope_reduction_proposal: |
  SR-01 (A-STRUCT-02 carryforward, accepted per chain rev 2 precedent —
  decision-log radar.md 2026-05-07).

  calculate.md is a single-scenario file with coverage_type: regression
  (multi-format × multi-Calculate-menu-item happy-path walk; pyramid_layer:
  integration). It therefore mechanically fails A-STRUCT-02 in isolation
  (no in-file scenario carries coverage_type: edge or perf).

  Real section-level dependency: A-STRUCT-02 is a section-wide structural
  invariant, not per-scenario. Forcing coverage_type: edge would
  mis-classify (the scenario walks the happy path, not a failure-mode
  invariant). Section-level satisfaction routes elsewhere:
    (a) scenario-chains/chem.yaml rev 2 `bug_focused_candidates[]`
        enumerates 10 chem-bug-focused specs (incl. chem-grok-17621-spec.ts
        for the Descriptors Docker timeout invariant directly intersecting
        this scenario, and chem-github-2942-spec.ts for filter+export);
        each naturally carries coverage_type: edge once authored;
    (b) same-cycle sibling r-group-analysis.md (same section) carries
        coverage_type: edge organically (per its Gate A verdict).

  Verdict on A-STRUCT-02 deferred to chain-level / section-complete
  evaluation (Gate F). Accept SR.

  All other Gate A checks (mechanical + A-STRUCT-01/03/04/05/06,
  A-CONT-01, A-BUG-01, A-MERIT-01/02) PASS independently.
claims:
  - check_id: A-STRUCT-MECH-01
    status: PASS
    evidence: "Frontmatter is parseable YAML (lines 1-11). Required fields present: feature=chem, sub_features_covered=[19 ids], target_layer=playwright, coverage_type=regression."
  - check_id: A-STRUCT-MECH-02
    status: PASS
    evidence: "Scenario H2 heading '## Scenarios' present at line 48 with H3 subheading '### Calculate menu walk per dataset' at line 50."
  - check_id: A-STRUCT-MECH-03
    status: PASS
    evidence: "Numbered steps 1.-6. present under '### Calculate menu walk per dataset' (lines 81-97) plus Setup numbered steps 1.-3. (lines 31-46)."
  - check_id: A-STRUCT-MECH-04
    status: PASS
    evidence: "Single scenario block ('Calculate menu walk per dataset') has 6 numbered steps plus cross-cell invariants; not empty."
  - check_id: A-STRUCT-MECH-05
    status: PASS
    evidence: "target_layer=playwright, valid enum value per allowed set {uitests-package, playwright, api-contract, manual-only}."
  - check_id: A-STRUCT-MECH-06
    status: PASS
    evidence: "coverage_type=regression, valid enum value per allowed set {smoke, regression, edge, perf}."
  - check_id: A-STRUCT-01
    status: PASS
    evidence: "Single scenario covers 19 sub_features in interaction (chem.calculate.descriptors + .top-menu + .transform + .tree; chem.calculate.properties + .top-menu; chem.calculate.toxicity-risks + .top-menu; chem.mpo + .top-menu; chem.calculate.bitbirch + .top-menu; chem.calculate.cluster-mcs + .top-menu; chem.calculate.map-identifiers + .top-menu; chem.notation.inchi; chem.notation.inchi-keys; chem.calculate.biochemical-properties) — well above 3+ threshold."
  - check_id: A-STRUCT-02
    status: FAIL
    evidence: "calculate.md is single-scenario with coverage_type=regression; no in-file scenario carries coverage_type: edge or perf. SR-01 carryforward (chain-level satisfaction via bug_focused_candidates[] + sibling r-group-analysis.md edge coverage) accepted per chain rev 2 precedent — converts verdict to SCOPE_REDUCTION, not FAIL. Documented in migration report Opt-outs §SR-01."
  - check_id: A-STRUCT-03
    status: PASS
    evidence: "coverage_type=regression label present at frontmatter level (line 5) — file-level label applies to the single scenario per migration-prompt convention; valid test-kind enum value."
  - check_id: A-STRUCT-04
    status: PASS
    evidence: "Single scenario; no duplication possible. Setup section (lines 29-46) factors dataset provisioning + chem-package load + fixture-state declaration once."
  - check_id: A-STRUCT-05
    status: PASS
    evidence: "19 sub_features covered in single scenario; coverage well above 70% for the chem.calculate.* subtree which is the scope of this scenario. Remaining chem sub-features (sketcher, similarity-search, etc.) covered by sibling Chem TestTrack scenarios per the section's chain YAML output_plan."
  - check_id: A-STRUCT-06
    status: PASS
    evidence: "Average sub-features per scenario = 19 (single scenario covering 19 ids); well above density threshold of 2. Smoke exemption not invoked (coverage_type=regression)."
  - check_id: A-CONT-01
    status: PASS
    evidence: "Real names throughout: dataset paths (System:DemoFiles/chem/smiles.csv, etc.), menu paths (Chem > Calculate > Descriptors..., etc.), column hints (MolWt, LogP, MW, HBA, HBD, etc.), atlas sub_feature ids. No angle-bracket / square-bracket / generic placeholders. smiles_2_columns.csv TODO resolved with concrete System:AppData/Chem/tests/smiles_2_columns.csv path per Decisions §Source-text fixes."
  - check_id: A-BUG-01
    status: PASS
    evidence: "Bug-library/chem.yaml rev 1 consulted (Decisions §Bug library consulted). Two bugs intersect (GROK-17621, github-2942); both cited in migrated Notes as cross-cutting awareness and owned by chain `bug_focused_candidates[]` per chain rev 2. Remaining 10 chem bugs disjoint from scenario sub_features. related_bugs: [] correct per delegation to bug-focused specs."
  - check_id: A-MERIT-01
    status: PASS
    evidence: "No effort-based opt-outs. SR-01 cites real section-level structural property and chain-routed resolution; not effort/complexity."
  - check_id: A-MERIT-02
    status: PASS
    evidence: "No TODO/deferred-to-later-phase clauses in migrated body. Original 'TODO: add to linked datasets' for smiles_2_columns resolved to concrete path. Selector-reference write proposal flagged in Decisions as Phase 2 enhancement with dependency reference (out of migration scope)."
