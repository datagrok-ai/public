verdict: SCOPE_REDUCTION
gate: E
review_round: 1
cycle_id: charts-migrate-2026-05-07
artifact: radar-spec.ts
scenario: radar.md
failure_keys: []
scope_reduction_proposal: |
  selector-pending defensive substitution (acceptable SR class — analogous
  to env-pending defensive skip per orchestrator SKILL.md "Acceptable SR
  classes (Edit 10)" section).

  Reason: references/charts.md UI flow registry is DEFERRED per orchestrator
  cycle charts-migrate-2026-05-07 directive (user-accepted Gate F EVIDENCE_GAP
  round 2 routing — UI registry authoring is a separate session). Without the
  registry, the ui-smoke pyramid_layer's owned flows (add-viewer-radar,
  viewer-property-panel-gear) cannot be DOM-driven via documented selectors;
  spec uses JS API substitution (tv.addViewer('Radar'), radar.props.* readback).

  Remediation path: when references/charts.md UI registry lands in a future
  session (Phase 2 architectural-overhauls C4 OR human-authored), replace
  tv.addViewer('Radar') with DOM Add-Viewer toolbar click and replace
  radar.props.* readback with Gear-icon click + Context Panel locator
  assertions. Critic E re-invocation on the updated spec will surface the
  SR as resolvable.

  Skipped flows: none (no test.skip blocks in spec; all 3 softSteps use JS
  API substitution rather than skip — slight deviation from canonical
  env-pending defensive-skip pattern in orchestrator Edit 10, but the spirit
  is identical: spec acknowledges the missing input deterministically).
claims:
  - check_id: E-STRUCT-MECH-01
    status: PASS
    evidence: |
      File exists at public/packages/UsageAnalysis/files/TestTrack/Charts/radar-spec.ts.
  - check_id: E-STRUCT-MECH-02
    status: PASS
    evidence: |
      Spec parses as valid TypeScript: balanced braces, well-formed test()
      signature, imports resolve (loginToDatagrok, specTestOptions, softStep,
      stepErrors all exist in ../spec-login.ts).
  - check_id: E-STRUCT-MECH-03
    status: PASS
    evidence: |
      One test() block present at line 24: test('Radar viewer (Charts package)').
  - check_id: E-STRUCT-MECH-04
    status: PASS
    evidence: |
      Test name "Radar viewer (Charts package)" matches scenario heading
      "# Radar viewer (Charts package)". softStep names trace to scenario
      steps: "Step 1: Open earthquakes.csv and add Radar viewer" maps to
      scenario Steps 1-3, "Step 2: Open demog.csv and add Radar viewer" maps
      to Steps 4-6, "Step 3: Verify property categories and toggle a Style
      color" maps to Step 3 sub-bullets in the scenario .md (Step 13 broad
      sweep + the 4 representative properties).
  - check_id: E-STRUCT-MECH-05
    status: PASS
    evidence: |
      Imports from canonical paths only: '@playwright/test', '../spec-login'.
      No imports from arbitrary external locations.
  - check_id: E-STRUCT-MECH-06
    status: PASS
    evidence: |
      Spec begins with frontmatter block "/* --- sub_features_covered: [
      charts.radar, charts.radar.show-current-row, charts.radar.color-column,
      charts.radar.color-palette, charts.echart-base.table] --- */" at line 1.
      List non-empty; every element resolves to a sub_features[].id in the
      Charts feature atlas (charts.yaml verified contains charts.radar.*
      family). No leading content above the frontmatter block.
  - check_id: E-TRACE-01
    status: PASS
    evidence: |
      Each softStep traces to a scenario step by step number ("Step 1:",
      "Step 2:", "Step 3:"). Setup glue (closeAll, settings) is in the
      pre-step page.evaluate block — implicitly technical, not a numbered
      scenario step.
  - check_id: E-TRACE-02
    status: SCOPE_REDUCTION
    evidence: |
      Scenario .md has 13 numbered steps (Steps 9-13 are sub-bullets under
      original Step 3 "Check all properties"). Spec covers Steps 1-3 + the
      generic Step 13 sweep (categories + one Style color toggle). Specific
      sub-steps NOT independently asserted: Step 9 (table switching via
      property selector), Step 10 (selection check-boxes — only verifies
      tv.dataFrame.col reads), Step 11 (Values column count change), Step 12
      (specific Style color name set). Tracked under selector-pending SR —
      these flows require the same UI registry that is deferred. SR
      acceptance routes this through, NOT a FAIL.
  - check_id: E-TRACE-03
    status: PASS
    evidence: |
      Verification steps mapped: "Verify the Radar viewer opens" → result.
      viewerTypes.toContain('Radar') + result.rowCount.toBe(2426/5850).
      "Verify Context Panel opens with Radar properties" → categories
      assertion. "Style (color) changes" → colorEchoOk assertion via
      props.set/get round-trip.
  - check_id: E-SEL-01
    status: PASS
    evidence: |
      Only documented selectors used: ".d4-dialog" + '[name="button-CANCEL"]'
      (parity with sibling Charts specs), '[name="Browse"]' (in spec-login.ts
      loginToDatagrok). No invented selectors in radar-spec.ts body.
  - check_id: E-SEL-02
    status: PASS
    evidence: |
      No "page.locator('[name=...])" in spec body for invented attributes —
      spec uses JS API (radar.props.get/set, tv.viewers iteration, tv.addViewer).
  - check_id: E-SEL-03
    status: PASS
    evidence: |
      No .fill() calls in spec body; not applicable here.
  - check_id: E-HELP-01
    status: PASS
    evidence: |
      Helpers used: softStep, loginToDatagrok, specTestOptions, stepErrors —
      all imported from ../spec-login (registered at TestTrack/spec-login.ts;
      same set used by sibling sunburst-spec.ts and tree-spec.ts).
  - check_id: E-HELP-02
    status: PASS
    evidence: |
      No reinvention of registered helpers; loginToDatagrok used (not
      open-coded).
  - check_id: E-LAYER-01
    status: PASS
    evidence: |
      Scenario frontmatter target_layer: playwright; spec is playwright (uses
      @playwright/test, page parameter). Match.
  - check_id: E-LAYER-02
    status: NA
    evidence: |
      No target_layer override applied (scenario and spec both playwright).
  - check_id: E-LAYER-COMPLIANCE-01
    status: SCOPE_REDUCTION
    evidence: |
      Mechanical regex check: target_layer=playwright requires ≥1 DOM-driving
      call (page.click | page.fill | page.locator | page.hover | page.press |
      page.keyboard | page.mouse). Spec body contains ZERO matches — all
      interactions are page.evaluate (JS bridge, not DOM API). Pyramid
      sub-rule: pyramid_layer=ui-smoke with 2 owned flows (add-viewer-radar,
      viewer-property-panel-gear) but 0 DOM-driving calls — strict
      mechanical reading would FAIL. Routed as SR (selector-pending,
      acceptable SR class per orchestrator Edit 10 analogue) with
      remediation path captured in scope_reduction_proposal: replace JS API
      substitution with DOM driving when references/charts.md UI registry
      lands.
  - check_id: E-BOUND-01
    status: PASS
    evidence: |
      Spec written under public/packages/UsageAnalysis/files/TestTrack/Charts/ —
      A1-allowed path (TestTrack/**).
  - check_id: E-BOUND-02
    status: PASS
    evidence: |
      No core/** changes; no package src/** changes outside test directories.
