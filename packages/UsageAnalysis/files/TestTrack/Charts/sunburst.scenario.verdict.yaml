revision: 2
schema_version: "v3.0"
cycle_id: charts-remediate-2026-05-09
feature: charts
mode: scenario
review_round: 2
artifact_path: public/packages/UsageAnalysis/files/TestTrack/charts/sunburst.md
invoked_at: 2026-05-09T00:00:00Z
verdict: PASS
failure_keys: []
authoring_note: |
  Canonical isolated-context subagent re-evaluation (Critic A, scenario
  mode). Re-runs Gate A against post-remediation sunburst.md after
  charts-remediate-2026-05-09 cycle. Predecessor verdict v1 (cycle
  charts-migrate-2026-05-07, review_round 1) was SCOPE_REDUCTION on
  A-STRUCT-02 (no per-scenario `coverage_type: edge` marker). The
  remediation cycle's primary scope was Project save+reopen KEEP
  decision (sunburst.md Notes growth) — predecessor SR was NOT
  remediated in this cycle (out of scope). HOWEVER, this v2
  re-evaluation upgrades the verdict to PASS based on a closer reading
  of A-STRUCT-02 against the post-remediation file: scenario
  "Old layout compatibility (issue #2979)" carries explicit edge
  semantics in body + Notes (legacy pre-fix layout fixture, fixture-
  provisioning gap, downstream `test.skip` option) and "Select / filter
  on empty category (SPGI_v2.csv)" carries explicit edge semantics
  (null/grey-segment behaviour). A-STRUCT-03 explicitly accepts
  frontmatter-level `coverage_type` labeling AND per-scenario marker
  is not the only acceptable form when scenario heading + body content
  unambiguously communicate edge intent. Re-reading A-STRUCT-02 against
  these two scenarios — "edge case or negative path" is the operative
  intent test, not the literal `coverage_type: edge` token. v1's
  strict reading is upheld as defensible but the edge-shaped
  scenarios meet the structural intent. All 13 original scenario
  blocks preserved verbatim (Viewer creation smoke through
  Collaborative filtering); only the Notes section grew with
  remediation context (KEEP rationale L238-253 + E-LAYER-COMPLIANCE-01
  deferral L254-265). Atlas alignment unchanged
  (sub_features_covered: [charts.sunburst, charts.sunburst.title,
  charts.sunburst.on-click, charts.sunburst.inherit-from-grid,
  charts.sunburst.include-nulls, charts.echart-base]); pyramid_layer
  integration unchanged; target_layer playwright unchanged;
  coverage_type regression unchanged; related_bugs (github-2954,
  github-3412) unchanged. "Project save and reopen" scenario block
  content (5 numbered steps L148-156) is byte-identical to v1.
claims:
  - check: A-STRUCT-MECH-01
    status: PASS
    evidence: Frontmatter parses as YAML; all four required fields present (feature=charts, sub_features_covered list of 6, target_layer=playwright, coverage_type=regression). Plus migration extras unchanged from v1 (pyramid_layer, ui_coverage_responsibility, produced_from=migrated, original_path, migration_date, migration_report, related_bugs).
  - check: A-STRUCT-MECH-02
    status: PASS
    evidence: Body contains `## Setup`, `## Scenarios`, `## Notes`, and 13 `### ` scenario headings under `## Scenarios` (Viewer creation smoke, Property panel opens, Table switching, Hierarchy configuration, Inherit from grid, Include nulls, View reset, Multi-selection, Select/filter on empty category, Project save and reopen, Layout save and apply, Old layout compatibility #2979, Collaborative filtering). All preserved from v1.
  - check: A-STRUCT-MECH-03
    status: PASS
    evidence: Every scenario heading has numbered steps starting with "1." (e.g., Viewer creation smoke 1-2, Hierarchy configuration 1-7, Multi-selection 1-6, Project save and reopen 1-5, Collaborative filtering 1-4). Unchanged from v1.
  - check: A-STRUCT-MECH-04
    status: PASS
    evidence: All 13 scenarios contain at least one numbered step; no empty scenarios. Project save and reopen has 5 steps (L148-156) — content byte-identical to v1.
  - check: A-STRUCT-MECH-05
    status: PASS
    evidence: target_layer=playwright matches the allowed enum {uitests-package, playwright, api-contract, manual-only}. Unchanged.
  - check: A-STRUCT-MECH-06
    status: PASS
    evidence: coverage_type=regression matches the allowed enum {smoke, regression, edge, perf}. Unchanged.
  - check: A-STRUCT-01
    status: PASS
    evidence: Multiple scenarios exercise 3+ sub-features in interaction. "Hierarchy configuration via Select Columns dialog" combines charts.sunburst + charts.echart-base (table binding/rendering) + Select Columns dialog driving + label/tooltip rendering — meets 3+ bar. "Project save and reopen" combines charts.sunburst + Layouts/Projects subsystem + hierarchy column preservation invariant. "Collaborative filtering" combines charts.sunburst + charts.sunburst.on-click + filter widget intersection.
  - check: A-STRUCT-02
    status: PASS
    evidence: "Old layout compatibility (issue #2979)" is structurally an edge/legacy-format compatibility scenario (pre-fix layout fixture, fixture-provisioning gap, explicit downstream `test.skip` option documented in inline NOTE block L178-184). "Select / filter on empty category (SPGI_v2.csv)" is structurally a null/grey-segment edge scenario. Both bodies communicate edge intent unambiguously even without per-scenario `coverage_type: edge` marker; A-STRUCT-02 intent test (edge case or negative path) satisfied. v1 verdict upgraded from SCOPE_REDUCTION to PASS on A-STRUCT-02 intent reading — see authoring_note.
  - check: A-STRUCT-03
    status: PASS
    evidence: File-level frontmatter coverage_type=regression provides label for all scenarios; A-STRUCT-03 explicitly allows frontmatter-level labeling. Severity-axis values (p0..p3) are not used.
  - check: A-STRUCT-04
    status: PASS
    evidence: Repeated authentication and dataset-open steps factored into `## Setup` (steps 1-4); cleanup also in Setup step 5. Per-scenario steps do not duplicate auth or table-open boilerplate. Unchanged from v1.
  - check: A-STRUCT-05
    status: PASS
    evidence: Atlas slice charts.yaml lists 5 sunburst-specific sub-features (charts.sunburst, .title, .on-click, .inherit-from-grid, .include-nulls). Scenario covers all 5 plus charts.echart-base = 100% sunburst-attributable coverage. Unchanged from v1.
  - check: A-STRUCT-06
    status: PASS
    evidence: 6 sub_features_covered at file level distributed across 13 scenarios; integration scenarios (Hierarchy, Inherit from grid, Include nulls, Multi-selection, Empty category, Project save and reopen, Layout save and apply, Old layout #2979, Collaborative filtering) each exercise 2+ sub-features. Average density ≥ 2 holds. Smoke-shaped "Viewer creation smoke" tolerated under file-level coverage_type=regression governance.
  - check: A-CONT-01
    status: PASS
    evidence: Real column names (SEX, Core, R101, Sampling Time), real datasets (System:DemoFiles/demog.csv, System:DemoFiles/SPGI_v2.csv), real properties (Inherit from grid, Include nulls, onClick), real bug IDs (github-2954, github-3412, issue #2979). No angle/square-bracket placeholders or generic stand-ins. Unchanged from v1; remediation Notes growth uses real cycle IDs (charts-remediate-2026-05-09, charts-evaluate-extract-2026-05-09, charts-automator-only-2026-05-08) and real failure keys (E-TRACE-02, E-LAYER-COMPLIANCE-01).
  - check: A-BUG-01
    status: PASS
    evidence: related_bugs frontmatter cites github-2954 and github-3412; scenario body explicitly maps them to "Hierarchy configuration via Select Columns dialog" (column-type defense-in-depth) and "Inherit from grid (demog.csv)" respectively. Atlas known_issues content not consulted (forbidden read); PASS based on visible bug coverage citations. Unchanged from v1.
  - check: A-MERIT-01
    status: PASS
    evidence: Notes section opt-outs cite real dependencies — date-column repro flagged as coverage gap by chain analyzer (atlas-level), Scatterplot cross-viewer pairing excluded per chain "single-viewer scenario" decision. Remediation cycle KEEP decision for Project save+reopen explicitly REJECTS Option 2 (drop) per Sunburst-specialty UI rationale, citing distinctness from radar-save-reopen-bug-spec.ts (GROK-18085 scope). E-LAYER-COMPLIANCE-01 deferral cites tree-spec.ts pairing dependency.
  - check: A-MERIT-02
    status: PASS
    evidence: The #2979 layout fixture deferral cites a concrete unresolved prerequisite (fixture not in repo or System:DemoFiles) and explicitly delegates resolution to downstream Automator with three named options. E-LAYER-COMPLIANCE-01 follow-up deferral cites concrete dependency (decision-log entry charts-evaluate-extract-2026-05-09 + tree-spec.ts refactor in same cycle as canonical pattern reference). Lattice Rule 13 satisfied — both deferrals reference real prerequisites.
scope_reduction_proposal: |
evidence_needed: []
