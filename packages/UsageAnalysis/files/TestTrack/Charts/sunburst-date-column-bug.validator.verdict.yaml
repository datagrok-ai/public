verdict: PASS
gate: B
review_round: 2
cycle_id: charts-automator-only-2026-05-08
artifact: sunburst-date-column-bug-spec.ts
scenario: sunburst-date-column-bug.md
prior_verdict_cycle: charts-migrate-2026-05-07
prior_round: 1
runtime_evidence:
  ran_against: https://dev.datagrok.ai
  duration_seconds: 31.7  # round 1 isolated (prior cycle)
  result: passed
  batch_run: '10/10 charts specs PASS in 6.2m wall clock (final 2026-05-08 batch — re-run confirmed)'
  spec_body_changed_in_cycle: false
  steps_executed:
    - "Steps 1-2: Open ae.csv, add Sunburst — PASS (date columns detected)"
    - "Step 3: hierarchy with date column — PASS (setOptions did not throw; no console error)"
    - "Step 6: Recovery to AETERM string column — PASS"
flake_observation:
  intermediate_batch_run_2026_05_08:
    summary: 'First 10/10 batch run on 2026-05-08 reported sunburst-date-column-bug-spec.ts as failing with 8.2m runtime'
    investigation: |
      No error-context.md or screenshot artifacts were produced for the
      failing run — only trace.zip. 8.2m duration exceeds the spec's
      test.setTimeout(300_000) = 5min. Likely cause: page.evaluate hung
      inside Step 3 setOptions(date-column hierarchy) — possibly the
      github-2954 bug class regressing transiently, or auth-context
      flake right after a long preceding spec in the batch.
    isolated_re_run_after_failure: 'PASS in 31.7s (deterministic)'
    final_batch_re_run: 'PASS as part of 10/10 batch (6.2m total)'
    classification: environmental-flake (transient; not reproducible
      in isolated re-run)
    remediation: monitor next 2 batch runs; if recurs, consider
      increasing test.setTimeout to 600_000 OR adding addViewer-level
      retry/recovery in Step 3.
notes: |
  github-2954 invariant satisfied: setOptions with date column did not throw,
  Sunburst remained visually stable, no console errors. Confirms the
  Charts 1.21.0 fix is in place on dev.
