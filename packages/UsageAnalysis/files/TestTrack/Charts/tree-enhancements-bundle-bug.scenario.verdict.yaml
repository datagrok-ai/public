verdict: PASS
gate: A
mode: scenario
review_round: 1
cycle_id: charts-remediate-2026-05-09
revision: 1
failure_keys: []
scope_reduction_proposal: |
evidence_needed: |
claims:
  - check_id: A-STRUCT-MECH-01
    status: PASS
    evidence: >
      Frontmatter parses as valid YAML. Required fields present: feature
      (charts), sub_features_covered (7 entries), target_layer (playwright),
      coverage_type (regression). C7 rename honored — coverage_type, not
      legacy priority.
  - check_id: A-STRUCT-MECH-02
    status: PASS
    evidence: >
      Body has "## Scenarios" heading (line 45) plus a "### Scenario 1: Tree
      8-enhancement bundle properties round-trip (github-3221 invariant)"
      heading (line 47).
  - check_id: A-STRUCT-MECH-03
    status: PASS
    evidence: >
      Scenario heading is followed by 10 numbered steps (1. through 10.) starting
      at line 51.
  - check_id: A-STRUCT-MECH-04
    status: PASS
    evidence: >
      Single scenario has 10 steps — no empty scenario.
  - check_id: A-STRUCT-MECH-05
    status: PASS
    evidence: >
      target_layer = playwright (valid enum value).
  - check_id: A-STRUCT-MECH-06
    status: PASS
    evidence: >
      coverage_type = regression (valid unified test-kind enum value, shared
      with atlas interactions[].coverage_type / edge_cases[].coverage_type).
  - check_id: A-STRUCT-01
    status: PASS
    evidence: >
      Single scenario interacts with 7 atlas sub-features (charts.tree,
      charts.tree.show-counts, charts.tree.on-click, charts.tree.font-size,
      charts.tree.layout, charts.tree.orient, charts.tree.include-nulls) — well
      above the 3-sub-feature threshold.
  - check_id: A-STRUCT-02
    status: PASS
    evidence: >
      coverage_type = regression — bug-focused regression scenario for
      github-3221. The scenario locks 8 capability regression bounds; the
      8-enhancement-bundle nature plus invariant assertions ("no console error
      across orientation transitions", "no white screen / DOM tear-down") cover
      bug-focused regression semantics. pyramid_layer: bug-focused makes the
      scenario class explicit. Regression coverage_type is the appropriate
      label for github-3221 reproduction.
  - check_id: A-STRUCT-03
    status: PASS
    evidence: >
      File-frontmatter coverage_type = regression (unified test-kind enum
      smoke|regression|edge|perf). Severity-axis values not used.
  - check_id: A-STRUCT-04
    status: PASS
    evidence: >
      Repeated setup factored into "## Setup" section (line 40) declaring
      clean Datagrok session + demog.csv as source table. Per-scenario
      hierarchy setup (CONTROL/SEX/RACE) is data-specific to step 1.
  - check_id: A-STRUCT-05
    status: PASS
    evidence: >
      Coverage of charts.tree.* sub-features is high (6 of charts.tree's
      property sub_features exercised) plus charts.tree top-level. Atlas line
      607 confirms charts.tree.layout exists; it is in sub_features_covered.
      No orphan declarations.
  - check_id: A-STRUCT-06
    status: PASS
    evidence: >
      Density: 7 sub-features / 1 scenario = 7.0 — well above the 2.0
      threshold. Bug-focused regression with very high information density
      per scenario.
  - check_id: A-CONT-01
    status: PASS
    evidence: >
      Real names throughout — System:DemoFiles/demog.csv (real DemoFiles
      path), CONTROL/SEX/RACE (real demog.csv columns), real property
      names (showCounts, onClick, fontSize, orient, includeNulls, layout,
      moleculeSize, showMouseOverLine), real reference values (LR, RL, TB,
      Filter, Select, orthogonal). No angle-bracket / square-bracket
      placeholders, no generic "column1" / "the column" stand-ins.
  - check_id: A-BUG-01
    status: PASS
    evidence: >
      related_bugs frontmatter populated with github-3221. Body explicitly
      cites the bug ("github-3221 invariant carrier", remediation cycle note
      on github-3221 fix bundle). Bug coverage requirement satisfied via
      route (a) — scenario lists bug id in related_bugs. Closes the gap
      surfaced in chain rev 2.
  - check_id: A-MERIT-01
    status: PASS
    evidence: >
      No scenario opted out for effort/complexity. Capability 6 (layout)
      conditional probe is justified by env-pending availability (Tree build
      on dev may not expose 'layout' property — real technical dependency).
      Capability 7 (moleculeSize) enumerate-only check justified by demog.csv
      lacking molecule columns + atlas not listing charts.tree.moleculeSize
      as a sub_feature (real atlas-level dependency).
  - check_id: A-MERIT-02
    status: PASS
    evidence: >
      No "TODO add later" / "to be done in next phase" without prerequisite.
      Notes section explicitly flags forward path: when Charts package
      exposes 'layout' on dev build, probe picks it up automatically;
      atlas curator may evaluate adding charts.tree.moleculeSize sub_feature
      in a future cycle (Migrator does not auto-amend atlas per A1 boundary).
      Both deferrals reference real prerequisites.
