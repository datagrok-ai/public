verdict: PASS
gate: E
review_round: 2
revision: 2
cycle_id: charts-remediate-2026-05-09
artifact: tree-enhancements-bundle-bug-spec.ts
scenario: tree-enhancements-bundle-bug.md
authoring_note: |
  Canonical isolated-context Critic E subagent verdict round 2 (2026-05-09).
  Predecessor verdict (review_round=1, cycle charts-automator-only-2026-05-08)
  was EVIDENCE_GAP citing E-TRACE-02 — charts.tree.layout in sub_features_covered
  but never probed in code; scenario step 7 (Capability 4 — layout) and step 8
  (Capability 7 — moleculeSize) uncovered.

  Migrator decision (Decision 3, Option A — TIGHTEN) implemented by Automator
  in this revision:
    1. `availableProps: string[]` captured during Setup softStep from
       `tree.props.getProperties().map(p => p.name)` (lines 59-61, 65).
    2. After existing capabilities, conditional probe block (lines 122-125):
         if (availableProps.includes('layout'))
           await exerciseProperty('layout', 'orthogonal', 'layout=orthogonal');
         else
           console.warn('[SKIP] charts.tree.layout not exposed on current Tree
             build; capability 4 deferred per scenario step 7 conditional');
    3. `// technical: capability 7 (moleculeSize) enumeration-only check
       delegated to Setup propNames log per scenario step 8 best-effort
       phrasing` comment (lines 127-128) near the showMouseOverLine
       softStep.

  Both formerly uncovered scenario steps now have either an active code path
  (layout: guarded probe) or a documented enumerate-only path (moleculeSize:
  technical comment + Setup propNames log). E-TRACE-02 cleared — guarded
  probe satisfies scenario step 7's conditional ("if
  tree.props.getProperties() includes 'layout'") and the enumeration-only
  technical comment satisfies scenario step 8's best-effort phrasing.

  Atlas mapping integrity preserved: charts.tree.layout retained in
  sub_features_covered (atlas line 607 confirmed — `charts.tree.layout`
  with description "`layout` — orthogonal or radial."). Forward path: when
  Charts package exposes `layout` on dev builds, probe automatically
  exercises it without scenario amendment.

failure_keys: []

claims:
  - check_id: E-STRUCT-MECH-01
    status: PASS
    evidence: |
      Spec file exists at public/packages/UsageAnalysis/files/TestTrack/charts/tree-enhancements-bundle-bug-spec.ts.
  - check_id: E-STRUCT-MECH-02
    status: PASS
    evidence: |
      TypeScript parses cleanly — balanced braces, well-formed test() signature, all imports resolve. The new conditional `if (availableProps.includes('layout'))` block (lines 122-125) is syntactically valid.
  - check_id: E-STRUCT-MECH-03
    status: PASS
    evidence: |
      One test('Tree — 8-enhancement bundle regression (github-3221)', ...) block at line 20.
  - check_id: E-STRUCT-MECH-04
    status: PASS
    evidence: |
      Test name "Tree — 8-enhancement bundle regression (github-3221)" substring-matches scenario heading "Tree viewer — 8-enhancement bundle regression (github-3221)".
  - check_id: E-STRUCT-MECH-05
    status: PASS
    evidence: |
      Canonical imports only — '@playwright/test' (line 13) and local '../spec-login' (line 14).
  - check_id: E-STRUCT-MECH-06
    status: PASS
    evidence: |
      Frontmatter block at lines 1-3 with sub_features_covered list of 7 IDs (charts.tree, charts.tree.show-counts, charts.tree.on-click, charts.tree.font-size, charts.tree.layout, charts.tree.orient, charts.tree.include-nulls); each resolves to a sub_features[].id in feature-atlas/charts.yaml. charts.tree.layout confirmed at atlas line 607.
  - check_id: E-TRACE-01
    status: PASS
    evidence: |
      All softStep calls and exerciseProperty invocations map to scenario capabilities by label. The guarded probe on line 122-123 maps to scenario step 7 (Capability 4 — layout). The technical comment on lines 127-128 documents that the showMouseOverLine softStep on line 129 covers Capability 8 (showMouseOverLine) and that scenario step 8 (Capability 7 — moleculeSize) is enumerate-only via the Setup propNames log (line 64).
  - check_id: E-TRACE-02
    status: PASS
    evidence: |
      Round 1 EVIDENCE_GAP cleared. Scenario step 7 (Capability 4 — layout) is now covered by guarded probe at lines 122-125: when `availableProps.includes('layout')` is true, `exerciseProperty('layout', 'orthogonal', 'layout=orthogonal')` runs; else `console.warn('[SKIP] charts.tree.layout not exposed...')` documents env-pending defensive skip per orchestrator Edit 10. Scenario step 8 (Capability 7 — moleculeSize) is covered by the enumeration via `tree.props.getProperties().map(p => p.name)` at line 60 plus the explicit `console.log('[Tree props]', result.propNames.join(', '))` on line 64 plus the technical-comment marker at lines 127-128. Every numbered scenario step (1-10) now has ≥1 code-step counterpart.
  - check_id: E-TRACE-03
    status: PASS
    evidence: |
      Verification steps use expect(...) — expect(result.types).toContain('Tree') (line 63), expect(result.ok).toBe(true) (lines 95, 142), expect(result.setOptionsThrew).toBe(false) (line 97), expect(result.hasContent).toBe(true) (lines 99, 143), expect(result.width).toBeGreaterThan(0) (lines 100, 144), expect(errorsDuring).toEqual([]) (line 102), expect(result.height).toBeGreaterThan(0) (line 145).
  - check_id: E-SEL-01
    status: PASS
    evidence: |
      Only selector used is page.locator('[name="Browse"]') at line 32; documented as Datagrok view-tab name attribute pattern.
  - check_id: E-SEL-02
    status: PASS
    evidence: |
      No invented selectors. All viewer + property access via JS API (`tree.setOptions`, `tree.props.getProperties()`, `tree.root`).
  - check_id: E-SEL-03
    status: PASS
    evidence: |
      No Dart input fields are typed into; no `.fill()` calls in the spec body.
  - check_id: E-HELP-01
    status: PASS
    evidence: |
      Helpers used (loginToDatagrok, specTestOptions, softStep, stepErrors) all imported from '../spec-login' (line 14). Inline `exerciseProperty` is local to the spec body and documented in migration report as "single-spec local pattern, not registry-eligible per A1 boundary".
  - check_id: E-HELP-02
    status: PASS
    evidence: |
      No reinvention of registered helpers — login + softStep + stepErrors used canonically.
  - check_id: E-LAYER-01
    status: PASS
    evidence: |
      Scenario frontmatter target_layer is 'playwright'; spec uses '@playwright/test' (line 13). Match.
  - check_id: E-LAYER-02
    status: NA
    evidence: |
      No layer override.
  - check_id: E-LAYER-COMPLIANCE-01
    status: PASS
    evidence: |
      target_layer=playwright requires ≥1 DOM-driving call. page.locator('[name="Browse"]').waitFor() at line 32 satisfies this. pyramid_layer is bug-focused (no ui-smoke sub-rule applies); ui_coverage_responsibility is empty.
  - check_id: E-BOUND-01
    status: PASS
    evidence: |
      Spec resides under public/packages/UsageAnalysis/files/TestTrack/charts/ (allowed by A1).
  - check_id: E-BOUND-02
    status: PASS
    evidence: |
      No edits to core/** or unrelated package source.
