revision: 2
schema_version: "v3.0"
cycle_id: projects-validate-batch-2026-05-05
feature: projects
mode: spec
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Projects/lifecycle-api-spec.ts
canonical_scenario: public/packages/UsageAnalysis/files/TestTrack/Projects/lifecycle-api.md
invoked_at: 2026-05-05T00:00:00Z
verdict: FAIL
gate: E
failure_keys: [E-TRACE-02, E-TRACE-03]

claims:
  - check: E-STRUCT-MECH-01
    status: PASS
    evidence: Spec file exists at expected path.
  - check: E-STRUCT-MECH-02
    status: PASS
    evidence: File parses as valid TypeScript; balanced braces, canonical imports.
  - check: E-STRUCT-MECH-03
    status: PASS
    evidence: At least one test() block present.
  - check: E-STRUCT-MECH-04
    status: PASS
    evidence: Test names traceable to scenario headings.
  - check: E-STRUCT-MECH-05
    status: PASS
    evidence: Imports limited to canonical paths.
  - check: E-STRUCT-MECH-06
    status: PASS
    evidence: Frontmatter with 5 sub_features resolving to feature-atlas/projects.yaml.
  - check: E-TRACE-01
    status: PASS
    evidence: All test code blocks trace to scenario steps where covered.
  - check: E-TRACE-02
    status: FAIL
    evidence: |
      Three numbered scenario steps uncovered:
      - S1.6 (find-by-id after delete must reject — "deleted means not-findable") absent
      - S2.3 (.page(1).by(2) second page with 3rd record) absent — spec only does one .by(2).list()
      - S3.2 (save → delete → repeat-delete on same id rejects) absent.
      Failure key.
  - check: E-TRACE-03
    status: FAIL
    evidence: |
      Degenerate verifications:
      - S3.1 uses expect([true,false]).toContain(r.rejected), accepts either outcome
        — nullifies "rejects" contract.
      - S2 pagination checks count<=2 && count>0 instead of expect(count).toBe(2).
      - S2.4 (count==3) folded into batch-save step instead of standalone aggregate
        check with pagination. Failure key.
  - check: E-SEL-01
    status: NA
    evidence: API-contract layer; no DOM selectors.
  - check: E-SEL-02
    status: NA
    evidence: No selectors used.
  - check: E-SEL-03
    status: NA
    evidence: No .fill() — JS API only.
  - check: E-HELP-01
    status: PASS
    evidence: Helpers used are registered or inline JS API.
  - check: E-HELP-02
    status: PASS
    evidence: No reinvention of registered helpers.
  - check: E-LAYER-01
    status: PASS
    evidence: target_layer api-contract matches spec.
  - check: E-LAYER-02
    status: NA
    evidence: No layer override.
  - check: E-BOUND-01
    status: PASS
    evidence: Under allowed path.
  - check: E-BOUND-02
    status: PASS
    evidence: No core/** changes.

scope_reduction_proposal: |
  (Not applicable — verdict is FAIL.)

evidence_needed: |
  (Not applicable — verdict is FAIL.)

remediation_required: |
  Add explicit S1.6 (find-after-delete rejection), S2.3 (page-2 by-2 pagination),
  S3.2 (double-delete rejection) tests. Strengthen S3.1 verification to assert
  rejection only (not [true,false]). Replace S2 count<=2 && count>0 with toBe(2).
  Surface S2.4 aggregate count==3 as standalone step.
