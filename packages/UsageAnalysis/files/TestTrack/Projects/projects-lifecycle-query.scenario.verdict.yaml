revision: 1
schema_version: "v3.0"
cycle_id: projects-migrate-force-2026-05-04
feature: projects
mode: scenario
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Projects/projects-lifecycle-query.md
invoked_at: 2026-05-04T00:00:00Z
verdict: PASS
gate: A
failure_keys: []
claims:
  - check_id: A-STRUCT-MECH-01
    status: PASS
    evidence: |
      Frontmatter parses as YAML. All 4 required fields present:
      `feature: projects`, `sub_features_covered:` (7 entries — all
      resolve in feature-atlas/projects.yaml: projects.upload,
      projects.api.save, projects.api.files.sync,
      projects.api.relations.list, projects.add-relation,
      projects.shell.share-via-context-menu, projects.api.get-by-id),
      `target_layer: playwright`, `coverage_type: regression`.
      C7 rename (`priority` → `coverage_type`) honored.
  - check_id: A-STRUCT-MECH-02
    status: PASS
    evidence: |
      `## Scenarios` heading present at line 59; one h3 scenario
      heading "### Main flow — query-source lifecycle" (line 61) under
      it. h3 satisfies the scenario-heading requirement (consistent
      with sibling lifecycle-api / complex / projects-lifecycle-files
      verdicts treating h3 as scenario heading).
  - check_id: A-STRUCT-MECH-03
    status: PASS
    evidence: |
      Single scenario carries 6 numbered steps (1.–6.: Open table,
      Save with Sync, Share, Rename Query, Rename project, Cleanup).
  - check_id: A-STRUCT-MECH-04
    status: PASS
    evidence: |
      Scenario non-empty (6 steps + Expected results sub-section).
  - check_id: A-STRUCT-MECH-05
    status: PASS
    evidence: |
      `target_layer: playwright` ∈ {uitests-package, playwright,
      api-contract, manual-only}.
  - check_id: A-STRUCT-MECH-06
    status: PASS
    evidence: |
      `coverage_type: regression` ∈ {smoke, regression, edge, perf}.
      The new `pyramid_layer: proactive-lifecycle` value is a
      DIFFERENT axis (pyramid_layer enum, not coverage_type) and is
      NOT subject to A-STRUCT-MECH-06; per chain rev 4 / Olena's Q3.2
      / D4 resolution, pyramid_layer enum was extended with
      `proactive-lifecycle` (chain analyzer prompt unresolved-
      ambiguity audit-only flag, not a Gate A failure). Gate A's
      mechanical checks gate target_layer + coverage_type only —
      pyramid_layer is informational.
  - check_id: A-STRUCT-01
    status: PASS
    evidence: |
      Single scenario combines 7 sub-features in interaction
      (projects.upload + projects.api.save + projects.api.files.sync
      across Steps 1-2; projects.shell.share-via-context-menu in
      Step 3; projects.api.relations.list + projects.api.get-by-id
      in Step 4 invariant assertion; projects.add-relation implicit
      via Data-Sync save). Far above the 3+ threshold.
  - check_id: A-STRUCT-02
    status: NA
    evidence: |
      Single-scenario file; coverage_type=regression at file level.
      A-STRUCT-02 requires at least one edge/perf scenario at
      section level — owned by Gate F territory and addressed by
      sibling scenarios (uploading.md, deleting.md, etc.). Same
      treatment as complex.scenario.verdict.yaml (also single mega-
      flow / single coverage_type) and projects-lifecycle-files
      pattern.
  - check_id: A-STRUCT-03
    status: PASS
    evidence: |
      `coverage_type: regression` at file frontmatter level (unified
      test-kind enum). Not a severity p0..p3 value.
  - check_id: A-STRUCT-04
    status: PASS
    evidence: |
      Repeated setup factored into `## Setup` section (6 numbered
      items: auth, project-name parametric, recipient placeholder,
      Helper 3 dependency, env deps with skip-with-logged-warning
      policy, cleanup contract). Single scenario so cross-scenario
      duplication is vacuously absent.
  - check_id: A-STRUCT-05
    status: NA
    evidence: |
      Section-level coverage owned by Gate F (5 dedicated keys
      including F-PROACTIVE-COVERAGE-01). This proactive-lifecycle
      scenario covers 7 atlas sub_features in one cell of the
      source_class × dep_lifecycle_op matrix (chain rev 4
      proactive_lifecycle_specs[1]). Atlas remainder owned by sibling
      scenarios per chain rev 4. Same treatment as
      lifecycle-api.scenario.verdict.yaml Reduction 2 and
      complex.scenario.verdict.yaml.
  - check_id: A-STRUCT-06
    status: PASS
    evidence: |
      Single scenario covers 7 sub_features; density 7/1 = 7 ≥ 2.
      `coverage_type: regression` (non-smoke), so non-exempt rule
      applies and passes comfortably.
  - check_id: A-CONT-01
    status: PASS
    evidence: |
      No placeholders flagged by A-CONT-01. `lifecycle-query-
      ${Date.now()}` is parametric naming (timestamp pattern, same
      as projects-lifecycle-files). `Samples:PostgresCustomers` is
      the canonical Samples-namespace query per
      `sa-2026-05-03-postgres-queries-public-data-substitution`.
      `Postgres:NorthwindTest` is a real DB connection name.
      `<RECIPIENT_USERNAME_TBD>` is intentional — explicitly
      documented in Setup point 3 as resolved at Automator stage,
      identical pattern to projects-lifecycle-files
      (mig-2026-04-29-fixture-placeholder decision-log policy
      precedent applies — same shape as complex.md "second user").
      Real `grok.dapi.queries.find/save`, `grok.functions.eval`,
      `grok.dapi.permissions.grant`, `grok.dapi.projects.find` API
      surface used (verified against atlas sub_features).
  - check_id: A-BUG-01
    status: PASS
    evidence: |
      bug-library/projects.yaml entry `github-3550` (line 162,
      test_coverage: needed) is addressed via path (a) — frontmatter
      `related_bugs: [github-3550]` AND scenario body explicitly
      cites the bug's full reproduction path: Step 4 walks
      "external-Query rename → project reopen → relation-resolution
      check" (the exact bug-library reproduction sequence: create
      query → use in dashboard/project → rename query → reopen).
      The github-3550 invariant assertion is named in scenario body
      ("Step 4 — Rename external dependency — Query rename
      (github-3550 full repro)") with explicit
      `projects.api.relations.list` observation surface citation in
      Notes. complex.md walks Step 9 partial repro (rename-only,
      single-scenario span); this scenario reinforces with full
      lifecycle reproduction including share + recipient-open
      stacking. Chain rev 4 records bugs_reinforcing: [github-3550].
  - check_id: A-MERIT-01
    status: PASS
    evidence: |
      No "effort" or "complexity" reasons cited. The two deferrals
      cite real dependencies: (1) Helper 3
      `helpers.playwright.session.logoutAndLoginAs` "NOT YET
      REGISTERED" (named in Setup point 4, applied to recipient-side
      assertions in Steps 3/5); (2) env-blocked DB connection +
      Query provisioning (Setup point 5) governed by
      skip-with-logged-warning policy per
      `sa-2026-05-03-postgres-queries-public-data-substitution`.
      Both are real prerequisite citations, not effort excuses.
      GROK-19403 un-shared-deps path explicitly excluded with
      cross-reference to projects-lifecycle-script.md (correct
      scope boundary).
  - check_id: A-MERIT-02
    status: PASS
    evidence: |
      No unprompted "TODO: add later" / "next phase" deferrals
      (Lattice Rule 13). Helper 3 deferral references the named
      registry entry; env skip references the named decision-log
      entry. Cleanup Step 6 restores Query name only if Step 4
      rename succeeded (conditional, not deferred). No surplus
      enumerated future work.
