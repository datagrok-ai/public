revision: 1
schema_version: "v3.0"
cycle_id: projects-migrate-force-2026-05-04
feature: projects
mode: scenario
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Projects/projects-lifecycle-derived.md
invoked_at: 2026-05-04T00:00:00Z
verdict: SCOPE_REDUCTION
gate: A
failure_keys: []
scope_reduction_proposal: |
  Phase A NEW scenario emitted by Orchestrator chain rev 4 covering the
  proactive_lifecycle_specs[5] cell (source_class=derived [Pivot,
  Aggregate, Join] x dep_lifecycle_op=share_with_recipient_open).
  bugs_reinforcing: [GROK-19103] — derivation-chain integrity reinforced
  via Step 2 explicit invariant assertion (Join result lands in active
  workspace via grok.shell.tables, NOT as stray separate project).

  Three reductions apply, each citing a real chain-/atlas-/decision-log-
  level dependency (no "effort" reasons). Pattern matches the precedent
  established by the sibling proactive-lifecycle slot
  `projects-lifecycle-db.scenario.verdict.yaml` (round 1, same cycle).

  REDUCTION 1 — A-STRUCT-02 (file-level edge or perf scenario): waive at
  the file level. Scenario is `coverage_type: regression` per chain rev
  3 output_plan and Migrator-prompt heuristic for proactive-lifecycle
  scenarios. Edge-class coverage for the derivation-chain integrity
  invariant is owned by the section-level chain rev 4 mechanism: this
  file IS the proactive-lifecycle slot for the derived cell. There is
  no further edge layer below it. Step 2's GROK-19103 invariant
  assertion is bug-reinforcement (explicitly typed `regression` per
  chain rev 3 output_plan classification, not `edge`). Section-level
  A-STRUCT-02 is satisfied via Gate F's F-PROACTIVE-COVERAGE-01 /
  F-PYRAMID-01 reading of the chain manifest, identical precedent to
  `projects-lifecycle-db.scenario.verdict.yaml` and
  `opening.scenario.verdict.yaml` (Edit 4 chain-driven section-coverage
  architecture).

  REDUCTION 2 — A-STRUCT-05 (>=70% atlas sub-feature coverage at file
  level): waive at the file level. The file declares 6
  sub_features_covered against ~21 REST sub_features and ~100+ total
  atlas sub_features. Per-file coverage of 6/100+ is by design for
  chain-aligned proactive-lifecycle scenarios; section-level coverage
  is owned by Gate F (F-PROACTIVE-COVERAGE-01). Same reduction
  precedent as `projects-lifecycle-db.scenario.verdict.yaml` and
  `opening.scenario.verdict.yaml` (file is a section-distributed slot,
  not a section-coverage owner).

  REDUCTION 3 — A-CONT-01 (placeholder without explicit `# parametric:`
  marker): accept the literal angle-bracket token
  `<RECIPIENT_USERNAME_TBD>` in Setup #3 under the same decision-log
  rationale as `share-project.scenario.verdict.yaml` and
  `projects-lifecycle-db.scenario.verdict.yaml`
  (`sa-2026-05-03-share-project-group-and-user-placeholders` —
  sed-substitution at Automator stage when accounts land). Setup #3
  prose ("Recipient placeholder: `<RECIPIENT_USERNAME_TBD>`") is
  semantically equivalent to the `# parametric:` marker the rule
  prescribes. One-line follow-up edit recommended for strict literal
  compliance, not required for SR acceptance.

  pyramid_layer enum: NEW value `proactive-lifecycle` is a Gate A
  pass-through (Gate A enforces `target_layer` and `coverage_type`
  enums via A-STRUCT-MECH-05/06; NOT `pyramid_layer`). The value is
  consumed by Gate F (F-PYRAMID-01 / F-PROACTIVE-COVERAGE-01) and by
  the chain manifest; no Gate-A failure_key trips on it. Per Q3.2 / D4
  resolution (chain rev 4 retro line 1698-1712), enum extension is
  intentional and out-of-scope for Gate A enforcement.

  GROK-19103 invariant assertion confirmed: Step 2 explicitly tests the
  bug invariant — the Join result lands in `grok.shell.tables` (active
  workspace), NOT as a stray separate project. Concrete assertion
  prescribed: `grok.shell.tables.length` increased by 1 (Join), not by
  2 (Join + stray project). This satisfies bug-reinforcement under
  chain rev 3 proactive_lifecycle_specs[5].bugs_reinforcing: [GROK-
  19103] and matches PROJECTS-SPLIT-COMPLETION-PLAN bug-traceability
  line 304 (primary bug-focused spec is `complex-derived-tables-
  spec.ts`; this file is reinforcement, not primary).

  UI delegation confirmed: frontmatter `ui_coverage_responsibility: []`
  and `ui_coverage_delegated_to: projects-ui-smoke.md` (lines 13-14).
  Prose at lines 33-36 and 113-117 documents that derived UI surfaces
  (Pivot dialog, Aggregate dialog, Join Tables dialog) are owned by
  `uploading.md`'s source-matrix scenarios (Cases 6/7 for Join,
  Cases 8/9 for Pivot+Aggregate). This scenario does NOT exercise
  those UI surfaces — it uses JS API or assumes matrix coverage.
  Helper 3 deferral (`helpers.playwright.session.logoutAndLoginAs` —
  NOT YET REGISTERED) cited in Setup #4 with explicit dependency
  reference; recipient-side assertions in Steps 4/6 are deferred per
  Lattice Rule 13.

  All other Gate A checks PASS.
claims:
  - check: A-STRUCT-MECH-01
    status: PASS
    evidence: |
      Frontmatter parses cleanly as YAML. All 4 required fields
      present: `feature: projects`, `sub_features_covered:` (6
      entries: projects.upload, projects.api.save,
      projects.api.files.sync, projects.add-relation,
      projects.shell.share-via-context-menu, projects.api.get-by-id),
      `target_layer: playwright`, `coverage_type: regression`.
      Additional fields (`pyramid_layer: proactive-lifecycle`,
      `ui_coverage_responsibility`, `ui_coverage_delegated_to`,
      `produced_from`, `original_path`, `migration_date`,
      `migration_report`, `related_bugs: [GROK-19103]`) parse as
      valid YAML. C7 rename honored (no scenario-side `priority`).
  - check: A-STRUCT-MECH-02
    status: PASS
    evidence: |
      `## Scenarios` heading at line 48; `### Main flow —
      derived-tables lifecycle` h3 scenario heading at line 50.
      Same shape as sibling `projects-lifecycle-files.md` /
      `projects-lifecycle-db.md` (precedent PASS).
  - check: A-STRUCT-MECH-03
    status: PASS
    evidence: |
      Single scenario contains 7 numbered steps (1. Open parent
      table from File share; 2. Build derived tables in workspace
      [Pivot/Aggregate/Join + GROK-19103 invariant assertion];
      3. Save project with Data Sync ON; 4. Share with second user;
      5. Rename external dependency — N/A for derived; 6. Rename
      project itself; 7. Cleanup).
  - check: A-STRUCT-MECH-04
    status: PASS
    evidence: |
      The single scenario has 7 steps; not empty.
  - check: A-STRUCT-MECH-05
    status: PASS
    evidence: |
      `target_layer: playwright` is a valid enum value
      (uitests-package | playwright | api-contract | manual-only).
  - check: A-STRUCT-MECH-06
    status: PASS
    evidence: |
      `coverage_type: regression` is a valid enum value
      (smoke | regression | edge | perf). NEW frontmatter value
      `pyramid_layer: proactive-lifecycle` is NOT validated by
      A-STRUCT-MECH-06 (this check covers `coverage_type` enum
      only); pyramid_layer enum extension per Q3.2 / D4 is a
      Gate A pass-through, consumed by Gate F.
  - check: A-STRUCT-01
    status: PASS
    evidence: |
      The Main flow scenario combines >=3 sub-features in
      interaction across the chained flow: projects.upload (Step 1
      — open demog.csv), projects.add-relation (Step 2 — derived-
      tables build creates parent->derived relations),
      projects.api.save + projects.api.files.sync (Step 3 — save
      with Data Sync ON for parent + 3 derived tables),
      projects.shell.share-via-context-menu + projects.api.get-by-id
      (Step 4 — share + recipient open verification),
      projects.api.get-by-id (Step 6 — rename via JS API). 6
      sub-features in the chained flow, well above the 3+ threshold.
  - check: A-STRUCT-02
    status: NA
    evidence: |
      Per scope_reduction_proposal Reduction 1: file-level
      A-STRUCT-02 non-applicable. Edge-class coverage for the
      derivation-chain integrity invariant is delegated to
      section-level via chain rev 3 proactive_lifecycle_specs[5]
      (this file IS the proactive slot; no further layer below).
      Authoritative gate: Gate F (F-PROACTIVE-COVERAGE-01,
      F-PYRAMID-01). Precedent:
      `projects-lifecycle-db.scenario.verdict.yaml`,
      `opening.scenario.verdict.yaml`.
  - check: A-STRUCT-03
    status: PASS
    evidence: |
      `coverage_type: regression` at file frontmatter level
      satisfies the unified test-kind enum
      (smoke|regression|edge|perf). No severity-axis values used.
      NEW pyramid_layer enum value `proactive-lifecycle` is not
      enforced by A-STRUCT-03 (this check covers test-kind enum
      only); pass-through to Gate F.
  - check: A-STRUCT-04
    status: PASS
    evidence: |
      Common setup (auth, project name allocation, recipient
      placeholder, helper-3 dependency, cleanup contract) factored
      into `## Setup` section at lines 38-46. Single scenario in
      the file — no duplication risk across sub-scenarios.
  - check: A-STRUCT-05
    status: NA
    evidence: |
      Per scope_reduction_proposal Reduction 2: file-level
      A-STRUCT-05 non-applicable for chain-aligned
      proactive-lifecycle slots. Section-level sub-feature
      coverage owned by Gate F via chain rev 3 manifest
      (proactive_lifecycle_specs[5] cell = derived x
      share_with_recipient_open). Precedent:
      `projects-lifecycle-db.scenario.verdict.yaml`,
      `opening.scenario.verdict.yaml`.
  - check: A-STRUCT-06
    status: PASS
    evidence: |
      Density: 1 scenario x 6 sub_features_covered = 6.0 average,
      well above the 2-per-scenario threshold (and above the 3+
      A-STRUCT-01 interaction floor). Threshold met without smoke
      exemption.
  - check: A-CONT-01
    status: FAIL
    evidence: |
      `<RECIPIENT_USERNAME_TBD>` angle-bracket placeholder appears
      in Setup #3 (line 42) without an explicit
      `# parametric: <reason>` marker. Decision-log
      `sa-2026-05-03-share-project-group-and-user-placeholders`
      and Setup #3 prose document the parameterization in human-
      readable form but the literal `# parametric:` marker is
      absent. Reduced under SR Reduction 3 — semantic equivalent
      (Setup #3 prose) accepted; one follow-up comment edit
      recommended for strict literal compliance. Same precedent
      as `share-project.scenario.verdict.yaml` and
      `projects-lifecycle-db.scenario.verdict.yaml`. Other
      parametric tokens (`lifecycle-derived-${Date.now()}` in
      Setup #2, `System:DemoFiles/demog.csv` qualified path) are
      real parametric naming patterns / real Datagrok file paths,
      not generic placeholders.
  - check: A-BUG-01
    status: PASS
    evidence: |
      `related_bugs: [GROK-19103]` in frontmatter (line 19-20)
      explicitly addresses the bug. Step 2 contains a concrete
      GROK-19103 invariant assertion: Join result lands in active
      workspace (`grok.shell.tables`), NOT as stray separate
      project; verified via `grok.shell.tables.length` increased
      by 1 (Join), not by 2 (Join + stray project). Atlas
      `known_issues[bug_id: GROK-19103]` (lines 970-977 of
      feature-atlas/projects.yaml) is therefore covered by this
      scenario via path (a) of A-BUG-01 (frontmatter
      `related_bugs` reference) AND path (b) (scenario body
      explicitly verifies the bug invariant). Reinforcement only —
      primary bug-focused coverage is `complex-derived-tables-
      spec.ts` per PROJECTS-SPLIT-COMPLETION-PLAN bug-
      traceability line 304.
  - check: A-MERIT-01
    status: PASS
    evidence: |
      No "effort" opt-outs. Step 5 (`Rename external dependency
      — N/A for derived`) cites a real atlas-level rationale:
      derived tables inherit their parent (here demog.csv); the
      parent File source has no rename surface (path is fixed).
      Per chain rev 3
      proactive_lifecycle_specs[5].dep_lifecycle_ops_covered:
      [share_with_recipient_open], `rename_external_dep` is NOT
      in scope. UI delegations (Pivot / Aggregate / Join dialog
      flows) cite `uploading.md` source-matrix Cases 6/7/8/9 and
      the standard `projects-ui-smoke.md` ownership pattern.
  - check: A-MERIT-02
    status: PASS
    evidence: |
      Recipient-side assertions in Steps 4/6 are deferred with
      explicit dependency reference to
      `helpers.playwright.session.logoutAndLoginAs` (Helper 3,
      "NOT YET REGISTERED" — Setup #4 cites same deferral
      pattern as `projects-lifecycle-files.md`). No "TODO: add
      later" without dependency reference. Lattice Rule 13
      satisfied. Notes block (lines 96-123) explicitly
      documents Phase A authoring origin, Wave 2C sequencing,
      Helper 3 deferral, and self-cleaning contract (Step 7
      deletes project; NOT in `deleting.md.depends_on`).
