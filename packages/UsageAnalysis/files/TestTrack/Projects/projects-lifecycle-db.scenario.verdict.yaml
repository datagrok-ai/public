revision: 1
schema_version: "v3.0"
cycle_id: projects-migrate-force-2026-05-04
feature: projects
mode: scenario
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Projects/projects-lifecycle-db.md
invoked_at: 2026-05-04T00:00:00Z
verdict: SCOPE_REDUCTION
gate: A
failure_keys: []
scope_reduction_proposal: |
  Phase-A NEW scenario emitted by Orchestrator chain rev 4 covering the
  proactive_lifecycle_specs[4] cell (source_class=db_table x
  dep_lifecycle_op=share_with_recipient_open). Pure proactive — no
  GROK ticket targets DB-source share+recipient-open today; the file
  exists to catch regressions in DB-connection-permission gating
  proactively (chain rev 3 proactive_lifecycle_specs[4],
  bugs_reinforcing: []).

  Three reductions, each citing a real chain-/atlas-/decision-log-level
  dependency (no "effort" reasons):

  REDUCTION 1 — A-STRUCT-02 (file-level edge or perf scenario): waive at
  the file level. Scenario is `coverage_type: regression` per chain
  rev 3 output_plan and Migrator-prompt heuristic for proactive-
  lifecycle scenarios. Edge coverage for the connection-share +
  per-user query-execution invariant is owned by the section-level
  chain rev 3 mechanism: this file IS the proactive-lifecycle slot
  for the DB-table cell. There is no further edge layer below it
  (bugs_reinforcing is empty by design — pure proactive). Section-
  level A-STRUCT-02 is satisfied via Gate F's F-PROACTIVE-COVERAGE-01
  / F-PYRAMID-01 reading of the chain manifest, identical to the
  precedent established in `opening.scenario.verdict.yaml` (Edit 4
  chain-driven section-coverage architecture).

  REDUCTION 2 — A-STRUCT-05 (>=70% atlas sub-feature coverage at file
  level): waive at the file level. The file declares 6
  sub_features_covered against ~21 REST sub_features and ~100+ total
  atlas sub_features. Per-file coverage of 6/100+ is by design for
  chain-aligned proactive-lifecycle scenarios; section-level
  coverage is owned by Gate F (F-PROACTIVE-COVERAGE-01). Same
  reduction precedent as `opening.scenario.verdict.yaml` (file is a
  section-distributed slot, not a section-coverage owner).

  REDUCTION 3 — A-CONT-01 (placeholder without explicit `# parametric:`
  marker): accept the literal angle-bracket token
  `<RECIPIENT_USERNAME_TBD>` in Setup #3 and Step 3 body under the
  same decision-log rationale as `share-project.scenario.verdict.yaml`
  (`sa-2026-05-03-share-project-group-and-user-placeholders` —
  sed-substitution at automator stage when accounts land). Setup #3
  prose semantically equivalent to the `# parametric:` marker the
  rule prescribes. One-line follow-up edit recommended for strict
  literal compliance, not required for SR acceptance.

  Filename deviation from canonical `kebab(source_class.id)=db-table`:
  per Olena's plan line 324 (D1 preference) the canonical filename
  uses `db` shorthand (`projects-lifecycle-db.md` /
  `projects-lifecycle-db-spec.ts`). Chain rev 4 reconciles this
  (`spec_target: projects-lifecycle-db-spec.ts`); scenario body lines
  35-41 explicitly surface the deviation for B14 retro / chain rev 4
  reconciliation. Confirmed: scenario references match the rev-4
  reconciled name. Deviation does not trip any A-* mechanical or
  structural check (filename is not a Gate A enforcement axis).

  Pyramid_layer enum: new value `proactive-lifecycle` is a Gate A
  pass-through (Gate A enforces `target_layer` and `coverage_type`
  enums, not `pyramid_layer`). The value is consumed by Gate F
  (F-PYRAMID-01 / F-PROACTIVE-COVERAGE-01) and by the chain manifest;
  no Gate-A failure_key is emitted for it.

  Connection-share proactive coverage confirmed: Step 3 explicitly
  tests project-share + connection-share coordination
  (`grok.dapi.permissions.grant(project, recipient, ...)` PLUS
  `grok.dapi.permissions.grant(connection, recipient, false)`),
  followed by the recipient-side Spawner-driven per-user query
  re-execution against `public.orders`. This is the proactive
  surface called out in the file's Notes block ("Connection-share
  is the proactive surface", lines 117-122) and matches chain rev 3
  proactive_lifecycle_specs[4].dep_lifecycle_ops_covered:
  [share_with_recipient_open].

  All other Gate A checks PASS.
claims:
  - check: A-STRUCT-MECH-01
    status: PASS
    evidence: |
      Frontmatter parses cleanly as YAML. All 4 required fields present:
      `feature: projects`, `sub_features_covered:` (6 entries:
      projects.upload, projects.api.save, projects.api.files.sync,
      projects.add-relation, projects.shell.share-via-context-menu,
      projects.api.get-by-id), `target_layer: playwright`,
      `coverage_type: regression`. Additional fields
      (pyramid_layer: proactive-lifecycle, ui_coverage_responsibility,
      ui_coverage_delegated_to, produced_from, original_path,
      migration_date, migration_report, related_bugs) parse as valid
      YAML. C7 rename honored (no `priority` at scenario frontmatter).
  - check: A-STRUCT-MECH-02
    status: PASS
    evidence: |
      `## Scenarios` heading at line 61; `### Main flow — DB-source
      lifecycle (proactive)` h3 scenario heading at line 63.
  - check: A-STRUCT-MECH-03
    status: PASS
    evidence: |
      Single scenario contains 6 numbered steps (1. Open table from
      DB via double-click; 2. Save project with Data Sync ON;
      3. Share project with second user; 4. Rename external
      dependency — N/A; 5. Rename project itself; 6. Cleanup).
  - check: A-STRUCT-MECH-04
    status: PASS
    evidence: |
      The single scenario has 6 steps; not empty.
  - check: A-STRUCT-MECH-05
    status: PASS
    evidence: |
      target_layer=playwright is a valid enum value
      (uitests-package | playwright | api-contract | manual-only).
  - check: A-STRUCT-MECH-06
    status: PASS
    evidence: |
      coverage_type=regression is a valid enum value
      (smoke | regression | edge | perf).
  - check: A-STRUCT-01
    status: PASS
    evidence: |
      The Main flow scenario combines >=3 sub-features in interaction:
      projects.upload (Step 1 — open table from DB),
      projects.api.save + projects.api.files.sync (Step 2 — save with
      Data Sync ON), projects.shell.share-via-context-menu +
      projects.add-relation (Step 3 — share project + share
      connection-relation), projects.api.get-by-id (Step 5 —
      rename via JS API). 5+ sub-features in the chained flow,
      well above the 3+ threshold.
  - check: A-STRUCT-02
    status: NA
    evidence: |
      Per scope_reduction_proposal Reduction 1: file-level
      A-STRUCT-02 non-applicable. Edge coverage delegated to
      section-level via chain rev 3 proactive_lifecycle_specs[4]
      (this file IS the proactive slot; no further layer below).
      Authoritative gate: Gate F (F-PROACTIVE-COVERAGE-01,
      F-PYRAMID-01). Precedent: `opening.scenario.verdict.yaml`.
  - check: A-STRUCT-03
    status: PASS
    evidence: |
      coverage_type=regression at file frontmatter level satisfies
      the unified test-kind enum (smoke|regression|edge|perf). No
      severity-axis values used. New pyramid_layer enum value
      `proactive-lifecycle` is not enforced by A-STRUCT-03 (this
      check covers test-kind enum only); pass-through to Gate F.
  - check: A-STRUCT-04
    status: PASS
    evidence: |
      Common setup (auth, project name allocation, recipient
      placeholder, helper-3 dependency, environment dependencies,
      cleanup contract) factored into `## Setup` section at file
      top (lines 43-59). Single scenario in the file — no
      duplication risk across sub-scenarios.
  - check: A-STRUCT-05
    status: NA
    evidence: |
      Per scope_reduction_proposal Reduction 2: file-level
      A-STRUCT-05 non-applicable for chain-aligned
      proactive-lifecycle slots. Section-level sub-feature
      coverage owned by Gate F via chain rev 3 manifest
      (proactive_lifecycle_specs[4] cell = db_table x
      share_with_recipient_open). Precedent:
      `opening.scenario.verdict.yaml`.
  - check: A-STRUCT-06
    status: PASS
    evidence: |
      Density: 1 scenario x 6 sub_features_covered = 6.0 average,
      well above the 2-per-scenario threshold (and above the 3+
      A-STRUCT-01 interaction floor). Threshold met without smoke
      exemption.
  - check: A-CONT-01
    status: FAIL
    evidence: |
      `<RECIPIENT_USERNAME_TBD>` angle-bracket placeholder appears
      in Setup #3 (line 47) and Step 3 (lines 73-74, 78-81)
      without an explicit `# parametric: <reason>` marker.
      Decision-log
      `sa-2026-05-03-share-project-group-and-user-placeholders`
      and Setup #3 prose document the parameterization in human-
      readable form but the literal `# parametric:` marker is
      absent. Reduced under SR Reduction 3 — semantic equivalent
      (Setup #3 prose) accepted; one follow-up comment edit
      recommended for strict literal compliance. Same precedent
      as `share-project.scenario.verdict.yaml`.
      Other parametric tokens (`lifecycle-db-${Date.now()}` in
      Setup #2, `Postgres:NorthwindTest:public:orders` qualified
      name) are real parametric naming patterns / real Datagrok
      connection paths, not generic placeholders.
  - check: A-BUG-01
    status: PASS
    evidence: |
      `related_bugs: []` and `bugs_reinforcing: []` (chain manifest)
      both empty by design — this file is pure proactive coverage
      per chain rev 3 proactive_lifecycle_specs[4]. Atlas
      `known_issues` carries no entry whose repro path intersects
      `source_class=db_table x share_with_recipient_open` (the 7
      cited cross-cutting bugs — GROK-19750, 19212, 19103, 19403,
      18345, 19728, github-3550 — are addressed by sibling
      scenarios per Gate F's F-BUG-COVERAGE-01 routing). The
      scenario body's Notes block (lines 106-122) explicitly
      documents the proactive-coverage rationale per
      PROJECTS-SPLIT-COMPLETION-PLAN bug-traceability line 311
      ("per-source bundle covers all plausible interaction cells
      regardless of whether a GROK ticket exists"). PASS by
      vacuity (no intersecting bug requires absorption here).
  - check: A-MERIT-01
    status: PASS
    evidence: |
      No "effort" opt-outs. Step 4 (`Rename external dependency
      — N/A for db_table`) cites a real atlas-level rationale:
      external DB-table rename is a connection-layer concern,
      not a project-relation concern; the project's relation
      refers to the table by qualified name and is not affected
      by table-name churn (per chain rev 3
      proactive_lifecycle_specs[4].dep_lifecycle_ops_covered
      explicitly excluding rename_external_dep). UI delegations
      (share / right-click flows) cite the standard
      `projects-ui-smoke.md` ownership pattern.
  - check: A-MERIT-02
    status: PASS
    evidence: |
      Recipient-side assertions in Steps 3/5 are deferred with
      explicit dependency reference to
      `helpers.playwright.session.logoutAndLoginAs` (Helper 3,
      "NOT YET REGISTERED" per b14-2026-04-30-re-auth-pattern-
      applied). Environment-skip conditions (DB connection
      provisioning + per-user permissions) reference real
      precedent
      `sa-2026-05-03-postgres-queries-public-data-substitution`.
      Filename deviation surfaced at lines 35-41 with concrete
      reconciliation paths for chain rev 4 (rename canonical to
      `db` OR rename file to `projects-lifecycle-db-table.md`)
      — chain rev 4 chose the former (`spec_target:
      projects-lifecycle-db-spec.ts`). No "TODO: add later"
      without dependency reference. Lattice Rule 13 satisfied.
