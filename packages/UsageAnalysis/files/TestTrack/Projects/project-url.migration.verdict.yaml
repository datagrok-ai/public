revision: 1
schema_version: "v3.0"
cycle_id: projects-migrate-force-2026-05-04
feature: projects
mode: migration
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Projects/project-url.md
invoked_at: 2026-05-04T00:00:00Z
verdict: PASS
gate: D
failure_keys: []
claims:
  - check: D-STRUCT-MECH-01
    status: PASS
    evidence: |
      Migration report present at
      public/packages/UsageAnalysis/files/TestTrack/Projects/project-url-migration-report.md
      and is non-empty (286 lines). Path matches the
      TestTrack/<section>/<original-name>-migration-report.md convention.
  - check: D-STRUCT-MECH-02
    status: PASS
    evidence: |
      Migration report contains a Markdown "Step mapping" table mapping
      every original numbered step (1, 2, 3, 4) plus the trailing
      "Expected" verification line and the trailing JSON `{ "order": 4 }`
      metadata. Six rows total.
  - check: D-STRUCT-MECH-03
    status: PASS
    evidence: |
      All 9 required Block 6 frontmatter fields present and well-formed:
      feature=projects, sub_features_covered (4 entries), target_layer=
      playwright, coverage_type=regression, produced_from=migrated,
      original_path, migration_date=2026-05-04, migration_report,
      related_bugs=[]. Deprecated `migrated_from` does NOT appear.
      C7 rename respected (coverage_type, not priority).
  - check: D-STRUCT-MECH-04
    status: PASS
    evidence: |
      All required `## ` sections present in migration report: Step
      mapping, Decisions, Opt-outs (SCOPE_REDUCTION proposals), Deferred
      items, Edge cases, Unresolved ambiguities.
  - check: D-STRUCT-MECH-05
    status: PASS
    evidence: |
      `original_path` field references
      public/packages/UsageAnalysis/files/TestTrack/Projects/project-url.md
      which exists in the repo (verified via Glob and git history; this is
      an in-place migration of the source).
  - check: D-STEP-01
    status: PASS
    evidence: |
      Every original numbered step (1 Browse > Dashboards; 2 Click project
      with 4 variants; 3 Context Panel > Links + copy URL; 4 New tab +
      paste URL) mapped to Scenarios steps 1-4 with the 4-variant list
      preserved in step 2 sub-bullets and parameterised over the four
      variants. No step opted out.
  - check: D-STEP-02
    status: PASS
    evidence: |
      Original "Expected: the corresponding project should be opened"
      preserved as Scenarios step 5 verification, expanded to enumerate
      what is verified (tables, viewers, view layout render; no console
      errors). The migration report's Step mapping row makes the axis
      preservation explicit.
  - check: D-EDGE-01
    status: PASS
    evidence: |
      Original lists no explicit edge cases. Migration report's "Edge
      cases" section enumerates 5 implicit edges (stale view-layout
      reference, session-bound permissions, variant-specific render
      differences, embedded URL params, source-class variation). Each
      is either preserved in the verification step or flagged for atlas
      curator / proactive_lifecycle_specs per chain rev 3. No silent
      drops.
  - check: D-STRUCT-01
    status: PASS
    evidence: |
      Cross-fixture dependency on upload-project.md / uploading.md /
      projects-copy-clone.md recorded in scenario-chains/projects.yaml
      rev 3 dependency_graph (project-url.md.depends_on lists all three
      producers; consumes lists the 4 specific variants). Acknowledged in
      scenario body Setup #1 and in the migration report's "Cross-fixture
      dependency complexity" Decisions bullet.
  - check: D-STRUCT-02
    status: PASS
    evidence: |
      Original 4-variant matrix (original / copied-with-link /
      copied-with-clone / saved-with-personal-view-customizations)
      preserved in migrated body via the parameterised loop "For each
      <variant> in the 4-variant list" with all 4 variants enumerated as
      sub-bullets in step 2. Matrix-paths-preserved = 4.
  - check: D-UI-DELEGATION-01
    status: PASS
    evidence: |
      No SCOPE_REDUCTION proposal at the .md level (Opt-outs section is
      "(none)"). The check is vacuously satisfied. The migration report's
      Decisions section explicitly addresses this (UI delegation under
      SCOPE_REDUCTION: N/A) and notes that the existing project-url-spec.ts
      Wave 1a SR is owned by the spec, not by this migration. The .md
      preserves all 4 variants and owns context-panel-links-url-copy +
      new-tab-open-url per chain rev 3 ui_coverage_responsibility.
  - check: D-SAN-01
    status: PASS
    evidence: |
      Migration report contains all required sections plus an "Edge cases"
      section. Decisions enumerated (target_layer, coverage_type,
      pyramid_layer, representative source, strategy, sibling tests,
      helpers, bug-library, decision-log, UI delegation, invariants, rev
      3 schema). Three relevant decision-log entries cross-referenced
      (mig-2026-04-30-project-url-migration as rev-2 baseline,
      mig-2026-04-29-source-text-correction, mig-2026-04-29-fixture-
      synthesized-inline).
  - check: D-SAN-02
    status: PASS
    evidence: |
      No silent drops. Original 4 numbered items + Expected line + JSON
      trailer all explicitly accounted for in the Step mapping table.
      Trailing `{ "order": 4 }` metadata routed to chain YAML
      `order_from_files` per chain-analyzer convention with explicit
      "metadata-not-step" decision row. The "with layout" → "personal
      view customizations" source-text correction (mig-2026-04-29-source-
      text-correction) is honored verbatim and acknowledged in Decisions
      and Unresolved ambiguities.
  - check: D-MERIT-01
    status: PASS
    evidence: |
      No steps opted out. Deferred items cite real automation
      prerequisites (composite fixture build coordination across two
      producers; Playwright BrowserContext.newPage vs newContext session
      pattern decision; clipboard-permissions vs DOM-read fallback;
      "original project" identity disambiguation) — each tied to
      helpers-registry gaps or chain-run state introspection, not "effort"
      alone.
  - check: D-MERIT-02
    status: PASS
    evidence: |
      All 4 Deferred items reference real prerequisites:
      helpers.playwright.projects.buildVariantsComposite candidate (not
      yet registered), helpers-registry.yaml :: playwright_layer.session
      missing, clipboard-permissions helper missing, runtime fixture-
      availability probe missing. Each is a concrete dependency, not a
      "next phase" hand-wave. Lattice Rule 13 satisfied.
