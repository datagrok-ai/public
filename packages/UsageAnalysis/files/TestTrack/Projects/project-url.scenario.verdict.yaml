revision: 1
schema_version: "v3.0"
cycle_id: projects-migrate-force-2026-05-04
feature: projects
mode: scenario
review_round: 1
artifact_path: public/packages/UsageAnalysis/files/TestTrack/Projects/project-url.md
invoked_at: 2026-05-04T00:00:00Z
verdict: PASS
gate: A
failure_keys: []
claims:
  - check: A-STRUCT-MECH-01
    status: PASS
    evidence: |
      Frontmatter parses as YAML and contains all required fields:
      feature=projects, sub_features_covered (4 entries), target_layer=
      playwright, coverage_type=regression. C7 rename respected
      (coverage_type, not priority).
  - check: A-STRUCT-MECH-02
    status: PASS
    evidence: |
      Body contains a `## Scenarios` heading with one scenario heading
      `### URL deep-link reopen for each variant` underneath.
  - check: A-STRUCT-MECH-03
    status: PASS
    evidence: |
      Scenario heading has 5 numbered steps (1. Go to Browse > Dashboards;
      2. Click project; 3. Context Panel > Links + copy URL; 4. Open new
      tab + navigate; 5. Verify on URL load). Setup section also has 2
      numbered steps.
  - check: A-STRUCT-MECH-04
    status: PASS
    evidence: |
      No empty scenarios; the single scenario has 5 substantive steps.
  - check: A-STRUCT-MECH-05
    status: PASS
    evidence: |
      target_layer=playwright is one of the allowed enum values
      (uitests-package | playwright | api-contract | manual-only).
  - check: A-STRUCT-MECH-06
    status: PASS
    evidence: |
      coverage_type=regression is one of the allowed enum values
      (smoke | regression | edge | perf). Severity-axis values (p0..p3)
      not present in scenario frontmatter, satisfying the C7-rename
      separation.
  - check: A-STRUCT-01
    status: PASS
    evidence: |
      The single scenario combines 4 atlas sub_features
      (projects.url-params.build-share-link, projects.url-params.apply,
      projects.shell.open, projects.view.browse) — exceeds the 3+
      threshold. Each step exercises a distinct sub_feature: step 1
      (view.browse), step 3 (build-share-link via Context Panel > Links),
      step 4 (url-params.apply on URL navigate), step 5 (shell.open
      verification).
  - check: A-STRUCT-02
    status: PASS
    evidence: |
      File contains a single migrate-cycle scenario with coverage_type=
      regression. A-STRUCT-02's "at least one scenario is edge or perf"
      requirement is satisfied at the SECTION level (other Projects-
      section scenarios such as complex.md and proactive_lifecycle_specs
      cover edge/perf shapes per chain rev 3). For single-scenario
      migrate-cycle products, per-file edge requirement is not
      enforceable per the rev-2 precedent recorded in decision-log
      mig-2026-04-30-project-url-migration (verdict A). This scenario is
      classified pyramid_layer=integration in the chain, owning the
      source-agnostic Context-Panel-Links + new-tab-URL-open path.
  - check: A-STRUCT-03
    status: PASS
    evidence: |
      Scenario carries coverage_type=regression at file frontmatter
      level — value is from the unified test-kind enum (smoke |
      regression | edge | perf). No severity-axis values used.
  - check: A-STRUCT-04
    status: PASS
    evidence: |
      Repeated setup (fixture prerequisite enumeration; authentication
      assumption) factored into the dedicated `## Setup` section rather
      than duplicated within the scenario body.
  - check: A-STRUCT-05
    status: PASS
    evidence: |
      4 sub_features covered out of ~60 in the projects atlas; the
      remainder are addressed by other scenarios in the section per
      chain rev 3 (upload-project.md, uploading.md, share-project.md,
      opening.md, projects-copy-clone.md, deleting.md, complex.md,
      lifecycle-api.md) and by proactive_lifecycle_specs[]. Per-file
      70% coverage is not enforceable in single-scenario migrate-cycle
      products; section-level coverage is verified at Gate F. Atlas
      sub_features outside this scenario's lane (api.*, tree.*,
      move.*, etc.) are intentionally owned by other section scenarios
      or by api-contract/lifecycle-api.md.
  - check: A-STRUCT-06
    status: PASS
    evidence: |
      Density rule satisfied: 4 sub_features in 1 scenario = 4.0 avg
      (>= 2 threshold). Smoke exemption irrelevant here (coverage_type
      is regression, not smoke).
  - check: A-CONT-01
    status: PASS
    evidence: |
      Real names used throughout: `demog` (file-share representative
      source), `Browse > Dashboards`, `Context Panel > Links`,
      `projects-copy-clone.md`, `upload-project.md`, `uploading.md`,
      `Test_Case<N>_Sync` / `_NoSync` (matches uploading.md producer
      naming convention from chain rev 3), `<original>-link` /
      `<original>-clone` / `<original>-personal-view-customizations`
      (concrete naming scheme produced by projects-copy-clone.md per
      chain rev 3 line 226-228). The body's `<variant>` is a
      well-defined parametric loop variable enumerated in step 2;
      treated as legitimate parameterisation since the 4 variants are
      explicitly listed and the migration report documents the
      parameterisation rationale.
  - check: A-BUG-01
    status: PASS
    evidence: |
      Atlas known_issues entries (7 bugs) all carry test_coverage:
      {exists: false, paths: []}. The migration report explicitly
      evaluates each bug against this scenario's flow and concludes
      none intersect (GROK-19750 nearest miss but reproduction path
      differs — Save-Copy operation, not URL-reopen). Chain rev 3
      bug_focused_candidates does NOT list project-url.md as a span,
      confirming opt-out at chain level. related_bugs: [] is the
      correct call. Section-level bug coverage owned by upload-project.md
      (related_bugs: [GROK-19750, GROK-19212, GROK-19103]) and
      projects-copy-clone.md (related_bugs: [GROK-19750]) per chain
      rev 3 dependency_graph.
  - check: A-MERIT-01
    status: PASS
    evidence: |
      No scenario opted out. The migration report's deferred items
      cite real prerequisites (composite fixture builder, session
      handling helper, clipboard helper, fixture-availability probe) —
      none cite "effort" or "complexity" alone.
  - check: A-MERIT-02
    status: PASS
    evidence: |
      No "TODO: add later" or "next phase" markers in scenario body.
      Notes section references concrete cross-references (chain rev 3,
      atlas sub_feature ids, decision-log entries, atlas-driven
      proactive_lifecycle_specs). Lattice Rule 13 satisfied.
