/* eslint-disable max-len */
/* eslint-disable comma-spacing */
/* eslint-disable quotes */
/* ---
generated: features/viewers/grid/grid-rows.feature
generator: @datagrok-libraries/bdd — do not edit; run `grok-bdd compile` to regenerate
sub_features_covered: [viewers.grid]
--- */
import {test} from '@playwright/test';
import '../../../bindings/grid.js';
import '../../../bindings/nx.js';
import '../../../bindings/spaces.js';
import '../../../bindings/tile-viewer.js';
import '../../../bindings/trellis-plot.js';
import '@datagrok-libraries/bdd/bindings/common/kinds';
import '@datagrok-libraries/bdd/bindings/common/parameter-types';
import '@datagrok-libraries/bdd/bindings/platform/datasets';
import '@datagrok-libraries/bdd/bindings/platform/elements';
import {loggedIn} from '@datagrok-libraries/bdd/bindings/common/session';
import {focusOn, pressKey, shouldBe} from '@datagrok-libraries/bdd/bindings/common/steps';
import {currentRowIs} from '@datagrok-libraries/bdd/bindings/platform/columns';
import {allRowsSelected, clearSelection, columnsSelected, currentRowValue, filterBetween, filterPasses, filterPassesAll, filterTo, noColumnsSelected, noneSelected, onlyOfAnySelected, onlyOfSelected, resetFilter, rowsRangeSelected, selectFirstRows, selectedPassFilter, selectedRowCount} from '@datagrok-libraries/bdd/bindings/platform/data';
import {openDataset} from '@datagrok-libraries/bdd/bindings/platform/steps';
import {clickArea, clickAreaHolding, doubleClickArea, dragAreaToArea, dragSelectionBetweenAreas, noErrors, pickFromAreaContextMenu, readingAsRemembered, readingDiffers, readingHigher, readingIs, readingReads, rememberReading, repainted, setProperty, showsRows, takeSnapshot} from '@datagrok-libraries/bdd/bindings/tiers/viewers/steps';
import {ds, el, feature, journey} from '@datagrok-libraries/bdd/runtime';

test.describe("Grid row selection, navigation and row source", () => {
  const session = feature(test, "features/viewers/grid/grid-rows.feature", import.meta.url);
  test("Grid row selection, navigation and row source", {tag: ["@journey", "@viewers", "@realizes:viewers.grid"]}, async ({browser}) => {
    const page = await session.page(browser);
    const run = journey(test, 12, page);
    await session.step(27, "Given user is logged in", () => loggedIn(page));
    await session.step(28, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(29, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
    await session.step(30, "And no rows should be selected", () => noneSelected(page));
    await run.scenario("A click sets the current row and selects nothing", async () => {
      await session.step(33, "When user clicks on the \"cell 6 of AGE\" area of grid", () => clickArea(page, "cell 6 of AGE", el("grid")));
      await session.step(34, "Then row 6 should be current", () => currentRowIs(page, 6));
      await session.step(35, "And the \"current row\" reading of grid should be 6", () => readingIs(page, "current row", el("grid"), 6));
      await session.step(36, "And the \"current column\" reading of grid should be \"AGE\"", () => readingReads(page, "current column", el("grid"), "AGE"));
      await session.step(37, "And no rows should be selected", () => noneSelected(page));
      await session.step(38, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Shift-dragging the row-number strip selects a range", async () => {
      await session.step(41, "When user drags a selection box from the \"row header 6\" area to the \"row header 11\" area of grid", () => dragSelectionBetweenAreas(page, "row header 6", "row header 11", el("grid")));
      await session.step(42, "Then 6 rows should be selected", () => selectedRowCount(page, 6));
      await session.step(43, "And rows 6 to 11 should be selected", () => rowsRangeSelected(page, 6, 11));
      await session.step(44, "And no columns should be selected", () => noColumnsSelected(page));
      await session.step(45, "And grid should have repainted", () => repainted(page, el("grid")));
      await session.step(46, "When user clears the row selection", () => clearSelection(page));
      await session.step(47, "Then no rows should be selected", () => noneSelected(page));
      await session.step(48, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Control+A selects every row and every column and Escape clears both", async () => {
      await session.step(51, "When user clicks on the \"cell 1 of AGE\" area of grid", () => clickArea(page, "cell 1 of AGE", el("grid")));
      await session.step(52, "And user presses Control+A", () => pressKey(page, "Control+A"));
      await session.step(53, "Then all rows should be selected", () => allRowsSelected(page));
      await session.step(54, "And columns \"USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY\" should be selected", () => columnsSelected(page, "USUBJID, AGE, SEX, RACE, DIS_POP, HEIGHT, WEIGHT, DEMOG, CONTROL, STARTED, SEVERITY"));
      await session.step(55, "When user focuses on grid overlay", () => focusOn(page, el("grid overlay")));
      await session.step(56, "Then grid overlay should be focused", () => shouldBe(page, el("grid overlay"), "focused"));
      await session.step(57, "When user presses Escape", () => pressKey(page, "Escape"));
      await session.step(58, "Then no rows should be selected", () => noneSelected(page));
      await session.step(59, "And no columns should be selected", () => noColumnsSelected(page));
      await session.step(60, "And the \"current row\" reading of grid should be 0", () => readingIs(page, "current row", el("grid"), 0));
      await session.step(61, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Six Control+clicks on headers select six columns without an error", async () => {
      await session.step(64, "When user clicks on the \"header AGE\" area of grid holding Control", () => clickAreaHolding(page, "header AGE", el("grid"), "Control"));
      await session.step(65, "And user clicks on the \"header HEIGHT\" area of grid holding Control", () => clickAreaHolding(page, "header HEIGHT", el("grid"), "Control"));
      await session.step(66, "And user clicks on the \"header WEIGHT\" area of grid holding Control", () => clickAreaHolding(page, "header WEIGHT", el("grid"), "Control"));
      await session.step(67, "And user clicks on the \"header SEX\" area of grid holding Control", () => clickAreaHolding(page, "header SEX", el("grid"), "Control"));
      await session.step(68, "And user clicks on the \"header RACE\" area of grid holding Control", () => clickAreaHolding(page, "header RACE", el("grid"), "Control"));
      await session.step(69, "And user clicks on the \"header DIS_POP\" area of grid holding Control", () => clickAreaHolding(page, "header DIS_POP", el("grid"), "Control"));
      await session.step(70, "Then columns \"AGE, HEIGHT, WEIGHT, SEX, RACE, DIS_POP\" should be selected", () => columnsSelected(page, "AGE, HEIGHT, WEIGHT, SEX, RACE, DIS_POP"));
      await session.step(71, "And no errors should have been logged", () => noErrors(page));
      await session.step(72, "When user presses Escape", () => pressKey(page, "Escape"));
      await session.step(73, "Then no columns should be selected", () => noColumnsSelected(page));
    });
    await run.scenario("Arrows and Control+Home, Control+End and PageDown move the current row", async () => {
      await session.step(76, "When user clicks on the \"cell 5 of AGE\" area of grid", () => clickArea(page, "cell 5 of AGE", el("grid")));
      await session.step(77, "And user presses ArrowDown", () => pressKey(page, "ArrowDown"));
      await session.step(78, "And user presses ArrowDown", () => pressKey(page, "ArrowDown"));
      await session.step(79, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
      await session.step(80, "And user presses ArrowRight", () => pressKey(page, "ArrowRight"));
      await session.step(81, "Then row 7 should be current", () => currentRowIs(page, 7));
      await session.step(82, "And the \"current column\" reading of grid should be \"RACE\"", () => readingReads(page, "current column", el("grid"), "RACE"));
      await session.step(83, "When user presses Control+Home", () => pressKey(page, "Control+Home"));
      await session.step(84, "Then row 1 should be current", () => currentRowIs(page, 1));
      await session.step(85, "When user presses Control+End", () => pressKey(page, "Control+End"));
      await session.step(86, "Then row 1000 should be current", () => currentRowIs(page, 1000));
      await session.step(87, "When user presses Control+Home", () => pressKey(page, "Control+Home"));
      await session.step(88, "And user takes a snapshot of grid", () => takeSnapshot(page, el("grid")));
      await session.step(89, "And user presses PageDown", () => pressKey(page, "PageDown"));
      await session.step(90, "Then the \"current row\" reading of grid should be higher than before", () => readingHigher(page, "current row", el("grid")));
      await session.step(91, "When user takes a snapshot of grid", () => takeSnapshot(page, el("grid")));
      await session.step(92, "And user presses PageDown", () => pressKey(page, "PageDown"));
      await session.step(93, "Then the \"current row\" reading of grid should be higher than before", () => readingHigher(page, "current row", el("grid")));
      await session.step(94, "When user presses Control+Home", () => pressKey(page, "Control+Home"));
      await session.step(95, "Then row 1 should be current", () => currentRowIs(page, 1));
      await session.step(96, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Space selects the current row and Shift+Enter every row of the same value", async () => {
      await session.step(99, "When user clicks on the \"cell 4 of AGE\" area of grid", () => clickArea(page, "cell 4 of AGE", el("grid")));
      await session.step(100, "And user presses Space", () => pressKey(page, "Space"));
      await session.step(101, "Then 1 row should be selected", () => selectedRowCount(page, 1));
      await session.step(102, "And only rows where \"USUBJID\" is \"X0273T21000400002\" should be selected", () => onlyOfSelected(page, "USUBJID", "X0273T21000400002"));
      await session.step(103, "When user presses Space", () => pressKey(page, "Space"));
      await session.step(104, "Then no rows should be selected", () => noneSelected(page));
      await session.step(105, "When user clicks on the \"cell 4 of SEX\" area of grid", () => clickArea(page, "cell 4 of SEX", el("grid")));
      await session.step(106, "And user presses Shift+Enter", () => pressKey(page, "Shift+Enter"));
      await session.step(107, "Then only rows where \"SEX\" is \"M\" should be selected", () => onlyOfSelected(page, "SEX", "M"));
      await session.step(108, "And 447 rows should be selected", () => selectedRowCount(page, 447));
      await session.step(109, "When user clears the row selection", () => clearSelection(page));
      await session.step(110, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Allow Row Selection off keeps the caret moving and the selection empty", async () => {
      await session.step(113, "When user sets \"Allow Row Selection\" property of grid to \"false\"", () => setProperty(page, "Allow Row Selection", el("grid"), "false"));
      await session.step(114, "And user clicks on the \"cell 5 of AGE\" area of grid", () => clickArea(page, "cell 5 of AGE", el("grid")));
      await session.step(115, "Then row 5 should be current", () => currentRowIs(page, 5));
      await session.step(116, "When user presses ArrowDown", () => pressKey(page, "ArrowDown"));
      await session.step(117, "Then row 6 should be current", () => currentRowIs(page, 6));
      await session.step(118, "When user presses Shift+ArrowDown", () => pressKey(page, "Shift+ArrowDown"));
      await session.step(119, "And user presses Shift+ArrowDown", () => pressKey(page, "Shift+ArrowDown"));
      await session.step(120, "And user presses Shift+ArrowDown", () => pressKey(page, "Shift+ArrowDown"));
      await session.step(121, "Then row 9 should be current", () => currentRowIs(page, 9));
      await session.step(122, "And no rows should be selected", () => noneSelected(page));
      await session.step(123, "When user presses Space", () => pressKey(page, "Space"));
      await session.step(124, "Then no rows should be selected", () => noneSelected(page));
      await session.step(125, "When user sets \"Allow Row Selection\" property of grid to \"true\"", () => setProperty(page, "Allow Row Selection", el("grid"), "true"));
      await session.step(126, "And user presses Space", () => pressKey(page, "Space"));
      await session.step(127, "Then 1 row should be selected", () => selectedRowCount(page, 1));
      await session.step(128, "When user clears the row selection", () => clearSelection(page));
      await session.step(129, "Then no rows should be selected", () => noneSelected(page));
      await session.step(130, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Row Source decides how many rows the grid shows", async () => {
      await session.step(133, "When user filters rows where \"SEX\" is \"M\"", () => filterTo(page, "SEX", "M"));
      await session.step(134, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(135, "When user selects the first 5 rows", () => selectFirstRows(page, 5));
      await session.step(136, "Then 5 rows should be selected", () => selectedRowCount(page, 5));
      await session.step(137, "When user sets \"Row Source\" property of grid to \"Filtered\"", () => setProperty(page, "Row Source", el("grid"), "Filtered"));
      await session.step(138, "Then grid should show 447 rows", () => showsRows(page, el("grid"), 447));
      await session.step(139, "When user sets \"Row Source\" property of grid to \"Selected\"", () => setProperty(page, "Row Source", el("grid"), "Selected"));
      await session.step(140, "Then grid should show 5 rows", () => showsRows(page, el("grid"), 5));
      await session.step(141, "When user sets \"Row Source\" property of grid to \"All\"", () => setProperty(page, "Row Source", el("grid"), "All"));
      await session.step(142, "Then grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
      await session.step(143, "And 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(144, "When user resets the filter", () => resetFilter(page));
      await session.step(145, "And user clears the row selection", () => clearSelection(page));
      await session.step(146, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(147, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("A filter and a sort share one order", async () => {
      await session.step(150, "When user filters rows where \"AGE\" is between 31 and 999", () => filterBetween(page, "AGE", 31, 999));
      await session.step(151, "Then 844 rows should pass the filter", () => filterPasses(page, 844));
      await session.step(152, "When user sets \"Row Source\" property of grid to \"Filtered\"", () => setProperty(page, "Row Source", el("grid"), "Filtered"));
      await session.step(153, "Then grid should show 844 rows", () => showsRows(page, el("grid"), 844));
      await session.step(154, "When user remembers the \"row order\" reading of grid", () => rememberReading(page, "row order", el("grid")));
      await session.step(155, "And user double-clicks on the \"header AGE\" area of grid", () => doubleClickArea(page, "header AGE", el("grid")));
      await session.step(156, "And user double-clicks on the \"header AGE\" area of grid", () => doubleClickArea(page, "header AGE", el("grid")));
      await session.step(157, "Then the \"sort direction\" reading of grid should be \"ascending\"", () => readingReads(page, "sort direction", el("grid"), "ascending"));
      await session.step(158, "And grid should show 844 rows", () => showsRows(page, el("grid"), 844));
      await session.step(159, "And the \"row order\" reading of grid should differ from before", () => readingDiffers(page, "row order", el("grid")));
      await session.step(160, "When user presses Control+Home", () => pressKey(page, "Control+Home"));
      await session.step(161, "Then the \"current row\" reading of grid should be 95", () => readingIs(page, "current row", el("grid"), 95));
      await session.step(162, "And \"AGE\" of the current row should be \"31\"", () => currentRowValue(page, "AGE", "31"));
      await session.step(163, "When user presses ArrowDown", () => pressKey(page, "ArrowDown"));
      await session.step(164, "Then the \"current row\" reading of grid should be 115", () => readingIs(page, "current row", el("grid"), 115));
      await session.step(165, "When user presses ArrowDown", () => pressKey(page, "ArrowDown"));
      await session.step(166, "Then the \"current row\" reading of grid should be 117", () => readingIs(page, "current row", el("grid"), 117));
      await session.step(167, "When user presses Shift+ArrowDown", () => pressKey(page, "Shift+ArrowDown"));
      await session.step(168, "And user presses Shift+ArrowDown", () => pressKey(page, "Shift+ArrowDown"));
      await session.step(169, "And user presses Shift+ArrowDown", () => pressKey(page, "Shift+ArrowDown"));
      await session.step(170, "And user presses Shift+ArrowDown", () => pressKey(page, "Shift+ArrowDown"));
      await session.step(171, "Then the \"current row\" reading of grid should be 497", () => readingIs(page, "current row", el("grid"), 497));
      await session.step(172, "And only rows where \"USUBJID\" is one of \"X0273T22000400018, X0273T26004000004, X0273T29013500101, X0273T37001500013\" should be selected", () => onlyOfAnySelected(page, "USUBJID", "X0273T22000400018, X0273T26004000004, X0273T29013500101, X0273T37001500013"));
      await session.step(173, "And every selected row should pass the filter", () => selectedPassFilter(page));
      await session.step(174, "And 844 rows should pass the filter", () => filterPasses(page, 844));
      await session.step(175, "When user clears the row selection", () => clearSelection(page));
      await session.step(176, "And user picks \"Sort > Reset\" from the context menu of the \"header AGE\" area of grid", () => pickFromAreaContextMenu(page, "Sort > Reset", "header AGE", el("grid")));
      await session.step(177, "Then the \"row order\" reading of grid should be as remembered", () => readingAsRemembered(page, "row order", el("grid")));
      await session.step(178, "When user sets \"Row Source\" property of grid to \"All\"", () => setProperty(page, "Row Source", el("grid"), "All"));
      await session.step(179, "And user resets the filter", () => resetFilter(page));
      await session.step(180, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(181, "And grid should show 1000 rows", () => showsRows(page, el("grid"), 1000));
      await session.step(182, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Shift+click, Control+click, a plain click and a drag on the row strip", async () => {
      await session.step(185, "When user clicks on the \"row header 5\" area of grid", () => clickArea(page, "row header 5", el("grid")));
      await session.step(186, "And user clicks on the \"row header 10\" area of grid holding Shift", () => clickAreaHolding(page, "row header 10", el("grid"), "Shift"));
      await session.step(187, "Then rows 5 to 10 should be selected", () => rowsRangeSelected(page, 5, 10));
      await session.step(188, "And 6 rows should be selected", () => selectedRowCount(page, 6));
      await session.step(189, "And the \"current row\" reading of grid should be 10", () => readingIs(page, "current row", el("grid"), 10));
      await session.step(190, "When user clicks on the \"row header 15\" area of grid holding Control", () => clickAreaHolding(page, "row header 15", el("grid"), "Control"));
      await session.step(191, "Then 7 rows should be selected", () => selectedRowCount(page, 7));
      await session.step(192, "And only rows where \"USUBJID\" is one of \"X0273T21000500006, X0273T21000500008, X0273T21000700006, X0273T21000800002, X0273T21000800004, X0273T21000800006, X0273T21001100003\" should be selected", () => onlyOfAnySelected(page, "USUBJID", "X0273T21000500006, X0273T21000500008, X0273T21000700006, X0273T21000800002, X0273T21000800004, X0273T21000800006, X0273T21001100003"));
      await session.step(193, "And the \"current row\" reading of grid should be 10", () => readingIs(page, "current row", el("grid"), 10));
      await session.step(194, "When user clicks on the \"row header 15\" area of grid holding Control", () => clickAreaHolding(page, "row header 15", el("grid"), "Control"));
      await session.step(195, "Then 6 rows should be selected", () => selectedRowCount(page, 6));
      await session.step(196, "And rows 5 to 10 should be selected", () => rowsRangeSelected(page, 5, 10));
      await session.step(197, "And the \"current row\" reading of grid should be 10", () => readingIs(page, "current row", el("grid"), 10));
      await session.step(198, "When user clicks on the \"row header 15\" area of grid holding Control", () => clickAreaHolding(page, "row header 15", el("grid"), "Control"));
      await session.step(199, "Then 7 rows should be selected", () => selectedRowCount(page, 7));
      await session.step(200, "When user clicks on the \"row header 8\" area of grid", () => clickArea(page, "row header 8", el("grid")));
      await session.step(201, "Then 7 rows should be selected", () => selectedRowCount(page, 7));
      await session.step(202, "And only rows where \"USUBJID\" is one of \"X0273T21000500006, X0273T21000500008, X0273T21000700006, X0273T21000800002, X0273T21000800004, X0273T21000800006, X0273T21001100003\" should be selected", () => onlyOfAnySelected(page, "USUBJID", "X0273T21000500006, X0273T21000500008, X0273T21000700006, X0273T21000800002, X0273T21000800004, X0273T21000800006, X0273T21001100003"));
      await session.step(203, "And the \"current row\" reading of grid should be 8", () => readingIs(page, "current row", el("grid"), 8));
      await session.step(204, "When user drags the \"row header 12\" area of grid to the \"row header 16\" area", () => dragAreaToArea(page, "row header 12", el("grid"), "row header 16"));
      await session.step(205, "Then rows 12 to 16 should be selected", () => rowsRangeSelected(page, 12, 16));
      await session.step(206, "And 5 rows should be selected", () => selectedRowCount(page, 5));
      await session.step(207, "When user clears the row selection", () => clearSelection(page));
      await session.step(208, "Then no rows should be selected", () => noneSelected(page));
      await session.step(209, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Shift+click adds headers to the selection and Control+click takes one back", async () => {
      await session.step(212, "When user clicks on the \"header AGE\" area of grid holding Shift", () => clickAreaHolding(page, "header AGE", el("grid"), "Shift"));
      await session.step(213, "And user clicks on the \"header HEIGHT\" area of grid holding Shift", () => clickAreaHolding(page, "header HEIGHT", el("grid"), "Shift"));
      await session.step(214, "Then columns \"AGE, HEIGHT\" should be selected", () => columnsSelected(page, "AGE, HEIGHT"));
      await session.step(215, "When user clicks on the \"header AGE\" area of grid holding Control", () => clickAreaHolding(page, "header AGE", el("grid"), "Control"));
      await session.step(216, "Then columns \"HEIGHT\" should be selected", () => columnsSelected(page, "HEIGHT"));
      await session.step(217, "When user presses Escape", () => pressKey(page, "Escape"));
      await session.step(218, "Then no columns should be selected", () => noColumnsSelected(page));
      await session.step(219, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Tab and Shift+Tab walk the cells and wrap at the row edges", async () => {
      await session.step(222, "When user clicks on the \"cell 3 of AGE\" area of grid", () => clickArea(page, "cell 3 of AGE", el("grid")));
      await session.step(223, "And user presses Tab", () => pressKey(page, "Tab"));
      await session.step(224, "Then the \"current column\" reading of grid should be \"SEX\"", () => readingReads(page, "current column", el("grid"), "SEX"));
      await session.step(225, "And the \"current row\" reading of grid should be 3", () => readingIs(page, "current row", el("grid"), 3));
      await session.step(226, "When user presses Shift+Tab", () => pressKey(page, "Shift+Tab"));
      await session.step(227, "Then the \"current column\" reading of grid should be \"AGE\"", () => readingReads(page, "current column", el("grid"), "AGE"));
      await session.step(228, "And the \"current row\" reading of grid should be 3", () => readingIs(page, "current row", el("grid"), 3));
      await session.step(229, "When user clicks on the \"cell 3 of SEVERITY\" area of grid", () => clickArea(page, "cell 3 of SEVERITY", el("grid")));
      await session.step(230, "And user presses Tab", () => pressKey(page, "Tab"));
      await session.step(231, "Then the \"current column\" reading of grid should be \"USUBJID\"", () => readingReads(page, "current column", el("grid"), "USUBJID"));
      await session.step(232, "And the \"current row\" reading of grid should be 4", () => readingIs(page, "current row", el("grid"), 4));
      await session.step(233, "When user presses Shift+Tab", () => pressKey(page, "Shift+Tab"));
      await session.step(234, "Then the \"current column\" reading of grid should be \"SEVERITY\"", () => readingReads(page, "current column", el("grid"), "SEVERITY"));
      await session.step(235, "And the \"current row\" reading of grid should be 3", () => readingIs(page, "current row", el("grid"), 3));
      await session.step(236, "When user clicks on the \"cell 3 of USUBJID\" area of grid", () => clickArea(page, "cell 3 of USUBJID", el("grid")));
      await session.step(237, "And user presses Shift+Tab", () => pressKey(page, "Shift+Tab"));
      await session.step(238, "Then the \"current column\" reading of grid should be \"SEVERITY\"", () => readingReads(page, "current column", el("grid"), "SEVERITY"));
      await session.step(239, "And the \"current row\" reading of grid should be 2", () => readingIs(page, "current row", el("grid"), 2));
      await session.step(240, "When user presses Tab", () => pressKey(page, "Tab"));
      await session.step(241, "Then the \"current column\" reading of grid should be \"USUBJID\"", () => readingReads(page, "current column", el("grid"), "USUBJID"));
      await session.step(242, "And the \"current row\" reading of grid should be 3", () => readingIs(page, "current row", el("grid"), 3));
      await session.step(243, "And no errors should have been logged", () => noErrors(page));
    });
    run.finish();
  });
});
