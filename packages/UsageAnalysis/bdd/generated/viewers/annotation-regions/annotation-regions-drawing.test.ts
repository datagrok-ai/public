/* eslint-disable max-len */
/* eslint-disable comma-spacing */
/* eslint-disable quotes */
/* ---
generated: features/viewers/annotation-regions/annotation-regions-drawing.feature
generator: @datagrok-libraries/bdd — do not edit; run `grok-bdd compile` to regenerate
sub_features_covered: [viewers.scatter-plot, viewers.density-plot, viewers.bar-chart, powerpack.dialogs.formula-lines]
--- */
import {test} from '@playwright/test';
import '../../../bindings/connections.js';
import '../../../bindings/grid.js';
import '../../../bindings/spaces.js';
import '../../../bindings/tile-viewer.js';
import '../../../bindings/trellis-plot.js';
import '@datagrok-libraries/bdd/bindings/common/kinds';
import '@datagrok-libraries/bdd/bindings/common/parameter-types';
import '@datagrok-libraries/bdd/bindings/platform/datasets';
import '@datagrok-libraries/bdd/bindings/platform/elements';
import {loggedIn} from '@datagrok-libraries/bdd/bindings/common/session';
import {clickOn, dragSliderTo, enterInto, shouldBe, shouldContainText, shouldHaveText, shouldHaveValue} from '@datagrok-libraries/bdd/bindings/common/steps';
import {setTableTag} from '@datagrok-libraries/bdd/bindings/platform/data';
import {dialogCloses, openDataset} from '@datagrok-libraries/bdd/bindings/platform/steps';
import {addViewerWith, areasSameSize, dragAcrossArea, dragAreaToArea, hasArea, hasNoArea, hoverArea, infoBalloonText, menuLists, noErrors, pickFromAreaContextMenu, pickFromContextMenu, pickFromOpenMenu, pointerAway, propertyShouldContain, readingIs, readingReads, repainted, resizeTo, rightClickArea, setProperties, setProperty, takeSnapshot} from '@datagrok-libraries/bdd/bindings/tiers/viewers/steps';
import {dragLasso} from '@datagrok-libraries/bdd/bindings/tiers/viewers/widgets';
import {ds, el, feature} from '@datagrok-libraries/bdd/runtime';

test.describe("Drawing and editing annotation regions", () => {
  const session = feature(test, "features/viewers/annotation-regions/annotation-regions-drawing.feature", import.meta.url);
  test("A rectangle drawn on the scatter plot becomes a titled area region, edited from its menu", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.density-plot", "@realizes:viewers.bar-chart", "@realizes:powerpack.dialogs.formula-lines"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(13, "Given user is logged in", () => loggedIn(page));
    await session.step(14, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(17, "Given user adds a scatter plot viewer with:", () => addViewerWith(page, "scatter plot", [["xColumnName","WEIGHT"],["yColumnName","HEIGHT"],["lassoTool","false"],["markerDefaultSize","2"]]), [["xColumnName","WEIGHT"],["yColumnName","HEIGHT"],["lassoTool","false"],["markerDefaultSize","2"]]);
    await session.step(22, "And user resizes scatter plot viewer to 800 by 500", () => resizeTo(page, el("scatter plot viewer"), 800, 500));
    await session.step(23, "Then the \"viewer regions\" reading of scatter plot viewer should be 0", () => readingIs(page, "viewer regions", el("scatter plot viewer"), 0));
    await session.step(24, "When user picks \"Tools > Draw Annotation Region\" from the context menu of scatter plot viewer", () => pickFromContextMenu(page, "Tools > Draw Annotation Region", el("scatter plot viewer")));
    await session.step(25, "Then an info balloon containing \"Click and drag to draw region\" should have been shown", () => infoBalloonText(page, "Click and drag to draw region"));
    await session.step(26, "And the \"region drawing mode\" reading of scatter plot viewer should be \"true\"", () => readingReads(page, "region drawing mode", el("scatter plot viewer"), "true"));
    await session.step(27, "When user drags the \"top edge of view\" area of scatter plot viewer to the \"left edge of view\" area", () => dragAreaToArea(page, "top edge of view", el("scatter plot viewer"), "left edge of view"));
    await session.step(28, "Then the \"region drawing mode\" reading of scatter plot viewer should be \"false\"", () => readingReads(page, "region drawing mode", el("scatter plot viewer"), "false"));
    await session.step(29, "And the \"viewer regions\" reading of scatter plot viewer should be 1", () => readingIs(page, "viewer regions", el("scatter plot viewer"), 1));
    await session.step(30, "And scatter plot viewer should have a \"region 1\" area", () => hasArea(page, el("scatter plot viewer"), "region 1"));
    await session.step(31, "And \"annotationRegions\" property of scatter plot viewer should contain \"\\\"type\\\":\\\"area\\\"\"", () => propertyShouldContain(page, "annotationRegions", el("scatter plot viewer"), "\"type\":\"area\""));
    await session.step(32, "And \"Formula Lines\" dialog should be visible", () => shouldBe(page, el("\"Formula Lines\" dialog"), "visible"));
    await session.step(33, "And editor of \"X column\" input in \"Formula Lines\" dialog should have text \"WEIGHT\"", () => shouldHaveText(page, el("editor of \"X column\" input in \"Formula Lines\" dialog"), "WEIGHT"));
    await session.step(34, "And editor of \"Y column\" input in \"Formula Lines\" dialog should have text \"HEIGHT\"", () => shouldHaveText(page, el("editor of \"Y column\" input in \"Formula Lines\" dialog"), "HEIGHT"));
    await session.step(35, "When user enters \"Band A\" into Title input in \"Formula Lines\" dialog", () => enterInto(page, "Band A", el("Title input in \"Formula Lines\" dialog")));
    await session.step(36, "And user clicks OK button in \"Formula Lines\" dialog", () => clickOn(page, el("OK button in \"Formula Lines\" dialog")));
    await session.step(37, "Then the \"Formula Lines\" dialog should close", () => dialogCloses(page, "Formula Lines"));
    await session.step(38, "And scatter plot viewer should have a \"region Band A\" area", () => hasArea(page, el("scatter plot viewer"), "region Band A"));
    await session.step(39, "And scatter plot viewer should have a \"region Band A title\" area", () => hasArea(page, el("scatter plot viewer"), "region Band A title"));
    await session.step(40, "When user hovers over the \"top edge of region Band A\" area of scatter plot viewer", () => hoverArea(page, "top edge of region Band A", el("scatter plot viewer")));
    await session.step(41, "Then the \"regions hovered\" reading of scatter plot viewer should be 1", () => readingIs(page, "regions hovered", el("scatter plot viewer"), 1));
    await session.step(42, "And tooltip should contain text \"Band A\"", () => shouldContainText(page, el("tooltip"), "Band A"));
    await session.step(43, "When user right-clicks on the \"top edge of region Band A\" area of scatter plot viewer", () => rightClickArea(page, "top edge of region Band A", el("scatter plot viewer")));
    await session.step(44, "Then the open menu should list \"Edit...\"", () => menuLists(page, "Edit..."));
    await session.step(45, "And the open menu should list \"Show Annotation Regions\"", () => menuLists(page, "Show Annotation Regions"));
    await session.step(46, "When user picks \"Edit...\" from the open menu", () => pickFromOpenMenu(page, "Edit..."));
    await session.step(47, "Then \"Formula Lines\" dialog should be visible", () => shouldBe(page, el("\"Formula Lines\" dialog"), "visible"));
    await session.step(48, "And Title input in \"Formula Lines\" dialog should have value \"Band A\"", () => shouldHaveValue(page, el("Title input in \"Formula Lines\" dialog"), "Band A"));
    await session.step(49, "When user enters \"Band B\" into Title input in \"Formula Lines\" dialog", () => enterInto(page, "Band B", el("Title input in \"Formula Lines\" dialog")));
    await session.step(50, "And user enters \"Edited band\" into Description input in \"Formula Lines\" dialog", () => enterInto(page, "Edited band", el("Description input in \"Formula Lines\" dialog")));
    await session.step(51, "And user enters \"3\" into Width input in \"Formula Lines\" dialog", () => enterInto(page, "3", el("Width input in \"Formula Lines\" dialog")));
    await session.step(52, "And user clicks OK button in \"Formula Lines\" dialog", () => clickOn(page, el("OK button in \"Formula Lines\" dialog")));
    await session.step(53, "Then scatter plot viewer should have a \"region Band B\" area", () => hasArea(page, el("scatter plot viewer"), "region Band B"));
    await session.step(54, "And scatter plot viewer should not have a \"region Band A\" area", () => hasNoArea(page, el("scatter plot viewer"), "region Band A"));
    await session.step(55, "And \"annotationRegions\" property of scatter plot viewer should contain \"\\\"description\\\":\\\"Edited band\\\"\"", () => propertyShouldContain(page, "annotationRegions", el("scatter plot viewer"), "\"description\":\"Edited band\""));
    await session.step(56, "And \"annotationRegions\" property of scatter plot viewer should contain \"\\\"outlineWidth\\\":3\"", () => propertyShouldContain(page, "annotationRegions", el("scatter plot viewer"), "\"outlineWidth\":3"));
    await session.step(57, "When user hovers over the \"top edge of region Band B\" area of scatter plot viewer", () => hoverArea(page, "top edge of region Band B", el("scatter plot viewer")));
    await session.step(58, "Then tooltip should contain text \"Band B\"", () => shouldContainText(page, el("tooltip"), "Band B"));
    await session.step(59, "And tooltip should contain text \"Edited band\"", () => shouldContainText(page, el("tooltip"), "Edited band"));
    await session.step(60, "When user moves the pointer away from scatter plot viewer", () => pointerAway(page, el("scatter plot viewer")));
    await session.step(61, "And user picks \"Tools > Formula Lines...\" from the context menu of the \"bottom edge of view\" area of scatter plot viewer", () => pickFromAreaContextMenu(page, "Tools > Formula Lines...", "bottom edge of view", el("scatter plot viewer")));
    await session.step(62, "Then \"Formula Lines\" dialog should be visible", () => shouldBe(page, el("\"Formula Lines\" dialog"), "visible"));
    await session.step(63, "When user clicks on Delete button in \"Formula Lines\" dialog", () => clickOn(page, el("Delete button in \"Formula Lines\" dialog")));
    await session.step(64, "And user clicks OK button in \"Formula Lines\" dialog", () => clickOn(page, el("OK button in \"Formula Lines\" dialog")));
    await session.step(65, "Then the \"viewer regions\" reading of scatter plot viewer should be 0", () => readingIs(page, "viewer regions", el("scatter plot viewer"), 0));
    await session.step(66, "And scatter plot viewer should not have a \"region Band B\" area", () => hasNoArea(page, el("scatter plot viewer"), "region Band B"));
    await session.step(67, "And no errors should have been logged", () => noErrors(page));
  });
  test("A lasso drawn on the density plot becomes a polygon region, and the dialog adds a formula region", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.density-plot", "@realizes:viewers.bar-chart", "@realizes:powerpack.dialogs.formula-lines"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(13, "Given user is logged in", () => loggedIn(page));
    await session.step(14, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(70, "Given user adds a density plot viewer with:", () => addViewerWith(page, "density plot", [["xColumnName","WEIGHT"],["yColumnName","HEIGHT"],["lassoTool","true"]]), [["xColumnName","WEIGHT"],["yColumnName","HEIGHT"],["lassoTool","true"]]);
    await session.step(74, "And user resizes density plot viewer to 800 by 500", () => resizeTo(page, el("density plot viewer"), 800, 500));
    await session.step(75, "When user picks \"Tools > Draw Annotation Region\" from the context menu of density plot viewer", () => pickFromContextMenu(page, "Tools > Draw Annotation Region", el("density plot viewer")));
    await session.step(76, "Then the \"region drawing mode\" reading of density plot viewer should be \"true\"", () => readingReads(page, "region drawing mode", el("density plot viewer"), "true"));
    await session.step(77, "When user drags a lasso over the \"view\" area of density plot viewer", () => dragLasso(page, "view", el("density plot viewer")));
    await session.step(78, "Then the \"region drawing mode\" reading of density plot viewer should be \"false\"", () => readingReads(page, "region drawing mode", el("density plot viewer"), "false"));
    await session.step(79, "And the \"viewer regions\" reading of density plot viewer should be 1", () => readingIs(page, "viewer regions", el("density plot viewer"), 1));
    await session.step(80, "And density plot viewer should have a \"region 1\" area", () => hasArea(page, el("density plot viewer"), "region 1"));
    await session.step(81, "And \"annotationRegions\" property of density plot viewer should contain \"\\\"type\\\":\\\"area\\\"\"", () => propertyShouldContain(page, "annotationRegions", el("density plot viewer"), "\"type\":\"area\""));
    await session.step(82, "When user clicks OK button in \"Formula Lines\" dialog", () => clickOn(page, el("OK button in \"Formula Lines\" dialog")));
    await session.step(83, "And user sets properties of density plot viewer:", () => setProperties(page, el("density plot viewer"), [["lassoTool","false"],["showViewerAnnotationRegions","false"]]), [["lassoTool","false"],["showViewerAnnotationRegions","false"]]);
    await session.step(86, "And user picks \"Tools > Formula Lines...\" from the context menu of density plot viewer", () => pickFromContextMenu(page, "Tools > Formula Lines...", el("density plot viewer")));
    await session.step(87, "Then \"Formula Lines\" dialog should be visible", () => shouldBe(page, el("\"Formula Lines\" dialog"), "visible"));
    await session.step(88, "When user clicks on \"ADD NEW\" button in \"Formula Lines\" dialog", () => clickOn(page, el("\"ADD NEW\" button in \"Formula Lines\" dialog")));
    await session.step(89, "And user picks \"Region - Formula Lines\" from the open menu", () => pickFromOpenMenu(page, "Region - Formula Lines"));
    await session.step(90, "And user clicks OK button in \"Formula Lines\" dialog", () => clickOn(page, el("OK button in \"Formula Lines\" dialog")));
    await session.step(91, "Then the \"viewer regions\" reading of density plot viewer should be 2", () => readingIs(page, "viewer regions", el("density plot viewer"), 2));
    await session.step(92, "And \"annotationRegions\" property of density plot viewer should contain \"\\\"type\\\":\\\"formula\\\"\"", () => propertyShouldContain(page, "annotationRegions", el("density plot viewer"), "\"type\":\"formula\""));
    await session.step(93, "And \"annotationRegions\" property of density plot viewer should contain \"\\\"formula1\\\":\\\"${HEIGHT} = ${WEIGHT}\"", () => propertyShouldContain(page, "annotationRegions", el("density plot viewer"), "\"formula1\":\"${HEIGHT} = ${WEIGHT}"));
    await session.step(94, "And \"annotationRegions\" property of density plot viewer should contain \"\\\"formula2\\\":\\\"${HEIGHT} = ${WEIGHT}\"", () => propertyShouldContain(page, "annotationRegions", el("density plot viewer"), "\"formula2\":\"${HEIGHT} = ${WEIGHT}"));
    await session.step(95, "When user sets \"showViewerAnnotationRegions\" property of density plot viewer to \"true\"", () => setProperty(page, "showViewerAnnotationRegions", el("density plot viewer"), "true"));
    await session.step(96, "Then the \"regions shown\" reading of density plot viewer should be 2", () => readingIs(page, "regions shown", el("density plot viewer"), 2));
    await session.step(97, "When user sets \"annotationRegions\" property of density plot viewer to \"[]\"", () => setProperty(page, "annotationRegions", el("density plot viewer"), "[]"));
    await session.step(98, "Then the \"viewer regions\" reading of density plot viewer should be 0", () => readingIs(page, "viewer regions", el("density plot viewer"), 0));
    await session.step(99, "And no errors should have been logged", () => noErrors(page));
  });
  test("A vertical bar chart locks the drawn region to its full width", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.density-plot", "@realizes:viewers.bar-chart", "@realizes:powerpack.dialogs.formula-lines"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(13, "Given user is logged in", () => loggedIn(page));
    await session.step(14, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(102, "Given user adds a bar chart viewer with:", () => addViewerWith(page, "bar chart", [["splitColumnName","RACE"],["valueColumnName","AGE"],["valueAggrType","avg"],["orientation","vertical"]]), [["splitColumnName","RACE"],["valueColumnName","AGE"],["valueAggrType","avg"],["orientation","vertical"]]);
    await session.step(107, "And user resizes bar chart viewer to 800 by 500", () => resizeTo(page, el("bar chart viewer"), 800, 500));
    await session.step(108, "When user picks \"Tools > Draw Annotation Region\" from the context menu of the \"top edge of view\" area of bar chart viewer", () => pickFromAreaContextMenu(page, "Tools > Draw Annotation Region", "top edge of view", el("bar chart viewer")));
    await session.step(109, "Then the \"region drawing mode\" reading of bar chart viewer should be \"true\"", () => readingReads(page, "region drawing mode", el("bar chart viewer"), "true"));
    await session.step(110, "When user drags across the \"top edge of view\" area of bar chart viewer", () => dragAcrossArea(page, "top edge of view", el("bar chart viewer")));
    await session.step(111, "Then the \"region drawing mode\" reading of bar chart viewer should be \"false\"", () => readingReads(page, "region drawing mode", el("bar chart viewer"), "false"));
    await session.step(112, "And the \"viewer regions\" reading of bar chart viewer should be 1", () => readingIs(page, "viewer regions", el("bar chart viewer"), 1));
    await session.step(113, "And \"annotationRegions\" property of bar chart viewer should contain \"\\\"type\\\":\\\"formula\\\"\"", () => propertyShouldContain(page, "annotationRegions", el("bar chart viewer"), "\"type\":\"formula\""));
    await session.step(114, "And \"annotationRegions\" property of bar chart viewer should contain \"\\\"formula1\\\":\\\"${AGE} = \"", () => propertyShouldContain(page, "annotationRegions", el("bar chart viewer"), "\"formula1\":\"${AGE} = "));
    await session.step(115, "And \"annotationRegions\" property of bar chart viewer should contain \"\\\"formula2\\\":\\\"${AGE} = \"", () => propertyShouldContain(page, "annotationRegions", el("bar chart viewer"), "\"formula2\":\"${AGE} = "));
    await session.step(116, "And bar chart viewer should have a \"region 1\" area", () => hasArea(page, el("bar chart viewer"), "region 1"));
    await session.step(117, "And the \"region 1\" and \"view\" areas of bar chart viewer should be the same width", () => areasSameSize(page, "region 1", "view", el("bar chart viewer"), "width"));
    await session.step(118, "And Title input in \"Formula Lines\" dialog should have value \"\"", () => shouldHaveValue(page, el("Title input in \"Formula Lines\" dialog"), ""));
    await session.step(119, "When user clicks OK button in \"Formula Lines\" dialog", () => clickOn(page, el("OK button in \"Formula Lines\" dialog")));
    await session.step(120, "Then no errors should have been logged", () => noErrors(page));
  });
  test("A horizontal bar chart locks the drawn region to its full height", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.density-plot", "@realizes:viewers.bar-chart", "@realizes:powerpack.dialogs.formula-lines"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(13, "Given user is logged in", () => loggedIn(page));
    await session.step(14, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(123, "Given user adds a bar chart viewer with:", () => addViewerWith(page, "bar chart", [["splitColumnName","RACE"],["valueColumnName","AGE"],["valueAggrType","avg"],["orientation","horizontal"]]), [["splitColumnName","RACE"],["valueColumnName","AGE"],["valueAggrType","avg"],["orientation","horizontal"]]);
    await session.step(128, "And user resizes bar chart viewer to 800 by 500", () => resizeTo(page, el("bar chart viewer"), 800, 500));
    await session.step(129, "When user picks \"Tools > Draw Annotation Region\" from the context menu of the \"top edge of view\" area of bar chart viewer", () => pickFromAreaContextMenu(page, "Tools > Draw Annotation Region", "top edge of view", el("bar chart viewer")));
    await session.step(130, "Then the \"region drawing mode\" reading of bar chart viewer should be \"true\"", () => readingReads(page, "region drawing mode", el("bar chart viewer"), "true"));
    await session.step(131, "When user drags across the \"top edge of view\" area of bar chart viewer", () => dragAcrossArea(page, "top edge of view", el("bar chart viewer")));
    await session.step(132, "Then the \"region drawing mode\" reading of bar chart viewer should be \"false\"", () => readingReads(page, "region drawing mode", el("bar chart viewer"), "false"));
    await session.step(133, "And the \"viewer regions\" reading of bar chart viewer should be 1", () => readingIs(page, "viewer regions", el("bar chart viewer"), 1));
    await session.step(134, "And \"annotationRegions\" property of bar chart viewer should contain \"\\\"formula1\\\":\\\"${AGE} = \"", () => propertyShouldContain(page, "annotationRegions", el("bar chart viewer"), "\"formula1\":\"${AGE} = "));
    await session.step(135, "And bar chart viewer should have a \"region 1\" area", () => hasArea(page, el("bar chart viewer"), "region 1"));
    await session.step(136, "And the \"region 1\" and \"view\" areas of bar chart viewer should be the same height", () => areasSameSize(page, "region 1", "view", el("bar chart viewer"), "height"));
    await session.step(137, "When user clicks OK button in \"Formula Lines\" dialog", () => clickOn(page, el("OK button in \"Formula Lines\" dialog")));
    await session.step(138, "Then no errors should have been logged", () => noErrors(page));
  });
  test("A box plot locks the drawn region to its full width", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.density-plot", "@realizes:viewers.bar-chart", "@realizes:powerpack.dialogs.formula-lines"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(13, "Given user is logged in", () => loggedIn(page));
    await session.step(14, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(141, "Given user adds a box plot viewer with:", () => addViewerWith(page, "box plot", [["category1ColumnName","RACE"],["valueColumnName","AGE"]]), [["category1ColumnName","RACE"],["valueColumnName","AGE"]]);
    await session.step(144, "And user resizes box plot viewer to 800 by 500", () => resizeTo(page, el("box plot viewer"), 800, 500));
    await session.step(145, "Then the \"viewer regions\" reading of box plot viewer should be 0", () => readingIs(page, "viewer regions", el("box plot viewer"), 0));
    await session.step(146, "When user picks \"Tools > Draw Annotation Region\" from the context menu of the \"top edge of view\" area of box plot viewer", () => pickFromAreaContextMenu(page, "Tools > Draw Annotation Region", "top edge of view", el("box plot viewer")));
    await session.step(147, "Then the \"region drawing mode\" reading of box plot viewer should be \"true\"", () => readingReads(page, "region drawing mode", el("box plot viewer"), "true"));
    await session.step(148, "When user drags across the \"top edge of view\" area of box plot viewer", () => dragAcrossArea(page, "top edge of view", el("box plot viewer")));
    await session.step(149, "Then the \"region drawing mode\" reading of box plot viewer should be \"false\"", () => readingReads(page, "region drawing mode", el("box plot viewer"), "false"));
    await session.step(150, "And the \"viewer regions\" reading of box plot viewer should be 1", () => readingIs(page, "viewer regions", el("box plot viewer"), 1));
    await session.step(151, "And \"annotationRegions\" property of box plot viewer should contain \"\\\"type\\\":\\\"formula\\\"\"", () => propertyShouldContain(page, "annotationRegions", el("box plot viewer"), "\"type\":\"formula\""));
    await session.step(152, "And \"annotationRegions\" property of box plot viewer should contain \"\\\"formula1\\\":\\\"${AGE} = \"", () => propertyShouldContain(page, "annotationRegions", el("box plot viewer"), "\"formula1\":\"${AGE} = "));
    await session.step(153, "And \"annotationRegions\" property of box plot viewer should contain \"\\\"formula2\\\":\\\"${AGE} = \"", () => propertyShouldContain(page, "annotationRegions", el("box plot viewer"), "\"formula2\":\"${AGE} = "));
    await session.step(154, "And box plot viewer should have a \"region 1\" area", () => hasArea(page, el("box plot viewer"), "region 1"));
    await session.step(155, "And the \"region 1\" and \"view\" areas of box plot viewer should be the same width", () => areasSameSize(page, "region 1", "view", el("box plot viewer"), "width"));
    await session.step(156, "And Title input in \"Formula Lines\" dialog should have value \"\"", () => shouldHaveValue(page, el("Title input in \"Formula Lines\" dialog"), ""));
    await session.step(157, "When user clicks OK button in \"Formula Lines\" dialog", () => clickOn(page, el("OK button in \"Formula Lines\" dialog")));
    await session.step(158, "Then no errors should have been logged", () => noErrors(page));
  });
  test("The dialog's colors and opacity reach the region", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.density-plot", "@realizes:viewers.bar-chart", "@realizes:powerpack.dialogs.formula-lines"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(13, "Given user is logged in", () => loggedIn(page));
    await session.step(14, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(161, "Given user adds a scatter plot viewer with:", () => addViewerWith(page, "scatter plot", [["xColumnName","AGE"],["yColumnName","WEIGHT"],["markerDefaultSize","2"],["annotationRegions","[{\"type\":\"area\",\"x\":\"AGE\",\"y\":\"WEIGHT\",\"header\":\"Adults\",\"area\":[[30.5,30],[60.5,30],[60.5,180],[30.5,180]]}]"]]), [["xColumnName","AGE"],["yColumnName","WEIGHT"],["markerDefaultSize","2"],["annotationRegions","[{\"type\":\"area\",\"x\":\"AGE\",\"y\":\"WEIGHT\",\"header\":\"Adults\",\"area\":[[30.5,30],[60.5,30],[60.5,180],[30.5,180]]}]"]]);
    await session.step(166, "And user resizes scatter plot viewer to 800 by 500", () => resizeTo(page, el("scatter plot viewer"), 800, 500));
    await session.step(167, "Then scatter plot viewer should have a \"region Adults\" area", () => hasArea(page, el("scatter plot viewer"), "region Adults"));
    await session.step(168, "When user takes a snapshot of scatter plot viewer", () => takeSnapshot(page, el("scatter plot viewer")));
    await session.step(169, "And user picks \"Edit...\" from the context menu of the \"left edge of region Adults\" area of scatter plot viewer", () => pickFromAreaContextMenu(page, "Edit...", "left edge of region Adults", el("scatter plot viewer")));
    await session.step(170, "Then \"Formula Lines\" dialog should be visible", () => shouldBe(page, el("\"Formula Lines\" dialog"), "visible"));
    await session.step(171, "When user enters \"#ff8800\" into Region Color input in \"Formula Lines\" dialog", () => enterInto(page, "#ff8800", el("Region Color input in \"Formula Lines\" dialog")));
    await session.step(172, "And user enters \"#003366\" into Outline Color input in \"Formula Lines\" dialog", () => enterInto(page, "#003366", el("Outline Color input in \"Formula Lines\" dialog")));
    await session.step(173, "And user enters \"3\" into Width input in \"Formula Lines\" dialog", () => enterInto(page, "3", el("Width input in \"Formula Lines\" dialog")));
    await session.step(174, "And user drags the slider of Opacity input in \"Formula Lines\" dialog to 60", () => dragSliderTo(page, el("Opacity input in \"Formula Lines\" dialog"), 60));
    await session.step(175, "And user enters \"#ff0000\" into Color input in \"Formula Lines\" dialog", () => enterInto(page, "#ff0000", el("Color input in \"Formula Lines\" dialog")));
    await session.step(176, "And user clicks OK button in \"Formula Lines\" dialog", () => clickOn(page, el("OK button in \"Formula Lines\" dialog")));
    await session.step(177, "Then the \"Formula Lines\" dialog should close", () => dialogCloses(page, "Formula Lines"));
    await session.step(178, "And \"annotationRegions\" property of scatter plot viewer should contain \"\\\"fillColor\\\"\"", () => propertyShouldContain(page, "annotationRegions", el("scatter plot viewer"), "\"fillColor\""));
    await session.step(179, "And \"annotationRegions\" property of scatter plot viewer should contain \"\\\"outlineColor\\\"\"", () => propertyShouldContain(page, "annotationRegions", el("scatter plot viewer"), "\"outlineColor\""));
    await session.step(180, "And \"annotationRegions\" property of scatter plot viewer should contain \"\\\"outlineWidth\\\":3\"", () => propertyShouldContain(page, "annotationRegions", el("scatter plot viewer"), "\"outlineWidth\":3"));
    await session.step(181, "And \"annotationRegions\" property of scatter plot viewer should contain \"\\\"headerColor\\\"\"", () => propertyShouldContain(page, "annotationRegions", el("scatter plot viewer"), "\"headerColor\""));
    await session.step(182, "And \"annotationRegions\" property of scatter plot viewer should contain \"\\\"opacity\\\"\"", () => propertyShouldContain(page, "annotationRegions", el("scatter plot viewer"), "\"opacity\""));
    await session.step(183, "And scatter plot viewer should have repainted", () => repainted(page, el("scatter plot viewer")));
    await session.step(184, "And scatter plot viewer should have a \"region Adults\" area", () => hasArea(page, el("scatter plot viewer"), "region Adults"));
    await session.step(185, "And no errors should have been logged", () => noErrors(page));
  });
  test("A dataframe region added from the DataFrame tab is counted on its own", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.density-plot", "@realizes:viewers.bar-chart", "@realizes:powerpack.dialogs.formula-lines"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(13, "Given user is logged in", () => loggedIn(page));
    await session.step(14, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(188, "Given user adds a scatter plot viewer with:", () => addViewerWith(page, "scatter plot", [["xColumnName","AGE"],["yColumnName","WEIGHT"]]), [["xColumnName","AGE"],["yColumnName","WEIGHT"]]);
    await session.step(191, "And user resizes scatter plot viewer to 800 by 500", () => resizeTo(page, el("scatter plot viewer"), 800, 500));
    await session.step(192, "Then the \"viewer regions\" reading of scatter plot viewer should be 0", () => readingIs(page, "viewer regions", el("scatter plot viewer"), 0));
    await session.step(193, "And the \"dataframe regions\" reading of scatter plot viewer should be 0", () => readingIs(page, "dataframe regions", el("scatter plot viewer"), 0));
    await session.step(194, "When user picks \"Tools > Formula Lines...\" from the context menu of scatter plot viewer", () => pickFromContextMenu(page, "Tools > Formula Lines...", el("scatter plot viewer")));
    await session.step(195, "Then \"Formula Lines\" dialog should be visible", () => shouldBe(page, el("\"Formula Lines\" dialog"), "visible"));
    await session.step(196, "When user clicks on DataFrame tab in \"Formula Lines\" dialog", () => clickOn(page, el("DataFrame tab in \"Formula Lines\" dialog")));
    await session.step(197, "And user clicks on \"ADD NEW\" button in \"Formula Lines\" dialog", () => clickOn(page, el("\"ADD NEW\" button in \"Formula Lines\" dialog")));
    await session.step(198, "And user picks \"Region - Formula Lines\" from the open menu", () => pickFromOpenMenu(page, "Region - Formula Lines"));
    await session.step(199, "And user enters \"Medium weight\" into Title input in \"Formula Lines\" dialog", () => enterInto(page, "Medium weight", el("Title input in \"Formula Lines\" dialog")));
    await session.step(200, "And user clicks OK button in \"Formula Lines\" dialog", () => clickOn(page, el("OK button in \"Formula Lines\" dialog")));
    await session.step(201, "Then the \"dataframe regions\" reading of scatter plot viewer should be 1", () => readingIs(page, "dataframe regions", el("scatter plot viewer"), 1));
    await session.step(202, "And the \"viewer regions\" reading of scatter plot viewer should be 0", () => readingIs(page, "viewer regions", el("scatter plot viewer"), 0));
    await session.step(203, "And scatter plot viewer should have a \"region Medium weight\" area", () => hasArea(page, el("scatter plot viewer"), "region Medium weight"));
    await session.step(204, "When user sets \"showDataframeAnnotationRegions\" property of scatter plot viewer to \"false\"", () => setProperty(page, "showDataframeAnnotationRegions", el("scatter plot viewer"), "false"));
    await session.step(205, "Then the \"regions shown\" reading of scatter plot viewer should be 0", () => readingIs(page, "regions shown", el("scatter plot viewer"), 0));
    await session.step(206, "And the \"dataframe regions\" reading of scatter plot viewer should be 1", () => readingIs(page, "dataframe regions", el("scatter plot viewer"), 1));
    await session.step(207, "When user sets the \".annotation-regions\" tag of the table to \"\"", () => setTableTag(page, ".annotation-regions", ""));
    await session.step(208, "Then the \"dataframe regions\" reading of scatter plot viewer should be 0", () => readingIs(page, "dataframe regions", el("scatter plot viewer"), 0));
    await session.step(209, "And no errors should have been logged", () => noErrors(page));
  });
});
