/* eslint-disable max-len */
/* eslint-disable comma-spacing */
/* eslint-disable quotes */
/* ---
generated: features/viewers/row-source/row-source-rebound.feature
generator: @datagrok-libraries/bdd — do not edit; run `grok-bdd compile` to regenerate
sub_features_covered: [viewers.scatter-plot, viewers.line-chart, viewers.histogram, viewers.bar-chart, viewers.pie-chart, viewers.box-plot, viewers.pc-plot]
--- */
import {test} from '@playwright/test';
import '../../../bindings/spaces.js';
import '../../../bindings/tile-viewer.js';
import '../../../bindings/trellis-plot.js';
import '@datagrok-libraries/bdd/bindings/common/kinds';
import '@datagrok-libraries/bdd/bindings/common/parameter-types';
import '@datagrok-libraries/bdd/bindings/platform/datasets';
import '@datagrok-libraries/bdd/bindings/platform/elements';
import {loggedIn} from '@datagrok-libraries/bdd/bindings/common/session';
import {addRangeFilterInTable, clearSelectionInTable, currentRowOfTableValue, makeRowCurrentInTable, selectInTableOneOf, selectedInTable, tableFilterCount} from '@datagrok-libraries/bdd/bindings/platform/data';
import {openDataset, switchTableView} from '@datagrok-libraries/bdd/bindings/platform/steps';
import {addViewer, addViewerWith, boundTable, hoverArea, noErrors, pointerAway, setProperties, setProperty, showsRows} from '@datagrok-libraries/bdd/bindings/tiers/viewers/steps';
import {ds, el, feature} from '@datagrok-libraries/bdd/runtime';

test.describe("Row Source on a viewer rebound to another table", () => {
  const session = feature(test, "features/viewers/row-source/row-source-rebound.feature", import.meta.url);
  test("scatter plot rebound to spgi-100 answers that table's rows under every row source [viewer=scatter plot, property=xColumnName, column=Chemical Space X, property 2=yColumnName, column 2=Chemical Space Y, source=pie chart, source property=categoryColumnName, source area=slice]", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.line-chart", "@realizes:viewers.histogram", "@realizes:viewers.bar-chart", "@realizes:viewers.pie-chart", "@realizes:viewers.box-plot", "@realizes:viewers.pc-plot"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(24, "Given user is logged in", () => loggedIn(page));
    await session.step(25, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(26, "And user opens spgi dataset", () => openDataset(page, ds("spgi")));
    await session.step(27, "And user switches to the \"demog-1000\" table view", () => switchTableView(page, "demog-1000"));
    await session.step(30, "Given user adds a scatter plot viewer", () => addViewer(page, "scatter plot"));
    await session.step(31, "When user sets properties of scatter plot viewer:", () => setProperties(page, el("scatter plot viewer"), [["table","spgi-100"],["xColumnName","Chemical Space X"],["yColumnName","Chemical Space Y"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]), [["table","spgi-100"],["xColumnName","Chemical Space X"],["yColumnName","Chemical Space Y"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]);
    await session.step(37, "Then scatter plot viewer should be bound to table \"spgi-100\"", () => boundTable(page, el("scatter plot viewer"), "spgi-100"));
    await session.step(38, "And scatter plot viewer should show 54 rows", () => showsRows(page, el("scatter plot viewer"), 54));
    await session.step(39, "When user adds a range filter on \"TPSA\" of table \"spgi-100\" from 30 to 80", () => addRangeFilterInTable(page, "TPSA", "spgi-100", 30, 80));
    await session.step(40, "Then 59 rows of table \"spgi-100\" should pass the filter", () => tableFilterCount(page, 59, "spgi-100"));
    await session.step(41, "And scatter plot viewer should show 32 rows", () => showsRows(page, el("scatter plot viewer"), 32));
    await session.step(42, "When user sets \"rowSource\" property of scatter plot viewer to \"All\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "All"));
    await session.step(43, "Then scatter plot viewer should show 54 rows", () => showsRows(page, el("scatter plot viewer"), 54));
    await session.step(44, "When user sets \"rowSource\" property of scatter plot viewer to \"Selected\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "Selected"));
    await session.step(45, "Then scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
    await session.step(46, "When user sets \"rowSource\" property of scatter plot viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "FilteredSelected"));
    await session.step(47, "Then scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
    await session.step(48, "When user sets \"rowSource\" property of scatter plot viewer to \"Selected\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "Selected"));
    await session.step(49, "And user selects rows of table \"spgi-100\" where \"Stereo Category\" is one of \"R_ONE, S_ACHIR\"", () => selectInTableOneOf(page, "spgi-100", "Stereo Category", "R_ONE, S_ACHIR"));
    await session.step(50, "Then 70 rows of table \"spgi-100\" should be selected", () => selectedInTable(page, 70, "spgi-100"));
    await session.step(51, "And scatter plot viewer should show 36 rows", () => showsRows(page, el("scatter plot viewer"), 36));
    await session.step(52, "When user sets \"rowSource\" property of scatter plot viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "FilteredSelected"));
    await session.step(53, "Then scatter plot viewer should show 22 rows", () => showsRows(page, el("scatter plot viewer"), 22));
    await session.step(54, "When user sets \"rowSource\" property of scatter plot viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "SelectedOrCurrent"));
    await session.step(55, "Then scatter plot viewer should show 36 rows", () => showsRows(page, el("scatter plot viewer"), 36));
    await session.step(56, "When user clears the row selection of table \"spgi-100\"", () => clearSelectionInTable(page, "spgi-100"));
    await session.step(57, "Then scatter plot viewer should show 1 rows", () => showsRows(page, el("scatter plot viewer"), 1));
    await session.step(58, "When user makes row 10 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 10, "spgi-100"));
    await session.step(59, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"S_ACHIR\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "S_ACHIR"));
    await session.step(60, "And scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
    await session.step(61, "When user sets \"rowSource\" property of scatter plot viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "CurrentRow"));
    await session.step(62, "Then scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
    await session.step(63, "When user makes row 1 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 1, "spgi-100"));
    await session.step(64, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"R_ONE\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "R_ONE"));
    await session.step(65, "And scatter plot viewer should show 1 rows", () => showsRows(page, el("scatter plot viewer"), 1));
    await session.step(66, "Given user adds a pie chart viewer with:", () => addViewerWith(page, "pie chart", [["table","spgi-100"],["categoryColumnName","Stereo Category"]]), [["table","spgi-100"],["categoryColumnName","Stereo Category"]]);
    await session.step(69, "When user sets \"rowSource\" property of scatter plot viewer to \"MouseOverGroup\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "MouseOverGroup"));
    await session.step(70, "And user moves the pointer away from pie chart viewer", () => pointerAway(page, el("pie chart viewer")));
    await session.step(71, "Then scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
    await session.step(72, "When user hovers over the \"slice R_ONE\" area of pie chart viewer", () => hoverArea(page, "slice R_ONE", el("pie chart viewer")));
    await session.step(73, "Then scatter plot viewer should show 22 rows", () => showsRows(page, el("scatter plot viewer"), 22));
    await session.step(74, "When user hovers over the \"slice S_ACHIR\" area of pie chart viewer", () => hoverArea(page, "slice S_ACHIR", el("pie chart viewer")));
    await session.step(75, "Then scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
    await session.step(76, "And no errors should have been logged", () => noErrors(page));
  });
  test("line chart rebound to spgi-100 answers that table's rows under every row source [viewer=line chart, property=xColumnName, column=Chemical Space X, property 2=yColumnNames, column 2=TPSA, source=pie chart, source property=categoryColumnName, source area=slice]", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.line-chart", "@realizes:viewers.histogram", "@realizes:viewers.bar-chart", "@realizes:viewers.pie-chart", "@realizes:viewers.box-plot", "@realizes:viewers.pc-plot"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(24, "Given user is logged in", () => loggedIn(page));
    await session.step(25, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(26, "And user opens spgi dataset", () => openDataset(page, ds("spgi")));
    await session.step(27, "And user switches to the \"demog-1000\" table view", () => switchTableView(page, "demog-1000"));
    await session.step(30, "Given user adds a line chart viewer", () => addViewer(page, "line chart"));
    await session.step(31, "When user sets properties of line chart viewer:", () => setProperties(page, el("line chart viewer"), [["table","spgi-100"],["xColumnName","Chemical Space X"],["yColumnNames","TPSA"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]), [["table","spgi-100"],["xColumnName","Chemical Space X"],["yColumnNames","TPSA"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]);
    await session.step(37, "Then line chart viewer should be bound to table \"spgi-100\"", () => boundTable(page, el("line chart viewer"), "spgi-100"));
    await session.step(38, "And line chart viewer should show 54 rows", () => showsRows(page, el("line chart viewer"), 54));
    await session.step(39, "When user adds a range filter on \"TPSA\" of table \"spgi-100\" from 30 to 80", () => addRangeFilterInTable(page, "TPSA", "spgi-100", 30, 80));
    await session.step(40, "Then 59 rows of table \"spgi-100\" should pass the filter", () => tableFilterCount(page, 59, "spgi-100"));
    await session.step(41, "And line chart viewer should show 32 rows", () => showsRows(page, el("line chart viewer"), 32));
    await session.step(42, "When user sets \"rowSource\" property of line chart viewer to \"All\"", () => setProperty(page, "rowSource", el("line chart viewer"), "All"));
    await session.step(43, "Then line chart viewer should show 54 rows", () => showsRows(page, el("line chart viewer"), 54));
    await session.step(44, "When user sets \"rowSource\" property of line chart viewer to \"Selected\"", () => setProperty(page, "rowSource", el("line chart viewer"), "Selected"));
    await session.step(45, "Then line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
    await session.step(46, "When user sets \"rowSource\" property of line chart viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("line chart viewer"), "FilteredSelected"));
    await session.step(47, "Then line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
    await session.step(48, "When user sets \"rowSource\" property of line chart viewer to \"Selected\"", () => setProperty(page, "rowSource", el("line chart viewer"), "Selected"));
    await session.step(49, "And user selects rows of table \"spgi-100\" where \"Stereo Category\" is one of \"R_ONE, S_ACHIR\"", () => selectInTableOneOf(page, "spgi-100", "Stereo Category", "R_ONE, S_ACHIR"));
    await session.step(50, "Then 70 rows of table \"spgi-100\" should be selected", () => selectedInTable(page, 70, "spgi-100"));
    await session.step(51, "And line chart viewer should show 36 rows", () => showsRows(page, el("line chart viewer"), 36));
    await session.step(52, "When user sets \"rowSource\" property of line chart viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("line chart viewer"), "FilteredSelected"));
    await session.step(53, "Then line chart viewer should show 22 rows", () => showsRows(page, el("line chart viewer"), 22));
    await session.step(54, "When user sets \"rowSource\" property of line chart viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("line chart viewer"), "SelectedOrCurrent"));
    await session.step(55, "Then line chart viewer should show 36 rows", () => showsRows(page, el("line chart viewer"), 36));
    await session.step(56, "When user clears the row selection of table \"spgi-100\"", () => clearSelectionInTable(page, "spgi-100"));
    await session.step(57, "Then line chart viewer should show 1 rows", () => showsRows(page, el("line chart viewer"), 1));
    await session.step(58, "When user makes row 10 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 10, "spgi-100"));
    await session.step(59, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"S_ACHIR\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "S_ACHIR"));
    await session.step(60, "And line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
    await session.step(61, "When user sets \"rowSource\" property of line chart viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("line chart viewer"), "CurrentRow"));
    await session.step(62, "Then line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
    await session.step(63, "When user makes row 1 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 1, "spgi-100"));
    await session.step(64, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"R_ONE\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "R_ONE"));
    await session.step(65, "And line chart viewer should show 1 rows", () => showsRows(page, el("line chart viewer"), 1));
    await session.step(66, "Given user adds a pie chart viewer with:", () => addViewerWith(page, "pie chart", [["table","spgi-100"],["categoryColumnName","Stereo Category"]]), [["table","spgi-100"],["categoryColumnName","Stereo Category"]]);
    await session.step(69, "When user sets \"rowSource\" property of line chart viewer to \"MouseOverGroup\"", () => setProperty(page, "rowSource", el("line chart viewer"), "MouseOverGroup"));
    await session.step(70, "And user moves the pointer away from pie chart viewer", () => pointerAway(page, el("pie chart viewer")));
    await session.step(71, "Then line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
    await session.step(72, "When user hovers over the \"slice R_ONE\" area of pie chart viewer", () => hoverArea(page, "slice R_ONE", el("pie chart viewer")));
    await session.step(73, "Then line chart viewer should show 22 rows", () => showsRows(page, el("line chart viewer"), 22));
    await session.step(74, "When user hovers over the \"slice S_ACHIR\" area of pie chart viewer", () => hoverArea(page, "slice S_ACHIR", el("pie chart viewer")));
    await session.step(75, "Then line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
    await session.step(76, "And no errors should have been logged", () => noErrors(page));
  });
  test("histogram rebound to spgi-100 answers that table's rows under every row source [viewer=histogram, property=valueColumnName, column=TPSA, property 2=valueColumnName, column 2=TPSA, source=pie chart, source property=categoryColumnName, source area=slice]", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.line-chart", "@realizes:viewers.histogram", "@realizes:viewers.bar-chart", "@realizes:viewers.pie-chart", "@realizes:viewers.box-plot", "@realizes:viewers.pc-plot"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(24, "Given user is logged in", () => loggedIn(page));
    await session.step(25, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(26, "And user opens spgi dataset", () => openDataset(page, ds("spgi")));
    await session.step(27, "And user switches to the \"demog-1000\" table view", () => switchTableView(page, "demog-1000"));
    await session.step(30, "Given user adds a histogram viewer", () => addViewer(page, "histogram"));
    await session.step(31, "When user sets properties of histogram viewer:", () => setProperties(page, el("histogram viewer"), [["table","spgi-100"],["valueColumnName","TPSA"],["valueColumnName","TPSA"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]), [["table","spgi-100"],["valueColumnName","TPSA"],["valueColumnName","TPSA"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]);
    await session.step(37, "Then histogram viewer should be bound to table \"spgi-100\"", () => boundTable(page, el("histogram viewer"), "spgi-100"));
    await session.step(38, "And histogram viewer should show 54 rows", () => showsRows(page, el("histogram viewer"), 54));
    await session.step(39, "When user adds a range filter on \"TPSA\" of table \"spgi-100\" from 30 to 80", () => addRangeFilterInTable(page, "TPSA", "spgi-100", 30, 80));
    await session.step(40, "Then 59 rows of table \"spgi-100\" should pass the filter", () => tableFilterCount(page, 59, "spgi-100"));
    await session.step(41, "And histogram viewer should show 32 rows", () => showsRows(page, el("histogram viewer"), 32));
    await session.step(42, "When user sets \"rowSource\" property of histogram viewer to \"All\"", () => setProperty(page, "rowSource", el("histogram viewer"), "All"));
    await session.step(43, "Then histogram viewer should show 54 rows", () => showsRows(page, el("histogram viewer"), 54));
    await session.step(44, "When user sets \"rowSource\" property of histogram viewer to \"Selected\"", () => setProperty(page, "rowSource", el("histogram viewer"), "Selected"));
    await session.step(45, "Then histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
    await session.step(46, "When user sets \"rowSource\" property of histogram viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("histogram viewer"), "FilteredSelected"));
    await session.step(47, "Then histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
    await session.step(48, "When user sets \"rowSource\" property of histogram viewer to \"Selected\"", () => setProperty(page, "rowSource", el("histogram viewer"), "Selected"));
    await session.step(49, "And user selects rows of table \"spgi-100\" where \"Stereo Category\" is one of \"R_ONE, S_ACHIR\"", () => selectInTableOneOf(page, "spgi-100", "Stereo Category", "R_ONE, S_ACHIR"));
    await session.step(50, "Then 70 rows of table \"spgi-100\" should be selected", () => selectedInTable(page, 70, "spgi-100"));
    await session.step(51, "And histogram viewer should show 36 rows", () => showsRows(page, el("histogram viewer"), 36));
    await session.step(52, "When user sets \"rowSource\" property of histogram viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("histogram viewer"), "FilteredSelected"));
    await session.step(53, "Then histogram viewer should show 22 rows", () => showsRows(page, el("histogram viewer"), 22));
    await session.step(54, "When user sets \"rowSource\" property of histogram viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("histogram viewer"), "SelectedOrCurrent"));
    await session.step(55, "Then histogram viewer should show 36 rows", () => showsRows(page, el("histogram viewer"), 36));
    await session.step(56, "When user clears the row selection of table \"spgi-100\"", () => clearSelectionInTable(page, "spgi-100"));
    await session.step(57, "Then histogram viewer should show 1 rows", () => showsRows(page, el("histogram viewer"), 1));
    await session.step(58, "When user makes row 10 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 10, "spgi-100"));
    await session.step(59, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"S_ACHIR\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "S_ACHIR"));
    await session.step(60, "And histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
    await session.step(61, "When user sets \"rowSource\" property of histogram viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("histogram viewer"), "CurrentRow"));
    await session.step(62, "Then histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
    await session.step(63, "When user makes row 1 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 1, "spgi-100"));
    await session.step(64, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"R_ONE\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "R_ONE"));
    await session.step(65, "And histogram viewer should show 1 rows", () => showsRows(page, el("histogram viewer"), 1));
    await session.step(66, "Given user adds a pie chart viewer with:", () => addViewerWith(page, "pie chart", [["table","spgi-100"],["categoryColumnName","Stereo Category"]]), [["table","spgi-100"],["categoryColumnName","Stereo Category"]]);
    await session.step(69, "When user sets \"rowSource\" property of histogram viewer to \"MouseOverGroup\"", () => setProperty(page, "rowSource", el("histogram viewer"), "MouseOverGroup"));
    await session.step(70, "And user moves the pointer away from pie chart viewer", () => pointerAway(page, el("pie chart viewer")));
    await session.step(71, "Then histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
    await session.step(72, "When user hovers over the \"slice R_ONE\" area of pie chart viewer", () => hoverArea(page, "slice R_ONE", el("pie chart viewer")));
    await session.step(73, "Then histogram viewer should show 22 rows", () => showsRows(page, el("histogram viewer"), 22));
    await session.step(74, "When user hovers over the \"slice S_ACHIR\" area of pie chart viewer", () => hoverArea(page, "slice S_ACHIR", el("pie chart viewer")));
    await session.step(75, "Then histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
    await session.step(76, "And no errors should have been logged", () => noErrors(page));
  });
  test("bar chart rebound to spgi-100 answers that table's rows under every row source [viewer=bar chart, property=valueColumnName, column=TPSA, property 2=splitColumnName, column 2=Stereo Category, source=pie chart, source property=categoryColumnName, source area=slice]", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.line-chart", "@realizes:viewers.histogram", "@realizes:viewers.bar-chart", "@realizes:viewers.pie-chart", "@realizes:viewers.box-plot", "@realizes:viewers.pc-plot"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(24, "Given user is logged in", () => loggedIn(page));
    await session.step(25, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(26, "And user opens spgi dataset", () => openDataset(page, ds("spgi")));
    await session.step(27, "And user switches to the \"demog-1000\" table view", () => switchTableView(page, "demog-1000"));
    await session.step(30, "Given user adds a bar chart viewer", () => addViewer(page, "bar chart"));
    await session.step(31, "When user sets properties of bar chart viewer:", () => setProperties(page, el("bar chart viewer"), [["table","spgi-100"],["valueColumnName","TPSA"],["splitColumnName","Stereo Category"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]), [["table","spgi-100"],["valueColumnName","TPSA"],["splitColumnName","Stereo Category"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]);
    await session.step(37, "Then bar chart viewer should be bound to table \"spgi-100\"", () => boundTable(page, el("bar chart viewer"), "spgi-100"));
    await session.step(38, "And bar chart viewer should show 54 rows", () => showsRows(page, el("bar chart viewer"), 54));
    await session.step(39, "When user adds a range filter on \"TPSA\" of table \"spgi-100\" from 30 to 80", () => addRangeFilterInTable(page, "TPSA", "spgi-100", 30, 80));
    await session.step(40, "Then 59 rows of table \"spgi-100\" should pass the filter", () => tableFilterCount(page, 59, "spgi-100"));
    await session.step(41, "And bar chart viewer should show 32 rows", () => showsRows(page, el("bar chart viewer"), 32));
    await session.step(42, "When user sets \"rowSource\" property of bar chart viewer to \"All\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "All"));
    await session.step(43, "Then bar chart viewer should show 54 rows", () => showsRows(page, el("bar chart viewer"), 54));
    await session.step(44, "When user sets \"rowSource\" property of bar chart viewer to \"Selected\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "Selected"));
    await session.step(45, "Then bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
    await session.step(46, "When user sets \"rowSource\" property of bar chart viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "FilteredSelected"));
    await session.step(47, "Then bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
    await session.step(48, "When user sets \"rowSource\" property of bar chart viewer to \"Selected\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "Selected"));
    await session.step(49, "And user selects rows of table \"spgi-100\" where \"Stereo Category\" is one of \"R_ONE, S_ACHIR\"", () => selectInTableOneOf(page, "spgi-100", "Stereo Category", "R_ONE, S_ACHIR"));
    await session.step(50, "Then 70 rows of table \"spgi-100\" should be selected", () => selectedInTable(page, 70, "spgi-100"));
    await session.step(51, "And bar chart viewer should show 36 rows", () => showsRows(page, el("bar chart viewer"), 36));
    await session.step(52, "When user sets \"rowSource\" property of bar chart viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "FilteredSelected"));
    await session.step(53, "Then bar chart viewer should show 22 rows", () => showsRows(page, el("bar chart viewer"), 22));
    await session.step(54, "When user sets \"rowSource\" property of bar chart viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "SelectedOrCurrent"));
    await session.step(55, "Then bar chart viewer should show 36 rows", () => showsRows(page, el("bar chart viewer"), 36));
    await session.step(56, "When user clears the row selection of table \"spgi-100\"", () => clearSelectionInTable(page, "spgi-100"));
    await session.step(57, "Then bar chart viewer should show 1 rows", () => showsRows(page, el("bar chart viewer"), 1));
    await session.step(58, "When user makes row 10 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 10, "spgi-100"));
    await session.step(59, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"S_ACHIR\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "S_ACHIR"));
    await session.step(60, "And bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
    await session.step(61, "When user sets \"rowSource\" property of bar chart viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "CurrentRow"));
    await session.step(62, "Then bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
    await session.step(63, "When user makes row 1 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 1, "spgi-100"));
    await session.step(64, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"R_ONE\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "R_ONE"));
    await session.step(65, "And bar chart viewer should show 1 rows", () => showsRows(page, el("bar chart viewer"), 1));
    await session.step(66, "Given user adds a pie chart viewer with:", () => addViewerWith(page, "pie chart", [["table","spgi-100"],["categoryColumnName","Stereo Category"]]), [["table","spgi-100"],["categoryColumnName","Stereo Category"]]);
    await session.step(69, "When user sets \"rowSource\" property of bar chart viewer to \"MouseOverGroup\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "MouseOverGroup"));
    await session.step(70, "And user moves the pointer away from pie chart viewer", () => pointerAway(page, el("pie chart viewer")));
    await session.step(71, "Then bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
    await session.step(72, "When user hovers over the \"slice R_ONE\" area of pie chart viewer", () => hoverArea(page, "slice R_ONE", el("pie chart viewer")));
    await session.step(73, "Then bar chart viewer should show 22 rows", () => showsRows(page, el("bar chart viewer"), 22));
    await session.step(74, "When user hovers over the \"slice S_ACHIR\" area of pie chart viewer", () => hoverArea(page, "slice S_ACHIR", el("pie chart viewer")));
    await session.step(75, "Then bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
    await session.step(76, "And no errors should have been logged", () => noErrors(page));
  });
  test("pie chart rebound to spgi-100 answers that table's rows under every row source [viewer=pie chart, property=categoryColumnName, column=Stereo Category, property 2=categoryColumnName, column 2=Stereo Category, source=bar chart, source property=splitColumnName, source area=bar]", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.line-chart", "@realizes:viewers.histogram", "@realizes:viewers.bar-chart", "@realizes:viewers.pie-chart", "@realizes:viewers.box-plot", "@realizes:viewers.pc-plot"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(24, "Given user is logged in", () => loggedIn(page));
    await session.step(25, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(26, "And user opens spgi dataset", () => openDataset(page, ds("spgi")));
    await session.step(27, "And user switches to the \"demog-1000\" table view", () => switchTableView(page, "demog-1000"));
    await session.step(30, "Given user adds a pie chart viewer", () => addViewer(page, "pie chart"));
    await session.step(31, "When user sets properties of pie chart viewer:", () => setProperties(page, el("pie chart viewer"), [["table","spgi-100"],["categoryColumnName","Stereo Category"],["categoryColumnName","Stereo Category"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]), [["table","spgi-100"],["categoryColumnName","Stereo Category"],["categoryColumnName","Stereo Category"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]);
    await session.step(37, "Then pie chart viewer should be bound to table \"spgi-100\"", () => boundTable(page, el("pie chart viewer"), "spgi-100"));
    await session.step(38, "And pie chart viewer should show 54 rows", () => showsRows(page, el("pie chart viewer"), 54));
    await session.step(39, "When user adds a range filter on \"TPSA\" of table \"spgi-100\" from 30 to 80", () => addRangeFilterInTable(page, "TPSA", "spgi-100", 30, 80));
    await session.step(40, "Then 59 rows of table \"spgi-100\" should pass the filter", () => tableFilterCount(page, 59, "spgi-100"));
    await session.step(41, "And pie chart viewer should show 32 rows", () => showsRows(page, el("pie chart viewer"), 32));
    await session.step(42, "When user sets \"rowSource\" property of pie chart viewer to \"All\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "All"));
    await session.step(43, "Then pie chart viewer should show 54 rows", () => showsRows(page, el("pie chart viewer"), 54));
    await session.step(44, "When user sets \"rowSource\" property of pie chart viewer to \"Selected\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "Selected"));
    await session.step(45, "Then pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
    await session.step(46, "When user sets \"rowSource\" property of pie chart viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "FilteredSelected"));
    await session.step(47, "Then pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
    await session.step(48, "When user sets \"rowSource\" property of pie chart viewer to \"Selected\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "Selected"));
    await session.step(49, "And user selects rows of table \"spgi-100\" where \"Stereo Category\" is one of \"R_ONE, S_ACHIR\"", () => selectInTableOneOf(page, "spgi-100", "Stereo Category", "R_ONE, S_ACHIR"));
    await session.step(50, "Then 70 rows of table \"spgi-100\" should be selected", () => selectedInTable(page, 70, "spgi-100"));
    await session.step(51, "And pie chart viewer should show 36 rows", () => showsRows(page, el("pie chart viewer"), 36));
    await session.step(52, "When user sets \"rowSource\" property of pie chart viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "FilteredSelected"));
    await session.step(53, "Then pie chart viewer should show 22 rows", () => showsRows(page, el("pie chart viewer"), 22));
    await session.step(54, "When user sets \"rowSource\" property of pie chart viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "SelectedOrCurrent"));
    await session.step(55, "Then pie chart viewer should show 36 rows", () => showsRows(page, el("pie chart viewer"), 36));
    await session.step(56, "When user clears the row selection of table \"spgi-100\"", () => clearSelectionInTable(page, "spgi-100"));
    await session.step(57, "Then pie chart viewer should show 1 rows", () => showsRows(page, el("pie chart viewer"), 1));
    await session.step(58, "When user makes row 10 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 10, "spgi-100"));
    await session.step(59, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"S_ACHIR\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "S_ACHIR"));
    await session.step(60, "And pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
    await session.step(61, "When user sets \"rowSource\" property of pie chart viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "CurrentRow"));
    await session.step(62, "Then pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
    await session.step(63, "When user makes row 1 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 1, "spgi-100"));
    await session.step(64, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"R_ONE\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "R_ONE"));
    await session.step(65, "And pie chart viewer should show 1 rows", () => showsRows(page, el("pie chart viewer"), 1));
    await session.step(66, "Given user adds a bar chart viewer with:", () => addViewerWith(page, "bar chart", [["table","spgi-100"],["splitColumnName","Stereo Category"]]), [["table","spgi-100"],["splitColumnName","Stereo Category"]]);
    await session.step(69, "When user sets \"rowSource\" property of pie chart viewer to \"MouseOverGroup\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "MouseOverGroup"));
    await session.step(70, "And user moves the pointer away from bar chart viewer", () => pointerAway(page, el("bar chart viewer")));
    await session.step(71, "Then pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
    await session.step(72, "When user hovers over the \"bar R_ONE\" area of bar chart viewer", () => hoverArea(page, "bar R_ONE", el("bar chart viewer")));
    await session.step(73, "Then pie chart viewer should show 22 rows", () => showsRows(page, el("pie chart viewer"), 22));
    await session.step(74, "When user hovers over the \"bar S_ACHIR\" area of bar chart viewer", () => hoverArea(page, "bar S_ACHIR", el("bar chart viewer")));
    await session.step(75, "Then pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
    await session.step(76, "And no errors should have been logged", () => noErrors(page));
  });
  test("box plot rebound to spgi-100 answers that table's rows under every row source [viewer=box plot, property=categoryColumnNames, column=Stereo Category, property 2=valueColumnName, column 2=TPSA, source=pie chart, source property=categoryColumnName, source area=slice]", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.line-chart", "@realizes:viewers.histogram", "@realizes:viewers.bar-chart", "@realizes:viewers.pie-chart", "@realizes:viewers.box-plot", "@realizes:viewers.pc-plot"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(24, "Given user is logged in", () => loggedIn(page));
    await session.step(25, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(26, "And user opens spgi dataset", () => openDataset(page, ds("spgi")));
    await session.step(27, "And user switches to the \"demog-1000\" table view", () => switchTableView(page, "demog-1000"));
    await session.step(30, "Given user adds a box plot viewer", () => addViewer(page, "box plot"));
    await session.step(31, "When user sets properties of box plot viewer:", () => setProperties(page, el("box plot viewer"), [["table","spgi-100"],["categoryColumnNames","Stereo Category"],["valueColumnName","TPSA"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]), [["table","spgi-100"],["categoryColumnNames","Stereo Category"],["valueColumnName","TPSA"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]);
    await session.step(37, "Then box plot viewer should be bound to table \"spgi-100\"", () => boundTable(page, el("box plot viewer"), "spgi-100"));
    await session.step(38, "And box plot viewer should show 54 rows", () => showsRows(page, el("box plot viewer"), 54));
    await session.step(39, "When user adds a range filter on \"TPSA\" of table \"spgi-100\" from 30 to 80", () => addRangeFilterInTable(page, "TPSA", "spgi-100", 30, 80));
    await session.step(40, "Then 59 rows of table \"spgi-100\" should pass the filter", () => tableFilterCount(page, 59, "spgi-100"));
    await session.step(41, "And box plot viewer should show 32 rows", () => showsRows(page, el("box plot viewer"), 32));
    await session.step(42, "When user sets \"rowSource\" property of box plot viewer to \"All\"", () => setProperty(page, "rowSource", el("box plot viewer"), "All"));
    await session.step(43, "Then box plot viewer should show 54 rows", () => showsRows(page, el("box plot viewer"), 54));
    await session.step(44, "When user sets \"rowSource\" property of box plot viewer to \"Selected\"", () => setProperty(page, "rowSource", el("box plot viewer"), "Selected"));
    await session.step(45, "Then box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
    await session.step(46, "When user sets \"rowSource\" property of box plot viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("box plot viewer"), "FilteredSelected"));
    await session.step(47, "Then box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
    await session.step(48, "When user sets \"rowSource\" property of box plot viewer to \"Selected\"", () => setProperty(page, "rowSource", el("box plot viewer"), "Selected"));
    await session.step(49, "And user selects rows of table \"spgi-100\" where \"Stereo Category\" is one of \"R_ONE, S_ACHIR\"", () => selectInTableOneOf(page, "spgi-100", "Stereo Category", "R_ONE, S_ACHIR"));
    await session.step(50, "Then 70 rows of table \"spgi-100\" should be selected", () => selectedInTable(page, 70, "spgi-100"));
    await session.step(51, "And box plot viewer should show 36 rows", () => showsRows(page, el("box plot viewer"), 36));
    await session.step(52, "When user sets \"rowSource\" property of box plot viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("box plot viewer"), "FilteredSelected"));
    await session.step(53, "Then box plot viewer should show 22 rows", () => showsRows(page, el("box plot viewer"), 22));
    await session.step(54, "When user sets \"rowSource\" property of box plot viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("box plot viewer"), "SelectedOrCurrent"));
    await session.step(55, "Then box plot viewer should show 36 rows", () => showsRows(page, el("box plot viewer"), 36));
    await session.step(56, "When user clears the row selection of table \"spgi-100\"", () => clearSelectionInTable(page, "spgi-100"));
    await session.step(57, "Then box plot viewer should show 1 rows", () => showsRows(page, el("box plot viewer"), 1));
    await session.step(58, "When user makes row 10 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 10, "spgi-100"));
    await session.step(59, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"S_ACHIR\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "S_ACHIR"));
    await session.step(60, "And box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
    await session.step(61, "When user sets \"rowSource\" property of box plot viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("box plot viewer"), "CurrentRow"));
    await session.step(62, "Then box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
    await session.step(63, "When user makes row 1 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 1, "spgi-100"));
    await session.step(64, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"R_ONE\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "R_ONE"));
    await session.step(65, "And box plot viewer should show 1 rows", () => showsRows(page, el("box plot viewer"), 1));
    await session.step(66, "Given user adds a pie chart viewer with:", () => addViewerWith(page, "pie chart", [["table","spgi-100"],["categoryColumnName","Stereo Category"]]), [["table","spgi-100"],["categoryColumnName","Stereo Category"]]);
    await session.step(69, "When user sets \"rowSource\" property of box plot viewer to \"MouseOverGroup\"", () => setProperty(page, "rowSource", el("box plot viewer"), "MouseOverGroup"));
    await session.step(70, "And user moves the pointer away from pie chart viewer", () => pointerAway(page, el("pie chart viewer")));
    await session.step(71, "Then box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
    await session.step(72, "When user hovers over the \"slice R_ONE\" area of pie chart viewer", () => hoverArea(page, "slice R_ONE", el("pie chart viewer")));
    await session.step(73, "Then box plot viewer should show 22 rows", () => showsRows(page, el("box plot viewer"), 22));
    await session.step(74, "When user hovers over the \"slice S_ACHIR\" area of pie chart viewer", () => hoverArea(page, "slice S_ACHIR", el("pie chart viewer")));
    await session.step(75, "Then box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
    await session.step(76, "And no errors should have been logged", () => noErrors(page));
  });
  test("pc plot rebound to spgi-100 answers that table's rows under every row source [viewer=pc plot, property=columnNames, column=Chemical Space X, Chemical Space Y, TPSA, property 2=columnNames, column 2=Chemical Space X, Chemical Space Y, TPSA, source=pie chart, source property=categoryColumnName, source area=slice]", {tag: ["@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.line-chart", "@realizes:viewers.histogram", "@realizes:viewers.bar-chart", "@realizes:viewers.pie-chart", "@realizes:viewers.box-plot", "@realizes:viewers.pc-plot"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(24, "Given user is logged in", () => loggedIn(page));
    await session.step(25, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(26, "And user opens spgi dataset", () => openDataset(page, ds("spgi")));
    await session.step(27, "And user switches to the \"demog-1000\" table view", () => switchTableView(page, "demog-1000"));
    await session.step(30, "Given user adds a pc plot viewer", () => addViewer(page, "pc plot"));
    await session.step(31, "When user sets properties of pc plot viewer:", () => setProperties(page, el("pc plot viewer"), [["table","spgi-100"],["columnNames","Chemical Space X, Chemical Space Y, TPSA"],["columnNames","Chemical Space X, Chemical Space Y, TPSA"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]), [["table","spgi-100"],["columnNames","Chemical Space X, Chemical Space Y, TPSA"],["columnNames","Chemical Space X, Chemical Space Y, TPSA"],["rowSource","Filtered"],["filter","${Stereo Category} in [\"R_ONE\", \"S_UNKN\"]"]]);
    await session.step(37, "Then pc plot viewer should be bound to table \"spgi-100\"", () => boundTable(page, el("pc plot viewer"), "spgi-100"));
    await session.step(38, "And pc plot viewer should show 54 rows", () => showsRows(page, el("pc plot viewer"), 54));
    await session.step(39, "When user adds a range filter on \"TPSA\" of table \"spgi-100\" from 30 to 80", () => addRangeFilterInTable(page, "TPSA", "spgi-100", 30, 80));
    await session.step(40, "Then 59 rows of table \"spgi-100\" should pass the filter", () => tableFilterCount(page, 59, "spgi-100"));
    await session.step(41, "And pc plot viewer should show 32 rows", () => showsRows(page, el("pc plot viewer"), 32));
    await session.step(42, "When user sets \"rowSource\" property of pc plot viewer to \"All\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "All"));
    await session.step(43, "Then pc plot viewer should show 54 rows", () => showsRows(page, el("pc plot viewer"), 54));
    await session.step(44, "When user sets \"rowSource\" property of pc plot viewer to \"Selected\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "Selected"));
    await session.step(45, "Then pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
    await session.step(46, "When user sets \"rowSource\" property of pc plot viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "FilteredSelected"));
    await session.step(47, "Then pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
    await session.step(48, "When user sets \"rowSource\" property of pc plot viewer to \"Selected\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "Selected"));
    await session.step(49, "And user selects rows of table \"spgi-100\" where \"Stereo Category\" is one of \"R_ONE, S_ACHIR\"", () => selectInTableOneOf(page, "spgi-100", "Stereo Category", "R_ONE, S_ACHIR"));
    await session.step(50, "Then 70 rows of table \"spgi-100\" should be selected", () => selectedInTable(page, 70, "spgi-100"));
    await session.step(51, "And pc plot viewer should show 36 rows", () => showsRows(page, el("pc plot viewer"), 36));
    await session.step(52, "When user sets \"rowSource\" property of pc plot viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "FilteredSelected"));
    await session.step(53, "Then pc plot viewer should show 22 rows", () => showsRows(page, el("pc plot viewer"), 22));
    await session.step(54, "When user sets \"rowSource\" property of pc plot viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "SelectedOrCurrent"));
    await session.step(55, "Then pc plot viewer should show 36 rows", () => showsRows(page, el("pc plot viewer"), 36));
    await session.step(56, "When user clears the row selection of table \"spgi-100\"", () => clearSelectionInTable(page, "spgi-100"));
    await session.step(57, "Then pc plot viewer should show 1 rows", () => showsRows(page, el("pc plot viewer"), 1));
    await session.step(58, "When user makes row 10 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 10, "spgi-100"));
    await session.step(59, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"S_ACHIR\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "S_ACHIR"));
    await session.step(60, "And pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
    await session.step(61, "When user sets \"rowSource\" property of pc plot viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "CurrentRow"));
    await session.step(62, "Then pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
    await session.step(63, "When user makes row 1 of table \"spgi-100\" current", () => makeRowCurrentInTable(page, 1, "spgi-100"));
    await session.step(64, "Then \"Stereo Category\" of the current row of table \"spgi-100\" should be \"R_ONE\"", () => currentRowOfTableValue(page, "Stereo Category", "spgi-100", "R_ONE"));
    await session.step(65, "And pc plot viewer should show 1 rows", () => showsRows(page, el("pc plot viewer"), 1));
    await session.step(66, "Given user adds a pie chart viewer with:", () => addViewerWith(page, "pie chart", [["table","spgi-100"],["categoryColumnName","Stereo Category"]]), [["table","spgi-100"],["categoryColumnName","Stereo Category"]]);
    await session.step(69, "When user sets \"rowSource\" property of pc plot viewer to \"MouseOverGroup\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "MouseOverGroup"));
    await session.step(70, "And user moves the pointer away from pie chart viewer", () => pointerAway(page, el("pie chart viewer")));
    await session.step(71, "Then pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
    await session.step(72, "When user hovers over the \"slice R_ONE\" area of pie chart viewer", () => hoverArea(page, "slice R_ONE", el("pie chart viewer")));
    await session.step(73, "Then pc plot viewer should show 22 rows", () => showsRows(page, el("pc plot viewer"), 22));
    await session.step(74, "When user hovers over the \"slice S_ACHIR\" area of pie chart viewer", () => hoverArea(page, "slice S_ACHIR", el("pie chart viewer")));
    await session.step(75, "Then pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
    await session.step(76, "And no errors should have been logged", () => noErrors(page));
  });
});
