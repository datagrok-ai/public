/* eslint-disable max-len */
/* eslint-disable comma-spacing */
/* eslint-disable quotes */
/* ---
generated: features/viewers/row-source/row-source.feature
generator: @datagrok-libraries/bdd — do not edit; run `grok-bdd compile` to regenerate
sub_features_covered: [viewers.scatter-plot, viewers.line-chart, viewers.histogram, viewers.bar-chart, viewers.pie-chart, viewers.box-plot, viewers.pc-plot]
--- */
import {test} from '@playwright/test';
import '../../../bindings/connections.js';
import '../../../bindings/grid.js';
import '../../../bindings/nx.js';
import '../../../bindings/spaces.js';
import '../../../bindings/tile-viewer.js';
import '../../../bindings/trellis-plot.js';
import '@datagrok-libraries/bdd/bindings/common/kinds';
import '@datagrok-libraries/bdd/bindings/common/parameter-types';
import '@datagrok-libraries/bdd/bindings/platform/datasets';
import '@datagrok-libraries/bdd/bindings/platform/elements';
import {loggedIn} from '@datagrok-libraries/bdd/bindings/common/session';
import {makeRowCurrent} from '@datagrok-libraries/bdd/bindings/platform/columns';
import {addCategoricalFilter, clearSelection, currentRowValue, filterPasses, filterPassesAll, mouseOverRowIs, noneSelected, rowCount, selectWhereBetween, selectedRowCount} from '@datagrok-libraries/bdd/bindings/platform/data';
import {openDataset} from '@datagrok-libraries/bdd/bindings/platform/steps';
import {addViewerWith, hoverArea, noErrors, pointerAway, propertyShouldBe, setProperty, showsRows} from '@datagrok-libraries/bdd/bindings/tiers/viewers/steps';
import {ds, el, feature, journey} from '@datagrok-libraries/bdd/runtime';

test.describe("Row Source, the same contract on seven viewers", () => {
  const session = feature(test, "features/viewers/row-source/row-source.feature", import.meta.url);
  test("Row Source, the same contract on seven viewers", {tag: ["@journey", "@viewers", "@realizes:viewers.scatter-plot", "@realizes:viewers.line-chart", "@realizes:viewers.histogram", "@realizes:viewers.bar-chart", "@realizes:viewers.pie-chart", "@realizes:viewers.box-plot", "@realizes:viewers.pc-plot"]}, async ({browser}) => {
    const page = await session.page(browser);
    const run = journey(test, 49, page);
    await session.step(27, "Given user is logged in", () => loggedIn(page));
    await session.step(28, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(29, "And user adds a scatter plot viewer with:", () => addViewerWith(page, "scatter plot", [["xColumnName","AGE"],["yColumnName","WEIGHT"],["colorColumnName","RACE"],["filter","${AGE} > 44"]]), [["xColumnName","AGE"],["yColumnName","WEIGHT"],["colorColumnName","RACE"],["filter","${AGE} > 44"]]);
    await session.step(34, "And user adds a line chart viewer with:", () => addViewerWith(page, "line chart", [["xColumnName","AGE"],["yColumnNames","WEIGHT"],["filter","${AGE} > 44"]]), [["xColumnName","AGE"],["yColumnNames","WEIGHT"],["filter","${AGE} > 44"]]);
    await session.step(38, "And user adds a histogram viewer with:", () => addViewerWith(page, "histogram", [["valueColumnName","AGE"],["filter","${AGE} > 44"]]), [["valueColumnName","AGE"],["filter","${AGE} > 44"]]);
    await session.step(41, "And user adds a bar chart viewer with:", () => addViewerWith(page, "bar chart", [["valueColumnName","AGE"],["splitColumnName","RACE"],["filter","${AGE} > 44"]]), [["valueColumnName","AGE"],["splitColumnName","RACE"],["filter","${AGE} > 44"]]);
    await session.step(45, "And user adds a pie chart viewer with:", () => addViewerWith(page, "pie chart", [["categoryColumnName","RACE"],["filter","${AGE} > 44"]]), [["categoryColumnName","RACE"],["filter","${AGE} > 44"]]);
    await session.step(48, "And user adds a box plot viewer with:", () => addViewerWith(page, "box plot", [["categoryColumnNames","RACE"],["valueColumnName","AGE"],["filter","${AGE} > 44"]]), [["categoryColumnNames","RACE"],["valueColumnName","AGE"],["filter","${AGE} > 44"]]);
    await session.step(52, "And user adds a pc plot viewer with:", () => addViewerWith(page, "pc plot", [["columnNames","AGE, WEIGHT"],["filter","${AGE} > 44"]]), [["columnNames","AGE, WEIGHT"],["filter","${AGE} > 44"]]);
    await session.step(55, "Then \"rowSource\" property of scatter plot viewer should be \"Filtered\"", () => propertyShouldBe(page, "rowSource", el("scatter plot viewer"), "Filtered"));
    await session.step(56, "And \"rowSource\" property of pie chart viewer should be \"Filtered\"", () => propertyShouldBe(page, "rowSource", el("pie chart viewer"), "Filtered"));
    await session.step(57, "And the table should have 1000 rows", () => rowCount(page, 1000));
    await run.scenario("Filtered — scatter plot answers the filter panel and its own filter at once [viewer=scatter plot, own filter=519, both filters=214]", async () => {
      await session.step(60, "Then scatter plot viewer should show 519 rows", () => showsRows(page, el("scatter plot viewer"), 519));
      await session.step(61, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(62, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(63, "And scatter plot viewer should show 214 rows", () => showsRows(page, el("scatter plot viewer"), 214));
      await session.step(64, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(65, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(66, "And scatter plot viewer should show 519 rows", () => showsRows(page, el("scatter plot viewer"), 519));
      await session.step(67, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Filtered — line chart answers the filter panel and its own filter at once [viewer=line chart, own filter=519, both filters=214]", async () => {
      await session.step(60, "Then line chart viewer should show 519 rows", () => showsRows(page, el("line chart viewer"), 519));
      await session.step(61, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(62, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(63, "And line chart viewer should show 214 rows", () => showsRows(page, el("line chart viewer"), 214));
      await session.step(64, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(65, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(66, "And line chart viewer should show 519 rows", () => showsRows(page, el("line chart viewer"), 519));
      await session.step(67, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Filtered — histogram answers the filter panel and its own filter at once [viewer=histogram, own filter=519, both filters=214]", async () => {
      await session.step(60, "Then histogram viewer should show 519 rows", () => showsRows(page, el("histogram viewer"), 519));
      await session.step(61, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(62, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(63, "And histogram viewer should show 214 rows", () => showsRows(page, el("histogram viewer"), 214));
      await session.step(64, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(65, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(66, "And histogram viewer should show 519 rows", () => showsRows(page, el("histogram viewer"), 519));
      await session.step(67, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Filtered — bar chart answers the filter panel and its own filter at once [viewer=bar chart, own filter=519, both filters=214]", async () => {
      await session.step(60, "Then bar chart viewer should show 519 rows", () => showsRows(page, el("bar chart viewer"), 519));
      await session.step(61, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(62, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(63, "And bar chart viewer should show 214 rows", () => showsRows(page, el("bar chart viewer"), 214));
      await session.step(64, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(65, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(66, "And bar chart viewer should show 519 rows", () => showsRows(page, el("bar chart viewer"), 519));
      await session.step(67, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Filtered — pie chart answers the filter panel and its own filter at once [viewer=pie chart, own filter=519, both filters=214]", async () => {
      await session.step(60, "Then pie chart viewer should show 519 rows", () => showsRows(page, el("pie chart viewer"), 519));
      await session.step(61, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(62, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(63, "And pie chart viewer should show 214 rows", () => showsRows(page, el("pie chart viewer"), 214));
      await session.step(64, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(65, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(66, "And pie chart viewer should show 519 rows", () => showsRows(page, el("pie chart viewer"), 519));
      await session.step(67, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Filtered — box plot answers the filter panel and its own filter at once [viewer=box plot, own filter=519, both filters=214]", async () => {
      await session.step(60, "Then box plot viewer should show 519 rows", () => showsRows(page, el("box plot viewer"), 519));
      await session.step(61, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(62, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(63, "And box plot viewer should show 214 rows", () => showsRows(page, el("box plot viewer"), 214));
      await session.step(64, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(65, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(66, "And box plot viewer should show 519 rows", () => showsRows(page, el("box plot viewer"), 519));
      await session.step(67, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Filtered — pc plot answers the filter panel and its own filter at once [viewer=pc plot, own filter=519, both filters=214]", async () => {
      await session.step(60, "Then pc plot viewer should show 519 rows", () => showsRows(page, el("pc plot viewer"), 519));
      await session.step(61, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(62, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(63, "And pc plot viewer should show 214 rows", () => showsRows(page, el("pc plot viewer"), 214));
      await session.step(64, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(65, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(66, "And pc plot viewer should show 519 rows", () => showsRows(page, el("pc plot viewer"), 519));
      await session.step(67, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("All — scatter plot ignores the filter panel and keeps its own filter [viewer=scatter plot, own filter=519]", async () => {
      await session.step(80, "When user sets \"rowSource\" property of scatter plot viewer to \"All\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "All"));
      await session.step(81, "And user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(82, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(83, "And grid should show 447 rows", () => showsRows(page, el("grid"), 447));
      await session.step(84, "And scatter plot viewer should show 519 rows", () => showsRows(page, el("scatter plot viewer"), 519));
      await session.step(85, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(86, "And user sets \"rowSource\" property of scatter plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "Filtered"));
      await session.step(87, "Then scatter plot viewer should show 519 rows", () => showsRows(page, el("scatter plot viewer"), 519));
      await session.step(88, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("All — line chart ignores the filter panel and keeps its own filter [viewer=line chart, own filter=519]", async () => {
      await session.step(80, "When user sets \"rowSource\" property of line chart viewer to \"All\"", () => setProperty(page, "rowSource", el("line chart viewer"), "All"));
      await session.step(81, "And user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(82, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(83, "And grid should show 447 rows", () => showsRows(page, el("grid"), 447));
      await session.step(84, "And line chart viewer should show 519 rows", () => showsRows(page, el("line chart viewer"), 519));
      await session.step(85, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(86, "And user sets \"rowSource\" property of line chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("line chart viewer"), "Filtered"));
      await session.step(87, "Then line chart viewer should show 519 rows", () => showsRows(page, el("line chart viewer"), 519));
      await session.step(88, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("All — histogram ignores the filter panel and keeps its own filter [viewer=histogram, own filter=519]", async () => {
      await session.step(80, "When user sets \"rowSource\" property of histogram viewer to \"All\"", () => setProperty(page, "rowSource", el("histogram viewer"), "All"));
      await session.step(81, "And user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(82, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(83, "And grid should show 447 rows", () => showsRows(page, el("grid"), 447));
      await session.step(84, "And histogram viewer should show 519 rows", () => showsRows(page, el("histogram viewer"), 519));
      await session.step(85, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(86, "And user sets \"rowSource\" property of histogram viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("histogram viewer"), "Filtered"));
      await session.step(87, "Then histogram viewer should show 519 rows", () => showsRows(page, el("histogram viewer"), 519));
      await session.step(88, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("All — bar chart ignores the filter panel and keeps its own filter [viewer=bar chart, own filter=519]", async () => {
      await session.step(80, "When user sets \"rowSource\" property of bar chart viewer to \"All\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "All"));
      await session.step(81, "And user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(82, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(83, "And grid should show 447 rows", () => showsRows(page, el("grid"), 447));
      await session.step(84, "And bar chart viewer should show 519 rows", () => showsRows(page, el("bar chart viewer"), 519));
      await session.step(85, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(86, "And user sets \"rowSource\" property of bar chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "Filtered"));
      await session.step(87, "Then bar chart viewer should show 519 rows", () => showsRows(page, el("bar chart viewer"), 519));
      await session.step(88, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("All — pie chart ignores the filter panel and keeps its own filter [viewer=pie chart, own filter=519]", async () => {
      await session.step(80, "When user sets \"rowSource\" property of pie chart viewer to \"All\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "All"));
      await session.step(81, "And user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(82, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(83, "And grid should show 447 rows", () => showsRows(page, el("grid"), 447));
      await session.step(84, "And pie chart viewer should show 519 rows", () => showsRows(page, el("pie chart viewer"), 519));
      await session.step(85, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(86, "And user sets \"rowSource\" property of pie chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "Filtered"));
      await session.step(87, "Then pie chart viewer should show 519 rows", () => showsRows(page, el("pie chart viewer"), 519));
      await session.step(88, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("All — box plot ignores the filter panel and keeps its own filter [viewer=box plot, own filter=519]", async () => {
      await session.step(80, "When user sets \"rowSource\" property of box plot viewer to \"All\"", () => setProperty(page, "rowSource", el("box plot viewer"), "All"));
      await session.step(81, "And user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(82, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(83, "And grid should show 447 rows", () => showsRows(page, el("grid"), 447));
      await session.step(84, "And box plot viewer should show 519 rows", () => showsRows(page, el("box plot viewer"), 519));
      await session.step(85, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(86, "And user sets \"rowSource\" property of box plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("box plot viewer"), "Filtered"));
      await session.step(87, "Then box plot viewer should show 519 rows", () => showsRows(page, el("box plot viewer"), 519));
      await session.step(88, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("All — pc plot ignores the filter panel and keeps its own filter [viewer=pc plot, own filter=519]", async () => {
      await session.step(80, "When user sets \"rowSource\" property of pc plot viewer to \"All\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "All"));
      await session.step(81, "And user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(82, "Then 447 rows should pass the filter", () => filterPasses(page, 447));
      await session.step(83, "And grid should show 447 rows", () => showsRows(page, el("grid"), 447));
      await session.step(84, "And pc plot viewer should show 519 rows", () => showsRows(page, el("pc plot viewer"), 519));
      await session.step(85, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(86, "And user sets \"rowSource\" property of pc plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "Filtered"));
      await session.step(87, "Then pc plot viewer should show 519 rows", () => showsRows(page, el("pc plot viewer"), 519));
      await session.step(88, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Selected — scatter plot is empty until rows are selected, then shows the selected rows its filter keeps [viewer=scatter plot, selected=69]", async () => {
      await session.step(101, "When user sets \"rowSource\" property of scatter plot viewer to \"Selected\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "Selected"));
      await session.step(102, "Then no rows should be selected", () => noneSelected(page));
      await session.step(103, "And scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
      await session.step(104, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(105, "Then 152 rows should be selected", () => selectedRowCount(page, 152));
      await session.step(106, "And scatter plot viewer should show 69 rows", () => showsRows(page, el("scatter plot viewer"), 69));
      await session.step(107, "When user clears the row selection", () => clearSelection(page));
      await session.step(108, "Then scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
      await session.step(109, "When user sets \"rowSource\" property of scatter plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "Filtered"));
      await session.step(110, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Selected — line chart is empty until rows are selected, then shows the selected rows its filter keeps [viewer=line chart, selected=69]", async () => {
      await session.step(101, "When user sets \"rowSource\" property of line chart viewer to \"Selected\"", () => setProperty(page, "rowSource", el("line chart viewer"), "Selected"));
      await session.step(102, "Then no rows should be selected", () => noneSelected(page));
      await session.step(103, "And line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
      await session.step(104, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(105, "Then 152 rows should be selected", () => selectedRowCount(page, 152));
      await session.step(106, "And line chart viewer should show 69 rows", () => showsRows(page, el("line chart viewer"), 69));
      await session.step(107, "When user clears the row selection", () => clearSelection(page));
      await session.step(108, "Then line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
      await session.step(109, "When user sets \"rowSource\" property of line chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("line chart viewer"), "Filtered"));
      await session.step(110, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Selected — histogram is empty until rows are selected, then shows the selected rows its filter keeps [viewer=histogram, selected=69]", async () => {
      await session.step(101, "When user sets \"rowSource\" property of histogram viewer to \"Selected\"", () => setProperty(page, "rowSource", el("histogram viewer"), "Selected"));
      await session.step(102, "Then no rows should be selected", () => noneSelected(page));
      await session.step(103, "And histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
      await session.step(104, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(105, "Then 152 rows should be selected", () => selectedRowCount(page, 152));
      await session.step(106, "And histogram viewer should show 69 rows", () => showsRows(page, el("histogram viewer"), 69));
      await session.step(107, "When user clears the row selection", () => clearSelection(page));
      await session.step(108, "Then histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
      await session.step(109, "When user sets \"rowSource\" property of histogram viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("histogram viewer"), "Filtered"));
      await session.step(110, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Selected — bar chart is empty until rows are selected, then shows the selected rows its filter keeps [viewer=bar chart, selected=69]", async () => {
      await session.step(101, "When user sets \"rowSource\" property of bar chart viewer to \"Selected\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "Selected"));
      await session.step(102, "Then no rows should be selected", () => noneSelected(page));
      await session.step(103, "And bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
      await session.step(104, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(105, "Then 152 rows should be selected", () => selectedRowCount(page, 152));
      await session.step(106, "And bar chart viewer should show 69 rows", () => showsRows(page, el("bar chart viewer"), 69));
      await session.step(107, "When user clears the row selection", () => clearSelection(page));
      await session.step(108, "Then bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
      await session.step(109, "When user sets \"rowSource\" property of bar chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "Filtered"));
      await session.step(110, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Selected — pie chart is empty until rows are selected, then shows the selected rows its filter keeps [viewer=pie chart, selected=69]", async () => {
      await session.step(101, "When user sets \"rowSource\" property of pie chart viewer to \"Selected\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "Selected"));
      await session.step(102, "Then no rows should be selected", () => noneSelected(page));
      await session.step(103, "And pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
      await session.step(104, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(105, "Then 152 rows should be selected", () => selectedRowCount(page, 152));
      await session.step(106, "And pie chart viewer should show 69 rows", () => showsRows(page, el("pie chart viewer"), 69));
      await session.step(107, "When user clears the row selection", () => clearSelection(page));
      await session.step(108, "Then pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
      await session.step(109, "When user sets \"rowSource\" property of pie chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "Filtered"));
      await session.step(110, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Selected — box plot is empty until rows are selected, then shows the selected rows its filter keeps [viewer=box plot, selected=69]", async () => {
      await session.step(101, "When user sets \"rowSource\" property of box plot viewer to \"Selected\"", () => setProperty(page, "rowSource", el("box plot viewer"), "Selected"));
      await session.step(102, "Then no rows should be selected", () => noneSelected(page));
      await session.step(103, "And box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
      await session.step(104, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(105, "Then 152 rows should be selected", () => selectedRowCount(page, 152));
      await session.step(106, "And box plot viewer should show 69 rows", () => showsRows(page, el("box plot viewer"), 69));
      await session.step(107, "When user clears the row selection", () => clearSelection(page));
      await session.step(108, "Then box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
      await session.step(109, "When user sets \"rowSource\" property of box plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("box plot viewer"), "Filtered"));
      await session.step(110, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Selected — pc plot is empty until rows are selected, then shows the selected rows its filter keeps [viewer=pc plot, selected=69]", async () => {
      await session.step(101, "When user sets \"rowSource\" property of pc plot viewer to \"Selected\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "Selected"));
      await session.step(102, "Then no rows should be selected", () => noneSelected(page));
      await session.step(103, "And pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
      await session.step(104, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(105, "Then 152 rows should be selected", () => selectedRowCount(page, 152));
      await session.step(106, "And pc plot viewer should show 69 rows", () => showsRows(page, el("pc plot viewer"), 69));
      await session.step(107, "When user clears the row selection", () => clearSelection(page));
      await session.step(108, "Then pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
      await session.step(109, "When user sets \"rowSource\" property of pc plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "Filtered"));
      await session.step(110, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("SelectedOrCurrent — scatter plot shows the selection, and without one the current row its filter keeps [viewer=scatter plot, selected=69]", async () => {
      await session.step(123, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(124, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(125, "And user sets \"rowSource\" property of scatter plot viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "SelectedOrCurrent"));
      await session.step(126, "Then \"AGE\" of the current row should be \"58\"", () => currentRowValue(page, "AGE", "58"));
      await session.step(127, "And scatter plot viewer should show 69 rows", () => showsRows(page, el("scatter plot viewer"), 69));
      await session.step(128, "When user clears the row selection", () => clearSelection(page));
      await session.step(129, "Then scatter plot viewer should show 1 rows", () => showsRows(page, el("scatter plot viewer"), 1));
      await session.step(130, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(131, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(132, "And scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
      await session.step(133, "When user sets \"rowSource\" property of scatter plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "Filtered"));
      await session.step(134, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("SelectedOrCurrent — line chart shows the selection, and without one the current row its filter keeps [viewer=line chart, selected=69]", async () => {
      await session.step(123, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(124, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(125, "And user sets \"rowSource\" property of line chart viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("line chart viewer"), "SelectedOrCurrent"));
      await session.step(126, "Then \"AGE\" of the current row should be \"58\"", () => currentRowValue(page, "AGE", "58"));
      await session.step(127, "And line chart viewer should show 69 rows", () => showsRows(page, el("line chart viewer"), 69));
      await session.step(128, "When user clears the row selection", () => clearSelection(page));
      await session.step(129, "Then line chart viewer should show 1 rows", () => showsRows(page, el("line chart viewer"), 1));
      await session.step(130, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(131, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(132, "And line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
      await session.step(133, "When user sets \"rowSource\" property of line chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("line chart viewer"), "Filtered"));
      await session.step(134, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("SelectedOrCurrent — histogram shows the selection, and without one the current row its filter keeps [viewer=histogram, selected=69]", async () => {
      await session.step(123, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(124, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(125, "And user sets \"rowSource\" property of histogram viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("histogram viewer"), "SelectedOrCurrent"));
      await session.step(126, "Then \"AGE\" of the current row should be \"58\"", () => currentRowValue(page, "AGE", "58"));
      await session.step(127, "And histogram viewer should show 69 rows", () => showsRows(page, el("histogram viewer"), 69));
      await session.step(128, "When user clears the row selection", () => clearSelection(page));
      await session.step(129, "Then histogram viewer should show 1 rows", () => showsRows(page, el("histogram viewer"), 1));
      await session.step(130, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(131, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(132, "And histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
      await session.step(133, "When user sets \"rowSource\" property of histogram viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("histogram viewer"), "Filtered"));
      await session.step(134, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("SelectedOrCurrent — bar chart shows the selection, and without one the current row its filter keeps [viewer=bar chart, selected=69]", async () => {
      await session.step(123, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(124, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(125, "And user sets \"rowSource\" property of bar chart viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "SelectedOrCurrent"));
      await session.step(126, "Then \"AGE\" of the current row should be \"58\"", () => currentRowValue(page, "AGE", "58"));
      await session.step(127, "And bar chart viewer should show 69 rows", () => showsRows(page, el("bar chart viewer"), 69));
      await session.step(128, "When user clears the row selection", () => clearSelection(page));
      await session.step(129, "Then bar chart viewer should show 1 rows", () => showsRows(page, el("bar chart viewer"), 1));
      await session.step(130, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(131, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(132, "And bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
      await session.step(133, "When user sets \"rowSource\" property of bar chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "Filtered"));
      await session.step(134, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("SelectedOrCurrent — pie chart shows the selection, and without one the current row its filter keeps [viewer=pie chart, selected=69]", async () => {
      await session.step(123, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(124, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(125, "And user sets \"rowSource\" property of pie chart viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "SelectedOrCurrent"));
      await session.step(126, "Then \"AGE\" of the current row should be \"58\"", () => currentRowValue(page, "AGE", "58"));
      await session.step(127, "And pie chart viewer should show 69 rows", () => showsRows(page, el("pie chart viewer"), 69));
      await session.step(128, "When user clears the row selection", () => clearSelection(page));
      await session.step(129, "Then pie chart viewer should show 1 rows", () => showsRows(page, el("pie chart viewer"), 1));
      await session.step(130, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(131, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(132, "And pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
      await session.step(133, "When user sets \"rowSource\" property of pie chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "Filtered"));
      await session.step(134, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("SelectedOrCurrent — box plot shows the selection, and without one the current row its filter keeps [viewer=box plot, selected=69]", async () => {
      await session.step(123, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(124, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(125, "And user sets \"rowSource\" property of box plot viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("box plot viewer"), "SelectedOrCurrent"));
      await session.step(126, "Then \"AGE\" of the current row should be \"58\"", () => currentRowValue(page, "AGE", "58"));
      await session.step(127, "And box plot viewer should show 69 rows", () => showsRows(page, el("box plot viewer"), 69));
      await session.step(128, "When user clears the row selection", () => clearSelection(page));
      await session.step(129, "Then box plot viewer should show 1 rows", () => showsRows(page, el("box plot viewer"), 1));
      await session.step(130, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(131, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(132, "And box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
      await session.step(133, "When user sets \"rowSource\" property of box plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("box plot viewer"), "Filtered"));
      await session.step(134, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("SelectedOrCurrent — pc plot shows the selection, and without one the current row its filter keeps [viewer=pc plot, selected=69]", async () => {
      await session.step(123, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(124, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(125, "And user sets \"rowSource\" property of pc plot viewer to \"SelectedOrCurrent\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "SelectedOrCurrent"));
      await session.step(126, "Then \"AGE\" of the current row should be \"58\"", () => currentRowValue(page, "AGE", "58"));
      await session.step(127, "And pc plot viewer should show 69 rows", () => showsRows(page, el("pc plot viewer"), 69));
      await session.step(128, "When user clears the row selection", () => clearSelection(page));
      await session.step(129, "Then pc plot viewer should show 1 rows", () => showsRows(page, el("pc plot viewer"), 1));
      await session.step(130, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(131, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(132, "And pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
      await session.step(133, "When user sets \"rowSource\" property of pc plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "Filtered"));
      await session.step(134, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("FilteredSelected — scatter plot shows the selected rows that pass the filter panel and its own filter [viewer=scatter plot, selected=69, selected and filtered=29]", async () => {
      await session.step(147, "When user sets \"rowSource\" property of scatter plot viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "FilteredSelected"));
      await session.step(148, "Then no rows should be selected", () => noneSelected(page));
      await session.step(149, "And scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
      await session.step(150, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(151, "Then scatter plot viewer should show 69 rows", () => showsRows(page, el("scatter plot viewer"), 69));
      await session.step(152, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(153, "Then scatter plot viewer should show 29 rows", () => showsRows(page, el("scatter plot viewer"), 29));
      await session.step(154, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(155, "And user clears the row selection", () => clearSelection(page));
      await session.step(156, "And user sets \"rowSource\" property of scatter plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "Filtered"));
      await session.step(157, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(158, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("FilteredSelected — line chart shows the selected rows that pass the filter panel and its own filter [viewer=line chart, selected=69, selected and filtered=29]", async () => {
      await session.step(147, "When user sets \"rowSource\" property of line chart viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("line chart viewer"), "FilteredSelected"));
      await session.step(148, "Then no rows should be selected", () => noneSelected(page));
      await session.step(149, "And line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
      await session.step(150, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(151, "Then line chart viewer should show 69 rows", () => showsRows(page, el("line chart viewer"), 69));
      await session.step(152, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(153, "Then line chart viewer should show 29 rows", () => showsRows(page, el("line chart viewer"), 29));
      await session.step(154, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(155, "And user clears the row selection", () => clearSelection(page));
      await session.step(156, "And user sets \"rowSource\" property of line chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("line chart viewer"), "Filtered"));
      await session.step(157, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(158, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("FilteredSelected — histogram shows the selected rows that pass the filter panel and its own filter [viewer=histogram, selected=69, selected and filtered=29]", async () => {
      await session.step(147, "When user sets \"rowSource\" property of histogram viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("histogram viewer"), "FilteredSelected"));
      await session.step(148, "Then no rows should be selected", () => noneSelected(page));
      await session.step(149, "And histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
      await session.step(150, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(151, "Then histogram viewer should show 69 rows", () => showsRows(page, el("histogram viewer"), 69));
      await session.step(152, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(153, "Then histogram viewer should show 29 rows", () => showsRows(page, el("histogram viewer"), 29));
      await session.step(154, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(155, "And user clears the row selection", () => clearSelection(page));
      await session.step(156, "And user sets \"rowSource\" property of histogram viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("histogram viewer"), "Filtered"));
      await session.step(157, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(158, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("FilteredSelected — bar chart shows the selected rows that pass the filter panel and its own filter [viewer=bar chart, selected=69, selected and filtered=29]", async () => {
      await session.step(147, "When user sets \"rowSource\" property of bar chart viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "FilteredSelected"));
      await session.step(148, "Then no rows should be selected", () => noneSelected(page));
      await session.step(149, "And bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
      await session.step(150, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(151, "Then bar chart viewer should show 69 rows", () => showsRows(page, el("bar chart viewer"), 69));
      await session.step(152, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(153, "Then bar chart viewer should show 29 rows", () => showsRows(page, el("bar chart viewer"), 29));
      await session.step(154, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(155, "And user clears the row selection", () => clearSelection(page));
      await session.step(156, "And user sets \"rowSource\" property of bar chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "Filtered"));
      await session.step(157, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(158, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("FilteredSelected — pie chart shows the selected rows that pass the filter panel and its own filter [viewer=pie chart, selected=69, selected and filtered=29]", async () => {
      await session.step(147, "When user sets \"rowSource\" property of pie chart viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "FilteredSelected"));
      await session.step(148, "Then no rows should be selected", () => noneSelected(page));
      await session.step(149, "And pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
      await session.step(150, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(151, "Then pie chart viewer should show 69 rows", () => showsRows(page, el("pie chart viewer"), 69));
      await session.step(152, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(153, "Then pie chart viewer should show 29 rows", () => showsRows(page, el("pie chart viewer"), 29));
      await session.step(154, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(155, "And user clears the row selection", () => clearSelection(page));
      await session.step(156, "And user sets \"rowSource\" property of pie chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "Filtered"));
      await session.step(157, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(158, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("FilteredSelected — box plot shows the selected rows that pass the filter panel and its own filter [viewer=box plot, selected=69, selected and filtered=29]", async () => {
      await session.step(147, "When user sets \"rowSource\" property of box plot viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("box plot viewer"), "FilteredSelected"));
      await session.step(148, "Then no rows should be selected", () => noneSelected(page));
      await session.step(149, "And box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
      await session.step(150, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(151, "Then box plot viewer should show 69 rows", () => showsRows(page, el("box plot viewer"), 69));
      await session.step(152, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(153, "Then box plot viewer should show 29 rows", () => showsRows(page, el("box plot viewer"), 29));
      await session.step(154, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(155, "And user clears the row selection", () => clearSelection(page));
      await session.step(156, "And user sets \"rowSource\" property of box plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("box plot viewer"), "Filtered"));
      await session.step(157, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(158, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("FilteredSelected — pc plot shows the selected rows that pass the filter panel and its own filter [viewer=pc plot, selected=69, selected and filtered=29]", async () => {
      await session.step(147, "When user sets \"rowSource\" property of pc plot viewer to \"FilteredSelected\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "FilteredSelected"));
      await session.step(148, "Then no rows should be selected", () => noneSelected(page));
      await session.step(149, "And pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
      await session.step(150, "When user selects rows where \"AGE\" is between 42 and 47", () => selectWhereBetween(page, "AGE", 42, 47));
      await session.step(151, "Then pc plot viewer should show 69 rows", () => showsRows(page, el("pc plot viewer"), 69));
      await session.step(152, "When user adds a categorical filter on \"SEX\" keeping \"M\"", () => addCategoricalFilter(page, "SEX", "M"));
      await session.step(153, "Then pc plot viewer should show 29 rows", () => showsRows(page, el("pc plot viewer"), 29));
      await session.step(154, "When user adds a categorical filter on \"SEX\" keeping \"F, M\"", () => addCategoricalFilter(page, "SEX", "F, M"));
      await session.step(155, "And user clears the row selection", () => clearSelection(page));
      await session.step(156, "And user sets \"rowSource\" property of pc plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "Filtered"));
      await session.step(157, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(158, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("CurrentRow — scatter plot shows the current row when its filter keeps it and nothing when it does not [viewer=scatter plot]", async () => {
      await session.step(171, "When user sets \"rowSource\" property of scatter plot viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "CurrentRow"));
      await session.step(172, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(173, "Then scatter plot viewer should show 1 rows", () => showsRows(page, el("scatter plot viewer"), 1));
      await session.step(174, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(175, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(176, "And scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
      await session.step(177, "When user sets \"rowSource\" property of scatter plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "Filtered"));
      await session.step(178, "Then scatter plot viewer should show 519 rows", () => showsRows(page, el("scatter plot viewer"), 519));
      await session.step(179, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("CurrentRow — line chart shows the current row when its filter keeps it and nothing when it does not [viewer=line chart]", async () => {
      await session.step(171, "When user sets \"rowSource\" property of line chart viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("line chart viewer"), "CurrentRow"));
      await session.step(172, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(173, "Then line chart viewer should show 1 rows", () => showsRows(page, el("line chart viewer"), 1));
      await session.step(174, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(175, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(176, "And line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
      await session.step(177, "When user sets \"rowSource\" property of line chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("line chart viewer"), "Filtered"));
      await session.step(178, "Then line chart viewer should show 519 rows", () => showsRows(page, el("line chart viewer"), 519));
      await session.step(179, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("CurrentRow — histogram shows the current row when its filter keeps it and nothing when it does not [viewer=histogram]", async () => {
      await session.step(171, "When user sets \"rowSource\" property of histogram viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("histogram viewer"), "CurrentRow"));
      await session.step(172, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(173, "Then histogram viewer should show 1 rows", () => showsRows(page, el("histogram viewer"), 1));
      await session.step(174, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(175, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(176, "And histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
      await session.step(177, "When user sets \"rowSource\" property of histogram viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("histogram viewer"), "Filtered"));
      await session.step(178, "Then histogram viewer should show 519 rows", () => showsRows(page, el("histogram viewer"), 519));
      await session.step(179, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("CurrentRow — bar chart shows the current row when its filter keeps it and nothing when it does not [viewer=bar chart]", async () => {
      await session.step(171, "When user sets \"rowSource\" property of bar chart viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "CurrentRow"));
      await session.step(172, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(173, "Then bar chart viewer should show 1 rows", () => showsRows(page, el("bar chart viewer"), 1));
      await session.step(174, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(175, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(176, "And bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
      await session.step(177, "When user sets \"rowSource\" property of bar chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "Filtered"));
      await session.step(178, "Then bar chart viewer should show 519 rows", () => showsRows(page, el("bar chart viewer"), 519));
      await session.step(179, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("CurrentRow — pie chart shows the current row when its filter keeps it and nothing when it does not [viewer=pie chart]", async () => {
      await session.step(171, "When user sets \"rowSource\" property of pie chart viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "CurrentRow"));
      await session.step(172, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(173, "Then pie chart viewer should show 1 rows", () => showsRows(page, el("pie chart viewer"), 1));
      await session.step(174, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(175, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(176, "And pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
      await session.step(177, "When user sets \"rowSource\" property of pie chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "Filtered"));
      await session.step(178, "Then pie chart viewer should show 519 rows", () => showsRows(page, el("pie chart viewer"), 519));
      await session.step(179, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("CurrentRow — box plot shows the current row when its filter keeps it and nothing when it does not [viewer=box plot]", async () => {
      await session.step(171, "When user sets \"rowSource\" property of box plot viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("box plot viewer"), "CurrentRow"));
      await session.step(172, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(173, "Then box plot viewer should show 1 rows", () => showsRows(page, el("box plot viewer"), 1));
      await session.step(174, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(175, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(176, "And box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
      await session.step(177, "When user sets \"rowSource\" property of box plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("box plot viewer"), "Filtered"));
      await session.step(178, "Then box plot viewer should show 519 rows", () => showsRows(page, el("box plot viewer"), 519));
      await session.step(179, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("CurrentRow — pc plot shows the current row when its filter keeps it and nothing when it does not [viewer=pc plot]", async () => {
      await session.step(171, "When user sets \"rowSource\" property of pc plot viewer to \"CurrentRow\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "CurrentRow"));
      await session.step(172, "And user makes row 3 current", () => makeRowCurrent(page, 3));
      await session.step(173, "Then pc plot viewer should show 1 rows", () => showsRows(page, el("pc plot viewer"), 1));
      await session.step(174, "When user makes row 1 current", () => makeRowCurrent(page, 1));
      await session.step(175, "Then \"AGE\" of the current row should be \"26\"", () => currentRowValue(page, "AGE", "26"));
      await session.step(176, "And pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
      await session.step(177, "When user sets \"rowSource\" property of pc plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "Filtered"));
      await session.step(178, "Then pc plot viewer should show 519 rows", () => showsRows(page, el("pc plot viewer"), 519));
      await session.step(179, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("MouseOverRow — scatter plot follows the row the pointer is over in the grid, within its filter [viewer=scatter plot]", async () => {
      await session.step(192, "When user sets \"rowSource\" property of scatter plot viewer to \"MouseOverRow\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "MouseOverRow"));
      await session.step(193, "And user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(194, "Then scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
      await session.step(195, "When user hovers over the \"cell 3 of AGE\" area of grid", () => hoverArea(page, "cell 3 of AGE", el("grid")));
      await session.step(196, "Then the mouse-over row of the table should be 3", () => mouseOverRowIs(page, 3));
      await session.step(197, "And scatter plot viewer should show 1 rows", () => showsRows(page, el("scatter plot viewer"), 1));
      await session.step(198, "When user hovers over the \"cell 1 of AGE\" area of grid", () => hoverArea(page, "cell 1 of AGE", el("grid")));
      await session.step(199, "Then the mouse-over row of the table should be 1", () => mouseOverRowIs(page, 1));
      await session.step(200, "And scatter plot viewer should show 0 rows", () => showsRows(page, el("scatter plot viewer"), 0));
      await session.step(201, "When user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(202, "And user sets \"rowSource\" property of scatter plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("scatter plot viewer"), "Filtered"));
      await session.step(203, "Then scatter plot viewer should show 519 rows", () => showsRows(page, el("scatter plot viewer"), 519));
      await session.step(204, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("MouseOverRow — line chart follows the row the pointer is over in the grid, within its filter [viewer=line chart]", async () => {
      await session.step(192, "When user sets \"rowSource\" property of line chart viewer to \"MouseOverRow\"", () => setProperty(page, "rowSource", el("line chart viewer"), "MouseOverRow"));
      await session.step(193, "And user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(194, "Then line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
      await session.step(195, "When user hovers over the \"cell 3 of AGE\" area of grid", () => hoverArea(page, "cell 3 of AGE", el("grid")));
      await session.step(196, "Then the mouse-over row of the table should be 3", () => mouseOverRowIs(page, 3));
      await session.step(197, "And line chart viewer should show 1 rows", () => showsRows(page, el("line chart viewer"), 1));
      await session.step(198, "When user hovers over the \"cell 1 of AGE\" area of grid", () => hoverArea(page, "cell 1 of AGE", el("grid")));
      await session.step(199, "Then the mouse-over row of the table should be 1", () => mouseOverRowIs(page, 1));
      await session.step(200, "And line chart viewer should show 0 rows", () => showsRows(page, el("line chart viewer"), 0));
      await session.step(201, "When user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(202, "And user sets \"rowSource\" property of line chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("line chart viewer"), "Filtered"));
      await session.step(203, "Then line chart viewer should show 519 rows", () => showsRows(page, el("line chart viewer"), 519));
      await session.step(204, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("MouseOverRow — histogram follows the row the pointer is over in the grid, within its filter [viewer=histogram]", async () => {
      await session.step(192, "When user sets \"rowSource\" property of histogram viewer to \"MouseOverRow\"", () => setProperty(page, "rowSource", el("histogram viewer"), "MouseOverRow"));
      await session.step(193, "And user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(194, "Then histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
      await session.step(195, "When user hovers over the \"cell 3 of AGE\" area of grid", () => hoverArea(page, "cell 3 of AGE", el("grid")));
      await session.step(196, "Then the mouse-over row of the table should be 3", () => mouseOverRowIs(page, 3));
      await session.step(197, "And histogram viewer should show 1 rows", () => showsRows(page, el("histogram viewer"), 1));
      await session.step(198, "When user hovers over the \"cell 1 of AGE\" area of grid", () => hoverArea(page, "cell 1 of AGE", el("grid")));
      await session.step(199, "Then the mouse-over row of the table should be 1", () => mouseOverRowIs(page, 1));
      await session.step(200, "And histogram viewer should show 0 rows", () => showsRows(page, el("histogram viewer"), 0));
      await session.step(201, "When user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(202, "And user sets \"rowSource\" property of histogram viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("histogram viewer"), "Filtered"));
      await session.step(203, "Then histogram viewer should show 519 rows", () => showsRows(page, el("histogram viewer"), 519));
      await session.step(204, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("MouseOverRow — bar chart follows the row the pointer is over in the grid, within its filter [viewer=bar chart]", async () => {
      await session.step(192, "When user sets \"rowSource\" property of bar chart viewer to \"MouseOverRow\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "MouseOverRow"));
      await session.step(193, "And user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(194, "Then bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
      await session.step(195, "When user hovers over the \"cell 3 of AGE\" area of grid", () => hoverArea(page, "cell 3 of AGE", el("grid")));
      await session.step(196, "Then the mouse-over row of the table should be 3", () => mouseOverRowIs(page, 3));
      await session.step(197, "And bar chart viewer should show 1 rows", () => showsRows(page, el("bar chart viewer"), 1));
      await session.step(198, "When user hovers over the \"cell 1 of AGE\" area of grid", () => hoverArea(page, "cell 1 of AGE", el("grid")));
      await session.step(199, "Then the mouse-over row of the table should be 1", () => mouseOverRowIs(page, 1));
      await session.step(200, "And bar chart viewer should show 0 rows", () => showsRows(page, el("bar chart viewer"), 0));
      await session.step(201, "When user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(202, "And user sets \"rowSource\" property of bar chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("bar chart viewer"), "Filtered"));
      await session.step(203, "Then bar chart viewer should show 519 rows", () => showsRows(page, el("bar chart viewer"), 519));
      await session.step(204, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("MouseOverRow — pie chart follows the row the pointer is over in the grid, within its filter [viewer=pie chart]", async () => {
      await session.step(192, "When user sets \"rowSource\" property of pie chart viewer to \"MouseOverRow\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "MouseOverRow"));
      await session.step(193, "And user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(194, "Then pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
      await session.step(195, "When user hovers over the \"cell 3 of AGE\" area of grid", () => hoverArea(page, "cell 3 of AGE", el("grid")));
      await session.step(196, "Then the mouse-over row of the table should be 3", () => mouseOverRowIs(page, 3));
      await session.step(197, "And pie chart viewer should show 1 rows", () => showsRows(page, el("pie chart viewer"), 1));
      await session.step(198, "When user hovers over the \"cell 1 of AGE\" area of grid", () => hoverArea(page, "cell 1 of AGE", el("grid")));
      await session.step(199, "Then the mouse-over row of the table should be 1", () => mouseOverRowIs(page, 1));
      await session.step(200, "And pie chart viewer should show 0 rows", () => showsRows(page, el("pie chart viewer"), 0));
      await session.step(201, "When user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(202, "And user sets \"rowSource\" property of pie chart viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pie chart viewer"), "Filtered"));
      await session.step(203, "Then pie chart viewer should show 519 rows", () => showsRows(page, el("pie chart viewer"), 519));
      await session.step(204, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("MouseOverRow — box plot follows the row the pointer is over in the grid, within its filter [viewer=box plot]", async () => {
      await session.step(192, "When user sets \"rowSource\" property of box plot viewer to \"MouseOverRow\"", () => setProperty(page, "rowSource", el("box plot viewer"), "MouseOverRow"));
      await session.step(193, "And user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(194, "Then box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
      await session.step(195, "When user hovers over the \"cell 3 of AGE\" area of grid", () => hoverArea(page, "cell 3 of AGE", el("grid")));
      await session.step(196, "Then the mouse-over row of the table should be 3", () => mouseOverRowIs(page, 3));
      await session.step(197, "And box plot viewer should show 1 rows", () => showsRows(page, el("box plot viewer"), 1));
      await session.step(198, "When user hovers over the \"cell 1 of AGE\" area of grid", () => hoverArea(page, "cell 1 of AGE", el("grid")));
      await session.step(199, "Then the mouse-over row of the table should be 1", () => mouseOverRowIs(page, 1));
      await session.step(200, "And box plot viewer should show 0 rows", () => showsRows(page, el("box plot viewer"), 0));
      await session.step(201, "When user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(202, "And user sets \"rowSource\" property of box plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("box plot viewer"), "Filtered"));
      await session.step(203, "Then box plot viewer should show 519 rows", () => showsRows(page, el("box plot viewer"), 519));
      await session.step(204, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("MouseOverRow — pc plot follows the row the pointer is over in the grid, within its filter [viewer=pc plot]", async () => {
      await session.step(192, "When user sets \"rowSource\" property of pc plot viewer to \"MouseOverRow\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "MouseOverRow"));
      await session.step(193, "And user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(194, "Then pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
      await session.step(195, "When user hovers over the \"cell 3 of AGE\" area of grid", () => hoverArea(page, "cell 3 of AGE", el("grid")));
      await session.step(196, "Then the mouse-over row of the table should be 3", () => mouseOverRowIs(page, 3));
      await session.step(197, "And pc plot viewer should show 1 rows", () => showsRows(page, el("pc plot viewer"), 1));
      await session.step(198, "When user hovers over the \"cell 1 of AGE\" area of grid", () => hoverArea(page, "cell 1 of AGE", el("grid")));
      await session.step(199, "Then the mouse-over row of the table should be 1", () => mouseOverRowIs(page, 1));
      await session.step(200, "And pc plot viewer should show 0 rows", () => showsRows(page, el("pc plot viewer"), 0));
      await session.step(201, "When user moves the pointer away from grid", () => pointerAway(page, el("grid")));
      await session.step(202, "And user sets \"rowSource\" property of pc plot viewer to \"Filtered\"", () => setProperty(page, "rowSource", el("pc plot viewer"), "Filtered"));
      await session.step(203, "Then pc plot viewer should show 519 rows", () => showsRows(page, el("pc plot viewer"), 519));
      await session.step(204, "And no errors should have been logged", () => noErrors(page));
    });
    run.finish();
  });
});
