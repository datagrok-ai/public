/* eslint-disable max-len */
/* eslint-disable comma-spacing */
/* eslint-disable quotes */
/* ---
generated: features/viewers/legend/legend-filtering.feature
generator: @datagrok-libraries/bdd — do not edit; run `grok-bdd compile` to regenerate
sub_features_covered: [viewers.legend]
--- */
import {test} from '@playwright/test';
import '../../../bindings/spaces.js';
import '../../../bindings/tile-viewer.js';
import '../../../bindings/trellis-plot.js';
import '@datagrok-libraries/bdd/bindings/common/kinds';
import '@datagrok-libraries/bdd/bindings/common/parameter-types';
import '@datagrok-libraries/bdd/bindings/platform/datasets';
import '@datagrok-libraries/bdd/bindings/platform/elements';
import {addCardFor} from '../../../bindings/filter-panel.js';
import {loggedIn} from '@datagrok-libraries/bdd/bindings/common/session';
import {clickOn, hoverOver, shouldBe} from '@datagrok-libraries/bdd/bindings/common/steps';
import {addCalculated, addRangeFilter, clearSelection, filterPasses, filterPassesAll, filterPassesFewer, openEmptyFilterPanel, selectWhereOneOf, selectedRowCount} from '@datagrok-libraries/bdd/bindings/platform/data';
import {openDataset} from '@datagrok-libraries/bdd/bindings/platform/steps';
import {addViewer, addViewerWith, clickArea, dragBetweenAreasHolding, legendLists, loadLayout, noErrors, pickFromContextMenu, propertyShouldBe, saveLayoutToServer, setProperties, setProperty} from '@datagrok-libraries/bdd/bindings/tiers/viewers/steps';
import {doubleClickEmptySpace, setInnerProperty} from '@datagrok-libraries/bdd/bindings/tiers/viewers/widgets';
import {ds, el, feature, journey} from '@datagrok-libraries/bdd/runtime';

test.describe("Legends follow the rows that are left", () => {
  const session = feature(test, "features/viewers/legend/legend-filtering.feature", import.meta.url);
  test("Legends follow the rows that are left", {tag: ["@journey", "@viewers", "@realizes:viewers.legend", "@known-failure"]}, async ({browser}) => {
    const page = await session.page(browser);
    const run = journey(test, 15, page);
    await session.step(60, "Given user is logged in", () => loggedIn(page));
    await session.step(61, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(62, "And user adds a scatter plot viewer with:", () => addViewerWith(page, "scatter plot", [["xColumnName","WEIGHT"],["yColumnName","HEIGHT"],["colorColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]));
    await session.step(68, "And user adds a histogram viewer with:", () => addViewerWith(page, "histogram", [["valueColumnName","AGE"],["splitColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]));
    await session.step(73, "And user adds a line chart viewer with:", () => addViewerWith(page, "line chart", [["xColumnName","AGE"],["yColumnNames","WEIGHT"],["splitColumnNames","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]));
    await session.step(79, "And user adds a bar chart viewer with:", () => addViewerWith(page, "bar chart", [["splitColumnName","RACE"],["stackColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]));
    await session.step(84, "And user adds a pie chart viewer with:", () => addViewerWith(page, "pie chart", [["categoryColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]));
    await session.step(88, "And user adds a trellis plot viewer with:", () => addViewerWith(page, "trellis plot", [["xColumnNames","RACE"],["yColumnNames",""],["Viewer Type","Scatter plot"],["Legend Visibility","Always"],["Legend Position","Right"]]));
    await session.step(94, "And user sets \"colorColumnName\" inner property of trellis plot viewer to \"RACE\"", () => setInnerProperty(page, "colorColumnName", el("trellis plot viewer"), "RACE"));
    await session.step(95, "And user adds a box plot viewer with:", () => addViewerWith(page, "box plot", [["categoryColumnNames","RACE"],["valueColumnName","AGE"],["markerColorColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]));
    await session.step(101, "And user opens an empty filter panel", () => openEmptyFilterPanel(page));
    await session.step(102, "And user adds a card for \"RACE\" to the filter panel", () => addCardFor(page, "RACE"));
    await session.step(103, "Then all rows should pass the filter", () => filterPassesAll(page));
    await session.step(104, "And the legend of scatter plot viewer should list 4 items", () => legendLists(page, el("scatter plot viewer"), 4));
    await session.step(105, "And the legend of box plot viewer should list 4 items", () => legendLists(page, el("box plot viewer"), 4));
    await run.scenario("A range filter and a category filter on the panel narrow every legend", async () => {
      await session.step(108, "When user adds a range filter on \"WEIGHT\" from 100 to 170", () => addRangeFilter(page, "WEIGHT", 100, 170));
      await session.step(109, "Then 143 rows should pass the filter", () => filterPasses(page, 143));
      await session.step(110, "And the legend of scatter plot viewer should list 3 items", () => legendLists(page, el("scatter plot viewer"), 3));
      await session.step(111, "And the legend of last histogram viewer should list 3 items", () => legendLists(page, el("last histogram viewer"), 3));
      await session.step(112, "And the legend of line chart viewer should list 3 items", () => legendLists(page, el("line chart viewer"), 3));
      await session.step(113, "And the legend of bar chart viewer should list 3 items", () => legendLists(page, el("bar chart viewer"), 3));
      await session.step(114, "And the legend of pie chart viewer should list 3 items", () => legendLists(page, el("pie chart viewer"), 3));
      await session.step(115, "And the legend of trellis plot viewer should list 3 items", () => legendLists(page, el("trellis plot viewer"), 3));
      await session.step(116, "And the legend of box plot viewer should list 3 items", () => legendLists(page, el("box plot viewer"), 3));
      await session.step(117, "And \"Asian\" legend item in legend of pie chart viewer should be absent", () => shouldBe(page, el("\"Asian\" legend item in legend of pie chart viewer"), "absent"));
      await session.step(118, "And \"Black\" legend item in legend of pie chart viewer should be visible", () => shouldBe(page, el("\"Black\" legend item in legend of pie chart viewer"), "visible"));
      await session.step(119, "When user clicks on the \"category Black of RACE\" area of filter panel", () => clickArea(page, "category Black of RACE", el("filter panel")));
      await session.step(120, "And user clicks on the \"checkbox Other of RACE\" area of filter panel", () => clickArea(page, "checkbox Other of RACE", el("filter panel")));
      await session.step(121, "Then 12 rows should pass the filter", () => filterPasses(page, 12));
      await session.step(122, "And the legend of scatter plot viewer should list 2 items", () => legendLists(page, el("scatter plot viewer"), 2));
      await session.step(123, "And the legend of last histogram viewer should list 2 items", () => legendLists(page, el("last histogram viewer"), 2));
      await session.step(124, "And the legend of line chart viewer should list 2 items", () => legendLists(page, el("line chart viewer"), 2));
      await session.step(125, "And the legend of bar chart viewer should list 2 items", () => legendLists(page, el("bar chart viewer"), 2));
      await session.step(126, "And the legend of pie chart viewer should list 2 items", () => legendLists(page, el("pie chart viewer"), 2));
      await session.step(127, "And the legend of trellis plot viewer should list 2 items", () => legendLists(page, el("trellis plot viewer"), 2));
      await session.step(128, "And the legend of box plot viewer should list 2 items", () => legendLists(page, el("box plot viewer"), 2));
      await session.step(129, "And \"Caucasian\" legend item in legend of box plot viewer should be absent", () => shouldBe(page, el("\"Caucasian\" legend item in legend of box plot viewer"), "absent"));
      await session.step(130, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The panel's reset gives every legend all four races back", async () => {
      await session.step(133, "Then 12 rows should pass the filter", () => filterPasses(page, 12));
      await session.step(134, "When user hovers over filter panel", () => hoverOver(page, el("filter panel")));
      await session.step(135, "And user clicks on reset icon of filter panel", () => clickOn(page, el("reset icon of filter panel")));
      await session.step(136, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(137, "And the legend of scatter plot viewer should list 4 items", () => legendLists(page, el("scatter plot viewer"), 4));
      await session.step(138, "And the legend of last histogram viewer should list 4 items", () => legendLists(page, el("last histogram viewer"), 4));
      await session.step(139, "And the legend of line chart viewer should list 4 items", () => legendLists(page, el("line chart viewer"), 4));
      await session.step(140, "And the legend of bar chart viewer should list 4 items", () => legendLists(page, el("bar chart viewer"), 4));
      await session.step(141, "And the legend of pie chart viewer should list 4 items", () => legendLists(page, el("pie chart viewer"), 4));
      await session.step(142, "And the legend of trellis plot viewer should list 4 items", () => legendLists(page, el("trellis plot viewer"), 4));
      await session.step(143, "And the legend of box plot viewer should list 4 items", () => legendLists(page, el("box plot viewer"), 4));
      await session.step(144, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The scatter plot's own Filter narrows its legend and no other", async () => {
      await session.step(147, "When user sets \"Filter\" property of scatter plot viewer to '${RACE} in [\"Asian\", \"Black\"]'", () => setProperty(page, "Filter", el("scatter plot viewer"), "${RACE} in [\"Asian\", \"Black\"]"));
      await session.step(148, "Then the legend of scatter plot viewer should list 2 items", () => legendLists(page, el("scatter plot viewer"), 2));
      await session.step(149, "And \"Caucasian\" legend item in legend of scatter plot viewer should be absent", () => shouldBe(page, el("\"Caucasian\" legend item in legend of scatter plot viewer"), "absent"));
      await session.step(150, "And all rows should pass the filter", () => filterPassesAll(page));
      await session.step(151, "And the legend of last histogram viewer should list 4 items", () => legendLists(page, el("last histogram viewer"), 4));
      await session.step(152, "And the legend of line chart viewer should list 4 items", () => legendLists(page, el("line chart viewer"), 4));
      await session.step(153, "And the legend of bar chart viewer should list 4 items", () => legendLists(page, el("bar chart viewer"), 4));
      await session.step(154, "And the legend of pie chart viewer should list 4 items", () => legendLists(page, el("pie chart viewer"), 4));
      await session.step(155, "And the legend of trellis plot viewer should list 4 items", () => legendLists(page, el("trellis plot viewer"), 4));
      await session.step(156, "And the legend of box plot viewer should list 4 items", () => legendLists(page, el("box plot viewer"), 4));
      await session.step(157, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The viewer's Filter and the panel's filter compose", async () => {
      await session.step(160, "When user adds a range filter on \"WEIGHT\" from 100 to 170", () => addRangeFilter(page, "WEIGHT", 100, 170));
      await session.step(161, "Then 143 rows should pass the filter", () => filterPasses(page, 143));
      await session.step(162, "And the legend of scatter plot viewer should list 1 item", () => legendLists(page, el("scatter plot viewer"), 1));
      await session.step(163, "And \"Black\" legend item in legend of scatter plot viewer should be visible", () => shouldBe(page, el("\"Black\" legend item in legend of scatter plot viewer"), "visible"));
      await session.step(164, "And \"Asian\" legend item in legend of scatter plot viewer should be absent", () => shouldBe(page, el("\"Asian\" legend item in legend of scatter plot viewer"), "absent"));
      await session.step(165, "And the legend of last histogram viewer should list 3 items", () => legendLists(page, el("last histogram viewer"), 3));
      await session.step(166, "When user hovers over filter panel", () => hoverOver(page, el("filter panel")));
      await session.step(167, "And user clicks on reset icon of filter panel", () => clickOn(page, el("reset icon of filter panel")));
      await session.step(168, "And user sets \"Filter\" property of scatter plot viewer to \"\"", () => setProperty(page, "Filter", el("scatter plot viewer"), ""));
      await session.step(169, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(170, "And the legend of scatter plot viewer should list 4 items", () => legendLists(page, el("scatter plot viewer"), 4));
      await session.step(171, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("A zoom-filter on the scatter plot drops the races with no point in the box", async () => {
      await session.step(174, "When user sets properties of scatter plot viewer:", () => setProperties(page, el("scatter plot viewer"), [["xColumnName","AGE"],["yColumnName","WEIGHT"],["Zoom And Filter","filter by zoom"]]));
      await session.step(178, "And user drags from the \"marker of row 5\" area to the \"marker of row 20\" area of scatter plot viewer holding Alt", () => dragBetweenAreasHolding(page, "marker of row 5", "marker of row 20", el("scatter plot viewer"), "Alt"));
      await session.step(179, "Then the legend of scatter plot viewer should list 2 items", () => legendLists(page, el("scatter plot viewer"), 2));
      await session.step(180, "And \"Caucasian\" legend item in legend of scatter plot viewer should be visible", () => shouldBe(page, el("\"Caucasian\" legend item in legend of scatter plot viewer"), "visible"));
      await session.step(181, "And \"Asian\" legend item in legend of scatter plot viewer should be absent", () => shouldBe(page, el("\"Asian\" legend item in legend of scatter plot viewer"), "absent"));
      await session.step(182, "And fewer than 30 rows should pass the filter", () => filterPassesFewer(page, 30));
      await session.step(183, "And the legend of pie chart viewer should list 2 items", () => legendLists(page, el("pie chart viewer"), 2));
      await session.step(184, "When user double-clicks on empty plot space of scatter plot viewer", () => doubleClickEmptySpace(page, el("scatter plot viewer")));
      await session.step(185, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(186, "And the legend of scatter plot viewer should list 4 items", () => legendLists(page, el("scatter plot viewer"), 4));
      await session.step(187, "When user sets properties of scatter plot viewer:", () => setProperties(page, el("scatter plot viewer"), [["Zoom And Filter","no action"],["xColumnName","WEIGHT"],["yColumnName","HEIGHT"]]));
      await session.step(191, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("A click on a bar with On Click Filter leaves only its race in the other legends", async () => {
      await session.step(194, "When user picks \"On Click > Filter\" from the context menu of bar chart viewer", () => pickFromContextMenu(page, "On Click > Filter", el("bar chart viewer")));
      await session.step(195, "Then \"On Click\" property of bar chart viewer should be \"Filter\"", () => propertyShouldBe(page, "On Click", el("bar chart viewer"), "Filter"));
      await session.step(196, "And \"Row Source\" property of bar chart viewer should be \"All\"", () => propertyShouldBe(page, "Row Source", el("bar chart viewer"), "All"));
      await session.step(197, "And the legend of bar chart viewer should list 4 items", () => legendLists(page, el("bar chart viewer"), 4));
      await session.step(198, "When user clicks on the \"bar Black\" area of bar chart viewer", () => clickArea(page, "bar Black", el("bar chart viewer")));
      await session.step(199, "Then 27 rows should pass the filter", () => filterPasses(page, 27));
      await session.step(200, "And the legend of bar chart viewer should list 4 items", () => legendLists(page, el("bar chart viewer"), 4));
      await session.step(201, "And \"Black\" legend item in legend of bar chart viewer should be visible", () => shouldBe(page, el("\"Black\" legend item in legend of bar chart viewer"), "visible"));
      await session.step(202, "And the legend of scatter plot viewer should list 1 item", () => legendLists(page, el("scatter plot viewer"), 1));
      await session.step(203, "And the legend of box plot viewer should list 1 item", () => legendLists(page, el("box plot viewer"), 1));
      await session.step(204, "When user hovers over filter panel", () => hoverOver(page, el("filter panel")));
      await session.step(205, "And user clicks on reset icon of filter panel", () => clickOn(page, el("reset icon of filter panel")));
      await session.step(206, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(207, "And the legend of bar chart viewer should list 4 items", () => legendLists(page, el("bar chart viewer"), 4));
      await session.step(208, "When user sets properties of bar chart viewer:", () => setProperties(page, el("bar chart viewer"), [["On Click","None"],["Row Source","Filtered"]]));
      await session.step(211, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("A click on a wedge with On Click Filter leaves only its race in the other legends", async () => {
      await session.step(214, "When user sets \"On Click\" property of pie chart viewer to \"Filter\"", () => setProperty(page, "On Click", el("pie chart viewer"), "Filter"));
      await session.step(215, "And user clicks on the 'slice \"Other\"' area of pie chart viewer", () => clickArea(page, "slice \"Other\"", el("pie chart viewer")));
      await session.step(216, "Then 62 rows should pass the filter", () => filterPasses(page, 62));
      await session.step(217, "And the legend of pie chart viewer should list 1 item", () => legendLists(page, el("pie chart viewer"), 1));
      await session.step(218, "And \"Other\" legend item in legend of pie chart viewer should be visible", () => shouldBe(page, el("\"Other\" legend item in legend of pie chart viewer"), "visible"));
      await session.step(219, "And the legend of last histogram viewer should list 1 item", () => legendLists(page, el("last histogram viewer"), 1));
      await session.step(220, "And the legend of line chart viewer should list 1 item", () => legendLists(page, el("line chart viewer"), 1));
      await session.step(221, "When user hovers over filter panel", () => hoverOver(page, el("filter panel")));
      await session.step(222, "And user clicks on reset icon of filter panel", () => clickOn(page, el("reset icon of filter panel")));
      await session.step(223, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(224, "And the legend of pie chart viewer should list 4 items", () => legendLists(page, el("pie chart viewer"), 4));
      await session.step(225, "When user sets properties of pie chart viewer:", () => setProperties(page, el("pie chart viewer"), [["On Click","None"],["Row Source","Filtered"]]));
      await session.step(228, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("A click on a trellis cell with On Click Filter leaves only its race in the other legends", async () => {
      await session.step(231, "When user sets \"On Click\" property of trellis plot viewer to \"Filter\"", () => setProperty(page, "On Click", el("trellis plot viewer"), "Filter"));
      await session.step(232, "And user clicks on the \"cell body Asian\" area of trellis plot viewer", () => clickArea(page, "cell body Asian", el("trellis plot viewer")));
      await session.step(233, "Then 15 rows should pass the filter", () => filterPasses(page, 15));
      await session.step(234, "And \"Row Source\" property of trellis plot viewer should be \"All\"", () => propertyShouldBe(page, "Row Source", el("trellis plot viewer"), "All"));
      await session.step(235, "And the legend of trellis plot viewer should list 4 items", () => legendLists(page, el("trellis plot viewer"), 4));
      await session.step(236, "And the legend of pie chart viewer should list 1 item", () => legendLists(page, el("pie chart viewer"), 1));
      await session.step(237, "And \"Asian\" legend item in legend of pie chart viewer should be visible", () => shouldBe(page, el("\"Asian\" legend item in legend of pie chart viewer"), "visible"));
      await session.step(238, "And the legend of line chart viewer should list 1 item", () => legendLists(page, el("line chart viewer"), 1));
      await session.step(239, "And the legend of box plot viewer should list 1 item", () => legendLists(page, el("box plot viewer"), 1));
      await session.step(240, "When user hovers over filter panel", () => hoverOver(page, el("filter panel")));
      await session.step(241, "And user clicks on reset icon of filter panel", () => clickOn(page, el("reset icon of filter panel")));
      await session.step(242, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(243, "And the legend of pie chart viewer should list 4 items", () => legendLists(page, el("pie chart viewer"), 4));
      await session.step(244, "When user sets properties of trellis plot viewer:", () => setProperties(page, el("trellis plot viewer"), [["On Click","None"],["Row Source","Filtered"]]));
      await session.step(247, "Then the legend of trellis plot viewer should list 4 items", () => legendLists(page, el("trellis plot viewer"), 4));
      await session.step(248, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The scatter plot's Row Source decides which rows its legend lists", async () => {
      await session.step(251, "Given \"xColumnName\" property of scatter plot viewer should be \"WEIGHT\"", () => propertyShouldBe(page, "xColumnName", el("scatter plot viewer"), "WEIGHT"));
      await session.step(252, "When user clicks on the \"category Asian of RACE\" area of filter panel", () => clickArea(page, "category Asian of RACE", el("filter panel")));
      await session.step(253, "And user clicks on the \"checkbox Black of RACE\" area of filter panel", () => clickArea(page, "checkbox Black of RACE", el("filter panel")));
      await session.step(254, "And user clicks on the \"checkbox Other of RACE\" area of filter panel", () => clickArea(page, "checkbox Other of RACE", el("filter panel")));
      await session.step(255, "And user selects rows where \"RACE\" is one of \"Black, Caucasian\"", () => selectWhereOneOf(page, "RACE", "Black, Caucasian"));
      await session.step(256, "Then 104 rows should pass the filter", () => filterPasses(page, 104));
      await session.step(257, "And 923 rows should be selected", () => selectedRowCount(page, 923));
      await session.step(258, "When user sets \"Row Source\" property of scatter plot viewer to \"All\"", () => setProperty(page, "Row Source", el("scatter plot viewer"), "All"));
      await session.step(259, "Then the legend of scatter plot viewer should list 4 items", () => legendLists(page, el("scatter plot viewer"), 4));
      await session.step(260, "When user sets \"Row Source\" property of scatter plot viewer to \"Filtered\"", () => setProperty(page, "Row Source", el("scatter plot viewer"), "Filtered"));
      await session.step(261, "Then the legend of scatter plot viewer should list 3 items", () => legendLists(page, el("scatter plot viewer"), 3));
      await session.step(262, "And \"Caucasian\" legend item in legend of scatter plot viewer should be absent", () => shouldBe(page, el("\"Caucasian\" legend item in legend of scatter plot viewer"), "absent"));
      await session.step(263, "When user sets \"Row Source\" property of scatter plot viewer to \"FilteredSelected\"", () => setProperty(page, "Row Source", el("scatter plot viewer"), "FilteredSelected"));
      await session.step(264, "Then the legend of scatter plot viewer should list 1 item", () => legendLists(page, el("scatter plot viewer"), 1));
      await session.step(265, "And \"Black\" legend item in legend of scatter plot viewer should be visible", () => shouldBe(page, el("\"Black\" legend item in legend of scatter plot viewer"), "visible"));
      await session.step(266, "When user sets \"Row Source\" property of scatter plot viewer to \"Selected\"", () => setProperty(page, "Row Source", el("scatter plot viewer"), "Selected"));
      await session.step(267, "Then the legend of scatter plot viewer should list 2 items", () => legendLists(page, el("scatter plot viewer"), 2));
      await session.step(268, "And \"Caucasian\" legend item in legend of scatter plot viewer should be visible", () => shouldBe(page, el("\"Caucasian\" legend item in legend of scatter plot viewer"), "visible"));
      await session.step(269, "And \"Asian\" legend item in legend of scatter plot viewer should be absent", () => shouldBe(page, el("\"Asian\" legend item in legend of scatter plot viewer"), "absent"));
      await session.step(270, "When user sets \"Row Source\" property of scatter plot viewer to \"Filtered\"", () => setProperty(page, "Row Source", el("scatter plot viewer"), "Filtered"));
      await session.step(271, "And user clears the row selection", () => clearSelection(page));
      await session.step(272, "And user hovers over filter panel", () => hoverOver(page, el("filter panel")));
      await session.step(273, "And user clicks on reset icon of filter panel", () => clickOn(page, el("reset icon of filter panel")));
      await session.step(274, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(275, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("A stacked bar chart with Include Nulls off lists no category the panel took away", async () => {
      await session.step(278, "When user adds a calculated column \"DIS_POP but Other\" with formula \"if(${RACE}=='Other', null, ${DIS_POP})\"", () => addCalculated(page, "DIS_POP but Other", "if(${RACE}=='Other', null, ${DIS_POP})"));
      await session.step(279, "And user sets properties of bar chart viewer:", () => setProperties(page, el("bar chart viewer"), [["valueColumnName","USUBJID"],["valueAggrType","count"],["splitColumnName","RACE"],["stackColumnName","DIS_POP but Other"],["Include Nulls","true"]]));
      await session.step(285, "Then the legend of bar chart viewer should list 7 items", () => legendLists(page, el("bar chart viewer"), 7));
      await session.step(286, "And \"(no value)\" legend item in legend of bar chart viewer should be visible", () => shouldBe(page, el("\"(no value)\" legend item in legend of bar chart viewer"), "visible"));
      await session.step(287, "When user sets \"Include Nulls\" property of bar chart viewer to \"false\"", () => setProperty(page, "Include Nulls", el("bar chart viewer"), "false"));
      await session.step(288, "Then the legend of bar chart viewer should list 6 items", () => legendLists(page, el("bar chart viewer"), 6));
      await session.step(289, "And \"(no value)\" legend item in legend of bar chart viewer should be absent", () => shouldBe(page, el("\"(no value)\" legend item in legend of bar chart viewer"), "absent"));
      await session.step(290, "When user adds a card for \"DIS_POP but Other\" to the filter panel", () => addCardFor(page, "DIS_POP but Other"));
      await session.step(291, "And user clicks on the \"category RA of DIS_POP but Other\" area of filter panel", () => clickArea(page, "category RA of DIS_POP but Other", el("filter panel")));
      await session.step(292, "And user clicks on the \"checkbox Psoriasis of DIS_POP but Other\" area of filter panel", () => clickArea(page, "checkbox Psoriasis of DIS_POP but Other", el("filter panel")));
      await session.step(293, "And user clicks on the \"checkbox UC of DIS_POP but Other\" area of filter panel", () => clickArea(page, "checkbox UC of DIS_POP but Other", el("filter panel")));
      await session.step(294, "Then fewer than 761 rows should pass the filter", () => filterPassesFewer(page, 761));
      await session.step(295, "And the legend of bar chart viewer should list 3 items", () => legendLists(page, el("bar chart viewer"), 3));
      await session.step(296, "And \"Indigestion\" legend item in legend of bar chart viewer should be absent", () => shouldBe(page, el("\"Indigestion\" legend item in legend of bar chart viewer"), "absent"));
      await session.step(297, "And \"(no value)\" legend item in legend of bar chart viewer should be absent", () => shouldBe(page, el("\"(no value)\" legend item in legend of bar chart viewer"), "absent"));
      await session.step(298, "And \"RA\" legend item in legend of bar chart viewer should be visible", () => shouldBe(page, el("\"RA\" legend item in legend of bar chart viewer"), "visible"));
      await session.step(299, "When user hovers over filter panel", () => hoverOver(page, el("filter panel")));
      await session.step(300, "And user clicks on reset icon of filter panel", () => clickOn(page, el("reset icon of filter panel")));
      await session.step(301, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(302, "And the legend of bar chart viewer should list 6 items", () => legendLists(page, el("bar chart viewer"), 6));
      await session.step(303, "When user sets properties of bar chart viewer:", () => setProperties(page, el("bar chart viewer"), [["stackColumnName","RACE"],["Include Nulls","true"]]));
      await session.step(306, "Then the legend of bar chart viewer should list 4 items", () => legendLists(page, el("bar chart viewer"), 4));
      await session.step(307, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The panel's filter and the legends it left come back from a saved layout", async () => {
      await session.step(310, "When user clicks on close icon of last histogram viewer", () => clickOn(page, el("close icon of last histogram viewer")));
      await session.step(311, "And user adds a range filter on \"WEIGHT\" from 100 to 170", () => addRangeFilter(page, "WEIGHT", 100, 170));
      await session.step(312, "And user clicks on the \"category Black of RACE\" area of filter panel", () => clickArea(page, "category Black of RACE", el("filter panel")));
      await session.step(313, "And user clicks on the \"checkbox Other of RACE\" area of filter panel", () => clickArea(page, "checkbox Other of RACE", el("filter panel")));
      await session.step(314, "Then 12 rows should pass the filter", () => filterPasses(page, 12));
      await session.step(315, "And the legend of pie chart viewer should list 2 items", () => legendLists(page, el("pie chart viewer"), 2));
      await session.step(316, "When user saves the layout of the current table view to the server", () => saveLayoutToServer(page));
      await session.step(317, "And user hovers over filter panel", () => hoverOver(page, el("filter panel")));
      await session.step(318, "And user clicks on reset icon of filter panel", () => clickOn(page, el("reset icon of filter panel")));
      await session.step(319, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(320, "And the legend of pie chart viewer should list 4 items", () => legendLists(page, el("pie chart viewer"), 4));
      await session.step(321, "When user loads the saved layout", () => loadLayout(page));
      await session.step(322, "Then 12 rows should pass the filter", () => filterPasses(page, 12));
      await session.step(323, "And the legend of scatter plot viewer should list 2 items", () => legendLists(page, el("scatter plot viewer"), 2));
      await session.step(324, "And the legend of line chart viewer should list 2 items", () => legendLists(page, el("line chart viewer"), 2));
      await session.step(325, "And the legend of bar chart viewer should list 2 items", () => legendLists(page, el("bar chart viewer"), 2));
      await session.step(326, "And the legend of pie chart viewer should list 2 items", () => legendLists(page, el("pie chart viewer"), 2));
      await session.step(327, "And the legend of trellis plot viewer should list 2 items", () => legendLists(page, el("trellis plot viewer"), 2));
      await session.step(328, "And the legend of box plot viewer should list 2 items", () => legendLists(page, el("box plot viewer"), 2));
      await session.step(329, "And \"Caucasian\" legend item in legend of scatter plot viewer should be absent", () => shouldBe(page, el("\"Caucasian\" legend item in legend of scatter plot viewer"), "absent"));
      await session.step(330, "And \"xColumnName\" property of scatter plot viewer should be \"WEIGHT\"", () => propertyShouldBe(page, "xColumnName", el("scatter plot viewer"), "WEIGHT"));
      await session.step(331, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("A layout brings the panel's filter back and the filter a click set does not come back", async () => {
      await session.step(334, "Then 12 rows should pass the filter", () => filterPasses(page, 12));
      await session.step(335, "When user hovers over filter panel", () => hoverOver(page, el("filter panel")));
      await session.step(336, "And user clicks on reset icon of filter panel", () => clickOn(page, el("reset icon of filter panel")));
      await session.step(337, "And user adds a range filter on \"WEIGHT\" from 100 to 170", () => addRangeFilter(page, "WEIGHT", 100, 170));
      await session.step(338, "Then 143 rows should pass the filter", () => filterPasses(page, 143));
      await session.step(339, "When user picks \"On Click > Filter\" from the context menu of bar chart viewer", () => pickFromContextMenu(page, "On Click > Filter", el("bar chart viewer")));
      await session.step(340, "And user clicks on the \"bar Black\" area of bar chart viewer", () => clickArea(page, "bar Black", el("bar chart viewer")));
      await session.step(341, "Then fewer than 143 rows should pass the filter", () => filterPassesFewer(page, 143));
      await session.step(342, "And the legend of box plot viewer should list 1 item", () => legendLists(page, el("box plot viewer"), 1));
      await session.step(343, "And \"Black\" legend item in legend of box plot viewer should be visible", () => shouldBe(page, el("\"Black\" legend item in legend of box plot viewer"), "visible"));
      await session.step(344, "When user saves the layout of the current table view to the server", () => saveLayoutToServer(page));
      await session.step(345, "And user hovers over filter panel", () => hoverOver(page, el("filter panel")));
      await session.step(346, "And user clicks on reset icon of filter panel", () => clickOn(page, el("reset icon of filter panel")));
      await session.step(347, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(348, "And the legend of box plot viewer should list 4 items", () => legendLists(page, el("box plot viewer"), 4));
      await session.step(349, "When user loads the saved layout", () => loadLayout(page));
      await session.step(350, "Then 143 rows should pass the filter", () => filterPasses(page, 143));
      await session.step(351, "And the legend of scatter plot viewer should list 3 items", () => legendLists(page, el("scatter plot viewer"), 3));
      await session.step(352, "And the legend of line chart viewer should list 3 items", () => legendLists(page, el("line chart viewer"), 3));
      await session.step(353, "And the legend of pie chart viewer should list 3 items", () => legendLists(page, el("pie chart viewer"), 3));
      await session.step(354, "And the legend of trellis plot viewer should list 3 items", () => legendLists(page, el("trellis plot viewer"), 3));
      await session.step(355, "And the legend of box plot viewer should list 3 items", () => legendLists(page, el("box plot viewer"), 3));
      await session.step(356, "And \"Black\" legend item in legend of box plot viewer should be visible", () => shouldBe(page, el("\"Black\" legend item in legend of box plot viewer"), "visible"));
      await session.step(357, "And \"Caucasian\" legend item in legend of box plot viewer should be visible", () => shouldBe(page, el("\"Caucasian\" legend item in legend of box plot viewer"), "visible"));
      await session.step(358, "When user clicks on the \"category Black of RACE\" area of filter panel", () => clickArea(page, "category Black of RACE", el("filter panel")));
      await session.step(359, "And user clicks on the \"checkbox Other of RACE\" area of filter panel", () => clickArea(page, "checkbox Other of RACE", el("filter panel")));
      await session.step(360, "Then 12 rows should pass the filter", () => filterPasses(page, 12));
      await session.step(361, "When user sets properties of bar chart viewer:", () => setProperties(page, el("bar chart viewer"), [["On Click","None"],["Row Source","Filtered"]]));
      await session.step(364, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("On Click Filter from the pie chart's own menu comes with Row Source All", async () => {
      await session.step(367, "Then 12 rows should pass the filter", () => filterPasses(page, 12));
      await session.step(368, "And the legend of pie chart viewer should list 2 items", () => legendLists(page, el("pie chart viewer"), 2));
      await session.step(369, "When user picks \"On Click > Filter\" from the context menu of pie chart viewer", () => pickFromContextMenu(page, "On Click > Filter", el("pie chart viewer")));
      await session.step(370, "Then \"On Click\" property of pie chart viewer should be \"Filter\"", () => propertyShouldBe(page, "On Click", el("pie chart viewer"), "Filter"));
      await session.step(371, "And \"Row Source\" property of pie chart viewer should be \"All\"", () => propertyShouldBe(page, "Row Source", el("pie chart viewer"), "All"));
      await session.step(372, "And the legend of pie chart viewer should list 4 items", () => legendLists(page, el("pie chart viewer"), 4));
      await session.step(373, "And 12 rows should pass the filter", () => filterPasses(page, 12));
      await session.step(374, "When user sets properties of pie chart viewer:", () => setProperties(page, el("pie chart viewer"), [["On Click","None"],["Row Source","Filtered"]]));
      await session.step(377, "Then the legend of pie chart viewer should list 2 items", () => legendLists(page, el("pie chart viewer"), 2));
      await session.step(378, "And 12 rows should pass the filter", () => filterPasses(page, 12));
      await session.step(379, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("A histogram viewer joins the view and a layout is saved with it", async () => {
      await session.step(382, "When user adds a histogram viewer", () => addViewer(page, "histogram"));
      await session.step(383, "And user sets properties of last histogram viewer:", () => setProperties(page, el("last histogram viewer"), [["valueColumnName","AGE"],["splitColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]));
      await session.step(388, "Then 12 rows should pass the filter", () => filterPasses(page, 12));
      await session.step(389, "When user saves the layout of the current table view to the server", () => saveLayoutToServer(page));
      await session.step(390, "And user hovers over filter panel", () => hoverOver(page, el("filter panel")));
      await session.step(391, "And user clicks on reset icon of filter panel", () => clickOn(page, el("reset icon of filter panel")));
      await session.step(392, "Then all rows should pass the filter", () => filterPassesAll(page));
      await session.step(393, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The layout that holds the histogram viewer brings the panel's filter back", async () => {
      await session.step(397, "When user loads the saved layout", () => loadLayout(page));
      await session.step(398, "Then 12 rows should pass the filter", () => filterPasses(page, 12));
    }, {knownFailure: true});
    run.finish();
  });
});
