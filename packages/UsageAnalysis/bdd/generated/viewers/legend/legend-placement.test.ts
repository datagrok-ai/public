/* eslint-disable max-len */
/* eslint-disable comma-spacing */
/* eslint-disable quotes */
/* ---
generated: features/viewers/legend/legend-placement.feature
generator: @datagrok-libraries/bdd — do not edit; run `grok-bdd compile` to regenerate
sub_features_covered: [viewers.legend]
--- */
import {test} from '@playwright/test';
import '../../../bindings/connections.js';
import '../../../bindings/grid.js';
import '../../../bindings/nx.js';
import '../../../bindings/spaces.js';
import '../../../bindings/tile-viewer.js';
import '../../../bindings/trellis-plot.js';
import '@datagrok-libraries/bdd/bindings/common/kinds';
import '@datagrok-libraries/bdd/bindings/common/parameter-types';
import '@datagrok-libraries/bdd/bindings/platform/datasets';
import '@datagrok-libraries/bdd/bindings/platform/elements';
import {loggedIn} from '@datagrok-libraries/bdd/bindings/common/session';
import {clickOn, hoverOver, rightClickOn, shouldBe} from '@datagrok-libraries/bdd/bindings/common/steps';
import {closeAllViews, openDataset, openProject, saveAsProject} from '@datagrok-libraries/bdd/bindings/platform/steps';
import {addViewerWith, closeContextMenu, dragLegendSplitter, legendDocked, legendInCorner, legendLists, legendMiniIcon, legendNarrower, legendPlacedNowhere, legendSlot, legendWider, loadLayout, noErrors, pickFromOpenMenu, propertiesShouldBe, propertyShouldBe, resizeTo, restoreSize, saveLayoutToServer, setProperties, setProperty, viewerCount} from '@datagrok-libraries/bdd/bindings/tiers/viewers/steps';
import {setInnerProperty} from '@datagrok-libraries/bdd/bindings/tiers/viewers/widgets';
import {ds, el, feature, journey} from '@datagrok-libraries/bdd/runtime';

test.describe("Where a legend sits, how big it is and when it folds away", () => {
  const session = feature(test, "features/viewers/legend/legend-placement.feature", import.meta.url);
  test("Where a legend sits, how big it is and when it folds away", {tag: ["@journey", "@viewers", "@realizes:viewers.legend"]}, async ({browser}) => {
    const page = await session.page(browser);
    const run = journey(test, 29, page);
    await session.step(32, "Given user is logged in", () => loggedIn(page));
    await session.step(33, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(34, "And user adds a scatter plot viewer with:", () => addViewerWith(page, "scatter plot", [["xColumnName","WEIGHT"],["yColumnName","HEIGHT"],["colorColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]), [["xColumnName","WEIGHT"],["yColumnName","HEIGHT"],["colorColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]);
    await session.step(40, "And user adds a histogram viewer with:", () => addViewerWith(page, "histogram", [["valueColumnName","AGE"],["splitColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]), [["valueColumnName","AGE"],["splitColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]);
    await session.step(45, "And user adds a line chart viewer with:", () => addViewerWith(page, "line chart", [["xColumnName","AGE"],["yColumnNames","WEIGHT"],["splitColumnNames","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]), [["xColumnName","AGE"],["yColumnNames","WEIGHT"],["splitColumnNames","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]);
    await session.step(51, "And user adds a bar chart viewer with:", () => addViewerWith(page, "bar chart", [["splitColumnName","SEX"],["stackColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]), [["splitColumnName","SEX"],["stackColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]);
    await session.step(56, "And user adds a pie chart viewer with:", () => addViewerWith(page, "pie chart", [["categoryColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]), [["categoryColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]);
    await session.step(60, "And user adds a trellis plot viewer with:", () => addViewerWith(page, "trellis plot", [["xColumnNames","RACE"],["Viewer Type","Scatter plot"],["Legend Visibility","Always"],["Legend Position","Right"]]), [["xColumnNames","RACE"],["Viewer Type","Scatter plot"],["Legend Visibility","Always"],["Legend Position","Right"]]);
    await session.step(65, "And user sets \"colorColumnName\" inner property of trellis plot viewer to \"RACE\"", () => setInnerProperty(page, "colorColumnName", el("trellis plot viewer"), "RACE"));
    await session.step(66, "And user adds a box plot viewer with:", () => addViewerWith(page, "box plot", [["categoryColumnNames","RACE"],["valueColumnName","AGE"],["markerColorColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]), [["categoryColumnNames","RACE"],["valueColumnName","AGE"],["markerColorColumnName","RACE"],["Legend Visibility","Always"],["Legend Position","Right"]]);
    await session.step(72, "Then the legend of scatter plot viewer should list 4 items", () => legendLists(page, el("scatter plot viewer"), 4));
    await session.step(73, "And the legend of box plot viewer should list 4 items", () => legendLists(page, el("box plot viewer"), 4));
    await run.scenario("The scatter plot legend grows and shrinks with its splitter [viewer=scatter plot]", async () => {
      await session.step(76, "Then the legend of scatter plot viewer should be docked", () => legendDocked(page, el("scatter plot viewer")));
      await session.step(77, "And the legend of scatter plot viewer should be in the \"right\" slot", () => legendSlot(page, el("scatter plot viewer"), "right"));
      await session.step(78, "When user drags the legend splitter of scatter plot viewer by 40 pixels to the left", () => dragLegendSplitter(page, el("scatter plot viewer"), 40, "left"));
      await session.step(79, "Then the legend of scatter plot viewer should be wider than before", () => legendWider(page, el("scatter plot viewer")));
      await session.step(80, "And the legend of scatter plot viewer should be in the \"right\" slot", () => legendSlot(page, el("scatter plot viewer"), "right"));
      await session.step(81, "And the legend of scatter plot viewer should list 4 items", () => legendLists(page, el("scatter plot viewer"), 4));
      await session.step(82, "When user drags the legend splitter of scatter plot viewer by 30 pixels to the right", () => dragLegendSplitter(page, el("scatter plot viewer"), 30, "right"));
      await session.step(83, "Then the legend of scatter plot viewer should be narrower than before", () => legendNarrower(page, el("scatter plot viewer")));
      await session.step(84, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The histogram legend grows and shrinks with its splitter [viewer=histogram]", async () => {
      await session.step(76, "Then the legend of histogram viewer should be docked", () => legendDocked(page, el("histogram viewer")));
      await session.step(77, "And the legend of histogram viewer should be in the \"right\" slot", () => legendSlot(page, el("histogram viewer"), "right"));
      await session.step(78, "When user drags the legend splitter of histogram viewer by 40 pixels to the left", () => dragLegendSplitter(page, el("histogram viewer"), 40, "left"));
      await session.step(79, "Then the legend of histogram viewer should be wider than before", () => legendWider(page, el("histogram viewer")));
      await session.step(80, "And the legend of histogram viewer should be in the \"right\" slot", () => legendSlot(page, el("histogram viewer"), "right"));
      await session.step(81, "And the legend of histogram viewer should list 4 items", () => legendLists(page, el("histogram viewer"), 4));
      await session.step(82, "When user drags the legend splitter of histogram viewer by 30 pixels to the right", () => dragLegendSplitter(page, el("histogram viewer"), 30, "right"));
      await session.step(83, "Then the legend of histogram viewer should be narrower than before", () => legendNarrower(page, el("histogram viewer")));
      await session.step(84, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The line chart legend grows and shrinks with its splitter [viewer=line chart]", async () => {
      await session.step(76, "Then the legend of line chart viewer should be docked", () => legendDocked(page, el("line chart viewer")));
      await session.step(77, "And the legend of line chart viewer should be in the \"right\" slot", () => legendSlot(page, el("line chart viewer"), "right"));
      await session.step(78, "When user drags the legend splitter of line chart viewer by 40 pixels to the left", () => dragLegendSplitter(page, el("line chart viewer"), 40, "left"));
      await session.step(79, "Then the legend of line chart viewer should be wider than before", () => legendWider(page, el("line chart viewer")));
      await session.step(80, "And the legend of line chart viewer should be in the \"right\" slot", () => legendSlot(page, el("line chart viewer"), "right"));
      await session.step(81, "And the legend of line chart viewer should list 4 items", () => legendLists(page, el("line chart viewer"), 4));
      await session.step(82, "When user drags the legend splitter of line chart viewer by 30 pixels to the right", () => dragLegendSplitter(page, el("line chart viewer"), 30, "right"));
      await session.step(83, "Then the legend of line chart viewer should be narrower than before", () => legendNarrower(page, el("line chart viewer")));
      await session.step(84, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The bar chart legend grows and shrinks with its splitter [viewer=bar chart]", async () => {
      await session.step(76, "Then the legend of bar chart viewer should be docked", () => legendDocked(page, el("bar chart viewer")));
      await session.step(77, "And the legend of bar chart viewer should be in the \"right\" slot", () => legendSlot(page, el("bar chart viewer"), "right"));
      await session.step(78, "When user drags the legend splitter of bar chart viewer by 40 pixels to the left", () => dragLegendSplitter(page, el("bar chart viewer"), 40, "left"));
      await session.step(79, "Then the legend of bar chart viewer should be wider than before", () => legendWider(page, el("bar chart viewer")));
      await session.step(80, "And the legend of bar chart viewer should be in the \"right\" slot", () => legendSlot(page, el("bar chart viewer"), "right"));
      await session.step(81, "And the legend of bar chart viewer should list 4 items", () => legendLists(page, el("bar chart viewer"), 4));
      await session.step(82, "When user drags the legend splitter of bar chart viewer by 30 pixels to the right", () => dragLegendSplitter(page, el("bar chart viewer"), 30, "right"));
      await session.step(83, "Then the legend of bar chart viewer should be narrower than before", () => legendNarrower(page, el("bar chart viewer")));
      await session.step(84, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The pie chart legend grows and shrinks with its splitter [viewer=pie chart]", async () => {
      await session.step(76, "Then the legend of pie chart viewer should be docked", () => legendDocked(page, el("pie chart viewer")));
      await session.step(77, "And the legend of pie chart viewer should be in the \"right\" slot", () => legendSlot(page, el("pie chart viewer"), "right"));
      await session.step(78, "When user drags the legend splitter of pie chart viewer by 40 pixels to the left", () => dragLegendSplitter(page, el("pie chart viewer"), 40, "left"));
      await session.step(79, "Then the legend of pie chart viewer should be wider than before", () => legendWider(page, el("pie chart viewer")));
      await session.step(80, "And the legend of pie chart viewer should be in the \"right\" slot", () => legendSlot(page, el("pie chart viewer"), "right"));
      await session.step(81, "And the legend of pie chart viewer should list 4 items", () => legendLists(page, el("pie chart viewer"), 4));
      await session.step(82, "When user drags the legend splitter of pie chart viewer by 30 pixels to the right", () => dragLegendSplitter(page, el("pie chart viewer"), 30, "right"));
      await session.step(83, "Then the legend of pie chart viewer should be narrower than before", () => legendNarrower(page, el("pie chart viewer")));
      await session.step(84, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The trellis plot legend grows and shrinks with its splitter [viewer=trellis plot]", async () => {
      await session.step(76, "Then the legend of trellis plot viewer should be docked", () => legendDocked(page, el("trellis plot viewer")));
      await session.step(77, "And the legend of trellis plot viewer should be in the \"right\" slot", () => legendSlot(page, el("trellis plot viewer"), "right"));
      await session.step(78, "When user drags the legend splitter of trellis plot viewer by 40 pixels to the left", () => dragLegendSplitter(page, el("trellis plot viewer"), 40, "left"));
      await session.step(79, "Then the legend of trellis plot viewer should be wider than before", () => legendWider(page, el("trellis plot viewer")));
      await session.step(80, "And the legend of trellis plot viewer should be in the \"right\" slot", () => legendSlot(page, el("trellis plot viewer"), "right"));
      await session.step(81, "And the legend of trellis plot viewer should list 4 items", () => legendLists(page, el("trellis plot viewer"), 4));
      await session.step(82, "When user drags the legend splitter of trellis plot viewer by 30 pixels to the right", () => dragLegendSplitter(page, el("trellis plot viewer"), 30, "right"));
      await session.step(83, "Then the legend of trellis plot viewer should be narrower than before", () => legendNarrower(page, el("trellis plot viewer")));
      await session.step(84, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The box plot legend grows and shrinks with its splitter [viewer=box plot]", async () => {
      await session.step(76, "Then the legend of box plot viewer should be docked", () => legendDocked(page, el("box plot viewer")));
      await session.step(77, "And the legend of box plot viewer should be in the \"right\" slot", () => legendSlot(page, el("box plot viewer"), "right"));
      await session.step(78, "When user drags the legend splitter of box plot viewer by 40 pixels to the left", () => dragLegendSplitter(page, el("box plot viewer"), 40, "left"));
      await session.step(79, "Then the legend of box plot viewer should be wider than before", () => legendWider(page, el("box plot viewer")));
      await session.step(80, "And the legend of box plot viewer should be in the \"right\" slot", () => legendSlot(page, el("box plot viewer"), "right"));
      await session.step(81, "And the legend of box plot viewer should list 4 items", () => legendLists(page, el("box plot viewer"), 4));
      await session.step(82, "When user drags the legend splitter of box plot viewer by 30 pixels to the right", () => dragLegendSplitter(page, el("box plot viewer"), 30, "right"));
      await session.step(83, "Then the legend of box plot viewer should be narrower than before", () => legendNarrower(page, el("box plot viewer")));
      await session.step(84, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Visibility Always from the scatter plot legend's menu holds, and Position Auto follows the viewer's shape [viewer=scatter plot, wide slot=rightBottom]", async () => {
      await session.step(97, "When user sets \"Legend Visibility\" property of scatter plot viewer to \"Auto\"", () => setProperty(page, "Legend Visibility", el("scatter plot viewer"), "Auto"));
      await session.step(98, "And user right-clicks on legend of scatter plot viewer", () => rightClickOn(page, el("legend of scatter plot viewer")));
      await session.step(99, "And user picks \"Always\" from the open menu", () => pickFromOpenMenu(page, "Always"));
      await session.step(100, "And user closes the context menu", () => closeContextMenu(page));
      await session.step(101, "Then \"Legend Visibility\" property of scatter plot viewer should be \"Always\"", () => propertyShouldBe(page, "Legend Visibility", el("scatter plot viewer"), "Always"));
      await session.step(102, "When user sets \"Legend Position\" property of scatter plot viewer to \"Auto\"", () => setProperty(page, "Legend Position", el("scatter plot viewer"), "Auto"));
      await session.step(103, "And user resizes scatter plot viewer to 320 by 640", () => resizeTo(page, el("scatter plot viewer"), 320, 640));
      await session.step(104, "Then the legend of scatter plot viewer should be docked", () => legendDocked(page, el("scatter plot viewer")));
      await session.step(105, "And the legend of scatter plot viewer should be in the \"top\" slot", () => legendSlot(page, el("scatter plot viewer"), "top"));
      await session.step(106, "When user resizes scatter plot viewer to 800 by 320", () => resizeTo(page, el("scatter plot viewer"), 800, 320));
      await session.step(107, "Then the legend of scatter plot viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("scatter plot viewer"), "rightBottom"));
      await session.step(108, "And legend of scatter plot viewer should be visible", () => shouldBe(page, el("legend of scatter plot viewer"), "visible"));
      await session.step(109, "When user resizes scatter plot viewer to 220 by 220", () => resizeTo(page, el("scatter plot viewer"), 220, 220));
      await session.step(110, "Then legend of scatter plot viewer should be visible", () => shouldBe(page, el("legend of scatter plot viewer"), "visible"));
      await session.step(111, "And mini legend icon of scatter plot viewer should be hidden", () => shouldBe(page, el("mini legend icon of scatter plot viewer"), "hidden"));
      await session.step(112, "When user restores the size of scatter plot viewer", () => restoreSize(page, el("scatter plot viewer")));
      await session.step(113, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Visibility Always from the histogram legend's menu holds, and Position Auto follows the viewer's shape [viewer=histogram, wide slot=rightBottom]", async () => {
      await session.step(97, "When user sets \"Legend Visibility\" property of histogram viewer to \"Auto\"", () => setProperty(page, "Legend Visibility", el("histogram viewer"), "Auto"));
      await session.step(98, "And user right-clicks on legend of histogram viewer", () => rightClickOn(page, el("legend of histogram viewer")));
      await session.step(99, "And user picks \"Always\" from the open menu", () => pickFromOpenMenu(page, "Always"));
      await session.step(100, "And user closes the context menu", () => closeContextMenu(page));
      await session.step(101, "Then \"Legend Visibility\" property of histogram viewer should be \"Always\"", () => propertyShouldBe(page, "Legend Visibility", el("histogram viewer"), "Always"));
      await session.step(102, "When user sets \"Legend Position\" property of histogram viewer to \"Auto\"", () => setProperty(page, "Legend Position", el("histogram viewer"), "Auto"));
      await session.step(103, "And user resizes histogram viewer to 320 by 640", () => resizeTo(page, el("histogram viewer"), 320, 640));
      await session.step(104, "Then the legend of histogram viewer should be docked", () => legendDocked(page, el("histogram viewer")));
      await session.step(105, "And the legend of histogram viewer should be in the \"top\" slot", () => legendSlot(page, el("histogram viewer"), "top"));
      await session.step(106, "When user resizes histogram viewer to 800 by 320", () => resizeTo(page, el("histogram viewer"), 800, 320));
      await session.step(107, "Then the legend of histogram viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("histogram viewer"), "rightBottom"));
      await session.step(108, "And legend of histogram viewer should be visible", () => shouldBe(page, el("legend of histogram viewer"), "visible"));
      await session.step(109, "When user resizes histogram viewer to 220 by 220", () => resizeTo(page, el("histogram viewer"), 220, 220));
      await session.step(110, "Then legend of histogram viewer should be visible", () => shouldBe(page, el("legend of histogram viewer"), "visible"));
      await session.step(111, "And mini legend icon of histogram viewer should be hidden", () => shouldBe(page, el("mini legend icon of histogram viewer"), "hidden"));
      await session.step(112, "When user restores the size of histogram viewer", () => restoreSize(page, el("histogram viewer")));
      await session.step(113, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Visibility Always from the line chart legend's menu holds, and Position Auto follows the viewer's shape [viewer=line chart, wide slot=right]", async () => {
      await session.step(97, "When user sets \"Legend Visibility\" property of line chart viewer to \"Auto\"", () => setProperty(page, "Legend Visibility", el("line chart viewer"), "Auto"));
      await session.step(98, "And user right-clicks on legend of line chart viewer", () => rightClickOn(page, el("legend of line chart viewer")));
      await session.step(99, "And user picks \"Always\" from the open menu", () => pickFromOpenMenu(page, "Always"));
      await session.step(100, "And user closes the context menu", () => closeContextMenu(page));
      await session.step(101, "Then \"Legend Visibility\" property of line chart viewer should be \"Always\"", () => propertyShouldBe(page, "Legend Visibility", el("line chart viewer"), "Always"));
      await session.step(102, "When user sets \"Legend Position\" property of line chart viewer to \"Auto\"", () => setProperty(page, "Legend Position", el("line chart viewer"), "Auto"));
      await session.step(103, "And user resizes line chart viewer to 320 by 640", () => resizeTo(page, el("line chart viewer"), 320, 640));
      await session.step(104, "Then the legend of line chart viewer should be docked", () => legendDocked(page, el("line chart viewer")));
      await session.step(105, "And the legend of line chart viewer should be in the \"top\" slot", () => legendSlot(page, el("line chart viewer"), "top"));
      await session.step(106, "When user resizes line chart viewer to 800 by 320", () => resizeTo(page, el("line chart viewer"), 800, 320));
      await session.step(107, "Then the legend of line chart viewer should be in the \"right\" slot", () => legendSlot(page, el("line chart viewer"), "right"));
      await session.step(108, "And legend of line chart viewer should be visible", () => shouldBe(page, el("legend of line chart viewer"), "visible"));
      await session.step(109, "When user resizes line chart viewer to 220 by 220", () => resizeTo(page, el("line chart viewer"), 220, 220));
      await session.step(110, "Then legend of line chart viewer should be visible", () => shouldBe(page, el("legend of line chart viewer"), "visible"));
      await session.step(111, "And mini legend icon of line chart viewer should be hidden", () => shouldBe(page, el("mini legend icon of line chart viewer"), "hidden"));
      await session.step(112, "When user restores the size of line chart viewer", () => restoreSize(page, el("line chart viewer")));
      await session.step(113, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Visibility Always from the bar chart legend's menu holds, and Position Auto follows the viewer's shape [viewer=bar chart, wide slot=rightBottom]", async () => {
      await session.step(97, "When user sets \"Legend Visibility\" property of bar chart viewer to \"Auto\"", () => setProperty(page, "Legend Visibility", el("bar chart viewer"), "Auto"));
      await session.step(98, "And user right-clicks on legend of bar chart viewer", () => rightClickOn(page, el("legend of bar chart viewer")));
      await session.step(99, "And user picks \"Always\" from the open menu", () => pickFromOpenMenu(page, "Always"));
      await session.step(100, "And user closes the context menu", () => closeContextMenu(page));
      await session.step(101, "Then \"Legend Visibility\" property of bar chart viewer should be \"Always\"", () => propertyShouldBe(page, "Legend Visibility", el("bar chart viewer"), "Always"));
      await session.step(102, "When user sets \"Legend Position\" property of bar chart viewer to \"Auto\"", () => setProperty(page, "Legend Position", el("bar chart viewer"), "Auto"));
      await session.step(103, "And user resizes bar chart viewer to 320 by 640", () => resizeTo(page, el("bar chart viewer"), 320, 640));
      await session.step(104, "Then the legend of bar chart viewer should be docked", () => legendDocked(page, el("bar chart viewer")));
      await session.step(105, "And the legend of bar chart viewer should be in the \"top\" slot", () => legendSlot(page, el("bar chart viewer"), "top"));
      await session.step(106, "When user resizes bar chart viewer to 800 by 320", () => resizeTo(page, el("bar chart viewer"), 800, 320));
      await session.step(107, "Then the legend of bar chart viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("bar chart viewer"), "rightBottom"));
      await session.step(108, "And legend of bar chart viewer should be visible", () => shouldBe(page, el("legend of bar chart viewer"), "visible"));
      await session.step(109, "When user resizes bar chart viewer to 220 by 220", () => resizeTo(page, el("bar chart viewer"), 220, 220));
      await session.step(110, "Then legend of bar chart viewer should be visible", () => shouldBe(page, el("legend of bar chart viewer"), "visible"));
      await session.step(111, "And mini legend icon of bar chart viewer should be hidden", () => shouldBe(page, el("mini legend icon of bar chart viewer"), "hidden"));
      await session.step(112, "When user restores the size of bar chart viewer", () => restoreSize(page, el("bar chart viewer")));
      await session.step(113, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Visibility Always from the trellis plot legend's menu holds, and Position Auto follows the viewer's shape [viewer=trellis plot, wide slot=right]", async () => {
      await session.step(97, "When user sets \"Legend Visibility\" property of trellis plot viewer to \"Auto\"", () => setProperty(page, "Legend Visibility", el("trellis plot viewer"), "Auto"));
      await session.step(98, "And user right-clicks on legend of trellis plot viewer", () => rightClickOn(page, el("legend of trellis plot viewer")));
      await session.step(99, "And user picks \"Always\" from the open menu", () => pickFromOpenMenu(page, "Always"));
      await session.step(100, "And user closes the context menu", () => closeContextMenu(page));
      await session.step(101, "Then \"Legend Visibility\" property of trellis plot viewer should be \"Always\"", () => propertyShouldBe(page, "Legend Visibility", el("trellis plot viewer"), "Always"));
      await session.step(102, "When user sets \"Legend Position\" property of trellis plot viewer to \"Auto\"", () => setProperty(page, "Legend Position", el("trellis plot viewer"), "Auto"));
      await session.step(103, "And user resizes trellis plot viewer to 320 by 640", () => resizeTo(page, el("trellis plot viewer"), 320, 640));
      await session.step(104, "Then the legend of trellis plot viewer should be docked", () => legendDocked(page, el("trellis plot viewer")));
      await session.step(105, "And the legend of trellis plot viewer should be in the \"top\" slot", () => legendSlot(page, el("trellis plot viewer"), "top"));
      await session.step(106, "When user resizes trellis plot viewer to 800 by 320", () => resizeTo(page, el("trellis plot viewer"), 800, 320));
      await session.step(107, "Then the legend of trellis plot viewer should be in the \"right\" slot", () => legendSlot(page, el("trellis plot viewer"), "right"));
      await session.step(108, "And legend of trellis plot viewer should be visible", () => shouldBe(page, el("legend of trellis plot viewer"), "visible"));
      await session.step(109, "When user resizes trellis plot viewer to 220 by 220", () => resizeTo(page, el("trellis plot viewer"), 220, 220));
      await session.step(110, "Then legend of trellis plot viewer should be visible", () => shouldBe(page, el("legend of trellis plot viewer"), "visible"));
      await session.step(111, "And mini legend icon of trellis plot viewer should be hidden", () => shouldBe(page, el("mini legend icon of trellis plot viewer"), "hidden"));
      await session.step(112, "When user restores the size of trellis plot viewer", () => restoreSize(page, el("trellis plot viewer")));
      await session.step(113, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Position Auto follows the pie chart's shape under Visibility Always [viewer=pie chart, tall slot=top, wide slot=right]", async () => {
      await session.step(124, "When user sets \"Legend Position\" property of pie chart viewer to \"Auto\"", () => setProperty(page, "Legend Position", el("pie chart viewer"), "Auto"));
      await session.step(125, "And user resizes pie chart viewer to 320 by 640", () => resizeTo(page, el("pie chart viewer"), 320, 640));
      await session.step(126, "Then the legend of pie chart viewer should be in the \"top\" slot", () => legendSlot(page, el("pie chart viewer"), "top"));
      await session.step(127, "When user resizes pie chart viewer to 800 by 320", () => resizeTo(page, el("pie chart viewer"), 800, 320));
      await session.step(128, "Then the legend of pie chart viewer should be in the \"right\" slot", () => legendSlot(page, el("pie chart viewer"), "right"));
      await session.step(129, "And the legend of pie chart viewer should be docked", () => legendDocked(page, el("pie chart viewer")));
      await session.step(130, "When user restores the size of pie chart viewer", () => restoreSize(page, el("pie chart viewer")));
      await session.step(131, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Position Auto follows the box plot's shape under Visibility Always [viewer=box plot, tall slot=leftTop, wide slot=right]", async () => {
      await session.step(124, "When user sets \"Legend Position\" property of box plot viewer to \"Auto\"", () => setProperty(page, "Legend Position", el("box plot viewer"), "Auto"));
      await session.step(125, "And user resizes box plot viewer to 320 by 640", () => resizeTo(page, el("box plot viewer"), 320, 640));
      await session.step(126, "Then the legend of box plot viewer should be in the \"leftTop\" slot", () => legendSlot(page, el("box plot viewer"), "leftTop"));
      await session.step(127, "When user resizes box plot viewer to 800 by 320", () => resizeTo(page, el("box plot viewer"), 800, 320));
      await session.step(128, "Then the legend of box plot viewer should be in the \"right\" slot", () => legendSlot(page, el("box plot viewer"), "right"));
      await session.step(129, "And the legend of box plot viewer should be docked", () => legendDocked(page, el("box plot viewer")));
      await session.step(130, "When user restores the size of box plot viewer", () => restoreSize(page, el("box plot viewer")));
      await session.step(131, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Visibility Always and Position Auto come back from a saved layout", async () => {
      await session.step(139, "When user saves the layout of the current table view to the server", () => saveLayoutToServer(page));
      await session.step(140, "And user sets properties of scatter plot viewer:", () => setProperties(page, el("scatter plot viewer"), [["Legend Visibility","Never"],["Legend Position","Left"]]), [["Legend Visibility","Never"],["Legend Position","Left"]]);
      await session.step(143, "And user sets properties of histogram viewer:", () => setProperties(page, el("histogram viewer"), [["Legend Visibility","Never"],["Legend Position","Left"]]), [["Legend Visibility","Never"],["Legend Position","Left"]]);
      await session.step(146, "And user sets properties of line chart viewer:", () => setProperties(page, el("line chart viewer"), [["Legend Visibility","Never"],["Legend Position","Left"]]), [["Legend Visibility","Never"],["Legend Position","Left"]]);
      await session.step(149, "And user sets properties of bar chart viewer:", () => setProperties(page, el("bar chart viewer"), [["Legend Visibility","Never"],["Legend Position","Left"]]), [["Legend Visibility","Never"],["Legend Position","Left"]]);
      await session.step(152, "And user sets properties of pie chart viewer:", () => setProperties(page, el("pie chart viewer"), [["Legend Visibility","Never"],["Legend Position","Left"]]), [["Legend Visibility","Never"],["Legend Position","Left"]]);
      await session.step(155, "And user sets properties of trellis plot viewer:", () => setProperties(page, el("trellis plot viewer"), [["Legend Visibility","Never"],["Legend Position","Left"]]), [["Legend Visibility","Never"],["Legend Position","Left"]]);
      await session.step(158, "And user sets properties of box plot viewer:", () => setProperties(page, el("box plot viewer"), [["Legend Visibility","Never"],["Legend Position","Left"]]), [["Legend Visibility","Never"],["Legend Position","Left"]]);
      await session.step(161, "Then legend of scatter plot viewer should be hidden", () => shouldBe(page, el("legend of scatter plot viewer"), "hidden"));
      await session.step(162, "And \"Legend Position\" property of box plot viewer should be \"Left\"", () => propertyShouldBe(page, "Legend Position", el("box plot viewer"), "Left"));
      await session.step(163, "When user loads the saved layout", () => loadLayout(page));
      await session.step(164, "Then properties of scatter plot viewer should be:", () => propertiesShouldBe(page, el("scatter plot viewer"), [["Legend Visibility","Always"],["Legend Position","Auto"]]), [["Legend Visibility","Always"],["Legend Position","Auto"]]);
      await session.step(167, "And properties of histogram viewer should be:", () => propertiesShouldBe(page, el("histogram viewer"), [["Legend Visibility","Always"],["Legend Position","Auto"]]), [["Legend Visibility","Always"],["Legend Position","Auto"]]);
      await session.step(170, "And properties of line chart viewer should be:", () => propertiesShouldBe(page, el("line chart viewer"), [["Legend Visibility","Always"],["Legend Position","Auto"]]), [["Legend Visibility","Always"],["Legend Position","Auto"]]);
      await session.step(173, "And properties of bar chart viewer should be:", () => propertiesShouldBe(page, el("bar chart viewer"), [["Legend Visibility","Always"],["Legend Position","Auto"]]), [["Legend Visibility","Always"],["Legend Position","Auto"]]);
      await session.step(176, "And properties of pie chart viewer should be:", () => propertiesShouldBe(page, el("pie chart viewer"), [["Legend Visibility","Always"],["Legend Position","Auto"]]), [["Legend Visibility","Always"],["Legend Position","Auto"]]);
      await session.step(179, "And properties of trellis plot viewer should be:", () => propertiesShouldBe(page, el("trellis plot viewer"), [["Legend Visibility","Always"],["Legend Position","Auto"]]), [["Legend Visibility","Always"],["Legend Position","Auto"]]);
      await session.step(182, "And properties of box plot viewer should be:", () => propertiesShouldBe(page, el("box plot viewer"), [["Legend Visibility","Always"],["Legend Position","Auto"]]), [["Legend Visibility","Always"],["Legend Position","Auto"]]);
      await session.step(185, "And legend of scatter plot viewer should be visible", () => shouldBe(page, el("legend of scatter plot viewer"), "visible"));
      await session.step(186, "And legend of line chart viewer should be visible", () => shouldBe(page, el("legend of line chart viewer"), "visible"));
      await session.step(187, "And the legend of scatter plot viewer should list 4 items", () => legendLists(page, el("scatter plot viewer"), 4));
      await session.step(188, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Visibility Auto folds the scatter plot legend into the mini icon on a small viewer and puts it away on a tiny one [viewer=scatter plot]", async () => {
      await session.step(191, "When user sets properties of scatter plot viewer:", () => setProperties(page, el("scatter plot viewer"), [["Legend Position","Right"],["Legend Visibility","Auto"]]), [["Legend Position","Right"],["Legend Visibility","Auto"]]);
      await session.step(194, "And user resizes scatter plot viewer to 220 by 220", () => resizeTo(page, el("scatter plot viewer"), 220, 220));
      await session.step(195, "Then the legend of scatter plot viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("scatter plot viewer")));
      await session.step(196, "And mini legend icon of scatter plot viewer should be visible", () => shouldBe(page, el("mini legend icon of scatter plot viewer"), "visible"));
      await session.step(197, "When user resizes scatter plot viewer to 60 by 60", () => resizeTo(page, el("scatter plot viewer"), 60, 60));
      await session.step(198, "Then the legend of scatter plot viewer should be placed nowhere", () => legendPlacedNowhere(page, el("scatter plot viewer")));
      await session.step(199, "And mini legend icon of scatter plot viewer should be hidden", () => shouldBe(page, el("mini legend icon of scatter plot viewer"), "hidden"));
      await session.step(200, "When user resizes scatter plot viewer to 480 by 480", () => resizeTo(page, el("scatter plot viewer"), 480, 480));
      await session.step(201, "Then legend of scatter plot viewer should be visible", () => shouldBe(page, el("legend of scatter plot viewer"), "visible"));
      await session.step(202, "And the legend of scatter plot viewer should be docked", () => legendDocked(page, el("scatter plot viewer")));
      await session.step(203, "And the legend of scatter plot viewer should be in the \"right\" slot", () => legendSlot(page, el("scatter plot viewer"), "right"));
      await session.step(204, "When user restores the size of scatter plot viewer", () => restoreSize(page, el("scatter plot viewer")));
      await session.step(205, "And user sets \"Legend Visibility\" property of scatter plot viewer to \"Always\"", () => setProperty(page, "Legend Visibility", el("scatter plot viewer"), "Always"));
      await session.step(206, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Visibility Auto folds the histogram legend into the mini icon on a small viewer and puts it away on a tiny one [viewer=histogram]", async () => {
      await session.step(191, "When user sets properties of histogram viewer:", () => setProperties(page, el("histogram viewer"), [["Legend Position","Right"],["Legend Visibility","Auto"]]), [["Legend Position","Right"],["Legend Visibility","Auto"]]);
      await session.step(194, "And user resizes histogram viewer to 220 by 220", () => resizeTo(page, el("histogram viewer"), 220, 220));
      await session.step(195, "Then the legend of histogram viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("histogram viewer")));
      await session.step(196, "And mini legend icon of histogram viewer should be visible", () => shouldBe(page, el("mini legend icon of histogram viewer"), "visible"));
      await session.step(197, "When user resizes histogram viewer to 60 by 60", () => resizeTo(page, el("histogram viewer"), 60, 60));
      await session.step(198, "Then the legend of histogram viewer should be placed nowhere", () => legendPlacedNowhere(page, el("histogram viewer")));
      await session.step(199, "And mini legend icon of histogram viewer should be hidden", () => shouldBe(page, el("mini legend icon of histogram viewer"), "hidden"));
      await session.step(200, "When user resizes histogram viewer to 480 by 480", () => resizeTo(page, el("histogram viewer"), 480, 480));
      await session.step(201, "Then legend of histogram viewer should be visible", () => shouldBe(page, el("legend of histogram viewer"), "visible"));
      await session.step(202, "And the legend of histogram viewer should be docked", () => legendDocked(page, el("histogram viewer")));
      await session.step(203, "And the legend of histogram viewer should be in the \"right\" slot", () => legendSlot(page, el("histogram viewer"), "right"));
      await session.step(204, "When user restores the size of histogram viewer", () => restoreSize(page, el("histogram viewer")));
      await session.step(205, "And user sets \"Legend Visibility\" property of histogram viewer to \"Always\"", () => setProperty(page, "Legend Visibility", el("histogram viewer"), "Always"));
      await session.step(206, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Visibility Auto folds the line chart legend into the mini icon on a small viewer and puts it away on a tiny one [viewer=line chart]", async () => {
      await session.step(191, "When user sets properties of line chart viewer:", () => setProperties(page, el("line chart viewer"), [["Legend Position","Right"],["Legend Visibility","Auto"]]), [["Legend Position","Right"],["Legend Visibility","Auto"]]);
      await session.step(194, "And user resizes line chart viewer to 220 by 220", () => resizeTo(page, el("line chart viewer"), 220, 220));
      await session.step(195, "Then the legend of line chart viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("line chart viewer")));
      await session.step(196, "And mini legend icon of line chart viewer should be visible", () => shouldBe(page, el("mini legend icon of line chart viewer"), "visible"));
      await session.step(197, "When user resizes line chart viewer to 60 by 60", () => resizeTo(page, el("line chart viewer"), 60, 60));
      await session.step(198, "Then the legend of line chart viewer should be placed nowhere", () => legendPlacedNowhere(page, el("line chart viewer")));
      await session.step(199, "And mini legend icon of line chart viewer should be hidden", () => shouldBe(page, el("mini legend icon of line chart viewer"), "hidden"));
      await session.step(200, "When user resizes line chart viewer to 480 by 480", () => resizeTo(page, el("line chart viewer"), 480, 480));
      await session.step(201, "Then legend of line chart viewer should be visible", () => shouldBe(page, el("legend of line chart viewer"), "visible"));
      await session.step(202, "And the legend of line chart viewer should be docked", () => legendDocked(page, el("line chart viewer")));
      await session.step(203, "And the legend of line chart viewer should be in the \"right\" slot", () => legendSlot(page, el("line chart viewer"), "right"));
      await session.step(204, "When user restores the size of line chart viewer", () => restoreSize(page, el("line chart viewer")));
      await session.step(205, "And user sets \"Legend Visibility\" property of line chart viewer to \"Always\"", () => setProperty(page, "Legend Visibility", el("line chart viewer"), "Always"));
      await session.step(206, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Visibility Auto folds the bar chart legend into the mini icon on a small viewer and puts it away on a tiny one [viewer=bar chart]", async () => {
      await session.step(191, "When user sets properties of bar chart viewer:", () => setProperties(page, el("bar chart viewer"), [["Legend Position","Right"],["Legend Visibility","Auto"]]), [["Legend Position","Right"],["Legend Visibility","Auto"]]);
      await session.step(194, "And user resizes bar chart viewer to 220 by 220", () => resizeTo(page, el("bar chart viewer"), 220, 220));
      await session.step(195, "Then the legend of bar chart viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("bar chart viewer")));
      await session.step(196, "And mini legend icon of bar chart viewer should be visible", () => shouldBe(page, el("mini legend icon of bar chart viewer"), "visible"));
      await session.step(197, "When user resizes bar chart viewer to 60 by 60", () => resizeTo(page, el("bar chart viewer"), 60, 60));
      await session.step(198, "Then the legend of bar chart viewer should be placed nowhere", () => legendPlacedNowhere(page, el("bar chart viewer")));
      await session.step(199, "And mini legend icon of bar chart viewer should be hidden", () => shouldBe(page, el("mini legend icon of bar chart viewer"), "hidden"));
      await session.step(200, "When user resizes bar chart viewer to 480 by 480", () => resizeTo(page, el("bar chart viewer"), 480, 480));
      await session.step(201, "Then legend of bar chart viewer should be visible", () => shouldBe(page, el("legend of bar chart viewer"), "visible"));
      await session.step(202, "And the legend of bar chart viewer should be docked", () => legendDocked(page, el("bar chart viewer")));
      await session.step(203, "And the legend of bar chart viewer should be in the \"right\" slot", () => legendSlot(page, el("bar chart viewer"), "right"));
      await session.step(204, "When user restores the size of bar chart viewer", () => restoreSize(page, el("bar chart viewer")));
      await session.step(205, "And user sets \"Legend Visibility\" property of bar chart viewer to \"Always\"", () => setProperty(page, "Legend Visibility", el("bar chart viewer"), "Always"));
      await session.step(206, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Visibility Auto folds the trellis plot legend into the mini icon on a small viewer and puts it away on a tiny one [viewer=trellis plot]", async () => {
      await session.step(191, "When user sets properties of trellis plot viewer:", () => setProperties(page, el("trellis plot viewer"), [["Legend Position","Right"],["Legend Visibility","Auto"]]), [["Legend Position","Right"],["Legend Visibility","Auto"]]);
      await session.step(194, "And user resizes trellis plot viewer to 220 by 220", () => resizeTo(page, el("trellis plot viewer"), 220, 220));
      await session.step(195, "Then the legend of trellis plot viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("trellis plot viewer")));
      await session.step(196, "And mini legend icon of trellis plot viewer should be visible", () => shouldBe(page, el("mini legend icon of trellis plot viewer"), "visible"));
      await session.step(197, "When user resizes trellis plot viewer to 60 by 60", () => resizeTo(page, el("trellis plot viewer"), 60, 60));
      await session.step(198, "Then the legend of trellis plot viewer should be placed nowhere", () => legendPlacedNowhere(page, el("trellis plot viewer")));
      await session.step(199, "And mini legend icon of trellis plot viewer should be hidden", () => shouldBe(page, el("mini legend icon of trellis plot viewer"), "hidden"));
      await session.step(200, "When user resizes trellis plot viewer to 480 by 480", () => resizeTo(page, el("trellis plot viewer"), 480, 480));
      await session.step(201, "Then legend of trellis plot viewer should be visible", () => shouldBe(page, el("legend of trellis plot viewer"), "visible"));
      await session.step(202, "And the legend of trellis plot viewer should be docked", () => legendDocked(page, el("trellis plot viewer")));
      await session.step(203, "And the legend of trellis plot viewer should be in the \"right\" slot", () => legendSlot(page, el("trellis plot viewer"), "right"));
      await session.step(204, "When user restores the size of trellis plot viewer", () => restoreSize(page, el("trellis plot viewer")));
      await session.step(205, "And user sets \"Legend Visibility\" property of trellis plot viewer to \"Always\"", () => setProperty(page, "Legend Visibility", el("trellis plot viewer"), "Always"));
      await session.step(206, "Then no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The scatter plot legend takes each corner, and its chevron folds it into the mini icon [viewer=scatter plot]", async () => {
      await session.step(217, "When user sets \"Legend Position\" property of scatter plot viewer to \"LeftTop\"", () => setProperty(page, "Legend Position", el("scatter plot viewer"), "LeftTop"));
      await session.step(218, "Then the legend of scatter plot viewer should be in a corner", () => legendInCorner(page, el("scatter plot viewer")));
      await session.step(219, "And the legend of scatter plot viewer should be in the \"leftTop\" slot", () => legendSlot(page, el("scatter plot viewer"), "leftTop"));
      await session.step(220, "When user sets \"Legend Position\" property of scatter plot viewer to \"LeftBottom\"", () => setProperty(page, "Legend Position", el("scatter plot viewer"), "LeftBottom"));
      await session.step(221, "Then the legend of scatter plot viewer should be in the \"leftBottom\" slot", () => legendSlot(page, el("scatter plot viewer"), "leftBottom"));
      await session.step(222, "When user sets \"Legend Position\" property of scatter plot viewer to \"RightTop\"", () => setProperty(page, "Legend Position", el("scatter plot viewer"), "RightTop"));
      await session.step(223, "Then the legend of scatter plot viewer should be in the \"rightTop\" slot", () => legendSlot(page, el("scatter plot viewer"), "rightTop"));
      await session.step(224, "When user sets \"Legend Position\" property of scatter plot viewer to \"RightBottom\"", () => setProperty(page, "Legend Position", el("scatter plot viewer"), "RightBottom"));
      await session.step(225, "Then the legend of scatter plot viewer should be in a corner", () => legendInCorner(page, el("scatter plot viewer")));
      await session.step(226, "And the legend of scatter plot viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("scatter plot viewer"), "rightBottom"));
      await session.step(227, "And the legend of scatter plot viewer should list 4 items", () => legendLists(page, el("scatter plot viewer"), 4));
      await session.step(228, "And mini legend icon of scatter plot viewer should be hidden", () => shouldBe(page, el("mini legend icon of scatter plot viewer"), "hidden"));
      await session.step(229, "When user hovers over legend of scatter plot viewer", () => hoverOver(page, el("legend of scatter plot viewer")));
      await session.step(230, "And user clicks on legend close chevron of scatter plot viewer", () => clickOn(page, el("legend close chevron of scatter plot viewer")));
      await session.step(231, "Then the legend of scatter plot viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("scatter plot viewer")));
      await session.step(232, "And mini legend icon of scatter plot viewer should be visible", () => shouldBe(page, el("mini legend icon of scatter plot viewer"), "visible"));
      await session.step(233, "And legend of scatter plot viewer should be hidden", () => shouldBe(page, el("legend of scatter plot viewer"), "hidden"));
      await session.step(234, "When user clicks on mini legend icon of scatter plot viewer", () => clickOn(page, el("mini legend icon of scatter plot viewer")));
      await session.step(235, "Then the legend of scatter plot viewer should be in a corner", () => legendInCorner(page, el("scatter plot viewer")));
      await session.step(236, "And the legend of scatter plot viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("scatter plot viewer"), "rightBottom"));
      await session.step(237, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The histogram legend takes each corner, and its chevron folds it into the mini icon [viewer=histogram]", async () => {
      await session.step(217, "When user sets \"Legend Position\" property of histogram viewer to \"LeftTop\"", () => setProperty(page, "Legend Position", el("histogram viewer"), "LeftTop"));
      await session.step(218, "Then the legend of histogram viewer should be in a corner", () => legendInCorner(page, el("histogram viewer")));
      await session.step(219, "And the legend of histogram viewer should be in the \"leftTop\" slot", () => legendSlot(page, el("histogram viewer"), "leftTop"));
      await session.step(220, "When user sets \"Legend Position\" property of histogram viewer to \"LeftBottom\"", () => setProperty(page, "Legend Position", el("histogram viewer"), "LeftBottom"));
      await session.step(221, "Then the legend of histogram viewer should be in the \"leftBottom\" slot", () => legendSlot(page, el("histogram viewer"), "leftBottom"));
      await session.step(222, "When user sets \"Legend Position\" property of histogram viewer to \"RightTop\"", () => setProperty(page, "Legend Position", el("histogram viewer"), "RightTop"));
      await session.step(223, "Then the legend of histogram viewer should be in the \"rightTop\" slot", () => legendSlot(page, el("histogram viewer"), "rightTop"));
      await session.step(224, "When user sets \"Legend Position\" property of histogram viewer to \"RightBottom\"", () => setProperty(page, "Legend Position", el("histogram viewer"), "RightBottom"));
      await session.step(225, "Then the legend of histogram viewer should be in a corner", () => legendInCorner(page, el("histogram viewer")));
      await session.step(226, "And the legend of histogram viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("histogram viewer"), "rightBottom"));
      await session.step(227, "And the legend of histogram viewer should list 4 items", () => legendLists(page, el("histogram viewer"), 4));
      await session.step(228, "And mini legend icon of histogram viewer should be hidden", () => shouldBe(page, el("mini legend icon of histogram viewer"), "hidden"));
      await session.step(229, "When user hovers over legend of histogram viewer", () => hoverOver(page, el("legend of histogram viewer")));
      await session.step(230, "And user clicks on legend close chevron of histogram viewer", () => clickOn(page, el("legend close chevron of histogram viewer")));
      await session.step(231, "Then the legend of histogram viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("histogram viewer")));
      await session.step(232, "And mini legend icon of histogram viewer should be visible", () => shouldBe(page, el("mini legend icon of histogram viewer"), "visible"));
      await session.step(233, "And legend of histogram viewer should be hidden", () => shouldBe(page, el("legend of histogram viewer"), "hidden"));
      await session.step(234, "When user clicks on mini legend icon of histogram viewer", () => clickOn(page, el("mini legend icon of histogram viewer")));
      await session.step(235, "Then the legend of histogram viewer should be in a corner", () => legendInCorner(page, el("histogram viewer")));
      await session.step(236, "And the legend of histogram viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("histogram viewer"), "rightBottom"));
      await session.step(237, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The line chart legend takes each corner, and its chevron folds it into the mini icon [viewer=line chart]", async () => {
      await session.step(217, "When user sets \"Legend Position\" property of line chart viewer to \"LeftTop\"", () => setProperty(page, "Legend Position", el("line chart viewer"), "LeftTop"));
      await session.step(218, "Then the legend of line chart viewer should be in a corner", () => legendInCorner(page, el("line chart viewer")));
      await session.step(219, "And the legend of line chart viewer should be in the \"leftTop\" slot", () => legendSlot(page, el("line chart viewer"), "leftTop"));
      await session.step(220, "When user sets \"Legend Position\" property of line chart viewer to \"LeftBottom\"", () => setProperty(page, "Legend Position", el("line chart viewer"), "LeftBottom"));
      await session.step(221, "Then the legend of line chart viewer should be in the \"leftBottom\" slot", () => legendSlot(page, el("line chart viewer"), "leftBottom"));
      await session.step(222, "When user sets \"Legend Position\" property of line chart viewer to \"RightTop\"", () => setProperty(page, "Legend Position", el("line chart viewer"), "RightTop"));
      await session.step(223, "Then the legend of line chart viewer should be in the \"rightTop\" slot", () => legendSlot(page, el("line chart viewer"), "rightTop"));
      await session.step(224, "When user sets \"Legend Position\" property of line chart viewer to \"RightBottom\"", () => setProperty(page, "Legend Position", el("line chart viewer"), "RightBottom"));
      await session.step(225, "Then the legend of line chart viewer should be in a corner", () => legendInCorner(page, el("line chart viewer")));
      await session.step(226, "And the legend of line chart viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("line chart viewer"), "rightBottom"));
      await session.step(227, "And the legend of line chart viewer should list 4 items", () => legendLists(page, el("line chart viewer"), 4));
      await session.step(228, "And mini legend icon of line chart viewer should be hidden", () => shouldBe(page, el("mini legend icon of line chart viewer"), "hidden"));
      await session.step(229, "When user hovers over legend of line chart viewer", () => hoverOver(page, el("legend of line chart viewer")));
      await session.step(230, "And user clicks on legend close chevron of line chart viewer", () => clickOn(page, el("legend close chevron of line chart viewer")));
      await session.step(231, "Then the legend of line chart viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("line chart viewer")));
      await session.step(232, "And mini legend icon of line chart viewer should be visible", () => shouldBe(page, el("mini legend icon of line chart viewer"), "visible"));
      await session.step(233, "And legend of line chart viewer should be hidden", () => shouldBe(page, el("legend of line chart viewer"), "hidden"));
      await session.step(234, "When user clicks on mini legend icon of line chart viewer", () => clickOn(page, el("mini legend icon of line chart viewer")));
      await session.step(235, "Then the legend of line chart viewer should be in a corner", () => legendInCorner(page, el("line chart viewer")));
      await session.step(236, "And the legend of line chart viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("line chart viewer"), "rightBottom"));
      await session.step(237, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The bar chart legend takes each corner, and its chevron folds it into the mini icon [viewer=bar chart]", async () => {
      await session.step(217, "When user sets \"Legend Position\" property of bar chart viewer to \"LeftTop\"", () => setProperty(page, "Legend Position", el("bar chart viewer"), "LeftTop"));
      await session.step(218, "Then the legend of bar chart viewer should be in a corner", () => legendInCorner(page, el("bar chart viewer")));
      await session.step(219, "And the legend of bar chart viewer should be in the \"leftTop\" slot", () => legendSlot(page, el("bar chart viewer"), "leftTop"));
      await session.step(220, "When user sets \"Legend Position\" property of bar chart viewer to \"LeftBottom\"", () => setProperty(page, "Legend Position", el("bar chart viewer"), "LeftBottom"));
      await session.step(221, "Then the legend of bar chart viewer should be in the \"leftBottom\" slot", () => legendSlot(page, el("bar chart viewer"), "leftBottom"));
      await session.step(222, "When user sets \"Legend Position\" property of bar chart viewer to \"RightTop\"", () => setProperty(page, "Legend Position", el("bar chart viewer"), "RightTop"));
      await session.step(223, "Then the legend of bar chart viewer should be in the \"rightTop\" slot", () => legendSlot(page, el("bar chart viewer"), "rightTop"));
      await session.step(224, "When user sets \"Legend Position\" property of bar chart viewer to \"RightBottom\"", () => setProperty(page, "Legend Position", el("bar chart viewer"), "RightBottom"));
      await session.step(225, "Then the legend of bar chart viewer should be in a corner", () => legendInCorner(page, el("bar chart viewer")));
      await session.step(226, "And the legend of bar chart viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("bar chart viewer"), "rightBottom"));
      await session.step(227, "And the legend of bar chart viewer should list 4 items", () => legendLists(page, el("bar chart viewer"), 4));
      await session.step(228, "And mini legend icon of bar chart viewer should be hidden", () => shouldBe(page, el("mini legend icon of bar chart viewer"), "hidden"));
      await session.step(229, "When user hovers over legend of bar chart viewer", () => hoverOver(page, el("legend of bar chart viewer")));
      await session.step(230, "And user clicks on legend close chevron of bar chart viewer", () => clickOn(page, el("legend close chevron of bar chart viewer")));
      await session.step(231, "Then the legend of bar chart viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("bar chart viewer")));
      await session.step(232, "And mini legend icon of bar chart viewer should be visible", () => shouldBe(page, el("mini legend icon of bar chart viewer"), "visible"));
      await session.step(233, "And legend of bar chart viewer should be hidden", () => shouldBe(page, el("legend of bar chart viewer"), "hidden"));
      await session.step(234, "When user clicks on mini legend icon of bar chart viewer", () => clickOn(page, el("mini legend icon of bar chart viewer")));
      await session.step(235, "Then the legend of bar chart viewer should be in a corner", () => legendInCorner(page, el("bar chart viewer")));
      await session.step(236, "And the legend of bar chart viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("bar chart viewer"), "rightBottom"));
      await session.step(237, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The pie chart legend takes each corner, and its chevron folds it into the mini icon [viewer=pie chart]", async () => {
      await session.step(217, "When user sets \"Legend Position\" property of pie chart viewer to \"LeftTop\"", () => setProperty(page, "Legend Position", el("pie chart viewer"), "LeftTop"));
      await session.step(218, "Then the legend of pie chart viewer should be in a corner", () => legendInCorner(page, el("pie chart viewer")));
      await session.step(219, "And the legend of pie chart viewer should be in the \"leftTop\" slot", () => legendSlot(page, el("pie chart viewer"), "leftTop"));
      await session.step(220, "When user sets \"Legend Position\" property of pie chart viewer to \"LeftBottom\"", () => setProperty(page, "Legend Position", el("pie chart viewer"), "LeftBottom"));
      await session.step(221, "Then the legend of pie chart viewer should be in the \"leftBottom\" slot", () => legendSlot(page, el("pie chart viewer"), "leftBottom"));
      await session.step(222, "When user sets \"Legend Position\" property of pie chart viewer to \"RightTop\"", () => setProperty(page, "Legend Position", el("pie chart viewer"), "RightTop"));
      await session.step(223, "Then the legend of pie chart viewer should be in the \"rightTop\" slot", () => legendSlot(page, el("pie chart viewer"), "rightTop"));
      await session.step(224, "When user sets \"Legend Position\" property of pie chart viewer to \"RightBottom\"", () => setProperty(page, "Legend Position", el("pie chart viewer"), "RightBottom"));
      await session.step(225, "Then the legend of pie chart viewer should be in a corner", () => legendInCorner(page, el("pie chart viewer")));
      await session.step(226, "And the legend of pie chart viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("pie chart viewer"), "rightBottom"));
      await session.step(227, "And the legend of pie chart viewer should list 4 items", () => legendLists(page, el("pie chart viewer"), 4));
      await session.step(228, "And mini legend icon of pie chart viewer should be hidden", () => shouldBe(page, el("mini legend icon of pie chart viewer"), "hidden"));
      await session.step(229, "When user hovers over legend of pie chart viewer", () => hoverOver(page, el("legend of pie chart viewer")));
      await session.step(230, "And user clicks on legend close chevron of pie chart viewer", () => clickOn(page, el("legend close chevron of pie chart viewer")));
      await session.step(231, "Then the legend of pie chart viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("pie chart viewer")));
      await session.step(232, "And mini legend icon of pie chart viewer should be visible", () => shouldBe(page, el("mini legend icon of pie chart viewer"), "visible"));
      await session.step(233, "And legend of pie chart viewer should be hidden", () => shouldBe(page, el("legend of pie chart viewer"), "hidden"));
      await session.step(234, "When user clicks on mini legend icon of pie chart viewer", () => clickOn(page, el("mini legend icon of pie chart viewer")));
      await session.step(235, "Then the legend of pie chart viewer should be in a corner", () => legendInCorner(page, el("pie chart viewer")));
      await session.step(236, "And the legend of pie chart viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("pie chart viewer"), "rightBottom"));
      await session.step(237, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The trellis plot legend takes each corner, and its chevron folds it into the mini icon [viewer=trellis plot]", async () => {
      await session.step(217, "When user sets \"Legend Position\" property of trellis plot viewer to \"LeftTop\"", () => setProperty(page, "Legend Position", el("trellis plot viewer"), "LeftTop"));
      await session.step(218, "Then the legend of trellis plot viewer should be in a corner", () => legendInCorner(page, el("trellis plot viewer")));
      await session.step(219, "And the legend of trellis plot viewer should be in the \"leftTop\" slot", () => legendSlot(page, el("trellis plot viewer"), "leftTop"));
      await session.step(220, "When user sets \"Legend Position\" property of trellis plot viewer to \"LeftBottom\"", () => setProperty(page, "Legend Position", el("trellis plot viewer"), "LeftBottom"));
      await session.step(221, "Then the legend of trellis plot viewer should be in the \"leftBottom\" slot", () => legendSlot(page, el("trellis plot viewer"), "leftBottom"));
      await session.step(222, "When user sets \"Legend Position\" property of trellis plot viewer to \"RightTop\"", () => setProperty(page, "Legend Position", el("trellis plot viewer"), "RightTop"));
      await session.step(223, "Then the legend of trellis plot viewer should be in the \"rightTop\" slot", () => legendSlot(page, el("trellis plot viewer"), "rightTop"));
      await session.step(224, "When user sets \"Legend Position\" property of trellis plot viewer to \"RightBottom\"", () => setProperty(page, "Legend Position", el("trellis plot viewer"), "RightBottom"));
      await session.step(225, "Then the legend of trellis plot viewer should be in a corner", () => legendInCorner(page, el("trellis plot viewer")));
      await session.step(226, "And the legend of trellis plot viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("trellis plot viewer"), "rightBottom"));
      await session.step(227, "And the legend of trellis plot viewer should list 4 items", () => legendLists(page, el("trellis plot viewer"), 4));
      await session.step(228, "And mini legend icon of trellis plot viewer should be hidden", () => shouldBe(page, el("mini legend icon of trellis plot viewer"), "hidden"));
      await session.step(229, "When user hovers over legend of trellis plot viewer", () => hoverOver(page, el("legend of trellis plot viewer")));
      await session.step(230, "And user clicks on legend close chevron of trellis plot viewer", () => clickOn(page, el("legend close chevron of trellis plot viewer")));
      await session.step(231, "Then the legend of trellis plot viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("trellis plot viewer")));
      await session.step(232, "And mini legend icon of trellis plot viewer should be visible", () => shouldBe(page, el("mini legend icon of trellis plot viewer"), "visible"));
      await session.step(233, "And legend of trellis plot viewer should be hidden", () => shouldBe(page, el("legend of trellis plot viewer"), "hidden"));
      await session.step(234, "When user clicks on mini legend icon of trellis plot viewer", () => clickOn(page, el("mini legend icon of trellis plot viewer")));
      await session.step(235, "Then the legend of trellis plot viewer should be in a corner", () => legendInCorner(page, el("trellis plot viewer")));
      await session.step(236, "And the legend of trellis plot viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("trellis plot viewer"), "rightBottom"));
      await session.step(237, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("The box plot legend takes each corner, and its chevron folds it into the mini icon [viewer=box plot]", async () => {
      await session.step(217, "When user sets \"Legend Position\" property of box plot viewer to \"LeftTop\"", () => setProperty(page, "Legend Position", el("box plot viewer"), "LeftTop"));
      await session.step(218, "Then the legend of box plot viewer should be in a corner", () => legendInCorner(page, el("box plot viewer")));
      await session.step(219, "And the legend of box plot viewer should be in the \"leftTop\" slot", () => legendSlot(page, el("box plot viewer"), "leftTop"));
      await session.step(220, "When user sets \"Legend Position\" property of box plot viewer to \"LeftBottom\"", () => setProperty(page, "Legend Position", el("box plot viewer"), "LeftBottom"));
      await session.step(221, "Then the legend of box plot viewer should be in the \"leftBottom\" slot", () => legendSlot(page, el("box plot viewer"), "leftBottom"));
      await session.step(222, "When user sets \"Legend Position\" property of box plot viewer to \"RightTop\"", () => setProperty(page, "Legend Position", el("box plot viewer"), "RightTop"));
      await session.step(223, "Then the legend of box plot viewer should be in the \"rightTop\" slot", () => legendSlot(page, el("box plot viewer"), "rightTop"));
      await session.step(224, "When user sets \"Legend Position\" property of box plot viewer to \"RightBottom\"", () => setProperty(page, "Legend Position", el("box plot viewer"), "RightBottom"));
      await session.step(225, "Then the legend of box plot viewer should be in a corner", () => legendInCorner(page, el("box plot viewer")));
      await session.step(226, "And the legend of box plot viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("box plot viewer"), "rightBottom"));
      await session.step(227, "And the legend of box plot viewer should list 4 items", () => legendLists(page, el("box plot viewer"), 4));
      await session.step(228, "And mini legend icon of box plot viewer should be hidden", () => shouldBe(page, el("mini legend icon of box plot viewer"), "hidden"));
      await session.step(229, "When user hovers over legend of box plot viewer", () => hoverOver(page, el("legend of box plot viewer")));
      await session.step(230, "And user clicks on legend close chevron of box plot viewer", () => clickOn(page, el("legend close chevron of box plot viewer")));
      await session.step(231, "Then the legend of box plot viewer should be collapsed to the mini icon", () => legendMiniIcon(page, el("box plot viewer")));
      await session.step(232, "And mini legend icon of box plot viewer should be visible", () => shouldBe(page, el("mini legend icon of box plot viewer"), "visible"));
      await session.step(233, "And legend of box plot viewer should be hidden", () => shouldBe(page, el("legend of box plot viewer"), "hidden"));
      await session.step(234, "When user clicks on mini legend icon of box plot viewer", () => clickOn(page, el("mini legend icon of box plot viewer")));
      await session.step(235, "Then the legend of box plot viewer should be in a corner", () => legendInCorner(page, el("box plot viewer")));
      await session.step(236, "And the legend of box plot viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("box plot viewer"), "rightBottom"));
      await session.step(237, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Corner positions come back from a saved layout", async () => {
      await session.step(250, "When user sets \"Legend Position\" property of scatter plot viewer to \"LeftTop\"", () => setProperty(page, "Legend Position", el("scatter plot viewer"), "LeftTop"));
      await session.step(251, "And user sets \"Legend Position\" property of pie chart viewer to \"LeftBottom\"", () => setProperty(page, "Legend Position", el("pie chart viewer"), "LeftBottom"));
      await session.step(252, "And user sets \"Legend Position\" property of box plot viewer to \"RightTop\"", () => setProperty(page, "Legend Position", el("box plot viewer"), "RightTop"));
      await session.step(253, "And user saves the layout of the current table view to the server", () => saveLayoutToServer(page));
      await session.step(254, "And user sets \"Legend Position\" property of scatter plot viewer to \"Right\"", () => setProperty(page, "Legend Position", el("scatter plot viewer"), "Right"));
      await session.step(255, "And user sets \"Legend Position\" property of pie chart viewer to \"Right\"", () => setProperty(page, "Legend Position", el("pie chart viewer"), "Right"));
      await session.step(256, "And user sets \"Legend Position\" property of box plot viewer to \"Right\"", () => setProperty(page, "Legend Position", el("box plot viewer"), "Right"));
      await session.step(257, "Then the legend of scatter plot viewer should be docked", () => legendDocked(page, el("scatter plot viewer")));
      await session.step(258, "When user loads the saved layout", () => loadLayout(page));
      await session.step(259, "Then the legend of scatter plot viewer should be in the \"leftTop\" slot", () => legendSlot(page, el("scatter plot viewer"), "leftTop"));
      await session.step(260, "And the legend of scatter plot viewer should be in a corner", () => legendInCorner(page, el("scatter plot viewer")));
      await session.step(261, "And the legend of pie chart viewer should be in the \"leftBottom\" slot", () => legendSlot(page, el("pie chart viewer"), "leftBottom"));
      await session.step(262, "And the legend of box plot viewer should be in the \"rightTop\" slot", () => legendSlot(page, el("box plot viewer"), "rightTop"));
      await session.step(263, "And the legend of bar chart viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("bar chart viewer"), "rightBottom"));
      await session.step(264, "And the legend of trellis plot viewer should be in a corner", () => legendInCorner(page, el("trellis plot viewer")));
      await session.step(265, "And no errors should have been logged", () => noErrors(page));
    });
    await run.scenario("Corner positions and the visibility come back from a saved project", async () => {
      await session.step(268, "When user sets \"Legend Visibility\" property of histogram viewer to \"Never\"", () => setProperty(page, "Legend Visibility", el("histogram viewer"), "Never"));
      await session.step(269, "And user sets \"Legend Position\" property of bar chart viewer to \"Auto\"", () => setProperty(page, "Legend Position", el("bar chart viewer"), "Auto"));
      await session.step(270, "And user saves the current view as project \"bdd-legend-placement\"", () => saveAsProject(page, "bdd-legend-placement"));
      await session.step(271, "And user closes all views", () => closeAllViews(page));
      await session.step(272, "And user opens the \"bdd-legend-placement\" project", () => openProject(page, "bdd-legend-placement"));
      await session.step(273, "Then the open tableview should have 1 scatter plot viewer", () => viewerCount(page, 1, "scatter plot"));
      await session.step(274, "And the legend of scatter plot viewer should be in the \"leftTop\" slot", () => legendSlot(page, el("scatter plot viewer"), "leftTop"));
      await session.step(275, "And the legend of pie chart viewer should be in the \"leftBottom\" slot", () => legendSlot(page, el("pie chart viewer"), "leftBottom"));
      await session.step(276, "And the legend of box plot viewer should be in the \"rightTop\" slot", () => legendSlot(page, el("box plot viewer"), "rightTop"));
      await session.step(277, "And the legend of line chart viewer should be in the \"rightBottom\" slot", () => legendSlot(page, el("line chart viewer"), "rightBottom"));
      await session.step(278, "And the legend of line chart viewer should be in a corner", () => legendInCorner(page, el("line chart viewer")));
      await session.step(279, "And \"Legend Visibility\" property of histogram viewer should be \"Never\"", () => propertyShouldBe(page, "Legend Visibility", el("histogram viewer"), "Never"));
      await session.step(280, "And legend of histogram viewer should be hidden", () => shouldBe(page, el("legend of histogram viewer"), "hidden"));
      await session.step(281, "And \"Legend Visibility\" property of scatter plot viewer should be \"Always\"", () => propertyShouldBe(page, "Legend Visibility", el("scatter plot viewer"), "Always"));
      await session.step(282, "And \"Legend Position\" property of bar chart viewer should be \"Auto\"", () => propertyShouldBe(page, "Legend Position", el("bar chart viewer"), "Auto"));
      await session.step(283, "And no errors should have been logged", () => noErrors(page));
    });
    run.finish();
  });
});
