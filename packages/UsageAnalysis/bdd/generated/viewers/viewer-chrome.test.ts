/* eslint-disable max-len */
/* eslint-disable comma-spacing */
/* eslint-disable quotes */
/* ---
generated: features/viewers/viewer-chrome.feature
generator: @datagrok-libraries/bdd — do not edit; run `grok-bdd compile` to regenerate
sub_features_covered: [viewers.chrome]
--- */
import {test} from '@playwright/test';
import '../../bindings/connections.js';
import '../../bindings/grid.js';
import '../../bindings/queries.js';
import '../../bindings/spaces.js';
import '../../bindings/tile-viewer.js';
import '../../bindings/trellis-plot.js';
import '@datagrok-libraries/bdd/bindings/common/kinds';
import '@datagrok-libraries/bdd/bindings/common/parameter-types';
import '@datagrok-libraries/bdd/bindings/platform/datasets';
import '@datagrok-libraries/bdd/bindings/platform/elements';
import {loggedIn} from '@datagrok-libraries/bdd/bindings/common/session';
import {clickOn, shouldBe, shouldContainText, shouldHaveText} from '@datagrok-libraries/bdd/bindings/common/steps';
import {openDataset} from '@datagrok-libraries/bdd/bindings/platform/steps';
import {addViewer, closeContextMenu, menuLists, noErrors, painted, propertyShouldBe, propertyShouldNotBe, setProperties, setProperty, viewerCount} from '@datagrok-libraries/bdd/bindings/tiers/viewers/steps';
import {descriptionBelow, dockToViewEdge, dockedAtViewEdge, notDockedAtViewEdge, openViewerHelp, openViewerMenu, pickFromViewerMenu} from '@datagrok-libraries/bdd/bindings/tiers/viewers/widgets';
import {ds, el, feature} from '@datagrok-libraries/bdd/runtime';

test.describe("Viewer chrome — title and description, Pick Up / Apply, the context menu, docking and help", () => {
  const session = feature(test, "features/viewers/viewer-chrome.feature", import.meta.url);
  test("bar chart shows and clears its title and description [viewer=bar chart]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(22, "Given user adds a bar chart viewer", () => addViewer(page, "bar chart"));
    await session.step(23, "When user sets properties of bar chart viewer:", () => setProperties(page, el("bar chart viewer"), [["Show Title","true"],["Title","Demographics"]]), [["Show Title","true"],["Title","Demographics"]]);
    await session.step(26, "Then title of bar chart viewer should have text \"Demographics\"", () => shouldHaveText(page, el("title of bar chart viewer"), "Demographics"));
    await session.step(27, "When user sets properties of bar chart viewer:", () => setProperties(page, el("bar chart viewer"), [["Description","By race"],["Description Visibility Mode","Always"]]), [["Description","By race"],["Description Visibility Mode","Always"]]);
    await session.step(30, "Then description of bar chart viewer should have text \"By race\"", () => shouldHaveText(page, el("description of bar chart viewer"), "By race"));
    await session.step(31, "When user sets \"Description Position\" property of bar chart viewer to \"Bottom\"", () => setProperty(page, "Description Position", el("bar chart viewer"), "Bottom"));
    await session.step(32, "Then description of bar chart viewer should be visible", () => shouldBe(page, el("description of bar chart viewer"), "visible"));
    await session.step(33, "And the description of bar chart viewer should be below its content", () => descriptionBelow(page, el("bar chart viewer")));
    await session.step(34, "When user sets \"Description Position\" property of bar chart viewer to \"Left\"", () => setProperty(page, "Description Position", el("bar chart viewer"), "Left"));
    await session.step(35, "Then description of bar chart viewer should be visible", () => shouldBe(page, el("description of bar chart viewer"), "visible"));
    await session.step(36, "When user sets \"Description Position\" property of bar chart viewer to \"Right\"", () => setProperty(page, "Description Position", el("bar chart viewer"), "Right"));
    await session.step(37, "Then description of bar chart viewer should be visible", () => shouldBe(page, el("description of bar chart viewer"), "visible"));
    await session.step(38, "When user sets \"Description Visibility Mode\" property of bar chart viewer to \"Never\"", () => setProperty(page, "Description Visibility Mode", el("bar chart viewer"), "Never"));
    await session.step(39, "Then description of bar chart viewer should be absent", () => shouldBe(page, el("description of bar chart viewer"), "absent"));
    await session.step(40, "When user sets properties of bar chart viewer:", () => setProperties(page, el("bar chart viewer"), [["Show Title","false"],["Title",""],["Description",""],["Description Visibility Mode","Auto"],["Description Position","Top"]]), [["Show Title","false"],["Title",""],["Description",""],["Description Visibility Mode","Auto"],["Description Position","Top"]]);
    await session.step(46, "Then description of bar chart viewer should be absent", () => shouldBe(page, el("description of bar chart viewer"), "absent"));
    await session.step(47, "And no errors should have been logged", () => noErrors(page));
  });
  test("box plot shows and clears its title and description [viewer=box plot]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(22, "Given user adds a box plot viewer", () => addViewer(page, "box plot"));
    await session.step(23, "When user sets properties of box plot viewer:", () => setProperties(page, el("box plot viewer"), [["Show Title","true"],["Title","Demographics"]]), [["Show Title","true"],["Title","Demographics"]]);
    await session.step(26, "Then title of box plot viewer should have text \"Demographics\"", () => shouldHaveText(page, el("title of box plot viewer"), "Demographics"));
    await session.step(27, "When user sets properties of box plot viewer:", () => setProperties(page, el("box plot viewer"), [["Description","By race"],["Description Visibility Mode","Always"]]), [["Description","By race"],["Description Visibility Mode","Always"]]);
    await session.step(30, "Then description of box plot viewer should have text \"By race\"", () => shouldHaveText(page, el("description of box plot viewer"), "By race"));
    await session.step(31, "When user sets \"Description Position\" property of box plot viewer to \"Bottom\"", () => setProperty(page, "Description Position", el("box plot viewer"), "Bottom"));
    await session.step(32, "Then description of box plot viewer should be visible", () => shouldBe(page, el("description of box plot viewer"), "visible"));
    await session.step(33, "And the description of box plot viewer should be below its content", () => descriptionBelow(page, el("box plot viewer")));
    await session.step(34, "When user sets \"Description Position\" property of box plot viewer to \"Left\"", () => setProperty(page, "Description Position", el("box plot viewer"), "Left"));
    await session.step(35, "Then description of box plot viewer should be visible", () => shouldBe(page, el("description of box plot viewer"), "visible"));
    await session.step(36, "When user sets \"Description Position\" property of box plot viewer to \"Right\"", () => setProperty(page, "Description Position", el("box plot viewer"), "Right"));
    await session.step(37, "Then description of box plot viewer should be visible", () => shouldBe(page, el("description of box plot viewer"), "visible"));
    await session.step(38, "When user sets \"Description Visibility Mode\" property of box plot viewer to \"Never\"", () => setProperty(page, "Description Visibility Mode", el("box plot viewer"), "Never"));
    await session.step(39, "Then description of box plot viewer should be absent", () => shouldBe(page, el("description of box plot viewer"), "absent"));
    await session.step(40, "When user sets properties of box plot viewer:", () => setProperties(page, el("box plot viewer"), [["Show Title","false"],["Title",""],["Description",""],["Description Visibility Mode","Auto"],["Description Position","Top"]]), [["Show Title","false"],["Title",""],["Description",""],["Description Visibility Mode","Auto"],["Description Position","Top"]]);
    await session.step(46, "Then description of box plot viewer should be absent", () => shouldBe(page, el("description of box plot viewer"), "absent"));
    await session.step(47, "And no errors should have been logged", () => noErrors(page));
  });
  test("histogram shows and clears its title and description [viewer=histogram]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(22, "Given user adds a histogram viewer", () => addViewer(page, "histogram"));
    await session.step(23, "When user sets properties of histogram viewer:", () => setProperties(page, el("histogram viewer"), [["Show Title","true"],["Title","Demographics"]]), [["Show Title","true"],["Title","Demographics"]]);
    await session.step(26, "Then title of histogram viewer should have text \"Demographics\"", () => shouldHaveText(page, el("title of histogram viewer"), "Demographics"));
    await session.step(27, "When user sets properties of histogram viewer:", () => setProperties(page, el("histogram viewer"), [["Description","By race"],["Description Visibility Mode","Always"]]), [["Description","By race"],["Description Visibility Mode","Always"]]);
    await session.step(30, "Then description of histogram viewer should have text \"By race\"", () => shouldHaveText(page, el("description of histogram viewer"), "By race"));
    await session.step(31, "When user sets \"Description Position\" property of histogram viewer to \"Bottom\"", () => setProperty(page, "Description Position", el("histogram viewer"), "Bottom"));
    await session.step(32, "Then description of histogram viewer should be visible", () => shouldBe(page, el("description of histogram viewer"), "visible"));
    await session.step(33, "And the description of histogram viewer should be below its content", () => descriptionBelow(page, el("histogram viewer")));
    await session.step(34, "When user sets \"Description Position\" property of histogram viewer to \"Left\"", () => setProperty(page, "Description Position", el("histogram viewer"), "Left"));
    await session.step(35, "Then description of histogram viewer should be visible", () => shouldBe(page, el("description of histogram viewer"), "visible"));
    await session.step(36, "When user sets \"Description Position\" property of histogram viewer to \"Right\"", () => setProperty(page, "Description Position", el("histogram viewer"), "Right"));
    await session.step(37, "Then description of histogram viewer should be visible", () => shouldBe(page, el("description of histogram viewer"), "visible"));
    await session.step(38, "When user sets \"Description Visibility Mode\" property of histogram viewer to \"Never\"", () => setProperty(page, "Description Visibility Mode", el("histogram viewer"), "Never"));
    await session.step(39, "Then description of histogram viewer should be absent", () => shouldBe(page, el("description of histogram viewer"), "absent"));
    await session.step(40, "When user sets properties of histogram viewer:", () => setProperties(page, el("histogram viewer"), [["Show Title","false"],["Title",""],["Description",""],["Description Visibility Mode","Auto"],["Description Position","Top"]]), [["Show Title","false"],["Title",""],["Description",""],["Description Visibility Mode","Auto"],["Description Position","Top"]]);
    await session.step(46, "Then description of histogram viewer should be absent", () => shouldBe(page, el("description of histogram viewer"), "absent"));
    await session.step(47, "And no errors should have been logged", () => noErrors(page));
  });
  test("pc plot shows and clears its title and description [viewer=pc plot]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(22, "Given user adds a pc plot viewer", () => addViewer(page, "pc plot"));
    await session.step(23, "When user sets properties of pc plot viewer:", () => setProperties(page, el("pc plot viewer"), [["Show Title","true"],["Title","Demographics"]]), [["Show Title","true"],["Title","Demographics"]]);
    await session.step(26, "Then title of pc plot viewer should have text \"Demographics\"", () => shouldHaveText(page, el("title of pc plot viewer"), "Demographics"));
    await session.step(27, "When user sets properties of pc plot viewer:", () => setProperties(page, el("pc plot viewer"), [["Description","By race"],["Description Visibility Mode","Always"]]), [["Description","By race"],["Description Visibility Mode","Always"]]);
    await session.step(30, "Then description of pc plot viewer should have text \"By race\"", () => shouldHaveText(page, el("description of pc plot viewer"), "By race"));
    await session.step(31, "When user sets \"Description Position\" property of pc plot viewer to \"Bottom\"", () => setProperty(page, "Description Position", el("pc plot viewer"), "Bottom"));
    await session.step(32, "Then description of pc plot viewer should be visible", () => shouldBe(page, el("description of pc plot viewer"), "visible"));
    await session.step(33, "And the description of pc plot viewer should be below its content", () => descriptionBelow(page, el("pc plot viewer")));
    await session.step(34, "When user sets \"Description Position\" property of pc plot viewer to \"Left\"", () => setProperty(page, "Description Position", el("pc plot viewer"), "Left"));
    await session.step(35, "Then description of pc plot viewer should be visible", () => shouldBe(page, el("description of pc plot viewer"), "visible"));
    await session.step(36, "When user sets \"Description Position\" property of pc plot viewer to \"Right\"", () => setProperty(page, "Description Position", el("pc plot viewer"), "Right"));
    await session.step(37, "Then description of pc plot viewer should be visible", () => shouldBe(page, el("description of pc plot viewer"), "visible"));
    await session.step(38, "When user sets \"Description Visibility Mode\" property of pc plot viewer to \"Never\"", () => setProperty(page, "Description Visibility Mode", el("pc plot viewer"), "Never"));
    await session.step(39, "Then description of pc plot viewer should be absent", () => shouldBe(page, el("description of pc plot viewer"), "absent"));
    await session.step(40, "When user sets properties of pc plot viewer:", () => setProperties(page, el("pc plot viewer"), [["Show Title","false"],["Title",""],["Description",""],["Description Visibility Mode","Auto"],["Description Position","Top"]]), [["Show Title","false"],["Title",""],["Description",""],["Description Visibility Mode","Auto"],["Description Position","Top"]]);
    await session.step(46, "Then description of pc plot viewer should be absent", () => shouldBe(page, el("description of pc plot viewer"), "absent"));
    await session.step(47, "And no errors should have been logged", () => noErrors(page));
  });
  test("Pick Up / Apply copies the scatter plot's Color onto a second scatter plot, and the two stay apart [viewer=scatter plot, property=Color, value=RACE, other=SEX]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(57, "Given user adds a scatter plot viewer", () => addViewer(page, "scatter plot"));
    await session.step(58, "And user adds a scatter plot viewer", () => addViewer(page, "scatter plot"));
    await session.step(59, "Then the open tableview should have 2 scatter plot viewers", () => viewerCount(page, 2, "scatter plot"));
    await session.step(60, "When user sets \"Color\" property of first scatter plot viewer to \"RACE\"", () => setProperty(page, "Color", el("first scatter plot viewer"), "RACE"));
    await session.step(61, "Then \"Color\" property of second scatter plot viewer should not be \"RACE\"", () => propertyShouldNotBe(page, "Color", el("second scatter plot viewer"), "RACE"));
    await session.step(62, "When user picks \"Pick Up / Apply > Pick Up\" from the viewer menu of first scatter plot viewer", () => pickFromViewerMenu(page, "Pick Up / Apply > Pick Up", el("first scatter plot viewer")));
    await session.step(63, "And user picks \"Pick Up / Apply > Apply\" from the viewer menu of second scatter plot viewer", () => pickFromViewerMenu(page, "Pick Up / Apply > Apply", el("second scatter plot viewer")));
    await session.step(64, "Then \"Color\" property of second scatter plot viewer should be \"RACE\"", () => propertyShouldBe(page, "Color", el("second scatter plot viewer"), "RACE"));
    await session.step(65, "When user sets \"Color\" property of first scatter plot viewer to \"SEX\"", () => setProperty(page, "Color", el("first scatter plot viewer"), "SEX"));
    await session.step(66, "Then \"Color\" property of first scatter plot viewer should be \"SEX\"", () => propertyShouldBe(page, "Color", el("first scatter plot viewer"), "SEX"));
    await session.step(67, "And \"Color\" property of second scatter plot viewer should be \"RACE\"", () => propertyShouldBe(page, "Color", el("second scatter plot viewer"), "RACE"));
    await session.step(68, "And no errors should have been logged", () => noErrors(page));
  });
  test("Pick Up / Apply copies the box plot's Category 1 onto a second box plot, and the two stay apart [viewer=box plot, property=Category 1, value=RACE, other=DIS_POP]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(57, "Given user adds a box plot viewer", () => addViewer(page, "box plot"));
    await session.step(58, "And user adds a box plot viewer", () => addViewer(page, "box plot"));
    await session.step(59, "Then the open tableview should have 2 box plot viewers", () => viewerCount(page, 2, "box plot"));
    await session.step(60, "When user sets \"Category 1\" property of first box plot viewer to \"RACE\"", () => setProperty(page, "Category 1", el("first box plot viewer"), "RACE"));
    await session.step(61, "Then \"Category 1\" property of second box plot viewer should not be \"RACE\"", () => propertyShouldNotBe(page, "Category 1", el("second box plot viewer"), "RACE"));
    await session.step(62, "When user picks \"Pick Up / Apply > Pick Up\" from the viewer menu of first box plot viewer", () => pickFromViewerMenu(page, "Pick Up / Apply > Pick Up", el("first box plot viewer")));
    await session.step(63, "And user picks \"Pick Up / Apply > Apply\" from the viewer menu of second box plot viewer", () => pickFromViewerMenu(page, "Pick Up / Apply > Apply", el("second box plot viewer")));
    await session.step(64, "Then \"Category 1\" property of second box plot viewer should be \"RACE\"", () => propertyShouldBe(page, "Category 1", el("second box plot viewer"), "RACE"));
    await session.step(65, "When user sets \"Category 1\" property of first box plot viewer to \"DIS_POP\"", () => setProperty(page, "Category 1", el("first box plot viewer"), "DIS_POP"));
    await session.step(66, "Then \"Category 1\" property of first box plot viewer should be \"DIS_POP\"", () => propertyShouldBe(page, "Category 1", el("first box plot viewer"), "DIS_POP"));
    await session.step(67, "And \"Category 1\" property of second box plot viewer should be \"RACE\"", () => propertyShouldBe(page, "Category 1", el("second box plot viewer"), "RACE"));
    await session.step(68, "And no errors should have been logged", () => noErrors(page));
  });
  test("Pick Up / Apply copies the trellis plot's Viewer Type onto a second trellis plot, and the two stay apart [viewer=trellis plot, property=Viewer Type, value=Bar chart, other=Histogram]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(57, "Given user adds a trellis plot viewer", () => addViewer(page, "trellis plot"));
    await session.step(58, "And user adds a trellis plot viewer", () => addViewer(page, "trellis plot"));
    await session.step(59, "Then the open tableview should have 2 trellis plot viewers", () => viewerCount(page, 2, "trellis plot"));
    await session.step(60, "When user sets \"Viewer Type\" property of first trellis plot viewer to \"Bar chart\"", () => setProperty(page, "Viewer Type", el("first trellis plot viewer"), "Bar chart"));
    await session.step(61, "Then \"Viewer Type\" property of second trellis plot viewer should not be \"Bar chart\"", () => propertyShouldNotBe(page, "Viewer Type", el("second trellis plot viewer"), "Bar chart"));
    await session.step(62, "When user picks \"Pick Up / Apply > Pick Up\" from the viewer menu of first trellis plot viewer", () => pickFromViewerMenu(page, "Pick Up / Apply > Pick Up", el("first trellis plot viewer")));
    await session.step(63, "And user picks \"Pick Up / Apply > Apply\" from the viewer menu of second trellis plot viewer", () => pickFromViewerMenu(page, "Pick Up / Apply > Apply", el("second trellis plot viewer")));
    await session.step(64, "Then \"Viewer Type\" property of second trellis plot viewer should be \"Bar chart\"", () => propertyShouldBe(page, "Viewer Type", el("second trellis plot viewer"), "Bar chart"));
    await session.step(65, "When user sets \"Viewer Type\" property of first trellis plot viewer to \"Histogram\"", () => setProperty(page, "Viewer Type", el("first trellis plot viewer"), "Histogram"));
    await session.step(66, "Then \"Viewer Type\" property of first trellis plot viewer should be \"Histogram\"", () => propertyShouldBe(page, "Viewer Type", el("first trellis plot viewer"), "Histogram"));
    await session.step(67, "And \"Viewer Type\" property of second trellis plot viewer should be \"Bar chart\"", () => propertyShouldBe(page, "Viewer Type", el("second trellis plot viewer"), "Bar chart"));
    await session.step(68, "And no errors should have been logged", () => noErrors(page));
  });
  test("Pick Up / Apply copies the line chart's lineWidth onto a second line chart, and the two stay apart [viewer=line chart, property=lineWidth, value=3, other=5]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(57, "Given user adds a line chart viewer", () => addViewer(page, "line chart"));
    await session.step(58, "And user adds a line chart viewer", () => addViewer(page, "line chart"));
    await session.step(59, "Then the open tableview should have 2 line chart viewers", () => viewerCount(page, 2, "line chart"));
    await session.step(60, "When user sets \"lineWidth\" property of first line chart viewer to \"3\"", () => setProperty(page, "lineWidth", el("first line chart viewer"), "3"));
    await session.step(61, "Then \"lineWidth\" property of second line chart viewer should not be \"3\"", () => propertyShouldNotBe(page, "lineWidth", el("second line chart viewer"), "3"));
    await session.step(62, "When user picks \"Pick Up / Apply > Pick Up\" from the viewer menu of first line chart viewer", () => pickFromViewerMenu(page, "Pick Up / Apply > Pick Up", el("first line chart viewer")));
    await session.step(63, "And user picks \"Pick Up / Apply > Apply\" from the viewer menu of second line chart viewer", () => pickFromViewerMenu(page, "Pick Up / Apply > Apply", el("second line chart viewer")));
    await session.step(64, "Then \"lineWidth\" property of second line chart viewer should be \"3\"", () => propertyShouldBe(page, "lineWidth", el("second line chart viewer"), "3"));
    await session.step(65, "When user sets \"lineWidth\" property of first line chart viewer to \"5\"", () => setProperty(page, "lineWidth", el("first line chart viewer"), "5"));
    await session.step(66, "Then \"lineWidth\" property of first line chart viewer should be \"5\"", () => propertyShouldBe(page, "lineWidth", el("first line chart viewer"), "5"));
    await session.step(67, "And \"lineWidth\" property of second line chart viewer should be \"3\"", () => propertyShouldBe(page, "lineWidth", el("second line chart viewer"), "3"));
    await session.step(68, "And no errors should have been logged", () => noErrors(page));
  });
  test("Tooltip > Edit... opens the tooltip editor of the line chart, and CANCEL closes it [viewer=line chart, dialog=Edit Aggregated Tooltip]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(78, "Given user adds a line chart viewer", () => addViewer(page, "line chart"));
    await session.step(79, "Then line chart viewer should be painted", () => painted(page, el("line chart viewer")));
    await session.step(80, "When user picks \"Tooltip > Edit...\" from the viewer menu of line chart viewer", () => pickFromViewerMenu(page, "Tooltip > Edit...", el("line chart viewer")));
    await session.step(81, "Then \"Edit Aggregated Tooltip\" dialog should be visible", () => shouldBe(page, el("\"Edit Aggregated Tooltip\" dialog"), "visible"));
    await session.step(82, "When user clicks on CANCEL button in \"Edit Aggregated Tooltip\" dialog", () => clickOn(page, el("CANCEL button in \"Edit Aggregated Tooltip\" dialog")));
    await session.step(83, "Then \"Edit Aggregated Tooltip\" dialog should be absent", () => shouldBe(page, el("\"Edit Aggregated Tooltip\" dialog"), "absent"));
    await session.step(84, "And no errors should have been logged", () => noErrors(page));
  });
  test("Tooltip > Edit... opens the tooltip editor of the box plot, and CANCEL closes it [viewer=box plot, dialog=Edit Tooltip]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(78, "Given user adds a box plot viewer", () => addViewer(page, "box plot"));
    await session.step(79, "Then box plot viewer should be painted", () => painted(page, el("box plot viewer")));
    await session.step(80, "When user picks \"Tooltip > Edit...\" from the viewer menu of box plot viewer", () => pickFromViewerMenu(page, "Tooltip > Edit...", el("box plot viewer")));
    await session.step(81, "Then \"Edit Tooltip\" dialog should be visible", () => shouldBe(page, el("\"Edit Tooltip\" dialog"), "visible"));
    await session.step(82, "When user clicks on CANCEL button in \"Edit Tooltip\" dialog", () => clickOn(page, el("CANCEL button in \"Edit Tooltip\" dialog")));
    await session.step(83, "Then \"Edit Tooltip\" dialog should be absent", () => shouldBe(page, el("\"Edit Tooltip\" dialog"), "absent"));
    await session.step(84, "And no errors should have been logged", () => noErrors(page));
  });
  test("Tooltip > Edit... opens the tooltip editor of the pc plot, and CANCEL closes it [viewer=pc plot, dialog=Edit Tooltip]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(78, "Given user adds a pc plot viewer", () => addViewer(page, "pc plot"));
    await session.step(79, "Then pc plot viewer should be painted", () => painted(page, el("pc plot viewer")));
    await session.step(80, "When user picks \"Tooltip > Edit...\" from the viewer menu of pc plot viewer", () => pickFromViewerMenu(page, "Tooltip > Edit...", el("pc plot viewer")));
    await session.step(81, "Then \"Edit Tooltip\" dialog should be visible", () => shouldBe(page, el("\"Edit Tooltip\" dialog"), "visible"));
    await session.step(82, "When user clicks on CANCEL button in \"Edit Tooltip\" dialog", () => clickOn(page, el("CANCEL button in \"Edit Tooltip\" dialog")));
    await session.step(83, "Then \"Edit Tooltip\" dialog should be absent", () => shouldBe(page, el("\"Edit Tooltip\" dialog"), "absent"));
    await session.step(84, "And no errors should have been logged", () => noErrors(page));
  });
  test("The pc plot's context menu offers the General and Tooltip groups, and General clones and closes it [viewer=pc plot, own item=Columns > Columns]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(93, "Given user adds a pc plot viewer", () => addViewer(page, "pc plot"));
    await session.step(94, "When user opens the viewer menu of pc plot viewer", () => openViewerMenu(page, el("pc plot viewer")));
    await session.step(95, "Then the open menu should list \"General > Clone\"", () => menuLists(page, "General > Clone"));
    await session.step(96, "And the open menu should list \"General > Full Screen\"", () => menuLists(page, "General > Full Screen"));
    await session.step(97, "And the open menu should list \"General > Save to Gallery\"", () => menuLists(page, "General > Save to Gallery"));
    await session.step(98, "And the open menu should list \"General > Save as PNG\"", () => menuLists(page, "General > Save as PNG"));
    await session.step(99, "And the open menu should list \"General > Embed...\"", () => menuLists(page, "General > Embed..."));
    await session.step(100, "And the open menu should list \"General > Close\"", () => menuLists(page, "General > Close"));
    await session.step(101, "And the open menu should list \"Tooltip > Edit...\"", () => menuLists(page, "Tooltip > Edit..."));
    await session.step(102, "And the open menu should list \"Columns > Columns\"", () => menuLists(page, "Columns > Columns"));
    await session.step(103, "When user closes the context menu", () => closeContextMenu(page));
    await session.step(104, "And user picks \"General > Clone\" from the viewer menu of pc plot viewer", () => pickFromViewerMenu(page, "General > Clone", el("pc plot viewer")));
    await session.step(105, "Then the open tableview should have 2 pc plot viewers", () => viewerCount(page, 2, "pc plot"));
    await session.step(106, "When user picks \"General > Close\" from the viewer menu of second pc plot viewer", () => pickFromViewerMenu(page, "General > Close", el("second pc plot viewer")));
    await session.step(107, "Then the open tableview should have 1 pc plot viewer", () => viewerCount(page, 1, "pc plot"));
    await session.step(108, "And no errors should have been logged", () => noErrors(page));
  });
  test("The trellis plot's context menu offers the General and Tooltip groups, and General clones and closes it [viewer=trellis plot, own item=Pick Up / Apply > Pick Up]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(93, "Given user adds a trellis plot viewer", () => addViewer(page, "trellis plot"));
    await session.step(94, "When user opens the viewer menu of trellis plot viewer", () => openViewerMenu(page, el("trellis plot viewer")));
    await session.step(95, "Then the open menu should list \"General > Clone\"", () => menuLists(page, "General > Clone"));
    await session.step(96, "And the open menu should list \"General > Full Screen\"", () => menuLists(page, "General > Full Screen"));
    await session.step(97, "And the open menu should list \"General > Save to Gallery\"", () => menuLists(page, "General > Save to Gallery"));
    await session.step(98, "And the open menu should list \"General > Save as PNG\"", () => menuLists(page, "General > Save as PNG"));
    await session.step(99, "And the open menu should list \"General > Embed...\"", () => menuLists(page, "General > Embed..."));
    await session.step(100, "And the open menu should list \"General > Close\"", () => menuLists(page, "General > Close"));
    await session.step(101, "And the open menu should list \"Tooltip > Edit...\"", () => menuLists(page, "Tooltip > Edit..."));
    await session.step(102, "And the open menu should list \"Pick Up / Apply > Pick Up\"", () => menuLists(page, "Pick Up / Apply > Pick Up"));
    await session.step(103, "When user closes the context menu", () => closeContextMenu(page));
    await session.step(104, "And user picks \"General > Clone\" from the viewer menu of trellis plot viewer", () => pickFromViewerMenu(page, "General > Clone", el("trellis plot viewer")));
    await session.step(105, "Then the open tableview should have 2 trellis plot viewers", () => viewerCount(page, 2, "trellis plot"));
    await session.step(106, "When user picks \"General > Close\" from the viewer menu of second trellis plot viewer", () => pickFromViewerMenu(page, "General > Close", el("second trellis plot viewer")));
    await session.step(107, "Then the open tableview should have 1 trellis plot viewer", () => viewerCount(page, 1, "trellis plot"));
    await session.step(108, "And no errors should have been logged", () => noErrors(page));
  });
  test("The pc plot docks along the left edge and opens its help from the title bar [viewer=pc plot, help=Parallel coordinates plot]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(116, "Given user adds a pc plot viewer", () => addViewer(page, "pc plot"));
    await session.step(117, "Then pc plot viewer should not be docked along the left edge of the view", () => notDockedAtViewEdge(page, el("pc plot viewer"), "left"));
    await session.step(118, "When user docks pc plot viewer to the left edge of the view", () => dockToViewEdge(page, el("pc plot viewer"), "left"));
    await session.step(119, "Then pc plot viewer should be docked along the left edge of the view", () => dockedAtViewEdge(page, el("pc plot viewer"), "left"));
    await session.step(120, "When user opens the help of pc plot viewer", () => openViewerHelp(page, el("pc plot viewer")));
    await session.step(121, "Then help panel should contain text \"Parallel coordinates plot\"", () => shouldContainText(page, el("help panel"), "Parallel coordinates plot"));
    await session.step(122, "And no errors should have been logged", () => noErrors(page));
  });
  test("The trellis plot docks along the left edge and opens its help from the title bar [viewer=trellis plot, help=Trellis]", {tag: ["@viewers", "@realizes:viewers.chrome"]}, async ({browser}) => {
    const page = await session.page(browser);
    await session.step(18, "Given user is logged in", () => loggedIn(page));
    await session.step(19, "And user opens demog-1000 dataset", () => openDataset(page, ds("demog-1000")));
    await session.step(116, "Given user adds a trellis plot viewer", () => addViewer(page, "trellis plot"));
    await session.step(117, "Then trellis plot viewer should not be docked along the left edge of the view", () => notDockedAtViewEdge(page, el("trellis plot viewer"), "left"));
    await session.step(118, "When user docks trellis plot viewer to the left edge of the view", () => dockToViewEdge(page, el("trellis plot viewer"), "left"));
    await session.step(119, "Then trellis plot viewer should be docked along the left edge of the view", () => dockedAtViewEdge(page, el("trellis plot viewer"), "left"));
    await session.step(120, "When user opens the help of trellis plot viewer", () => openViewerHelp(page, el("trellis plot viewer")));
    await session.step(121, "Then help panel should contain text \"Trellis\"", () => shouldContainText(page, el("help panel"), "Trellis"));
    await session.step(122, "And no errors should have been logged", () => noErrors(page));
  });
});
