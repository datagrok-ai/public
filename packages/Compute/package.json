{
	"name": "compute",
	"beta": true,
	"friendlyName": "Compute",
	"version": "0.0.1",
	"description": "",
	"dependencies": {
		"datagrok-api": "../../js-api",
		"cash-dom": "latest",
		"dayjs": "latest",
		"ts-loader": "latest",
		"typescript": "latest"
	},
	"devDependencies": {
		"webpack": "latest",
		"webpack-cli": "latest"
	},
	"scripts": {
		"debug-compute": "grok publish --rebuild",
		"release-compute": "grok publish --rebuild --release",
		"build-compute": "webpack",
		"build": "webpack",
		"debug-compute-dev": "grok publish dev --rebuild",
		"release-compute-dev": "grok publish dev --rebuild --release",
		"debug-compute-local": "grok publish local --rebuild",
		"release-compute-local": "grok publish local --rebuild --release",
		"debug-compute-localhost": "grok publish localhost --rebuild",
		"release-compute-localhost": "grok publish localhost --rebuild --release",
		"debug-compute-compose": "grok publish compose --rebuild",
		"release-compute-compose": "grok publish compose --rebuild --release"
	}
}
