# Peptides → bdd survey A (specs: peptides, sar, info-panels, peptide-space, sar-viewer-lifecycle)

Read-only survey by an agent, 2026-09-11. Verified afterwards on the stand: the d4 accordion pane
header ALREADY sets `aria-expanded` (`accordion.dart:351, 365`), so the "needs-core" for
expanding a pane below is wrong; `user expands Peptides pane` / `should be expanded` work as is.
The pane's WebLogo has NO `viewer-WebLogo` name on the stand (the probe found none) — the package
must set one. The settings wrench has `aria-label="Peptides analysis settings"` (matched by the
`icon` kind).

## 0. Corrections to the brief

- **The fixture is not under `public/`.** `find public -name peptides.csv` is empty; the file is `C:/Users/rizhi/Desktop/GROK-CORE/reddata/data/demo/bio/peptides.csv` (repo root, git-tracked in reddata). 647 rows; columns `ID` (int 1..651), `AlignedSequence` (text, 464 distinct, separator `-`, every row 17 tokens `NH2-…-COOH`, 29–38 chars), `IC50` (float, min 4.14e-7, max 0.979, no empties). The panel's Activity default is **IC50** (`src/widgets/peptides.ts:70-71`: no `activity` column, so `df.col('IC50')`); Scaling is enabled because `IC50.stats.min > 0` (`peptides.ts:89`); Scaling items are `none | lg | -lg` (`src/utils/constants.ts:58-62`).
- **The WebLogo status pattern is not in the bio library.** `grep getWidgetStatus public/libraries/bio/src` finds nothing; the JS-viewer pattern lives in the **Bio package**: `public/packages/Bio/src/viewers/web-logo-viewer.ts:1007-1040` (`isRenderPending`, `getWidgetStatus` with `parts.canvas`, `hitAreas['monomer M at position N']`, `values['rows selected']`) and `:1396-1398` (`onRendered`). Same shape in `packages/Bio/src/analysis/sequence-search-base-viewer.ts:32-49`.
- **`input-Stack-split-categories` is not a Peptides control.** It is the Dart histogram's hover-revealed in-plot checkbox (`core/client/d4/lib/src/viewers/histogram/histogram_core.dart:540-548`, tooltip `Stack split categories`, hover-visible only per `:624`) inside the Distribution pane's throwaway histogram. The Distribution widget's own inputs are `input-Monomers`, `input-Positions`, `input-Clusters` (`src/widgets/distribution.ts:63-72`, caption = key minus `separate`), enabled only when a monomer-position selection exists (`:79-81`).
- **`peptide-space.test.ts` never touches Sequence space.** It launches SAR from the top menu and re-runs MCL. The `Sequence space` toggle (`settings.ts:123`) is exercised by no old spec.
- **`public/packages/Peptides/bdd/` is already scaffolded** (`smoke.feature`, `bindings/elements.ts` with a `Peptides app` context on `[data-u2-name="Peptides"]`, `bindings/steps.ts` `user opens the Peptides app`). The app view (`src/package.ts:113-123`) stamps no `data-u2-name`, so that step can never pass — drop it or fix the app.
- **`the top menu command should have completed` is not an analysis-done marker.** `Bio > Analyze > SAR...` runs `peptidesDialog` (`package.ts:127-150`) which returns the dialog; the analysis starts from OK. And `COMMAND_CAP` (2 min, `bindings/platform/commands.ts:12`) applies only to that call; every other `expect` is the 5 s default. MCL takes 60–80 s on dev (`peptide-space-run.md:40`), so "viewers attach" needs its own budget (see §2).

## 1. Per old spec, per softStep

Legend: **direct** = existing phrase; **needs-core** = a name/reading/area/event first; **manual** = no UI path; **vacuous** = passes with the behaviour broken.

### `peptides.test.ts` (Peptides — SAR parameters and WebLogo)

| what it asserts | verdict | Gherkin |
|---|---|---|
| Steps 1-2: rows 647, `AlignedSequence.semType === 'Macromolecule'` (after 3 s + 5 s + 40 s loops, package `.load()` calls, `body.selenium`) | direct | `Given user opens peptides dataset` (NEW dataset entry `System:DemoFiles/bio/peptides.csv`) · `Then the table should have 647 rows` · `"AlignedSequence" column should have semantic type "Macromolecule"` |
| Step 3: pane `[name=pane-Peptides]` attached; header gets `expanded` after click; `.bio-wl-host canvas` exists; `input-Scaling`, `input-Activity`, `input-Clusters` exist | vacuous (existence, not shown; `hasWebLogo` = a canvas element) / needs-core for expand | `Given the context panel is open` · `When user clicks on the "header AlignedSequence" area of grid` · `Then the context panel should show "AlignedSequence"` · `When user expands Peptides pane in context panel` (**needs-core**: Dart pane header has class `expanded`, no `aria-expanded` — `core/client/d4/lib/src/widgets/accordion/accordion.dart:229,362-368`; `readExpanded` knows only `aria-expanded`/tree twisties, `bdd/src/runtime/gestures.ts:336-346`) [WRONG — aria-expanded exists, see the note at the top] · `Then Peptides pane in context panel should be expanded` · `And "Launch SAR" button in Peptides pane should be visible` · `And editor of Activity column input in Peptides pane should have text "IC50"` · `And Scaling choice input in Peptides pane should be enabled` · `And "Generate clusters" checkbox in Peptides pane should be checked` · `And WebLogo viewer in Peptides pane should be painted` · `And WebLogo viewer in Peptides pane should have a "position 5" area` |
| Steps 4-5: `selectOption('lg')` then 1.5 s; `sel.value === 'lg'` (what it wrote); `reRendered` = WebLogo canvas and `viewer-Histogram` exist (they existed before the change) | vacuous | `When user selects "lg" in Scaling choice input in Peptides pane` · `Then Scaling choice input in Peptides pane should have value "lg"` · pair with a reading that only moves when the histogram was rebuilt on the scaled column: `the "axis max" reading of Histogram viewer in Peptides pane should be lower than 0` — **NEW phrase** `the {string} reading of {widget} should be lower than {float}` (histogram readings `axis min/max`, `core/client/d4/lib/src/viewers/histogram/CLAUDE.md:51-55`; lg(IC50) ≤ 0, none-scaled max ≈ 0.98) [or the existing `should be between -7 and 0`]. The histogram root is *replaced* on every change (`peptides.ts:83-84`), so `repainted` / `remembered value range` (keyed by root) cannot be used — an open question in §5 |
| Steps 6-9: five synthetic MouseEvents at 40 %/45 % of the canvas, 800 ms sleep, `clicked:true` (always), `selectedRows > 0` | vacuous gesture, real assertion; hits an unknown glyph | `Given no rows should be selected` · `When user clicks on the "monomer T at position 5" area of WebLogo viewer in Peptides pane` · `Then 630 rows should be selected` · `And the "rows selected" reading of WebLogo viewer in Peptides pane should be 630` · `And only rows where "AlignedSequence" ... ` — no existing phrase for "token at position N"; **NEW (package)**: `only rows with {string} at position {int} of {string} column should be selected` (split on the column's separator). Counts from the file (token index 1-based): pos 5 `T` 630, pos 13 `N` 643, pos 3 `A` 535, pos 6 `W` 136, pos 7 `Q` 130 — provided the splitter numbers `NH2` as position 1 (§5). Bio's `composition.feature:28-35` is the exact pattern. |

### `info-panels.test.ts` (Peptides — Info Panels)

| what it asserts | verdict | Gherkin |
|---|---|---|
| Setup: rows 647, semType, `.grok-prop-panel` exists | direct | as above + `Given the context panel is open` |
| Step 1: `col.getTag('cell.renderer') === 'sequence'` | direct | `"AlignedSequence" column should have tag "cell.renderer" equal to "sequence"` and the shown side: `the "cell type of AlignedSequence" reading of grid should be "sequence"` (Bio `render/renderers.feature:26`) |
| Step 2: `df.currentCol = col; grok.shell.o = col` + retry, then `pane-Details` and `pane-Peptides` present | vacuous (API sets the object; a 1.5 s sleep) | `When user clicks on the "header AlignedSequence" area of grid` · `Then the context panel should show "AlignedSequence"` · `Then Details pane in context panel should be visible` · `And Peptides pane in context panel should be visible` |
| Step 3: `toHaveCount(1)` each | direct (presence) | same as above (`visible` is stronger than count) |
| Step 4: click headers unless `.expanded`, 2.5 s sleep, `.expanded` count 1 | direct (aria-expanded exists) | `When user expands Details pane in context panel` / `Peptides pane …` · `Then … should be expanded` |
| Step 5: Details innerText contains `Data type`, `Semantic type`; Peptides text non-empty, has `button-Launch-SAR`, `input-host-Activity`, text `LAUNCH SAR`/`Activity` | direct | `Then Details pane in context panel should contain text "Data type"` · `… "Semantic type"` · `"Launch SAR" button in Peptides pane should be visible` · `Activity column input in Peptides pane should be visible` |

### `sar.test.ts` (SAR — context-panel entry path)

| what it asserts | verdict | Gherkin |
|---|---|---|
| Setup: sleeps 4 s+4 s, `Peptides:initPeptides` prewarm | direct | `Given the Peptides package is initialized` — **NEW (package)**: `grok.functions.call('Peptides:initPeptides')` (the `@init`, `package.ts:83-92`), same idea as Bio's `bindings/steps.ts:16-18` |
| Sc.1 steps 1-4: pane found, `button-Launch-SAR` found, click, `waitForFunction(temp.peptidesModel)` 60 s | direct except the wait | `When user clicks on "Launch SAR" button in Peptides pane` · `Then Sequence Variability Map viewer should be added to the open tableview` · `And Most Potent Residues viewer should be added to the open tableview` (both are added synchronously by `startAnalysis`, `peptides.ts:311-312`) |
| Sc.1 step 5: `waitForViewers(['Sequence Variability Map','Most Potent Residues','MCL','Logo Summary Table'])` up to 120 s (`helpers.ts:22-32`, timeout swallowed) | needs-core (a completion signal + budget) | `Then the SAR clustering should have completed` — **NEW (package)**, waits ≤ 180 s on a `peptides-mcl-clustered` DOM event the model dispatches after `addMCLClusters` finishes (`src/model.ts:1430-1437`, after the LST re-render) — then `MCL viewer should be added to the open tableview` · `Logo Summary Table viewer should be added to the open tableview` (LST is only added after MCL: `peptides.ts:333-345`) · `a new column matching "^Cluster \(MCL\)" should have been added` (MCL adds 5 hidden columns, `model.ts:1358-1363`) · `no error or warning balloon should have been shown` |
| Sc.2 steps 6-8: wrench `i.fa-wrench[aria-label="Peptides analysis settings"]` click, 2 s, `dialog-Peptides-settings`; expand MCL pane by header text; write `inner.value='50'` + dispatch `input`/`change`; `newVal === '50'` (checks what it wrote); OK; closed | vacuous value check, dispatched events | `When user clicks on "Peptides analysis settings" icon` (aria-label set by `ui.iconFA`, `js-api/ui.ts:258-261`; ribbon `model.ts:1115-1116`) · `Then "Peptides settings" dialog should be visible` · `And "Similarity Threshold" number input in "Peptides settings" dialog should have value "70"` (`types.ts:44`; the dialog shows `threshold ?? 80`, `settings.ts:291`) · `When user enters "50" into "Similarity Threshold" number input in "Peptides settings" dialog` (scope by dialog: the panel's hidden MCL form has an input of the same caption, `peptides.ts:125-129`) · `And user clicks on OK button in "Peptides settings" dialog` · `Then the "Peptides settings" dialog should close` |
| Sc.2 step 9: 8 s sleep; model present; SVM/MPR types present; SVM has a canvas; `lastError` !~ `/setTrue\|null/` | vacuous (canvas exists; only the *last* error) | `Then the analysis setting "mclSettings.threshold" should be "50"` — **NEW (package)**: reads the `settings` tag JSON (`model.ts:176-181`) · `And the SAR clustering should have completed` (a second event) · `And the open tableview should have 1 MCL viewer` (old one closed, `model.ts:1345-1350`) · `And Sequence Variability Map viewer should be visible` · `And no errors should have been logged` · `And no error or warning balloon should have been shown` |
| Sc.3 step 10: `im.click()` / `mc.click()` dispatch + 1.5 s, checks the radios' `checked` | vacuous w.r.t. the mode (reads the input it clicked; never the viewer) | `When user checks "Invariant Map" checkbox in Sequence Variability Map viewer` (`ui.input.bool` roots `.ui-input-bool`, editor retyped to `radio`, `sar-viewer.ts:1306-1327`; click handler on the root `:1308,1315`) · `Then the "mode" reading of Sequence Variability Map viewer should be "Invariant Map"` (**needs-core**) · `And Sequence Variability Map viewer should have repainted` (**needs-core**: `parts.canvas` = inner grid canvas) · then back to Mutation Cliffs |
| Sc.3 steps 11-12: synthetic click at (120,120) of the largest canvas, 2.5 s; `selAfter > selBefore`; `pane-Mutation-Cliffs-pairs` and `pane-Distribution` in `[name^=pane-]` | vacuous gesture (a guessed cell; in Mutation Cliffs mode only a cell with cliffs selects, `sar-viewer.ts:1244-1249`) | in Invariant Map mode (any cell with stats selects, `:1239-1242`): `When user clicks on the "cell T at 5" area of Sequence Variability Map viewer` (**needs-core**) · `Then 630 rows should be selected` · `And the "selected cells" reading of Sequence Variability Map viewer should be 1` (**needs-core**) · `And Mutation Cliffs pairs pane in context panel should be visible` · `And Distribution pane in context panel should be visible` · `And context panel should contain text "selected rows"` (`model.ts:511`). `the context panel should show "…"` cannot be used: the model sets `grok.shell.o = acc.root`, an HTMLDivElement (`model.ts:910`), and the step reads `constructor.name "name"` (`bindings/platform/steps.ts:193-199`). Note both panes appear whenever the accordion source is a SAR viewer with cliffs computed (`model.ts:614-627,633`) — the selection count is the real check |
| Sc.4 step 13: expand `pane-Distribution`, click `input-Stack-split-categories`, state flipped, `childCount > 5` | vacuous (a platform histogram control; child count) | `When user checks Positions checkbox in Distribution pane` (`distribution.ts:67`, enabled since a monomer-position selection exists) · `Then Distribution pane in context panel should contain text "Positions"` (a `ui.h1('Positions')` section is appended, `:194`) · `And Histogram viewer in Distribution pane should be painted` |

### `peptide-space.test.ts` (top-menu SAR + MCL re-run)

| what it asserts | verdict | Gherkin |
|---|---|---|
| Setup | direct | as above |
| Sc.1 step 1: `div-Bio` click, `mouseenter/mousemove` dispatch on `div-Bio---Analyze`, `div-Bio---Analyze---SAR...` click, 0.7/0.7/2.5 s sleeps, `offsetParent` check, `dialog-Analyze-Peptides` found | vacuous gesture | `When user picks "Bio > Analyze > SAR..." from the top menu` · `Then "Analyze Peptides" dialog should be visible` · `And editor of Sequence column input in "Analyze Peptides" dialog should have text "AlignedSequence"` · `And editor of Activity column input in "Analyze Peptides" dialog should have text "IC50"` · `And Scaling choice input in "Analyze Peptides" dialog should have value "none"` · `And "Generate clusters" checkbox in "Analyze Peptides" dialog should be checked` |
| Sc.1 step 2: OK = `dlg [name=button-OK]` **or any** `button-OK` on the page; `waitForFunction(peptidesModel)` 90 s; `waitForViewers` 120 s | direct + needs-core budget | `When user clicks on OK button in "Analyze Peptides" dialog` · viewers added / clustering completed as in sar Sc.1 |
| Sc.2 step 3: wrench, 2 s, dialog, pane headers include `MCL` | direct | `… "Peptides analysis settings" icon` · `"Peptides settings" dialog should be visible` · `MCL pane in "Peptides settings" dialog should be visible` (`pane-MCL`, `accordion.dart:341`) |
| Sc.2 step 4: expand MCL pane by text; `inflationBefore === 1.4` from `model._settings` (private); write `2.5` + dispatch; `newVal === '2.5'`; OK; closed | vacuous value check; API read | `"Inflation Factor" number input in "Peptides settings" dialog should have value "1.4"` (`types.ts:43`) · `When user enters "2.5" into "Inflation Factor" number input in "Peptides settings" dialog` · OK · `the "Peptides settings" dialog should close` |
| Sc.2 step 5: 12 s sleep; `inflationAfter === 2.5` (private); `MCL` type present; `getImageData` scan of the largest canvas (every 41st byte, >50 non-white; any exception → false); `lastError` regex | vacuous pixel scan, private reads | `Then the analysis setting "mclSettings.inflation" should be "2.5"` · `And the SAR clustering should have completed` · `And the open tableview should have 1 MCL viewer` · `And scatter plot viewer in MCL viewer should be painted` (**needs-library**, §2: the runtime resolves the *outer* MCL viewer for the inner Dart scatter plot) · `And no errors should have been logged` |

### `sar-viewer-lifecycle.test.ts`

| what it asserts | verdict | Gherkin |
|---|---|---|
| Setup + Sc.1 steps 1-3: top-menu dispatch hack, OK fallback, `peptidesModel` wait | as peptide-space | same |
| Sc.1 step 4: 4 viewers via `waitForViewers`; `model.findViewer(type)` non-null for SVM/MPR/MCL | direct (`findViewer` is `tv.viewers` by type, `model.ts:1193-1195`, identical to the step's read) | `{viewer} viewer should be added to the open tableview` ×4 (after the clustering event) |
| Sc.1 step 4b: `await model.addClusterMaxActivityViewer()` + 6 s; type present | manual (a direct model call, not a UI path) | replace with the Settings toggle (below) |
| Sc.1 step 5: `input-Dendrogram` `isEnabled()`; click; OK; poll `tv.grid.temp['__dendrogram_neighbor_temp__']` 120 s | vacuous enabled check (`settings.ts:139` compares `getTreeHelperInstance() !== null`, but `treeHelper` is an uninitialized `let` in `package.ts:25` → `undefined !== null` → always enabled); the attach read is API (`Dendrogram/src/utils/hierarchical-clustering.ts:14,221`) | **needs-core**: the dendrogram is a `GridNeighbor`, not a viewer; it needs a `name` on its root before a feature can claim `Dendrogram … should be visible`. Also a product bug: `closeViewer(VIEWER_TYPE.DENDROGRAM)` (`model.ts:268,1046-1050`) never finds it, and `isDendrogramEnabled` (`settings.ts:118`) never reads true → the toggle-off path is a no-op and the dialog reopens unchecked. Recommend `@known-failure` scenario or leave it out; requires the Dendrogram package on the stand |
| Sc.2 steps 1-2: wrench; pane names contain General/Viewers/MCL; Viewers pane checkboxes named `input-Dendrogram`, `input-Sequence-space`, `input-Active-peptide-selection`; Cancel | direct | `General pane in "Peptides settings" dialog should be visible` (…Viewers, MCL) · `Dendrogram checkbox in "Peptides settings" dialog should be present` · `"Sequence space" checkbox …` · `"Active peptide selection" checkbox … should be unchecked` (`settings.ts:121`: `!!settings.showClusterMaxActivity`, unset by `startAnalysis`) · `When user clicks on CANCEL button in "Peptides settings" dialog` |
| Sc.2 steps 3-4: precondition CMA attached (from the direct call); click-twice OFF via composed `MouseEvent` dispatch; OK dispatch; poll 12×500 ms; `settings.showClusterMaxActivity === false`; `findViewer(CMA)` null; SVM/MPR persist | vacuous ordering (OFF of something the UI never turned on), dispatched events, private reads | UI order: `When user checks "Active peptide selection" checkbox in "Peptides settings" dialog` · OK · `Then Active peptide selection viewer should be added to the open tableview` (`model.ts:270-272 → 1217-1234`, docked below LST) · `And the analysis setting "showClusterMaxActivity" should be "true"` · reopen · `When user unchecks …` · OK · `Then the open tableview should have 0 Active peptide selection viewers` · `And the open tableview should have 1 Sequence Variability Map viewer` · `… 1 Most Potent Residues viewer` |
| Sc.2 steps 5-7: click-once ON, 16×500 ms poll, `findViewer(CMA)` non-null, SVM/MPR/MCL persist | direct (covered by the order above) | as above |
| Sc.2 step 8: `lastError` !~ `/setTrue\|fire.*on (null\|undefined)\|Cannot read…/` | vacuous (the last error only) | `no errors should have been logged` · `no error or warning balloon should have been shown` |

## 2. Signals to add (old hack → replacement → where it comes from)

**Peptides viewers — none has `getWidgetStatus` / `isRenderPending` / `onRendered` today.** Both SAR viewers render into an inner Dart `DG.Grid` (`sar-viewer.ts:1073-1078` MonomerPosition, `:1505-1510` MostPotentResidues; `render()` empties the root and appends `this.viewerGrid.root`, `:1286-1348`, `:1647-1662`). The Dart grid already reports `cell <tableRow> of <col>`, `text of cell`, `color of cell`, `current row`, `rows shown` and `isRenderPending` (`core/client/d4/lib/src/viewers/grid/CLAUDE.md:319-339, 396-400`), so the viewer status is a translation of the grid's, not a new geometry.

| old hack | replacement | source |
|---|---|---|
| synthetic click at (120,120) of the SVM's largest canvas | `hitAreas['cell <monomer> at <position>']` for every visible inner-grid cell of a position column, translated from the grid's `cell <r> of <pos>` with monomer = `AAR` value of table row r (`getMonomerPosition`, `sar-viewer.ts:1279-1284`); `parts.canvas` = the inner grid's canvas so areas are in its CSS px; skip cells `renderCell` leaves blank (`:1706-1710`) and, in Mutation Cliffs mode, cells without cliffs (`:1244-1249`) | `MonomerPosition` status, delegating to `this.viewerGrid.getWidgetStatus()` |
| radios' `checked` flags | `values.mode` = `this.mode` (`sar-viewer.ts:886-889`, the `monomerPositionMode` table tag) | MonomerPosition |
| `selAfter > selBefore` | `values['selected cells']` = count over `invariantMapSelection` / `mutationCliffsSelection` per mode (`:462-468`, `:376-381`), and `values.selection` as `pos:monomer, …` (the string `model.ts:456-463` builds) | SARViewer |
| `waitForTimeout(8000)` after settings OK, `svmHasCanvas` | `isRenderPending` = `viewerGrid.isRenderPending || this._mutationCliffs === null` (cliffs recomputed in a debounced worker after any property/settings change, `:532-533`, `:794-795`); `onRendered` = a Subject fed by `grid.onAfterDrawContent` (already subscribed at `:1258`) plus `values['cliffs computed']` | SARViewer |
| — | `values['rows shown']` (inner df filter, moved by the monomer search `:852-870`), `positions`, `monomers` | MonomerPosition |
| MPR untested beyond attach | `hitAreas['cell <monomer> at <position>']` on the `Diff` column only (the only clickable column, `:1604`), `values['most potent at <pos>']` = AAR, `values['diff at <pos>']`, `positions` | MostPotentResidues |
| `mclHasRender` pixel scan | `MCLViewer`: `isRenderPending` = `!initialized` (`libraries/ml/src/MCL/mcl-viewer.ts:22-35`), `parts.canvas` = `this.sc`'s canvas; `ClusterMaxActivityViewer` likewise over its `DG.Viewer.scatterPlot` (`cluster-max-activity-viewer.ts:192`) | ml library + Peptides |
| `scatter plot viewer in MCL viewer` unreachable | **library**: `findViewer` (`bdd/src/runtime/viewer-runtime.ts:263-268`) returns the first `tv.viewers` entry whose root *contains* the element — the outer MCL/CMA JsViewer wins over the inner Dart scatter plot; prefer the innermost `[name^="viewer-"]` | `@datagrok-libraries/bdd` |
| `waitForViewers` 120 s / `peptidesModel` polls / `initPeptides` prewarm | DOM custom events from the model: `peptides-analysis-started` (end of `startAnalysis`, `peptides.ts:363`), `peptides-mcl-clustered` (`model.ts:1430-1437`), `peptides-viewer-added`/`-closed` (`addX`/`closeViewer`); package steps `the SAR clustering should have completed` (≤ 180 s) and `the Peptides package is initialized` | Peptides model; existing library `user listens for {string} custom event` needs its budget checked |
| `model._settings.mclSettings.inflation` | package step `the analysis setting {string} should be {string}` over `df.getTag('settings')` (`model.ts:176-181`) | package binding |
| `header.classList.contains('expanded')` / `getComputedStyle(content).display` | `aria-expanded` on `.d4-accordion-pane-header` [already there] | core |
| `grid.temp['__dendrogram_neighbor_temp__']` | a `name` on the dendrogram `GridNeighbor` root (`Dendrogram/src/utils/hierarchical-clustering.ts:221`) — and the `closeViewer(DENDROGRAM)` no-op fixed | Dendrogram + Peptides |
| `grok.shell.o = acc.root` then `[name^=pane-]` scan | a name on the analysis accordion root (`ui.accordion('Peptides analysis panel')`, `model.ts:451`) or panes asserted directly; `the context panel should show` cannot name an HTMLElement | Peptides / library |

**Names that already exist** (all from Dart `name=` conventions): `pane-Peptides`, `pane-Details`, `pane-Mutation-Cliffs-pairs`, `pane-Distribution`, `pane-Selection`, `pane-General/Viewers/Columns/Sequence-space/MCL` (`accordion.dart:341`); `input-host-Activity`, `input-Scaling` / `input-host-Scaling`, `input-host-Clusters`, `input-Generate-clusters`, `button-Launch-SAR`, the panel's gear `aria-label="Adjust clustering parameters"` (`peptides.ts:74-107,181-183,207`); `viewer-Histogram` inside the pane (`peptides.ts:73-84,213-217`; the WebLogo has no name — the probe); `dialog-Analyze-Peptides` (`package.ts:144`), `dialog-Peptides-settings` (`settings.ts:329`), `button-OK`/`button-CANCEL`; wrench `aria-label="Peptides analysis settings"` in the ribbon (`model.ts:1115-1116`); settings inputs `input-host-Activity`, `input-host-Activity-scaling`, `input-Dendrogram`, `input-Sequence-space`, `input-Active-peptide-selection`, `input-host-Similarity-Threshold`, `input-host-Inflation-Factor`, `input-host-Max-Iterations`, `input-host-Min-Cluster-Size`, `input-host-Distance-Function`, `input-host-Fingerprint-Type`, `input-host-Gap-Open-Penalty`, `input-host-Gap-Extend-Penalty`, `input-Use-WebGPU` (`settings.ts:92-98,119-123,281-313`); viewer roots `viewer-Sequence-Variability-Map`, `viewer-Most-Potent-Residues`, `viewer-MCL`, `viewer-Logo-Summary-Table`, `viewer-Active-peptide-selection`; SVM radios `input-Mutation-Cliffs` / `input-Invariant-Map`, search icon `aria-label="Toggle monomer search input visibility"`, `input-Search` (`sar-viewer.ts:850-871,1306-1338`); Distribution `input-Monomers/Positions/Clusters`. **No name**: the SVM/MPR cells (canvas), the dendrogram neighbor, the analysis accordion root, the `Selection` grid docked under LST (a second `viewer-Grid`, `peptides.ts:353-362`).

## 3. Proposed features (`public/packages/Peptides/bdd/features/`)

1. `panel/peptides-pane.feature` @journey — merges **info-panels** and **peptides** (same dataset, no analysis): *The Macromolecule column renders as sequence* · *The column's context panel has Details and Peptides panes* · *Details lists the column's metadata* · *The Peptides pane offers the SAR parameters with IC50 as activity* · *A scaling change rebuilds the activity histogram* · *A click on a WebLogo glyph selects the rows with that monomer* (clears the selection after).
2. `sar/sar-from-panel.feature` @journey — **sar.test.ts**: *Launch SAR adds the two SAR viewers at once* · *Clustering adds the MCL viewer, the cluster column and the Logo Summary Table* · *The wrench opens the settings with the MCL defaults* · *A lower similarity threshold re-clusters* · *The map switches to Invariant Map mode* · *A cell click selects its rows and fills the context panel* · *The Distribution pane splits by position* · *Back to Mutation Cliffs mode, selection cleared*.
3. `sar/sar-from-top-menu.feature` @journey — **peptide-space** + **sar-viewer-lifecycle** (same dataset, same launch): *The Analyze Peptides dialog opens with the sequence and IC50 preselected* · *OK launches the analysis and the clustering* · *The settings dialog shows the Viewers and MCL panes* · *A new inflation factor re-runs MCL* · *Active peptide selection toggles on* · *…and off again, the SAR viewers staying* · (optional `@known-failure` *Dendrogram toggles on* — product bug, needs a name).

## 4. Hacks and vacuous checks in the old specs — do not restore

- Fixed sleeps everywhere (200 ms–12 s), 40 s hand-rolled polls, `helpers.ts:22-32` swallowing its own timeout; `document.body.classList.add('selenium')`, `simpleMode = false`, `showFiltersIconsConstantly`, package `.load()` calls, `Peptides:initPeptides` prewarm in `page.evaluate`.
- Synthetic `dispatchEvent(MouseEvent)` on canvases (`peptides.test.ts:165-169`, `sar.test.ts:179-182`), on menu items (`peptide-space.test.ts:53-54`), on checkboxes/OK (`sar-viewer-lifecycle.test.ts:307-315,383-389`); `inner.value = …` + `input`/`change` dispatch then asserting the value it wrote; clicking a guessed coordinate; `document.querySelector('[name="button-OK"]')` as a fallback for *any* dialog.
- Existence as "shown": `hasWebLogo`/`svmHasCanvas` (a `<canvas>` exists), `reRendered` (elements that existed before the change), `toHaveCount(1)`, `innerText.length > 0`, `childCount > 5`.
- `grok.shell.lastError` regexes (only the last error, only two patterns) as a "no crash" invariant.
- `grok.shell.o = col` / `df.currentCol = col` instead of a header click; `model.addClusterMaxActivityViewer()` as a "UI" lifecycle; `model._settings` private reads.
- `cb.isEnabled()` on `input-Dendrogram` — cannot fail (§1); the CMA "click twice to fire OFF" ordering.
- `input-Stack-split-categories` — the platform histogram's control, mistaken for a Distribution parameter.
- The mode toggle asserting the radios it clicked, never the viewer.

## 5. Open questions (need a run)

- **Position numbering.** My counts split on `-` with `NH2` as token 1 (so `T at position 5`, 630 rows; `N at 13`, 643). Whether `splitAlignedSequences` (bio splitter) and the WebLogo's `pi.label` number the same way — or drop terminals / start at 0 — decides every `cell … at …` and `monomer … at position …` name and the expected counts.
- Does a click on the grid's `header AlignedSequence` area make the column `grok.shell.o` (so `pane-Peptides` appears), and is the panel content loaded on expand only?
- The panel's histogram root is replaced on each Scaling change (`peptides.ts:83-84`): confirm a fresh phrase resolves to the new element and that `axis max` moves as expected; no `repainted`/`remembered range` across the swap.
- `user checks "Invariant Map" checkbox in Sequence Variability Map viewer` on a `.ui-input-bool` whose editor was retyped to `radio` (no `name` group) — whether `setChecked` and the `checked` state read work on it.
- Budget of `the {string} custom event should have fired` versus 60–80 s MCL runs; whether `viewer-MCL` is the root name Dart gives a JsViewer added via `tv.addViewer` [the probe: yes, `viewer-MCL`].
- EDA (`markovClusteringViewer`, `model.ts:1412-1414`) and Dendrogram packages present on the stand; whether `useWebGPU` differences change cluster counts.
- The analysis-accordion root's name (Dart `Accordion` with a key), and whether `grok.shell.o = acc.root` survives the library's `afterEach` reset.
- Whether a hover/settle on the SVM's inner grid (1 s table-view focus timer, `CLAUDE.md` gotchas) interferes with the cell click; and the two `viewer-Grid` roots (analysis grid, the docked `Selection` grid) versus the reserved `grid` element [the probe: five `viewer-Grid` elements after a launch].
