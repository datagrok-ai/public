# Peptides → bdd survey C (specs: export-invariant-map, export-mutation-cliffs, manual-alignment, sar-save-reopen, peptide-sar-demo-dashboard)

Read-only survey by an agent, 2026-09-11.

## 0. Fixture and code facts (verified offline)

**`System:DemoFiles/bio/peptides.csv`** = `data/demo/bio/peptides.csv`: **647 rows**, columns `ID, AlignedSequence, IC50`; every sequence is 17 `-`-separated tokens `NH2 … COOH` (row 1 `NH2--A-Q-T-T-Y-K-N-Y-R-R-N-L-L--COOH`, row 2 `NH2-M-A-N-T-T-Y-K-N-Y-R-N-N-L-L--COOH`); 464 distinct sequences; 647 distinct IC50 values, none blank, min 4.1e-7 > 0 (so the Scaling input is enabled, `widgets/peptides.ts:64`). Gaps are empty tokens (269 at position 2, 254 at 15, 638 at 16).

After SAR the split columns are named **`"1"`…`"17"`** — `libraries/bio/src/utils/splitter.ts:16` names column `(index + 1)`; tagged `isPositionCol` (`model.ts:752-753`, `constants.ts:44`).

**Export Invariant Map** (`sar-viewer.ts:675-704`): `AAR` (`COLUMNS_NAMES.MONOMER`, `constants.ts:8`) + one int column per position key of `monomerPositionStats` → **18 columns, 22 rows** (`A,C,COOH,D,E,F,G,H,I,K,L,M,N,NH2,P,Q,R,S,T,V,W,Y` sorted; the gap is skipped at `utils/algorithms.ts:149-151`, and the separator gap original is `''`, `bio/src/utils/macromolecule/consts.ts:74`). Cell values to claim: `"1"` of row 14 (NH2) = 647; `"17"` of row 3 (COOH) = 647; `"2"` of row 1 (A) = 299; `"2"` of row 12 (M) = 9; `"10"` of row 22 (Y) = 604. Table and view name `Invariant Map` (`:702-703`).

**Export Mutation Cliffs** (`sar-viewer.ts:561-672`): dialog title `Export Mutation Cliffs`, one input captioned `Extra columns` (`:572-574`); columns exactly `Seq 1, Seq 2, Mutation, Seq 1 IC50, Seq 2 IC50, Delta` (`:635-657`; the viewer's `activityColumnName` is the raw `IC50`, set from `settings.activityColumnName` at `model.ts:1243`), `+ Seq 1 <extra>, Seq 2 <extra>` per extra column (`:663-667`), `Mutation` semtype `MacromoleculeDifference` (`:649`), one row per index pair (`:603-606`). With the launch defaults (`maxMutations: 1, minActivityDelta: 0`, `model.ts:1241-1248`; the worker accepts `|delta| >= 0`, `workers/mutation-cliffs-worker.ts:28`; no filter → all rows, `sar-viewer.ts:803-804`) every Hamming-1 row pair qualifies: **6253 rows** (1443 over distinct sequences — the export is over rows). Table/view name `Mutation Cliffs`.

**Context menu**: `grok.events.onContextMenu` subscriber adds group `Export` with items `Export Mutation Cliffs...` and `Export Invariant Map` (`sar-viewer.ts:726-733`), only when the right-click target is inside `this.root` (`:727-728`) — both subclasses, since it is in `SARViewer.onTableAttached`.

**Launch defaults**: the Scaling input starts at `none` and is reset to `none` whenever the activity column changes (`widgets/peptides.ts:49-50, 63-66`). All four `.md` files say "Scaling `-lg`" — **stale**. `Generate clusters` defaults to true (`:81`) → `addMCL` + `useEmbeddingsClusters` (`:195-197`) → viewers after launch: Grid, `Sequence Variability Map`, `Most Potent Residues`, `MCL`, `Logo Summary Table`, and a `Selection` grid docked under the LST (`:353-361`) — six.

**Manual alignment** (`widgets/manual-alignment.ts`): textarea created with an empty caption (`:17`), `Apply` button (`:20`), Reset is an icon button whose name comes from its tooltip `Reset` (`:30-31`, the old spec found `button-Reset`). **Off-by-one**: `:26-29` writes split index `i` into column `i.toString()`, but the column for split index `i` is named `i+1` (`splitter.ts:16`) — Apply on an *unchanged* sequence shifts the row's per-position cells left by one and never writes column `"1"`. The old spec papered over it: line 252 "Splitter remaps positions, so assert 'at least one changed'". Verify on the stand; if confirmed it is a one-line fix (`(i + 1).toString()`) or a `@known-failure`.

**Restore on project open**: nothing "restores" a model; each SAR viewer's `model` getter (`sar-viewer.ts:212-213`) calls `PeptidesModel.getInstance` (`model.ts:361-373`), which reads the `settings` tag (`:175-184`, `constants.ts:36`) and runs `init` (`:1105-1119`: ribbon, `updateGrid` → WebLogo headers, `setGridProperties`). So "the analysis is restored" == the table carries the `settings` tag (with `activityScaling`), 17 `isPositionCol` columns, `temp.peptidesModel.isInitialized`, and the layout re-created the viewers.

**Demo** (`demo/fasta.ts`): table `Simple peptides` (`:17`), `AlignedSequence` tags `alphabet=PT`, `units=fasta`, `aligned=SEQ.MSA` (`:38-41`), scaling `-lg` (`:42`), `mclSettings.threshold = 94` (`:43-44`), `addMCL + useEmbeddingsClusters` (`:45-47`). Registered as `Peptide SAR`, `demoPath: Bioinformatics | Peptide SAR`, `isDemoDashboard` (`package.ts:275-285`).

**Landing** (`package.ts:94-124`): view `Peptides`, buttons `Simple demo / Complex demo / HELM demo` (`:118-120` → `button-Simple-demo` …), sets `showToolbox/showHelp/showContextPanel = false` (`:109-111`); `openDemoData` names the table `Peptides` and the view `PeptidesView` and turns the context panel back on (`:70-79`). **It is not an app**: `package.g.ts:10-11` emits only `//output: view result`, no `app` tag — the library's `user opens the "Peptides" app` (`platform/steps.ts:136-148`, `DG.Func.find({tags: ['app']})`) will throw. Sample files: `aligned.csv` 647 rows `ID, IC50, AlignedSequence` (fasta, width 15 → 15 position columns); `aligned_2.csv` 540 rows `MSA, Activity` (separator `/`); `aligned_3.csv` 334 rows `HELM, Activity`.

**Pass 0**: no Peptides viewer has `getWidgetStatus`, `isRenderPending` or `onRendered` (grep of `src/` is empty). Every settle on an SAR viewer falls to the 300 ms cap and every canvas click in the old specs has no hit area to become. That is the job.

## 1. Per old spec, per softStep

Legend: **direct** = existing phrase; **needs-core** = package must expose it first; **manual**; **vacuous** (why).

### export-invariant-map.test.ts

| Old step | Asserts | Verdict | Gherkin |
|---|---|---|---|
| Setup 1 (`:13-38`) | dataset open, grid canvas, `initPeptides` pre-warm, 4 s sleeps | direct | `Given user opens peptides dataset` (NEW dataset `peptides` → `System:DemoFiles/bio/peptides.csv`), `And the Bio package is initialized`, NEW `And the Peptides package is initialized` (calls `Peptides:initPeptides`) |
| Setup 2 (`:39-77`) | `pane-Peptides` + `button-Launch-SAR` found, model attached, 9 s sleep, SVM has a canvas, MPR present | direct for the launch; the "has canvas" is vacuous (liveness) | md says top menu: `When user picks "Bio > Analyze > SAR..." from the top menu` / `Then "Analyze Peptides" dialog should be visible` / `And Scaling input in "Analyze Peptides" dialog should have value "none"` / `When user clicks on OK button in "Analyze Peptides" dialog` / `Then the current view should hold at least 6 viewers` / `And the open tableview should have 1 Sequence Variability Map viewer` / `And the open tableview should have 1 Most Potent Residues viewer` / `And "Sequence Variability Map" viewer should be painted` (needs-core: `getWidgetStatus.parts.canvas`) |
| Setup 3 (`:79-86`) | count of `/^\d+$/` columns > 0 | direct, strengthen | `Then 17 new columns matching "^\d+$"…` — no such phrase; use `the table should have a column "1"` … `"17"` and `the table should not have a column "18"`, `"1" column should have semantic type "Monomer"` |
| Sc 1 steps 1-3 (`:88-125`) | synthetic `contextmenu` at (120,120), `mouseenter` on `Export`, 1200/600 ms sleeps, item found | direct (gesture) | `When user picks "Export > Export Invariant Map" from the context menu of "Sequence Variability Map" viewer` (`menuPoint` needs `isRenderPending` to settle instead of the 300 ms cap → needs-core) |
| Sc 1 steps 4-7 (`:127-188`) | view `Invariant Map` current; leading col `AAR` string; position count == setup count; all int; rows > 0; ints; some zero; `monomerLooksLikeId` regex | direct; regex + "all integers on an int column" vacuous | `Then table "Invariant Map" should be open` / `And the "Invariant Map" view should be current` / `And table "Invariant Map" should have columns "AAR, 1, 2, …, 17"` / `And table "Invariant Map" should have 22 rows` / `And "1" column should have type "int"` / `And the value of "AAR" column in row 14 should be "NH2"` / `And the value of "1" column in row 14 should be "647"` / `And the value of "2" column in row 1 should be "299"` / `And the value of "1" column in row 1 should be "0"` / `And table "Invariant Map" should have no missing values in "2" column` |
| Sc 2 (`:190-281`) | same from MPR; `threw` around `.click()`; console filter for `Cannot read … null` registered mid-step | direct; `threw` and the console filter are vacuous (a handler exception never reaches `.click()`) | `When user closes the current view` / `And user switches to the "peptides" table view` / `When user picks "Export > Export Invariant Map" from the context menu of "Most Potent Residues" viewer` / same table claims / `And no errors should have been logged` / `And no error or warning balloon should have been shown` |

### export-mutation-cliffs.test.ts

| Old step | Asserts | Verdict | Gherkin |
|---|---|---|---|
| Setup 1-2 | as above | direct | as above |
| Sc 1 steps 1-3 (`:72-118`) | menu item found, `dialog-Export-Mutation-Cliffs` present, OK clicked, 1.5 s sleep | direct | `When user picks "Export > Export Mutation Cliffs..." from the context menu of "Sequence Variability Map" viewer` / `Then "Export Mutation Cliffs" dialog should be visible` / `And "Extra columns" input in "Export Mutation Cliffs" dialog should be visible` / `When user clicks on OK button in "Export Mutation Cliffs" dialog` / `Then "Export Mutation Cliffs" dialog should be hidden` |
| Sc 1 steps 4-6 (`:119-147`) | view current; columns `toEqual` base; `hasAllBase` (duplicate); `Mutation` semtype; row 0 contains `#`; rows > 0 | direct; `hasAllBase` and "contains #" weak | `Then table "Mutation Cliffs" should be open` / `And the "Mutation Cliffs" view should be current` / `And table "Mutation Cliffs" should have columns "Seq 1, Seq 2, Mutation, Seq 1 IC50, Seq 2 IC50, Delta"` / `And table "Mutation Cliffs" should have 6253 rows` / `And "Mutation" column should have semantic type "MacromoleculeDifference"` / `And "Seq 1" column should have semantic type "Macromolecule"` / `And every value of "Mutation" column should be "Seq 1" and "Seq 2" of the same row joined by "#"` / `And table "Mutation Cliffs" should have no missing values in "Delta" column` |
| Sc 2 dialog re-open (`:149-184`) | dialog re-opens, `input-Extra-columns` present, CANCEL | direct | `When user picks … from the context menu of …` / `Then "Extra columns" input in "Export Mutation Cliffs" dialog should be visible` / `When user clicks on CANCEL button in "Export Mutation Cliffs" dialog` |
| Sc 2 export with ID (`:185-228`) | **calls the private `svm._doExportMutationCliffs([idCol])`**, then finds a table with `Seq 1 ID`/`Seq 2 ID`, 8 columns, rows > 0 | vacuous for the dialog (the picker is never driven) | drive the picker: `When user selects "ID" in "Extra columns" input in "Export Mutation Cliffs" dialog` — `ui.input.columns` opens a Dart multi-select column dialog, not the `.d4-column-selector` typeahead `select()` handles (`gestures.ts:251-258`); **probe on the stand** — the fallback is `user clicks on "Extra columns" input …` + `user checks "ID" checkbox in "Select columns" dialog` + OK. Then `Then table "Mutation Cliffs" should have columns "Seq 1, Seq 2, Mutation, Seq 1 IC50, Seq 2 IC50, Delta, Seq 1 ID, Seq 2 ID"` / `And table "Mutation Cliffs" should have 6253 rows` / `And table "Mutation Cliffs" should have no missing values in "Seq 1 ID" column` |

### manual-alignment.test.ts

| Old step | Asserts | Verdict | Gherkin |
|---|---|---|---|
| Setup launch (`:50-96`) | position columns > 0, `col("2").semType === 'Monomer'`, model present | direct | launch as above; `Then "2" column should have semantic type "Monomer"` |
| Anchor row 1 (`:99-114`) | `currentRowIdx = 1`, col 2 populated | direct | `When user makes row 2 current` / `Then the value of "2" column in row 2 should be "M"` / `And the value of "AlignedSequence" column in row 2 should be "NH2-M-A-N-T-T-Y-K-N-Y-R-N-N-L-L--COOH"` |
| Sc 1 step 1 (`:117-150`) | `elementFromPoint` on the cell, six dispatched pointer/mouse events, 2.5 s sleep, `grok.shell.o.semType === 'Monomer'` | direct (the grid publishes `cell <r> of <c>` areas) | `Given the context panel is open` / `When user clicks on the "cell 2 of 2" area of grid` / `Then the "current column" reading of grid should be "2"` / `And the context panel should show "M"` (the SemanticValue's name — probe what `contextPanelShows` reads for it) |
| Sc 1 step 2-3 (`:153-212`) | pane `Manual Alignment` present; inner `.expanded`; textarea, Reset, Apply found; textarea == live value | direct once the textarea is named | `Then "Manual Alignment" section should be visible` / `When user expands "Manual Alignment" section` / `Then Apply button in "Manual Alignment" section should be visible` / `And Reset button in "Manual Alignment" section should be visible` / `And Sequence text area in "Manual Alignment" section should have value "NH2-M-A-N-…"` (needs-core: caption) |
| Sc 1 steps 4-5 (`:214-254`) | `textarea.value =` + dispatched `input`/`change`; Apply; `AlignedSequence` == edited; ≠ baseline; "at least one position column changed" | direct; the last claim is **vacuous** (hides the off-by-one) | `When user enters "NH2-V-A-N-T-T-Y-K-N-Y-R-N-N-L-L--COOH" into Sequence text area in "Manual Alignment" section` / `And user clicks on Apply button in "Manual Alignment" section` / `Then the value of "AlignedSequence" column in row 2 should be "NH2-V-A-N-…"` / `And the value of "2" column in row 2 should be "V"` / `And the value of "1" column in row 2 should be "NH2"` / `And the value of "3" column in row 2 should be "A"` — expected to fail today (§0) |
| Sc 1 step 8 (`:257-276`) | model present, SVM/MPR still in the view, SVM has a canvas, `cell.renderer === 'sequence'` | canvas liveness vacuous; rest direct | `Then the open tableview should have 1 Sequence Variability Map viewer` / `And "Sequence Variability Map" viewer should report no error` (needs-core) / `And "AlignedSequence" column should have tag "cell.renderer" equal to "sequence"` / `And the "cell type of AlignedSequence" reading of grid should be "sequence"` |
| Sc 1 steps 6-7 (`:278-284`) | `String(grok.shell.lastError)` matches `/null\|undefined.*reading\|setTrue/` | **vacuous**: `lastError` is a Promise (the demo spec says so at `:10`), `String()` gives `[object Promise]`, never matches | `And no errors should have been logged` |
| Sc 2 step 1 (`:287-310`) | pane still there, textarea == live (edited) value | direct | `Then Sequence text area in "Manual Alignment" section should have value "NH2-V-…"` (the panel is rebuilt after Apply — `manual-alignment.ts:31-39` nulls and restores `grok.shell.o`; a fresh locate is fine) |
| Sc 2 step 2 (`:313-349`) | Reset re-binds, columns unchanged | direct | `When user clicks on Reset button in "Manual Alignment" section` / `Then Sequence text area … should have value "NH2-V-…"` / `And the value of "AlignedSequence" column in row 2 should be "NH2-V-…"` / `And the value of "2" column in row 2 should be "V"` |
| Sc 2 steps 3-4 (`:352-396`) | unsaved edit, Reset discards, columns unchanged | direct | `When user enters "NH2-V-A-X-…" into Sequence text area …` / `And user clicks on Reset button …` / `Then Sequence text area … should have value "NH2-V-…"` / column claims as above |
| Sc 2 step 5 (`:399-418`) | **`model.fireBitsetChanged('WebLogo')` called directly**, no throw, lastError regex | vacuous (a direct model call instead of the WebLogo click; the regex never matches) | `When user clicks on the "monomer V at position 2" area of grid`? — the WebLogo header glyphs are drawn by Peptides' `setWebLogoRenderer` on the grid's column headers, not the Bio WebLogo viewer: needs-core (header glyph areas on the grid or an SVM cell area); until then `When user clicks on the "cell M at position 3" area of "Sequence Variability Map" viewer` (needs-core) |
| Sc 2 step 6 (`:420-440`) | sets `selection` bits 1,5,8 via API, then asserts `trueCount > 0`; SVM canvas exists | **vacuous** (counts what it wrote; liveness) | after the glyph click: `Then some rows should be selected` / `And only rows where "3" is "M" should be selected` / `And "Sequence Variability Map" viewer should show a selection highlight` (needs-core) |

### sar-save-reopen.test.ts

| Old step | Asserts | Verdict | Gherkin |
|---|---|---|---|
| Setup 1-2 (`:13-76`) | launch, 8 s sleep, viewers `SVM`, `MPR` | direct | as above |
| Setup 3 (`:78-108`) | reads `model._settings.activityScaling`; synthetic click at canvas (120,120) on SVM; selection grew | vacuous gesture (blind offset); the delta claim is right | `When user clicks on the "cell M at position 2" area of "Sequence Variability Map" viewer` (needs-core hit areas) / `Then 9 rows should be selected` / `And only rows where "2" is "M" should be selected` |
| Sc 1 steps 1-2 (`:110-144`) | `button-Save` → `dialog-Save-project`, `input.value=` + events, OK, 6 s sleep, project found by name | manual UI path → replaced by the API step | `When user saves the current view as project "peptides-sar-roundtrip"` (`platform/steps.ts:90-115`, cleaned at feature end) |
| Sc 1 steps 3-8 (`:146-204`) | `closeAll`; open; **14 s sleep**; a table view; `AlignedSequence` Macromolecule; viewers contain SVM, MPR, `MCL`; `colHeaderHeight > 60`; model present; `typeof scaling === 'string'` | direct except `typeof … string` (vacuous — any string, and the md's `-lg` is wrong: the default is `none`) | `When user closes all views` / `And user opens the "peptides-sar-roundtrip" project` / `Then "AlignedSequence" column should have semantic type "Macromolecule"` / `And the table should have a column "17"` / `And the current view should hold at least 6 viewers` / `And the open tableview should have 1 Sequence Variability Map viewer` / `… 1 Most Potent Residues viewer` / `… 1 MCL viewer` / `… 1 Logo Summary Table viewer` / NEW `And the SAR analysis of table "peptides" should be initialized with scaling "none"` (package reading: `settings` tag JSON + `temp.peptidesModel.isInitialized` + `isPositionCol` count) / `And the "activity scaling" reading of "Sequence Variability Map" viewer should be "none"` (needs-core) / header: NEW grid reading or `the "header 2" area of grid should be at least 60 pixels tall` (the grid publishes `header <col>` areas — `core/client/d4/lib/src/viewers/grid/CLAUDE.md:321-324`; the phrase `the {string} area of {widget} should be at least {int} pixels tall` exists) |
| Sc 2 steps 1-3 (`:189-197`) | `0 <= trueCount <= rowCount` | **vacuous** (always true) | decide on the stand what the API save keeps; then either `Then 9 rows should be selected` or `Then no rows should be selected` — one, not "or" |
| Sc 2 steps 4-6 (`:199-237`) | blind canvas click; selection grew; panes `pane-Mutation-Cliffs-pairs` and `pane-Distribution` present; lastError regex | gesture vacuous; panes direct; regex vacuous | `When user clicks on the "cell M at position 2" area of "Sequence Variability Map" viewer` / `Then 9 rows should be selected` / `And "Mutation Cliffs pairs" section should be present` / `And "Distribution" section should be present` (`model.ts:618, 633` — counted panes hide at 0, claim `present`) / `And no errors should have been logged` |
| Cleanup (`:239-245`) | deletes by name | direct | `atFeatureEnd` of the save step |

### peptide-sar-demo-dashboard.test.ts

| Old step | Asserts | Verdict | Gherkin |
|---|---|---|---|
| Setup (`:25-47`) | `initPeptides`, `tableViews == 0` | direct | `Given user is logged in` / `And the Bio package is initialized` / `And the Peptides package is initialized` |
| Sc 1 launch (`:50-75`) | func registered; `demoPath`; `isDemoDashboard`; call doesn't throw | direct (call), meta needs a phrase | `When user calls "Peptides:macromoleculeSarFastaDemo" function` / `Then the result should be empty`; the meta: NEW `Then "Peptides:macromoleculeSarFastaDemo" function should have option "demoPath" equal to "Bioinformatics \| Peptide SAR"` (generic, `platform/functions.ts`) |
| settle (`:78-91`) | poll for MCL, LST, SVM, MPR; 2 s sleep | direct | `Then the current view should hold at least 6 viewers` / four `the open tableview should have 1 <type> viewer` |
| table (`:93-115`) | table `Simple peptides`, `AlignedSequence` Macromolecule | direct | `Then table "Simple peptides" should be open` / `And table "Simple peptides" should have 647 rows` / `And "AlignedSequence" column should have semantic type "Macromolecule"` / `And the table should have a column "15"` / `And the table should not have a column "16"` |
| tags (`:117-132`) | alphabet PT, units fasta, aligned SEQ.MSA | direct | `"AlignedSequence" column should have tag "alphabet" equal to "PT"` / `… should have units "fasta"` / `… tag "aligned" equal to "SEQ.MSA"` |
| scaling (`:134-143`) | `model._settings.activityScaling === '-lg'` | needs-core | NEW package reading `the SAR analysis of table "Simple peptides" should be initialized with scaling "-lg"` and/or `the "activity scaling" reading of "Sequence Variability Map" viewer should be "-lg"` |
| MCL (`:145-161`) | threshold 94, `MCL` viewer | needs-core / direct | NEW `the SAR analysis of table "Simple peptides" should use MCL threshold 94` (same reading) / `the open tableview should have 1 MCL viewer` / `a new column matching "^Cluster \(MCL\)" …` — no baseline; use `the table should have a column "Cluster (MCL)"` (probe the exact name, `model.ts:1360-1364` matches `cluster…mcl`) [the probe: `Cluster (MCL)`] |
| crash (`:163-186`) | viewers; awaited lastError regex | direct / weak | `And no errors should have been logged` / `And no error or warning balloon should have been shown` |
| Sc 2 landing (`:189-246`) | `packages.list` has Peptides; `AppData/Peptides` non-empty; **a "Peptides" browse group declared by *other* packages' apps**; view name; three button labels; three window flags | first three **vacuous** (environment / other packages); rest direct once the view can be opened | NEW `Given user opens the Peptides landing view` (package step: `grok.functions.call('Peptides:Peptides')` + `addView`) — or register it as an app (product decision, see §5) / `Then the "Peptides" view should be current` / `And "Simple demo" button should be visible` / `"Complex demo" button` / `"HELM demo" button` / `And toolbox should be hidden` / `And context panel should be hidden` / help pane: NEW element `help panel` |
| Simple (`:248-275`) | `button-Simple-demo` click; a new table view; "last view" is it; view `PeptidesView`; `AlignedSequence`; ≥1 Macromolecule column | direct | `When user clicks on "Simple demo" button` / `Then the "PeptidesView" view should be current` / `And table "Peptides" should have 647 rows` / `And "AlignedSequence" column should have semantic type "Macromolecule"` / `And "AlignedSequence" column should have units "fasta"` |
| Complex (`:278-309`) | view `PeptidesView`, ≥1 Macromolecule column (`MSA`) | direct | `When user switches to the "Peptides" view` / `And user clicks on "Complex demo" button` / `Then the "PeptidesView" view should be current` / `And the table should have 540 rows` / `And "MSA" column should have semantic type "Macromolecule"` / `And "MSA" column should have units "separator"` |
| HELM (`:312-349`) | same, `HELM` units helm logged as a note only | direct | `… "HELM demo" button` / `the table should have 334 rows` / `"HELM" column should have units "helm"` |
| crash (`:351-363`) | regex | weak | `no errors should have been logged` |

Note: all three demo tables are named `Peptides` (`package.ts:74`) and all three views `PeptidesView` — claims must go through the current table (`the table should have N rows`), not `table "Peptides"`.

## 2. Package signals to add

| Old hack | Replacement | Source |
|---|---|---|
| `[name="viewer-Sequence-Variability-Map"] canvas` exists; 300 ms settle cap on every menu/click | `getWidgetStatus()` (`parts.canvas`, `values`, `hitAreas`), `get isRenderPending()`, `onRendered` on `SARViewer` (both subclasses inherit); values: `rows shown`, `positions` (17), `monomers` (22), `mutation cliffs` (pair count), `activity scaling`, `data source`, `mode` (SVM), `error` | `sar-viewer.ts:105` (base), `viewerGrid` `:187-190`, `calculateMutationCliffs` `:799-814` |
| synthetic click at canvas (120,120) to "select something" | hit areas `cell <monomer> at position <p>` (SVM: monomer × position grid), `position <p>` per row of MPR; the click then selects the rows carrying that monomer there | `MonomerPosition.createViewerGrid` (grid cells), `MostPotentResidues` |
| `document.querySelector('.d4-accordion-pane header "Manual Alignment"')`, `.pep-textinput textarea` | caption the textarea (`ui.input.textArea('Sequence', …)`) so `Sequence text area in "Manual Alignment" section` resolves; `Apply` / `Reset` already resolve as `button-Apply` / `button-Reset` | `manual-alignment.ts:17, 20, 30-31`; panel name `package.ts:257` |
| `.replace(/^NH2-(.)-/, …)` + "at least one column changed" | fix the write-back to `(i + 1).toString()` so `the value of "2" column in row 2 should be "V"` holds | `manual-alignment.ts:26-29` vs `splitter.ts:16` |
| `model.fireBitsetChanged('WebLogo')` direct call | grid header glyph areas for the WebLogo headers (`monomer <m> at position <p>` on `header <col>`), or use the SVM cell area | `model.ts:725-726` `setWebLogoRenderer`, `utils/cell-renderer.ts` |
| `df.temp.peptidesModel._settings.activityScaling`, `mclSettings.threshold`, `colHeaderHeight > 60` | package binding `the SAR analysis of table {string} should be initialized with scaling {string}` (+ `use MCL threshold {int}`) reading the `settings` tag and `temp.peptidesModel.isInitialized`, position-column count by `isPositionCol`; header height via the grid's `header <col>` area | `model.ts:175-184, 361-373, 1105-1119`; `constants.ts:36, 44` |
| `grok.functions.call('Peptides:initPeptides')` + 4 s sleeps | `Given the Peptides package is initialized` (same call, no sleep) | `package.ts:83-92` |
| `grok.functions.call('Peptides:Peptides')` + `grok.shell.addView` | either an `app` registration (`meta.role: app`, `browsePath`) so `user opens the "Peptides" app` works, or a package Given `user opens the Peptides landing view` | `package.ts:94-124`, `package.g.ts:10-11` |
| `grok.shell.windows.showHelp` read | NEW library element `help panel` (probe the selector) | `package.ts:110` |
| context-menu label scan (`.d4-menu-item-label` text `Export`) | the path text as registered: `Export > Export Mutation Cliffs...` and `Export > Export Invariant Map` | `sar-viewer.ts:730-732` |
| `[name="dialog-Export-Mutation-Cliffs"]`, `[name="input-Extra-columns"]` | already named: `"Export Mutation Cliffs" dialog`, `"Extra columns" input` | `sar-viewer.ts:572-574` |
| `[name="pane-Peptides"]`, `[name="button-Launch-SAR"]` | already named: `Peptides section`, `"Launch SAR" button in Peptides section`; needs `grok.shell.o` = the column — no library step sets a column current; `user clicks on the "header AlignedSequence" area of grid` should do it (grid `header <col>` areas) — probe | `package.ts:163`, `widgets/peptides.ts:207` |
| `[name="button-Simple-demo"]` etc. | already named | `package.ts:118-120` |

## 3. Proposed features

All under `public/packages/Peptides/bdd/features/`, dataset `peptides` registered in `bdd/bindings/elements.ts`.

- **`sar/export.feature`** `@journey` — Background: login, `peptides`, Bio + Peptides initialized, `Bio > Analyze > SAR...`, `Analyze Peptides` OK (scaling `none` claimed), six viewers, `"1"`…`"17"` Monomer columns. Scenarios: *Export Invariant Map from the Sequence Variability Map is monomer × position counts* (18 cols, 22 rows, the five cell values); *…from Most Potent Residues gives the same table*; *Export Mutation Cliffs with no extra columns* (6 cols, 6253 rows, `Mutation` = `Seq 1#Seq 2`, semtypes); *An extra column is exported as a Seq 1 / Seq 2 pair* (8 cols). Each scenario closes the exported view and switches back.
- **`sar/manual-alignment.feature`** `@journey` — same Background + row 2 current + context panel open. Scenarios: *A Monomer cell shows the Manual Alignment panel with the row's sequence*; *Apply rewrites the sequence and the per-position cells* (the `"1"`/`"2"`/`"3"` claims — `@known-failure` if the off-by-one is confirmed and not fixed); *Reset re-binds to the stored sequence*; *Reset discards an unsaved edit*; *A glyph click after the edit still selects* (needs the hit areas).
- **`sar/project-round-trip.feature`** `@journey` — Background + an SVM cell click selecting the 9 `M`-at-2 rows. Scenarios: *Saving as a project and reopening restores the analysis* (viewers, `settings` tag, position columns, header area height); *The reopened analysis answers a click* (selection, `Mutation Cliffs pairs` / `Distribution` present, no errors).
- **`entry/demo-dashboard.feature`** (not a journey with the others: its own table) — *The Peptide SAR demo builds the dashboard on Simple peptides* (647 rows, 15 positions, tags, `-lg`, MCL 94, six viewers); *The demo is registered as a dashboard under Bioinformatics* (function meta).
- **`entry/landing.feature`** `@journey` — Background: landing view open. Scenarios: *The landing view offers three demos and folds the side panels*; *Simple demo opens aligned.csv as PeptidesView* (647, fasta); *Complex demo* (540, `MSA` separator); *HELM demo* (334, `HELM` helm). Each switches back to `Peptides`.

## 4. Hacks and vacuous checks in the old specs — do not restore

- Every spec: `document.body.classList.add('selenium')`, `showFiltersIconsConstantly`, `initPeptides` pre-warm wrapped in try/catch with a console note, `setTimeout(resolve, 4000)` race against `onSemanticTypeDetected`, 4/8/9/14 s `waitForTimeout`s, `helpers.ts:22-32` `waitForViewers` that swallows its own timeout.
- Every spec launches SAR through the context-panel button while its `.md` prescribes the top menu; the md's "Scaling `-lg`" default and `col('0')` numbering are wrong (`widgets/peptides.ts:49-50`, `splitter.ts:16`).
- Dispatched synthetic `contextmenu`/`mousemove`/`mouseenter`/pointer events at fixed offsets (`export-invariant-map:100-111`, `export-mutation-cliffs:85-96`, `manual-alignment:130-138`, `sar-save-reopen:95-99, 213-219`); `textarea.value =` / `nameInput.value =` with hand-fired `input`/`change` (`manual-alignment:235-238, 371-374`, `sar-save-reopen:124-130`).
- `String(grok.shell.lastError)` regexes (`manual-alignment:279-283, 409, 416`; `sar-save-reopen:225, 235`) — `lastError` is a Promise, the string is `[object Promise]`, the check cannot fail.
- `expect(threw).toBeNull()` around `item.click()` (`export-invariant-map:229-239`); a console-error filter subscribed mid-step for one regex (`:198-202, 277-280`).
- `_doExportMutationCliffs([idCol])` on the viewer instance (`export-mutation-cliffs:195`) — the extra-column dialog is never driven.
- "At least one per-position column changed" (`manual-alignment:253`) hides the off-by-one; `model.fireBitsetChanged('WebLogo')` (`:406`) and `df.selection.set(...)` then `trueCount > 0` (`:424-437`) count what the test wrote.
- `typeof scaling === 'string'` (`sar-save-reopen:185-186`); `0 <= trueCount <= rowCount` (`:194-196`); the md's "restored OR cleanly re-initialized" either/or.
- `monomerLooksLikeId` regex (`export-invariant-map:151`), `allIntegers` on an int column (`:146, 160`), `hasAllBase` next to `toEqual` (`export-mutation-cliffs:134, 140`), "has a canvas" liveness probes everywhere.
- `packageInstalled`, `appDataEntries > 0`, `peptidesBrowseGroup` computed from other packages' `browsePath` (`demo-dashboard:200-206, 234-239`); `tvs[tvs.length - 1]` as "the new view" (`:255-257`).

## 5. Open questions / needs the stand

1. **Manual-alignment off-by-one** (`manual-alignment.ts:26-29`): confirm by applying an unchanged sequence and reading columns `"1"`…`"3"`; decide fix vs `@known-failure`.
2. **6253 mutation-cliff rows**: confirm the worker pairs on all rows with `dataSource: 'Filtered'` and no filter, and that `seenPairs` yields one row per index pair.
3. **`ui.input.columns` picker**: whether `user selects "ID" in "Extra columns" input in "Export Mutation Cliffs" dialog` resolves (`gestures.ts:251-258` handles the `.d4-column-selector` typeahead, not the multi-select column dialog) — probe the DOM; a new generic step may be needed.
4. **Making a column the current object** for the `Peptides` pane: does a click on the grid's `header AlignedSequence` area set `grok.shell.o` to the column (`allowColSelection` is turned off after launch, `model.ts:995`, but the launch happens before)? Otherwise the feature takes the top-menu path only.
5. **`the context panel should show "…"` for a Monomer SemanticValue**: what `friendlyName`/`name` a `DG.SemanticValue` reports (`platform/steps.ts:193-199`).
6. **Landing view**: register `Peptides` as an app (changes Browse > Apps) or add a package step — lead's call.
7. **API project save vs selection**: whether `saveLayout({saveWithData: true})` + `uploadDataFrame` keeps the selection and the `settings` tag; then write the one true claim.
8. **`MCL` viewer type and column names** (`Cluster (MCL)`, `Embed_X (MCL)`…) on the reopened project and the demo [the probe: `EmbedX (MCL)`, `EmbedY (MCL)`, `Cluster (MCL)`, `Cluster size (MCL)`, `Connectivity (MCL)`]; the `help panel` selector; whether `startAnalysis` shows info balloons (`widgets/peptides.ts:36-37, 66-67` — none expected on this fixture).
9. The gallery route to the demo (`Bioinformatics | Peptide SAR`) is not exercised; calling the function directly skips `grok.shell.isInDemo` (`utils/misc.ts:500-502`, `model.ts:1210, 1252, 1276`), which is what the old spec did too.
