# NodeJS API Demo changelog

## v.next

* GROK-20443: Initial version — nodejs script using the js-api (grok/DG) with a package
  python function call and package file access, verified end-to-end on sandbox
