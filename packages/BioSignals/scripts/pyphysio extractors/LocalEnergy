#name: LocalEnergy
#description: Estimate the local energy of the signal, by windowing
#language: python
#tags: biosignals, extractors, pyphysio
#input: dataframe dataframe
#input: column column
#input: double sampling_frequency
#input: double window_length = 2 [Length of the window]
#input: double window_step = 2
#output: dataframe newDf

import pyphysio as ph

sig = ph.EvenlySignal(values=dataframe[column], sampling_freq=sampling_frequency)

extracted = ph.Energy(win_len=window_length, win_step=window_step)(sig)

newDf = pd.DataFrame({
    'Index': range(len(extracted)),
    'extracted': extracted
})