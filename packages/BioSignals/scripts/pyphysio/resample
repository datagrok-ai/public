#name: Resample
#description: Change sampling frequency of signal
#language: python
#tags: biosignals, filters, pyphysio
#input: dataframe dataframe
#input: column column
#input: double sampling_frequency [Input sampling frequency]
#input: double output_sampling_frequency [The sampling frequency for resampling]
#input: string interpolation_method {choices: ['linear', 'nearest', 'zero', 'slinear', 'quadratic', 'cubic']} [Method for interpolation]
#output: dataframe df

import pyphysio as ph

sig = ph.EvenlySignal(values=dataframe[column], sampling_freq=sampling_frequency)

sig = sig.resample(fout=output_sampling_frequency, kind=interpolation_method)

df = pd.DataFrame({
    'sig': sig
})