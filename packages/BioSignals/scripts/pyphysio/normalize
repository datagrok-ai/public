#name: normalize
#language: python
#input: dataframe input_signals
#input: int sampling_frequency
#input: string norm_method
#output: dataframe df

import pyphysio as ph

sig = ph.EvenlySignal(values=input_signals.iloc[:,0], sampling_freq=sampling_frequency)

sig = ph.Normalize(norm_method=norm_method)(sig)

df = pd.DataFrame({
    'time': range(len(sig)),
    'sig': sig
})