#name: Normalize
#description: Normalized the input signal using the general formula: ( signal - BIAS ) / RANGE
#language: python
#tags: biosignals, filters, pyphysio
#input: dataframe dataframe
#input: column column
#input: double sampling_frequency
#input: string normalization_method {choices: ['mean', 'standard', 'min', 'maxmin', 'custom']} [Method for the normalization]
#output: dataframe df

import pyphysio as ph

sig = ph.EvenlySignal(values=dataframe[column], sampling_freq=sampling_frequency)

sig = ph.Normalize(norm_method=normalization_method)(sig)

df = pd.DataFrame({
    'time': range(len(sig)),
    'sig': sig
})