#name: IIRFilter
#language: python
#input: dataframe input_signals
#input: int sampling_frequency
#input: double fp
#input: double fs
#input: string ftype
#output: dataframe df

import numpy as np
import pyphysio as ph

sig = ph.EvenlySignal(values=np.array(input_signals.iloc[:,0]), sampling_freq=sampling_frequency)

sig = ph.IIRFilter(fp=fp, fs=fs, ftype=ftype)(sig)

df = pd.DataFrame({
    'time': range(len(sig)),
    'sig': sig
})