# ArtifactAlignment changelog

## v.next

* Introduced the artifact-alignment PoC for Compute2 workflow and function runs: the `artifacts` domain schema (programs, studies, compounds, tags, alignment + history), the publish service (frozen-copy deep clone, latest-approved-stays-live versioning over the (program, study, name) key, curation tags/path), per-program audience groups with column-level write gating, the drift check, and the Artifact Catalog Browse app
