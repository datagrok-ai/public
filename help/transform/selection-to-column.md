---
title: "Selection to column"
description: Save the currently selected rows as a new boolean column.
keywords:
  - selection to column
  - save selection as column
  - boolean selection flag
  - persist row selection
  - mark selected rows
---

Saves currently selected rows as a column.

Creates a boolean column with the specified name, where the value is set to true if the row is currently selected. By
default, the name of the column is "Selection".
