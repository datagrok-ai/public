package grok_connect.table_mutation;

/** Marker base for DDL operations: one instanceof for the capability gate, the privilege gate, and the batch refusal. */
public abstract class DdlMutation extends TableMutation {
}
