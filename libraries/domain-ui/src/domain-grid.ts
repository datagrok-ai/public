/**
 * Moved into the platform. {@link DomainGrid} now ships inside `datagrok-api`
 * (`src/ui/domains/domains-grid.ts`) — this module keeps the import path, and
 * the name below is the SAME class `DG.DomainGrid` carries.
 *
 * @module domain-grid
 */

export {DomainGrid} from 'datagrok-api/dg';

export type {DomainGridOptions} from 'datagrok-api/dg';
