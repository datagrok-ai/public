type: script
extends: function
was: Script
description: >
  A function whose body is a script file under a package's scripts/ folder, run by a
  language worker through the call queue.
properties:
  language:      "'python' | 'r' | 'julia' | 'octave' | 'js' | 'grok' | 'other'"
  path:          Path
  environment?:  ScriptEnvironment
  reference?:    Url
  sample?:       Path
  test?:         boolean = false        # has //test: lines
