type: evidences
abstract: true
from: Artifact
to: Feature
derived_by: [annotation, filesystem, llm]
description: Base of the edges through which an artifact vouches for a feature, so "any evidence for this feature" is one query.
properties:
  strength?:     "'weak' | 'normal' | 'strong' = 'normal'"
