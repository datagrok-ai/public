type: editor
extends: function
description: >
  A custom editor for a function's parameters or a value type (roles editor, valueEditor, cellEditor).
properties:
  edits?:        string               # function or type it edits (//editor-for, meta); callers may select it with //editor: instead
rules:
  - roles includes one of [editor, valueEditor, cellEditor]
