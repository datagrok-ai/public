type: sem-type-detector
extends: function
description: >
  A detector in detectors.js (or the Dart registry) that assigns a semantic type to a column.
properties:                            # detected types are targets-semtype edges with role detects
  skip_test?:    boolean = false
notes:
  - roles contains 'semTypeDetector'
