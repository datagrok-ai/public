type: sample
extends: artifact
was: ApiSample
description: >
  A runnable script under packages/ApiSamples/scripts that shows an API in use. Reaches
  the declarations it uses through demonstrates and features through the same edge.
properties:
  path:          Path
  language:      "'js' | 'python' | 'r'"
  folder:        string
  api_members?:  string[]               # from an //api: header or DG./ui./grok. matches
