import {DataFrame} from './dataframe';

/** The visual shell. */
export class Shell {
  /** Shows a message. */
  info(s: string): void {
  }

  error(s: string): void {
  }

  addTableView(table: DataFrame): void {
  }
}
