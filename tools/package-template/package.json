{
  "name": "#{PACKAGE_NAME_LOWERCASE}",
  "fullName": "#{PACKAGE_NAME}",
  "version": "0.0.1",
  "description": "",
  "dependencies": {
    "datagrok-api": "latest"
  },
  "scripts": {
    "debug-#{PACKAGE_NAME_LOWERCASE}": "grok publish --rebuild",
    "deploy-#{PACKAGE_NAME_LOWERCASE}": "grok publish --rebuild --release",
    "build": "webpack"
  }
}
