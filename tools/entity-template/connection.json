{
    "#type": "DataConnection",
    "parameters": {
      "server": "#{GROK_DB_SERVER}",
      "db": "#{NAME}"
    },
    "credentials" : {
      "parameters": {
        "login": "",
        "password": ""
      }
    },
    "dataSource": "PostgreSQL",
    "description": "#{NAME} db",
    "tags": ["demo"]
}
