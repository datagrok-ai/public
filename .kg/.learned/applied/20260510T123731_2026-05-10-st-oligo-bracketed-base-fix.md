feature:SequenceTranslator:oligo-duplex-rendering  IS_IMPLEMENTED_IN  packages/SequenceTranslator/src/oligo-renderer/helm-parser.ts  reason: parseMonomer() now calls cleanupHelmSymbol on the base substring — fixes bracketed non-canonical bases like ([5Br-dC]) not being stripped of brackets before shortening/color lookup. Fixed in commit f5f39b49bc.
feature:SequenceTranslator:oligo-duplex-rendering  IS_IMPLEMENTED_IN  packages/SequenceTranslator/src/oligo-renderer/analog-cache.ts  reason: getNaturalAnalog() is the lookup that bridges non-canonical base symbols → natural analog color; fails silently if base still has brackets (symbol not found in monomer lib).
feature:SequenceTranslator:oligo-duplex-rendering  IS_IMPLEMENTED_IN  packages/SequenceTranslator/src/oligo-renderer/canvas-renderer.ts  reason: pickBaseLabel()/displayBase() apply the shortening logic (>2 chars → first letter + ellipsis); only works after brackets are stripped from base symbol.
feature:SequenceTranslator:oligo-duplex-rendering  IS_TESTED_IN  packages/SequenceTranslator/src/tests/oligo-renderer-tests.ts  reason: 5 new tests added in f5f39b49bc specifically covering bracketed base parsing and canonicalization round-trip.
