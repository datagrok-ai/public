feature:SequenceTranslator:oligo-duplex-rendering  IS_IMPLEMENTED_IN  packages/SequenceTranslator/src/oligo-renderer/helm-parser.ts  reason: parseMonomer() strips brackets from sugar and phosphate but NOT from base codes; e.g. ([5Br-dC]) yields base=[5Br-dC] with brackets still present
feature:SequenceTranslator:oligo-duplex-rendering  IS_IMPLEMENTED_IN  packages/SequenceTranslator/src/oligo-renderer/types.ts  reason: resolveSugar/resolvePhosphate canonicalize but there is no resolveBase; BASE_COLORS lookup fails for bracketed base symbols
func:Bio:cleanupHelmSymbol  IS_IMPLEMENTED_IN  libraries/bio/src/helm/utils.ts  reason: strips [...] brackets from a single HELM monomer symbol; the canonical bracket-stripping utility; used by Bio/src/utils/helm-to-molfile/converter/simple-polymer.ts
feature:SequenceTranslator:oligo-duplex-rendering  DEPENDS_ON  libraries/bio/src/helm/utils.ts  reason: helm-parser.ts should apply cleanupHelmSymbol to the parsed base string just as it already does for sugar/phosphate
