# GENERATED CODE - DO NOT MODIFY BY HAND
# Generated from @ExportApi() annotated server plugins
# See core/server/datlas/docs/code-gen.md
openapi: "3.1.0"
info:
  title: Datagrok API
  version: "1.0.0"
  description: Auto-generated API specification for Datagrok server
servers:
  - url: /api
    description: Datagrok server
paths:
  /domains/schemas:
    get:
      operationId: getSchemas
      summary: get schemas
      tags:
        - domains
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
            format: int64
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            format: int64
        - name: order
          in: query
          required: false
          schema:
            type: string
        - name: include
          in: query
          required: false
          schema:
            type: string
        - name: text
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DomainSchema"
  /domains/grants/{entityId}:
    get:
      operationId: getGrants
      summary: get grants
      description: "Direct permission rows on a registry entity (a domain schema, table, or property schema): [{group: {id, friendlyName, personal}, permission}]. Requires Share on the entity."
      tags:
        - domains
      parameters:
        - name: entityId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Successful response
    post:
      operationId: grantPermission
      summary: grant permission
      description: "Idempotently grants body 'permission' (View|Edit|Delete|Share) on a registry entity to body 'group' (a group id — sharing with a user resolves to their personal group on the client, as everywhere). Requires Share on the entity."
      tags:
        - domains
      parameters:
        - name: entityId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Map"
      responses:
        "200":
          description: Successful response
    delete:
      operationId: revokePermission
      summary: revoke permission
      description: "Revokes 'permission' from 'group' on a registry entity; omitting 'permission' revokes all four common permissions for that group. Requires Share on the entity."
      tags:
        - domains
      parameters:
        - name: entityId
          in: path
          required: true
          schema:
            type: string
        - name: group
          in: query
          required: false
          schema:
            type: string
        - name: permission
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /domains/{schema}/{table}/query:
    post:
      operationId: queryRows
      summary: query rows
      description: "Output format is selected by the spec's 'format': JSON rows (default) or a d42 binary DataFrame (pattern: TablesRouter.getTableData); unknown formats are rejected by the spec parse (400)."
      tags:
        - domains
      parameters:
        - name: schema
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Map"
      responses:
        "200":
          description: Successful response
  /domains/{schema}/{table}/aggregate:
    post:
      operationId: aggregateRows
      summary: aggregate rows
      tags:
        - domains
      parameters:
        - name: schema
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Map"
      responses:
        "200":
          description: Successful response
  /domains/{schema}/{table}/batch:
    post:
      operationId: batchRows
      summary: batch rows
      description: "Bulk upload (§5.6); the body format is selected by Content-Type: application/json (row-object array, bare or under 'rows'), text/csv, or application/octet-stream (d42 DataFrame). No server-side Parquet — see PLAN.md phase-2 decision 1 (clients convert via the Arrow package). The generated dapi2 client method is a non-functional stub (it sends no body): use grok.dapi.domains.table(...).batch (JS) or raw HTTP with a JSON/CSV/d42 body instead."
      tags:
        - domains
      parameters:
        - name: schema
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /domains/{schema}/transaction:
    post:
      operationId: runTransaction
      summary: run transaction
      description: "Ordered per-op results, shaped like the single-op endpoints; any failure rolls the whole transaction back and the error body carries 'opIndex'. [ops] is the ops list; a {'ops': [...]} wrapper is also accepted over the wire (`@RequestBody() dynamic`, not `List` — see insertRows)."
      tags:
        - domains
      parameters:
        - name: schema
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              {}
      responses:
        "200":
          description: Successful response
  /domains/{schema}/{table}:
    post:
      operationId: insertRows
      summary: insert rows
      description: "[rows] is a single row map or a list of row maps. The body is `@RequestBody() dynamic`, not `Map` — a checked-mode VM enforces handler annotations on the mirror invoke, and the wire accepts both shapes (pinned by domain_rest_test)."
      tags:
        - domains
      parameters:
        - name: schema
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              {}
      responses:
        "200":
          description: Successful response
  /domains/{schema}/{table}/{id}:
    get:
      operationId: getRow
      summary: get row
      tags:
        - domains
      parameters:
        - name: schema
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Successful response
    delete:
      operationId: deleteRow
      summary: delete row
      tags:
        - domains
      parameters:
        - name: schema
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Successful response
    patch:
      operationId: patchRow
      summary: patch row
      tags:
        - domains
      parameters:
        - name: schema
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Map"
      responses:
        "200":
          description: Successful response
  /domains/{schema}/{table}/{id}/promote:
    post:
      operationId: promoteRow
      summary: promote row
      tags:
        - domains
      parameters:
        - name: schema
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /domains/{schema}/{table}/{id}/audit:
    get:
      operationId: rowAudit
      summary: row audit
      tags:
        - domains
      parameters:
        - name: schema
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Successful response
components:
  schemas:
    DomainSchema:
      type: object
      description: DomainSchema entity
    Map:
      type: object
      description: Map entity
